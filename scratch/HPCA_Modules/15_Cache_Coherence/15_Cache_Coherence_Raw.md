# 15_Cache_Coherence (Raw Transcripts)

# Playlist 4 Module 6 Raw Transcript

## Cache Incoherence Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our cache let's look at the solution to our cache let's look at the solution to our cache incoherence quiz we had three caches and incoherence quiz we had three caches and incoherence quiz we had three caches and three cores a location that starts at three cores a location that starts at three cores a location that starts at zero and then each core reads increments zero and then each core reads increments zero and then each core reads increments and then writes to that memory location and then writes to that memory location and then writes to that memory location so the question is what's the final so the question is what's the final so the question is what's the final value we write here the correct behavior value we write here the correct behavior value we write here the correct behavior would be four cores zero to do this and would be four cores zero to do this and would be four cores zero to do this and then it replaces this from its cache so then it replaces this from its cache so then it replaces this from its cache so that core 1 reads a 1 here increments that core 1 reads a 1 here increments that core 1 reads a 1 here increments that writes a 2 and then core 2 reads that writes a 2 and then core 2 reads that writes a 2 and then core 2 reads that increments and writes a 3 so this 3 that increments and writes a 3 so this 3 that increments and writes a 3 so this 3 here is a possible answer if core 0 here here is a possible answer if core 0 here here is a possible answer if core 0 here happens to replace this block that happens to replace this block that happens to replace this block that contains a from its cash before core 1 contains a from its cash before core 1 contains a from its cash before core 1 tries to read it and then core 1 tries to read it and then core 1 tries to read it and then core 1 replaces before core to reads it and so replaces before core to reads it and so replaces before core to reads it and so on in that case we get actually a 3 here on in that case we get actually a 3 here on in that case we get actually a 3 here which is the correct answer so let's see which is the correct answer so let's see which is the correct answer so let's see now if we can get to well if core 0 does now if we can get to well if core 0 does now if we can get to well if core 0 does a replacement so the memory location a replacement so the memory location a replacement so the memory location after this actually contains a 1 then after this actually contains a 1 then after this actually contains a 1 then core 1 does this but doesn't replace the core 1 does this but doesn't replace the core 1 does this but doesn't replace the block from the cache then core to will block from the cache then core to will block from the cache then core to will still see a 1 it will not see what core still see a 1 it will not see what core still see a 1 it will not see what core 1 produced because core one keeps it in 1 produced because core one keeps it in 1 produced because core one keeps it in its own cache so core to here reads 1 its own cache so core to here reads 1 its own cache so core to here reads 1 increments it and then core 2 ends up increments it and then core 2 ends up increments it and then core 2 ends up writing a 2 so this is a possible answer writing a 2 so this is a possible answer writing a 2 so this is a possible answer and then we can get a 1 here by and then we can get a 1 here by and then we can get a 1 here by basically core 0 delaying the basically core 0 delaying the basically core 0 delaying the replacement and then core 1 can fetch a replacement and then core 1 can fetch a replacement and then core 1 can fetch a 0 as well put a 1 there but not replace 0 as well put a 1 there but not replace 0 as well put a 1 there but not replace until later and that means that when until later and that means that when until later and that means that when core 2 reads this it gets a 0 and then core 2 reads this it gets a 0 and then core 2 reads this it gets a 0 and then puts a 1 here so this is also possible puts a 1 here so this is also possible puts a 1 here so this is also possible it is not possible for core 2 to write a it is not possible for core 2 to write a it is not possible for core 2 to write a 0 because whatever it reads it will 0 because whatever it reads it will 0 because whatever it reads it will increment and write increment and write increment and write so for Core 2 to produce a 0 here it so for Core 2 to produce a 0 here it so for Core 2 to produce a 0 here it would need to read minus 1 note that would need to read minus 1 note that would need to read minus 1 note that each of these cores is still behaving as each of these cores is still behaving as each of these cores is still behaving as a correct uniprocessor so 0 is not a correct uniprocessor so 0 is not a correct uniprocessor so 0 is not possible possible possible similarly for is not possible because similarly for is not possible because similarly for is not possible because for 4 to be produced here we need to for 4 to be produced here we need to for 4 to be produced here we need to read a tree and there is just no way read a tree and there is just no way read a tree and there is just no way that to increase that to increase that to increase from zero can produce a three here and from zero can produce a three here and from zero can produce a three here and similarly more than four is not possible similarly more than four is not possible similarly more than four is not possible so the correct answers here are 1 2 &amp; 3 so the correct answers here are 1 2 &amp; 3 so the correct answers here are 1 2 &amp; 3 are possible at this point

## Coherence Definition： Part 5
Kind: captions Language: en so let's now Define what does it mean so let's now Define what does it mean so let's now Define what does it mean for caches to be coherent intuitively for caches to be coherent intuitively for caches to be coherent intuitively they're coherent when the whole thing they're coherent when the whole thing they're coherent when the whole thing behaves like there was no caches but behaves like there was no caches but behaves like there was no caches but let's define more precisely what exactly let's define more precisely what exactly let's define more precisely what exactly does it mean there are three does it mean there are three does it mean there are three requirements for coherence the first one requirements for coherence the first one requirements for coherence the first one says that the read let's call that read says that the read let's call that read says that the read let's call that read R from some address X on some core C1 R from some address X on some core C1 R from some address X on some core C1 will return the value written by the will return the value written by the will return the value written by the most recent WR let's call that right W most recent WR let's call that right W most recent WR let's call that right W to the same location and that right was to the same location and that right was to the same location and that right was on the same core C1 if no other core has on the same core C1 if no other core has on the same core C1 if no other core has written to that location between the written to that location between the written to that location between the axises W and R so this first part of the axises W and R so this first part of the axises W and R so this first part of the coherence definition really says that if coherence definition really says that if coherence definition really says that if one core is operating on a location one core is operating on a location one core is operating on a location alone then its reads should be getting alone then its reads should be getting alone then its reads should be getting the most recent rides from the same core the most recent rides from the same core the most recent rides from the same core this basically says that a coherent this basically says that a coherent this basically says that a coherent Behavior includes correct unit processor Behavior includes correct unit processor Behavior includes correct unit processor Behavior as long as the location is Behavior as long as the location is Behavior as long as the location is accessed by only one core the second accessed by only one core the second accessed by only one core the second part of the definition says that if core part of the definition says that if core part of the definition says that if core C1 wres to allocation X and C2 reads X C1 wres to allocation X and C2 reads X C1 wres to allocation X and C2 reads X after a sufficient time and if there are after a sufficient time and if there are after a sufficient time and if there are no other rights that happen in between no other rights that happen in between no other rights that happen in between c1s right and c2's read then c2's read c1s right and c2's read then c2's read c1s right and c2's read then c2's read must return the value C1 wrote so pretty must return the value C1 wrote so pretty must return the value C1 wrote so pretty much if there is a right and the read much if there is a right and the read much if there is a right and the read happens sufficiently after that right happens sufficiently after that right happens sufficiently after that right then the read has to read what the right then the read has to read what the right then the read has to read what the right wrote except if there was another right wrote except if there was another right wrote except if there was another right in between in which case the read is in between in which case the read is in between in which case the read is allowed to return that value instead of allowed to return that value instead of allowed to return that value instead of this one so this part of the definition this one so this part of the definition this one so this part of the definition really prevents the case when for really prevents the case when for really prevents the case when for example C1 writes something to its own example C1 writes something to its own example C1 writes something to its own cache C2 reads forever from its cache a cache C2 reads forever from its cache a cache C2 reads forever from its cache a stale value to be stale value to be stale value to be coherent if C1 wres then after some coherent if C1 wres then after some coherent if C1 wres then after some sufficient time C2 must start reading sufficient time C2 must start reading sufficient time C2 must start reading the new value even if maybe a normal the new value even if maybe a normal the new value even if maybe a normal cache with no coherence support would cache with no coherence support would cache with no coherence support would just keep reading the stale value just keep reading the stale value just keep reading the stale value because it never gets replaced so we because it never gets replaced so we because it never gets replaced so we have to do something in order to make have to do something in order to make have to do something in order to make this part of the definition work for this part of the definition work for this part of the definition work for private caches that are caching data in private caches that are caching data in private caches that are caching data in a coherent system so we must do a coherent system so we must do a coherent system so we must do something to make these caches behave something to make these caches behave something to make these caches behave this way and finally the third part of this way and finally the third part of this way and finally the third part of the the definition says that if there is the the definition says that if there is the the definition says that if there is more than one right to the same location more than one right to the same location more than one right to the same location then these rights must be serialized then these rights must be serialized then these rights must be serialized that is any two rights to the same that is any two rights to the same that is any two rights to the same location must be seen to occur in the location must be seen to occur in the location must be seen to occur in the same order on all the cores what this same order on all the cores what this same order on all the cores what this part of the definition really says part of the definition really says part of the definition really says different cores should not be able to different cores should not be able to different cores should not be able to disagree about which right occurred disagree about which right occurred disagree about which right occurred first and which occurred second note first and which occurred second note first and which occurred second note that the second part of the definition that the second part of the definition that the second part of the definition doesn't really require this to happen doesn't really require this to happen doesn't really require this to happen let's say there was one right on C1 and let's say there was one right on C1 and let's say there was one right on C1 and another right on C3 when C2 reads it's another right on C3 when C2 reads it's another right on C3 when C2 reads it's allowed to return after a sufficient allowed to return after a sufficient allowed to return after a sufficient time either c1s or c3's value because time either c1s or c3's value because time either c1s or c3's value because it's allowed to interpret this order as it's allowed to interpret this order as it's allowed to interpret this order as either C1 and then C3 wrote or the other either C1 and then C3 wrote or the other either C1 and then C3 wrote or the other way around what this part of the way around what this part of the way around what this part of the definition says is that whatever C2 sees definition says is that whatever C2 sees definition says is that whatever C2 sees if C2 for example CES that the final if C2 for example CES that the final if C2 for example CES that the final value is that from C1 and not from C3 value is that from C1 and not from C3 value is that from C1 and not from C3 then all the other cores when they read then all the other cores when they read then all the other cores when they read that location after C2 need to also see that location after C2 need to also see that location after C2 need to also see that order so it cannot happen that C2 that order so it cannot happen that C2 that order so it cannot happen that C2 thinks C1 wrote last some other core thinks C1 wrote last some other core thinks C1 wrote last some other core sometime later reads and still sees c3's sometime later reads and still sees c3's sometime later reads and still sees c3's value so what this is really saying is value so what this is really saying is value so what this is really saying is that everybody including the writing that everybody including the writing that everybody including the writing course will see the value that is course will see the value that is course will see the value that is consistent with their being some consistent with their being some consistent with their being some particular order to the rights it particular order to the rights it particular order to the rights it doesn't require any particular order for doesn't require any particular order for doesn't require any particular order for example the rights can happen almost example the rights can happen almost example the rights can happen almost simultaneously in which case we are free simultaneously in which case we are free simultaneously in which case we are free to choose which happen first and which to choose which happen first and which to choose which happen first and which happen second but we cannot allow some happen second but we cannot allow some happen second but we cannot allow some cores to think that one right was first cores to think that one right was first cores to think that one right was first and other cores think that another right and other cores think that another right and other cores think that another right was first

## Coherence Definition Quiz： Part 5
Kind: captions Language: en so let's see if we understood the so let's see if we understood the so let's see if we understood the coherence definition coherence definition coherence definition let's say that we have two cores in a let's say that we have two cores in a let's say that we have two cores in a system that is supposed to be coherent system that is supposed to be coherent system that is supposed to be coherent let's say that core one executes this let's say that core one executes this let's say that core one executes this program program program it sets a to one it sets a to one it sets a to one then waits for a to change from 1 so then waits for a to change from 1 so then waits for a to change from 1 so it's going to loop here as long as a is it's going to loop here as long as a is it's going to loop here as long as a is equal to 1 sets a again to 1 and then equal to 1 sets a again to 1 and then equal to 1 sets a again to 1 and then prints done 1. at the same time when prints done 1. at the same time when prints done 1. at the same time when core 1 is started on this core 1 is started on this core 1 is started on this core 2 starts on another program that core 2 starts on another program that core 2 starts on another program that writes 0 to the same location a writes 0 to the same location a writes 0 to the same location a in shared memory waits until a changes in shared memory waits until a changes in shared memory waits until a changes from 0 from 0 from 0 sets a to 0 again sets a to 0 again sets a to 0 again then prints down 2 and remember that then prints down 2 and remember that then prints down 2 and remember that core one and core two core one and core two core one and core two need not necessarily execute these two need not necessarily execute these two need not necessarily execute these two programs in lock steps so it's not programs in lock steps so it's not programs in lock steps so it's not necessary that core one executes this necessary that core one executes this necessary that core one executes this while core two does this core one does while core two does this core one does while core two does this core one does this while core to this and so on it this while core to this and so on it this while core to this and so on it could be for example that core runs could be for example that core runs could be for example that core runs executes several of these while core 2 executes several of these while core 2 executes several of these while core 2 is still is still is still not even beginning not even beginning not even beginning so you can insert arbitrary delays in so you can insert arbitrary delays in so you can insert arbitrary delays in between any of the statements here and between any of the statements here and between any of the statements here and here the question for you is which of here the question for you is which of here the question for you is which of the following printouts are possible the following printouts are possible the following printouts are possible first core one prints done one then core first core one prints done one then core first core one prints done one then core two prints down two two prints down two two prints down two the next possibility first core two the next possibility first core two the next possibility first core two prints down two then core one prints prints down two then core one prints prints down two then core one prints done one the next option core one prints done one the next option core one prints done one the next option core one prints down one down one down one but core two but core two but core two gets stuck and never gets to print done gets stuck and never gets to print done gets stuck and never gets to print done two two two the next option the next option the next option is score one gets stuck and never gets is score one gets stuck and never gets is score one gets stuck and never gets to print down one but core two to print down one but core two to print down one but core two successfully prints done too successfully prints done too successfully prints done too and the final option and the final option and the final option is that nothing is printed because both is that nothing is printed because both is that nothing is printed because both of the cores get stuck so neither of of the cores get stuck so neither of of the cores get stuck so neither of them prints anything ever so which of them prints anything ever so which of them prints anything ever so which of these five these five these five options options options are actually possible are actually possible are actually possible when these two programs when these two programs when these two programs run in a coherent system

## Coherence Definition Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our coherence definition quiz where we had coherence definition quiz where we had coherence definition quiz where we had two cores running in a coherent system two cores running in a coherent system two cores running in a coherent system executing some code and the question was executing some code and the question was executing some code and the question was basically which of these five basically which of these five basically which of these five possibilities for what gets printed are possibilities for what gets printed are possibilities for what gets printed are possible let's see what can happen in possible let's see what can happen in possible let's see what can happen in these two programs if core one gets to these two programs if core one gets to these two programs if core one gets to go first with a equals one it's then go first with a equals one it's then go first with a equals one it's then going to spin here for as long as X going to spin here for as long as X going to spin here for as long as X equal one so it gets stuck until Core 2 equal one so it gets stuck until Core 2 equal one so it gets stuck until Core 2 does something eventually however core 2 does something eventually however core 2 does something eventually however core 2 we'll do a equals 0 in which case core 1 we'll do a equals 0 in which case core 1 we'll do a equals 0 in which case core 1 gets to continue and now core 1 we'll gets to continue and now core 1 we'll gets to continue and now core 1 we'll set a to 1 korto will wait here until a set a to 1 korto will wait here until a set a to 1 korto will wait here until a is set to 1 because as long as it's set is set to 1 because as long as it's set is set to 1 because as long as it's set to 0 the way it set it is going to get to 0 the way it set it is going to get to 0 the way it set it is going to get stuck here stuck here stuck here so what then happens is core 1 when it so what then happens is core 1 when it so what then happens is core 1 when it sets this release is called - now core 2 sets this release is called - now core 2 sets this release is called - now core 2 can do this and this first while core 1 can do this and this first while core 1 can do this and this first while core 1 is still being delayed here or it can be is still being delayed here or it can be is still being delayed here or it can be that core 1 gets here first and then that core 1 gets here first and then that core 1 gets here first and then core 2 gets here so definitely these two core 2 gets here so definitely these two core 2 gets here so definitely these two outcomes are possible outcomes are possible outcomes are possible they basically depend on after core 1 they basically depend on after core 1 they basically depend on after core 1 waits for Core 2 to release it and then waits for Core 2 to release it and then waits for Core 2 to release it and then it itself releases core to who gets to it itself releases core to who gets to it itself releases core to who gets to print first now let's see if it's print first now let's see if it's print first now let's see if it's possible for core 1 to never get to this possible for core 1 to never get to this possible for core 1 to never get to this point for that to happen core one needs point for that to happen core one needs point for that to happen core one needs to get stuck here in this while loop to get stuck here in this while loop to get stuck here in this while loop which means that a needs to stay 1 which means that a needs to stay 1 which means that a needs to stay 1 forever that can only occur if core 2 is forever that can only occur if core 2 is forever that can only occur if core 2 is done with its program and the resulting done with its program and the resulting done with its program and the resulting value of a is still one or if core to value of a is still one or if core to value of a is still one or if core to also get stuck here and the resulting also get stuck here and the resulting also get stuck here and the resulting value of a is still one so core 1 is value of a is still one so core 1 is value of a is still one so core 1 is stuck here and a is 1 if that happens stuck here and a is 1 if that happens stuck here and a is 1 if that happens before core 2 gets to a equals 0 then before core 2 gets to a equals 0 then before core 2 gets to a equals 0 then core 2 is gonna release core 1 so a core 2 is gonna release core 1 so a core 2 is gonna release core 1 so a equals 0 had to happen before this if a equals 0 had to happen before this if a equals 0 had to happen before this if a equals 0 happened before all of this equals 0 happened before all of this equals 0 happened before all of this then what happens is a equals 0 equals 1 then what happens is a equals 0 equals 1 then what happens is a equals 0 equals 1 now we're stuck waiting for 1 but core 2 now we're stuck waiting for 1 but core 2 now we're stuck waiting for 1 but core 2 will continue and it has will continue and it has will continue and it has to see the one because this is a to see the one because this is a to see the one because this is a coherent system eventually because coherent system eventually because coherent system eventually because equals one happened after equals zero equals one happened after equals zero equals one happened after equals zero Core 2 has to see the 1 and proceed once Core 2 has to see the 1 and proceed once Core 2 has to see the 1 and proceed once it proceeds is going to set a 2-0 core it proceeds is going to set a 2-0 core it proceeds is going to set a 2-0 core one has to see that because of the one has to see that because of the one has to see that because of the second part of the definition and second part of the definition and second part of the definition and proceed so it's not possible to get core proceed so it's not possible to get core proceed so it's not possible to get core 1 stuck here by just moving the a equals 1 stuck here by just moving the a equals 1 stuck here by just moving the a equals 0 to the beginning now let's see if it's 0 to the beginning now let's see if it's 0 to the beginning now let's see if it's possible for Core 2 to finish the whole possible for Core 2 to finish the whole possible for Core 2 to finish the whole thing before this happens in which case thing before this happens in which case thing before this happens in which case core 1 will get stuck here but that's core 1 will get stuck here but that's core 1 will get stuck here but that's not possible because core 2 now needs to not possible because core 2 now needs to not possible because core 2 now needs to execute this whole program here before execute this whole program here before execute this whole program here before core 1 even begins that's not possible core 1 even begins that's not possible core 1 even begins that's not possible because core 2 sets a to 0 and then because core 2 sets a to 0 and then because core 2 sets a to 0 and then waits for it to become nonzero so core waits for it to become nonzero so core waits for it to become nonzero so core to cannot finish the whole thing until to cannot finish the whole thing until to cannot finish the whole thing until core 1 does at least this at which point core 1 does at least this at which point core 1 does at least this at which point we are back to the first case of pretty we are back to the first case of pretty we are back to the first case of pretty much core 1 will eventually see that the much core 1 will eventually see that the much core 1 will eventually see that the a is 0 because core to set it that way a is 0 because core to set it that way a is 0 because core to set it that way after a became 1 after a became 1 after a became 1 it's also important to notice that it's it's also important to notice that it's it's also important to notice that it's not possible for for example equals 1 not possible for for example equals 1 not possible for for example equals 1 and a equals 2 to happen one after the and a equals 2 to happen one after the and a equals 2 to happen one after the other and then core one thinks that this other and then core one thinks that this other and then core one thinks that this was the last right so it gets stuck here was the last right so it gets stuck here was the last right so it gets stuck here while core 2 thinks this was the last while core 2 thinks this was the last while core 2 thinks this was the last right and then gets stuck here because right and then gets stuck here because right and then gets stuck here because the third part of the coherence the third part of the coherence the third part of the coherence definition says that both of them have definition says that both of them have definition says that both of them have to think that the same axis was the last to think that the same axis was the last to think that the same axis was the last one so if they agree that this was the one so if they agree that this was the one so if they agree that this was the last one then this core gets released last one then this core gets released last one then this core gets released and then ends up releasing the first one and then ends up releasing the first one and then ends up releasing the first one if they think this was the last access if they think this was the last access if they think this was the last access then this core gets to be released and then this core gets to be released and then this core gets to be released and releases the second one so either way releases the second one so either way releases the second one so either way it's not possible to get any of the 3 it's not possible to get any of the 3 it's not possible to get any of the 3 printouts in an incoherent system it can printouts in an incoherent system it can printouts in an incoherent system it can easily happen that you get these or any easily happen that you get these or any easily happen that you get these or any of these other three so for example of these other three so for example of these other three so for example nothing can be printed then by simply nothing can be printed then by simply nothing can be printed then by simply storing one to the cache and then never storing one to the cache and then never storing one to the cache and then never really checking anything else just really checking anything else just really checking anything else just looping on the value in the cache here looping on the value in the cache here looping on the value in the cache here meanwhile this one stores another value meanwhile this one stores another value meanwhile this one stores another value in its own cache then just infinitely in its own cache then just infinitely in its own cache then just infinitely space space space on it and then you can get the other two on it and then you can get the other two on it and then you can get the other two possibilities here by simply one of them possibilities here by simply one of them possibilities here by simply one of them not being entirely incoherent so as you not being entirely incoherent so as you not being entirely incoherent so as you can see a coherent system prevents some can see a coherent system prevents some can see a coherent system prevents some of the behaviors that are possible of the behaviors that are possible of the behaviors that are possible in an incoherent system but all the in an incoherent system but all the in an incoherent system but all the behaviors that are possible in the behaviors that are possible in the behaviors that are possible in the coherent system are also possible in an coherent system are also possible in an coherent system are also possible in an incoherent one so coherence is allowed incoherent one so coherence is allowed incoherent one so coherence is allowed in an incoherent system is just that in an incoherent system is just that in an incoherent system is just that incoherent systems don't guarantee that incoherent systems don't guarantee that incoherent systems don't guarantee that all their behaviors are going to be all their behaviors are going to be all their behaviors are going to be coherent

## How to Get Coherence： Part 5
Kind: captions Language: en okay so we have seen that we want okay so we have seen that we want okay so we have seen that we want coherence and now the question is how do coherence and now the question is how do coherence and now the question is how do we get it so how do we make caches we get it so how do we make caches we get it so how do we make caches become coherent the first option is become coherent the first option is become coherent the first option is don't do caching but this is really bad don't do caching but this is really bad don't do caching but this is really bad for performance so we will not seriously for performance so we will not seriously for performance so we will not seriously consider this but if there are no caches consider this but if there are no caches consider this but if there are no caches then all of the axises go to memory and then all of the axises go to memory and then all of the axises go to memory and then rights to that memory will be seen then rights to that memory will be seen then rights to that memory will be seen by the reads to that memory and also the by the reads to that memory and also the by the reads to that memory and also the order in which the memory receives the order in which the memory receives the order in which the memory receives the rights is the order in which the rights rights is the order in which the rights rights is the order in which the rights will be seen by all the court so the will be seen by all the court so the will be seen by all the court so the system will be coherent the second system will be coherent the second system will be coherent the second option is if all cores share the same option is if all cores share the same option is if all cores share the same level one cache then the same argument level one cache then the same argument level one cache then the same argument for coherence supplies the order of for coherence supplies the order of for coherence supplies the order of Rights is the order seen by this level Rights is the order seen by this level Rights is the order seen by this level one one one cache and all of the accesses are going cache and all of the accesses are going cache and all of the accesses are going to go to the same cache where they see to go to the same cache where they see to go to the same cache where they see the same version of the block so the the same version of the block so the the same version of the block so the system will be coherent but will still system will be coherent but will still system will be coherent but will still have really bad performance better than have really bad performance better than have really bad performance better than this but not by much now we can try to this but not by much now we can try to this but not by much now we can try to do things like having private caches but do things like having private caches but do things like having private caches but make them right through in that case the make them right through in that case the make them right through in that case the memory is actually seeing all the rights memory is actually seeing all the rights memory is actually seeing all the rights but the problem is that these caches are but the problem is that these caches are but the problem is that these caches are still allowed to cache data that has still allowed to cache data that has still allowed to cache data that has only been read forever so once we read a only been read forever so once we read a only been read forever so once we read a value it doesn't matter that all the value it doesn't matter that all the value it doesn't matter that all the other cores are writing their values other cores are writing their values other cores are writing their values through to memory because the cache that through to memory because the cache that through to memory because the cache that only reads will potentially see forever only reads will potentially see forever only reads will potentially see forever the same stale value even after many the same stale value even after many the same stale value even after many many rights have occurred and for a long many rights have occurred and for a long many rights have occurred and for a long time so this is really not a way to get time so this is really not a way to get time so this is really not a way to get coherence even with write through caches coherence even with write through caches coherence even with write through caches private caches are not necessarily private caches are not necessarily private caches are not necessarily coherent what we must do really is to coherent what we must do really is to coherent what we must do really is to force reads made to one cash to see the force reads made to one cash to see the force reads made to one cash to see the rights made in another so we cannot rights made in another so we cannot rights made in another so we cannot allow cashes to Simply provide the same allow cashes to Simply provide the same allow cashes to Simply provide the same value over and over once it's a cash hit value over and over once it's a cash hit value over and over once it's a cash hit somehow once a right occurs in one cash somehow once a right occurs in one cash somehow once a right occurs in one cash another cash needs to eventually start another cash needs to eventually start another cash needs to eventually start returning that value to its local returning that value to its local returning that value to its local processor so how do we do that one way processor so how do we do that one way processor so how do we do that one way is for every right to be sent to all the is for every right to be sent to all the is for every right to be sent to all the other caches that might potentially have other caches that might potentially have other caches that might potentially have this block and they need to update their this block and they need to update their this block and they need to update their value so pretty much I write to my cach value so pretty much I write to my cach value so pretty much I write to my cach it's kind of like a write through cach it's kind of like a write through cach it's kind of like a write through cach in that it continues past the cache but in that it continues past the cache but in that it continues past the cache but now what really happens to it is not now what really happens to it is not now what really happens to it is not only does it get written back to memory only does it get written back to memory only does it get written back to memory it also get sent and seen by all the it also get sent and seen by all the it also get sent and seen by all the other caches which now take that new other caches which now take that new other caches which now take that new value if that that happens then when I value if that that happens then when I value if that that happens then when I write other caches now have my new value write other caches now have my new value write other caches now have my new value and then reads on those scores will and then reads on those scores will and then reads on those scores will actually provide the same value this is actually provide the same value this is actually provide the same value this is called Write update coherence because called Write update coherence because called Write update coherence because wres update all of the other cach copies wres update all of the other cach copies wres update all of the other cach copies the other approach for maintaining the other approach for maintaining the other approach for maintaining coherence is for rights to coherence is for rights to coherence is for rights to prevent other copies of the block from prevent other copies of the block from prevent other copies of the block from having hits so after a write to one having hits so after a write to one having hits so after a write to one cache all of the caches that had a copy cache all of the caches that had a copy cache all of the caches that had a copy of this block need need to behave like of this block need need to behave like of this block need need to behave like they're having a miss or they need to they're having a miss or they need to they're having a miss or they need to behave like the block is not valid in behave like the block is not valid in behave like the block is not valid in that cache so this is called Write that cache so this is called Write that cache so this is called Write invalidate a write will invalidate all invalidate a write will invalidate all invalidate a write will invalidate all the other copies note that these are the other copies note that these are the other copies note that these are just the two general approaches we will just the two general approaches we will just the two general approaches we will be looking at several right invalidate be looking at several right invalidate be looking at several right invalidate coherence protocols and also we will be coherence protocols and also we will be coherence protocols and also we will be considering a right update protocol so considering a right update protocol so considering a right update protocol so this ensures really that reads see the this ensures really that reads see the this ensures really that reads see the values produced by wres values produced by wres values produced by wres so the coherence property two is so the coherence property two is so the coherence property two is maintained by either this or this so we maintained by either this or this so we maintained by either this or this so we need to pick one of these two approaches need to pick one of these two approaches need to pick one of these two approaches to maintain coherence property two for to maintain coherence property two for to maintain coherence property two for coherence property 3 we need to decide coherence property 3 we need to decide coherence property 3 we need to decide what's the order of Rights and all the what's the order of Rights and all the what's the order of Rights and all the cores need to see the same order one way cores need to see the same order one way cores need to see the same order one way to enforce the same order of Rights to enforce the same order of Rights to enforce the same order of Rights being seen by all the cores is that all being seen by all the cores is that all being seen by all the cores is that all rights are broadcast on the shared bus rights are broadcast on the shared bus rights are broadcast on the shared bus and then the order in which they appear and then the order in which they appear and then the order in which they appear in this shared bus is the order that in this shared bus is the order that in this shared bus is the order that should be seen by everybody coherence should be seen by everybody coherence should be seen by everybody coherence done in this way is called snooping done in this way is called snooping done in this way is called snooping because when rights are broadcast on the because when rights are broadcast on the because when rights are broadcast on the bus everybody snoops those rights in bus everybody snoops those rights in bus everybody snoops those rights in order to tell what order they occurred order to tell what order they occurred order to tell what order they occurred on and also to update or invalidate on and also to update or invalidate on and also to update or invalidate their values in the caches of course the their values in the caches of course the their values in the caches of course the shared bus can now become a bottleneck shared bus can now become a bottleneck shared bus can now become a bottleneck the other approach is that each block is the other approach is that each block is the other approach is that each block is assigned an ordering point and different assigned an ordering point and different assigned an ordering point and different ordering points can be used for ordering points can be used for ordering points can be used for different blocks so that for each block different blocks so that for each block different blocks so that for each block all axess Es are ordered by the same all axess Es are ordered by the same all axess Es are ordered by the same entity but different blocks use entity but different blocks use entity but different blocks use different entities so they don't have different entities so they don't have different entities so they don't have contention this ordering point is called contention this ordering point is called contention this ordering point is called directory when doing coherence so this directory when doing coherence so this directory when doing coherence so this family of approaches is called a family of approaches is called a family of approaches is called a directory based coherence and this directory based coherence and this directory based coherence and this directory both serves as a ordering directory both serves as a ordering directory both serves as a ordering point for a block and also figures out point for a block and also figures out point for a block and also figures out who to update or invalidate when who to update or invalidate when who to update or invalidate when multiple caches have a version of the multiple caches have a version of the multiple caches have a version of the same block and a right occurs so among same block and a right occurs so among same block and a right occurs so among these two approaches we can also pick these two approaches we can also pick these two approaches we can also pick one and indeed all four possibilities one and indeed all four possibilities one and indeed all four possibilities this and this this and this this and this and this this and this this and this and this this and this this and this and this and that are all possible this and this and that are all possible this and this and that are all possible so you can have WR update with snooping so you can have WR update with snooping so you can have WR update with snooping WR update with directory WR inv validate WR update with directory WR inv validate WR update with directory WR inv validate with snooping right inv validate with with snooping right inv validate with with snooping right inv validate with directory

## Write Update Snooping Coherence： Part 5
Kind: captions Language: en So let's look at the right update So let's look at the right update So let's look at the right update snooping coherence first. Let's look at snooping coherence first. Let's look at snooping coherence first. Let's look at two caches only each of which has two two caches only each of which has two two caches only each of which has two blocks with a single valid bit per block blocks with a single valid bit per block blocks with a single valid bit per block and a tag and then the data and both of and a tag and then the data and both of and a tag and then the data and both of these caches are connected to the same these caches are connected to the same these caches are connected to the same bus to which we also connect the memory. bus to which we also connect the memory. bus to which we also connect the memory. So now let's say that the caches are So now let's say that the caches are So now let's say that the caches are initially empty. So the valid bits are initially empty. So the valid bits are initially empty. So the valid bits are all zero. And this processor reads all zero. And this processor reads all zero. And this processor reads something in block A. We have a cache something in block A. We have a cache something in block A. We have a cache miss here. We send a request to memory. miss here. We send a request to memory. miss here. We send a request to memory. This cache is always monitoring what's This cache is always monitoring what's This cache is always monitoring what's going on on the bus, but it's only going on on the bus, but it's only going on on the bus, but it's only interested in writes. So this read is interested in writes. So this read is interested in writes. So this read is really going to be seen and ignored by really going to be seen and ignored by really going to be seen and ignored by this cache. The data for A comes back this cache. The data for A comes back this cache. The data for A comes back from memory. And now we put this block from memory. And now we put this block from memory. And now we put this block in the cache. The block becomes valid. in the cache. The block becomes valid. in the cache. The block becomes valid. And the tag, let's say, just indicates And the tag, let's say, just indicates And the tag, let's say, just indicates that this is the block A. Now let's say that this is the block A. Now let's say that this is the block A. Now let's say that this processor writes to A. So now that this processor writes to A. So now that this processor writes to A. So now we have a situation where this processor we have a situation where this processor we have a situation where this processor even if it does a right through access even if it does a right through access even if it does a right through access directly to memory a read on this directly to memory a read on this directly to memory a read on this processor would still read the stale processor would still read the stale processor would still read the stale value of a. Let's say that the value we value of a. Let's say that the value we value of a. Let's say that the value we read here was zero. Now let's say that read here was zero. Now let's say that read here was zero. Now let's say that this processor writes one to a. this this processor writes one to a. this this processor writes one to a. this right even if we made it go all the way right even if we made it go all the way right even if we made it go all the way to memory would not result in coherent to memory would not result in coherent to memory would not result in coherent behavior for this cache because a behavior for this cache because a behavior for this cache because a subsequent read here would still hit in subsequent read here would still hit in subsequent read here would still hit in the cache and get a zero. So this is the cache and get a zero. So this is the cache and get a zero. So this is when write update and snooping come into when write update and snooping come into when write update and snooping come into the picture. We have a cache miss but the picture. We have a cache miss but the picture. We have a cache miss but because it's a write we send a request because it's a write we send a request because it's a write we send a request for the block along with an indication for the block along with an indication for the block along with an indication that it's a right and what is the value that it's a right and what is the value that it's a right and what is the value and what's the address. So now what and what's the address. So now what and what's the address. So now what happens is this request goes to memory happens is this request goes to memory happens is this request goes to memory and the memory writes one to A. But more and the memory writes one to A. But more and the memory writes one to A. But more importantly this cache is monitoring the importantly this cache is monitoring the importantly this cache is monitoring the bus and sees that block A is being bus and sees that block A is being bus and sees that block A is being updated. It checks its internal state updated. It checks its internal state updated. It checks its internal state and sees that it has block A and then it and sees that it has block A and then it and sees that it has block A and then it takes the value that is being written. takes the value that is being written. takes the value that is being written. So this part is the snooping part where So this part is the snooping part where So this part is the snooping part where we see what's going on on the bus. So we we see what's going on on the bus. So we we see what's going on on the bus. So we cannot miss any of the rides happening cannot miss any of the rides happening cannot miss any of the rides happening on another core because we're monitoring on another core because we're monitoring on another core because we're monitoring the bus and all rightes have to go the bus and all rightes have to go the bus and all rightes have to go through the bus. That's the snooping through the bus. That's the snooping through the bus. That's the snooping part. The right update part is that when part. The right update part is that when part. The right update part is that when we see a right, we update our copy of we see a right, we update our copy of we see a right, we update our copy of the data with the value that is being the data with the value that is being the data with the value that is being written. So this cache now gets a one written. So this cache now gets a one written. So this cache now gets a one here. If we read a after this, we are here. If we read a after this, we are here. If we read a after this, we are going to get this one. So this ensures going to get this one. So this ensures going to get this one. So this ensures correct behavior. If there are multiple correct behavior. If there are multiple correct behavior. If there are multiple cores, they cannot disagree about which cores, they cannot disagree about which cores, they cannot disagree about which right happened first, which one happened right happened first, which one happened right happened first, which one happened last because all of the rights are sent last because all of the rights are sent last because all of the rights are sent through the bus and because it's a through the bus and because it's a through the bus and because it's a single broadcast bus, it happens one at single broadcast bus, it happens one at single broadcast bus, it happens one at a time. So all of them see rights in a time. So all of them see rights in a time. So all of them see rights in exactly the same order in the order in exactly the same order in the order in exactly the same order in the order in which writes got to the bus. Now let's which writes got to the bus. Now let's which writes got to the bus. Now let's see what happens if both of these see what happens if both of these see what happens if both of these processor try to write to a at about the processor try to write to a at about the processor try to write to a at about the same time. So this is a situation where same time. So this is a situation where same time. So this is a situation where they might disagree about whose right they might disagree about whose right they might disagree about whose right happened first. Let's say that this one happened first. Let's say that this one happened first. Let's say that this one is trying to write two and this one is is trying to write two and this one is is trying to write two and this one is trying to write three. Well, there is a trying to write three. Well, there is a trying to write three. Well, there is a single bus. So there is a single set of single bus. So there is a single set of single bus. So there is a single set of wires that can carry the value and the wires that can carry the value and the wires that can carry the value and the address and so on. So processors when address and so on. So processors when address and so on. So processors when they want to send something on the bus they want to send something on the bus they want to send something on the bus first have to arbitrate for it. And this first have to arbitrate for it. And this first have to arbitrate for it. And this arbitration for the bus basically arbitration for the bus basically arbitration for the bus basically decides who gets the bus next. If this decides who gets the bus next. If this decides who gets the bus next. If this score won then it sends its right first. score won then it sends its right first. score won then it sends its right first. Now both of the caches will have two. Now both of the caches will have two. Now both of the caches will have two. This one because this is where the right This one because this is where the right This one because this is where the right happened. This one because this is what happened. This one because this is what happened. This one because this is what we pick up from snooping. And only then we pick up from snooping. And only then we pick up from snooping. And only then after this is complete this right here after this is complete this right here after this is complete this right here gets to occur. So now we go to three gets to occur. So now we go to three gets to occur. So now we go to three broadcast that here and it gets here. So broadcast that here and it gets here. So broadcast that here and it gets here. So as you can see both of them agree which as you can see both of them agree which as you can see both of them agree which right was first and which was second. right was first and which was second. right was first and which was second. Obviously the one that wrote three was Obviously the one that wrote three was Obviously the one that wrote three was the second one because that's the last the second one because that's the last the second one because that's the last value we kept. So the right update is value we kept. So the right update is value we kept. So the right update is ensuring that rights are seen by reads ensuring that rights are seen by reads ensuring that rights are seen by reads on all cores and the snooping of the on all cores and the snooping of the on all cores and the snooping of the common bus and the serialization on that common bus and the serialization on that common bus and the serialization on that bus basically going one at a time to the bus basically going one at a time to the bus basically going one at a time to the bus is guaranteeing that everybody sees bus is guaranteeing that everybody sees bus is guaranteeing that everybody sees the same order of rights.

## Write Update Coherence Quiz (new)： Part 5
Kind: captions Language: en let's take a moment to check if we let's take a moment to check if we let's take a moment to check if we understand how write-update coherence understand how write-update coherence understand how write-update coherence works works works suppose that the core has a single block suppose that the core has a single block suppose that the core has a single block cache with a valid bit a tag and the cache with a valid bit a tag and the cache with a valid bit a tag and the data and suppose that we have for such data and suppose that we have for such data and suppose that we have for such cores numbered 0 1 2 &amp; 3 cores numbered 0 1 2 &amp; 3 cores numbered 0 1 2 &amp; 3 suppose that core 0 reads from 700 first suppose that core 0 reads from 700 first suppose that core 0 reads from 700 first so now it gets the valid bit to be 1 the so now it gets the valid bit to be 1 the so now it gets the valid bit to be 1 the tag is 700 and let's say that the data tag is 700 and let's say that the data tag is 700 and let's say that the data trees is 6 trees is 6 trees is 6 after this core 1 reads the same after this core 1 reads the same after this core 1 reads the same location then Core 2 writes a value of location then Core 2 writes a value of location then Core 2 writes a value of 17 into this location and finally core 17 into this location and finally core 17 into this location and finally core tree reads from that location after all tree reads from that location after all tree reads from that location after all this in core 0 what is the valid bit in this in core 0 what is the valid bit in this in core 0 what is the valid bit in the cache the tag is still 700 and what the cache the tag is still 700 and what the cache the tag is still 700 and what is the data that we get and the same is the data that we get and the same is the data that we get and the same four cores one two and three four cores one two and three four cores one two and three what is the valid bit in each of these what is the valid bit in each of these what is the valid bit in each of these caches and what's the data value placed caches and what's the data value placed caches and what's the data value placed here you can use this as an example here you can use this as an example here you can use this as an example where the valid bit is either 1 or 0 and where the valid bit is either 1 or 0 and where the valid bit is either 1 or 0 and the data value in this case is 6 the data value in this case is 6 the data value in this case is 6 after this read so what's going on in after this read so what's going on in after this read so what's going on in these 4 caches after we do this sequence these 4 caches after we do this sequence these 4 caches after we do this sequence we complete this then this then this we complete this then this then this we complete this then this then this then this

## Write Update Coherence Quiz Solution (new)： Part 5
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our write-update coherence quiz where what write-update coherence quiz where what write-update coherence quiz where what we had is for access is like this we had is for access is like this we had is for access is like this happening in a write-update happening in a write-update happening in a write-update coherent set of caches after the first coherent set of caches after the first coherent set of caches after the first read completes here we have data six read completes here we have data six read completes here we have data six tags 700 hex and the valid bit is 1 so tags 700 hex and the valid bit is 1 so tags 700 hex and the valid bit is 1 so when this read completes we have a valid when this read completes we have a valid when this read completes we have a valid bit 1 here the data is 6 and the tag is bit 1 here the data is 6 and the tag is bit 1 here the data is 6 and the tag is 700 at that time in these other caches 700 at that time in these other caches 700 at that time in these other caches the valid bit is simply 0 because they the valid bit is simply 0 because they the valid bit is simply 0 because they have not accessed anything yet have not accessed anything yet have not accessed anything yet when core 1 reads it sets the valid bit when core 1 reads it sets the valid bit when core 1 reads it sets the valid bit and fetch is the same data when quarto and fetch is the same data when quarto and fetch is the same data when quarto writes it broadcasts the value in the writes it broadcasts the value in the writes it broadcasts the value in the address so these caches that already address so these caches that already address so these caches that already have this block will update what they have this block will update what they have this block will update what they have now 217 and now of course on core have now 217 and now of course on core have now 217 and now of course on core to this block starts being valid and has to this block starts being valid and has to this block starts being valid and has a value of 17 and finally when core tree a value of 17 and finally when core tree a value of 17 and finally when core tree reads it also gets the value of 17 so reads it also gets the value of 17 so reads it also gets the value of 17 so this is the final state of the caches this is the final state of the caches this is the final state of the caches all caches have the block and all caches all caches have the block and all caches all caches have the block and all caches have the same value of the block which have the same value of the block which have the same value of the block which means that really the caches are means that really the caches are means that really the caches are coherent for this block

## Write Update Optimization： Part 5
Kind: captions Language: en now we will look at two possible now we will look at two possible now we will look at two possible optimizations of a write-update protocol optimizations of a write-update protocol optimizations of a write-update protocol the first optimization will look at is the first optimization will look at is the first optimization will look at is about avoiding memory rights remember about avoiding memory rights remember about avoiding memory rights remember that in a write-update protocol every that in a write-update protocol every that in a write-update protocol every write in any of the processors needs to write in any of the processors needs to write in any of the processors needs to be broadcast on the bus and needs to be broadcast on the bus and needs to be broadcast on the bus and needs to update the memory because memory is big update the memory because memory is big update the memory because memory is big and slow it cannot keep up with all the and slow it cannot keep up with all the and slow it cannot keep up with all the rights and that means that the memory rights and that means that the memory rights and that means that the memory throughput becomes a bottleneck for this throughput becomes a bottleneck for this throughput becomes a bottleneck for this system pretty much it's not about how system pretty much it's not about how system pretty much it's not about how much traffic can the bus take it's about much traffic can the bus take it's about much traffic can the bus take it's about how much traffic and the memory take so how much traffic and the memory take so how much traffic and the memory take so we want to avoid unnecessary memory we want to avoid unnecessary memory we want to avoid unnecessary memory writes these caches previously behaved writes these caches previously behaved writes these caches previously behaved as right through caches every write was as right through caches every write was as right through caches every write was going through the cache to memory and we going through the cache to memory and we going through the cache to memory and we have seen already that right through is have seen already that right through is have seen already that right through is bad for memory traffic even with a bad for memory traffic even with a bad for memory traffic even with a single core now we're adding more than single core now we're adding more than single core now we're adding more than one core so it's going to be even a one core so it's going to be even a one core so it's going to be even a bigger problem so the solution to bigger problem so the solution to bigger problem so the solution to reducing memory traffic here will be the reducing memory traffic here will be the reducing memory traffic here will be the same we will add a dirty bit to each same we will add a dirty bit to each same we will add a dirty bit to each block in each of the caches and this block in each of the caches and this block in each of the caches and this dirty bit will allow us to delay a dirty bit will allow us to delay a dirty bit will allow us to delay a memory right until we actually replace a memory right until we actually replace a memory right until we actually replace a dirty block from our cache so now let us dirty block from our cache so now let us dirty block from our cache so now let us see how this work and how it avoids see how this work and how it avoids see how this work and how it avoids memory writes suppose that this cache memory writes suppose that this cache memory writes suppose that this cache reads a it gets this value from memory reads a it gets this value from memory reads a it gets this value from memory let's say it's 6 the tag will be a it's let's say it's 6 the tag will be a it's let's say it's 6 the tag will be a it's valid and it's not dirty valid and it's not dirty valid and it's not dirty now this cache writes a at this point it now this cache writes a at this point it now this cache writes a at this point it will broadcast the write with the will broadcast the write with the will broadcast the write with the address of a and the value that it address of a and the value that it address of a and the value that it writes let's say 17 so this cache here writes let's say 17 so this cache here writes let's say 17 so this cache here would update the value now without a would update the value now without a would update the value now without a dirty bit the memory would also be dirty bit the memory would also be dirty bit the memory would also be updated with a equals 17 we want to updated with a equals 17 we want to updated with a equals 17 we want to avoid that why well because there could avoid that why well because there could avoid that why well because there could be a lot of writes here until we finally be a lot of writes here until we finally be a lot of writes here until we finally replace it in a write back cache so what replace it in a write back cache so what replace it in a write back cache so what we will do is we will write 17 tag it we will do is we will write 17 tag it we will do is we will write 17 tag it this way and mark this as dirty dirty this way and mark this as dirty dirty this way and mark this as dirty dirty means two things here one is that the means two things here one is that the means two things here one is that the memory is not up-to-date and we need to memory is not up-to-date and we need to memory is not up-to-date and we need to update it this is exactly the same update it this is exactly the same update it this is exactly the same as in a normal unit processor write-back as in a normal unit processor write-back as in a normal unit processor write-back cache if we replace something dirty we cache if we replace something dirty we cache if we replace something dirty we have to send a write back to memory in a have to send a write back to memory in a have to send a write back to memory in a write-update protocol dirty has another write-update protocol dirty has another write-update protocol dirty has another meaning that we will now illustrate meaning that we will now illustrate meaning that we will now illustrate suppose that this cache replaces this suppose that this cache replaces this suppose that this cache replaces this block and then wants to read it again so block and then wants to read it again so block and then wants to read it again so it issues a request to read on the bus it issues a request to read on the bus it issues a request to read on the bus and now normally the memory would and now normally the memory would and now normally the memory would respond with the value but the memory respond with the value but the memory respond with the value but the memory still has the old value of equals 6 this still has the old value of equals 6 this still has the old value of equals 6 this cache here has the only up-to-date copy cache here has the only up-to-date copy cache here has the only up-to-date copy that's what the dirty bit also stands that's what the dirty bit also stands that's what the dirty bit also stands for so now this cache needs to stop read for so now this cache needs to stop read for so now this cache needs to stop read as well notice that we are reading the as well notice that we are reading the as well notice that we are reading the same block that it has in a dirty state same block that it has in a dirty state same block that it has in a dirty state so instead of the memory being allowed so instead of the memory being allowed so instead of the memory being allowed to respond we respond first the cache is to respond we respond first the cache is to respond we respond first the cache is faster than the memory so it will faster than the memory so it will faster than the memory so it will respond quicker and say that a is 17 so respond quicker and say that a is 17 so respond quicker and say that a is 17 so now here we get a becomes 17 in this now here we get a becomes 17 in this now here we get a becomes 17 in this cache and it's not dirty now after this cache and it's not dirty now after this cache and it's not dirty now after this let's say that we write a new value here let's say that we write a new value here let's say that we write a new value here of let's say 20 we put the new value of let's say 20 we put the new value of let's say 20 we put the new value here we broadcast the new value on the here we broadcast the new value on the here we broadcast the new value on the bus the snooping here gets that 20 but bus the snooping here gets that 20 but bus the snooping here gets that 20 but the memory again doesn't get updated if the memory again doesn't get updated if the memory again doesn't get updated if this block however gets replaced from this block however gets replaced from this block however gets replaced from this cache then an update is sent to this cache then an update is sent to this cache then an update is sent to memory another interesting situation memory another interesting situation memory another interesting situation here is what happens if we have the here is what happens if we have the here is what happens if we have the block dirty in this cache and now this block dirty in this cache and now this block dirty in this cache and now this cache writes let's say 32 a in that case cache writes let's say 32 a in that case cache writes let's say 32 a in that case this cache broadcasts the right this this cache broadcasts the right this this cache broadcasts the right this cache snoops the right puts 30 in its cache snoops the right puts 30 in its cache snoops the right puts 30 in its cache now this cache is no longer cache now this cache is no longer cache now this cache is no longer responsible for writing back to memory responsible for writing back to memory responsible for writing back to memory because the new writer takes that because the new writer takes that because the new writer takes that responsibility and the memory still responsibility and the memory still responsibility and the memory still doesn't get updated so as you can see we doesn't get updated so as you can see we doesn't get updated so as you can see we can now do many writes without updating can now do many writes without updating can now do many writes without updating the memory we can even move the the memory we can even move the the memory we can even move the responsibility for updating memory to responsibility for updating memory to responsibility for updating memory to another cache and keep writing there another cache and keep writing there another cache and keep writing there without the updates to memory only when without the updates to memory only when without the updates to memory only when the last writer replaces the block that the last writer replaces the block that the last writer replaces the block that is still dirty will we is still dirty will we is still dirty will we a single right to memory so the benefits a single right to memory so the benefits a single right to memory so the benefits of having the dirty bit are that we can of having the dirty bit are that we can of having the dirty bit are that we can now write to the memory only when the now write to the memory only when the now write to the memory only when the block is replaced so we can do many block is replaced so we can do many block is replaced so we can do many writes without updating the memory does writes without updating the memory does writes without updating the memory does saving a lot of the writes to memory and saving a lot of the writes to memory and saving a lot of the writes to memory and also we avoid memory reads because now also we avoid memory reads because now also we avoid memory reads because now we allow reads from memory only when we allow reads from memory only when we allow reads from memory only when nobody has the block in a dirty state if nobody has the block in a dirty state if nobody has the block in a dirty state if the block is dirty for example here then the block is dirty for example here then the block is dirty for example here then whoever asks for this block this is the whoever asks for this block this is the whoever asks for this block this is the cache that will provide the value and cache that will provide the value and cache that will provide the value and the memory doesn't need to do that so we the memory doesn't need to do that so we the memory doesn't need to do that so we are also saving reads from memory this are also saving reads from memory this are also saving reads from memory this way

## Write Update Optimization 2 Bus Writes： Part 5
Kind: captions Language: en and now let's look at the second and now let's look at the second and now let's look at the second possible right update optimization which possible right update optimization which possible right update optimization which is about reducing the number of bus is about reducing the number of bus is about reducing the number of bus rights so after we added our dirty bit rights so after we added our dirty bit rights so after we added our dirty bit what we got is that the memory now gets what we got is that the memory now gets what we got is that the memory now gets relatively few rights only those rights relatively few rights only those rights relatively few rights only those rights that are coming from replacements and that are coming from replacements and that are coming from replacements and also the memory no longer needs to also the memory no longer needs to also the memory no longer needs to respond to all the reads but the bus respond to all the reads but the bus respond to all the reads but the bus still gets all of the traffic for all of still gets all of the traffic for all of still gets all of the traffic for all of the rights every single right will need the rights every single right will need the rights every single right will need to go on the bus and broadcast the value to go on the bus and broadcast the value to go on the bus and broadcast the value so now the bus which sees all of the so now the bus which sees all of the so now the bus which sees all of the rights will become the bottleneck in rights will become the bottleneck in rights will become the bottleneck in this system this system this system because every single write from every because every single write from every because every single write from every single core ends up going to the bus and single core ends up going to the bus and single core ends up going to the bus and the bus can only take so much the bus the bus can only take so much the bus the bus can only take so much the bus broadcasts for rights that are needed to broadcasts for rights that are needed to broadcasts for rights that are needed to update other copies of the data need to update other copies of the data need to update other copies of the data need to happen because this is a write-update happen because this is a write-update happen because this is a write-update protocol it needs to update other copies protocol it needs to update other copies protocol it needs to update other copies of the data but the optimization we can of the data but the optimization we can of the data but the optimization we can do is about the rights that do not need do is about the rights that do not need do is about the rights that do not need to update anybody so we will add another to update anybody so we will add another to update anybody so we will add another bit called the shared bit to each block bit called the shared bit to each block bit called the shared bit to each block in each cache and that bit will tell us in each cache and that bit will tell us in each cache and that bit will tell us whether the block is shared with others whether the block is shared with others whether the block is shared with others or not let's see how things would work or not let's see how things would work or not let's see how things would work suppose we read a here we get this valid suppose we read a here we get this valid suppose we read a here we get this valid not dirty but for the shared bit we need not dirty but for the shared bit we need not dirty but for the shared bit we need to know whether others also have this to know whether others also have this to know whether others also have this block or not for this purpose block or not for this purpose block or not for this purpose we add a line to the bus when a reed we add a line to the bus when a reed we add a line to the bus when a reed goes to memory others snoop this read goes to memory others snoop this read goes to memory others snoop this read and if they have that same block in the and if they have that same block in the and if they have that same block in the cache then they pull the shared line to cache then they pull the shared line to cache then they pull the shared line to one if nobody pulls the shared line to one if nobody pulls the shared line to one if nobody pulls the shared line to one then it will remain at zero so for one then it will remain at zero so for one then it will remain at zero so for this particular block because this is this particular block because this is this particular block because this is the first read nobody pulls the shared the first read nobody pulls the shared the first read nobody pulls the shared line to one so the block we know is not line to one so the block we know is not line to one so the block we know is not shared this is the tag and let's say shared this is the tag and let's say shared this is the tag and let's say we're at six now let's see what happens we're at six now let's see what happens we're at six now let's see what happens when this processor writes to a and when this processor writes to a and when this processor writes to a and let's say it puts a value of seventeen let's say it puts a value of seventeen let's say it puts a value of seventeen there it's a Miss we broadcast both the there it's a Miss we broadcast both the there it's a Miss we broadcast both the right and the value the first time and right and the value the first time and right and the value the first time and we see now that somebody is writing we see now that somebody is writing we see now that somebody is writing and this cash here snoops that right and and this cash here snoops that right and and this cash here snoops that right and those two things one it now knows that those two things one it now knows that those two things one it now knows that the block is shared because somebody is the block is shared because somebody is the block is shared because somebody is accessing it now second it updates the accessing it now second it updates the accessing it now second it updates the value third it pulls this line to one so value third it pulls this line to one so value third it pulls this line to one so that this cash knows the block is shared that this cash knows the block is shared that this cash knows the block is shared and now it has the Block in the dirty and now it has the Block in the dirty and now it has the Block in the dirty State with the value of 17 so so far State with the value of 17 so so far State with the value of 17 so so far this has behaved exactly as it should this has behaved exactly as it should this has behaved exactly as it should without the shared line and without the without the shared line and without the without the shared line and without the shared bit so what's the benefit of the shared bit so what's the benefit of the shared bit so what's the benefit of the shared bits well the benefit is that now shared bits well the benefit is that now shared bits well the benefit is that now this cash if it wants to write again it this cash if it wants to write again it this cash if it wants to write again it sees that the shared bit is one which sees that the shared bit is one which sees that the shared bit is one which means that it does need to broadcast means that it does need to broadcast means that it does need to broadcast same as before so the broadcast will same as before so the broadcast will same as before so the broadcast will happen exactly as before if there is happen exactly as before if there is happen exactly as before if there is another sharer because we need to update another sharer because we need to update another sharer because we need to update that sharer to the new value for the that sharer to the new value for the that sharer to the new value for the data the benefit of the shared it is in data the benefit of the shared it is in data the benefit of the shared it is in that if this block reads let's say B and that if this block reads let's say B and that if this block reads let's say B and gets a value of 2 here and it's valid gets a value of 2 here and it's valid gets a value of 2 here and it's valid and it's not dirty and it is that so far and it's not dirty and it is that so far and it's not dirty and it is that so far nobody has B so sharing is 0 here and nobody has B so sharing is 0 here and nobody has B so sharing is 0 here and then wants to write 5 to be it checks then wants to write 5 to be it checks then wants to write 5 to be it checks the shared bit here and because we know the shared bit here and because we know the shared bit here and because we know that nobody else has this block we can that nobody else has this block we can that nobody else has this block we can write the value here make the thing write the value here make the thing write the value here make the thing dirty because the memory is no longer dirty because the memory is no longer dirty because the memory is no longer up-to-date but because the shared bit up-to-date but because the shared bit up-to-date but because the shared bit stays at 0 meaning we are the only one stays at 0 meaning we are the only one stays at 0 meaning we are the only one who has it who has it who has it we don't do a bus right here so as long we don't do a bus right here so as long we don't do a bus right here so as long as some block is being accessed only by as some block is being accessed only by as some block is being accessed only by one core it's no longer gonna broadcast one core it's no longer gonna broadcast one core it's no longer gonna broadcast all of the rights meanwhile this core all of the rights meanwhile this core all of the rights meanwhile this core could be having in a non shared State could be having in a non shared State could be having in a non shared State for example Block C and it can write for example Block C and it can write for example Block C and it can write anything in once there you can write 17 anything in once there you can write 17 anything in once there you can write 17 then 65 etc so B and C because they then 65 etc so B and C because they then 65 etc so B and C because they belong to only one core at this time belong to only one core at this time belong to only one core at this time will not do bus rights while a will do will not do bus rights while a will do will not do bus rights while a will do bus rights so we still get the right bus rights so we still get the right bus rights so we still get the right update behavior when there is sharing update behavior when there is sharing update behavior when there is sharing but now we avoid rights going to the bus but now we avoid rights going to the bus but now we avoid rights going to the bus when there is no sharing when there is no sharing when there is no sharing this can happen a lot for example B this can happen a lot for example B this can happen a lot for example B could be something that belongs to the could be something that belongs to the could be something that belongs to the stack of course zero seek would be stack of course zero seek would be stack of course zero seek would be something that is on the stack of cor something that is on the stack of cor something that is on the stack of cor one so naturally we will be just pushing one so naturally we will be just pushing one so naturally we will be just pushing and popping things to our own stack and and popping things to our own stack and and popping things to our own stack and never really needing to broadcast the never really needing to broadcast the never really needing to broadcast the updates somewhere else but for data that updates somewhere else but for data that updates somewhere else but for data that is actually shared we still get exactly is actually shared we still get exactly is actually shared we still get exactly the same right update behavior as before the same right update behavior as before the same right update behavior as before so this optimization will save us a lot so this optimization will save us a lot so this optimization will save us a lot of Rights on the bus it will save us all of Rights on the bus it will save us all of Rights on the bus it will save us all of the rights that are not necessary to of the rights that are not necessary to of the rights that are not necessary to maintain coherence among multiple copies maintain coherence among multiple copies maintain coherence among multiple copies of the same block in different caches

## Write Update Performance Quiz： Part 5
Kind: captions Language: en let's quickly check if we understood the let's quickly check if we understood the let's quickly check if we understood the right update optimizations suppose that right update optimizations suppose that right update optimizations suppose that we have a system with two cores that we have a system with two cores that we have a system with two cores that uses write-update coherence suppose that uses write-update coherence suppose that uses write-update coherence suppose that the two cores are executing a program the two cores are executing a program the two cores are executing a program where core 0 writes something to a where core 0 writes something to a where core 0 writes something to a memory address a then core 1 reads it memory address a then core 1 reads it memory address a then core 1 reads it and this continues so that the write and and this continues so that the write and and this continues so that the write and then read pattern repeats for a total of then read pattern repeats for a total of then read pattern repeats for a total of 1000 times so this is the first of 1,000 1000 times so this is the first of 1,000 1000 times so this is the first of 1,000 after they do this for 1,000 times this after they do this for 1,000 times this after they do this for 1,000 times this course go to do something else where course go to do something else where course go to do something else where core 0 replaces a and then core 1 also core 0 replaces a and then core 1 also core 0 replaces a and then core 1 also replaces a the question for you is how replaces a the question for you is how replaces a the question for you is how many bus uses do we see when the system many bus uses do we see when the system many bus uses do we see when the system uses no optimizations so there is no uses no optimizations so there is no uses no optimizations so there is no dirty bit and no shared it when the dirty bit and no shared it when the dirty bit and no shared it when the system uses only the dirty bit system uses only the dirty bit system uses only the dirty bit optimization and when the system uses optimization and when the system uses optimization and when the system uses both the dirty and the shared bit both the dirty and the shared bit both the dirty and the shared bit optimization so for each of these how optimization so for each of these how optimization so for each of these how many bus uses do we get during this many bus uses do we get during this many bus uses do we get during this whole time for block a and also how many whole time for block a and also how many whole time for block a and also how many memory writes do we get in each of these memory writes do we get in each of these memory writes do we get in each of these three cases

## Write Update Performance Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to a write let's look at the solution to a write let's look at the solution to a write update optimization quiz where we have update optimization quiz where we have update optimization quiz where we have two cores with write update coherence two cores with write update coherence two cores with write update coherence and and and we consider the case of no optimization we consider the case of no optimization we consider the case of no optimization dirty bit only optimization and both dirty bit only optimization and both dirty bit only optimization and both dirty bit and shared bit optimization dirty bit and shared bit optimization dirty bit and shared bit optimization and for this program how many bus uses and for this program how many bus uses and for this program how many bus uses and memory writes do we get so let's and memory writes do we get so let's and memory writes do we get so let's consider the case of no optimization consider the case of no optimization consider the case of no optimization first let us first consider the no first let us first consider the no first let us first consider the no optimization case optimization case optimization case this right here we'll use the bus this right here we'll use the bus this right here we'll use the bus and we'll write to memory this read here and we'll write to memory this read here and we'll write to memory this read here the first one the first one the first one will use the bus will use the bus will use the bus but will not update the memory because but will not update the memory because but will not update the memory because this is a read miss this is a read miss this is a read miss from then on each write on core 0 will from then on each write on core 0 will from then on each write on core 0 will use the bus to broadcast and update use the bus to broadcast and update use the bus to broadcast and update memory memory memory so we get 999 more so we get 999 more so we get 999 more accesses to each because of this accesses to each because of this accesses to each because of this whereas reads are no longer going to be whereas reads are no longer going to be whereas reads are no longer going to be misses misses misses and they find what they are looking for and they find what they are looking for and they find what they are looking for in the cache so they are no longer going in the cache so they are no longer going in the cache so they are no longer going to either bus or memory to either bus or memory to either bus or memory eventually these caches replace this but eventually these caches replace this but eventually these caches replace this but nothing happens on the replacement in nothing happens on the replacement in nothing happens on the replacement in this case because everything is already this case because everything is already this case because everything is already up to date so overall here we got 1001 up to date so overall here we got 1001 up to date so overall here we got 1001 bus use and 1000 memory writes now let bus use and 1000 memory writes now let bus use and 1000 memory writes now let us look at the dirty bit only us look at the dirty bit only us look at the dirty bit only optimization optimization optimization in this optimization in this optimization in this optimization the writes on core 0 will all be the writes on core 0 will all be the writes on core 0 will all be happening to core 0's cache and the only happening to core 0's cache and the only happening to core 0's cache and the only write to memory will be when the block write to memory will be when the block write to memory will be when the block is replaced so we get only one memory is replaced so we get only one memory is replaced so we get only one memory write how many bus users do we need well write how many bus users do we need well write how many bus users do we need well the first write and every subsequent the first write and every subsequent the first write and every subsequent write will be broadcast so we get 1000 write will be broadcast so we get 1000 write will be broadcast so we get 1000 bus accesses because of these rights bus accesses because of these rights bus accesses because of these rights we still have the one access because the we still have the one access because the we still have the one access because the first read is a miss after that that all first read is a miss after that that all first read is a miss after that that all hits and don't go to the bus hits and don't go to the bus hits and don't go to the bus and now we have an additional bus axis and now we have an additional bus axis and now we have an additional bus axis because the replacement of a here needs because the replacement of a here needs because the replacement of a here needs to send the data to memory for which we to send the data to memory for which we to send the data to memory for which we need a bus axis need a bus axis need a bus axis so overall we reduce dramatically the so overall we reduce dramatically the so overall we reduce dramatically the number of memory writes in exchange for number of memory writes in exchange for number of memory writes in exchange for one more one more one more bus use bus use bus use now let's see what happens with the now let's see what happens with the now let's see what happens with the dirty and shared optimization dirty and shared optimization dirty and shared optimization we're right here and at this point we we're right here and at this point we we're right here and at this point we are the only one who has this block so are the only one who has this block so are the only one who has this block so the shared bit the shared bit the shared bit is not set is not set is not set this would normally allow us to write to this would normally allow us to write to this would normally allow us to write to it without going to the bus anymore but it without going to the bus anymore but it without going to the bus anymore but because the very next axis is read of a because the very next axis is read of a because the very next axis is read of a on core 1 on core 1 on core 1 the sharing starts the sharing starts the sharing starts and that means that really and that means that really and that means that really we have exactly the same number of bus we have exactly the same number of bus we have exactly the same number of bus uses and memory writes uses and memory writes uses and memory writes remember that the sharing optimization remember that the sharing optimization remember that the sharing optimization only works when we are accessing only works when we are accessing only works when we are accessing a block only on one core this block is a block only on one core this block is a block only on one core this block is actually being actually being actually being read on one core written on another so read on one core written on another so read on one core written on another so all the writes on this score need to be all the writes on this score need to be all the writes on this score need to be propagated to the bus so that this propagated to the bus so that this propagated to the bus so that this course cache can be updated so really course cache can be updated so really course cache can be updated so really here the shared bit optimization doesn't here the shared bit optimization doesn't here the shared bit optimization doesn't result in any savings

## Write Invalidate Snooping Coherence： Part 5
Kind: captions Language: en We said that there are two types of We said that there are two types of We said that there are two types of snooping coherence approaches. One was snooping coherence approaches. One was snooping coherence approaches. One was write update. The other is write write update. The other is write write update. The other is write invalidate. So going back to our invalidate. So going back to our invalidate. So going back to our optimize write update setup where we had optimize write update setup where we had optimize write update setup where we had the valid and the tag bits also the the valid and the tag bits also the the valid and the tag bits also the dirty bit for write back caches and a dirty bit for write back caches and a dirty bit for write back caches and a shared bit that tells us whether we are shared bit that tells us whether we are shared bit that tells us whether we are alone in this. A write invalidate alone in this. A write invalidate alone in this. A write invalidate protocol will broadcast rights on the protocol will broadcast rights on the protocol will broadcast rights on the bus so that they can be seen by bus so that they can be seen by bus so that they can be seen by everybody. But instead of updating their everybody. But instead of updating their everybody. But instead of updating their copies, they will invalidate them. So copies, they will invalidate them. So copies, they will invalidate them. So let's see how this works. Let's say this let's see how this works. Let's say this let's see how this works. Let's say this score reads score reads score reads a. We send this get the value from a. We send this get the value from a. We send this get the value from memory. Now we have it as valid, not memory. Now we have it as valid, not memory. Now we have it as valid, not dirty. The tag is a the value is let's dirty. The tag is a the value is let's dirty. The tag is a the value is let's say zero and it's not shared. Let's say say zero and it's not shared. Let's say say zero and it's not shared. Let's say now we have the right to a and write one now we have the right to a and write one now we have the right to a and write one to it. We have a cache miss. We to it. We have a cache miss. We to it. We have a cache miss. We broadcast the right. We now don't need broadcast the right. We now don't need broadcast the right. We now don't need to broadcast the value with it because to broadcast the value with it because to broadcast the value with it because the other caches don't need this value. the other caches don't need this value. the other caches don't need this value. What's going to happen is as we send What's going to happen is as we send What's going to happen is as we send this to the bus, it's going to be this to the bus, it's going to be this to the bus, it's going to be snooped by this cache and it sees that snooped by this cache and it sees that snooped by this cache and it sees that somebody is trying to write to the block somebody is trying to write to the block somebody is trying to write to the block that I have in my cache. In a right that I have in my cache. In a right that I have in my cache. In a right update protocol, we would now need the update protocol, we would now need the update protocol, we would now need the value to put it here. In a right value to put it here. In a right value to put it here. In a right invalidate protocol, all we do is change invalidate protocol, all we do is change invalidate protocol, all we do is change the valid bit to zero. So now we will the valid bit to zero. So now we will the valid bit to zero. So now we will miss in the cache next time we read this miss in the cache next time we read this miss in the cache next time we read this particular cache now gets the valid it's particular cache now gets the valid it's particular cache now gets the valid it's not shared every time we do a write and not shared every time we do a write and not shared every time we do a write and broadcast it we know it's not shared broadcast it we know it's not shared broadcast it we know it's not shared because all the other copies have been because all the other copies have been because all the other copies have been destroyed so a write makes the block destroyed so a write makes the block destroyed so a write makes the block non-shared in the writing processor it non-shared in the writing processor it non-shared in the writing processor it is dirty and now it has a value of one is dirty and now it has a value of one is dirty and now it has a value of one so now a read of a here in a right so now a read of a here in a right so now a read of a here in a right update protocol would be a hit and just update protocol would be a hit and just update protocol would be a hit and just read here but in a right invalid date read here but in a right invalid date read here but in a right invalid date protocol, the read will still get the protocol, the read will still get the protocol, the read will still get the new value. It's not going to be new value. It's not going to be new value. It's not going to be incoherent because it doesn't get the incoherent because it doesn't get the incoherent because it doesn't get the value of zero. It will now be a miss and value of zero. It will now be a miss and value of zero. It will now be a miss and be sent to the bus. Now, the memory has be sent to the bus. Now, the memory has be sent to the bus. Now, the memory has not been updated yet, but the processor not been updated yet, but the processor not been updated yet, but the processor who has the dirty data and matches the who has the dirty data and matches the who has the dirty data and matches the address of the block will see that there address of the block will see that there address of the block will see that there is a read request on the bus and respond is a read request on the bus and respond is a read request on the bus and respond with the data. So now the block becomes with the data. So now the block becomes with the data. So now the block becomes valid. Somebody gave us the data. So valid. Somebody gave us the data. So valid. Somebody gave us the data. So definitely now it's shared. we get the definitely now it's shared. we get the definitely now it's shared. we get the data and the cache where we wrote now data and the cache where we wrote now data and the cache where we wrote now also knows it's shared because we gave also knows it's shared because we gave also knows it's shared because we gave it to somebody. So clearly we are not it to somebody. So clearly we are not it to somebody. So clearly we are not the only one who has it anymore. So now the only one who has it anymore. So now the only one who has it anymore. So now note that just like in a right update note that just like in a right update note that just like in a right update protocol if we have a lot of reads now protocol if we have a lot of reads now protocol if we have a lot of reads now they can have cash hits another write they can have cash hits another write they can have cash hits another write will invalidate this. So there will be a will invalidate this. So there will be a will invalidate this. So there will be a cache miss also. So the disadvantage of cache miss also. So the disadvantage of cache miss also. So the disadvantage of write invalidate protocols is that there write invalidate protocols is that there write invalidate protocols is that there is a miss on all the readers when is a miss on all the readers when is a miss on all the readers when somebody writes. However, there is an somebody writes. However, there is an somebody writes. However, there is an advantage to it as well in that if you advantage to it as well in that if you advantage to it as well in that if you need to update the same block twice. need to update the same block twice. need to update the same block twice. Let's say you write two to a, we Let's say you write two to a, we Let's say you write two to a, we invalidate this copy by broadcasting. invalidate this copy by broadcasting. invalidate this copy by broadcasting. Now the block is no longer shared. We Now the block is no longer shared. We Now the block is no longer shared. We put two there. So we had a broadcast for put two there. So we had a broadcast for put two there. So we had a broadcast for this first write. If we now do some more this first write. If we now do some more this first write. If we now do some more reads and writes, they can all be done reads and writes, they can all be done reads and writes, they can all be done locally. We don't have to broadcast locally. We don't have to broadcast locally. We don't have to broadcast them. So basically by invalidating this them. So basically by invalidating this them. So basically by invalidating this copy we made ourselves be the only one copy we made ourselves be the only one copy we made ourselves be the only one who has the block and now we don't need who has the block and now we don't need who has the block and now we don't need to broadcast all the rights. So for to broadcast all the rights. So for to broadcast all the rights. So for anything where one core works on it for anything where one core works on it for anything where one core works on it for a while then another one works on it for a while then another one works on it for a while then another one works on it for a while. If the work involves writing a while. If the work involves writing a while. If the work involves writing only the first write really has to be only the first write really has to be only the first write really has to be broadcast after that it's a local access broadcast after that it's a local access broadcast after that it's a local access for both reads and writes. In contrast for both reads and writes. In contrast for both reads and writes. In contrast with write update we would have to keep with write update we would have to keep with write update we would have to keep updating the other cache until it updating the other cache until it updating the other cache until it replaces the block. Just like with write replaces the block. Just like with write replaces the block. Just like with write update, write invalidate can benefit update, write invalidate can benefit update, write invalidate can benefit from the same type of optimization when from the same type of optimization when from the same type of optimization when we are writing two different blocks and we are writing two different blocks and we are writing two different blocks and accessing it later. So in that case B is accessing it later. So in that case B is accessing it later. So in that case B is fetched here is not shared becomes dirty fetched here is not shared becomes dirty fetched here is not shared becomes dirty and C is fetched here is not shared and C is fetched here is not shared and C is fetched here is not shared becomes dirty and gets a value of one. becomes dirty and gets a value of one. becomes dirty and gets a value of one. And now we can have cache hits on both And now we can have cache hits on both And now we can have cache hits on both reads and writes without going to the reads and writes without going to the reads and writes without going to the bus for B here. And we can access C here bus for B here. And we can access C here bus for B here. And we can access C here with no going to the bus. So for blocks with no going to the bus. So for blocks with no going to the bus. So for blocks that are only accessed on one core, we that are only accessed on one core, we that are only accessed on one core, we have the same behavior for both write have the same behavior for both write have the same behavior for both write update and write update and write update and write invalidate. But for blocks that are invalidate. But for blocks that are invalidate. But for blocks that are shared, we get different behavior. Write shared, we get different behavior. Write shared, we get different behavior. Write update tends to result in more hits but update tends to result in more hits but update tends to result in more hits but does more broadcasts if we repeatedly does more broadcasts if we repeatedly does more broadcasts if we repeatedly update. Write invalidate generates a update. Write invalidate generates a update. Write invalidate generates a miss on all the readers after a right miss on all the readers after a right miss on all the readers after a right occurred but it allows us to locally occurred but it allows us to locally occurred but it allows us to locally write after the first write. Note that write after the first write. Note that write after the first write. Note that with write update we ensured for a that with write update we ensured for a that with write update we ensured for a that the second property of coherence is the second property of coherence is the second property of coherence is maintained by updating all the copies. maintained by updating all the copies. maintained by updating all the copies. So when a right occurs all copies are up So when a right occurs all copies are up So when a right occurs all copies are up to date and thus they will return the to date and thus they will return the to date and thus they will return the new value of the right. With write new value of the right. With write new value of the right. With write invalidate we ensure the same property invalidate we ensure the same property invalidate we ensure the same property by the right invalidating all the other by the right invalidating all the other by the right invalidating all the other copies. So that now they have no choice copies. So that now they have no choice copies. So that now they have no choice but to get the new version of the data. but to get the new version of the data. but to get the new version of the data. So either way reads will get the new So either way reads will get the new So either way reads will get the new version of the data but that is achieved version of the data but that is achieved version of the data but that is achieved in different ways. The third property of in different ways. The third property of in different ways. The third property of coherence is still ensured by snooping coherence is still ensured by snooping coherence is still ensured by snooping in a right invalidate protocol with in a right invalidate protocol with in a right invalidate protocol with snooping. So basically snooping on the snooping. So basically snooping on the snooping. So basically snooping on the bus still ensures that any rights that bus still ensures that any rights that bus still ensures that any rights that need to go to the bus because there is need to go to the bus because there is need to go to the bus because there is sharing will be ordered by the order to sharing will be ordered by the order to sharing will be ordered by the order to which they go to the bus. So for example which they go to the bus. So for example which they go to the bus. So for example a right here will invalidate everything. a right here will invalidate everything. a right here will invalidate everything. Now a right here has to put a request on Now a right here has to put a request on Now a right here has to put a request on the bus because it's a miss and thus the bus because it's a miss and thus the bus because it's a miss and thus obtain the copy from here and now obtain the copy from here and now obtain the copy from here and now everybody knows what the order was. In everybody knows what the order was. In everybody knows what the order was. In fact, at any given time, only the last fact, at any given time, only the last fact, at any given time, only the last version really survives in caches version really survives in caches version really survives in caches because a right invalidates all the because a right invalidates all the because a right invalidates all the other copies. So, it's pretty clear other copies. So, it's pretty clear other copies. So, it's pretty clear whose version was last because all the whose version was last because all the whose version was last because all the readers need to get the copy that was readers need to get the copy that was readers need to get the copy that was written last anyway.

## Write Update vs Write Invalidate Quiz： Part 5
Kind: captions Language: en So now let's see how write update and So now let's see how write update and So now let's see how write update and write invalidate differ from each other. write invalidate differ from each other. write invalidate differ from each other. Suppose that we have two cores and a Suppose that we have two cores and a Suppose that we have two cores and a program where core zero writes to a and program where core zero writes to a and program where core zero writes to a and then core one reads from A and this then core one reads from A and this then core one reads from A and this repeats a thousand times. And you can repeats a thousand times. And you can repeats a thousand times. And you can assume that the caches are empty before assume that the caches are empty before assume that the caches are empty before all of this and that we don't care what all of this and that we don't care what all of this and that we don't care what happens after all of this. The question happens after all of this. The question happens after all of this. The question for you is how many bus uses do we get for you is how many bus uses do we get for you is how many bus uses do we get with a right update protocol that uses with a right update protocol that uses with a right update protocol that uses both the shared and the dirty bit both the shared and the dirty bit both the shared and the dirty bit optimization? Put your answer here. And optimization? Put your answer here. And optimization? Put your answer here. And then with a right invalidate protocol then with a right invalidate protocol then with a right invalidate protocol with the same shared and dirty bits, put with the same shared and dirty bits, put with the same shared and dirty bits, put your answer here.

## Write Update vs Write Invalidate Quiz Solution： Part 5
Kind: captions Language: en let's see the solution to a write-update let's see the solution to a write-update let's see the solution to a write-update versus write-invalidate quiz we have two versus write-invalidate quiz we have two versus write-invalidate quiz we have two cores that run a program where basically cores that run a program where basically cores that run a program where basically course zero writes and then core 1 reads course zero writes and then core 1 reads course zero writes and then core 1 reads and that happens a thousand times how and that happens a thousand times how and that happens a thousand times how many bus uses do we get with the many bus uses do we get with the many bus uses do we get with the write-update protocol and then with the write-update protocol and then with the write-update protocol and then with the write invalidate protocol we have write invalidate protocol we have write invalidate protocol we have already seen a similar example for write already seen a similar example for write already seen a similar example for write update what's going to happen is with update what's going to happen is with update what's going to happen is with the shared and dirty bits we do not the shared and dirty bits we do not the shared and dirty bits we do not really save much because there is actual really save much because there is actual really save much because there is actual sharing going on here so we are going to sharing going on here so we are going to sharing going on here so we are going to have a broadcast on the bus for each of have a broadcast on the bus for each of have a broadcast on the bus for each of these rights so we get a thousand bus these rights so we get a thousand bus these rights so we get a thousand bus accesses from these rights and the very accesses from these rights and the very accesses from these rights and the very first read is going to be a Miss so we first read is going to be a Miss so we first read is going to be a Miss so we get the bus access there so overall we get the bus access there so overall we get the bus access there so overall we get one thousand one bus uses in a write get one thousand one bus uses in a write get one thousand one bus uses in a write invalidate protocol let's see what invalidate protocol let's see what invalidate protocol let's see what happens core 0 writes this is a Miss we happens core 0 writes this is a Miss we happens core 0 writes this is a Miss we send something to the bus and now we get send something to the bus and now we get send something to the bus and now we get the only copy of this data core 1 reads the only copy of this data core 1 reads the only copy of this data core 1 reads has a miss core 0 has to supply the data has a miss core 0 has to supply the data has a miss core 0 has to supply the data so so far we had 1 and 1 and then what so so far we had 1 and 1 and then what so so far we had 1 and 1 and then what happens is core 0 writes we know that happens is core 0 writes we know that happens is core 0 writes we know that there is a sharer now because this core there is a sharer now because this core there is a sharer now because this core has a block so we need to send out an has a block so we need to send out an has a block so we need to send out an invalidation that means that when core 1 invalidation that means that when core 1 invalidation that means that when core 1 reads next it's going to have a Miss and reads next it's going to have a Miss and reads next it's going to have a Miss and use the bus so this will repeat a use the bus so this will repeat a use the bus so this will repeat a thousand times and we get a total of thousand times and we get a total of thousand times and we get a total of 2,000 bus uses so as you can see right 2,000 bus uses so as you can see right 2,000 bus uses so as you can see right update is a lot more efficient than update is a lot more efficient than update is a lot more efficient than write invalidate write invalidate write invalidate if we actually have a pattern where one if we actually have a pattern where one if we actually have a pattern where one core produces the data continuously and core produces the data continuously and core produces the data continuously and the other one keeps reading the data if the other one keeps reading the data if the other one keeps reading the data if we have a pattern in which one core is we have a pattern in which one core is we have a pattern in which one core is the producer of all the data and the the producer of all the data and the the producer of all the data and the other core is the consumer because the other core is the consumer because the other core is the consumer because the data flow from the producer to the data flow from the producer to the data flow from the producer to the consumer through the same memory consumer through the same memory consumer through the same memory location in a write-update protocol location in a write-update protocol location in a write-update protocol involves just the update by the right involves just the update by the right involves just the update by the right but the read can then find the data in but the read can then find the data in but the read can then find the data in the cache whereas in the invalidation the cache whereas in the invalidation the cache whereas in the invalidation protocol the write just sends out an protocol the write just sends out an protocol the write just sends out an invalidation and then the reader has to invalidation and then the reader has to invalidation and then the reader has to still suffer image to obtain the data



---

# Playlist 4 Module 7 Raw Transcript

## Write Update vs Write Invalidate Quiz 2： Part 5
Kind: captions Language: en let's see another comparison of updates let's see another comparison of updates let's see another comparison of updates and invalidates this time let's look at and invalidates this time let's look at and invalidates this time let's look at two cores that execute the following two cores that execute the following two cores that execute the following program core zero will read and then program core zero will read and then program core zero will read and then write a and this is repeated 500 times write a and this is repeated 500 times write a and this is repeated 500 times and then core one will read and then and then core one will read and then and then core one will read and then write a and this is repeated 500 times write a and this is repeated 500 times write a and this is repeated 500 times and meanwhile nothing else happens in and meanwhile nothing else happens in and meanwhile nothing else happens in this course so nothing gets replaced the this course so nothing gets replaced the this course so nothing gets replaced the question for you is again how many bus question for you is again how many bus question for you is again how many bus uses do we get with the right update uses do we get with the right update uses do we get with the right update protocol and with the right invalidate protocol and with the right invalidate protocol and with the right invalidate protocol in this case

## Write Update vs Write Invalidate Quiz Solution 2： Part 5
Kind: captions Language: en let's look at the solution to our second let's look at the solution to our second let's look at the solution to our second write-update versus write-invalidate write-update versus write-invalidate write-update versus write-invalidate quiz this time we have a program where quiz this time we have a program where quiz this time we have a program where one core repeatedly reads and then one core repeatedly reads and then one core repeatedly reads and then writes a and then the other so how many writes a and then the other so how many writes a and then the other so how many bus uses do we get with the right update bus uses do we get with the right update bus uses do we get with the right update and with the right invalidate protocol and with the right invalidate protocol and with the right invalidate protocol this time the write-update protocol is this time the write-update protocol is this time the write-update protocol is going to have one bus access here to going to have one bus access here to going to have one bus access here to read because this is a Miss because read because this is a Miss because read because this is a Miss because there is no sharing the write doesn't there is no sharing the write doesn't there is no sharing the write doesn't need a write axis because we know that need a write axis because we know that need a write axis because we know that we privately have this thing in our we privately have this thing in our we privately have this thing in our cache so this is what happens the first cache so this is what happens the first cache so this is what happens the first time in the remaining 499 iterations of time in the remaining 499 iterations of time in the remaining 499 iterations of this nothing else happens we just hit in this nothing else happens we just hit in this nothing else happens we just hit in our cache once we get to core 1 however our cache once we get to core 1 however our cache once we get to core 1 however the read a is going to get the data from the read a is going to get the data from the read a is going to get the data from core 0 but now we have the shared bit core 0 but now we have the shared bit core 0 but now we have the shared bit set at 1 in both caches so the right set at 1 in both caches so the right set at 1 in both caches so the right here will need to do a broadcast the here will need to do a broadcast the here will need to do a broadcast the first time and then the next time and so first time and then the next time and so first time and then the next time and so on so overall we get one for the initial on so overall we get one for the initial on so overall we get one for the initial miss and then five hundred updates of miss and then five hundred updates of miss and then five hundred updates of cores zeros cache note that this is cores zeros cache note that this is cores zeros cache note that this is wasteful because core 0 is no longer wasteful because core 0 is no longer wasteful because core 0 is no longer really interested in this so overall we really interested in this so overall we really interested in this so overall we get 502 bus accesses in a write get 502 bus accesses in a write get 502 bus accesses in a write invalidate protocol we have the same invalidate protocol we have the same invalidate protocol we have the same thing here we have a mess and the data thing here we have a mess and the data thing here we have a mess and the data is not shared is not shared is not shared so the write and all of the reads and so the write and all of the reads and so the write and all of the reads and writes after that are just cache hits writes after that are just cache hits writes after that are just cache hits however here what happens is the read however here what happens is the read however here what happens is the read here will make the data shared and fetch here will make the data shared and fetch here will make the data shared and fetch the data from the other core so that's the data from the other core so that's the data from the other core so that's going to be a miss because the data going to be a miss because the data going to be a miss because the data shared the right will have to do an shared the right will have to do an shared the right will have to do an invalidation broadcast so we will have invalidation broadcast so we will have invalidation broadcast so we will have another bus access but that makes the another bus access but that makes the another bus access but that makes the data private to core 1 because this data private to core 1 because this data private to core 1 because this version is invalidated so for the version is invalidated so for the version is invalidated so for the remaining 499 iterations of this nothing remaining 499 iterations of this nothing remaining 499 iterations of this nothing else happens so overall we get only else happens so overall we get only else happens so overall we get only three bus accesses as you can see right three bus accesses as you can see right three bus accesses as you can see right invalidate is a lot more efficient if invalidate is a lot more efficient if invalidate is a lot more efficient if the the the it tends to be used frequently on one it tends to be used frequently on one it tends to be used frequently on one core and then the frequent use moves to core and then the frequent use moves to core and then the frequent use moves to another core

## Update VS Invalidate Coherence： Part 5
Kind: captions Language: en so let's compare update and invalidate so let's compare update and invalidate so let's compare update and invalidate coherence approaches we will compare coherence approaches we will compare coherence approaches we will compare them for different things that the them for different things that the them for different things that the applications often do here we will say applications often do here we will say applications often do here we will say what the update protocols are gonna do what the update protocols are gonna do what the update protocols are gonna do and here's what an invalidate protocol and here's what an invalidate protocol and here's what an invalidate protocol is going to do if the application has a is going to do if the application has a is going to do if the application has a burst of rights to one address and if burst of rights to one address and if burst of rights to one address and if that address is shared then each write that address is shared then each write that address is shared then each write will send an update in an update will send an update in an update will send an update in an update protocol this is bad because each update protocol this is bad because each update protocol this is bad because each update results in competing for the bus so it results in competing for the bus so it results in competing for the bus so it creates bus contention and also it creates bus contention and also it creates bus contention and also it consumes energy and makes write slower consumes energy and makes write slower consumes energy and makes write slower in an invalidate protocol only the first in an invalidate protocol only the first in an invalidate protocol only the first right will send out an invalidation and right will send out an invalidation and right will send out an invalidation and the remaining writes are just gonna be the remaining writes are just gonna be the remaining writes are just gonna be cache hits that don't result in any bus cache hits that don't result in any bus cache hits that don't result in any bus traffic that is good why would an traffic that is good why would an traffic that is good why would an application do abrasive writes to one application do abrasive writes to one application do abrasive writes to one address well because when we are address well because when we are address well because when we are computing something we may need to computing something we may need to computing something we may need to update it several times until we get to update it several times until we get to update it several times until we get to the final result another common behavior the final result another common behavior the final result another common behavior is when an application writes two is when an application writes two is when an application writes two different words in the same block for different words in the same block for different words in the same block for example when we are initializing the example when we are initializing the example when we are initializing the block we will write to every word in block we will write to every word in block we will write to every word in that block if that block is shared an that block if that block is shared an that block if that block is shared an update protocol will send an update for update protocol will send an update for update protocol will send an update for each word that we write that is bad each word that we write that is bad each word that we write that is bad because it means that one cache line because it means that one cache line because it means that one cache line worth of writes might result in worth of writes might result in worth of writes might result in something like 10 updates in an something like 10 updates in an something like 10 updates in an invalidation based protocol the first invalidation based protocol the first invalidation based protocol the first write that we have is gonna invalidate write that we have is gonna invalidate write that we have is gonna invalidate the other copies and then the remaining the other copies and then the remaining the other copies and then the remaining accesses to the block to the same or accesses to the block to the same or accesses to the block to the same or different words are gonna be just cache different words are gonna be just cache different words are gonna be just cache hits that don't result in bus traffic so hits that don't result in bus traffic so hits that don't result in bus traffic so this is again good for the invalidate this is again good for the invalidate this is again good for the invalidate bad for the update and the final bad for the update and the final bad for the update and the final scenario we will consider is the scenario we will consider is the scenario we will consider is the producer-consumer scenario where one producer-consumer scenario where one producer-consumer scenario where one core keeps writing data and another core core keeps writing data and another core core keeps writing data and another core is supposed to read it in this case for is supposed to read it in this case for is supposed to read it in this case for the update protocol the producer will the update protocol the producer will the update protocol the producer will send updates every time it modifies the send updates every time it modifies the send updates every time it modifies the data and the consumer then finds the data and the consumer then finds the data and the consumer then finds the data in its cache for example if there data in its cache for example if there data in its cache for example if there is a buffer the first pass through the is a buffer the first pass through the is a buffer the first pass through the buffer will put it in the cache and then buffer will put it in the cache and then buffer will put it in the cache and then reading the buffer keeps it in the cache reading the buffer keeps it in the cache reading the buffer keeps it in the cache and here writes are gonna put new data and here writes are gonna put new data and here writes are gonna put new data in the buffer so that the consumer just in the buffer so that the consumer just in the buffer so that the consumer just keeps getting it from its own keeps getting it from its own keeps getting it from its own - this is not great the producer sends - this is not great the producer sends - this is not great the producer sends updates but it's kind of the best we can updates but it's kind of the best we can updates but it's kind of the best we can do because the data really has to get do because the data really has to get do because the data really has to get from one place to the other so this is from one place to the other so this is from one place to the other so this is about as good as this scenario gets in about as good as this scenario gets in about as good as this scenario gets in an invalidation based protocol however an invalidation based protocol however an invalidation based protocol however the producer will do a write that the producer will do a write that the producer will do a write that invalidates the readers copy so when the invalidates the readers copy so when the invalidates the readers copy so when the reader the consumer wants to read it's reader the consumer wants to read it's reader the consumer wants to read it's gonna be a cache miss and it has to get gonna be a cache miss and it has to get gonna be a cache miss and it has to get the data from the writer and then a new the data from the writer and then a new the data from the writer and then a new write will happen and invalidate the write will happen and invalidate the write will happen and invalidate the copy and so on so this is about as bad copy and so on so this is about as bad copy and so on so this is about as bad as it gets for this scenario because not as it gets for this scenario because not as it gets for this scenario because not only do we create bus traffic when we only do we create bus traffic when we only do we create bus traffic when we invalidate we then have to have a cache invalidate we then have to have a cache invalidate we then have to have a cache miss and have another bus operation to miss and have another bus operation to miss and have another bus operation to get the data so as we can see from this get the data so as we can see from this get the data so as we can see from this table update and invalidate based table update and invalidate based table update and invalidate based protocols have different advantages so protocols have different advantages so protocols have different advantages so which one do we choose in practice well which one do we choose in practice well which one do we choose in practice well it turns out that all modern processors it turns out that all modern processors it turns out that all modern processors use the invalidate type protocol but not use the invalidate type protocol but not use the invalidate type protocol but not exactly for these reasons as we can see exactly for these reasons as we can see exactly for these reasons as we can see invalidate based protocols have some invalidate based protocols have some invalidate based protocols have some strengths and some weaknesses overall strengths and some weaknesses overall strengths and some weaknesses overall they tend to be slightly stronger than they tend to be slightly stronger than they tend to be slightly stronger than update on average but that's not really update on average but that's not really update on average but that's not really the knockout punch that makes them win the knockout punch that makes them win the knockout punch that makes them win handily what really makes them win with handily what really makes them win with handily what really makes them win with no contest is when a thread moves to no contest is when a thread moves to no contest is when a thread moves to another core so a thread was using one another core so a thread was using one another core so a thread was using one core and then simply the operating core and then simply the operating core and then simply the operating system decides to move it to another system decides to move it to another system decides to move it to another core so what happens in the update core so what happens in the update core so what happens in the update protocol is that it now will keep protocol is that it now will keep protocol is that it now will keep updating the old course cache note that updating the old course cache note that updating the old course cache note that our working set was in one cache now it our working set was in one cache now it our working set was in one cache now it moved to another one but in reality the moved to another one but in reality the moved to another one but in reality the other cache still has blocks that other cache still has blocks that other cache still has blocks that correspond to our data so what happens correspond to our data so what happens correspond to our data so what happens is all the rights that we make in a is all the rights that we make in a is all the rights that we make in a near-future near-future near-future are gonna be updating the other course are gonna be updating the other course are gonna be updating the other course cache until that cache replaces those cache until that cache replaces those cache until that cache replaces those blocks by running another core for blocks by running another core for blocks by running another core for example this is pretty horrible because example this is pretty horrible because example this is pretty horrible because a thread movement ideally what would a thread movement ideally what would a thread movement ideally what would happen is we would just move the data happen is we would just move the data happen is we would just move the data over and continue working privately on over and continue working privately on over and continue working privately on on our private data so let's see how on our private data so let's see how on our private data so let's see how this horrible scenario for updates works this horrible scenario for updates works this horrible scenario for updates works when we have invalidates in that case when we have invalidates in that case when we have invalidates in that case the first write to each of the the first write to each of the the first write to each of the blocks will have to invalidate the old blocks will have to invalidate the old blocks will have to invalidate the old copy but after that initial period there copy but after that initial period there copy but after that initial period there will be no traffic anymore will be no traffic anymore will be no traffic anymore so this is about as good as it gets so this is about as good as it gets so this is about as good as it gets without actually taking the content of without actually taking the content of without actually taking the content of the old cache and copying it explicitly the old cache and copying it explicitly the old cache and copying it explicitly to the new cache and again this is to the new cache and again this is to the new cache and again this is pretty common operating system behavior pretty common operating system behavior pretty common operating system behavior so we really don't want it to perform so we really don't want it to perform so we really don't want it to perform horribly and because invalidate based horribly and because invalidate based horribly and because invalidate based protocols are commonly used we will now protocols are commonly used we will now protocols are commonly used we will now look at invalidation based protocols for look at invalidation based protocols for look at invalidation based protocols for the rest of this lesson

## MSI Coherence： Part 5
Kind: captions Language: en what we will look at now is one of the what we will look at now is one of the what we will look at now is one of the simpler coherence protocols called MSI simpler coherence protocols called MSI simpler coherence protocols called MSI in MSI a block can be in one of three in MSI a block can be in one of three in MSI a block can be in one of three states in a cache I stands for the states in a cache I stands for the states in a cache I stands for the invalid State a block is in an invalid invalid State a block is in an invalid invalid State a block is in an invalid State either when it is present in the State either when it is present in the State either when it is present in the cache but its valid bit would not be set cache but its valid bit would not be set cache but its valid bit would not be set or when it's simply not present in the or when it's simply not present in the or when it's simply not present in the cach so a block that is not present in cach so a block that is not present in cach so a block that is not present in the cach is treated like it is present the cach is treated like it is present the cach is treated like it is present but in an invalid State s stands for the but in an invalid State s stands for the but in an invalid State s stands for the shared State a block that is in the shared State a block that is in the shared State a block that is in the shared state in a cache can be read shared state in a cache can be read shared state in a cache can be read without any further Ado but if we write without any further Ado but if we write without any further Ado but if we write it we have to do something a local read it we have to do something a local read it we have to do something a local read to a block that is in the shared State to a block that is in the shared State to a block that is in the shared State keeps the Block in the shared State and keeps the Block in the shared State and keeps the Block in the shared State and doesn't do anything else and finally m doesn't do anything else and finally m doesn't do anything else and finally m stands for the modified state in this stands for the modified state in this stands for the modified state in this state we can read the block just like in state we can read the block just like in state we can read the block just like in the shared State without sending the shared State without sending the shared State without sending anything out and we can also write to anything out and we can also write to anything out and we can also write to this block by the local core without this block by the local core without this block by the local core without sending anything out now because this is sending anything out now because this is sending anything out now because this is an invalidation based protocol what an invalidation based protocol what an invalidation based protocol what happens is in the modify State we are happens is in the modify State we are happens is in the modify State we are sure that there are no other cached sure that there are no other cached sure that there are no other cached copies which is why we are allowed to copies which is why we are allowed to copies which is why we are allowed to locally write we don't have to tell locally write we don't have to tell locally write we don't have to tell anybody that we are writing because we anybody that we are writing because we anybody that we are writing because we know there is nobody so this is sort of know there is nobody so this is sort of know there is nobody so this is sort of equivalent to having valid equals 1 and equivalent to having valid equals 1 and equivalent to having valid equals 1 and dirty equals 1 dirty equals 1 dirty equals 1 in a normal cache we just call this in a normal cache we just call this in a normal cache we just call this state modified because it's easier to state modified because it's easier to state modified because it's easier to understand what's going on this way then understand what's going on this way then understand what's going on this way then if we have to look at individual bits if we have to look at individual bits if we have to look at individual bits and we will see that with more and we will see that with more and we will see that with more complicated coherence protocols we will complicated coherence protocols we will complicated coherence protocols we will add maybe a state or two but we would add maybe a state or two but we would add maybe a state or two but we would need to add several bits in order to need to add several bits in order to need to add several bits in order to track what's going on so now in the track what's going on so now in the track what's going on so now in the invalid State we have what's equivalent invalid State we have what's equivalent invalid State we have what's equivalent to valid bit being zero pretty much we to valid bit being zero pretty much we to valid bit being zero pretty much we behave like we don't have the block behave like we don't have the block behave like we don't have the block either because we really don't have it either because we really don't have it either because we really don't have it or because we have it or because we have it or because we have it but we are not supposed to read it or but we are not supposed to read it or but we are not supposed to read it or write it so when we have a write and the write it so when we have a write and the write it so when we have a write and the block is in the invalid State we move block is in the invalid State we move block is in the invalid State we move the block to the modified state so when the block to the modified state so when the block to the modified state so when we have a local write meaning the local we have a local write meaning the local we have a local write meaning the local processor wants to write and the block processor wants to write and the block processor wants to write and the block is in the invalid State we move the is in the invalid State we move the is in the invalid State we move the block to the modified state but we block to the modified state but we block to the modified state but we cannot just do that to do that we have cannot just do that to do that we have cannot just do that to do that we have to put the right request on the bus so to put the right request on the bus so to put the right request on the bus so pretty much this is a right Miss we have pretty much this is a right Miss we have pretty much this is a right Miss we have to tell others that it's a right Miss we to tell others that it's a right Miss we to tell others that it's a right Miss we get the data and now we can put it in get the data and now we can put it in get the data and now we can put it in the modified State why well because our the modified State why well because our the modified State why well because our right request on the bus pretty much right request on the bus pretty much right request on the bus pretty much made sure that nobody else has a copy made sure that nobody else has a copy made sure that nobody else has a copy anymore cuz the right request that we anymore cuz the right request that we anymore cuz the right request that we made invalidated everybody else if we made invalidated everybody else if we made invalidated everybody else if we have a block in the modified State and have a block in the modified State and have a block in the modified State and we observe on the bus that another cach we observe on the bus that another cach we observe on the bus that another cach is placing a right on the bus then we is placing a right on the bus then we is placing a right on the bus then we will go to the invalid state so we do will go to the invalid state so we do will go to the invalid state so we do this when we Snoop the right on the bus this when we Snoop the right on the bus this when we Snoop the right on the bus and we cannot just do this and be done and we cannot just do this and be done and we cannot just do this and be done when we Snoop a right on the bus because when we Snoop a right on the bus because when we Snoop a right on the bus because we are in the modified state where we we are in the modified state where we we are in the modified state where we have the dirty copy meaning really the have the dirty copy meaning really the have the dirty copy meaning really the only fresh copy of the data in the only fresh copy of the data in the only fresh copy of the data in the system is in our cache it's not even in system is in our cache it's not even in system is in our cache it's not even in memory cuz it's dirty here we have to memory cuz it's dirty here we have to memory cuz it's dirty here we have to write the block back as we're write the block back as we're write the block back as we're transitioning to invalid and we have to transitioning to invalid and we have to transitioning to invalid and we have to delay the data that would be sent to the delay the data that would be sent to the delay the data that would be sent to the right request until we are done with the right request until we are done with the right request until we are done with the right back this can be done in two ways right back this can be done in two ways right back this can be done in two ways one is to actually cancel that request one is to actually cancel that request one is to actually cancel that request write back move to invalid and then that write back move to invalid and then that write back move to invalid and then that request gets repeated and now gets the request gets repeated and now gets the request gets repeated and now gets the data from memory another one is when we data from memory another one is when we data from memory another one is when we see a write on the bus if it's see a write on the bus if it's see a write on the bus if it's requesting data then what we do is we requesting data then what we do is we requesting data then what we do is we suppress the memory response and feed suppress the memory response and feed suppress the memory response and feed the data to that processor the simpler the data to that processor the simpler the data to that processor the simpler way of doing things is just to delay way of doing things is just to delay way of doing things is just to delay that request right back and then let it that request right back and then let it that request right back and then let it proceed but either way we have to make proceed but either way we have to make proceed but either way we have to make sure that before we move from modifi to sure that before we move from modifi to sure that before we move from modifi to invalid we write back the data to memory invalid we write back the data to memory invalid we write back the data to memory and also make sure that the right and also make sure that the right and also make sure that the right request if it is a right Miss and needs request if it is a right Miss and needs request if it is a right Miss and needs the data gets our new data not the data the data gets our new data not the data the data gets our new data not the data from memory now if we Snoop a read on from memory now if we Snoop a read on from memory now if we Snoop a read on the bus and we are in the modified State the bus and we are in the modified State the bus and we are in the modified State then we move to the shared state so we then we move to the shared state so we then we move to the shared state so we have snooped a read on the bus we will have snooped a read on the bus we will have snooped a read on the bus we will now not be the only one who has a copy now not be the only one who has a copy now not be the only one who has a copy of the data that means we will not be of the data that means we will not be of the data that means we will not be able to write without telling others so able to write without telling others so able to write without telling others so we will move to the shared state we will move to the shared state we will move to the shared state just like here we have to also make sure just like here we have to also make sure just like here we have to also make sure that the block is written back the that the block is written back the that the block is written back the shared state is equivalent to having V shared state is equivalent to having V shared state is equivalent to having V equals 1 and D equal Z so we will not be equals 1 and D equal Z so we will not be equals 1 and D equal Z so we will not be doing a write back from our shared State doing a write back from our shared State doing a write back from our shared State cuz it only allows reads anyway so as we cuz it only allows reads anyway so as we cuz it only allows reads anyway so as we are moving from the dirty to a clean are moving from the dirty to a clean are moving from the dirty to a clean State modified to Shared we're going to State modified to Shared we're going to State modified to Shared we're going to write back the data the reading write back the data the reading write back the data the reading processor wants a copy of this block processor wants a copy of this block processor wants a copy of this block again we have to either the delay that again we have to either the delay that again we have to either the delay that request do a right back change to the request do a right back change to the request do a right back change to the shared State and then let that processor shared State and then let that processor shared State and then let that processor proceed with a read or we have to proceed with a read or we have to proceed with a read or we have to suppress the memory response that would suppress the memory response that would suppress the memory response that would happen as a result of this read and happen as a result of this read and happen as a result of this read and provide our data instead as we are provide our data instead as we are provide our data instead as we are transitioning to the shared state if the transitioning to the shared state if the transitioning to the shared state if the processor reads a block and the block is processor reads a block and the block is processor reads a block and the block is in the invalid State then the block will in the invalid State then the block will in the invalid State then the block will be moved to a shared state so in be moved to a shared state so in be moved to a shared state so in response to a local read we put a read response to a local read we put a read response to a local read we put a read request on the bus and then we get the request on the bus and then we get the request on the bus and then we get the data and then we are in the shared state data and then we are in the shared state data and then we are in the shared state for that data note that the data might for that data note that the data might for that data note that the data might come from memory or it may come from come from memory or it may come from come from memory or it may come from another cache because it had the data in another cache because it had the data in another cache because it had the data in the modified State and we needed to get the modified State and we needed to get the modified State and we needed to get that data but as far as this local cache that data but as far as this local cache that data but as far as this local cache is concerned it puts a read request on is concerned it puts a read request on is concerned it puts a read request on the bus it gets the data and then it the bus it gets the data and then it the bus it gets the data and then it puts that block in the shared state in puts that block in the shared state in puts that block in the shared state in the local cache then we can do many the local cache then we can do many the local cache then we can do many reads without telling anybody if we have reads without telling anybody if we have reads without telling anybody if we have a block in the shared state and we a block in the shared state and we a block in the shared state and we observe a snoop right on the bus then we observe a snoop right on the bus then we observe a snoop right on the bus then we move the block to the invalid State we move the block to the invalid State we move the block to the invalid State we don't have to do anything because the don't have to do anything because the don't have to do anything because the block is clean as far as we are block is clean as far as we are block is clean as far as we are concerned we don't have to write it back concerned we don't have to write it back concerned we don't have to write it back we simply have to do the equivalent of we simply have to do the equivalent of we simply have to do the equivalent of resetting the valid bit and move to the resetting the valid bit and move to the resetting the valid bit and move to the invalid state so there is nothing really invalid state so there is nothing really invalid state so there is nothing really that we have to do except change the that we have to do except change the that we have to do except change the state so that next time we try to read state so that next time we try to read state so that next time we try to read it we can no longer read it here we have it we can no longer read it here we have it we can no longer read it here we have to put a read request on the bus again to put a read request on the bus again to put a read request on the bus again if we Snoop a read on the bus in the if we Snoop a read on the bus in the if we Snoop a read on the bus in the shared state we don't need to do shared state we don't need to do shared state we don't need to do anything why well because our shared anything why well because our shared anything why well because our shared state is okay with there being other state is okay with there being other state is okay with there being other sharers that are only going to read the sharers that are only going to read the sharers that are only going to read the data and finally although local reads data and finally although local reads data and finally although local reads are okay in the shared state if we have are okay in the shared state if we have are okay in the shared state if we have a local right that is not allowed in the a local right that is not allowed in the a local right that is not allowed in the shared state so a local right that finds shared state so a local right that finds shared state so a local right that finds the Block in the shared state will make the Block in the shared state will make the Block in the shared state will make us move the block to a modified state us move the block to a modified state us move the block to a modified state but we cannot do that silently we cannot but we cannot do that silently we cannot but we cannot do that silently we cannot just move the block and write to it what just move the block and write to it what just move the block and write to it what we have to do is put an invalidation on we have to do is put an invalidation on we have to do is put an invalidation on the bus know that things are slightly the bus know that things are slightly the bus know that things are slightly different when we move from invalid different when we move from invalid different when we move from invalid State on a local right to modified and State on a local right to modified and State on a local right to modified and when we moved from shared state to a when we moved from shared state to a when we moved from shared state to a modified State on a local right here we modified State on a local right here we modified State on a local right here we put a right request that asks for a put a right request that asks for a put a right request that asks for a block so that we can write it here we block so that we can write it here we block so that we can write it here we already have the block this is the most already have the block this is the most already have the block this is the most upto-date copy because if there was upto-date copy because if there was upto-date copy because if there was another right after that we would not another right after that we would not another right after that we would not have it in the shared state so now we have it in the shared state so now we have it in the shared state so now we want to write to it we already have the want to write to it we already have the want to write to it we already have the data we just need to put an invalidation data we just need to put an invalidation data we just need to put an invalidation request on the bus so this invalidation request on the bus so this invalidation request on the bus so this invalidation request does not actually request the request does not actually request the request does not actually request the data it just puts a request on the bus data it just puts a request on the bus data it just puts a request on the bus for everybody to invalidate their copies for everybody to invalidate their copies for everybody to invalidate their copies but it's not asking for the data so but it's not asking for the data so but it's not asking for the data so pretty much as soon as we put risk pretty much as soon as we put risk pretty much as soon as we put risk request on the bus we can write because request on the bus we can write because request on the bus we can write because by putting an invalidation on the bus we by putting an invalidation on the bus we by putting an invalidation on the bus we have made sure that now everybody is have made sure that now everybody is have made sure that now everybody is invalidated at which point we are in the invalidated at which point we are in the invalidated at which point we are in the modified State now of course in the modified State now of course in the modified State now of course in the invalid State whatever we Snoop for this invalid State whatever we Snoop for this invalid State whatever we Snoop for this block on the bus doesn't matter we're block on the bus doesn't matter we're block on the bus doesn't matter we're going to stay in the invalid State also going to stay in the invalid State also going to stay in the invalid State also in the shared state it doesn't matter in the shared state it doesn't matter in the shared state it doesn't matter whether we Snoop somebody's request to whether we Snoop somebody's request to whether we Snoop somebody's request to write or an invalidation that they put write or an invalidation that they put write or an invalidation that they put on the bus we still move to the invalid on the bus we still move to the invalid on the bus we still move to the invalid State without doing much and finally State without doing much and finally State without doing much and finally note that if we are in a modified State note that if we are in a modified State note that if we are in a modified State we will observe a right if somebody we will observe a right if somebody we will observe a right if somebody wants to write and not an invalidation wants to write and not an invalidation wants to write and not an invalidation because if we are in the modified state because if we are in the modified state because if we are in the modified state that means that everybody else is in the that means that everybody else is in the that means that everybody else is in the invalid state for this block so if they invalid state for this block so if they invalid state for this block so if they want to write they'll have to put a want to write they'll have to put a want to write they'll have to put a right request on the bus they cannot be right request on the bus they cannot be right request on the bus they cannot be in the shared State and put just an in the shared State and put just an in the shared State and put just an invalidation out

## Cache To Cache Transfers： Part 5
Kind: captions Language: en so let's look more closely at what is so let's look more closely at what is so let's look more closely at what is called cash to cash transfers which is called cash to cash transfers which is called cash to cash transfers which is what happens when Corwin has some Block what happens when Corwin has some Block what happens when Corwin has some Block B in a modified state and another core B in a modified state and another core B in a modified state and another core puts a read request on the bus so that puts a read request on the bus so that puts a read request on the bus so that it wants to basically bring the Block in it wants to basically bring the Block in it wants to basically bring the Block in its own cash at this point c1 has to its own cash at this point c1 has to its own cash at this point c1 has to react because c1 is the one that has to react because c1 is the one that has to react because c1 is the one that has to provide the data if we have the Block in provide the data if we have the Block in provide the data if we have the Block in the M state that means that the only the M state that means that the only the M state that means that the only up-to-date copy in the system is our up-to-date copy in the system is our up-to-date copy in the system is our copy even the memory doesn't have an copy even the memory doesn't have an copy even the memory doesn't have an up-to-date copy so if the data is going up-to-date copy so if the data is going up-to-date copy so if the data is going to be sent to see to it had better be to be sent to see to it had better be to be sent to see to it had better be our data but how and we have briefly our data but how and we have briefly our data but how and we have briefly touched on this when we were discussing touched on this when we were discussing touched on this when we were discussing the MSI protocol there are really two the MSI protocol there are really two the MSI protocol there are really two solutions the first solution is aboard solutions the first solution is aboard solutions the first solution is aboard and retry the idea here is that c1 and retry the idea here is that c1 and retry the idea here is that c1 somehow cancels c2 request and for that somehow cancels c2 request and for that somehow cancels c2 request and for that we need some sort of an abort bus signal we need some sort of an abort bus signal we need some sort of an abort bus signal that when a request is placed on the bus that when a request is placed on the bus that when a request is placed on the bus another core can assert the abort signal another core can assert the abort signal another core can assert the abort signal that tells the requesting core to kind that tells the requesting core to kind that tells the requesting core to kind of back off and try again later of back off and try again later of back off and try again later now that c2 request has been aborted c1 now that c2 request has been aborted c1 now that c2 request has been aborted c1 can do a normal right back to memory at can do a normal right back to memory at can do a normal right back to memory at this point the memory has the up-to-date this point the memory has the up-to-date this point the memory has the up-to-date data and when c2 retries its read data and when c2 retries its read data and when c2 retries its read request it will get the data from memory request it will get the data from memory request it will get the data from memory the problem with this approach is that the problem with this approach is that the problem with this approach is that really from the time c2 makes the really from the time c2 makes the really from the time c2 makes the request if this data was normally coming request if this data was normally coming request if this data was normally coming from memory we would have a memory from memory we would have a memory from memory we would have a memory latency so if this is just a read miss latency so if this is just a read miss latency so if this is just a read miss we will have a memory latency but if it we will have a memory latency but if it we will have a memory latency but if it is a read miss where another core has is a read miss where another core has is a read miss where another core has the data we really have to have two the data we really have to have two the data we really have to have two memory latencies before c2 can get the memory latencies before c2 can get the memory latencies before c2 can get the data one for the write back to memory to data one for the write back to memory to data one for the write back to memory to happen on c1 and then for us to actually happen on c1 and then for us to actually happen on c1 and then for us to actually get the data from memory so the read get the data from memory so the read get the data from memory so the read miss from c1 has a latency that is twice miss from c1 has a latency that is twice miss from c1 has a latency that is twice the memory latency plus some so this is the memory latency plus some so this is the memory latency plus some so this is why this approach is not ideal the why this approach is not ideal the why this approach is not ideal the second solution is called intervention second solution is called intervention second solution is called intervention in this case C one sees the read request in this case C one sees the read request in this case C one sees the read request is being made is being made is being made and it tells the memory somehow that c1 and it tells the memory somehow that c1 and it tells the memory somehow that c1 will supply the data instead of the will supply the data instead of the will supply the data instead of the memory so normally the memory would memory so normally the memory would memory so normally the memory would respond to a read miss request now c1 respond to a read miss request now c1 respond to a read miss request now c1 kind of tells memory that I will do that kind of tells memory that I will do that kind of tells memory that I will do that and for that we need an intervention bus and for that we need an intervention bus and for that we need an intervention bus signal through which c1 by asserting the signal through which c1 by asserting the signal through which c1 by asserting the intervention signal signals to the intervention signal signals to the intervention signal signals to the memory not to respond so the memory now memory not to respond so the memory now memory not to respond so the memory now doesn't respond with the data but c1 doesn't respond with the data but c1 doesn't respond with the data but c1 does and now the memory must pick up the does and now the memory must pick up the does and now the memory must pick up the data that c1 was responding with in data that c1 was responding with in data that c1 was responding with in order to put it in the appropriate order to put it in the appropriate order to put it in the appropriate memory block the reason the memory has memory block the reason the memory has memory block the reason the memory has to pick up the data is that once c1 to pick up the data is that once c1 to pick up the data is that once c1 responds with the data both c1 and c2 responds with the data both c1 and c2 responds with the data both c1 and c2 will have the Block in the shared state will have the Block in the shared state will have the Block in the shared state so both of them will think that the so both of them will think that the so both of them will think that the block is not dirty and that means that block is not dirty and that means that block is not dirty and that means that if the memory doesn't get the data now if the memory doesn't get the data now if the memory doesn't get the data now it will never get the fresh data so by it will never get the fresh data so by it will never get the fresh data so by picking up the data the memory ensures picking up the data the memory ensures picking up the data the memory ensures that it gets the data that will not be that it gets the data that will not be that it gets the data that will not be written back anymore so the disadvantage written back anymore so the disadvantage written back anymore so the disadvantage of this approach is that it needs more of this approach is that it needs more of this approach is that it needs more complex Hardware now a cache needs to be complex Hardware now a cache needs to be complex Hardware now a cache needs to be able to kind of insert its data into able to kind of insert its data into able to kind of insert its data into what would normally be the memory what would normally be the memory what would normally be the memory sending data and also the memory has to sending data and also the memory has to sending data and also the memory has to have enough intelligence to pick up the have enough intelligence to pick up the have enough intelligence to pick up the data when a core is responding to an data when a core is responding to an data when a core is responding to an intervention request instead of just intervention request instead of just intervention request instead of just picking up the data when there is a picking up the data when there is a picking up the data when there is a write back request in modern processors write back request in modern processors write back request in modern processors we nowadays mostly use a variant of an we nowadays mostly use a variant of an we nowadays mostly use a variant of an intervention approach and the reason is intervention approach and the reason is intervention approach and the reason is that yes the hardware is slightly more that yes the hardware is slightly more that yes the hardware is slightly more complex but fancier snooping protocols complex but fancier snooping protocols complex but fancier snooping protocols actually eliminate the problem of memory actually eliminate the problem of memory actually eliminate the problem of memory picking up the data and the complexity picking up the data and the complexity picking up the data and the complexity to respond with the data is not too to respond with the data is not too to respond with the data is not too large in contrast here the performance large in contrast here the performance large in contrast here the performance suffers so pretty much between having a suffers so pretty much between having a suffers so pretty much between having a fancier protocol and having enough fancier protocol and having enough fancier protocol and having enough transistors from Moore's Law to actually transistors from Moore's Law to actually transistors from Moore's Law to actually add this capability to caches we pretty add this capability to caches we pretty add this capability to caches we pretty much can do this and not have to suffer much can do this and not have to suffer much can do this and not have to suffer in performance like this

## MSI Quiz： Part 5
Kind: captions Language: en so let's see if we can do some MSI so let's see if we can do some MSI so let's see if we can do some MSI protocol transitions let's say we have protocol transitions let's say we have protocol transitions let's say we have two cores with private caches and that two cores with private caches and that two cores with private caches and that the caches are using MSI to maintain the caches are using MSI to maintain the caches are using MSI to maintain coherence initially block X is in memory coherence initially block X is in memory coherence initially block X is in memory and no cache has it so here are the and no cache has it so here are the and no cache has it so here are the actions of the caches in C1 and c2's actions of the caches in C1 and c2's actions of the caches in C1 and c2's cache core One issues a redex mean while cache core One issues a redex mean while cache core One issues a redex mean while core2 doesn't do anything what is the core2 doesn't do anything what is the core2 doesn't do anything what is the state of block X in C1 cache after this state of block X in C1 cache after this state of block X in C1 cache after this read is completed and what is the state read is completed and what is the state read is completed and what is the state of the block X in c2's Cache after this of the block X in c2's Cache after this of the block X in c2's Cache after this read on C1 is completed so put your Ms read on C1 is completed so put your Ms read on C1 is completed so put your Ms or I State here and another one here now or I State here and another one here now or I State here and another one here now let's say that the next axis is that C2 let's say that the next axis is that C2 let's say that the next axis is that C2 redex after this is completed what is redex after this is completed what is redex after this is completed what is the new state of X in c1s and c2's cache the new state of X in c1s and c2's cache the new state of X in c1s and c2's cache and finally C1 does another axis right x and finally C1 does another axis right x and finally C1 does another axis right x what is now the state in C1 cache after what is now the state in C1 cache after what is now the state in C1 cache after this right is complete and what's the this right is complete and what's the this right is complete and what's the state in c2's Cache after this right on state in c2's Cache after this right on state in c2's Cache after this right on C1 is complete

## MSI Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our MSI let's look at the solution to our MSI let's look at the solution to our MSI access quiz we have two cores with access quiz we have two cores with access quiz we have two cores with private caches and MSI coherence private caches and MSI coherence private caches and MSI coherence initially block X is in memory which initially block X is in memory which initially block X is in memory which means that it's really invalid in both means that it's really invalid in both means that it's really invalid in both of the caches after we read X in C once of the caches after we read X in C once of the caches after we read X in C once cache see one's cache will transition cache see one's cache will transition cache see one's cache will transition this block from I to s the shared state this block from I to s the shared state this block from I to s the shared state because this was a read we move to the because this was a read we move to the because this was a read we move to the shared state even though it is not shared state even though it is not shared state even though it is not really shared this is our way of saying really shared this is our way of saying really shared this is our way of saying basically that the block is clean and basically that the block is clean and basically that the block is clean and that we only want to read it in c2s that we only want to read it in c2s that we only want to read it in c2s cache it stays invalid when c2 then does cache it stays invalid when c2 then does cache it stays invalid when c2 then does a read it also gets the Block in the a read it also gets the Block in the a read it also gets the Block in the shared state and if we snoop a read shared state and if we snoop a read shared state and if we snoop a read request in the shared State we stay in request in the shared State we stay in request in the shared State we stay in the shared state so finally when c1 the shared state so finally when c1 the shared state so finally when c1 writes to X because it has the Block in writes to X because it has the Block in writes to X because it has the Block in the shared state it needs to send an the shared state it needs to send an the shared state it needs to send an invalidation out that results in c2 invalidation out that results in c2 invalidation out that results in c2 moving the block to the invalid state it moving the block to the invalid state it moving the block to the invalid state it can no longer be read until we get the can no longer be read until we get the can no longer be read until we get the new copy from C 1 and C 1 after writing new copy from C 1 and C 1 after writing new copy from C 1 and C 1 after writing has the Block in the modified State

## MSI Quiz 2： Part 5
Kind: captions Language: en let's now do another quiz on MSI same let's now do another quiz on MSI same let's now do another quiz on MSI same thing to course private caches MSI thing to course private caches MSI thing to course private caches MSI coherence block X initially in memory coherence block X initially in memory coherence block X initially in memory but now c1 is reading and then c2 is but now c1 is reading and then c2 is but now c1 is reading and then c2 is writing and then c1 writes so what is writing and then c1 writes so what is writing and then c1 writes so what is the state of C ones and c2s cache after the state of C ones and c2s cache after the state of C ones and c2s cache after each of these things occurs

## MSI Quiz 2 Solution： Part 5
Kind: captions Language: en let's look at the solution to our MSI let's look at the solution to our MSI let's look at the solution to our MSI quiz Redux results in the same behavior quiz Redux results in the same behavior quiz Redux results in the same behavior as in the previous quiz both of them as in the previous quiz both of them as in the previous quiz both of them have the state as I and then when readex have the state as I and then when readex have the state as I and then when readex occurs in C ones cache it sends out the occurs in C ones cache it sends out the occurs in C ones cache it sends out the read request gets the block from memory read request gets the block from memory read request gets the block from memory and now it has the Block in the shared and now it has the Block in the shared and now it has the Block in the shared state c2 still has the Block in the state c2 still has the Block in the state c2 still has the Block in the invalid state invalid state invalid state now when c2 writes to X it sends out a now when c2 writes to X it sends out a now when c2 writes to X it sends out a write miss request it will get the data write miss request it will get the data write miss request it will get the data and then write to its cache so it gets and then write to its cache so it gets and then write to its cache so it gets the block now in the modified state c1 the block now in the modified state c1 the block now in the modified state c1 meanwhile transitions from the shared meanwhile transitions from the shared meanwhile transitions from the shared state to the invalid state because it state to the invalid state because it state to the invalid state because it observes the write requests sent by c2 observes the write requests sent by c2 observes the write requests sent by c2 and then when C one wants to write it and then when C one wants to write it and then when C one wants to write it does pretty much the same thing as c2 does pretty much the same thing as c2 does pretty much the same thing as c2 just did it puts a write miss request on just did it puts a write miss request on just did it puts a write miss request on the bus gets the block rights to it puts the bus gets the block rights to it puts the bus gets the block rights to it puts it in the modified State it in the modified State it in the modified State meanwhile c2 has to intervene and meanwhile c2 has to intervene and meanwhile c2 has to intervene and provide the data that c1 gets and then provide the data that c1 gets and then provide the data that c1 gets and then because we snoop the right we need to because we snoop the right we need to because we snoop the right we need to transition to an invalid state note that transition to an invalid state note that transition to an invalid state note that if a block is in M state in any cache if a block is in M state in any cache if a block is in M state in any cache all the other caches have to have that all the other caches have to have that all the other caches have to have that block in the invalid state if a cache block in the invalid state if a cache block in the invalid state if a cache has a block in a shared state all the has a block in a shared state all the has a block in a shared state all the other caches need to have the Block in other caches need to have the Block in other caches need to have the Block in either the shared or the invalid caches either the shared or the invalid caches either the shared or the invalid caches so pretty much we can have one has M or so pretty much we can have one has M or so pretty much we can have one has M or others have I or we have several can others have I or we have several can others have I or we have several can have s and the rest have I we cannot have s and the rest have I we cannot have s and the rest have I we cannot have m and s in the system at the same have m and s in the system at the same have m and s in the system at the same time for the same block and definitely time for the same block and definitely time for the same block and definitely we cannot have mmm

## Avoiding Memory Writes On Cache to Cache Transfers： Part 5
Kind: captions Language: en so now we have seen that in MSI even so now we have seen that in MSI even so now we have seen that in MSI even when we have an intervention we need to when we have an intervention we need to when we have an intervention we need to write to memory every time we have a write to memory every time we have a write to memory every time we have a cache to cach transfer so for example C1 cache to cach transfer so for example C1 cache to cach transfer so for example C1 has the Block in the M State cuz it just has the Block in the M State cuz it just has the Block in the M State cuz it just wrote it C2 wants to read the data so it wrote it C2 wants to read the data so it wrote it C2 wants to read the data so it puts a read request on the bus C1 puts a read request on the bus C1 puts a read request on the bus C1 responds with the data so now C2 has the responds with the data so now C2 has the responds with the data so now C2 has the data but C1 can no longer just write to data but C1 can no longer just write to data but C1 can no longer just write to this block so C1 gets the block in the this block so C1 gets the block in the this block so C1 gets the block in the shared State C2 gets the Block in the shared State C2 gets the Block in the shared State C2 gets the Block in the shared State and that's why the memory shared State and that's why the memory shared State and that's why the memory had to grab the data when C1 was had to grab the data when C1 was had to grab the data when C1 was responding now what can happen is C2 responding now what can happen is C2 responding now what can happen is C2 writes so it sends an invalidation cuz writes so it sends an invalidation cuz writes so it sends an invalidation cuz it already has the data now C1 gets it already has the data now C1 gets it already has the data now C1 gets invalid State C2 gets the modified State invalid State C2 gets the modified State invalid State C2 gets the modified State and then let's say this read write and then let's say this read write and then let's say this read write Behavior moves to another core like C1 Behavior moves to another core like C1 Behavior moves to another core like C1 so pretty much if one core reads and so pretty much if one core reads and so pretty much if one core reads and writes then another one reads and writes writes then another one reads and writes writes then another one reads and writes and so on we can have a repetition of and so on we can have a repetition of and so on we can have a repetition of this so pretty much the data is kind of this so pretty much the data is kind of this so pretty much the data is kind of moving around the caches but the problem moving around the caches but the problem moving around the caches but the problem is every time it moves between caches is every time it moves between caches is every time it moves between caches the memory also gets written and the the memory also gets written and the the memory also gets written and the memory doesn't have as much bandwidth as memory doesn't have as much bandwidth as memory doesn't have as much bandwidth as the caches do so the problem really is the caches do so the problem really is the caches do so the problem really is that here we needed to do a memory right that here we needed to do a memory right that here we needed to do a memory right another problem occurs because when C2 another problem occurs because when C2 another problem occurs because when C2 has written for example and has the data has written for example and has the data has written for example and has the data in the modified State and then C2 in the modified State and then C2 in the modified State and then C2 responds with data and that causes a responds with data and that causes a responds with data and that causes a memory right we again get C1 has the memory right we again get C1 has the memory right we again get C1 has the Block in shared State C2 has the Block Block in shared State C2 has the Block Block in shared State C2 has the Block in shared State now if C3 reads because in shared State now if C3 reads because in shared State now if C3 reads because both of these have the Block in the both of these have the Block in the both of these have the Block in the shared State the memory provides the shared State the memory provides the shared State the memory provides the data so we have memory reads here even data so we have memory reads here even data so we have memory reads here even though one of these could have responded though one of these could have responded though one of these could have responded with the data if we only knew which of with the data if we only knew which of with the data if we only knew which of the two should respond definitely both the two should respond definitely both the two should respond definitely both of them should not respond so if we had of them should not respond so if we had of them should not respond so if we had the way to tell which one of them should the way to tell which one of them should the way to tell which one of them should respond we could avoid this memory read respond we could avoid this memory read respond we could avoid this memory read by the one that is responsible for by the one that is responsible for by the one that is responsible for responding providing the data as long as responding providing the data as long as responding providing the data as long as there are new requesters this can happen there are new requesters this can happen there are new requesters this can happen a lot of times so if another core reads a lot of times so if another core reads a lot of times so if another core reads same thing we get the memory read same thing we get the memory read same thing we get the memory read although now there are three cores with although now there are three cores with although now there are three cores with the data in the shared State and so on the data in the shared State and so on the data in the shared State and so on we want to avoid the update to memory as we want to avoid the update to memory as we want to avoid the update to memory as long as there is a cache that still long as there is a cache that still long as there is a cache that still holds the most recent value of the block holds the most recent value of the block holds the most recent value of the block and we want to avoid memory reads if and we want to avoid memory reads if and we want to avoid memory reads if there is a cache that can provide this there is a cache that can provide this there is a cache that can provide this block because again memory bandwidth is block because again memory bandwidth is block because again memory bandwidth is a lot lower than cash bandwidth and also a lot lower than cash bandwidth and also a lot lower than cash bandwidth and also memory reads and wrs are a lot more memory reads and wrs are a lot more memory reads and wrs are a lot more expensive in terms of power and latency expensive in terms of power and latency expensive in terms of power and latency than cach reads and wrs but to do this than cach reads and wrs but to do this than cach reads and wrs but to do this we need to make a non-modified version we need to make a non-modified version we need to make a non-modified version of the Block in one of the caches of the Block in one of the caches of the Block in one of the caches responsible for giving data to other responsible for giving data to other responsible for giving data to other caches when they want it so that the caches when they want it so that the caches when they want it so that the memory doesn't need to respond and memory doesn't need to respond and memory doesn't need to respond and eventually writing the block to memory eventually writing the block to memory eventually writing the block to memory so that the memory doesn't need to be so that the memory doesn't need to be so that the memory doesn't need to be updated every time we do something like updated every time we do something like updated every time we do something like this so what we really need to know is this so what we really need to know is this so what we really need to know is which of the shared copies is the one which of the shared copies is the one which of the shared copies is the one responsible and we will know this by responsible and we will know this by responsible and we will know this by introducing another state called o which introducing another state called o which introducing another state called o which stands for owned so there will be one stands for owned so there will be one stands for owned so there will be one owner of the block if others are sharing owner of the block if others are sharing owner of the block if others are sharing it the O state will behave just like the it the O state will behave just like the it the O state will behave just like the shared State except that whenever there shared State except that whenever there shared State except that whenever there is a request for the data in the block is a request for the data in the block is a request for the data in the block the owner is the one of the sharers that the owner is the one of the sharers that the owner is the one of the sharers that gets to respond and also if the owner gets to respond and also if the owner gets to respond and also if the owner replaces the block from the cache it replaces the block from the cache it replaces the block from the cache it gets to write back to memory so that we gets to write back to memory so that we gets to write back to memory so that we ensure that memory gets updated

## MOSI Coherence： Part 5
Kind: captions Language: en so now we have what is called a mozi so now we have what is called a mozi so now we have what is called a mozi coherence protocol M s and I are the coherence protocol M s and I are the coherence protocol M s and I are the same as before but now there is a new same as before but now there is a new same as before but now there is a new old state so the Moche protocol is like old state so the Moche protocol is like old state so the Moche protocol is like MSI except when we are in the m state MSI except when we are in the m state MSI except when we are in the m state and we snoopeh read from another core we and we snoopeh read from another core we and we snoopeh read from another core we provide the data as before but we now provide the data as before but we now provide the data as before but we now move into the off state not the s state move into the off state not the s state move into the off state not the s state when we are providing the data the when we are providing the data the when we are providing the data the memory now doesn't get accessed in MSI memory now doesn't get accessed in MSI memory now doesn't get accessed in MSI the memory would now need to be written the memory would now need to be written the memory would now need to be written with the data here we simply provide the with the data here we simply provide the with the data here we simply provide the data to the requester move to the O data to the requester move to the O data to the requester move to the O state without updating the memory while state without updating the memory while state without updating the memory while the block is in the O state in a cache the block is in the O state in a cache the block is in the O state in a cache it behaves similarly to the S state it behaves similarly to the S state it behaves similarly to the S state except that if we snoop a read we except that if we snoop a read we except that if we snoop a read we continue to provide the data just like continue to provide the data just like continue to provide the data just like we did in the M State and that we need we did in the M State and that we need we did in the M State and that we need to write the data back to memory if the to write the data back to memory if the to write the data back to memory if the block in the O state is replaced from block in the O state is replaced from block in the O state is replaced from that cache and that's because all of the that cache and that's because all of the that cache and that's because all of the other caches will have the Block in other caches will have the Block in other caches will have the Block in either invalid or shared state so they either invalid or shared state so they either invalid or shared state so they don't know that the block is dirty and don't know that the block is dirty and don't know that the block is dirty and needs to be replaced so the M state needs to be replaced so the M state needs to be replaced so the M state indicated both that we have exclusive indicated both that we have exclusive indicated both that we have exclusive readwrite access to the block nobody readwrite access to the block nobody readwrite access to the block nobody else has it and that the block is dirty else has it and that the block is dirty else has it and that the block is dirty as indicated that we have the shared as indicated that we have the shared as indicated that we have the shared read access to the block others may also read access to the block others may also read access to the block others may also have it in shared state and that the have it in shared state and that the have it in shared state and that the block is clean the memory has an block is clean the memory has an block is clean the memory has an up-to-date copy now all combines the up-to-date copy now all combines the up-to-date copy now all combines the properties of M and s States in that it properties of M and s States in that it properties of M and s States in that it is a shared read access state but the is a shared read access state but the is a shared read access state but the block is dirty and we are responsible block is dirty and we are responsible block is dirty and we are responsible for putting the data in memory and also for putting the data in memory and also for putting the data in memory and also because there is only one cache with the because there is only one cache with the because there is only one cache with the Block in the O state that cache knows Block in the O state that cache knows Block in the O state that cache knows that it can respond to all the read that it can respond to all the read that it can respond to all the read accesses to the data so that the memory accesses to the data so that the memory accesses to the data so that the memory doesn't

## M(O)SI Inefficiency： Part 5
Kind: captions Language: en so we have used the old state to avoid so we have used the old state to avoid so we have used the old state to avoid inefficiency of the MSI protocol that inefficiency of the MSI protocol that inefficiency of the MSI protocol that had to do with the memory getting had to do with the memory getting had to do with the memory getting unnecessary accesses that could be unnecessary accesses that could be unnecessary accesses that could be satisfied by caches that already have satisfied by caches that already have satisfied by caches that already have the data there is another inefficiency the data there is another inefficiency the data there is another inefficiency that both MSI and MOSI have and that that both MSI and MOSI have and that that both MSI and MOSI have and that inefficiencies related to thread private inefficiencies related to thread private inefficiencies related to thread private data or data that is only ever accessed data or data that is only ever accessed data or data that is only ever accessed by a single thread and thread private by a single thread and thread private by a single thread and thread private data is for example all data in a data is for example all data in a data is for example all data in a single-threaded program will be thread single-threaded program will be thread single-threaded program will be thread private so if we have a four core private so if we have a four core private so if we have a four core processor around for single threaded processor around for single threaded processor around for single threaded programs they never share any data all programs they never share any data all programs they never share any data all of the data in these programs is like of the data in these programs is like of the data in these programs is like this even when we have a parallel this even when we have a parallel this even when we have a parallel program we still have data such as for program we still have data such as for program we still have data such as for example the stacks of the individual example the stacks of the individual example the stacks of the individual threads that are still gonna be accessed threads that are still gonna be accessed threads that are still gonna be accessed only by a single thread and then only by a single thread and then only by a single thread and then efficiency occurs when we read the data efficiency occurs when we read the data efficiency occurs when we read the data in a thread and then rewrite it and this in a thread and then rewrite it and this in a thread and then rewrite it and this is really the only thread accessing this is really the only thread accessing this is really the only thread accessing this data with both MSI and mosy what's going data with both MSI and mosy what's going data with both MSI and mosy what's going to happen is the data was invalid before to happen is the data was invalid before to happen is the data was invalid before we had it we read it we have a mess now we had it we read it we have a mess now we had it we read it we have a mess now we get the data in the shared State we get the data in the shared State we get the data in the shared State when we want to write it because it's a when we want to write it because it's a when we want to write it because it's a shared state we need to send out an shared state we need to send out an shared state we need to send out an invalidation and then we have the Block invalidation and then we have the Block invalidation and then we have the Block in the modified state and for every in the modified state and for every in the modified state and for every single block of data we have to do this single block of data we have to do this single block of data we have to do this in contrast if we had a uniprocessor the in contrast if we had a uniprocessor the in contrast if we had a uniprocessor the block would have a valid bit of zero we block would have a valid bit of zero we block would have a valid bit of zero we read it we have a mess now the valid bit read it we have a mess now the valid bit read it we have a mess now the valid bit becomes one and then we have a hit when becomes one and then we have a hit when becomes one and then we have a hit when we write it and then we just change the we write it and then we just change the we write it and then we just change the dirty bit to one so somehow yeah dirty bit to one so somehow yeah dirty bit to one so somehow yeah uniprocessor gets to do a Miss here and uniprocessor gets to do a Miss here and uniprocessor gets to do a Miss here and a hit here whereas to do msi or mossy we a hit here whereas to do msi or mossy we a hit here whereas to do msi or mossy we have to do a miss followed by another have to do a miss followed by another have to do a miss followed by another bus request granted it doesn't care it bus request granted it doesn't care it bus request granted it doesn't care it data but still we have to wait for the data but still we have to wait for the data but still we have to wait for the bus send something outside the chip and bus send something outside the chip and bus send something outside the chip and so on so this is still a lot slower than so on so this is still a lot slower than so on so this is still a lot slower than just a cache hit and it's ok to pay the just a cache hit and it's ok to pay the just a cache hit and it's ok to pay the penalty for having coherence if we're penalty for having coherence if we're penalty for having coherence if we're actually sharing data because a actually sharing data because a actually sharing data because a uniprocessor would not provide coherent uniprocessor would not provide coherent uniprocessor would not provide coherent behavior but here we actually behavior but here we actually behavior but here we actually no sharing whatsoever so what we want to no sharing whatsoever so what we want to no sharing whatsoever so what we want to do is avoid this invalidation and to do do is avoid this invalidation and to do do is avoid this invalidation and to do that we introduce a new state called that we introduce a new state called that we introduce a new state called exclusive

## The E State： Part 5
Kind: captions Language: en so what does the e state do the M State so what does the e state do the M State so what does the e state do the M State gives us exclusive access so we can both gives us exclusive access so we can both gives us exclusive access so we can both read and write and makes us the owner of read and write and makes us the owner of read and write and makes us the owner of a dirty block so that we have to respond a dirty block so that we have to respond a dirty block so that we have to respond with the data and also update memory the with the data and also update memory the with the data and also update memory the S state gives us shared access so that S state gives us shared access so that S state gives us shared access so that we can only read and we are not we can only read and we are not we can only read and we are not responsible for giving others our data responsible for giving others our data responsible for giving others our data and also we are not responsible for and also we are not responsible for and also we are not responsible for updating memory we have seen that the updating memory we have seen that the updating memory we have seen that the old state also gives us shared access so old state also gives us shared access so old state also gives us shared access so we can do reads we can do reads we can do reads but makes us responsible for updating but makes us responsible for updating but makes us responsible for updating memory and giving data to others so that memory and giving data to others so that memory and giving data to others so that we avoid memory rights that occur when we avoid memory rights that occur when we avoid memory rights that occur when we don't have this state what it does is we don't have this state what it does is we don't have this state what it does is it gives us exclusive so that we can it gives us exclusive so that we can it gives us exclusive so that we can read and write read and write read and write although the block is still clean the although the block is still clean the although the block is still clean the memory still doesn't need to be updated memory still doesn't need to be updated memory still doesn't need to be updated so when acordes read a followed by write so when acordes read a followed by write so when acordes read a followed by write a in the MSI protocol we move from a in the MSI protocol we move from a in the MSI protocol we move from invalid to shared and suffer a mess and invalid to shared and suffer a mess and invalid to shared and suffer a mess and then we move from shared to modified and then we move from shared to modified and then we move from shared to modified and need to send out an invalidation so we need to send out an invalidation so we need to send out an invalidation so we had to bus accesses if we have the M o had to bus accesses if we have the M o had to bus accesses if we have the M o si we have the same sequence because si we have the same sequence because si we have the same sequence because really the old state is not helping us really the old state is not helping us really the old state is not helping us here it's helping us save memory axises here it's helping us save memory axises here it's helping us save memory axises after we had a write and other start after we had a write and other start after we had a write and other start reading if we have the m es i with no o reading if we have the m es i with no o reading if we have the m es i with no o state or a protocol that combines all of state or a protocol that combines all of state or a protocol that combines all of the states when we do a read we now the states when we do a read we now the states when we do a read we now detect that although we have read the detect that although we have read the detect that although we have read the block we are the only one having it so block we are the only one having it so block we are the only one having it so instead of going to the shared state we instead of going to the shared state we instead of going to the shared state we go to the e state and we suffer a miss go to the e state and we suffer a miss go to the e state and we suffer a miss but the idea now is that when our right but the idea now is that when our right but the idea now is that when our right to a occurs we know we have exclusive to a occurs we know we have exclusive to a occurs we know we have exclusive access so we can just write without access so we can just write without access so we can just write without telling anybody although we have to telling anybody although we have to telling anybody although we have to transition to the end state now because transition to the end state now because transition to the end state now because now the block is also dirty so now we now the block is also dirty so now we now the block is also dirty so now we get the same behavior that we would get get the same behavior that we would get get the same behavior that we would get if we did the sequence of accesses on a if we did the sequence of accesses on a if we did the sequence of accesses on a cash-in a uniprocessor so this is not a cash-in a uniprocessor so this is not a cash-in a uniprocessor so this is not a shared block here and we want to get the shared block here and we want to get the shared block here and we want to get the same number of missed same number of missed same number of missed and accesses as we do in a uniprocessor and accesses as we do in a uniprocessor and accesses as we do in a uniprocessor that doesn't share data so here we will that doesn't share data so here we will that doesn't share data so here we will have a Miss followed by a cache hit we have a Miss followed by a cache hit we have a Miss followed by a cache hit we want to have a cache hit here and the want to have a cache hit here and the want to have a cache hit here and the east state allows us to achieve that

## MOESI Quiz： Part 5
Kind: captions Language: en so now let's try our hand at the mo SC so now let's try our hand at the mo SC so now let's try our hand at the mo SC protocol suppose we have three cores protocol suppose we have three cores protocol suppose we have three cores maintain moiseev coherence and the maintain moiseev coherence and the maintain moiseev coherence and the variable X starts out by only being in variable X starts out by only being in variable X starts out by only being in memory so it's not in any of the caches memory so it's not in any of the caches memory so it's not in any of the caches so we have our three course C 0 C 1 and so we have our three course C 0 C 1 and so we have our three course C 0 C 1 and C 2 and the first axis that happens is C 2 and the first axis that happens is C 2 and the first axis that happens is read X on core 0 what is the new state read X on core 0 what is the new state read X on core 0 what is the new state of this block in the cache that belongs of this block in the cache that belongs of this block in the cache that belongs to this core next c1 does read X what is to this core next c1 does read X what is to this core next c1 does read X what is the new state of this block on this the new state of this block on this the new state of this block on this score and what is the state of this score and what is the state of this score and what is the state of this block on C 0 after c1 is done with this block on C 0 after c1 is done with this block on C 0 after c1 is done with this read now c2 does read X what is its new read now c2 does read X what is its new read now c2 does read X what is its new state for this block and what is the new state for this block and what is the new state for this block and what is the new state of this block in c1s cache or does state of this block in c1s cache or does state of this block in c1s cache or does it just stay the same and finally see it just stay the same and finally see it just stay the same and finally see one does a write to X what is the new one does a write to X what is the new one does a write to X what is the new state of the Block in see once cash-in state of the Block in see once cash-in state of the Block in see once cash-in c2s cache and in the cache of course is c2s cache and in the cache of course is c2s cache and in the cache of course is

## MOESI Solution Quiz： Part 5
Kind: captions Language: en in a mercy protocol the state can be m in a mercy protocol the state can be m in a mercy protocol the state can be m OE s or I initially all of the course OE s or I initially all of the course OE s or I initially all of the course have the Block in the I state in their have the Block in the I state in their have the Block in the I state in their caches when c0 reads it detects that caches when c0 reads it detects that caches when c0 reads it detects that it's the only one who actually has a it's the only one who actually has a it's the only one who actually has a copy of the block and because he's doing copy of the block and because he's doing copy of the block and because he's doing a read it doesn't go into the modified a read it doesn't go into the modified a read it doesn't go into the modified State it goes into the exclusive east State it goes into the exclusive east State it goes into the exclusive east state the others keep the Block in the I state the others keep the Block in the I state the others keep the Block in the I state when C one reads it detected it's state when C one reads it detected it's state when C one reads it detected it's not the only one who has the block so it not the only one who has the block so it not the only one who has the block so it normally goes to s state furthermore c0 normally goes to s state furthermore c0 normally goes to s state furthermore c0 now snoops another reader so it's no now snoops another reader so it's no now snoops another reader so it's no longer exclusive and it moves to the longer exclusive and it moves to the longer exclusive and it moves to the shared State - C - still has the invalid shared State - C - still has the invalid shared State - C - still has the invalid state for its block when c2 reads it state for its block when c2 reads it state for its block when c2 reads it also detects that there are two other also detects that there are two other also detects that there are two other sharers so it gets the Block in the sharers so it gets the Block in the sharers so it gets the Block in the shared state c1 nothing happens to its shared state c1 nothing happens to its shared state c1 nothing happens to its shared state when we snow parade and we shared state when we snow parade and we shared state when we snow parade and we are in the shared state we just stay are in the shared state we just stay are in the shared state we just stay there because others reading is there because others reading is there because others reading is compatible with the shared state c0 also compatible with the shared state c0 also compatible with the shared state c0 also actually is in the shared state although actually is in the shared state although actually is in the shared state although you were not asked to do this and now you were not asked to do this and now you were not asked to do this and now finally when c1 writes because it was in finally when c1 writes because it was in finally when c1 writes because it was in the shared state it will put an the shared state it will put an the shared state it will put an invalidation request on the bus and once invalidation request on the bus and once invalidation request on the bus and once it does that it can move to the modified it does that it can move to the modified it does that it can move to the modified State State State others see that invalidation request and others see that invalidation request and others see that invalidation request and go to the invalid state pretty much go to the invalid state pretty much go to the invalid state pretty much after a write we are always in the after a write we are always in the after a write we are always in the modified state and all others are always modified state and all others are always modified state and all others are always in the eye state at that point



---

# Playlist 4 Module 8 Raw Transcript

## MESI, MOSI, MOESI Quiz： Part 5
Kind: captions Language: en now let's compare the mes I am OSI and now let's compare the mes I am OSI and now let's compare the mes I am OSI and Morrissey protocols we have three cores Morrissey protocols we have three cores Morrissey protocols we have three cores and we start out with block a being in and we start out with block a being in and we start out with block a being in the invalid state in all three caches the invalid state in all three caches the invalid state in all three caches suppose that we have this sequence of suppose that we have this sequence of suppose that we have this sequence of accesses core 1 reads a core 1 then accesses core 1 reads a core 1 then accesses core 1 reads a core 1 then writes a core 2 reads a then writes a writes a core 2 reads a then writes a writes a core 2 reads a then writes a core 3 then reads a core 1 then reads a core 3 then reads a core 1 then reads a core 3 then reads a core 1 then reads a and core 2 also reads a assuming that a and core 2 also reads a assuming that a and core 2 also reads a assuming that a is the only thing we access so so we is the only thing we access so so we is the only thing we access so so we don't have to worry about replacing a don't have to worry about replacing a don't have to worry about replacing a for these caches unless it needs to be for these caches unless it needs to be for these caches unless it needs to be replaced because of coherence how many replaced because of coherence how many replaced because of coherence how many memory reads and bus requests do we have memory reads and bus requests do we have memory reads and bus requests do we have in this sequence of events for mes I M o in this sequence of events for mes I M o in this sequence of events for mes I M o SI m OE si protocols

## MESI, MOSI, MOESI Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution for our tree let's look at the solution for our tree let's look at the solution for our tree protocol comparison quiz where we had protocol comparison quiz where we had protocol comparison quiz where we had three cores a block a that starts in an three cores a block a that starts in an three cores a block a that starts in an invalid state in all three caches and invalid state in all three caches and invalid state in all three caches and then cores access this block in this way then cores access this block in this way then cores access this block in this way we were asked how many memory reads and we were asked how many memory reads and we were asked how many memory reads and bus requests do we have in each of these bus requests do we have in each of these bus requests do we have in each of these protocols when I read a occurs will have protocols when I read a occurs will have protocols when I read a occurs will have a memory read in each of these protocols a memory read in each of these protocols a memory read in each of these protocols because this is a Miss on core one at because this is a Miss on core one at because this is a Miss on core one at that point c1 has the Block in the that point c1 has the Block in the that point c1 has the Block in the shared or exclusive state depending on shared or exclusive state depending on shared or exclusive state depending on whether the protocol has a shared state whether the protocol has a shared state whether the protocol has a shared state or not so in the mozi we will have or not so in the mozi we will have or not so in the mozi we will have shared state in messy and mossy we will shared state in messy and mossy we will shared state in messy and mossy we will have the exclusive state because at this have the exclusive state because at this have the exclusive state because at this point c1 is the only one who has this point c1 is the only one who has this point c1 is the only one who has this block at all when c1 writes to a if we block at all when c1 writes to a if we block at all when c1 writes to a if we have the Block in the shared state we have the Block in the shared state we have the Block in the shared state we need to put an invalidation request on need to put an invalidation request on need to put an invalidation request on the bus because we don't know whether the bus because we don't know whether the bus because we don't know whether there are any other sharers and of there are any other sharers and of there are any other sharers and of course if we did a memory read we also course if we did a memory read we also course if we did a memory read we also had a bus request in each of the three had a bus request in each of the three had a bus request in each of the three now when we do a write a four protocols now when we do a write a four protocols now when we do a write a four protocols that have the e state we just move to that have the e state we just move to that have the e state we just move to the modified state we don't have to put the modified state we don't have to put the modified state we don't have to put anything on the bus or read memory so anything on the bus or read memory so anything on the bus or read memory so here we don't do anything but for mossie here we don't do anything but for mossie here we don't do anything but for mossie we were in the shared state we have to we were in the shared state we have to we were in the shared state we have to put an invalidation request on the bus put an invalidation request on the bus put an invalidation request on the bus there is no memory read here we already there is no memory read here we already there is no memory read here we already have the data we just need to invalidate have the data we just need to invalidate have the data we just need to invalidate others because we think there might be others because we think there might be others because we think there might be others so now we have the Block in the others so now we have the Block in the others so now we have the Block in the modified state in c1 everybody else modified state in c1 everybody else modified state in c1 everybody else still has the Block in the invalid state still has the Block in the invalid state still has the Block in the invalid state when c2 reads a c1 must supply this data when c2 reads a c1 must supply this data when c2 reads a c1 must supply this data so this is a bus request because we have so this is a bus request because we have so this is a bus request because we have read in all of them seto will get the read in all of them seto will get the read in all of them seto will get the data in the shared state because c1 will data in the shared state because c1 will data in the shared state because c1 will be downgraded from modified to either be downgraded from modified to either be downgraded from modified to either shared or owned but either way c2 will shared or owned but either way c2 will shared or owned but either way c2 will be shared c1 will have the Block in be shared c1 will have the Block in be shared c1 will have the Block in either the O state if there is an O either the O state if there is an O either the O state if there is an O state because from motive state because from motive state because from motive we go to all once we start sharing or in we go to all once we start sharing or in we go to all once we start sharing or in the shared state assuming we have the shared state assuming we have the shared state assuming we have intervention based supply of this value intervention based supply of this value intervention based supply of this value Tutsi - there is no memory read because Tutsi - there is no memory read because Tutsi - there is no memory read because see one is able to provide the block - see one is able to provide the block - see one is able to provide the block - see - now in situ writes to the block see - now in situ writes to the block see - now in situ writes to the block because it has the Block in the shared because it has the Block in the shared because it has the Block in the shared State State State there will be a bus request to send out there will be a bus request to send out there will be a bus request to send out an invalidation an invalidation an invalidation either way see one now has the block in either way see one now has the block in either way see one now has the block in the invalid state and c2 has the Block the invalid state and c2 has the Block the invalid state and c2 has the Block in the modified State so from this rate in the modified State so from this rate in the modified State so from this rate to this right having the exclusive state to this right having the exclusive state to this right having the exclusive state didn't help us save an access on a right didn't help us save an access on a right didn't help us save an access on a right and that's because here we had no and that's because here we had no and that's because here we had no exclusive access to the block anymore c1 exclusive access to the block anymore c1 exclusive access to the block anymore c1 also had the block now let's see what also had the block now let's see what also had the block now let's see what happens when c3 reads when c3 reads c3 happens when c3 reads when c3 reads c3 happens when c3 reads when c3 reads c3 we'll get the Block in the shared state we'll get the Block in the shared state we'll get the Block in the shared state and this is a read miss it needs the and this is a read miss it needs the and this is a read miss it needs the data so there is definitely a bus access data so there is definitely a bus access data so there is definitely a bus access in all of them c2 however moves to in all of them c2 however moves to in all of them c2 however moves to either the owned or the shared State either the owned or the shared State either the owned or the shared State depending on whether we have the own depending on whether we have the own depending on whether we have the own state in this case again see to supply state in this case again see to supply state in this case again see to supply the data to c3 so there was no memory the data to c3 so there was no memory the data to c3 so there was no memory read however when c1 reads if we are in read however when c1 reads if we are in read however when c1 reads if we are in the shared state in c2 c1 ends up the shared state in c2 c1 ends up the shared state in c2 c1 ends up reading memory so if we have the O state reading memory so if we have the O state reading memory so if we have the O state c2 is able to intervene and supply the c2 is able to intervene and supply the c2 is able to intervene and supply the data to c1 and then C - if we have the data to c1 and then C - if we have the data to c1 and then C - if we have the shared state then there are memory shared state then there are memory shared state then there are memory accesses in both of these c1 had a block accesses in both of these c1 had a block accesses in both of these c1 had a block in the invalid state so definitely the in the invalid state so definitely the in the invalid state so definitely the read request will be put on the bus and read request will be put on the bus and read request will be put on the bus and now what we have is basically c1 and c3 now what we have is basically c1 and c3 now what we have is basically c1 and c3 have the Block in the shared state c2 have the Block in the shared state c2 have the Block in the shared state c2 retains the Block in the owned or shared retains the Block in the owned or shared retains the Block in the owned or shared state so there was a basic West here and state so there was a basic West here and state so there was a basic West here and there was a memory request if we didn't there was a memory request if we didn't there was a memory request if we didn't have the O state so here we have an have the O state so here we have an have the O state so here we have an additional memory read now C to reach additional memory read now C to reach additional memory read now C to reach the block either way it can read the the block either way it can read the the block either way it can read the block because at this point it's either block because at this point it's either block because at this point it's either owned or shared so there is no bus owned or shared so there is no bus owned or shared so there is no bus request nor memory read in any of these request nor memory read in any of these request nor memory read in any of these protocols so overall we have one memory protocols so overall we have one memory protocols so overall we have one memory read in protocols that have the O state read in protocols that have the O state read in protocols that have the O state and two in the protocol that doesn't for and two in the protocol that doesn't for and two in the protocol that doesn't for bus request we have five bus requests in bus request we have five bus requests in bus request we have five bus requests in protocols that have the exclusive state protocols that have the exclusive state protocols that have the exclusive state and six bus requests in the protocol and six bus requests in the protocol and six bus requests in the protocol that doesn't

## Directory Based Coherence： Part 5
Kind: captions Language: en We said already that snooping is only We said already that snooping is only We said already that snooping is only one of the two ways to do coherence. The one of the two ways to do coherence. The one of the two ways to do coherence. The other way is directory based coherence. other way is directory based coherence. other way is directory based coherence. So let's first see why do we even want So let's first see why do we even want So let's first see why do we even want directory based coherence. What's wrong directory based coherence. What's wrong directory based coherence. What's wrong with snooping? In snooping we had to with snooping? In snooping we had to with snooping? In snooping we had to broadcast all of the requests we make so broadcast all of the requests we make so broadcast all of the requests we make so that others can see them and also to that others can see them and also to that others can see them and also to establish ordering. For example, for establish ordering. For example, for establish ordering. For example, for rights we need to establish the ordering rights we need to establish the ordering rights we need to establish the ordering so that we can obey the third coherence so that we can obey the third coherence so that we can obey the third coherence property. But that means that there property. But that means that there property. But that means that there needs to be a single bus on which all of needs to be a single bus on which all of needs to be a single bus on which all of the requests from all of the cores go the requests from all of the cores go the requests from all of the cores go both cache misses and also coherence both cache misses and also coherence both cache misses and also coherence requests that are just sending out requests that are just sending out requests that are just sending out invalidations and so on. So the bus that invalidations and so on. So the bus that invalidations and so on. So the bus that we were using for broadcast becomes a we were using for broadcast becomes a we were using for broadcast becomes a bottleneck and as a result snooping does bottleneck and as a result snooping does bottleneck and as a result snooping does not really work well if we have more not really work well if we have more not really work well if we have more than something like 8 to 16 cores. than something like 8 to 16 cores. than something like 8 to 16 cores. Beyond that point, most of the time the Beyond that point, most of the time the Beyond that point, most of the time the cores are just spending waiting for the cores are just spending waiting for the cores are just spending waiting for the bus to put requests on it. So really we bus to put requests on it. So really we bus to put requests on it. So really we get no further benefit from additional get no further benefit from additional get no further benefit from additional cores. So what we need to solve this cores. So what we need to solve this cores. So what we need to solve this bottleneck is a nonbroadcast network bottleneck is a nonbroadcast network bottleneck is a nonbroadcast network over which we're going to send our over which we're going to send our over which we're going to send our requests so that many requests could be requests so that many requests could be requests so that many requests could be going on at the same time. But then we going on at the same time. But then we going on at the same time. But then we have the problems of how do we observe have the problems of how do we observe have the problems of how do we observe those requests that we need to see. So those requests that we need to see. So those requests that we need to see. So for example, if we are in a shared for example, if we are in a shared for example, if we are in a shared state, it had better be that when there state, it had better be that when there state, it had better be that when there is a right by somebody else, we observe is a right by somebody else, we observe is a right by somebody else, we observe that right so that we can move to the that right so that we can move to the that right so that we can move to the invalid state to avoid incoherence. And invalid state to avoid incoherence. And invalid state to avoid incoherence. And also how do we decide what the ordering also how do we decide what the ordering also how do we decide what the ordering is for requests that go to the same is for requests that go to the same is for requests that go to the same block. So for example, how do we decide block. So for example, how do we decide block. So for example, how do we decide what's the ordering between rights done what's the ordering between rights done what's the ordering between rights done at different cores if these requests can at different cores if these requests can at different cores if these requests can be made on different parts of the be made on different parts of the be made on different parts of the network? And the structure that does network? And the structure that does network? And the structure that does this is called a directory.

## Directory： Part 5
Kind: captions Language: en so the directory is a distributed so the directory is a distributed so the directory is a distributed structure across the course it's not structure across the course it's not structure across the course it's not centralized in one place like the bus so centralized in one place like the bus so centralized in one place like the bus so not all requests have to go to the same not all requests have to go to the same not all requests have to go to the same part of the directory each slice of the part of the directory each slice of the part of the directory each slice of the directory will serve a set of blocks directory will serve a set of blocks directory will serve a set of blocks where a slice of the directory is the where a slice of the directory is the where a slice of the directory is the part of the directory that is next to a part of the directory that is next to a part of the directory that is next to a particular core and that means that particular core and that means that particular core and that means that different blocks will be served by different blocks will be served by different blocks will be served by slices that are in different places and slices that are in different places and slices that are in different places and that's how we achieve a higher bandwidth that's how we achieve a higher bandwidth that's how we achieve a higher bandwidth because really each slice could operate because really each slice could operate because really each slice could operate independently because it serves a independently because it serves a independently because it serves a disjoint set of blocks what does a slice disjoint set of blocks what does a slice disjoint set of blocks what does a slice do it has one entry for each block do it has one entry for each block do it has one entry for each block served by that slice and the entry for a served by that slice and the entry for a served by that slice and the entry for a block tracks which caches in the system block tracks which caches in the system block tracks which caches in the system have this block in a non- invalid state have this block in a non- invalid state have this block in a non- invalid state of course cu the Block in an invalid of course cu the Block in an invalid of course cu the Block in an invalid state is like it's not in that cache at state is like it's not in that cache at state is like it's not in that cache at all and the Order of accesses for a all and the Order of accesses for a all and the Order of accesses for a particular block is determined by the particular block is determined by the particular block is determined by the Home Slice for that block that's the Home Slice for that block that's the Home Slice for that block that's the slice of the directory that has the slice of the directory that has the slice of the directory that has the entry for this block we call that the entry for this block we call that the entry for this block we call that the home slice of the directory so pretty home slice of the directory so pretty home slice of the directory so pretty much accesses that go to the same block much accesses that go to the same block much accesses that go to the same block are almost serialized in how they can are almost serialized in how they can are almost serialized in how they can access their entry and the order in access their entry and the order in access their entry and the order in which that happens is the order in which which that happens is the order in which which that happens is the order in which the accesses are officially said to the accesses are officially said to the accesses are officially said to happen note that with the directory happen note that with the directory happen note that with the directory protocol the caches still have the same protocol the caches still have the same protocol the caches still have the same States they did with Sno hoping it's States they did with Sno hoping it's States they did with Sno hoping it's just now when we send a request to read just now when we send a request to read just now when we send a request to read or write that request no longer goes on or write that request no longer goes on or write that request no longer goes on a bus it gets sent via our Network to a bus it gets sent via our Network to a bus it gets sent via our Network to the directory so we can have many the directory so we can have many the directory so we can have many requests traveling to their individual requests traveling to their individual requests traveling to their individual slices and then the directory figures slices and then the directory figures slices and then the directory figures out what to do

## Directory Entry： Part 5
Kind: captions Language: en the directory entry has one Dirty Bit the directory entry has one Dirty Bit the directory entry has one Dirty Bit that indicates that the block is dirty that indicates that the block is dirty that indicates that the block is dirty in some cache in the system and for in some cache in the system and for in some cache in the system and for every cache in the system the directory every cache in the system the directory every cache in the system the directory entry has one bit that indicates whether entry has one bit that indicates whether entry has one bit that indicates whether the block is present in that cache if the block is present in that cache if the block is present in that cache if the presence bit is one for a particular the presence bit is one for a particular the presence bit is one for a particular cache that means that we think that cache that means that we think that cache that means that we think that cache has a copy of the block if the cache has a copy of the block if the cache has a copy of the block if the presence bit is zero that means that we presence bit is zero that means that we presence bit is zero that means that we know for sure that that cach does not know for sure that that cach does not know for sure that that cach does not have the Block in a state that is not I have the Block in a state that is not I have the Block in a state that is not I so let's say we have one block caches at so let's say we have one block caches at so let's say we have one block caches at core zero and one and let's say this is core zero and one and let's say this is core zero and one and let's say this is the directory entry for Block B where the directory entry for Block B where the directory entry for Block B where this is the Dirty Bit and these are the this is the Dirty Bit and these are the this is the Dirty Bit and these are the presence bits for Cores 0 1 2 3 4 5 6 presence bits for Cores 0 1 2 3 4 5 6 presence bits for Cores 0 1 2 3 4 5 6 and 7 so this is an eight core system so and 7 so this is an eight core system so and 7 so this is an eight core system so we need eight bits to represent presence we need eight bits to represent presence we need eight bits to represent presence of the Block in the caches initially the of the Block in the caches initially the of the Block in the caches initially the block is not dirty and is not present in block is not dirty and is not present in block is not dirty and is not present in any of the caches let's say that core any of the caches let's say that core any of the caches let's say that core zero reads From the Block B the block is zero reads From the Block B the block is zero reads From the Block B the block is not in its cache so it's treated like not in its cache so it's treated like not in its cache so it's treated like it's in the invalid State we put the it's in the invalid State we put the it's in the invalid State we put the read request out normally this read read request out normally this read read request out normally this read request would be put out on the bus and request would be put out on the bus and request would be put out on the bus and because no other cache has this the because no other cache has this the because no other cache has this the memory would respond but now we don't memory would respond but now we don't memory would respond but now we don't have snooping anymore so all requests have snooping anymore so all requests have snooping anymore so all requests for a block would be sent first first to for a block would be sent first first to for a block would be sent first first to the home slice of the block and we the home slice of the block and we the home slice of the block and we determine what is the Home Slice by determine what is the Home Slice by determine what is the Home Slice by looking at the address of the block looking at the address of the block looking at the address of the block different blocks are distributed among different blocks are distributed among different blocks are distributed among different slices so that we get an even different slices so that we get an even different slices so that we get an even load between the slices so let's say we load between the slices so let's say we load between the slices so let's say we determine that for Block B this is the determine that for Block B this is the determine that for Block B this is the slice where we go and once the request slice where we go and once the request slice where we go and once the request gets there we look at the directory gets there we look at the directory gets there we look at the directory entry for it the directory entry says entry for it the directory entry says entry for it the directory entry says that the block is not anywhere in the that the block is not anywhere in the that the block is not anywhere in the system so now we can get get the data system so now we can get get the data system so now we can get get the data from memory and send the data back to from memory and send the data back to from memory and send the data back to the cach zero and we need to tell the the cach zero and we need to tell the the cach zero and we need to tell the cach zero everything that the bus would cach zero everything that the bus would cach zero everything that the bus would normally tell it so we tell it that it normally tell it so we tell it that it normally tell it so we tell it that it has exclusive access because there are has exclusive access because there are has exclusive access because there are no other sharers and we give it the data no other sharers and we give it the data no other sharers and we give it the data that response gets back to cach zero and that response gets back to cach zero and that response gets back to cach zero and now if it has the exclusive state it can now if it has the exclusive state it can now if it has the exclusive state it can put the Block in that state and put the put the Block in that state and put the put the Block in that state and put the data for B here when the directory sends data for B here when the directory sends data for B here when the directory sends the data to cach Z it changes is the the data to cach Z it changes is the the data to cach Z it changes is the presence bit for that cache to one also presence bit for that cache to one also presence bit for that cache to one also we sent the data with exclusive access we sent the data with exclusive access we sent the data with exclusive access if cach Z modifies the data it doesn't if cach Z modifies the data it doesn't if cach Z modifies the data it doesn't have to notify the directory back so we have to notify the directory back so we have to notify the directory back so we also set the Dirty Bit the Dirty Bit also set the Dirty Bit the Dirty Bit also set the Dirty Bit the Dirty Bit here will not cause a write back it will here will not cause a write back it will here will not cause a write back it will cause us to find if a cash needs to do a cause us to find if a cash needs to do a cause us to find if a cash needs to do a right back so now why does this work right back so now why does this work right back so now why does this work better than a bus well because while better than a bus well because while better than a bus well because while cash Zer was doing this with this cash Zer was doing this with this cash Zer was doing this with this directory slice cash one could have done directory slice cash one could have done directory slice cash one could have done something like that for another block something like that for another block something like that for another block with another directory slice completely with another directory slice completely with another directory slice completely independently however if cachan tried to independently however if cachan tried to independently however if cachan tried to write to be at about the same time when write to be at about the same time when write to be at about the same time when read B was occurring we would send a read B was occurring we would send a read B was occurring we would send a right request to the same directory right request to the same directory right request to the same directory entry and now between these two messages entry and now between these two messages entry and now between these two messages the directory controller needs to select the directory controller needs to select the directory controller needs to select one in this case it chose read request one in this case it chose read request one in this case it chose read request first so officially this read happened first so officially this read happened first so officially this read happened before this right so the core here gets before this right so the core here gets before this right so the core here gets to read the Block B and only then the to read the Block B and only then the to read the Block B and only then the right gets processed by the directory so right gets processed by the directory so right gets processed by the directory so when this right request arrives here when this right request arrives here when this right request arrives here again this right request would be placed again this right request would be placed again this right request would be placed on the bus seen by this cache as a on the bus seen by this cache as a on the bus seen by this cache as a result this cach would invalidate and we result this cach would invalidate and we result this cach would invalidate and we will get the Block in the modified state will get the Block in the modified state will get the Block in the modified state with the directory we sent the request with the directory we sent the request with the directory we sent the request to the directory controller it sees that to the directory controller it sees that to the directory controller it sees that the block is present and possibly dirty the block is present and possibly dirty the block is present and possibly dirty in the in this case cach Zero in the in this case cach Zero in the in this case cach Zero so what now happens is the directory so what now happens is the directory so what now happens is the directory forwards the right request for B to the forwards the right request for B to the forwards the right request for B to the cashes that are present in this case it cashes that are present in this case it cashes that are present in this case it forwards it to cash Z cash Z Now sees forwards it to cash Z cash Z Now sees forwards it to cash Z cash Z Now sees this request just as if it was snooped this request just as if it was snooped this request just as if it was snooped from the bus and because it has the data from the bus and because it has the data from the bus and because it has the data in the exclusive state it can choose to in the exclusive state it can choose to in the exclusive state it can choose to respond with the data or it may just respond with the data or it may just respond with the data or it may just keep quiet and confirm the invalidation keep quiet and confirm the invalidation keep quiet and confirm the invalidation so what happens here is once the right so what happens here is once the right so what happens here is once the right request has been given to this cachee we request has been given to this cachee we request has been given to this cachee we need to send the acknowledgement at need to send the acknowledgement at need to send the acknowledgement at least if not the data to the back to the least if not the data to the back to the least if not the data to the back to the directory controller that says that we directory controller that says that we directory controller that says that we are done invalidating our copy so at are done invalidating our copy so at are done invalidating our copy so at this point we have invalidated our copy this point we have invalidated our copy this point we have invalidated our copy when the acknowledgement arrives here when the acknowledgement arrives here when the acknowledgement arrives here the directory controller can change this the directory controller can change this the directory controller can change this to zero if the data didn't arrive then to zero if the data didn't arrive then to zero if the data didn't arrive then we can erase the Dirty Bit And now we can erase the Dirty Bit And now we can erase the Dirty Bit And now because we don't have the data still we because we don't have the data still we because we don't have the data still we will read the memory and send the data will read the memory and send the data will read the memory and send the data to cach one and and because we are to cach one and and because we are to cach one and and because we are acknowledging a write request we again acknowledging a write request we again acknowledging a write request we again set a dirty bit and now the presence set a dirty bit and now the presence set a dirty bit and now the presence Vector gets a one for the bit that Vector gets a one for the bit that Vector gets a one for the bit that corresponds to cach one and now we get corresponds to cach one and now we get corresponds to cach one and now we get to set M here and write to the B here so to set M here and write to the B here so to set M here and write to the B here so as you can see this is how the caches as you can see this is how the caches as you can see this is how the caches can do their normal coherence but now can do their normal coherence but now can do their normal coherence but now instead of everybody seeing everything instead of everybody seeing everything instead of everybody seeing everything that we need to send request to the that we need to send request to the that we need to send request to the directory which then sends out the directory which then sends out the directory which then sends out the messages that need to be sent only to messages that need to be sent only to messages that need to be sent only to those caches that actually might have those caches that actually might have those caches that actually might have the block so if a block is shared by the block so if a block is shared by the block so if a block is shared by only two cores then we will be only only two cores then we will be only only two cores then we will be only sending a very few messages in contrast sending a very few messages in contrast sending a very few messages in contrast the bus would force us to basically the bus would force us to basically the bus would force us to basically broadcast to all of the cores so this broadcast to all of the cores so this broadcast to all of the cores so this also saves a lot of bandwidth because we also saves a lot of bandwidth because we also saves a lot of bandwidth because we are using point-to-point network links are using point-to-point network links are using point-to-point network links to send just this meanwhile other cores to send just this meanwhile other cores to send just this meanwhile other cores could be doing something like this with could be doing something like this with could be doing something like this with another directory entry completely another directory entry completely another directory entry completely independently over a network

## Directory Example： Part 5
Kind: captions Language: en now we're going to do a slightly more now we're going to do a slightly more now we're going to do a slightly more detailed directory example to show how detailed directory example to show how detailed directory example to show how the directory doesn't become a the directory doesn't become a the directory doesn't become a bottleneck when a bus would here we have bottleneck when a bus would here we have bottleneck when a bus would here we have four cores each with its own cache and four cores each with its own cache and four cores each with its own cache and also next to each core is a slice of the also next to each core is a slice of the also next to each core is a slice of the directory let's assume that each slice directory let's assume that each slice directory let's assume that each slice just keeps track of one block so this is just keeps track of one block so this is just keeps track of one block so this is the directory slice where we keep the the directory slice where we keep the the directory slice where we keep the block X information and then next to block X information and then next to block X information and then next to cach one there is directory y z and w cach one there is directory y z and w cach one there is directory y z and w and now let's assume that the core zero and now let's assume that the core zero and now let's assume that the core zero right X at about the same time where right X at about the same time where right X at about the same time where core one is trying to read why this is a core one is trying to read why this is a core one is trying to read why this is a cach miss and we send the request to the cach miss and we send the request to the cach miss and we send the request to the directory that is the home for X which directory that is the home for X which directory that is the home for X which happens to be our own nearby slice of happens to be our own nearby slice of happens to be our own nearby slice of the directory when we read y we go to the directory when we read y we go to the directory when we read y we go to our own slice let's say that cach 2 also our own slice let's say that cach 2 also our own slice let's say that cach 2 also reads Y at about the same time so what reads Y at about the same time so what reads Y at about the same time so what happens now is the right to X and the happens now is the right to X and the happens now is the right to X and the read to Y here are probably going to be read to Y here are probably going to be read to Y here are probably going to be processed at about the same time processed at about the same time processed at about the same time completely independently by these completely independently by these completely independently by these directories meanwhile the read Y is directories meanwhile the read Y is directories meanwhile the read Y is traveling through the network to this traveling through the network to this traveling through the network to this directory in each of these directories directory in each of these directories directory in each of these directories we see that the block is not present in we see that the block is not present in we see that the block is not present in any of the caches so we allow exclusive any of the caches so we allow exclusive any of the caches so we allow exclusive access for the read and here of course access for the read and here of course access for the read and here of course the block will be modified anyway so the block will be modified anyway so the block will be modified anyway so here we will set the Dirty Bit And note here we will set the Dirty Bit And note here we will set the Dirty Bit And note that block X is now in the cache zero that block X is now in the cache zero that block X is now in the cache zero and here we will set the dirty bit and here we will set the dirty bit and here we will set the dirty bit because exclusive can become dir easily because exclusive can become dir easily because exclusive can become dir easily and set the presence bit for cache one and set the presence bit for cache one and set the presence bit for cache one now the read Y is traveling to this now the read Y is traveling to this now the read Y is traveling to this directory let's say that cach one is directory let's say that cach one is directory let's say that cach one is being accessed with a right tox and it's being accessed with a right tox and it's being accessed with a right tox and it's a cach miss so it gets sent to this a cach miss so it gets sent to this a cach miss so it gets sent to this directory so this will probably arrive directory so this will probably arrive directory so this will probably arrive here first we will check the presence here first we will check the presence here first we will check the presence vector and the dirtiness information and vector and the dirtiness information and vector and the dirtiness information and see that cach one is the one that has see that cach one is the one that has see that cach one is the one that has the data and that it might be dirty the data and that it might be dirty the data and that it might be dirty there so we forward the request to read there so we forward the request to read there so we forward the request to read to cach one cach one acknowledges that to cach one cach one acknowledges that to cach one cach one acknowledges that request changes its state to Shared we request changes its state to Shared we request changes its state to Shared we get the response that doesn't carry data get the response that doesn't carry data get the response that doesn't carry data so now we know that the block was not so now we know that the block was not so now we know that the block was not dirty and we change it to not dirty we dirty and we change it to not dirty we dirty and we change it to not dirty we then get the data and forward it to cach then get the data and forward it to cach then get the data and forward it to cach two and now we note also that cach 2 two and now we note also that cach 2 two and now we note also that cach 2 might have the data and the cache to might have the data and the cache to might have the data and the cache to eventually will get the data so it will eventually will get the data so it will eventually will get the data so it will eventually get the shared State and the eventually get the shared State and the eventually get the shared State and the block y meanwhile the right X is getting block y meanwhile the right X is getting block y meanwhile the right X is getting here and because we need to write we're here and because we need to write we're here and because we need to write we're going to send an invalidation and an going to send an invalidation and an going to send an invalidation and an external write request to Cache zero so external write request to Cache zero so external write request to Cache zero so here the cache needs to respond with the here the cache needs to respond with the here the cache needs to respond with the data and because it's a right request it data and because it's a right request it data and because it's a right request it sets a state to invalid and responds sets a state to invalid and responds sets a state to invalid and responds with the data this is also a right so with the data this is also a right so with the data this is also a right so the block block will remain dirty but the block block will remain dirty but the block block will remain dirty but now the directory knows that cach Z no now the directory knows that cach Z no now the directory knows that cach Z no longer has the data and we send the data longer has the data and we send the data longer has the data and we send the data to cach one so now the Cashman will to cach one so now the Cashman will to cach one so now the Cashman will eventually get the data in the modified eventually get the data in the modified eventually get the data in the modified State and as you can see the right to X State and as you can see the right to X State and as you can see the right to X and the read to Y are largely proceeding and the read to Y are largely proceeding and the read to Y are largely proceeding independent of each other except for independent of each other except for independent of each other except for maybe possible connection in the network maybe possible connection in the network maybe possible connection in the network so if our network is good and has many so if our network is good and has many so if our network is good and has many different paths between these caches and different paths between these caches and different paths between these caches and directories then really there was no directories then really there was no directories then really there was no point where these requests competed with point where these requests competed with point where these requests competed with each other for anything this one was each other for anything this one was each other for anything this one was processed by this cache and this processed by this cache and this processed by this cache and this directory this one was processed by this directory this one was processed by this directory this one was processed by this cache and this directory and we involved cache and this directory and we involved cache and this directory and we involved other caches if necessary but in this other caches if necessary but in this other caches if necessary but in this case it was this cach so these requests case it was this cach so these requests case it was this cach so these requests were still largely independent now let's were still largely independent now let's were still largely independent now let's say we want to write to Y on in this say we want to write to Y on in this say we want to write to Y on in this cache this will be sent to the directory cache this will be sent to the directory cache this will be sent to the directory for y so the message travels over here for y so the message travels over here for y so the message travels over here the directory for y now sees that caches the directory for y now sees that caches the directory for y now sees that caches one and two might have the data and that one and two might have the data and that one and two might have the data and that none of them are dirty so it just sends none of them are dirty so it just sends none of them are dirty so it just sends the invalidation request to caches one the invalidation request to caches one the invalidation request to caches one and two when they receive this and two when they receive this and two when they receive this invalidate the block Y and send back invalidate the block Y and send back invalidate the block Y and send back responses that indicate that they have responses that indicate that they have responses that indicate that they have done so when the First Response arise done so when the First Response arise done so when the First Response arise from let's say cach one the directory from let's say cach one the directory from let's say cach one the directory eliminates this bit when the second eliminates this bit when the second eliminates this bit when the second response comes in the directory response comes in the directory response comes in the directory eliminates this bit now that we know eliminates this bit now that we know eliminates this bit now that we know there are no more remaining sharers we there are no more remaining sharers we there are no more remaining sharers we can send the data to the requesting can send the data to the requesting can send the data to the requesting writer and note that it's going to have writer and note that it's going to have writer and note that it's going to have the block and that the block will be the block and that the block will be the block and that the block will be dirty so now we send the block over here dirty so now we send the block over here dirty so now we send the block over here and this block becomes modified here and and this block becomes modified here and and this block becomes modified here and so on so again this right only involved so on so again this right only involved so on so again this right only involved the caches and the directory entry that the caches and the directory entry that the caches and the directory entry that actually needed to be involved for this actually needed to be involved for this actually needed to be involved for this block this cash and directory for block this cash and directory for block this cash and directory for example were completely operating example were completely operating example were completely operating independently of this and these two independently of this and these two independently of this and these two directories could have done something directories could have done something directories could have done something else

## Directory MOESI Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out what so let's see if we can figure out what so let's see if we can figure out what the directory will be doing in a mosc the directory will be doing in a mosc the directory will be doing in a mosc protocol if we have four cores numbered protocol if we have four cores numbered protocol if we have four cores numbered zero through three zero through three zero through three and if the directory slice for location and if the directory slice for location and if the directory slice for location a is in slice zero a is in slice zero a is in slice zero if what happens is this core 0 reads a if what happens is this core 0 reads a if what happens is this core 0 reads a core 0 writes a core 1 core 0 writes a core 1 core 0 writes a core 1 reads a reads a reads a core 2 core 2 core 2 reads a reads a reads a core 3 reads a and then core 0 core 3 reads a and then core 0 core 3 reads a and then core 0 writes a again writes a again writes a again the question for you is the question for you is the question for you is in this sequence of accesses assuming in this sequence of accesses assuming in this sequence of accesses assuming that a is that a is that a is not in any of the caches at the not in any of the caches at the not in any of the caches at the beginning of this how many beginning of this how many beginning of this how many requests are sent by these caches to the requests are sent by these caches to the requests are sent by these caches to the directory only counting the requests directory only counting the requests directory only counting the requests then the directory forwards these then the directory forwards these then the directory forwards these requests through how many messages note requests through how many messages note requests through how many messages note that it need not be the same number here that it need not be the same number here that it need not be the same number here as a result of sending these the as a result of sending these the as a result of sending these the directory gets how many responses and directory gets how many responses and directory gets how many responses and replies replies replies and finally how many responses does the and finally how many responses does the and finally how many responses does the directory send itself again the protocol directory send itself again the protocol directory send itself again the protocol is more ec for this course is more ec for this course is more ec for this course and we have a directory protocol with and we have a directory protocol with and we have a directory protocol with four cores

## Directory Moesi Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution for what let's look at the solution for what let's look at the solution for what happens in our directory Morrissey quiz happens in our directory Morrissey quiz happens in our directory Morrissey quiz when course zero issues are read it when course zero issues are read it when course zero issues are read it sends a request to the directory the sends a request to the directory the sends a request to the directory the directory doesn't forward or get any directory doesn't forward or get any directory doesn't forward or get any responses it just sends a response when responses it just sends a response when responses it just sends a response when there is a write the course zero gets there is a write the course zero gets there is a write the course zero gets this block a in exclusive state because this block a in exclusive state because this block a in exclusive state because Massey has such a state cores ero can do Massey has such a state cores ero can do Massey has such a state cores ero can do the right without telling anybody so the right without telling anybody so the right without telling anybody so there is no request sent to the there is no request sent to the there is no request sent to the directory and then there is no director directory and then there is no director directory and then there is no director activity as a result of that when core 1 activity as a result of that when core 1 activity as a result of that when core 1 reads a it sends a request to the reads a it sends a request to the reads a it sends a request to the directory the directory will now send a directory the directory will now send a directory the directory will now send a request to the only core that is request to the only core that is request to the only core that is involved in sharing this block which is involved in sharing this block which is involved in sharing this block which is called 0 so it sends one message core 0 called 0 so it sends one message core 0 called 0 so it sends one message core 0 responds with the data and acknowledges responds with the data and acknowledges responds with the data and acknowledges that now it's OK to read this block and that now it's OK to read this block and that now it's OK to read this block and when the directory gets this message it when the directory gets this message it when the directory gets this message it sends to core 1 a response sends to core 1 a response sends to core 1 a response now core 1 can read the block so now we now core 1 can read the block so now we now core 1 can read the block so now we have core 1 has the Block in the shared have core 1 has the Block in the shared have core 1 has the Block in the shared state court 0 is now in the own state state court 0 is now in the own state state court 0 is now in the own state because it downgraded itself from because it downgraded itself from because it downgraded itself from modified to owned core 2 now tries to modified to owned core 2 now tries to modified to owned core 2 now tries to read sends a request to the directory read sends a request to the directory read sends a request to the directory the directory if it has only the state the directory if it has only the state the directory if it has only the state that we have seen on the previous that we have seen on the previous that we have seen on the previous segments does not know who is the owner segments does not know who is the owner segments does not know who is the owner of this block but it doesn't need to of this block but it doesn't need to of this block but it doesn't need to send anything so it gets to memory so send anything so it gets to memory so send anything so it gets to memory so pretty much the directory here doesn't pretty much the directory here doesn't pretty much the directory here doesn't send any messages forwarded because it send any messages forwarded because it send any messages forwarded because it no longer sees the block in the dirty no longer sees the block in the dirty no longer sees the block in the dirty State so it doesn't forward anything it State so it doesn't forward anything it State so it doesn't forward anything it doesn't get responses it just sends a doesn't get responses it just sends a doesn't get responses it just sends a response back to core to note that here response back to core to note that here response back to core to note that here we are unable to benefit from the own we are unable to benefit from the own we are unable to benefit from the own state simply because the directory state simply because the directory state simply because the directory doesn't know to ask core 0 to provide doesn't know to ask core 0 to provide doesn't know to ask core 0 to provide the data with a fancier directory we the data with a fancier directory we the data with a fancier directory we would be able to do that but not with would be able to do that but not with would be able to do that but not with the directory that only tracks presence the directory that only tracks presence the directory that only tracks presence and dirtiness so now C 2 also has the and dirtiness so now C 2 also has the and dirtiness so now C 2 also has the Block in the shared State the same thing Block in the shared State the same thing Block in the shared State the same thing happens for see trees request there is a happens for see trees request there is a happens for see trees request there is a request the directory doesn't forward request the directory doesn't forward request the directory doesn't forward anything just sends a response because anything just sends a response because anything just sends a response because it has to read the data from memory and it has to read the data from memory and it has to read the data from memory and now core 0 now core 0 now core 0 rights because it's in the old state it rights because it's in the old state it rights because it's in the old state it is not able to write without notifying is not able to write without notifying is not able to write without notifying the directory so it notifies the the directory so it notifies the the directory so it notifies the directory this right is an invalidation directory this right is an invalidation directory this right is an invalidation request because we have the data but we request because we have the data but we request because we have the data but we need to send in validations to these need to send in validations to these need to send in validations to these three processors so the directory knows three processors so the directory knows three processors so the directory knows that calls 1 2 &amp; 3 their caches have the that calls 1 2 &amp; 3 their caches have the that calls 1 2 &amp; 3 their caches have the block so it sends 3 messages to these block so it sends 3 messages to these block so it sends 3 messages to these three destinations it gets three three destinations it gets three three destinations it gets three responses from each of them once they responses from each of them once they responses from each of them once they have all acknowledged that they have have all acknowledged that they have have all acknowledged that they have invalidated it sends the response back invalidated it sends the response back invalidated it sends the response back to core 0 and now it's able to write so to core 0 and now it's able to write so to core 0 and now it's able to write so overall we had 5 requests sent to the overall we had 5 requests sent to the overall we had 5 requests sent to the directory and that also means that there directory and that also means that there directory and that also means that there are 5 responses from the directory are 5 responses from the directory are 5 responses from the directory because every request will have a because every request will have a because every request will have a response the directory has forwarded 4 response the directory has forwarded 4 response the directory has forwarded 4 messages messages messages note that this is not like 4 out of 5 note that this is not like 4 out of 5 note that this is not like 4 out of 5 got messages forwarding in fact one of got messages forwarding in fact one of got messages forwarding in fact one of these was for one message and the other these was for one message and the other these was for one message and the other three were for the second message and three were for the second message and three were for the second message and then we got four responses to these then we got four responses to these then we got four responses to these messages again every message needs to messages again every message needs to messages again every message needs to get a response because the directory get a response because the directory get a response because the directory cannot acknowledge and allow a court for cannot acknowledge and allow a court for cannot acknowledge and allow a court for example right until all of the shares example right until all of the shares example right until all of the shares have indicated that they have have indicated that they have have indicated that they have invalidated their state in the cache

## Cache Misses With Coherence： Part 5
Kind: captions Language: en so let's now revisit the topic of cache so let's now revisit the topic of cache so let's now revisit the topic of cache misses when we have coherence going on misses when we have coherence going on misses when we have coherence going on we have seen that caches have three C's we have seen that caches have three C's we have seen that caches have three C's 3 types of misses compulsory misses 3 types of misses compulsory misses 3 types of misses compulsory misses conflict misses and capacity misses conflict misses and capacity misses conflict misses and capacity misses remember that compulsory misses we have remember that compulsory misses we have remember that compulsory misses we have them because we're accessing the block them because we're accessing the block them because we're accessing the block for the first time so because it was for the first time so because it was for the first time so because it was never in the cache we have to have this never in the cache we have to have this never in the cache we have to have this miss capacity misses we have because the miss capacity misses we have because the miss capacity misses we have because the block doesn't fit in a cache of this block doesn't fit in a cache of this block doesn't fit in a cache of this size and conflict misses we have because size and conflict misses we have because size and conflict misses we have because the cache doesn't have enough the cache doesn't have enough the cache doesn't have enough associativity with coherence another see associativity with coherence another see associativity with coherence another see we can now have a cache miss caused by we can now have a cache miss caused by we can now have a cache miss caused by coherence for example when we read coherence for example when we read coherence for example when we read something somebody writes it and we want something somebody writes it and we want something somebody writes it and we want to read it again that would not be a to read it again that would not be a to read it again that would not be a Miss except for coherence so we call Miss except for coherence so we call Miss except for coherence so we call this type of a miss coherence miss so this type of a miss coherence miss so this type of a miss coherence miss so remember that there is actually forces remember that there is actually forces remember that there is actually forces the three we have seen before and the three we have seen before and the three we have seen before and coherence miss there are really two coherence miss there are really two coherence miss there are really two types of coherence misses what we have types of coherence misses what we have types of coherence misses what we have seen so far is the true sharing type of seen so far is the true sharing type of seen so far is the true sharing type of miss which occurs when different cores miss which occurs when different cores miss which occurs when different cores access the same data so naturally we access the same data so naturally we access the same data so naturally we have to perform coherence and thus have have to perform coherence and thus have have to perform coherence and thus have some misses so for example if we read a some misses so for example if we read a some misses so for example if we read a and then write a and then read a again and then write a and then read a again and then write a and then read a again there are some misses necessary in order there are some misses necessary in order there are some misses necessary in order to get the right behavior for the reads to get the right behavior for the reads to get the right behavior for the reads but these were all happening on the same but these were all happening on the same but these were all happening on the same data there is another type of coherence data there is another type of coherence data there is another type of coherence miss called false sharing which occurs miss called false sharing which occurs miss called false sharing which occurs when different cores access different when different cores access different when different cores access different data so there shouldn't be any coherence data so there shouldn't be any coherence data so there shouldn't be any coherence messages because of that except that messages because of that except that messages because of that except that these data items are in the same block these data items are in the same block these data items are in the same block so it's called false sharing because so it's called false sharing because so it's called false sharing because really there is no true data sharing but really there is no true data sharing but really there is no true data sharing but as far as coherence is concerned we do as far as coherence is concerned we do as far as coherence is concerned we do everything at the ground of cache blocks everything at the ground of cache blocks everything at the ground of cache blocks so two items being in the same block so two items being in the same block so two items being in the same block makes them behave like the same item as makes them behave like the same item as makes them behave like the same item as far as coherence is concerned

## False Sharing Quiz： Part 5
Kind: captions Language: en so let's see an example of false sharing so let's see an example of false sharing so let's see an example of false sharing let's say that these are two memory let's say that these are two memory let's say that these are two memory blocks block zero and block 1 the words blocks block zero and block 1 the words blocks block zero and block 1 the words in block 0 will be a B C and D so for in block 0 will be a B C and D so for in block 0 will be a B C and D so for example this is a 16 byte block that has example this is a 16 byte block that has example this is a 16 byte block that has 4 4 byte words and the words in block 1 4 4 byte words and the words in block 1 4 4 byte words and the words in block 1 are X Y Z and W so which of these are X Y Z and W so which of these are X Y Z and W so which of these execution fragments have for sharing execution fragments have for sharing execution fragments have for sharing misses if these accesses happen misses if these accesses happen misses if these accesses happen are there any false sharing misses if are there any false sharing misses if are there any false sharing misses if these accesses happen these accesses happen these accesses happen are there any false sharing misses and are there any false sharing misses and are there any false sharing misses and if these accesses happen are there any if these accesses happen are there any if these accesses happen are there any false sharing misses

## False Sharing Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our full let's look at the solution to our full let's look at the solution to our full sharing quiz we have two blocks a block sharing quiz we have two blocks a block sharing quiz we have two blocks a block that has a B C and D in it and the block that has a B C and D in it and the block that has a B C and D in it and the block that has X Y Z and W in it we're looking that has X Y Z and W in it we're looking that has X Y Z and W in it we're looking for which of these if any have for for which of these if any have for for which of these if any have for sharing misses so let's first look at sharing misses so let's first look at sharing misses so let's first look at this one there is a read X and the write this one there is a read X and the write this one there is a read X and the write X the first will be a Miss the second X the first will be a Miss the second X the first will be a Miss the second will also be a Miss both of these are will also be a Miss both of these are will also be a Miss both of these are compulsory misses X wasn't in c0 s cache compulsory misses X wasn't in c0 s cache compulsory misses X wasn't in c0 s cache X wasn't in C ones cache so they have to X wasn't in C ones cache so they have to X wasn't in C ones cache so they have to be compulsory misses a read a here is be compulsory misses a read a here is be compulsory misses a read a here is also a compulsory miss this block wasn't also a compulsory miss this block wasn't also a compulsory miss this block wasn't in c2s cache finally c3 is writing to be in c2s cache finally c3 is writing to be in c2s cache finally c3 is writing to be this block wasn't in see trees cache so this block wasn't in see trees cache so this block wasn't in see trees cache so this is also a compulsory miss so in this is also a compulsory miss so in this is also a compulsory miss so in fact this sequence has no coherence fact this sequence has no coherence fact this sequence has no coherence misses whatsoever all of them are misses whatsoever all of them are misses whatsoever all of them are compulsory misses now let's look here compulsory misses now let's look here compulsory misses now let's look here this is a compulsory miss for c0 this is this is a compulsory miss for c0 this is this is a compulsory miss for c0 this is a compulsory miss for c1 this is a a compulsory miss for c1 this is a a compulsory miss for c1 this is a compulsory miss for c2 and this is a compulsory miss for c2 and this is a compulsory miss for c2 and this is a compulsory miss to c3 so regardless compulsory miss to c3 so regardless compulsory miss to c3 so regardless whether we had coherence or not these whether we had coherence or not these whether we had coherence or not these four accesses would be misses because four accesses would be misses because four accesses would be misses because each of these caches needs to fetch the each of these caches needs to fetch the each of these caches needs to fetch the block for the first time so there are no block for the first time so there are no block for the first time so there are no coherence misses in this sequence either coherence misses in this sequence either coherence misses in this sequence either now let's look here c0 reads X c1 writes now let's look here c0 reads X c1 writes now let's look here c0 reads X c1 writes to X this invalidates X in C 0 this to X this invalidates X in C 0 this to X this invalidates X in C 0 this invalidation needs to happen in order invalidation needs to happen in order invalidation needs to happen in order for X to be written in C 1 C 2 now for X to be written in C 1 C 2 now for X to be written in C 1 C 2 now writes to W that invalidates the data in writes to W that invalidates the data in writes to W that invalidates the data in see once cache the data and sees zeros see once cache the data and sees zeros see once cache the data and sees zeros cache was already invalidated at that cache was already invalidated at that cache was already invalidated at that point when C 0 tries to read X the point when C 0 tries to read X the point when C 0 tries to read X the reason it doesn't have the data in its reason it doesn't have the data in its reason it doesn't have the data in its cache is because C 1 invalidated it and cache is because C 1 invalidated it and cache is because C 1 invalidated it and c1 was accessing the same word so this c1 was accessing the same word so this c1 was accessing the same word so this is an example of true sharing is an example of true sharing is an example of true sharing we got invalidated because somebody was we got invalidated because somebody was we got invalidated because somebody was trying to write the same world that we trying to write the same world that we trying to write the same world that we were accessing so this is actually a were accessing so this is actually a were accessing so this is actually a sequence of three compulsory misses sequence of three compulsory misses sequence of three compulsory misses followed by a true sharing coherence followed by a true sharing coherence followed by a true sharing coherence miss so in this sequence there is also miss so in this sequence there is also miss so in this sequence there is also no false sharing so none of these should no false sharing so none of these should no false sharing so none of these should have been selected

## Coherence Outro： Part 5
Kind: captions Language: en in this lesson we have seen that we do in this lesson we have seen that we do in this lesson we have seen that we do need each core to have its own cache but need each core to have its own cache but need each core to have its own cache but that these caches have to be kept that these caches have to be kept that these caches have to be kept coherent so shared memory programs can coherent so shared memory programs can coherent so shared memory programs can work work work we then learn several ways of keeping we then learn several ways of keeping we then learn several ways of keeping caches coherent caches coherent caches coherent in the next lesson we will see what we in the next lesson we will see what we in the next lesson we will see what we need to do so course can coordinate need to do so course can coordinate need to do so course can coordinate their work in a parallel program

## Synchronization Intro： Part 5
Kind: captions Language: en in this lesson we will learn how to in this lesson we will learn how to in this lesson we will learn how to provide efficient synchronization among provide efficient synchronization among provide efficient synchronization among cores and threads that are trying to cores and threads that are trying to cores and threads that are trying to work together on the same program

## Synchronization Example： Part 5
Kind: captions Language: en so let's look at one example of why we so let's look at one example of why we so let's look at one example of why we may need synchronization so let's say we may need synchronization so let's say we may need synchronization so let's say we have two threads and what they're doing have two threads and what they're doing have two threads and what they're doing is counting occurrences of different is counting occurrences of different is counting occurrences of different letters in a letter the first thread letters in a letter the first thread letters in a letter the first thread let's call it thread a is counting in let's call it thread a is counting in let's call it thread a is counting in the first half of the document so it's the first half of the document so it's the first half of the document so it's going to count occurrences of each going to count occurrences of each going to count occurrences of each letter in the first half of the document letter in the first half of the document letter in the first half of the document and the second thread let's call that and the second thread let's call that and the second thread let's call that thread B is counting in the second half thread B is counting in the second half thread B is counting in the second half of the document and then they will put of the document and then they will put of the document and then they will put these counts together somehow so the these counts together somehow so the these counts together somehow so the code might look like this a thread will code might look like this a thread will code might look like this a thread will load a letter load from memory the count load a letter load from memory the count load a letter load from memory the count for that letter add one to that count for that letter add one to that count for that letter add one to that count and then store the new count back into and then store the new count back into and then store the new count back into memory while thread a is doing this memory while thread a is doing this memory while thread a is doing this thread B might be doing this it loads a thread B might be doing this it loads a thread B might be doing this it loads a letter from its own part of the array letter from its own part of the array letter from its own part of the array note that R1 and R2 are different in note that R1 and R2 are different in note that R1 and R2 are different in these threads this is kind of the these threads this is kind of the these threads this is kind of the pointer to where I got in this part of pointer to where I got in this part of pointer to where I got in this part of the array this is the pointer where this the array this is the pointer where this the array this is the pointer where this thread got in its part of the array So thread got in its part of the array So thread got in its part of the array So This Thread is doing exactly the same This Thread is doing exactly the same This Thread is doing exactly the same work as this one and as long as the work as this one and as long as the work as this one and as long as the letters they're processing are different letters they're processing are different letters they're processing are different this is going to work this thread is this is going to work this thread is this is going to work this thread is incrementing one part of the count array incrementing one part of the count array incrementing one part of the count array this one is incrementing something else this one is incrementing something else this one is incrementing something else in the count array but what if this in the count array but what if this in the count array but what if this thread sees letter a and this thread at thread sees letter a and this thread at thread sees letter a and this thread at the same time sees another letter A in the same time sees another letter A in the same time sees another letter A in its own part of the array then what its own part of the array then what its own part of the array then what happens is they both try to load the happens is they both try to load the happens is they both try to load the same element from the count aray let's same element from the count aray let's same element from the count aray let's say the count was 15 so this one will say the count was 15 so this one will say the count was 15 so this one will load 15 and this one will load 15 now we load 15 and this one will load 15 now we load 15 and this one will load 15 now we add one and we get 16 and we do the same add one and we get 16 and we do the same add one and we get 16 and we do the same here finally we store 16 to memory and here finally we store 16 to memory and here finally we store 16 to memory and here we also store 16 to memory so now here we also store 16 to memory so now here we also store 16 to memory so now there were two occurrences of letter A there were two occurrences of letter A there were two occurrences of letter A but the value in the array ends up being but the value in the array ends up being but the value in the array ends up being 16 which is only one more than the 16 which is only one more than the 16 which is only one more than the original 15 so after both of these original 15 so after both of these original 15 so after both of these threads are done updating the counter threads are done updating the counter threads are done updating the counter the counter should have been 17 so the counter should have been 17 so the counter should have been 17 so clearly this is incorrect Behavior what clearly this is incorrect Behavior what clearly this is incorrect Behavior what we need is really that this happens and we need is really that this happens and we need is really that this happens and then this happens or the other way then this happens or the other way then this happens or the other way around but these should not be happening around but these should not be happening around but these should not be happening simultaneously because then we can get simultaneously because then we can get simultaneously because then we can get the incorrect result if for example this the incorrect result if for example this the incorrect result if for example this happens first we load we get 15 happens first we load we get 15 happens first we load we get 15 increment store 16 now when we load this increment store 16 now when we load this increment store 16 now when we load this here cache coherence ensures that we here cache coherence ensures that we here cache coherence ensures that we load 16 and we end up with 17 here which load 16 and we end up with 17 here which load 16 and we end up with 17 here which is a correct result similarly if we did is a correct result similarly if we did is a correct result similarly if we did this first it would produce 16 in memory this first it would produce 16 in memory this first it would produce 16 in memory then we would load that in increment to then we would load that in increment to then we would load that in increment to 17 so no matter which one of these 17 so no matter which one of these 17 so no matter which one of these happens first we should get the correct happens first we should get the correct happens first we should get the correct result but we don't get the correct result but we don't get the correct result but we don't get the correct result if they happen in an interleaved result if they happen in an interleaved result if they happen in an interleaved fashion they should happen one at a time fashion they should happen one at a time fashion they should happen one at a time either this and this or all of this and either this and this or all of this and either this and this or all of this and then all of this so these things that then all of this so these things that then all of this so these things that have to execute kind of one at a time have to execute kind of one at a time have to execute kind of one at a time are called Atomic or critical sections are called Atomic or critical sections are called Atomic or critical sections so we have one critical section here and so we have one critical section here and so we have one critical section here and another one here they can actually be another one here they can actually be another one here they can actually be the same code it's just that both the same code it's just that both the same code it's just that both threads are currently in the same piece threads are currently in the same piece threads are currently in the same piece of code but as you can see if we have a of code but as you can see if we have a of code but as you can see if we have a section that should behave as a critical section that should behave as a critical section that should behave as a critical section meaning we do this without section meaning we do this without section meaning we do this without interference from any other thread if we interference from any other thread if we interference from any other thread if we just let it happen it's not going to be just let it happen it's not going to be just let it happen it's not going to be done correctly so what we need is done correctly so what we need is done correctly so what we need is synchronization we need additional code synchronization we need additional code synchronization we need additional code that ensures that only one thread gets that ensures that only one thread gets that ensures that only one thread gets to do this at any given time

## Synchronization Example   Lock： Part 5
Kind: captions Language: en the type of synchronization we use for the type of synchronization we use for the type of synchronization we use for atomic sections is called mutual atomic sections is called mutual atomic sections is called mutual exclusion or 'rock so this is really an exclusion or 'rock so this is really an exclusion or 'rock so this is really an example of where a lock will be needed example of where a lock will be needed example of where a lock will be needed what we really need to do is say what we really need to do is say what we really need to do is say something like I'm entering don't come something like I'm entering don't come something like I'm entering don't come in here then do this then say I'm in here then do this then say I'm in here then do this then say I'm leaving now you can come in and when leaving now you can come in and when leaving now you can come in and when this one is done this one can then say this one is done this one can then say this one is done this one can then say I'm entering I'm leaving but if this I'm entering I'm leaving but if this I'm entering I'm leaving but if this thread comes and wants to continue past thread comes and wants to continue past thread comes and wants to continue past this point while the first thread is this point while the first thread is this point while the first thread is already in the critical section we already in the critical section we already in the critical section we should wait here so what we want here should wait here so what we want here should wait here so what we want here really is before we get into the really is before we get into the really is before we get into the critical section we need to lock a lock critical section we need to lock a lock critical section we need to lock a lock let's call that count lock that let's call that count lock that let's call that count lock that corresponds to a given element of this corresponds to a given element of this corresponds to a given element of this array so count lock of L is a lock that array so count lock of L is a lock that array so count lock of L is a lock that corresponds to count of L if this lock corresponds to count of L if this lock corresponds to count of L if this lock currently is open then we will enter if currently is open then we will enter if currently is open then we will enter if this lock is currently locked then we this lock is currently locked then we this lock is currently locked then we will spin there until it becomes open will spin there until it becomes open will spin there until it becomes open once we have grabbed a lock we can enter once we have grabbed a lock we can enter once we have grabbed a lock we can enter here the other thread will do the same here the other thread will do the same here the other thread will do the same so the first one of them that succeeds so the first one of them that succeeds so the first one of them that succeeds in grabbing the lock will enter this in grabbing the lock will enter this in grabbing the lock will enter this critical section the other one will now critical section the other one will now critical section the other one will now realize that this count lock has been realize that this count lock has been realize that this count lock has been locked and we'll have to wait there once locked and we'll have to wait there once locked and we'll have to wait there once we leave the critical section we will we leave the critical section we will we leave the critical section we will unlock the lock and now if this one is unlock the lock and now if this one is unlock the lock and now if this one is waiting here at that point it sees that waiting here at that point it sees that waiting here at that point it sees that the lock is now available and starts the lock is now available and starts the lock is now available and starts executing the critical section and then executing the critical section and then executing the critical section and then releases the lock note that having the releases the lock note that having the releases the lock note that having the lock and unlock and forces mutual lock and unlock and forces mutual lock and unlock and forces mutual exclusion of this code we do this alone exclusion of this code we do this alone exclusion of this code we do this alone and we do this alone but it doesn't and we do this alone but it doesn't and we do this alone but it doesn't impose a particular order between them impose a particular order between them impose a particular order between them so whichever of them comes first to this so whichever of them comes first to this so whichever of them comes first to this lock will execute it's critical section lock will execute it's critical section lock will execute it's critical section first so we can either have this happen first so we can either have this happen first so we can either have this happen first and then this happens second or we first and then this happens second or we first and then this happens second or we can have this happens first and this can have this happens first and this can have this happens first and this happens second what the lock is really happens second what the lock is really happens second what the lock is really preventing is interleaving where these preventing is interleaving where these preventing is interleaving where these are happening simultaneously but it are happening simultaneously but it are happening simultaneously but it doesn't prevent for example this second doesn't prevent for example this second doesn't prevent for example this second thread doing this first and only then thread doing this first and only then thread doing this first and only then the first thread gets to do it



---


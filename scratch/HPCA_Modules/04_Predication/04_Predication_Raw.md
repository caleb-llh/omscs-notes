# 04_Predication (Raw Transcripts)

# Playlist 0 Module 10 Raw Transcript

## Hierarchical Predictors： Part 1
Kind: captions Language: en now let's see another type of predictor now let's see another type of predictor now let's see another type of predictor that combines prediction decisions which that combines prediction decisions which that combines prediction decisions which is called a hierarchical predictor it is is called a hierarchical predictor it is is called a hierarchical predictor it is similar to a tournament predictor but similar to a tournament predictor but similar to a tournament predictor but the tournament predictor typically is the tournament predictor typically is the tournament predictor typically is trying to combine two good predictors trying to combine two good predictors trying to combine two good predictors one is better for some branches the one is better for some branches the one is better for some branches the other for others a hierarchical other for others a hierarchical other for others a hierarchical predictor is trying to combine a good predictor is trying to combine a good predictor is trying to combine a good predictor with an okay predictor here predictor with an okay predictor here predictor with an okay predictor here you're basically doing two good you're basically doing two good you're basically doing two good predictions for every Branch just to use predictions for every Branch just to use predictions for every Branch just to use one of them and each of the good one of them and each of the good one of them and each of the good predictors costs you a lot for each predictors costs you a lot for each predictors costs you a lot for each entry here the idea is that you have a entry here the idea is that you have a entry here the idea is that you have a very good predictor that costs you a lot very good predictor that costs you a lot very good predictor that costs you a lot for its entry but you don't want to be for its entry but you don't want to be for its entry but you don't want to be using the good predictor for branches using the good predictor for branches using the good predictor for branches that are very easy to predict anyway so that are very easy to predict anyway so that are very easy to predict anyway so the idea is that for those branches you the idea is that for those branches you the idea is that for those branches you want to use the okay predictor and use want to use the okay predictor and use want to use the okay predictor and use the good predictor entries which are the good predictor entries which are the good predictor entries which are very expensive only for branches that very expensive only for branches that very expensive only for branches that really need that good predictor so in really need that good predictor so in really need that good predictor so in the tournament predictor you will update the tournament predictor you will update the tournament predictor you will update both predictors on each decision so that both predictors on each decision so that both predictors on each decision so that they can both stay current with what's they can both stay current with what's they can both stay current with what's going on and thus they both try to do going on and thus they both try to do going on and thus they both try to do their best on each branch decision but their best on each branch decision but their best on each branch decision but in the hierarchical predictor you might in the hierarchical predictor you might in the hierarchical predictor you might update the okay predictor on each update the okay predictor on each update the okay predictor on each decision but you update the good decision but you update the good decision but you update the good predictor only if the okay predictor did predictor only if the okay predictor did predictor only if the okay predictor did not work well for that Branch the idea not work well for that Branch the idea not work well for that Branch the idea here is to not use the entries of the here is to not use the entries of the here is to not use the entries of the good predictor if the okay one is doing good predictor if the okay one is doing good predictor if the okay one is doing fine on this Branch so really what that fine on this Branch so really what that fine on this Branch so really what that means is that we can have very expensive means is that we can have very expensive means is that we can have very expensive entries in the good predictor and we entries in the good predictor and we entries in the good predictor and we have relatively few entries because most have relatively few entries because most have relatively few entries because most of the branches will be handled just of the branches will be handled just of the branches will be handled just fine with the okay predictor if we try fine with the okay predictor if we try fine with the okay predictor if we try to compare the tournament predictor with to compare the tournament predictor with to compare the tournament predictor with the hierarchical predictor the the hierarchical predictor the the hierarchical predictor the hierarchical one usually wins because it hierarchical one usually wins because it hierarchical one usually wins because it turns out that there are many many turns out that there are many many turns out that there are many many branches that can be predicted just fine branches that can be predicted just fine branches that can be predicted just fine using something like just a simple 2bit using something like just a simple 2bit using something like just a simple 2bit counter on the other hand there are some counter on the other hand there are some counter on the other hand there are some branches that really require a very branches that really require a very branches that really require a very strong predictor to predict them in a strong predictor to predict them in a strong predictor to predict them in a tournament predictor each of the tournament predictor each of the tournament predictor each of the predictors is a balance between cost and predictors is a balance between cost and predictors is a balance between cost and accuracy in this predictor you can build accuracy in this predictor you can build accuracy in this predictor you can build a very expensive predictor with very few a very expensive predictor with very few a very expensive predictor with very few entries and an okay predictor that has a entries and an okay predictor that has a entries and an okay predictor that has a lot of Entry so most branches get lot of Entry so most branches get lot of Entry so most branches get handled by this which leaves the rest of handled by this which leaves the rest of handled by this which leaves the rest of the resources for the good predictor and the resources for the good predictor and the resources for the good predictor and that means that it can have extremely that means that it can have extremely that means that it can have extremely long histories or be a very very fancy long histories or be a very very fancy long histories or be a very very fancy predictor you can even have a hierarchy predictor you can even have a hierarchy predictor you can even have a hierarchy or for that matter a tournament with or for that matter a tournament with or for that matter a tournament with more than just two predictors

## Hierarchical Predictor Example： Part 1
Kind: captions Language: en so let's look at a real example of a so let's look at a real example of a so let's look at a real example of a hierarchical predictor we will look at hierarchical predictor we will look at hierarchical predictor we will look at intel's pentium m processor that has intel's pentium m processor that has intel's pentium m processor that has these predictors first it has these predictors first it has these predictors first it has a cheap predictor that contains just two a cheap predictor that contains just two a cheap predictor that contains just two bit counters then it has a local history bit counters then it has a local history bit counters then it has a local history predictor and a global history predictor predictor and a global history predictor predictor and a global history predictor so what the processor is doing is it has so what the processor is doing is it has so what the processor is doing is it has a large array of two bit counters a large array of two bit counters a large array of two bit counters a local history predictor that stores a local history predictor that stores a local history predictor that stores the local history for each branch and an the local history for each branch and an the local history for each branch and an array of two-bit counters for different array of two-bit counters for different array of two-bit counters for different histories and the global history histories and the global history histories and the global history predictor with a global history that is predictor with a global history that is predictor with a global history that is longer than the local histories here and longer than the local histories here and longer than the local histories here and also an array of counters for that also an array of counters for that also an array of counters for that predictor predictor predictor so if we try to predict a single branch so if we try to predict a single branch so if we try to predict a single branch what we would do is we would look up what we would do is we would look up what we would do is we would look up the pc for that branch in the two bit the pc for that branch in the two bit the pc for that branch in the two bit counter array we will also try to look counter array we will also try to look counter array we will also try to look up the local predictor and the global up the local predictor and the global up the local predictor and the global predictor prediction predictor prediction predictor prediction and then we form the actual prediction and then we form the actual prediction and then we form the actual prediction for the processor by using the result of for the processor by using the result of for the processor by using the result of the global predictor if the global the global predictor if the global the global predictor if the global predictor says that the branch is predictor says that the branch is predictor says that the branch is predicted here and for that what we do predicted here and for that what we do predicted here and for that what we do is we keep another array here that is is we keep another array here that is is we keep another array here that is just saying whether this branch so this just saying whether this branch so this just saying whether this branch so this is kind of like a tag array that says is kind of like a tag array that says is kind of like a tag array that says whether this branch is going to be whether this branch is going to be whether this branch is going to be predicted by the global predictor predicted by the global predictor predicted by the global predictor if the global predictor does not have a if the global predictor does not have a if the global predictor does not have a matching tag that means that this branch matching tag that means that this branch matching tag that means that this branch was not inserted in the global predictor was not inserted in the global predictor was not inserted in the global predictor in which case we shouldn't use it in which case we shouldn't use it in which case we shouldn't use it if that happens we will use the if that happens we will use the if that happens we will use the prediction from the local predictor prediction from the local predictor prediction from the local predictor which will also have a tag array that which will also have a tag array that which will also have a tag array that says whether this predictor should be says whether this predictor should be says whether this predictor should be predicted by the local predictor and if predicted by the local predictor and if predicted by the local predictor and if so so so then we use that otherwise if the branch then we use that otherwise if the branch then we use that otherwise if the branch is not in the local predictor we just is not in the local predictor we just is not in the local predictor we just use the result of the two-bit counter use the result of the two-bit counter use the result of the two-bit counter when we update the predictor however when we update the predictor however when we update the predictor however what we do is we update the two-bit what we do is we update the two-bit what we do is we update the two-bit counter if we counter if we counter if we have the branch in the local predictor have the branch in the local predictor have the branch in the local predictor we update the local predictor if we have we update the local predictor if we have we update the local predictor if we have the branch in the global predictor we the branch in the global predictor we the branch in the global predictor we update the global predictor but then update the global predictor but then update the global predictor but then what happens is if we are using the what happens is if we are using the what happens is if we are using the two-bit counter and the branch is not in two-bit counter and the branch is not in two-bit counter and the branch is not in the local predictor then what we do is the local predictor then what we do is the local predictor then what we do is if the two-bit counter is mispredicting if the two-bit counter is mispredicting if the two-bit counter is mispredicting the branch we insert it into the local the branch we insert it into the local the branch we insert it into the local predictor so now the branch will be predictor so now the branch will be predictor so now the branch will be present here next time present here next time present here next time if the local predictor is not giving us if the local predictor is not giving us if the local predictor is not giving us good prediction for this branch then we good prediction for this branch then we good prediction for this branch then we insert the branch into the global insert the branch into the global insert the branch into the global predictor so what's going to happen is predictor so what's going to happen is predictor so what's going to happen is pretty much branches that are almost pretty much branches that are almost pretty much branches that are almost perfectly predictable by two-bit perfectly predictable by two-bit perfectly predictable by two-bit counters and that's a lot of branches counters and that's a lot of branches counters and that's a lot of branches for example for example for example every every every always taken branch or branch that is always taken branch or branch that is always taken branch or branch that is dominantly taken is going to be mostly dominantly taken is going to be mostly dominantly taken is going to be mostly predicted by the 2-bit predictor so we predicted by the 2-bit predictor so we predicted by the 2-bit predictor so we never really need to consult the local never really need to consult the local never really need to consult the local and global predictors and that saves and global predictors and that saves and global predictors and that saves space in these predictors space in these predictors space in these predictors for other branches that need them for other branches that need them for other branches that need them saving space means that we can have a saving space means that we can have a saving space means that we can have a lot fewer of the two bit entries than lot fewer of the two bit entries than lot fewer of the two bit entries than the history entries than we would the history entries than we would the history entries than we would otherwise need if we were trying to otherwise need if we were trying to otherwise need if we were trying to predict all the branches with the local predict all the branches with the local predict all the branches with the local or even the global predictor so how do or even the global predictor so how do or even the global predictor so how do we say whether the branch is present or we say whether the branch is present or we say whether the branch is present or not well what we really do is we insert not well what we really do is we insert not well what we really do is we insert some bits of the branch's address some bits of the branch's address some bits of the branch's address into the into the into the corresponding line of the predictor so corresponding line of the predictor so corresponding line of the predictor so the history entry here we will index the history entry here we will index the history entry here we will index with some bits of the pc to find the with some bits of the pc to find the with some bits of the pc to find the history and in that same entry we will history and in that same entry we will history and in that same entry we will also insert some of the upper bits of also insert some of the upper bits of also insert some of the upper bits of the pc so that different branches that the pc so that different branches that the pc so that different branches that map to this entry only one of them gets map to this entry only one of them gets map to this entry only one of them gets to be predicted by this entry the rest to be predicted by this entry the rest to be predicted by this entry the rest of them say that they are not found here of them say that they are not found here of them say that they are not found here so the idea is that if most of the so the idea is that if most of the so the idea is that if most of the branches that end up mapping to this branches that end up mapping to this branches that end up mapping to this entry are really predictable by the two entry are really predictable by the two entry are really predictable by the two bit predictor then we don't really need bit predictor then we don't really need bit predictor then we don't really need to use this entry for those branches

## Multi Predictor Quiz： Part 1
Kind: captions Language: en so let's see if we know how to use so let's see if we know how to use so let's see if we know how to use multiple predictors and combine their multiple predictors and combine their multiple predictors and combine their decisions suppose that we have a program decisions suppose that we have a program decisions suppose that we have a program in which a 2-bit predictor work just in which a 2-bit predictor work just in which a 2-bit predictor work just fine for 95% of our instructions a fine for 95% of our instructions a fine for 95% of our instructions a pshare predictor we just fine for the pshare predictor we just fine for the pshare predictor we just fine for the same 95% of the instructions plus it same 95% of the instructions plus it same 95% of the instructions plus it also works fine for 2% that this 2bit also works fine for 2% that this 2bit also works fine for 2% that this 2bit predictor doesn't cover so overall it predictor doesn't cover so overall it predictor doesn't cover so overall it works fine for 97% of all instructions works fine for 97% of all instructions works fine for 97% of all instructions but the 95 % here and here overlap a but the 95 % here and here overlap a but the 95 % here and here overlap a gshare predictor again works just fine gshare predictor again works just fine gshare predictor again works just fine for the same 95% that all the other for the same 95% that all the other for the same 95% that all the other predictors are just fine for plus it predictors are just fine for plus it predictors are just fine for plus it works fine for the other 3% so overall works fine for the other 3% so overall works fine for the other 3% so overall it's 98% accurate 95 of those are the it's 98% accurate 95 of those are the it's 98% accurate 95 of those are the same as the 2bit predictor but the three same as the 2bit predictor but the three same as the 2bit predictor but the three that it covers in addition to that are that it covers in addition to that are that it covers in addition to that are not the same but the three that it not the same but the three that it not the same but the three that it covers in addition to that are different covers in addition to that are different covers in addition to that are different from the two % that the pshare predictor from the two % that the pshare predictor from the two % that the pshare predictor covers so together these three covers so together these three covers so together these three predictors should be able to correctly predictors should be able to correctly predictors should be able to correctly predict all predict all predict all instructions so the question for you is instructions so the question for you is instructions so the question for you is how do we combine them so the overall how do we combine them so the overall how do we combine them so the overall predictor that we will use is a what predictor that we will use is a what predictor that we will use is a what kind of predictor that chooses between a kind of predictor that chooses between a kind of predictor that chooses between a what kind of predictor and what kind of what kind of predictor and what kind of what kind of predictor and what kind of predictor and this last predictor here predictor and this last predictor here predictor and this last predictor here itself will choose between what kind and itself will choose between what kind and itself will choose between what kind and what kind predictor what kind predictor what kind predictor instead of writing in the names of the instead of writing in the names of the instead of writing in the names of the predictors just select among the predictors just select among the predictors just select among the following so for each of them say if following so for each of them say if following so for each of them say if it's a b c d or E A is the two-bit it's a b c d or E A is the two-bit it's a b c d or E A is the two-bit predictor B is the pshare predictor C is predictor B is the pshare predictor C is predictor B is the pshare predictor C is the gshare predictor D is the tournament the gshare predictor D is the tournament the gshare predictor D is the tournament predictor e is a hierarchical predictor predictor e is a hierarchical predictor predictor e is a hierarchical predictor and F is the return address stag so and F is the return address stag so and F is the return address stag so again for each of these five boxes put again for each of these five boxes put again for each of these five boxes put which letter corresponds to the which letter corresponds to the which letter corresponds to the predictor that is the right answer for predictor that is the right answer for predictor that is the right answer for that box

## Multi Predictor Solution Quiz： Part 1
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our multi-predictor quiz where we have a multi-predictor quiz where we have a multi-predictor quiz where we have a program for which the two-bit predictor program for which the two-bit predictor program for which the two-bit predictor works for many instructions works for many instructions works for many instructions p-share p-share p-share covers some additional instructions and covers some additional instructions and covers some additional instructions and g-share covers the rest that p-share g-share covers the rest that p-share g-share covers the rest that p-share doesn't cover doesn't cover doesn't cover but all of them can correctly predict but all of them can correctly predict but all of them can correctly predict the same branches that the 2-bit the same branches that the 2-bit the same branches that the 2-bit predictor can predictor can predictor can so because the 2-bit predictor covers so because the 2-bit predictor covers so because the 2-bit predictor covers the most branches and it's the cheapest the most branches and it's the cheapest the most branches and it's the cheapest predictor what we want to do predictor what we want to do predictor what we want to do is is is use the 2-bit predictor for most of the use the 2-bit predictor for most of the use the 2-bit predictor for most of the branches branches branches so the correct answer here is e so the correct answer here is e so the correct answer here is e hierarchical predictor hierarchical predictor hierarchical predictor that chooses between that chooses between that chooses between a 2-bit predictor a 2-bit predictor a 2-bit predictor and a and a and a predictor that combines the patient and predictor that combines the patient and predictor that combines the patient and g-share g-share g-share so what this first part does is it so what this first part does is it so what this first part does is it basically makes most of the branches basically makes most of the branches basically makes most of the branches just go to the two-bit predictor so that just go to the two-bit predictor so that just go to the two-bit predictor so that the space in the more expensive the space in the more expensive the space in the more expensive predictor here predictor here predictor here will be reserved for only the branches will be reserved for only the branches will be reserved for only the branches that actually need to go there because that actually need to go there because that actually need to go there because the two bit predictor is not predicting the two bit predictor is not predicting the two bit predictor is not predicting them correctly because pischer and them correctly because pischer and them correctly because pischer and g-share have different strengths g-share have different strengths g-share have different strengths and neither of them is clearly superior and neither of them is clearly superior and neither of them is clearly superior to the other we are not going to combine to the other we are not going to combine to the other we are not going to combine them using a hierarchical predictor them using a hierarchical predictor them using a hierarchical predictor we are going to combine them using a we are going to combine them using a we are going to combine them using a tournament predictor tournament predictor tournament predictor that will choose between that will choose between that will choose between p share p share p share and g share and g share and g share so the correct answer is that the so the correct answer is that the so the correct answer is that the overall predictor is a hierarchical overall predictor is a hierarchical overall predictor is a hierarchical predictor that chooses between a predictor that chooses between a predictor that chooses between a two bit predictor and two bit predictor and two bit predictor and a tournament predictor which itself a tournament predictor which itself a tournament predictor which itself chooses between p-share and g-share or chooses between p-share and g-share or chooses between p-share and g-share or the other way around

## Return Address Stack (RAS)： Part 1
Kind: captions Language: en now let's talk about the return address now let's talk about the return address now let's talk about the return address stack predictor or Ras we have seen that stack predictor or Ras we have seen that stack predictor or Ras we have seen that there are several types of branches that there are several types of branches that there are several types of branches that we need to predict for conditional we need to predict for conditional we need to predict for conditional branches we need to predict the branches we need to predict the branches we need to predict the direction is it taken or not and by now direction is it taken or not and by now direction is it taken or not and by now we have seen a number of predictors that we have seen a number of predictors that we have seen a number of predictors that predict Direction reasonably well for predict Direction reasonably well for predict Direction reasonably well for these types of things if this branch is these types of things if this branch is these types of things if this branch is taken we will need to also predict the taken we will need to also predict the taken we will need to also predict the target address and for that a simple btb target address and for that a simple btb target address and for that a simple btb that just remembers the previous Target that just remembers the previous Target that just remembers the previous Target when the branch was taken will do just when the branch was taken will do just when the branch was taken will do just fine because we always have the same fine because we always have the same fine because we always have the same Target another type of branches are Target another type of branches are Target another type of branches are unconditional jumps function calls Etc unconditional jumps function calls Etc unconditional jumps function calls Etc as far as their direction is concerned as far as their direction is concerned as far as their direction is concerned it's trivial they are always it's trivial they are always it's trivial they are always taken so even the simplest predictor of taken so even the simplest predictor of taken so even the simplest predictor of the direction predictor type will do the direction predictor type will do the direction predictor type will do well on these as far as the target well on these as far as the target well on these as far as the target prediction is concerned most of these prediction is concerned most of these prediction is concerned most of these either jump to a label or call a either jump to a label or call a either jump to a label or call a specific function so a simple btb that specific function so a simple btb that specific function so a simple btb that just remembers the previous Target when just remembers the previous Target when just remembers the previous Target when this instruction was executed will do this instruction was executed will do this instruction was executed will do just fine so it looks like we have just fine so it looks like we have just fine so it looks like we have handled both the direction and the handled both the direction and the handled both the direction and the target for most of the common Branch target for most of the common Branch target for most of the common Branch types however there is a type of Branch types however there is a type of Branch types however there is a type of Branch specifically the function returns which specifically the function returns which specifically the function returns which is always taken so the direction is always taken so the direction is always taken so the direction prediction will do just fine but the prediction will do just fine but the prediction will do just fine but the target is often difficult to predict if target is often difficult to predict if target is often difficult to predict if this function is always called from the this function is always called from the this function is always called from the same place then the return will always same place then the return will always same place then the return will always jump back to the same location and in jump back to the same location and in jump back to the same location and in that case the the btb will do fine but that case the the btb will do fine but that case the the btb will do fine but usually we create a function so that it usually we create a function so that it usually we create a function so that it can be called from multiple places in can be called from multiple places in can be called from multiple places in the program something like this we have the program something like this we have the program something like this we have an instruction that can call this an instruction that can call this an instruction that can call this function somewhere else in the program function somewhere else in the program function somewhere else in the program we have another instruction that can we have another instruction that can we have another instruction that can call the same function this function has call the same function this function has call the same function this function has a return instruction and now the problem a return instruction and now the problem a return instruction and now the problem is that this return instruction should is that this return instruction should is that this return instruction should either jump here or here depending on either jump here or here depending on either jump here or here depending on where it was called from the btb in this where it was called from the btb in this where it was called from the btb in this case will not do too well why well case will not do too well why well case will not do too well why well because when we call this and return because when we call this and return because when we call this and return here the btb will remember that this is here the btb will remember that this is here the btb will remember that this is the target address then we reach this the target address then we reach this the target address then we reach this point call again return we should be point call again return we should be point call again return we should be returning here but the btb is predicting returning here but the btb is predicting returning here but the btb is predicting this so we have a misprediction the btb this so we have a misprediction the btb this so we have a misprediction the btb now will learn that this is the correct now will learn that this is the correct now will learn that this is the correct Target next we come back to this code Target next we come back to this code Target next we come back to this code again and again the btb is predicting again and again the btb is predicting again and again the btb is predicting here while we should be returning here here while we should be returning here here while we should be returning here and then again and again so it never and then again and again so it never and then again and again so it never predicts correctly so the question is predicts correctly so the question is predicts correctly so the question is how can we predict returns accurately

## RAS： Part 1
Kind: captions Language: en and the answer is we will use a return and the answer is we will use a return and the answer is we will use a return address stack which is a separate address stack which is a separate address stack which is a separate predictor dedicated to predicting predictor dedicated to predicting predictor dedicated to predicting function returns the way it works function returns the way it works function returns the way it works is that we have a small stack in is that we have a small stack in is that we have a small stack in hardware hardware hardware with a pointer with a pointer with a pointer as we execute the program when we have a as we execute the program when we have a as we execute the program when we have a function call function call function call we will push the return address in this we will push the return address in this we will push the return address in this case one two three case one two three case one two three zero is the address of the call so one zero is the address of the call so one zero is the address of the call so one two three four would be the return two three four would be the return two three four would be the return address to the return address stack f address to the return address stack f address to the return address stack f move the pointer move the pointer move the pointer in the function when we encounter the in the function when we encounter the in the function when we encounter the return instruction return instruction return instruction we will pop we will pop we will pop from this return address stack and use from this return address stack and use from this return address stack and use the one two three four to return and in the one two three four to return and in the one two three four to return and in that case get the correct target address that case get the correct target address that case get the correct target address now our pointer is here we don't really now our pointer is here we don't really now our pointer is here we don't really have to delete this entry because the have to delete this entry because the have to delete this entry because the pointer being here means that it's free pointer being here means that it's free pointer being here means that it's free next when we reach the next place where next when we reach the next place where next when we reach the next place where the function is called from we will save the function is called from we will save the function is called from we will save the return address in this case one two the return address in this case one two the return address in this case one two five 4 onto the return address stack the five 4 onto the return address stack the five 4 onto the return address stack the function executes when we see the return function executes when we see the return function executes when we see the return instruction instruction instruction we pop the return address and we again we pop the return address and we again we pop the return address and we again predict correctly so why is this a predict correctly so why is this a predict correctly so why is this a predictor why not just use the actual predictor why not just use the actual predictor why not just use the actual stack of the program stack of the program stack of the program well because this predictor needs to be well because this predictor needs to be well because this predictor needs to be on chip very close to where the rest of on chip very close to where the rest of on chip very close to where the rest of the branch prediction is happening and the branch prediction is happening and the branch prediction is happening and needs to be very very small so unlike needs to be very very small so unlike needs to be very very small so unlike your traditional stack your traditional stack your traditional stack where you will push something onto the where you will push something onto the where you will push something onto the stack call another function from there stack call another function from there stack call another function from there call a nested function and you can do call a nested function and you can do call a nested function and you can do that many many times until you run out that many many times until you run out that many many times until you run out of memory in this particular case we can of memory in this particular case we can of memory in this particular case we can have a very small hardware structure so have a very small hardware structure so have a very small hardware structure so that it can make a prediction very that it can make a prediction very that it can make a prediction very quickly like in one cycle thus it can quickly like in one cycle thus it can quickly like in one cycle thus it can only have a limited number of entries so only have a limited number of entries so only have a limited number of entries so what happens when we exceed the size of what happens when we exceed the size of what happens when we exceed the size of the ras the ras the ras let's say we have only four entries let's say we have only four entries let's say we have only four entries let's say that we call a function we let's say that we call a function we let's say that we call a function we call another one we call another one we call another one we call another one we call another one we call another one we call another one and now call another one and now call another one and now the question is what do we do the question is what do we do the question is what do we do so if our ass is full what do we do so if our ass is full what do we do so if our ass is full what do we do there are really two choices one there are really two choices one there are really two choices one don't push anything pretty much preserve don't push anything pretty much preserve don't push anything pretty much preserve what we already have on our wrist so what we already have on our wrist so what we already have on our wrist so that we don't overwrite that we don't overwrite that we don't overwrite anything anything anything the other choice the other choice the other choice is once we have filled these four is once we have filled these four is once we have filled these four entries just wrap around and keep entries just wrap around and keep entries just wrap around and keep filling them if we call more functions

## RAS Full Quiz： Part 1
Kind: captions Language: en so let's see in a quiz which one is so let's see in a quiz which one is so let's see in a quiz which one is better when the race is full the don't better when the race is full the don't better when the race is full the don't push approach or the wraparound approach

## RAS Full Solution Quiz： Part 1
Kind: captions Language: en let's look at the solution to a raspful let's look at the solution to a raspful let's look at the solution to a raspful quiz so the question was is it better to quiz so the question was is it better to quiz so the question was is it better to not push anything anymore once the rice not push anything anymore once the rice not push anything anymore once the rice is full or simply wrap around and keep is full or simply wrap around and keep is full or simply wrap around and keep overwriting all the dresses with new overwriting all the dresses with new overwriting all the dresses with new ones it turns out that the wraparound ones it turns out that the wraparound ones it turns out that the wraparound approach is much better than the don't approach is much better than the don't approach is much better than the don't push approach to see why keep in mind push approach to see why keep in mind push approach to see why keep in mind that our program will look like this that our program will look like this that our program will look like this we have the main function we have the main function we have the main function it executes for a while then calls some it executes for a while then calls some it executes for a while then calls some other function other function other function this results in pushing something onto this results in pushing something onto this results in pushing something onto the rest our high level function here the rest our high level function here the rest our high level function here will do a lot of work occasionally will do a lot of work occasionally will do a lot of work occasionally calling calling calling other functions other functions other functions and so on we push to see and so on we push to see and so on we push to see what happens here without running out of what happens here without running out of what happens here without running out of space let's say that our ass only has space let's say that our ass only has space let's say that our ass only has two entries the first entry will be two entries the first entry will be two entries the first entry will be consumed by pushing consumed by pushing consumed by pushing the return address when main calls do it the return address when main calls do it the return address when main calls do it the second one will be consumed when we the second one will be consumed when we the second one will be consumed when we call the function func from do it call the function func from do it call the function func from do it at this point at this point at this point we're calling the function do less we're calling the function do less we're calling the function do less and it might call for example the add and it might call for example the add and it might call for example the add function many many times so we're function many many times so we're function many many times so we're getting to the place where functions are getting to the place where functions are getting to the place where functions are really small and ideally we would you really small and ideally we would you really small and ideally we would you know push each time and then pop when we know push each time and then pop when we know push each time and then pop when we return and so on if we only have one return and so on if we only have one return and so on if we only have one entry then really our entire res will be entry then really our entire res will be entry then really our entire res will be used just when we call do it and used just when we call do it and used just when we call do it and as long as we stay and do it which might as long as we stay and do it which might as long as we stay and do it which might be a very large function because it be a very large function because it be a very large function because it might be doing most of the actual work might be doing most of the actual work might be doing most of the actual work in the program in the program in the program all of the function calls that we are all of the function calls that we are all of the function calls that we are doing really end up being mispredicted doing really end up being mispredicted doing really end up being mispredicted because we ran out of raw space and the because we ran out of raw space and the because we ran out of raw space and the one time that we will have a correct one time that we will have a correct one time that we will have a correct prediction is when we finally return prediction is when we finally return prediction is when we finally return from do it if we have two entries a from do it if we have two entries a from do it if we have two entries a similar thing will happen we will similar thing will happen we will similar thing will happen we will consume the first entry by basically consume the first entry by basically consume the first entry by basically pushing the first thing so pretty much pushing the first thing so pretty much pushing the first thing so pretty much we're kind of using an entry here just we're kind of using an entry here just we're kind of using an entry here just to save one misprediction at the final to save one misprediction at the final to save one misprediction at the final return return return here again we are using the second entry here again we are using the second entry here again we are using the second entry just to save the return from func just to save the return from func just to save the return from func whenever that happens and func itself whenever that happens and func itself whenever that happens and func itself could be calling a lot of functions so could be calling a lot of functions so could be calling a lot of functions so the don't push approach basically the don't push approach basically the don't push approach basically spending entries spending entries spending entries on kind of long-term things that need to on kind of long-term things that need to on kind of long-term things that need to be remembered be remembered be remembered so that we can finally save a so that we can finally save a so that we can finally save a misprediction once we reach the return misprediction once we reach the return misprediction once we reach the return point from the large function in point from the large function in point from the large function in contrast the wrap around approach contrast the wrap around approach contrast the wrap around approach is gonna is gonna is gonna push this push this push this push this push this push this push this thus overwriting this entry so push this thus overwriting this entry so push this thus overwriting this entry so now what we are doing is we are now what we are doing is we are now what we are doing is we are basically gonna be correctly predicting basically gonna be correctly predicting basically gonna be correctly predicting calls to small functions and returns and calls to small functions and returns and calls to small functions and returns and we can have many of those we can have many of those we can have many of those in return for inaccurately predicting in return for inaccurately predicting in return for inaccurately predicting the final returns from large functions the final returns from large functions the final returns from large functions because we have many more small function because we have many more small function because we have many more small function calls than large function calls calls than large function calls calls than large function calls in the end we are more effectively using in the end we are more effectively using in the end we are more effectively using the few entries we have pretty much here the few entries we have pretty much here the few entries we have pretty much here we are kind of using the entry to save we are kind of using the entry to save we are kind of using the entry to save a misprediction over a very short period a misprediction over a very short period a misprediction over a very short period of time thus this entry gets utilized of time thus this entry gets utilized of time thus this entry gets utilized and then released very quickly whereas and then released very quickly whereas and then released very quickly whereas here we are using an entry for a long here we are using an entry for a long here we are using an entry for a long time to save a single misprediction and time to save a single misprediction and time to save a single misprediction and in the end we want to save as many in the end we want to save as many in the end we want to save as many mispredictions as we can so that's why mispredictions as we can so that's why mispredictions as we can so that's why the wraparound approach works better the wraparound approach works better the wraparound approach works better because it ends up correctly predicting because it ends up correctly predicting because it ends up correctly predicting calls to small functions calls to small functions calls to small functions many of which we can have in return for many of which we can have in return for many of which we can have in return for not correctly predicting the return not correctly predicting the return not correctly predicting the return from large functions to main etc from large functions to main etc from large functions to main etc another thing to keep in mind with the another thing to keep in mind with the another thing to keep in mind with the res is that it is a predictor so res is that it is a predictor so res is that it is a predictor so either way we will be having some either way we will be having some either way we will be having some mispredictions we are allowed to have mispredictions we are allowed to have mispredictions we are allowed to have mispredictions all that will happen is mispredictions all that will happen is mispredictions all that will happen is that there will be a branch that there will be a branch that there will be a branch misprediction basically we will fetch misprediction basically we will fetch misprediction basically we will fetch the wrong instructions and have to the wrong instructions and have to the wrong instructions and have to recover from that recover from that recover from that so in the end the discussion about what so in the end the discussion about what so in the end the discussion about what to do with the ras has to do with to do with the ras has to do with to do with the ras has to do with achieving a larger number of correct achieving a larger number of correct achieving a larger number of correct predictions predictions predictions but neither of these two ways can but neither of these two ways can but neither of these two ways can actually do things perfectly so in the actually do things perfectly so in the actually do things perfectly so in the end we need to just choose which one end we need to just choose which one end we need to just choose which one gives us more correct predictions and it gives us more correct predictions and it gives us more correct predictions and it turns out that the wraparound approach turns out that the wraparound approach turns out that the wraparound approach predicts better than the don't push predicts better than the don't push predicts better than the don't push approach while both of them result in approach while both of them result in approach while both of them result in mispredictions anyway

## But How Do We Know It's A RET： Part 1
Kind: captions Language: en the last thing is but but the last thing is but but the last thing is but but how do we know it's a red instruction how do we know it's a red instruction how do we know it's a red instruction remember we need to make our prediction remember we need to make our prediction remember we need to make our prediction while we're fetching the instruction so while we're fetching the instruction so while we're fetching the instruction so if this is a return instruction we need if this is a return instruction we need if this is a return instruction we need to use the return address stack before to use the return address stack before to use the return address stack before we even determine that it is a return we even determine that it is a return we even determine that it is a return instruction we cannot just keep pushing instruction we cannot just keep pushing instruction we cannot just keep pushing and popping things from the return and popping things from the return and popping things from the return address stack address stack address stack before we know it's a return because for before we know it's a return because for before we know it's a return because for example if there is an ad and we pop example if there is an ad and we pop example if there is an ad and we pop from the return address stack then it's from the return address stack then it's from the return address stack then it's not going to work correctly we actually not going to work correctly we actually not going to work correctly we actually need to figure out or at least very need to figure out or at least very need to figure out or at least very accurately predict what's a return accurately predict what's a return accurately predict what's a return instruction so that we can use the ras instruction so that we can use the ras instruction so that we can use the ras appropriately so the problem is that we appropriately so the problem is that we appropriately so the problem is that we are using the rat while fetching the are using the rat while fetching the are using the rat while fetching the instruction it's not been decoded yet so instruction it's not been decoded yet so instruction it's not been decoded yet so we don't know it's a return instruction we don't know it's a return instruction we don't know it's a return instruction yet so how do we do it one way is to yet so how do we do it one way is to yet so how do we do it one way is to just use a very simple predictor that just use a very simple predictor that just use a very simple predictor that will be trained on whether the will be trained on whether the will be trained on whether the instruction we're fetching is a red or instruction we're fetching is a red or instruction we're fetching is a red or not simply train the predictor one way not simply train the predictor one way not simply train the predictor one way or the other depending on whether it's a or the other depending on whether it's a or the other depending on whether it's a red or not and then make that predictor red or not and then make that predictor red or not and then make that predictor tell us whether to use the return tell us whether to use the return tell us whether to use the return address stack or not address stack or not address stack or not this would be a very accurate predictor this would be a very accurate predictor this would be a very accurate predictor if at a particular pc we have seen a red if at a particular pc we have seen a red if at a particular pc we have seen a red instruction instruction instruction previously previously previously then it is very likely that if we see then it is very likely that if we see then it is very likely that if we see the same pc the same pc the same pc we still have a return instruction we still have a return instruction we still have a return instruction so we can use a single bit predictor so we can use a single bit predictor so we can use a single bit predictor very easily here very easily here very easily here another approach another approach another approach is to use a so-called is to use a so-called is to use a so-called pre-decoding we will soon see pre-decoding we will soon see pre-decoding we will soon see that the processor really contains a that the processor really contains a that the processor really contains a cache cache cache that stores instructions that have been that stores instructions that have been that stores instructions that have been fetched from memory and it's fetching fetched from memory and it's fetching fetched from memory and it's fetching instructions from the cache and only if instructions from the cache and only if instructions from the cache and only if the cache doesn't already contain the the cache doesn't already contain the the cache doesn't already contain the instruction it goes through memory so instruction it goes through memory so instruction it goes through memory so the pre-decoding the pre-decoding the pre-decoding works by when i'm fetching from memory works by when i'm fetching from memory works by when i'm fetching from memory i decode enough of the instruction i decode enough of the instruction i decode enough of the instruction to know that it's a return or not and i to know that it's a return or not and i to know that it's a return or not and i store that information along with the store that information along with the store that information along with the actual instruction in the cache so actual instruction in the cache so actual instruction in the cache so pretty much my memory will for example pretty much my memory will for example pretty much my memory will for example have 32-bit instructions have 32-bit instructions have 32-bit instructions in my cache in my cache in my cache i will store 33 bits per instruction 32 i will store 33 bits per instruction 32 i will store 33 bits per instruction 32 bits of the actual instruction and one bits of the actual instruction and one bits of the actual instruction and one bit that tells me whether the bit that tells me whether the bit that tells me whether the instruction is a return or not instruction is a return or not instruction is a return or not so the idea is that as the instruction so the idea is that as the instruction so the idea is that as the instruction is coming from memory and we put it to is coming from memory and we put it to is coming from memory and we put it to cache cache cache we can be pre-decoding instructions we can be pre-decoding instructions we can be pre-decoding instructions keeping some additional bits in the keeping some additional bits in the keeping some additional bits in the cache about the pre-decoding of the cache about the pre-decoding of the cache about the pre-decoding of the instructions so that when we fetch we instructions so that when we fetch we instructions so that when we fetch we already know that information already know that information already know that information the alternative to this would have been the alternative to this would have been the alternative to this would have been to just fetch the instructions from to just fetch the instructions from to just fetch the instructions from memory to cache as they are and then memory to cache as they are and then memory to cache as they are and then every time we fetch we have to figure every time we fetch we have to figure every time we fetch we have to figure out what this instruction is out what this instruction is out what this instruction is because it's also more power efficient because it's also more power efficient because it's also more power efficient to do this once and then fetch the to do this once and then fetch the to do this once and then fetch the instruction many times rather than redo instruction many times rather than redo instruction many times rather than redo it every single time pre-decoding is a it every single time pre-decoding is a it every single time pre-decoding is a very popular approach very popular approach very popular approach so predecoding is used for things like so predecoding is used for things like so predecoding is used for things like is it a return instruction is it a return instruction is it a return instruction is it a branch instruction at all if we is it a branch instruction at all if we is it a branch instruction at all if we know it's not a branch then we can know it's not a branch then we can know it's not a branch then we can completely omit the use of any branch completely omit the use of any branch completely omit the use of any branch vectors thus saving a lot of power vectors thus saving a lot of power vectors thus saving a lot of power if our instructions are of variable if our instructions are of variable if our instructions are of variable sizes predecoding can also tell us for sizes predecoding can also tell us for sizes predecoding can also tell us for example how many bytes this instruction example how many bytes this instruction example how many bytes this instruction is long so that we can fetch the next is long so that we can fetch the next is long so that we can fetch the next instruction quickly and not rely on just instruction quickly and not rely on just instruction quickly and not rely on just decoding the instruction before we can decoding the instruction before we can decoding the instruction before we can fetch the next one and so on so there is fetch the next one and so on so there is fetch the next one and so on so there is a lot of things that modern processors a lot of things that modern processors a lot of things that modern processors do in this pre-decoding phase in order do in this pre-decoding phase in order do in this pre-decoding phase in order to avoid doing it when we are on the to avoid doing it when we are on the to avoid doing it when we are on the clock after we have fetched the clock after we have fetched the clock after we have fetched the instruction

## Branches Outro： Part 1
Kind: captions Language: en we now know how de procesor spre datang we now know how de procesor spre datang we now know how de procesor spre datang which instruction tu fericit next you which instruction tu fericit next you which instruction tu fericit next you put this knowledge to work in a Project put this knowledge to work in a Project put this knowledge to work in a Project and this was but some drawings I've made and this was but some drawings I've made and this was but some drawings I've made difficult to predict sonax la sân is difficult to predict sonax la sân is difficult to predict sonax la sân is about helping compar completely about helping compar completely about helping compar completely eliminate this Branch from de program eliminate this Branch from de program eliminate this Branch from de program sau I do not have to protect them

## Predication Intro： Part 1
Kind: captions Language: en The address is sleep and continue another The address is sleep and continue another The address is sleep and continue another state found the hard we all have state found the hard we all have state found the hard we all have nobody Breaks but we also know that some nobody Breaks but we also know that some nobody Breaks but we also know that some Brothers really difficult to predict Brothers really difficult to predict Brothers really difficult to predict even when I have a sophisticated and even when I have a sophisticated and even when I have a sophisticated and Victor suing distillation or you spin with Victor suing distillation or you spin with Victor suing distillation or you spin with pala who erred after complete Evo 8 pala who erred after complete Evo 8 pala who erred after complete Evo 8 Samuel the heart to lose free

## Predication： Part 1
Kind: captions Language: en Predication is another way that we can Predication is another way that we can Predication is another way that we can try to deal with control hazards. Unlike try to deal with control hazards. Unlike try to deal with control hazards. Unlike branch prediction when we are really branch prediction when we are really branch prediction when we are really trying to guess which way the if then trying to guess which way the if then trying to guess which way the if then else or a loop or something branch will else or a loop or something branch will else or a loop or something branch will be going. Predication is another way for be going. Predication is another way for be going. Predication is another way for dealing with control dependencies. dealing with control dependencies. dealing with control dependencies. Branch fiction is about guessing where Branch fiction is about guessing where Branch fiction is about guessing where the program is going. Usually there is the program is going. Usually there is the program is going. Usually there is no penalty for being correct in branch no penalty for being correct in branch no penalty for being correct in branch fiction. Pretty much we just keep fiction. Pretty much we just keep fiction. Pretty much we just keep fetching as if the branch wasn't even fetching as if the branch wasn't even fetching as if the branch wasn't even there. Even if the branch is actually there. Even if the branch is actually there. Even if the branch is actually taken, we can just keep fetching from taken, we can just keep fetching from taken, we can just keep fetching from the right place and thus avoid any the right place and thus avoid any the right place and thus avoid any penalty. But the problem with branch penalty. But the problem with branch penalty. But the problem with branch fiction is that in modern processors, fiction is that in modern processors, fiction is that in modern processors, there is a huge penalty if we are wrong there is a huge penalty if we are wrong there is a huge penalty if we are wrong in predicting. Keep in mind that modern in predicting. Keep in mind that modern in predicting. Keep in mind that modern processors have a deep pipe with lots of processors have a deep pipe with lots of processors have a deep pipe with lots of stages before the one in which we figure stages before the one in which we figure stages before the one in which we figure out the misprediction. And in each out the misprediction. And in each out the misprediction. And in each stage, we have a number of instructions stage, we have a number of instructions stage, we have a number of instructions that we have been doing. that we have been doing. that we have been doing. So a misprediction results in throwing So a misprediction results in throwing So a misprediction results in throwing away all of that work. Usually at least away all of that work. Usually at least away all of that work. Usually at least several tens of instructions worth of several tens of instructions worth of several tens of instructions worth of work. So the penalty is usually zero work. So the penalty is usually zero work. So the penalty is usually zero instructions here. And if we have instructions here. And if we have instructions here. And if we have something like maybe a 12 stage pipeline something like maybe a 12 stage pipeline something like maybe a 12 stage pipeline with four instructions per with four instructions per with four instructions per cycle, we're talking about something cycle, we're talking about something cycle, we're talking about something like 48 possibly instructions or so. So like 48 possibly instructions or so. So like 48 possibly instructions or so. So let's say about 50 instructions worth of let's say about 50 instructions worth of let's say about 50 instructions worth of work wasted. Predication is about doing work wasted. Predication is about doing work wasted. Predication is about doing work of the both of the directions. So work of the both of the directions. So work of the both of the directions. So we basically do the work of both a taken we basically do the work of both a taken we basically do the work of both a taken and not taken direction for a branch. and not taken direction for a branch. and not taken direction for a branch. That way the waste is up to 50% of the That way the waste is up to 50% of the That way the waste is up to 50% of the work we've been doing because at the work we've been doing because at the work we've been doing because at the position where the branch was supposed position where the branch was supposed position where the branch was supposed to be about half of the stuff we've been to be about half of the stuff we've been to be about half of the stuff we've been fetching could have been from one path fetching could have been from one path fetching could have been from one path half of the stuff was from the other and half of the stuff was from the other and half of the stuff was from the other and only half of that work will actually be only half of that work will actually be only half of that work will actually be used the rest will be thrown away and used the rest will be thrown away and used the rest will be thrown away and then we simply throw away the work from then we simply throw away the work from then we simply throw away the work from the wrong path. So what is good about the wrong path. So what is good about the wrong path. So what is good about this? It seems like here if we are this? It seems like here if we are this? It seems like here if we are correct we lose nothing. If we are wrong correct we lose nothing. If we are wrong correct we lose nothing. If we are wrong we lose a lot but we can have very we lose a lot but we can have very we lose a lot but we can have very accurate accurate accurate prediction. Well here either way we lose prediction. Well here either way we lose prediction. Well here either way we lose quite a bit. Well let's see how the quite a bit. Well let's see how the quite a bit. Well let's see how the branch fiction and something like branch fiction and something like branch fiction and something like predication compare on different types predication compare on different types predication compare on different types of conditional branches that we might of conditional branches that we might of conditional branches that we might want to predict. For a loop usually we want to predict. For a loop usually we want to predict. For a loop usually we want to do branch fiction. loop want to do branch fiction. loop want to do branch fiction. loop branches the more iterations we have the branches the more iterations we have the branches the more iterations we have the more they are more they are more they are predictable. Whereas with predication in predictable. Whereas with predication in predictable. Whereas with predication in each iteration of the loop we will be each iteration of the loop we will be each iteration of the loop we will be dividing our work into two. One is do dividing our work into two. One is do dividing our work into two. One is do the next iteration the other one is do the next iteration the other one is do the next iteration the other one is do the work after the loop. So if we have a the work after the loop. So if we have a the work after the loop. So if we have a thousand iterations very little of the thousand iterations very little of the thousand iterations very little of the work will end up being done correctly. work will end up being done correctly. work will end up being done correctly. Simply the branch is diverging too much. Simply the branch is diverging too much. Simply the branch is diverging too much. When we need to decide whether to stay When we need to decide whether to stay When we need to decide whether to stay in the loop or not, we would be starting in the loop or not, we would be starting in the loop or not, we would be starting the work of both of these. One path goes the work of both of these. One path goes the work of both of these. One path goes after the loop, the other one goes back after the loop, the other one goes back after the loop, the other one goes back into the loop. And then here we have a into the loop. And then here we have a into the loop. And then here we have a decision again to follow that path or decision again to follow that path or decision again to follow that path or stay in the loop, that path, stay in the stay in the loop, that path, stay in the stay in the loop, that path, stay in the loop. So after a while if we're still loop. So after a while if we're still loop. So after a while if we're still doing the work of all of these parts doing the work of all of these parts doing the work of all of these parts concurrently a very small fraction of concurrently a very small fraction of concurrently a very small fraction of the work we are doing ends up being the the work we are doing ends up being the the work we are doing ends up being the staying in the loop work and a lot of it staying in the loop work and a lot of it staying in the loop work and a lot of it has to do with various varants of has to do with various varants of has to do with various varants of exiting the loop in each iteration. So exiting the loop in each iteration. So exiting the loop in each iteration. So for a loop we want to predict function for a loop we want to predict function for a loop we want to predict function call returns same thing except call returns same thing except call returns same thing except predication really makes no sense predication really makes no sense predication really makes no sense because calls and returns always go because calls and returns always go because calls and returns always go there. So somehow the direction of not there. So somehow the direction of not there. So somehow the direction of not going there makes no sense to do. So we going there makes no sense to do. So we going there makes no sense to do. So we want to want to want to predict. Let's say now we have a large predict. Let's say now we have a large predict. Let's say now we have a large if then else. So what happens? We have a if then else. So what happens? We have a if then else. So what happens? We have a decision and based on it we do decision and based on it we do decision and based on it we do this or we do this or we do this or we do this. Now the question is should we try this. Now the question is should we try this. Now the question is should we try to predict this decision or predicate so to predict this decision or predicate so to predict this decision or predicate so that we are doing the work of both and that we are doing the work of both and that we are doing the work of both and eventually they will merge and we eventually they will merge and we eventually they will merge and we continue. And the answer here is that if continue. And the answer here is that if continue. And the answer here is that if we predict we waste up to 50 we predict we waste up to 50 we predict we waste up to 50 instructions more likely we waste instructions more likely we waste instructions more likely we waste nothing. If we predicate and these nothing. If we predicate and these nothing. If we predicate and these branches have equal weight then let's branches have equal weight then let's branches have equal weight then let's say we have a 100 instructions here and say we have a 100 instructions here and say we have a 100 instructions here and 100 instructions here. We will execute 100 instructions here. We will execute 100 instructions here. We will execute 200 instructions waste 100 of them 200 instructions waste 100 of them 200 instructions waste 100 of them either way. So somehow the amount of either way. So somehow the amount of either way. So somehow the amount of waste with predication waste with predication waste with predication here is larger than even the penalty of here is larger than even the penalty of here is larger than even the penalty of branch misprediction. Pretty much if we branch misprediction. Pretty much if we branch misprediction. Pretty much if we predict and we are right we lose predict and we are right we lose predict and we are right we lose nothing. If we predict and we are wrong nothing. If we predict and we are wrong nothing. If we predict and we are wrong we lose 50 instructions. Here we lose we lose 50 instructions. Here we lose we lose 50 instructions. Here we lose 100 instructions and we cannot be wrong 100 instructions and we cannot be wrong 100 instructions and we cannot be wrong or right but either way we are losing or right but either way we are losing or right but either way we are losing 100 instructions. So for a large if then 100 instructions. So for a large if then 100 instructions. So for a large if then else it seems like we should be else it seems like we should be else it seems like we should be predicting but let's see how things look predicting but let's see how things look predicting but let's see how things look like for a small if then else. Let's say like for a small if then else. Let's say like for a small if then else. Let's say that we have a decision to make and that we have a decision to make and that we have a decision to make and depending on the decision we have the depending on the decision we have the depending on the decision we have the then or the else path but let's say each then or the else path but let's say each then or the else path but let's say each is now something like five instructions is now something like five instructions is now something like five instructions long. Now it becomes interesting if we long. Now it becomes interesting if we long. Now it becomes interesting if we do predication we will be executing 10 do predication we will be executing 10 do predication we will be executing 10 instructions here followed by instructions here followed by instructions here followed by instructions that we would do anyway. So instructions that we would do anyway. So instructions that we would do anyway. So the waste is five instructions either the waste is five instructions either the waste is five instructions either way. Here we are predicting and if we way. Here we are predicting and if we way. Here we are predicting and if we are right we waste nothing but if we are are right we waste nothing but if we are are right we waste nothing but if we are wrong we waste 50 instructions. So our wrong we waste 50 instructions. So our wrong we waste 50 instructions. So our comparison is that 100% of the time we comparison is that 100% of the time we comparison is that 100% of the time we waste five instructions with waste five instructions with waste five instructions with predication. So we end up with five on predication. So we end up with five on predication. So we end up with five on average per instance of this. Whereas average per instance of this. Whereas average per instance of this. Whereas here our branch prediction rate here our branch prediction rate here our branch prediction rate determines how often we waste this 50. determines how often we waste this 50. determines how often we waste this 50. So it's going to So it's going to So it's going to be let's say be let's say be let's say 0 10 because it's 10% probability of 0 10 because it's 10% probability of 0 10 because it's 10% probability of mispredicting in which case we end up at mispredicting in which case we end up at mispredicting in which case we end up at 52. So if our accuracy of prediction is 52. So if our accuracy of prediction is 52. So if our accuracy of prediction is 90% or better then in this case we are 90% or better then in this case we are 90% or better then in this case we are better off predicting the branch. But if better off predicting the branch. But if better off predicting the branch. But if our accuracy is less than 90%, we are our accuracy is less than 90%, we are our accuracy is less than 90%, we are better off better off better off predicating. If the if then else is even predicating. If the if then else is even predicating. If the if then else is even smaller, then it biases things more in smaller, then it biases things more in smaller, then it biases things more in favor of predication and less in favor favor of predication and less in favor favor of predication and less in favor of branch prediction. So for really of branch prediction. So for really of branch prediction. So for really small if than else's, we want to small if than else's, we want to small if than else's, we want to predicate unless we are reasonably sure predicate unless we are reasonably sure predicate unless we are reasonably sure that our branch prediction is going to that our branch prediction is going to that our branch prediction is going to be highly accurate.

## If Conversion： Part 1
Kind: captions Language: en so before we go into how predication so before we go into how predication so before we go into how predication works in hardware let's first look at works in hardware let's first look at works in hardware let's first look at the technique called if conversion which the technique called if conversion which the technique called if conversion which is how the compiler creates the code is how the compiler creates the code is how the compiler creates the code that will be executed along both paths that will be executed along both paths that will be executed along both paths so let's say we have a relatively small so let's say we have a relatively small so let's say we have a relatively small if then else here where depending on the if then else here where depending on the if then else here where depending on the condition condition condition x gets either the value from array of i x gets either the value from array of i x gets either the value from array of i or array from j or array from j or array from j and y gets either incremented or and y gets either incremented or and y gets either incremented or decremented decremented decremented if conversion transforms this code into if conversion transforms this code into if conversion transforms this code into the work of both parts the work of both parts the work of both parts followed by a selection of some sort followed by a selection of some sort followed by a selection of some sort between the results of the two parts between the results of the two parts between the results of the two parts but there is still a question of how do but there is still a question of how do but there is still a question of how do we do this we do this we do this if we convert this into some sort of a if we convert this into some sort of a if we convert this into some sort of a conditional expression that still conditional expression that still conditional expression that still branches based on the condition and then branches based on the condition and then branches based on the condition and then does the move of x2 into x otherwise does the move of x2 into x otherwise does the move of x2 into x otherwise does the move of x into x1 then we does the move of x into x1 then we does the move of x into x1 then we really haven't done much because we really haven't done much because we really haven't done much because we converted one branch converted one branch converted one branch into another branch and now we have this into another branch and now we have this into another branch and now we have this branch twice so unless we have some sort branch twice so unless we have some sort branch twice so unless we have some sort of a correlating predictor like for of a correlating predictor like for of a correlating predictor like for example example example the global history predictor we will the global history predictor we will the global history predictor we will possibly have two mispredictions here possibly have two mispredictions here possibly have two mispredictions here instead of the one we might have had instead of the one we might have had instead of the one we might have had so if this is all you can do then you so if this is all you can do then you so if this is all you can do then you don't do if conversion what we need for don't do if conversion what we need for don't do if conversion what we need for if conversion to work if conversion to work if conversion to work is some sort of a move instruction that is some sort of a move instruction that is some sort of a move instruction that will do something like will do something like will do something like move x1 into x if move x1 into x if move x1 into x if a flag is true

## Conditional Move： Part 1
Kind: captions Language: en so the simplest form of predication so the simplest form of predication so the simplest form of predication supporting hardware will be a supporting hardware will be a supporting hardware will be a conditional move instruction conditional move instruction conditional move instruction in the mips instruction set for example in the mips instruction set for example in the mips instruction set for example there is a move z there is a move z there is a move z instruction that takes two sources and instruction that takes two sources and instruction that takes two sources and the destination register and the way the destination register and the way the destination register and the way this works this works this works is that this instruction compares rt to is that this instruction compares rt to is that this instruction compares rt to 0 and if it is 0 0 and if it is 0 0 and if it is 0 then then then changes rd to become equal to rs changes rd to become equal to rs changes rd to become equal to rs otherwise leaves rd alone note that otherwise leaves rd alone note that otherwise leaves rd alone note that there is no branch here anymore because there is no branch here anymore because there is no branch here anymore because this is a single instruction maps also this is a single instruction maps also this is a single instruction maps also has a has a has a move n move n move n instruction that works exactly the same instruction that works exactly the same instruction that works exactly the same way except way except way except it moves rs into rd only if rt is not it moves rs into rd only if rt is not it moves rs into rd only if rt is not zero zero zero so we would implement our so we would implement our so we would implement our x equals depending on condition x1 or x2 x equals depending on condition x1 or x2 x equals depending on condition x1 or x2 by doing something like using by doing something like using by doing something like using comparisons or whatever to put the comparisons or whatever to put the comparisons or whatever to put the result of the condition into let's say result of the condition into let's say result of the condition into let's say r3 then r1 would be our whatever x1 r3 then r1 would be our whatever x1 r3 then r1 would be our whatever x1 expression we wanted to implement expression we wanted to implement expression we wanted to implement r2 would be our x2 r2 would be our x2 r2 would be our x2 and then we would do move n and then we would do move n and then we would do move n x x x r one r three r one r three r one r three move z move z move z x x x r r r two r three two r three two r three and this puts one of these two into x and this puts one of these two into x and this puts one of these two into x depending on whether r3 was true or depending on whether r3 was true or depending on whether r3 was true or false which means depending on the false which means depending on the false which means depending on the condition the x86 instruction set has a condition the x86 instruction set has a condition the x86 instruction set has a whole set of so-called c move whole set of so-called c move whole set of so-called c move instructions so for example it has c instructions so for example it has c instructions so for example it has c move z c move nz move z c move nz move z c move nz c move greater than etc c move greater than etc c move greater than etc where the condition here is determined where the condition here is determined where the condition here is determined by the flags and all of these by the flags and all of these by the flags and all of these instructions effectively implement the instructions effectively implement the instructions effectively implement the if the condition codes if the condition codes if the condition codes correspond to one of these conditions correspond to one of these conditions correspond to one of these conditions depending on the instruction then the depending on the instruction then the depending on the instruction then the destination register gets the value of destination register gets the value of destination register gets the value of the source register otherwise the the source register otherwise the the source register otherwise the destination register is not modified so destination register is not modified so destination register is not modified so you can see how we can implement similar you can see how we can implement similar you can see how we can implement similar behavior here except maybe we don't even behavior here except maybe we don't even behavior here except maybe we don't even need to put the condition in a register need to put the condition in a register need to put the condition in a register because probably we can just test the because probably we can just test the because probably we can just test the condition and then do like greater than condition and then do like greater than condition and then do like greater than less than and so on on it

## MovZ MovN Quiz： Part 1
Kind: captions Language: en so now that we have seen how the so now that we have seen how the so now that we have seen how the conditional move instructions work let's conditional move instructions work let's conditional move instructions work let's do a quiz about how to use them let's do a quiz about how to use them let's do a quiz about how to use them let's say that the code we have is this if r1 say that the code we have is this if r1 say that the code we have is this if r1 is 0 we branch here and increment r3 is 0 we branch here and increment r3 is 0 we branch here and increment r3 otherwise we stay here and increment our otherwise we stay here and increment our otherwise we stay here and increment our 2 and then either way we go to the end 2 and then either way we go to the end 2 and then either way we go to the end after if conversion we will have after if conversion we will have after if conversion we will have something like add 1 to R 2 and put the something like add 1 to R 2 and put the something like add 1 to R 2 and put the result in our for add 1 to R 3 and put result in our for add 1 to R 3 and put result in our for add 1 to R 3 and put the result in our 5 followed by what the result in our 5 followed by what the result in our 5 followed by what instruction in order to properly make instruction in order to properly make instruction in order to properly make this equivalent to this code



---

# Playlist 0 Module 11 Raw Transcript

## MovZ MovN Quiz Solution： Part 1
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our move z move n quiz we have this code move z move n quiz we have this code move z move n quiz we have this code and we want to do if conversion of it so and we want to do if conversion of it so and we want to do if conversion of it so that we get a branch-free code that does that we get a branch-free code that does that we get a branch-free code that does the same work the same work the same work if conversion amounts to doing the work if conversion amounts to doing the work if conversion amounts to doing the work of both parts both the not taken and the of both parts both the not taken and the of both parts both the not taken and the taken path taken path taken path and then selecting between the two and and then selecting between the two and and then selecting between the two and correcting the results so that only one correcting the results so that only one correcting the results so that only one of them of them of them is active is active is active so what we have done is these two so what we have done is these two so what we have done is these two instructions are already doing the work instructions are already doing the work instructions are already doing the work of the of the of the not taken path not taken path not taken path it's just that instead of in r2 which it's just that instead of in r2 which it's just that instead of in r2 which would permanently modify r2 we're would permanently modify r2 we're would permanently modify r2 we're putting the result in r4 putting the result in r4 putting the result in r4 and here and here and here the taken the taken the taken path r3 and 1 are added then we put path r3 and 1 are added then we put path r3 and 1 are added then we put result in r5 instead of r3 so what now result in r5 instead of r3 so what now result in r5 instead of r3 so what now needs to happen needs to happen needs to happen is is is if this was the correct path to take if this was the correct path to take if this was the correct path to take then r2 should get the result of r4 then r2 should get the result of r4 then r2 should get the result of r4 otherwise r3 should get the result from otherwise r3 should get the result from otherwise r3 should get the result from r5 r5 r5 this is a branch on equal this is a branch on equal this is a branch on equal 0 r1 0 r1 0 r1 so so so if r1 is not equal 0 if r1 is not equal 0 if r1 is not equal 0 meaning move n on r1 used as a condition on r1 used as a condition on r1 used as a condition we're gonna be moving r4 we're gonna be moving r4 we're gonna be moving r4 into into into r2 r2 r2 if the branch is branching then r1 is if the branch is branching then r1 is if the branch is branching then r1 is zero zero zero and in that case we want to be moving r5 and in that case we want to be moving r5 and in that case we want to be moving r5 into r3 into r3 into r3 and this is our if converted code that and this is our if converted code that and this is our if converted code that has four instructions worth of work but has four instructions worth of work but has four instructions worth of work but no branches in them in contrast the no branches in them in contrast the no branches in them in contrast the original code has original code has original code has three instructions worth of work in this three instructions worth of work in this three instructions worth of work in this path path path and two instructions worth of work in and two instructions worth of work in and two instructions worth of work in this path but it has this branch that this path but it has this branch that this path but it has this branch that might be difficult to predict

## MovZ MovN Performance： Part 1
Kind: captions Language: en so now let's look at the performance of so now let's look at the performance of so now let's look at the performance of move Z and move and type of in if move Z and move and type of in if move Z and move and type of in if conversion in the previous quiz we have conversion in the previous quiz we have conversion in the previous quiz we have already seen that branch code like this already seen that branch code like this already seen that branch code like this can be if converted into code like this can be if converted into code like this can be if converted into code like this let's suppose that this Branch here is let's suppose that this Branch here is let's suppose that this Branch here is perfectly predicted so all we have to perfectly predicted so all we have to perfectly predicted so all we have to worry about is this one and let's say worry about is this one and let's say worry about is this one and let's say that this branch is 80% accurately that this branch is 80% accurately that this branch is 80% accurately predicted and let's say there is a 40 predicted and let's say there is a 40 predicted and let's say there is a 40 instruction penalty on mispredictions in instruction penalty on mispredictions in instruction penalty on mispredictions in that case we have here this cost us four that case we have here this cost us four that case we have here this cost us four instructions this costs us three instructions this costs us three instructions this costs us three instructions on this path or two instructions on this path or two instructions on this path or two instructions on this path let's say it's instructions on this path let's say it's instructions on this path let's say it's a very difficult to predict Branch so a very difficult to predict Branch so a very difficult to predict Branch so it's not it's not it's not biased in which case we have 2.5 biased in which case we have 2.5 biased in which case we have 2.5 instructions all the time plus 20% of instructions all the time plus 20% of instructions all the time plus 20% of the time we have a 40 instruction the time we have a 40 instruction the time we have a 40 instruction penalty this gives us on average 10.5 penalty this gives us on average 10.5 penalty this gives us on average 10.5 instructions worth of work on this instructions worth of work on this instructions worth of work on this versus four here so obviously the if versus four here so obviously the if versus four here so obviously the if converted code is more optimal

## MovZ MovN Performance Quiz： Part 1
Kind: captions Language: en in fact let's do a quiz on moves in in fact let's do a quiz on moves in in fact let's do a quiz on moves in movement performance when we do if movement performance when we do if movement performance when we do if conversions so this is the original code conversions so this is the original code conversions so this is the original code let's say that this branch here is 50 of let's say that this branch here is 50 of let's say that this branch here is 50 of the time taken so half of the time we the time taken so half of the time we the time taken so half of the time we end up doing this and half of the time end up doing this and half of the time end up doing this and half of the time we end up doing this we end up doing this we end up doing this so we can say then that the if so we can say then that the if so we can say then that the if conversion code works better when the conversion code works better when the conversion code works better when the prediction accuracy is below prediction accuracy is below prediction accuracy is below what assuming a 30 instruction what assuming a 30 instruction what assuming a 30 instruction misprediction penalty for this branch misprediction penalty for this branch misprediction penalty for this branch and assuming of course that this branch and assuming of course that this branch and assuming of course that this branch is always perfectly predicted because is always perfectly predicted because is always perfectly predicted because it's easy

## MovZ MovN Performance Quiz Solution： Part 1
Kind: captions Language: en and in Loto focuses The Solution for All and in Loto focuses The Solution for All and in Loto focuses The Solution for All modes in motion performance crisis da modes in motion performance crisis da modes in motion performance crisis da knife stunning first person to take knife stunning first person to take knife stunning first person to take some Things That on Average o Yakult some Things That on Average o Yakult some Things That on Average o Yakult half of the time Tree structure and a half of the time Tree structure and a half of the time Tree structure and a Half the time to inflation is with Half the time to inflation is with Half the time to inflation is with Perfect liked this will be executing to Perfect liked this will be executing to Perfect liked this will be executing to Point for instructions on the Average Point for instructions on the Average Point for instructions on the Average prediction when discussing finds QR prediction when discussing finds QR prediction when discussing finds QR get for instructions but we can of get for instructions but we can of get for instructions but we can of explorations of Heavy or Miss I predict explorations of Heavy or Miss I predict explorations of Heavy or Miss I predict an Eight times Eight Streisand Penalty an Eight times Eight Streisand Penalty an Eight times Eight Streisand Penalty and Damage protection research This and Damage protection research This and Damage protection research This world is equal to four da aurex de month world is equal to four da aurex de month world is equal to four da aurex de month production right it needs to be  for me production right it needs to be  for me production right it needs to be  for me I'm Stuck On Fire lyrics One in five I'm Stuck On Fire lyrics One in five I'm Stuck On Fire lyrics One in five instructions worthy of the best Year and instructions worthy of the best Year and instructions worthy of the best Year and three strikes with this old vintage and it three strikes with this old vintage and it three strikes with this old vintage and it was me super tip carrots inside was me super tip carrots inside was me super tip carrots inside balance of the month protection witnesses to balance of the month protection witnesses to balance of the month protection witnesses to be European on Live I'm going to call a be European on Live I'm going to call a be European on Live I'm going to call a stew on top Sou black and mate and stew on top Sou black and mate and stew on top Sou black and mate and make present in Carol these be long make present in Carol these be long make present in Carol these be long night five percent and converts got night five percent and converts got night five percent and converts got this better if the predictions in the this better if the predictions in the this better if the predictions in the skies above and Flowers and then The skies above and Flowers and then The skies above and Flowers and then The region Code beat and do spirit Nights region Code beat and do spirit Nights region Code beat and do spirit Nights when we first need to have Cooper films

## MOVc Summary： Part 1
Kind: captions Language: en so to summarize the approach where we so to summarize the approach where we so to summarize the approach where we have the conditional move instructions have the conditional move instructions have the conditional move instructions we need a compiler support to do any if we need a compiler support to do any if we need a compiler support to do any if conversion if the compiler doesn't do conversion if the compiler doesn't do conversion if the compiler doesn't do anything the code will be just left with anything the code will be just left with anything the code will be just left with a branch so this is not a fully a branch so this is not a fully a branch so this is not a fully backwards compatible approach in that if backwards compatible approach in that if backwards compatible approach in that if we add the conditional move instructions we add the conditional move instructions we add the conditional move instructions to the is a we're only allowing the to the is a we're only allowing the to the is a we're only allowing the compiler to take advantage of that but compiler to take advantage of that but compiler to take advantage of that but old code will still not use these old code will still not use these old code will still not use these conditional instructions this approach conditional instructions this approach conditional instructions this approach will remove hard to predict branches so will remove hard to predict branches so will remove hard to predict branches so if we have a hard to predict branch for if we have a hard to predict branch for if we have a hard to predict branch for example through profiling we determine example through profiling we determine example through profiling we determine that a particular branch is difficult to that a particular branch is difficult to that a particular branch is difficult to predict we can perform if conversion to predict we can perform if conversion to predict we can perform if conversion to get rid of it if conversion typically get rid of it if conversion typically get rid of it if conversion typically requires use of more registers than the requires use of more registers than the requires use of more registers than the original code because we need to keep original code because we need to keep original code because we need to keep all the results from both of the paths all the results from both of the paths all the results from both of the paths and also more instructions will be and also more instructions will be and also more instructions will be executed as a result of if conversion executed as a result of if conversion executed as a result of if conversion because we are executing instructions because we are executing instructions because we are executing instructions from both paths not just one and also from both paths not just one and also from both paths not just one and also because we need to add the conditional because we need to add the conditional because we need to add the conditional move instructions to select the actual move instructions to select the actual move instructions to select the actual results the ones we want to keep from results the ones we want to keep from results the ones we want to keep from among the results that we have generated among the results that we have generated among the results that we have generated from both paths some of these are from both paths some of these are from both paths some of these are necessary in order to do if conversion necessary in order to do if conversion necessary in order to do if conversion for example the compiler support is okay for example the compiler support is okay for example the compiler support is okay we need that it will remove the we need that it will remove the we need that it will remove the conditional branches that are hard to conditional branches that are hard to conditional branches that are hard to predict so that we want to stay the way predict so that we want to stay the way predict so that we want to stay the way it is but the need for more registers it is but the need for more registers it is but the need for more registers for example can we do something about for example can we do something about for example can we do something about that more instructions are executed that more instructions are executed that more instructions are executed because we execute both paths this again because we execute both paths this again because we execute both paths this again is okay that's how we do if conversion is okay that's how we do if conversion is okay that's how we do if conversion but do we really have to have additional but do we really have to have additional but do we really have to have additional instructions just to select the results instructions just to select the results instructions just to select the results to get rid of this in this problem we to get rid of this in this problem we to get rid of this in this problem we can change the rice a so that every can change the rice a so that every can change the rice a so that every instruction is now conditional every instruction is now conditional every instruction is now conditional every instruction computes its result and then instruction computes its result and then instruction computes its result and then writes it or doesn't write it to the writes it or doesn't write it to the writes it or doesn't write it to the destination register depending on destination register depending on destination register depending on whether a condition is true or not this whether a condition is true or not this whether a condition is true or not this approach is called full predication and approach is called full predication and approach is called full predication and it requires extensive support in the it requires extensive support in the it requires extensive support in the instruction set

## Full Predication HW Support： Part 1
Kind: captions Language: en so let's compare what we need for the so let's compare what we need for the so let's compare what we need for the conditional moves at what we need for conditional moves at what we need for conditional moves at what we need for full predication so for the move full predication so for the move full predication so for the move instructions that are conditional we instructions that are conditional we instructions that are conditional we need a separate opcode that tells us need a separate opcode that tells us need a separate opcode that tells us this is a conditional instruction this is a conditional instruction this is a conditional instruction usually we have a separate opcode for usually we have a separate opcode for usually we have a separate opcode for each particular condition so we need a each particular condition so we need a each particular condition so we need a number of new opcode number of new opcode number of new opcode for full predication we use the same for full predication we use the same for full predication we use the same instructions that we use without instructions that we use without instructions that we use without conditions conditions conditions we add condition bits for full we add condition bits for full we add condition bits for full predication however we add condition predication however we add condition predication however we add condition bits to every instructions so every bits to every instructions so every bits to every instructions so every instruction word contains some bits that instruction word contains some bits that instruction word contains some bits that tell us what is the condition for tell us what is the condition for tell us what is the condition for writing to the destination register for writing to the destination register for writing to the destination register for example the intel itanium instruction example the intel itanium instruction example the intel itanium instruction set has full predication and this is set has full predication and this is set has full predication and this is what an add instruction would look like what an add instruction would look like what an add instruction would look like it specifies the two source registers it specifies the two source registers it specifies the two source registers the destination registers that this is the destination registers that this is the destination registers that this is an ad and it has a field within the an ad and it has a field within the an ad and it has a field within the instruction to specify the so-called instruction to specify the so-called instruction to specify the so-called qualifying predicate that is where the qualifying predicate that is where the qualifying predicate that is where the condition for doing the right to the r1 condition for doing the right to the r1 condition for doing the right to the r1 register will be found in the attained register will be found in the attained register will be found in the attained iam instruction has 41 bits where the iam instruction has 41 bits where the iam instruction has 41 bits where the least significant six bits specify what least significant six bits specify what least significant six bits specify what the qualifying predicate is the the qualifying predicate is the the qualifying predicate is the predicates in the Itanium are actually predicates in the Itanium are actually predicates in the Itanium are actually small one bit registers so that you can small one bit registers so that you can small one bit registers so that you can do a condition check and store the do a condition check and store the do a condition check and store the result in this one bit register and then result in this one bit register and then result in this one bit register and then really the six bits here tell you which really the six bits here tell you which really the six bits here tell you which of the 64 bit conditional registers are of the 64 bit conditional registers are of the 64 bit conditional registers are you gonna use to determine whether to you gonna use to determine whether to you gonna use to determine whether to write to r1

## Full Predication Example： Part 1
Kind: captions Language: en so let's say we have this code where so let's say we have this code where so let's say we have this code where again r1 is compared to zero any branch again r1 is compared to zero any branch again r1 is compared to zero any branch here if it's equal to zero if it's not here if it's equal to zero if it's not here if it's equal to zero if it's not equal to zero we do this and branch out equal to zero we do this and branch out equal to zero we do this and branch out if it is equal to zero we do this and if it is equal to zero we do this and if it is equal to zero we do this and continue we have seen that this code continue we have seen that this code continue we have seen that this code results in four instructions worth of results in four instructions worth of results in four instructions worth of execution when we have the conditional execution when we have the conditional execution when we have the conditional moves two instructions that actually do moves two instructions that actually do moves two instructions that actually do the work of adye's and two instructions the work of adye's and two instructions the work of adye's and two instructions that conditionally write results back to that conditionally write results back to that conditionally write results back to r2 and r3 with full predication Allah r2 and r3 with full predication Allah r2 and r3 with full predication Allah the Itanium we get the condition check the Itanium we get the condition check the Itanium we get the condition check is done using the predicate set is done using the predicate set is done using the predicate set instruction that compares equals zero on instruction that compares equals zero on instruction that compares equals zero on r1 and sets p1 and p2 accordingly the r1 and sets p1 and p2 accordingly the r1 and sets p1 and p2 accordingly the predicates are always set opposite of predicates are always set opposite of predicates are always set opposite of each other so that if r1 is 0 P 1 is set each other so that if r1 is 0 P 1 is set each other so that if r1 is 0 P 1 is set to true and P 2 to false if r1 is not to true and P 2 to false if r1 is not to true and P 2 to false if r1 is not equal to 0 then P 1 is set to false and equal to 0 then P 1 is set to false and equal to 0 then P 1 is set to false and P 2 is set to true now we can simply do P 2 is set to true now we can simply do P 2 is set to true now we can simply do the add I of 1 plus R 2 into R 2 but we the add I of 1 plus R 2 into R 2 but we the add I of 1 plus R 2 into R 2 but we will predicate it with p2 so that it will predicate it with p2 so that it will predicate it with p2 so that it only gets done if r1 was not equal to 0 only gets done if r1 was not equal to 0 only gets done if r1 was not equal to 0 and then this instruction becomes and then this instruction becomes and then this instruction becomes exactly what it was here except that now exactly what it was here except that now exactly what it was here except that now it's predicated so that it only gets it's predicated so that it only gets it's predicated so that it only gets done if R 1 is equal to 0 so as you can done if R 1 is equal to 0 so as you can done if R 1 is equal to 0 so as you can see with this after we do this initial see with this after we do this initial see with this after we do this initial instruction that simply sets the instruction that simply sets the instruction that simply sets the predicate any code that was here simply predicate any code that was here simply predicate any code that was here simply gets predicated with one of the gets predicated with one of the gets predicated with one of the conditions the opposite condition conditions the opposite condition conditions the opposite condition predicates the other branches code and predicates the other branches code and predicates the other branches code and that means that first of all we can use that means that first of all we can use that means that first of all we can use the same registers that we use before we the same registers that we use before we the same registers that we use before we don't need the additional r4 and r5 that don't need the additional r4 and r5 that don't need the additional r4 and r5 that we had in the previous example and we had in the previous example and we had in the previous example and second we still do have the work of both second we still do have the work of both second we still do have the work of both paths but there is no additional work to paths but there is no additional work to paths but there is no additional work to now move the results into the registers now move the results into the registers now move the results into the registers that have the destination registers so that have the destination registers so that have the destination registers so the overhead here is kind of similar to the overhead here is kind of similar to the overhead here is kind of similar to what we had with branches with branches what we had with branches with branches what we had with branches with branches we needed to have the branch and then we needed to have the branch and then we needed to have the branch and then the code from either path here we have the code from either path here we have the code from either path here we have the condition set followed by the code the condition set followed by the code the condition set followed by the code from both parts so the only really from both parts so the only really from both parts so the only really overhead that we have in terms of overhead that we have in terms of overhead that we have in terms of executing more instructions comes from executing more instructions comes from executing more instructions comes from doing the work of both paths instead of doing the work of both paths instead of doing the work of both paths instead of just one but there are no additional just one but there are no additional just one but there are no additional instructions to select between the instructions to select between the instructions to select between the results

## Full Predication Quiz： Part 1
Kind: captions Language: en let us see if we can do some full let us see if we can do some full let us see if we can do some full predication of our own suppose that this predication of our own suppose that this predication of our own suppose that this is the original code where we check if is the original code where we check if is the original code where we check if r2 is equal to zero if it is we jump r2 is equal to zero if it is we jump r2 is equal to zero if it is we jump here and decrement r1 by one here and decrement r1 by one here and decrement r1 by one if r2 is not 0 then we continue if r2 is not 0 then we continue if r2 is not 0 then we continue increment r1 by 1 increment r1 by 1 increment r1 by 1 and then go out so after if conversion and then go out so after if conversion and then go out so after if conversion with full predication with full predication with full predication we get this we get this we get this first first first we set predicates p1 and p2 according to we set predicates p1 and p2 according to we set predicates p1 and p2 according to whether r2 is equal to 0. remember that whether r2 is equal to 0. remember that whether r2 is equal to 0. remember that this one will be set this one will be set this one will be set if r2 is equal to 0 and this one if r2 is equal to 0 and this one if r2 is equal to 0 and this one is set if r2 is not equal to zero so is set if r2 is not equal to zero so is set if r2 is not equal to zero so they're opposite predicates they're opposite predicates they're opposite predicates this one is set if r2 is equal to zero this one is set if r2 is equal to zero this one is set if r2 is equal to zero the actual question for you is what goes the actual question for you is what goes the actual question for you is what goes into the predicate for the next into the predicate for the next into the predicate for the next instruction and the predicate for the instruction and the predicate for the instruction and the predicate for the next instruction after that and then the next instruction after that and then the next instruction after that and then the two instructions are add r1 r1 1 and add two instructions are add r1 r1 1 and add two instructions are add r1 r1 1 and add r1 r1 r1 r1 -1 so what are the predicates here r1 -1 so what are the predicates here r1 -1 so what are the predicates here also if we assume that this code gets us also if we assume that this code gets us also if we assume that this code gets us a cpf 0.5 without mispredictions a cpf 0.5 without mispredictions a cpf 0.5 without mispredictions and that the misprediction penalty is 10 and that the misprediction penalty is 10 and that the misprediction penalty is 10 cycles then we should do the if cycles then we should do the if cycles then we should do the if conversion if the branch equals zero conversion if the branch equals zero conversion if the branch equals zero here is predicted below what accuracy so here is predicted below what accuracy so here is predicted below what accuracy so doing conversion if branch equals zero doing conversion if branch equals zero doing conversion if branch equals zero prediction is less than what percentage prediction is less than what percentage prediction is less than what percentage correct

## Full Predication Quiz Solution： Part 1
Kind: captions Language: en let us look at the solution to our full let us look at the solution to our full let us look at the solution to our full predication quiz we have this branch predication quiz we have this branch predication quiz we have this branch based if-then-else called we're trying based if-then-else called we're trying based if-then-else called we're trying to if convert it using full predication to if convert it using full predication to if convert it using full predication p1 here is set when r2 is 0 when R 2 is p1 here is set when r2 is 0 when R 2 is p1 here is set when r2 is 0 when R 2 is we branch to the else part so this is we branch to the else part so this is we branch to the else part so this is what gets done if r2 is 0 that means what gets done if r2 is 0 that means what gets done if r2 is 0 that means that we do this when p1 is true and then that we do this when p1 is true and then that we do this when p1 is true and then of course we do this when it's not true of course we do this when it's not true of course we do this when it's not true the next part that we want to do is when the next part that we want to do is when the next part that we want to do is when should we do the if conversion so what should we do the if conversion so what should we do the if conversion so what needs to be the accuracy here let's needs to be the accuracy here let's needs to be the accuracy here let's first see how many instructions we have first see how many instructions we have first see how many instructions we have in each of these here we have 3 in each of these here we have 3 in each of these here we have 3 instructions with the CPI of 0.5 this instructions with the CPI of 0.5 this instructions with the CPI of 0.5 this always takes us one point five cycles always takes us one point five cycles always takes us one point five cycles assuming that there are other things assuming that there are other things assuming that there are other things that can be done in the rest of the that can be done in the rest of the that can be done in the rest of the cycle here when this branch is taken we cycle here when this branch is taken we cycle here when this branch is taken we do two instructions when this branch is do two instructions when this branch is do two instructions when this branch is not-taken not-taken not-taken we do three instructions so let's split we do three instructions so let's split we do three instructions so let's split the difference and say that on average the difference and say that on average the difference and say that on average we do 2.5 instructions at the CPI of 0.5 we do 2.5 instructions at the CPI of 0.5 we do 2.5 instructions at the CPI of 0.5 that means that without a misprediction that means that without a misprediction that means that without a misprediction we get 1.25 cycles a misprediction adds we get 1.25 cycles a misprediction adds we get 1.25 cycles a misprediction adds 10 cycles these two perform equally when 10 cycles these two perform equally when 10 cycles these two perform equally when the cost of mispredictions here is 0.25 the cost of mispredictions here is 0.25 the cost of mispredictions here is 0.25 cycles per branch so pretty much the cycles per branch so pretty much the cycles per branch so pretty much the difference between these two is 0.25 difference between these two is 0.25 difference between these two is 0.25 cycles so on average we can lose this cycles so on average we can lose this cycles so on average we can lose this much to get this to be equal the penalty much to get this to be equal the penalty much to get this to be equal the penalty for a single misprediction is 10 cycles for a single misprediction is 10 cycles for a single misprediction is 10 cycles if we can lose only 0.25 cycles per if we can lose only 0.25 cycles per if we can lose only 0.25 cycles per branch that means that only one in 40 branch that means that only one in 40 branch that means that only one in 40 branches can be mispredicted one in fort branches can be mispredicted one in fort branches can be mispredicted one in fort is 2.5% that means that our correct is 2.5% that means that our correct is 2.5% that means that our correct prediction needs to be at least 97.5% to prediction needs to be at least 97.5% to prediction needs to be at least 97.5% to not do the if conversion if the correct not do the if conversion if the correct not do the if conversion if the correct prediction rate is less than this then prediction rate is less than this then prediction rate is less than this then we are better off with if conversion

## Predication Outro： Part 1
Kind: captions Language: en in this lesson we discussed if in this lesson we discussed if in this lesson we discussed if conversion where we convert difficult to conversion where we convert difficult to conversion where we convert difficult to predict branches predict branches predict branches into code that is longer but has no into code that is longer but has no into code that is longer but has no branches branches branches we also learned about special we also learned about special we also learned about special instructions that we need for if instructions that we need for if instructions that we need for if conversion to work conversion to work conversion to work now that we are up to speed now that we are up to speed now that we are up to speed with how control hazards are handled we with how control hazards are handled we with how control hazards are handled we need to learn how to handle data hazards need to learn how to handle data hazards need to learn how to handle data hazards while keeping a good instruction flow in while keeping a good instruction flow in while keeping a good instruction flow in our pipeline



---


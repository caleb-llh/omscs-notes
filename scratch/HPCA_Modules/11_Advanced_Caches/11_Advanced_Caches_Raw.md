# 11_Advanced_Caches (Raw Transcripts)

# Playlist 3 Module 4 Raw Transcript

## VIPT Aliasing Avoidance Quiz： Part 4
Kind: captions Language: en so now that we know how big the virtual so now that we know how big the virtual so now that we know how big the virtual index physically tagged Cache can be to index physically tagged Cache can be to index physically tagged Cache can be to avoid aliasing let's do a VIP aling avoid aliasing let's do a VIP aling avoid aliasing let's do a VIP aling avoidance squiz let's say we want a avoidance squiz let's say we want a avoidance squiz let's say we want a cache that is four-way set associative cache that is four-way set associative cache that is four-way set associative and has a 16 by block size and let's say and has a 16 by block size and let's say and has a 16 by block size and let's say that the system has an 8 kiloby page that the system has an 8 kiloby page that the system has an 8 kiloby page size if we want no aliasing in this size if we want no aliasing in this size if we want no aliasing in this cache its maximum size can be how many cache its maximum size can be how many cache its maximum size can be how many kilobytes

## VIPT Aliasing Avoidance Quiz Solution： Part 4
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our virtual index physically tag cache virtual index physically tag cache virtual index physically tag cache aliasing avoidance quiz we want no aliasing avoidance quiz we want no aliasing avoidance quiz we want no aliasing which means the index bits have aliasing which means the index bits have aliasing which means the index bits have to come from the page offset the page to come from the page offset the page to come from the page offset the page has 2 to the 13th bytes so the page has 2 to the 13th bytes so the page has 2 to the 13th bytes so the page offset has 13 bits in the address offset has 13 bits in the address offset has 13 bits in the address breakdown for accessing the cache that breakdown for accessing the cache that breakdown for accessing the cache that means that our index and offset for the means that our index and offset for the means that our index and offset for the cache need to fit in 13 bits the 16 byte cache need to fit in 13 bits the 16 byte cache need to fit in 13 bits the 16 byte block size means that the cache block block size means that the cache block block size means that the cache block offset is four bits that's how many bits offset is four bits that's how many bits offset is four bits that's how many bits we need to tell us where in the 16 bytes we need to tell us where in the 16 bytes we need to tell us where in the 16 bytes we are and that means that we have nine we are and that means that we have nine we are and that means that we have nine bits for the index we can use fewer but bits for the index we can use fewer but bits for the index we can use fewer but that would make the cache even smaller that would make the cache even smaller that would make the cache even smaller we cannot use more because then we would we cannot use more because then we would we cannot use more because then we would get aliasing so the maximum cach size get aliasing so the maximum cach size get aliasing so the maximum cach size will be when it has 2 to the 9th sets so will be when it has 2 to the 9th sets so will be when it has 2 to the 9th sets so we have 2 to the 9 sets time 2 to the we have 2 to the 9 sets time 2 to the we have 2 to the 9 sets time 2 to the 4th bytes in each block times it's a 4th bytes in each block times it's a 4th bytes in each block times it's a four-way set associative cache so we four-way set associative cache so we four-way set associative cache so we have 2 to the 2 four blocks in a set so have 2 to the 2 four blocks in a set so have 2 to the 2 four blocks in a set so this is basically bytes per block blocks this is basically bytes per block blocks this is basically bytes per block blocks per set how many sets and that amounts per set how many sets and that amounts per set how many sets and that amounts to 2 to the 15th bytes in the cache so to 2 to the 15th bytes in the cache so to 2 to the 15th bytes in the cache so the answer here is 32 kilobytes which is the answer here is 32 kilobytes which is the answer here is 32 kilobytes which is 2 to the 15th bytes so we cannot have a 2 to the 15th bytes so we cannot have a 2 to the 15th bytes so we cannot have a cache that is larger than 32 kiloby and cache that is larger than 32 kiloby and cache that is larger than 32 kiloby and yet it avoids aling with these yet it avoids aling with these yet it avoids aling with these parameters know that really these two parameters know that really these two parameters know that really these two have to be equal to the page size have to be equal to the page size have to be equal to the page size because two to this number of bits time because two to this number of bits time because two to this number of bits time 2 to this number of bits has to be equal 2 to this number of bits has to be equal 2 to this number of bits has to be equal to two to this number of bits so really to two to this number of bits so really to two to this number of bits so really the maximum size of the cache here is the maximum size of the cache here is the maximum size of the cache here is the page size times the associativity the page size times the associativity the page size times the associativity and the only way to avoid aliasing in a and the only way to avoid aliasing in a and the only way to avoid aliasing in a cache while making it larger is to cache while making it larger is to cache while making it larger is to increase associativity

## Real VIPT Caches： Part 4
Kind: captions Language: en so let's look at the sizes of some so let's look at the sizes of some so let's look at the sizes of some actual virtually indexed physically actual virtually indexed physically actual virtually indexed physically tagged caches while doing the previous tagged caches while doing the previous tagged caches while doing the previous quiz we have figured out that the cache quiz we have figured out that the cache quiz we have figured out that the cache size needs to be less than or equal to size needs to be less than or equal to size needs to be less than or equal to the associativity of the cache times the the associativity of the cache times the the associativity of the cache times the page size so if we look at for example page size so if we look at for example page size so if we look at for example the Intel Pentium 4 processor it has a the Intel Pentium 4 processor it has a the Intel Pentium 4 processor it has a 4-way set associative cache and a page 4-way set associative cache and a page 4-way set associative cache and a page size of 4 kilobytes and it's level 1 size of 4 kilobytes and it's level 1 size of 4 kilobytes and it's level 1 cache is 16 kilobytes the more recent cache is 16 kilobytes the more recent cache is 16 kilobytes the more recent court to Nehalem Sandy Bridge and court to Nehalem Sandy Bridge and court to Nehalem Sandy Bridge and Haswell processors from Intel R 8 weight Haswell processors from Intel R 8 weight Haswell processors from Intel R 8 weight set associative of course they still set associative of course they still set associative of course they still have the 4 kilobyte page size and have the 4 kilobyte page size and have the 4 kilobyte page size and surprise surprise the level 1 cache is surprise surprise the level 1 cache is surprise surprise the level 1 cache is 32 kilobytes the upcoming skylight 32 kilobytes the upcoming skylight 32 kilobytes the upcoming skylight design from Intel is rumored to be 16 design from Intel is rumored to be 16 design from Intel is rumored to be 16 Way set associative and you guessed it Way set associative and you guessed it Way set associative and you guessed it it's rumored to have a 64 kilobyte level it's rumored to have a 64 kilobyte level it's rumored to have a 64 kilobyte level 1 cache

## Associativity and Hit Time： Part 4
Kind: captions Language: en for the next method to reduce hit time for the next method to reduce hit time for the next method to reduce hit time let's revisit the relationship between let's revisit the relationship between let's revisit the relationship between the associativity and hit time so if we the associativity and hit time so if we the associativity and hit time so if we have high associativity in the cache we have high associativity in the cache we have high associativity in the cache we will have fewer conflicts for the set in will have fewer conflicts for the set in will have fewer conflicts for the set in the cache when multiple blocks want to the cache when multiple blocks want to the cache when multiple blocks want to use the same set which leads to a lower use the same set which leads to a lower use the same set which leads to a lower miss rate and that we like with high miss rate and that we like with high miss rate and that we like with high associativity we have just seen we can associativity we have just seen we can associativity we have just seen we can build larger virtual index physical build larger virtual index physical build larger virtual index physical little caches which reduces the miss little caches which reduces the miss little caches which reduces the miss rate and so we like that too but high rate and so we like that too but high rate and so we like that too but high associativity leads to slower hits the associativity leads to slower hits the associativity leads to slower hits the hit time goes up and that we don't like hit time goes up and that we don't like hit time goes up and that we don't like on the other hand if we go all the way on the other hand if we go all the way on the other hand if we go all the way to direct mapped cache it's the Miss to direct mapped cache it's the Miss to direct mapped cache it's the Miss rate goes up for these two reasons and rate goes up for these two reasons and rate goes up for these two reasons and that is bad but the hit time is reduced that is bad but the hit time is reduced that is bad but the hit time is reduced because the direct mapped cache has only because the direct mapped cache has only because the direct mapped cache has only one place where a block can be so we can one place where a block can be so we can one place where a block can be so we can quickly check whether it's there or not quickly check whether it's there or not quickly check whether it's there or not and we like that so we can see that high and we like that so we can see that high and we like that so we can see that high associativity in a cache improves the associativity in a cache improves the associativity in a cache improves the miss rate but makes the hit time worse miss rate but makes the hit time worse miss rate but makes the hit time worse while simple direct mapped cache is while simple direct mapped cache is while simple direct mapped cache is sacrifice the meets rate to improve hit sacrifice the meets rate to improve hit sacrifice the meets rate to improve hit time so now we will see if we can get time so now we will see if we can get time so now we will see if we can get this and this simultaneously by kind of this and this simultaneously by kind of this and this simultaneously by kind of cheating on our associativity

## Way Prediction： Part 4
Kind: captions Language: en one way of cheating on associativity is one way of cheating on associativity is one way of cheating on associativity is called way prediction we start with a called way prediction we start with a called way prediction we start with a set associative cache which has a low set associative cache which has a low set associative cache which has a low miss rate but would have a slow hit time miss rate but would have a slow hit time miss rate but would have a slow hit time now what we do we guess which line in now what we do we guess which line in now what we do we guess which line in the set is the most likely to hit so the set is the most likely to hit so the set is the most likely to hit so what we do is we use the index bits to what we do is we use the index bits to what we do is we use the index bits to determine which side should we'll be determine which side should we'll be determine which side should we'll be looking at and now instead of reading looking at and now instead of reading looking at and now instead of reading out all the tags in that line and out all the tags in that line and out all the tags in that line and checking which one hits we guess which checking which one hits we guess which checking which one hits we guess which one is the likely to hit and just check one is the likely to hit and just check one is the likely to hit and just check that one so this gives us the access that one so this gives us the access that one so this gives us the access time similar to a direct mapped cache if time similar to a direct mapped cache if time similar to a direct mapped cache if we guessed right if we are wrong then we we guessed right if we are wrong then we we guessed right if we are wrong then we got no hit on the line we tried but the got no hit on the line we tried but the got no hit on the line we tried but the set has other lines so we try a normal set has other lines so we try a normal set has other lines so we try a normal set associative check which will have a set associative check which will have a set associative check which will have a higher hit time so the idea is that higher hit time so the idea is that higher hit time so the idea is that overall our miss rate is that of a set overall our miss rate is that of a set overall our miss rate is that of a set associative cache we either find it this associative cache we either find it this associative cache we either find it this way or this way so the Miss rate will be way or this way so the Miss rate will be way or this way so the Miss rate will be similar to a set associative cache similar to a set associative cache similar to a set associative cache because even if we didn't find it in a because even if we didn't find it in a because even if we didn't find it in a kind of direct mapped way we will find kind of direct mapped way we will find kind of direct mapped way we will find it this way but the hit time if what it this way but the hit time if what it this way but the hit time if what gases are mostly right is mostly that of gases are mostly right is mostly that of gases are mostly right is mostly that of a direct mapped cache so we have a direct mapped cache so we have a direct mapped cache so we have combined the low miss rate of a set combined the low miss rate of a set combined the low miss rate of a set associative cache with most of the time associative cache with most of the time associative cache with most of the time getting the hit time of a direct mapped getting the hit time of a direct mapped getting the hit time of a direct mapped cache and sometimes only having to cache and sometimes only having to cache and sometimes only having to suffer the longer hit time of a normal suffer the longer hit time of a normal suffer the longer hit time of a normal set associative cache

## Way Prediction Performance： Part 4
Kind: captions Language: en okay so how often can we expect to guess okay so how often can we expect to guess okay so how often can we expect to guess correctly in a way prediction cache we correctly in a way prediction cache we correctly in a way prediction cache we can get an idea for this by looking at a can get an idea for this by looking at a can get an idea for this by looking at a smaller direct map cache basically let's smaller direct map cache basically let's smaller direct map cache basically let's say we have a 2-way set-associative say we have a 2-way set-associative say we have a 2-way set-associative cache each set of this cache has two cache each set of this cache has two cache each set of this cache has two ways two lines in it and by doing way ways two lines in it and by doing way ways two lines in it and by doing way prediction we are effectively using one prediction we are effectively using one prediction we are effectively using one of these at first and only if we don't of these at first and only if we don't of these at first and only if we don't find it there we look at the other one find it there we look at the other one find it there we look at the other one so effectively a way prediction cache so effectively a way prediction cache so effectively a way prediction cache first tries to access what looks like a first tries to access what looks like a first tries to access what looks like a smaller direct mapped cache then it will smaller direct mapped cache then it will smaller direct mapped cache then it will try the entire set associative cache so try the entire set associative cache so try the entire set associative cache so we can get a pretty good estimate for we can get a pretty good estimate for we can get a pretty good estimate for the hit rate and hit latency and so on the hit rate and hit latency and so on the hit rate and hit latency and so on of a way prediction cache if we look at of a way prediction cache if we look at of a way prediction cache if we look at the direct map cache and a set the direct map cache and a set the direct map cache and a set associative cache so let's look at the associative cache so let's look at the associative cache so let's look at the basic 32 kilobyte 8 way set associative basic 32 kilobyte 8 way set associative basic 32 kilobyte 8 way set associative cache let's look at its hit write hit cache let's look at its hit write hit cache let's look at its hit write hit latency miss penalty and the overall aim latency miss penalty and the overall aim latency miss penalty and the overall aim at and we will also look at a 32 at and we will also look at a 32 at and we will also look at a 32 kilobyte 8 way set associative cache kilobyte 8 way set associative cache kilobyte 8 way set associative cache with way prediction the hit rate of this with way prediction the hit rate of this with way prediction the hit rate of this cache let's say it's around 90% the hit cache let's say it's around 90% the hit cache let's say it's around 90% the hit latency might be 2 cycle because it's latency might be 2 cycle because it's latency might be 2 cycle because it's highly associative the miss penalty highly associative the miss penalty highly associative the miss penalty would be 20 cycles for example and the would be 20 cycles for example and the would be 20 cycles for example and the overall aim at would then be 2 for the overall aim at would then be 2 for the overall aim at would then be 2 for the hit latency plus 10 percent of the time hit latency plus 10 percent of the time hit latency plus 10 percent of the time we have the miss penalty and we get 4 we have the miss penalty and we get 4 we have the miss penalty and we get 4 here now we already said that way here now we already said that way here now we already said that way prediction behaves like accessing only prediction behaves like accessing only prediction behaves like accessing only one of the ways in the cache with way one of the ways in the cache with way one of the ways in the cache with way prediction in an 8 way set associative prediction in an 8 way set associative prediction in an 8 way set associative cache we are really guessing which of cache we are really guessing which of cache we are really guessing which of the eight places is going to have the the eight places is going to have the the eight places is going to have the block so effectively with way prediction block so effectively with way prediction block so effectively with way prediction our first attempt will be to a 4 our first attempt will be to a 4 our first attempt will be to a 4 kilobyte direct map subset of this cache kilobyte direct map subset of this cache kilobyte direct map subset of this cache if we look at a normal 4 kilobyte direct if we look at a normal 4 kilobyte direct if we look at a normal 4 kilobyte direct mapped cache that's gonna have for mapped cache that's gonna have for mapped cache that's gonna have for example a 70% hit rate but the one cycle example a 70% hit rate but the one cycle example a 70% hit rate but the one cycle hit latency because it's faster and the hit latency because it's faster and the hit latency because it's faster and the Miss penalty will be the same if when I Miss penalty will be the same if when I Miss penalty will be the same if when I try to get the aim at for this basic try to get the aim at for this basic try to get the aim at for this basic direct mapped cache we will have 1 + 0 direct mapped cache we will have 1 + 0 direct mapped cache we will have 1 + 0 point three the Miss rate times twenty point three the Miss rate times twenty point three the Miss rate times twenty and this is gonna be one plus six so it and this is gonna be one plus six so it and this is gonna be one plus six so it gives us seven so obviously we are gives us seven so obviously we are gives us seven so obviously we are better off with the 8 way set better off with the 8 way set better off with the 8 way set associative cache however we're paying associative cache however we're paying associative cache however we're paying an extra cycle each time we access it an extra cycle each time we access it an extra cycle each time we access it for the hit latency in order to get the for the hit latency in order to get the for the hit latency in order to get the low miss penalties here with the way low miss penalties here with the way low miss penalties here with the way prediction cache what we are getting is prediction cache what we are getting is prediction cache what we are getting is 70% of the time we are going to find 70% of the time we are going to find 70% of the time we are going to find what we are looking for in the four what we are looking for in the four what we are looking for in the four kilobyte subset that we are checking kilobyte subset that we are checking kilobyte subset that we are checking first and we will have a one cycle first and we will have a one cycle first and we will have a one cycle latency for that in the remaining 20% latency for that in the remaining 20% latency for that in the remaining 20% we're gonna check the rest of the cash we're gonna check the rest of the cash we're gonna check the rest of the cash so our overall hit rate will be 90% what so our overall hit rate will be 90% what so our overall hit rate will be 90% what we don't find here we will still find we don't find here we will still find we don't find here we will still find here if it's in the 32 kilobyte cache so here if it's in the 32 kilobyte cache so here if it's in the 32 kilobyte cache so we get a 90% hit rate overall our hit we get a 90% hit rate overall our hit we get a 90% hit rate overall our hit latency now is 1 or 2 and our Miss latency now is 1 or 2 and our Miss latency now is 1 or 2 and our Miss penalty is 20 so how do we compute the penalty is 20 so how do we compute the penalty is 20 so how do we compute the aim at well what we now have is that 70% aim at well what we now have is that 70% aim at well what we now have is that 70% of the time we find what we are looking of the time we find what we are looking of the time we find what we are looking for in one cycle the other 30% of the for in one cycle the other 30% of the for in one cycle the other 30% of the time whether we have a hit or a Miss in time whether we have a hit or a Miss in time whether we have a hit or a Miss in the eight-way cache we have to check it the eight-way cache we have to check it the eight-way cache we have to check it so the other 30% of the time we are so the other 30% of the time we are so the other 30% of the time we are checking all of the ways for which we checking all of the ways for which we checking all of the ways for which we pay a two cycle latency and then 10% of pay a two cycle latency and then 10% of pay a two cycle latency and then 10% of the time we don't find what we are the time we don't find what we are the time we don't find what we are looking for in all of the ways so we looking for in all of the ways so we looking for in all of the ways so we have to pay the Miss penalty so now we have to pay the Miss penalty so now we have to pay the Miss penalty so now we have what looks like our hit time 70 have what looks like our hit time 70 have what looks like our hit time 70 percent of the time it's 130 percent of percent of the time it's 130 percent of percent of the time it's 130 percent of the time we have to use a tool so we end the time we have to use a tool so we end the time we have to use a tool so we end up having a one point three cycle on up having a one point three cycle on up having a one point three cycle on average hit time and the Miss penalty of average hit time and the Miss penalty of average hit time and the Miss penalty of the 8 way set associative cache which is the 8 way set associative cache which is the 8 way set associative cache which is still going to be - and this is how we still going to be - and this is how we still going to be - and this is how we improve things we now get an aim at of improve things we now get an aim at of improve things we now get an aim at of three point three because what we three point three because what we three point three because what we effectively got was the misses of an 8 effectively got was the misses of an 8 effectively got was the misses of an 8 way set associative cache but the hit way set associative cache but the hit way set associative cache but the hit time that is much closer to a direct time that is much closer to a direct time that is much closer to a direct mapped cache than it is to an 8 way set mapped cache than it is to an 8 way set mapped cache than it is to an 8 way set associative cache

## Way Prediction Quiz： Part 4
Kind: captions Language: en so let's see now if we understood why so let's see now if we understood why so let's see now if we understood why prediction we can use way prediction in prediction we can use way prediction in prediction we can use way prediction in what kind of a cache a fully associative what kind of a cache a fully associative what kind of a cache a fully associative cache an eight-way set assistive cash a cache an eight-way set assistive cash a cache an eight-way set assistive cash a 2-way set associative cache and a direct 2-way set associative cache and a direct 2-way set associative cache and a direct mapped cache check all of those that can mapped cache check all of those that can mapped cache check all of those that can benefit from a prediction

## Way Prediction Quiz Quiz Solution： Part 4
Kind: captions Language: en let's look at the answer for our way let's look at the answer for our way let's look at the answer for our way prediction quiz we can use way prediction quiz we can use way prediction quiz we can use way prediction in any cache that has more prediction in any cache that has more prediction in any cache that has more than one block in a set a direct mapped than one block in a set a direct mapped than one block in a set a direct mapped cache has only one block in a set so we cache has only one block in a set so we cache has only one block in a set so we cannot use way prediction basically a cannot use way prediction basically a cannot use way prediction basically a direct mapped cache already knows which direct mapped cache already knows which direct mapped cache already knows which one block to access without having to one block to access without having to one block to access without having to guess in any set associative or fully guess in any set associative or fully guess in any set associative or fully associative cache we can benefit from a associative cache we can benefit from a associative cache we can benefit from a prediction for example in a 2-way prediction for example in a 2-way prediction for example in a 2-way set-associative cache we can guess which set-associative cache we can guess which set-associative cache we can guess which one of the two is more likely to have one of the two is more likely to have one of the two is more likely to have the block in an 8 way set associative the block in an 8 way set associative the block in an 8 way set associative cache we are guessing which one of the cache we are guessing which one of the cache we are guessing which one of the eight and in a fully associative cache eight and in a fully associative cache eight and in a fully associative cache we are guessing which block in the whole we are guessing which block in the whole we are guessing which block in the whole cache is the most likely to have the cache is the most likely to have the cache is the most likely to have the block so the correct answer is that block so the correct answer is that block so the correct answer is that these should be selected and this one these should be selected and this one these should be selected and this one should not

## Replacement Policy and Hit Time： Part 4
Kind: captions Language: en next we will look at how the next we will look at how the next we will look at how the replacements policy for the cash which replacements policy for the cash which replacements policy for the cash which choses if you remember which block to choses if you remember which block to choses if you remember which block to kick out when we need more wrong kick out when we need more wrong kick out when we need more wrong interacts with the hit time of the cash interacts with the hit time of the cash interacts with the hit time of the cash a simple replacement policy such as a simple replacement policy such as a simple replacement policy such as random which randomly picks the block to random which randomly picks the block to random which randomly picks the block to replace among those that are in the set replace among those that are in the set replace among those that are in the set has nothing to update on a cache hit has nothing to update on a cache hit has nothing to update on a cache hit when we have a cache hit we don't have when we have a cache hit we don't have when we have a cache hit we don't have to do anything in order to be later able to do anything in order to be later able to do anything in order to be later able to do random when we have a Miss that's to do random when we have a Miss that's to do random when we have a Miss that's when we start implementing our policy by when we start implementing our policy by when we start implementing our policy by randomly selecting a block to kick out randomly selecting a block to kick out randomly selecting a block to kick out this results in faster hits because this results in faster hits because this results in faster hits because there is nothing we need to do for the there is nothing we need to do for the there is nothing we need to do for the replacement policy when we have a hit replacement policy when we have a hit replacement policy when we have a hit and that's nice however the random and that's nice however the random and that's nice however the random policy is not very good for miss rate policy is not very good for miss rate policy is not very good for miss rate because we often kick out blocks that we because we often kick out blocks that we because we often kick out blocks that we will very soon need to use and we don't will very soon need to use and we don't will very soon need to use and we don't like that in contrast least recently like that in contrast least recently like that in contrast least recently used or LRU results in a lower miss rate used or LRU results in a lower miss rate used or LRU results in a lower miss rate which we like but we have to on every which we like but we have to on every which we like but we have to on every cache hit update potentially a lot of cache hit update potentially a lot of cache hit update potentially a lot of counters even if we don't update and we counters even if we don't update and we counters even if we don't update and we have to look at them so there is a lot have to look at them so there is a lot have to look at them so there is a lot of activity on every cache hit that of activity on every cache hit that of activity on every cache hit that results from needing the state that will results from needing the state that will results from needing the state that will later use to select the block we will later use to select the block we will later use to select the block we will kick out and this spends a lot of power kick out and this spends a lot of power kick out and this spends a lot of power and also results in slower hits to and also results in slower hits to and also results in slower hits to illustrate what happens on a cache hit illustrate what happens on a cache hit illustrate what happens on a cache hit using LRU let's look at the 4-way set using LRU let's look at the 4-way set using LRU let's look at the 4-way set associative cache whose counters are associative cache whose counters are associative cache whose counters are currently 0 1 2 &amp; 3 if the processor currently 0 1 2 &amp; 3 if the processor currently 0 1 2 &amp; 3 if the processor accesses this line here and has a hit accesses this line here and has a hit accesses this line here and has a hit the new values of the counters are going the new values of the counters are going the new values of the counters are going to be this one becomes the most recently to be this one becomes the most recently to be this one becomes the most recently used and all of the other counters get used and all of the other counters get used and all of the other counters get decremented as you can see all of the decremented as you can see all of the decremented as you can see all of the counters have changed and this happens counters have changed and this happens counters have changed and this happens on a cache hit so a cache hit can result on a cache hit so a cache hit can result on a cache hit so a cache hit can result in updating up to n counters where n is in updating up to n counters where n is in updating up to n counters where n is our associativity let's look at what our associativity let's look at what our associativity let's look at what happens when one of the more recently happens when one of the more recently happens when one of the more recently used blocks is accessed let's say we used blocks is accessed let's say we used blocks is accessed let's say we access not the most recent list but the access not the most recent list but the access not the most recent list but the next one in that case this becomes the next one in that case this becomes the next one in that case this becomes the most recently used block the counters most recently used block the counters most recently used block the counters that are larger than this one get that are larger than this one get that are larger than this one get decremented so this becomes 2 and the decremented so this becomes 2 and the decremented so this becomes 2 and the smaller than this stay the same smaller than this stay the same smaller than this stay the same so we get this so only two counters have so we get this so only two counters have so we get this so only two counters have changed now but note that even though we changed now but note that even though we changed now but note that even though we have only changed two counters we had to have only changed two counters we had to have only changed two counters we had to actually check for each counter whether actually check for each counter whether actually check for each counter whether it's lower than this or not so even when it's lower than this or not so even when it's lower than this or not so even when we access the most recently used block we access the most recently used block we access the most recently used block and the counters don't change at all and the counters don't change at all and the counters don't change at all because this becomes three and counters because this becomes three and counters because this becomes three and counters larger than it get decremented but there larger than it get decremented but there larger than it get decremented but there is nothing larger and this lower then it is nothing larger and this lower then it is nothing larger and this lower then it stayed the same stayed the same stayed the same we still had to determine that this is we still had to determine that this is we still had to determine that this is the highest counter anyway so that we the highest counter anyway so that we the highest counter anyway so that we don't decrement anything or we had to don't decrement anything or we had to don't decrement anything or we had to actually check for each of these actually check for each of these actually check for each of these countries whether it's lower so one way countries whether it's lower so one way countries whether it's lower so one way or the other even for a hit to the most or the other even for a hit to the most or the other even for a hit to the most recently used block we have to do recently used block we have to do recently used block we have to do something and that again slows down the something and that again slows down the something and that again slows down the hits and spends power so what we want is hits and spends power so what we want is hits and spends power so what we want is a replacement policy that retains the a replacement policy that retains the a replacement policy that retains the Miss rate very close to LRU meaning we Miss rate very close to LRU meaning we Miss rate very close to LRU meaning we want to smartly replace the blocks so want to smartly replace the blocks so want to smartly replace the blocks so that we kick out the block that is that we kick out the block that is that we kick out the block that is unlikely to be used soon or the least unlikely to be used soon or the least unlikely to be used soon or the least likely to be a sone but we want to do likely to be a sone but we want to do likely to be a sone but we want to do less activity on cache hits to do that

## NMRU Replacement： Part 4
Kind: captions Language: en so now we will look at one of the so now we will look at one of the so now we will look at one of the so-called lru approximation algorithms so-called lru approximation algorithms so-called lru approximation algorithms which this one is called NMU or not most which this one is called NMU or not most which this one is called NMU or not most recently used and it's trying to recently used and it's trying to recently used and it's trying to approximate the performance of lru but approximate the performance of lru but approximate the performance of lru but with less activity on hits so again NMU with less activity on hits so again NMU with less activity on hits so again NMU stands for not most recently used the stands for not most recently used the stands for not most recently used the way it would work is that we would track way it would work is that we would track way it would work is that we would track which one block in the set is the most which one block in the set is the most which one block in the set is the most recently used one at any given time and recently used one at any given time and recently used one at any given time and then when we need to replace something then when we need to replace something then when we need to replace something we pick a block that is not the block we pick a block that is not the block we pick a block that is not the block that is the most recently used so for that is the most recently used so for that is the most recently used so for example we can pick randomly among the example we can pick randomly among the example we can pick randomly among the other blocks but we save the most other blocks but we save the most other blocks but we save the most recently used block from eviction from recently used block from eviction from recently used block from eviction from the cache so how do we track which block the cache so how do we track which block the cache so how do we track which block is mru let's say we have an nway set is mru let's say we have an nway set is mru let's say we have an nway set asstive cache and we want to track the asstive cache and we want to track the asstive cache and we want to track the mru block for each set we need only one mru block for each set we need only one mru block for each set we need only one mru pointer per set we only need to know mru pointer per set we only need to know mru pointer per set we only need to know which block has been the most recently which block has been the most recently which block has been the most recently accessed one in the set so for example accessed one in the set so for example accessed one in the set so for example if we have a two-way set asstive cache if we have a two-way set asstive cache if we have a two-way set asstive cache we only need one bit to tell us which of we only need one bit to tell us which of we only need one bit to tell us which of the two is the most recently accessed if the two is the most recently accessed if the two is the most recently accessed if we have a four-way set asstive cache we we have a four-way set asstive cache we we have a four-way set asstive cache we will have a 2bit pointer that tells us will have a 2bit pointer that tells us will have a 2bit pointer that tells us which of the four has been the most which of the four has been the most which of the four has been the most recently accessed and so on compare this recently accessed and so on compare this recently accessed and so on compare this to n lru counters that we need per set to n lru counters that we need per set to n lru counters that we need per set in a four-way set assertive cache we in a four-way set assertive cache we in a four-way set assertive cache we would need need a 2bit pointer for would need need a 2bit pointer for would need need a 2bit pointer for NMU we would need four 2bit lru counters NMU we would need four 2bit lru counters NMU we would need four 2bit lru counters to maintain lru so this policy keeps end to maintain lru so this policy keeps end to maintain lru so this policy keeps end times less state for an N wasted asstive times less state for an N wasted asstive times less state for an N wasted asstive cash than the true lru policy it turns cash than the true lru policy it turns cash than the true lru policy it turns out that this policy Works reasonably out that this policy Works reasonably out that this policy Works reasonably well it does have a heat rate that is well it does have a heat rate that is well it does have a heat rate that is slightly lower than a true lru but if slightly lower than a true lru but if slightly lower than a true lru but if this is what it takes to make our cash this is what it takes to make our cash this is what it takes to make our cash go from two cycles hit time to one cycle go from two cycles hit time to one cycle go from two cycles hit time to one cycle hit time it might be worth it a hit time it might be worth it a hit time it might be worth it a disadvantage of this policy is that disadvantage of this policy is that disadvantage of this policy is that although it prevents the most recently although it prevents the most recently although it prevents the most recently accessed line from being evicted it accessed line from being evicted it accessed line from being evicted it doesn't know what the order is among the doesn't know what the order is among the doesn't know what the order is among the rest of them so there is a truly least rest of them so there is a truly least rest of them so there is a truly least recently used block among these three recently used block among these three recently used block among these three for example but we don't know which one for example but we don't know which one for example but we don't know which one so we'll pick one of these three and so we'll pick one of these three and so we'll pick one of these three and might select something that is not the might select something that is not the might select something that is not the least recently used so although this least recently used so although this least recently used so although this works well because the most frequently works well because the most frequently works well because the most frequently accessed thing will be saved in the cash accessed thing will be saved in the cash accessed thing will be saved in the cash it is slightly weaker than through lru it is slightly weaker than through lru it is slightly weaker than through lru because of this because it doesn't know because of this because it doesn't know because of this because it doesn't know which one is the really least recently which one is the really least recently which one is the really least recently used so what we might want to have is a used so what we might want to have is a used so what we might want to have is a policy that is still simpler than policy that is still simpler than policy that is still simpler than lru but keeps more track about what's lru but keeps more track about what's lru but keeps more track about what's less recently used than the mru

## PLRU Replacement： Part 4
Kind: captions Language: en and one policy that tries to more and one policy that tries to more and one policy that tries to more closely approximate LRU is the so called closely approximate LRU is the so called closely approximate LRU is the so called pseudo LRU policy so it's really trying pseudo LRU policy so it's really trying pseudo LRU policy so it's really trying to approximate LRU but sometimes it to approximate LRU but sometimes it to approximate LRU but sometimes it doesn't exactly match it so let's look doesn't exactly match it so let's look doesn't exactly match it so let's look at an 8 way set associative cache and at an 8 way set associative cache and at an 8 way set associative cache and with the su bel-air your policy what we with the su bel-air your policy what we with the su bel-air your policy what we keep is one bit per line in the set so keep is one bit per line in the set so keep is one bit per line in the set so in an 8 way set associative cache we in an 8 way set associative cache we in an 8 way set associative cache we will have a 1 bit associated with each will have a 1 bit associated with each will have a 1 bit associated with each line as opposed to three bits that we line as opposed to three bits that we line as opposed to three bits that we would need for LRU counters so how does would need for LRU counters so how does would need for LRU counters so how does it allow you work all of the bits start it allow you work all of the bits start it allow you work all of the bits start out at zero and what we will do is every out at zero and what we will do is every out at zero and what we will do is every time a line is accessed we will set its time a line is accessed we will set its time a line is accessed we will set its bit to one so for example this one bit to one so for example this one bit to one so for example this one becomes one this one becomes one then becomes one this one becomes one then becomes one this one becomes one then this one becomes one as long as there is this one becomes one as long as there is this one becomes one as long as there is at least one zero bit here we keep doing at least one zero bit here we keep doing at least one zero bit here we keep doing this and if you ever need to replace this and if you ever need to replace this and if you ever need to replace something we pick among the blocks whose something we pick among the blocks whose something we pick among the blocks whose bits are zero so all the recently bits are zero so all the recently bits are zero so all the recently accessed blocks will have the bits at accessed blocks will have the bits at accessed blocks will have the bits at one and thus be saved from replacement one and thus be saved from replacement one and thus be saved from replacement eventually however if we have a lot of eventually however if we have a lot of eventually however if we have a lot of hits so for example in this particular hits so for example in this particular hits so for example in this particular state we will pick the least recently state we will pick the least recently state we will pick the least recently used block because it's the only one used block because it's the only one used block because it's the only one that has a zero bit that has a zero bit that has a zero bit eventually however all of the bits will eventually however all of the bits will eventually however all of the bits will be set so eventually we will have this be set so eventually we will have this be set so eventually we will have this become one and now we cannot replace become one and now we cannot replace become one and now we cannot replace anything so we detect this situation anything so we detect this situation anything so we detect this situation where all the bits have become one and where all the bits have become one and where all the bits have become one and when we set the last zero bit to one we when we set the last zero bit to one we when we set the last zero bit to one we zero out the remaining bits so now we zero out the remaining bits so now we zero out the remaining bits so now we have state that is similar to what not have state that is similar to what not have state that is similar to what not most recently used would tell us we only most recently used would tell us we only most recently used would tell us we only know what was the most recently used know what was the most recently used know what was the most recently used block for the rest of them we need to block for the rest of them we need to block for the rest of them we need to pick at random if something is to be pick at random if something is to be pick at random if something is to be replaced but as we start accessing some replaced but as we start accessing some replaced but as we start accessing some other blocks we get a better idea of other blocks we get a better idea of other blocks we get a better idea of which ones are more and which ones are which ones are more and which ones are which ones are more and which ones are less recently used so at any given point less recently used so at any given point less recently used so at any given point in time this pseudo LRU policy is in time this pseudo LRU policy is in time this pseudo LRU policy is somewhere between the NMR you and the somewhere between the NMR you and the somewhere between the NMR you and the LRU policy we have seen that when one LRU policy we have seen that when one LRU policy we have seen that when one bit is said we are effectively bit is said we are effectively bit is said we are effectively implementing the NMR policy when more implementing the NMR policy when more implementing the NMR policy when more than one bit is set than one bit is set than one bit is set not almost all of them we are in between not almost all of them we are in between not almost all of them we are in between and when all but one are set we are and when all but one are set we are and when all but one are set we are implementing the true LRU policy because implementing the true LRU policy because implementing the true LRU policy because we know exactly which one block is the we know exactly which one block is the we know exactly which one block is the least recently used so this policy least recently used so this policy least recently used so this policy behaves somewhere in between LRU and behaves somewhere in between LRU and behaves somewhere in between LRU and anna-marie Oh in exchange for using more anna-marie Oh in exchange for using more anna-marie Oh in exchange for using more bits but it's also very simple because bits but it's also very simple because bits but it's also very simple because on a hit all we have to do is set the on a hit all we have to do is set the on a hit all we have to do is set the bit that corresponds to the block which bit that corresponds to the block which bit that corresponds to the block which can be done very quickly and efficiently can be done very quickly and efficiently can be done very quickly and efficiently so as far as activity on a cache head is so as far as activity on a cache head is so as far as activity on a cache head is concerned both pseudo LRU and NMR you concerned both pseudo LRU and NMR you concerned both pseudo LRU and NMR you have much less activity than LRU which have much less activity than LRU which have much less activity than LRU which helps us with both power and also hit helps us with both power and also hit helps us with both power and also hit time of the cache

## NMRU Quiz： Part 4
Kind: captions Language: en so now that we have seen two policies so now that we have seen two policies so now that we have seen two policies that try to approximate LRU replacement that try to approximate LRU replacement that try to approximate LRU replacement let's see if we understood how NMR you let's see if we understood how NMR you let's see if we understood how NMR you works suppose we have a fully works suppose we have a fully works suppose we have a fully associative cache with only four lines associative cache with only four lines associative cache with only four lines and it's using NMR you replacement and it's using NMR you replacement and it's using NMR you replacement starts out empty none of the lines starts out empty none of the lines starts out empty none of the lines contain any blocks and suppose that the contain any blocks and suppose that the contain any blocks and suppose that the processor accesses blocks in the processor accesses blocks in the processor accesses blocks in the following order it accesses something in following order it accesses something in following order it accesses something in block a then again something in block a block a then again something in block a block a then again something in block a then something in block B then something then something in block B then something then something in block B then something in block a then something in Block C in block a then something in Block C in block a then something in Block C then something in block a then something then something in block a then something then something in block a then something in block D then something in block a in block D then something in block a in block D then something in block a then something in block e ay ay ay ay then something in block e ay ay ay ay then something in block e ay ay ay ay and finally something in block B the and finally something in block B the and finally something in block B the question for you what's the smallest question for you what's the smallest question for you what's the smallest number of cache misses we will have on number of cache misses we will have on number of cache misses we will have on the sequence of accesses and what's the the sequence of accesses and what's the the sequence of accesses and what's the largest number of cache misses that we largest number of cache misses that we largest number of cache misses that we might have on these accesses

## NMRU Quiz Quiz Solution： Part 4
Kind: captions Language: en let's look at the solution to our not let's look at the solution to our not let's look at the solution to our not most recently used replacement policy most recently used replacement policy most recently used replacement policy quiz quiz quiz we have these accesses and now let's see we have these accesses and now let's see we have these accesses and now let's see which ones might be hits which ones might be hits which ones might be hits and which ones might be misses and which ones might be misses and which ones might be misses this one is definitely a miss because we this one is definitely a miss because we this one is definitely a miss because we have nothing in the cache so now we have have nothing in the cache so now we have have nothing in the cache so now we have brought a into the cache brought a into the cache brought a into the cache this one is definitely a hit this one is definitely a hit this one is definitely a hit now we're gonna access b now we're gonna access b now we're gonna access b and this is definitely a miss and we're and this is definitely a miss and we're and this is definitely a miss and we're gonna put b in a separate place in the gonna put b in a separate place in the gonna put b in a separate place in the cache from where we put a because a is cache from where we put a because a is cache from where we put a because a is the most recently used block so we will the most recently used block so we will the most recently used block so we will put b put b put b in one of the other three blocks and we in one of the other three blocks and we in one of the other three blocks and we definitely will put b definitely will put b definitely will put b in a line that doesn't contain a so we in a line that doesn't contain a so we in a line that doesn't contain a so we will not kick out a because a is the will not kick out a because a is the will not kick out a because a is the most recently used block most recently used block most recently used block we will choose one of the other we will choose one of the other we will choose one of the other three lines to evict to put b there so three lines to evict to put b there so three lines to evict to put b there so so far if this is our cache we have a so far if this is our cache we have a so far if this is our cache we have a and b there now we will access a and and b there now we will access a and and b there now we will access a and have a hit so c is definitely a miss and have a hit so c is definitely a miss and have a hit so c is definitely a miss and at that point a is the most recently at that point a is the most recently at that point a is the most recently used block and we will find one of these used block and we will find one of these used block and we will find one of these three to evict to put c there three to evict to put c there three to evict to put c there in this type of a situation it looks in this type of a situation it looks in this type of a situation it looks like b might actually be evicted but like b might actually be evicted but like b might actually be evicted but remember that we also have valid bits remember that we also have valid bits remember that we also have valid bits for all these blocks so when we have a for all these blocks so when we have a for all these blocks so when we have a replacement policy typically replacement policy typically replacement policy typically we will first fill all the lines in the we will first fill all the lines in the we will first fill all the lines in the cash set before we move on and cash set before we move on and cash set before we move on and start evicting something so we will put start evicting something so we will put start evicting something so we will put c into one of the empty slots and now c into one of the empty slots and now c into one of the empty slots and now that is the most recently accessed line that is the most recently accessed line that is the most recently accessed line a a a is now a hit and is now a hit and is now a hit and that is now the most recently accessed that is now the most recently accessed that is now the most recently accessed line line line now when we access d it's not in the now when we access d it's not in the now when we access d it's not in the cache cache cache so we will have a miss so we will have a miss so we will have a miss we will again put the in an unused place we will again put the in an unused place we will again put the in an unused place and that will now be the most recently and that will now be the most recently and that will now be the most recently used one then a gets accessed and we used one then a gets accessed and we used one then a gets accessed and we have a hit and at this point a is the have a hit and at this point a is the have a hit and at this point a is the most recently used block now we will most recently used block now we will most recently used block now we will have a miss on e because it's not in the have a miss on e because it's not in the have a miss on e because it's not in the cache and now the question is what will cache and now the question is what will cache and now the question is what will be evicted we will definitely not evict be evicted we will definitely not evict be evicted we will definitely not evict a a a so we know so we know so we know that the next four accesses to a will be that the next four accesses to a will be that the next four accesses to a will be all hits e will be replacing one of all hits e will be replacing one of all hits e will be replacing one of these three blocks these three blocks these three blocks so if we replace c or d then here we so if we replace c or d then here we so if we replace c or d then here we will have a hit if we however select b will have a hit if we however select b will have a hit if we however select b for replacement and put e there then b for replacement and put e there then b for replacement and put e there then b here will have a miss so we have a hit here will have a miss so we have a hit here will have a miss so we have a hit or a miss here in total we have one two or a miss here in total we have one two or a miss here in total we have one two three four five or six three four five or six three four five or six misses so this is the final answer to misses so this is the final answer to misses so this is the final answer to this quiz

## Reducing the Miss Rate： Part 4
Kind: captions Language: en now let's go back to our cache now let's go back to our cache now let's go back to our cache optimizations which are about reducing optimizations which are about reducing optimizations which are about reducing the Amat we have seen that Amat can be the Amat we have seen that Amat can be the Amat we have seen that Amat can be reduced by reducing the heat time and we reduced by reducing the heat time and we reduced by reducing the heat time and we have seen several have seen several have seen several techniques that do this the aat can also techniques that do this the aat can also techniques that do this the aat can also be reduced by reducing the M rate and we be reduced by reducing the M rate and we be reduced by reducing the M rate and we will now look at some techniques that will now look at some techniques that will now look at some techniques that try to do that so in our work on try to do that so in our work on try to do that so in our work on reducing the Miss rate we really need to reducing the Miss rate we really need to reducing the Miss rate we really need to consider what are the causes of Misses consider what are the causes of Misses consider what are the causes of Misses so what can cause misses and the causes so what can cause misses and the causes so what can cause misses and the causes of Misses are what we call the Tre C's of Misses are what we call the Tre C's of Misses are what we call the Tre C's you should remember this because very you should remember this because very you should remember this because very often we will refer to the trees and you often we will refer to the trees and you often we will refer to the trees and you need to know that the trees are need to know that the trees are need to know that the trees are compulsory misses capacity misses and compulsory misses capacity misses and compulsory misses capacity misses and conflict misses a compulsory Miss is a conflict misses a compulsory Miss is a conflict misses a compulsory Miss is a miss that occurs when a block is miss that occurs when a block is miss that occurs when a block is accessed for the first time it's called accessed for the first time it's called accessed for the first time it's called compulsory because you have to have this compulsory because you have to have this compulsory because you have to have this Miss so even if we have an INF inite Miss so even if we have an INF inite Miss so even if we have an INF inite cash that starts out empty we would cash that starts out empty we would cash that starts out empty we would still have the compulsory misses but we still have the compulsory misses but we still have the compulsory misses but we would not have any of the other two would not have any of the other two would not have any of the other two kinds so a compulsory Miss is a miss you kinds so a compulsory Miss is a miss you kinds so a compulsory Miss is a miss you have to have simply because for the have to have simply because for the have to have simply because for the first time you have to bring a block first time you have to bring a block first time you have to bring a block into the cash capacity misses are misses into the cash capacity misses are misses into the cash capacity misses are misses that we have on blocks that were in the that we have on blocks that were in the that we have on blocks that were in the cach but were evicted because we didn't cach but were evicted because we didn't cach but were evicted because we didn't have enough room in the cash because the have enough room in the cash because the have enough room in the cash because the cash is of a finite size so these are cash is of a finite size so these are cash is of a finite size so these are misses that we would not have in an misses that we would not have in an misses that we would not have in an infinite cash but capacity misses would infinite cash but capacity misses would infinite cash but capacity misses would be misses even in a fully associative be misses even in a fully associative be misses even in a fully associative cache of the given size so for example cache of the given size so for example cache of the given size so for example if we have an 8 kilobyte cache that is if we have an 8 kilobyte cache that is if we have an 8 kilobyte cache that is let's say direct map we have a capacity let's say direct map we have a capacity let's say direct map we have a capacity Miss if that block would not be found in Miss if that block would not be found in Miss if that block would not be found in the cach even if it was fully the cach even if it was fully the cach even if it was fully associative 8 kilobyte cache and finally associative 8 kilobyte cache and finally associative 8 kilobyte cache and finally conflict misses occur because of conflict misses occur because of conflict misses occur because of conflicts within a set so the block was conflicts within a set so the block was conflicts within a set so the block was evicted not because the cash didn't have evicted not because the cash didn't have evicted not because the cash didn't have enough space in it but because it had enough space in it but because it had enough space in it but because it had limited limited limited associativity so a conflict Miss is a associativity so a conflict Miss is a associativity so a conflict Miss is a miss that would not be a Miss if the miss that would not be a Miss if the miss that would not be a Miss if the cash was fully associative and of the cash was fully associative and of the cash was fully associative and of the same size so now some of the obvious same size so now some of the obvious same size so now some of the obvious techniques for reducing the Mis rate techniques for reducing the Mis rate techniques for reducing the Mis rate will Target some of these so for example will Target some of these so for example will Target some of these so for example a larger a larger a larger cache would help us reduce the number of cache would help us reduce the number of cache would help us reduce the number of capacity misses a larger associativity capacity misses a larger associativity capacity misses a larger associativity would help us reduce the conflict misses would help us reduce the conflict misses would help us reduce the conflict misses a better replacement policy would also a better replacement policy would also a better replacement policy would also help us with conflict misses but all of help us with conflict misses but all of help us with conflict misses but all of those techniques also affect the heat those techniques also affect the heat those techniques also affect the heat time so now let's look at some time so now let's look at some time so now let's look at some techniques that are trying to reduce the techniques that are trying to reduce the techniques that are trying to reduce the Miss rate without necessarily increasing Miss rate without necessarily increasing Miss rate without necessarily increasing the hit time in return

## Larger Cache Blocks： Part 4
Kind: captions Language: en so the first technique for reducing the so the first technique for reducing the so the first technique for reducing the Mis rate is using larger cach blocks Mis rate is using larger cach blocks Mis rate is using larger cach blocks they help the Mis rate because more they help the Mis rate because more they help the Mis rate because more words are brought in when we have a miss words are brought in when we have a miss words are brought in when we have a miss so subsequent axises that might access so subsequent axises that might access so subsequent axises that might access those additional words we brought in those additional words we brought in those additional words we brought in will not be will not be will not be misses and they would be misses if we misses and they would be misses if we misses and they would be misses if we had a smaller cach block so this does had a smaller cach block so this does had a smaller cach block so this does reduce the Mis rate but only when reduce the Mis rate but only when reduce the Mis rate but only when spatial locality is good but when spatial locality is good but when spatial locality is good but when spatial locality is poor the my rate spatial locality is poor the my rate spatial locality is poor the my rate will increase because now what happens will increase because now what happens will increase because now what happens is we bring in the word we need and is we bring in the word we need and is we bring in the word we need and along with it now we bring more words along with it now we bring more words along with it now we bring more words that we didn't need so that we didn't need so that we didn't need so effectively our cachee now looks like a effectively our cachee now looks like a effectively our cachee now looks like a lower capacity cach because it has more lower capacity cach because it has more lower capacity cach because it has more junk in it so we end up suffering more junk in it so we end up suffering more junk in it so we end up suffering more of those capacity of those capacity of those capacity misses so if we plot the Miss rate and misses so if we plot the Miss rate and misses so if we plot the Miss rate and how it changes as the block size changes how it changes as the block size changes how it changes as the block size changes for a small cach we might get something for a small cach we might get something for a small cach we might get something like this where the M rate drops as we like this where the M rate drops as we like this where the M rate drops as we increase the block size but then starts increase the block size but then starts increase the block size but then starts increasing as we exhaust the amount of increasing as we exhaust the amount of increasing as we exhaust the amount of spatial spatial spatial locality that the cach can locality that the cach can locality that the cach can support but then it starts to grow as we support but then it starts to grow as we support but then it starts to grow as we start to suffer from the Mis rate start to suffer from the Mis rate start to suffer from the Mis rate increase because the there is not enough increase because the there is not enough increase because the there is not enough spal locality for a 4 kilobyte cache spal locality for a 4 kilobyte cache spal locality for a 4 kilobyte cache this might happen for example with a this might happen for example with a this might happen for example with a block size of 64 interestingly however block size of 64 interestingly however block size of 64 interestingly however for larger caches we will start out with for larger caches we will start out with for larger caches we will start out with a lower Mis rate to begin with and it a lower Mis rate to begin with and it a lower Mis rate to begin with and it will continue to drop up to larger block will continue to drop up to larger block will continue to drop up to larger block sizes eventually it's going to start to sizes eventually it's going to start to sizes eventually it's going to start to increase but here we might be looking at increase but here we might be looking at increase but here we might be looking at for example 256 byte block size this for example 256 byte block size this for example 256 byte block size this effect has to do with how much junk are effect has to do with how much junk are effect has to do with how much junk are we putting in the cache and how much we putting in the cache and how much we putting in the cache and how much room there is in the cache so some room there is in the cache so some room there is in the cache so some blocks will have more locality than blocks will have more locality than blocks will have more locality than others and then in a small cache those others and then in a small cache those others and then in a small cache those that don't have enough spatial locality that don't have enough spatial locality that don't have enough spatial locality are creating some junk and those that do are creating some junk and those that do are creating some junk and those that do have a lot of spatial have a lot of spatial have a lot of spatial locality are being placed in the cash locality are being placed in the cash locality are being placed in the cash but there is not enough room for them but there is not enough room for them but there is not enough room for them because of all the junk when we have a because of all the junk when we have a because of all the junk when we have a larger cache for example 256 kiloby then larger cache for example 256 kiloby then larger cache for example 256 kiloby then what happens is those blocks that don't what happens is those blocks that don't what happens is those blocks that don't have enough locality put some junk in have enough locality put some junk in have enough locality put some junk in the cash but the cash is bigger so it the cash but the cash is bigger so it the cash but the cash is bigger so it can also contain those blocks that do can also contain those blocks that do can also contain those blocks that do have spatial locality so the cache have spatial locality so the cache have spatial locality so the cache tolerates a slightly bigger block size tolerates a slightly bigger block size tolerates a slightly bigger block size so overall we can reduce the Miss rate so overall we can reduce the Miss rate so overall we can reduce the Miss rate especially in a larger cache if we especially in a larger cache if we especially in a larger cache if we increase the block size



---

# Playlist 3 Module 5 Raw Transcript

## Miss Rate Quiz： Part 4
Kind: captions Language: en so now that we have seen that the so now that we have seen that the so now that we have seen that the increasing block size can result in increasing block size can result in increasing block size can result in reduction in a miss rate let's see if we reduction in a miss rate let's see if we reduction in a miss rate let's see if we understood what's going on so when we understood what's going on so when we understood what's going on so when we increase the block size and the locality increase the block size and the locality increase the block size and the locality is good so the miss rate goes down the is good so the miss rate goes down the is good so the miss rate goes down the question for you is which types of the question for you is which types of the question for you is which types of the mesas are reduced the compulsory misses mesas are reduced the compulsory misses mesas are reduced the compulsory misses the capacity misses or the conflict the capacity misses or the conflict the capacity misses or the conflict misses

## Miss Rate Quiz Quiz Solution： Part 4
Kind: captions Language: en Let's look at the answer to our quiz Let's look at the answer to our quiz Let's look at the answer to our quiz about the relationship between the block about the relationship between the block about the relationship between the block size and the miss rate. The question was size and the miss rate. The question was size and the miss rate. The question was which of the three C's are really which of the three C's are really which of the three C's are really reduced when the miss rate goes down? reduced when the miss rate goes down? reduced when the miss rate goes down? Obviously, some misses have been Obviously, some misses have been Obviously, some misses have been eliminated. The question is which type eliminated. The question is which type eliminated. The question is which type suffered the elimination? Let's first suffered the elimination? Let's first suffered the elimination? Let's first look at the compulsory misses. If we look at the compulsory misses. If we look at the compulsory misses. If we have a small block size, and this is our have a small block size, and this is our have a small block size, and this is our memory, we'll bring in this block and memory, we'll bring in this block and memory, we'll bring in this block and suffer a miss for the first time. We suffer a miss for the first time. We suffer a miss for the first time. We bring in another block and suffer a bring in another block and suffer a bring in another block and suffer a miss. Now, let's say we bring in this miss. Now, let's say we bring in this miss. Now, let's say we bring in this block and suffer a miss. block and suffer a miss. block and suffer a miss. If the cache line size was twice of what If the cache line size was twice of what If the cache line size was twice of what it is now, it is now, it is now, then the second one would not have been then the second one would not have been then the second one would not have been a miss because when we fetch the first a miss because when we fetch the first a miss because when we fetch the first one, one, one, the second one would have also been in the second one would have also been in the second one would have also been in the same cache block. So, as you can the same cache block. So, as you can the same cache block. So, as you can see, the increase in the block size, see, the increase in the block size, see, the increase in the block size, assuming there is enough locality, will assuming there is enough locality, will assuming there is enough locality, will result in a reduction in compulsory result in a reduction in compulsory result in a reduction in compulsory misses. Another way to look at this is misses. Another way to look at this is misses. Another way to look at this is to note that we get a compulsory miss to note that we get a compulsory miss to note that we get a compulsory miss when we access the block for the very when we access the block for the very when we access the block for the very first time. Well, if you have larger first time. Well, if you have larger first time. Well, if you have larger blocks, there are fewer blocks, so there blocks, there are fewer blocks, so there blocks, there are fewer blocks, so there are fewer blocks to access for the very are fewer blocks to access for the very are fewer blocks to access for the very first time, and that will guarantee a first time, and that will guarantee a first time, and that will guarantee a reduction in compulsory misses, assuming reduction in compulsory misses, assuming reduction in compulsory misses, assuming there is any spatial locality. Now, there is any spatial locality. Now, there is any spatial locality. Now, let's look at capacity misses. Let's say let's look at capacity misses. Let's say let's look at capacity misses. Let's say that the program is accessing a that the program is accessing a that the program is accessing a relatively large array that doesn't fit relatively large array that doesn't fit relatively large array that doesn't fit in the cache, and then in the cache, and then in the cache, and then we go back and access it again. On the we go back and access it again. On the we go back and access it again. On the second set of accesses, we're having second set of accesses, we're having second set of accesses, we're having capacity misses. capacity misses. capacity misses. The first set of accesses were The first set of accesses were The first set of accesses were compulsory misses because these blocks compulsory misses because these blocks compulsory misses because these blocks were never in the cache. Because they were never in the cache. Because they were never in the cache. Because they don't fit in the cache, by the time we don't fit in the cache, by the time we don't fit in the cache, by the time we get to the end of the array, get to the end of the array, get to the end of the array, the beginning of the array is kicked out the beginning of the array is kicked out the beginning of the array is kicked out of the cache, and now we're suffering of the cache, and now we're suffering of the cache, and now we're suffering capacity misses. These blocks would be capacity misses. These blocks would be capacity misses. These blocks would be in the infinite cache. in the infinite cache. in the infinite cache. They're not in the cache because it was They're not in the cache because it was They're not in the cache because it was not large enough. But how many misses we not large enough. But how many misses we not large enough. But how many misses we will have for this array depends on the will have for this array depends on the will have for this array depends on the block size. With smaller blocks, the block size. With smaller blocks, the block size. With smaller blocks, the same size array would be occupying more same size array would be occupying more same size array would be occupying more blocks. With larger blocks, we will have blocks. With larger blocks, we will have blocks. With larger blocks, we will have fewer blocks. The number of misses will fewer blocks. The number of misses will fewer blocks. The number of misses will be equal to the number of blocks. So, as be equal to the number of blocks. So, as be equal to the number of blocks. So, as you can see, if we increase the block you can see, if we increase the block you can see, if we increase the block size size size and our miss rate goes down because and our miss rate goes down because and our miss rate goes down because there is plenty of spatial locality, there is plenty of spatial locality, there is plenty of spatial locality, we are also reducing the number of we are also reducing the number of we are also reducing the number of capacity misses. And finally, if we look capacity misses. And finally, if we look capacity misses. And finally, if we look at the conflict misses, we can construct at the conflict misses, we can construct at the conflict misses, we can construct a similar example where two blocks are a similar example where two blocks are a similar example where two blocks are kicking each other out. And because each kicking each other out. And because each kicking each other out. And because each time we fetch a larger block, if the time we fetch a larger block, if the time we fetch a larger block, if the block size goes up, we will be having block size goes up, we will be having block size goes up, we will be having fewer conflict misses. So, the increase fewer conflict misses. So, the increase fewer conflict misses. So, the increase in block size in block size in block size can improve all types of misses. That can improve all types of misses. That can improve all types of misses. That is, it can reduce the number is, it can reduce the number is, it can reduce the number for all three types of misses. When we for all three types of misses. When we for all three types of misses. When we increase the block size, the compulsory increase the block size, the compulsory increase the block size, the compulsory misses are almost always going to go misses are almost always going to go misses are almost always going to go down. The capacity misses are going to down. The capacity misses are going to down. The capacity misses are going to go down as long as there is enough go down as long as there is enough go down as long as there is enough locality, and the conflict misses are locality, and the conflict misses are locality, and the conflict misses are likely to stay similar, but might go likely to stay similar, but might go likely to stay similar, but might go down.

## Prefetching： Part 4
Kind: captions Language: en another technique that uses a similar another technique that uses a similar another technique that uses a similar idea to what we did when we increased idea to what we did when we increased idea to what we did when we increased the block size to reduce the number of the block size to reduce the number of the block size to reduce the number of Misses is called prefetching the way Misses is called prefetching the way Misses is called prefetching the way prefetching works is that we guess which prefetching works is that we guess which prefetching works is that we guess which blocks will be accessed soon in the blocks will be accessed soon in the blocks will be accessed soon in the future before they're actually accessed future before they're actually accessed future before they're actually accessed and then we bring those blocks into the and then we bring those blocks into the and then we bring those blocks into the cache ahead of time before they actually cache ahead of time before they actually cache ahead of time before they actually will be accessed so with no prefetching will be accessed so with no prefetching will be accessed so with no prefetching if time goes to the right we might have if time goes to the right we might have if time goes to the right we might have an axis here let's say load Word a and an axis here let's say load Word a and an axis here let's say load Word a and then we check then we check then we check quickly whether it's in our cache and if quickly whether it's in our cache and if quickly whether it's in our cache and if it is not we will go to memory and spend it is not we will go to memory and spend it is not we will go to memory and spend a lot of time waiting for it until it a lot of time waiting for it until it a lot of time waiting for it until it comes back so here we can supply the comes back so here we can supply the comes back so here we can supply the data to the processor so this is what data to the processor so this is what data to the processor so this is what happens with no prefetching with happens with no prefetching with happens with no prefetching with prefetching assuming that the load would prefetching assuming that the load would prefetching assuming that the load would happen here sometime before the load we happen here sometime before the load we happen here sometime before the load we have to guess that a will be accessed have to guess that a will be accessed have to guess that a will be accessed and request it from memory now what was and request it from memory now what was and request it from memory now what was memory latency here will be here and at memory latency here will be here and at memory latency here will be here and at this point the block is in the cache so this point the block is in the cache so this point the block is in the cache so when we try to load it we have a hit so when we try to load it we have a hit so when we try to load it we have a hit so as you can see with prefetching we're as you can see with prefetching we're as you can see with prefetching we're guessing what will be accessed and start guessing what will be accessed and start guessing what will be accessed and start fetching it from memory into the cache fetching it from memory into the cache fetching it from memory into the cache if we guess correctly instead of a Miss if we guess correctly instead of a Miss if we guess correctly instead of a Miss we get a hit if we guess we get a hit if we guess we get a hit if we guess incorrectly we brought something into incorrectly we brought something into incorrectly we brought something into the cache that replaced something that the cache that replaced something that the cache that replaced something that might have been useful so with might have been useful so with might have been useful so with prefetching we have that good guesses prefetching we have that good guesses prefetching we have that good guesses will eliminate misses but bad guesses will eliminate misses but bad guesses will eliminate misses but bad guesses lead to what is called Cash pollution lead to what is called Cash pollution lead to what is called Cash pollution because we're bringing stuff that is because we're bringing stuff that is because we're bringing stuff that is useless into the cash if it doesn't get useless into the cash if it doesn't get useless into the cash if it doesn't get access that means that maybe it would access that means that maybe it would access that means that maybe it would never have been brought into the cash in never have been brought into the cash in never have been brought into the cash in the first place and because of cash the first place and because of cash the first place and because of cash pollution because we kick out something pollution because we kick out something pollution because we kick out something that might have been useful with useless that might have been useful with useless that might have been useful with useless data not data not data not only that we didn't eliminate the Miss only that we didn't eliminate the Miss only that we didn't eliminate the Miss here because we guessed wrongly we also here because we guessed wrongly we also here because we guessed wrongly we also might have created another Miss because might have created another Miss because might have created another Miss because we kicked out something that later might we kicked out something that later might we kicked out something that later might be accessed and that we don't like so be accessed and that we don't like so be accessed and that we don't like so overall prefetching is about trying to overall prefetching is about trying to overall prefetching is about trying to make good guesses so that we eliminate make good guesses so that we eliminate make good guesses so that we eliminate misses while trying to eliminate bad misses while trying to eliminate bad misses while trying to eliminate bad guesses because we don't want to create guesses because we don't want to create guesses because we don't want to create additional misses

## Prefetch Instructions： Part 4
Kind: captions Language: en so one way of implementing prefetching so one way of implementing prefetching so one way of implementing prefetching is to just add prefetch instructions to is to just add prefetch instructions to is to just add prefetch instructions to the instruction set and let the compiler the instruction set and let the compiler the instruction set and let the compiler or the programmer figure out when to or the programmer figure out when to or the programmer figure out when to request prefetches so let's say we have request prefetches so let's say we have request prefetches so let's say we have a program that adds up the elements of a a program that adds up the elements of a a program that adds up the elements of a large array into a sum this program will large array into a sum this program will large array into a sum this program will access these elements one at a time so access these elements one at a time so access these elements one at a time so it has a lot of spatial locality not it has a lot of spatial locality not it has a lot of spatial locality not much temporal locality and it will have much temporal locality and it will have much temporal locality and it will have a lot of misses with prefetching a lot of misses with prefetching a lot of misses with prefetching instructions this program will become instructions this program will become instructions this program will become something like this we still have the something like this we still have the something like this we still have the original program but in this loop in original program but in this loop in original program but in this loop in addition to accessing the appropriate addition to accessing the appropriate addition to accessing the appropriate element of the array in an iteration we element of the array in an iteration we element of the array in an iteration we will be prefetching a element that we will be prefetching a element that we will be prefetching a element that we will access in the future P this tier will access in the future P this tier will access in the future P this tier should be at least one so that we are should be at least one so that we are should be at least one so that we are pre fetching the next element but you pre fetching the next element but you pre fetching the next element but you might want to prefetch the next next or might want to prefetch the next next or might want to prefetch the next next or even more advanced elements so here one even more advanced elements so here one even more advanced elements so here one of the trickier questions is what should of the trickier questions is what should of the trickier questions is what should be the pedis how far in advance to be the pedis how far in advance to be the pedis how far in advance to prefetch if P deist is too small for prefetch if P deist is too small for prefetch if P deist is too small for example if it's only 1 we end up having example if it's only 1 we end up having example if it's only 1 we end up having something like this our access is here something like this our access is here something like this our access is here we issue our prefetch in the previous we issue our prefetch in the previous we issue our prefetch in the previous iteration for this element the memory iteration for this element the memory iteration for this element the memory latency might be this which means that latency might be this which means that latency might be this which means that when we do our access the data still when we do our access the data still when we do our access the data still hasn't arrived from memory we will still hasn't arrived from memory we will still hasn't arrived from memory we will still have to wait for it although we will have to wait for it although we will have to wait for it although we will have to wait for it less than if there have to wait for it less than if there have to wait for it less than if there was no prefetch so here we prefetch too was no prefetch so here we prefetch too was no prefetch so here we prefetch too late and the access happens before the late and the access happens before the late and the access happens before the data arrives from memory so we still data arrives from memory so we still data arrives from memory so we still have a miss although it's a slightly have a miss although it's a slightly have a miss although it's a slightly less expensive one in terms of how long less expensive one in terms of how long less expensive one in terms of how long we have to wait we have to wait we have to wait however if pedis is too large let's say however if pedis is too large let's say however if pedis is too large let's say that the access is here and we prefetch that the access is here and we prefetch that the access is here and we prefetch this element way ahead of time it this element way ahead of time it this element way ahead of time it arrives from memory here sits in the arrives from memory here sits in the arrives from memory here sits in the cache and because other elements are cache and because other elements are cache and because other elements are being accessed in this loop all the time being accessed in this loop all the time being accessed in this loop all the time this might be eventually kicked out and this might be eventually kicked out and this might be eventually kicked out and in this case we say that we have in this case we say that we have in this case we say that we have prefetch too early so as you can see it prefetch too early so as you can see it prefetch too early so as you can see it is not easy to guess how far in advance is not easy to guess how far in advance is not easy to guess how far in advance to prefetch furthermore if you are to prefetch furthermore if you are to prefetch furthermore if you are writing a program and changing it to do writing a program and changing it to do writing a program and changing it to do prefetching prefetching prefetching it might be that in one generation of it might be that in one generation of it might be that in one generation of hardware the correct pedis might be hardware the correct pedis might be hardware the correct pedis might be let's say 20 and then the processor for let's say 20 and then the processor for let's say 20 and then the processor for example becomes faster but the memory example becomes faster but the memory example becomes faster but the memory doesn't and now the processor does more doesn't and now the processor does more doesn't and now the processor does more iterations while the memory is iterations while the memory is iterations while the memory is responding so we need to increase the responding so we need to increase the responding so we need to increase the distance so basically kind of coding distance so basically kind of coding distance so basically kind of coding this pedis into the program becomes this pedis into the program becomes this pedis into the program becomes tricky because the correct value for it tricky because the correct value for it tricky because the correct value for it so it's not too small or too large so it's not too small or too large so it's not too small or too large changes as Hardware changes so prefetch changes as Hardware changes so prefetch changes as Hardware changes so prefetch instructions are very useful but they instructions are very useful but they instructions are very useful but they are not easy to get right

## Prefetch Instructions Quiz： Part 4
Kind: captions Language: en so let's see if we know how to add some so let's see if we know how to add some so let's see if we know how to add some prefetch instructions suppose we have a prefetch instructions suppose we have a prefetch instructions suppose we have a program like this where I goes from 0 to program like this where I goes from 0 to program like this where I goes from 0 to 1000 and J goes from 0 to 1000 and in 1000 and J goes from 0 to 1000 and in 1000 and J goes from 0 to 1000 and in every iteration of this inner loop we every iteration of this inner loop we every iteration of this inner loop we add to the ice element of the a array add to the ice element of the a array add to the ice element of the a array the element of the B matrix so what is the element of the B matrix so what is the element of the B matrix so what is really happening here is that in the really happening here is that in the really happening here is that in the first iteration of the outer loop we first iteration of the outer loop we first iteration of the outer loop we will compute a 0 then a 1 and so on and will compute a 0 then a 1 and so on and will compute a 0 then a 1 and so on and we do that by basically adding up all we do that by basically adding up all we do that by basically adding up all the appropriate elements from the B the appropriate elements from the B the appropriate elements from the B array let's say that the elements of the array let's say that the elements of the array let's say that the elements of the a and B arrays are both floating-point a and B arrays are both floating-point a and B arrays are both floating-point numbers that are each 8 bytes in size numbers that are each 8 bytes in size numbers that are each 8 bytes in size let's say that the cache size is 16 let's say that the cache size is 16 let's say that the cache size is 16 kilobytes fully associative with LRU kilobytes fully associative with LRU kilobytes fully associative with LRU replacement let's say that a single replacement let's say that a single replacement let's say that a single iteration of this inner loop takes 10 iteration of this inner loop takes 10 iteration of this inner loop takes 10 cycles if there are no misses and that cycles if there are no misses and that cycles if there are no misses and that the miss penalty is 200 cycles because the miss penalty is 200 cycles because the miss penalty is 200 cycles because the memory latency is 200 cycles the the memory latency is 200 cycles the the memory latency is 200 cycles the question for you is if we insert a question for you is if we insert a question for you is if we insert a prefetch instruction here so that we can prefetch instruction here so that we can prefetch instruction here so that we can prefetch one of the next a elements what prefetch one of the next a elements what prefetch one of the next a elements what should be the distance here or check should be the distance here or check should be the distance here or check this if we simply should not be doing this if we simply should not be doing this if we simply should not be doing this here for prefetching elements of B this here for prefetching elements of B this here for prefetching elements of B in this loop what is the distance at in this loop what is the distance at in this loop what is the distance at which we should be prefetching or should which we should be prefetching or should which we should be prefetching or should we just not be doing this

## Prefetch Instructions Quiz Quiz Solution： Part 4
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our prefetch instructions squiz let's look prefetch instructions squiz let's look prefetch instructions squiz let's look first at this Inner Loop and the first at this Inner Loop and the first at this Inner Loop and the prefetch distance for fetching elements prefetch distance for fetching elements prefetch distance for fetching elements of the B array we were told that each of the B array we were told that each of the B array we were told that each iteration takes two 10 Cycles if there iteration takes two 10 Cycles if there iteration takes two 10 Cycles if there are no misses and we want to eliminate are no misses and we want to eliminate are no misses and we want to eliminate misses so if we do prefetching correctly misses so if we do prefetching correctly misses so if we do prefetching correctly there will be 10 cycles per iteration there will be 10 cycles per iteration there will be 10 cycles per iteration here the Miss penalty and the memory here the Miss penalty and the memory here the Miss penalty and the memory latency are 200 Cycles which means that latency are 200 Cycles which means that latency are 200 Cycles which means that we should be ising a prefetch for a b we should be ising a prefetch for a b we should be ising a prefetch for a b element and 200 Cycles before we reach element and 200 Cycles before we reach element and 200 Cycles before we reach it it it corresponds to 20 iterations so we will corresponds to 20 iterations so we will corresponds to 20 iterations so we will prefetch 20 iterations ahead of time and prefetch 20 iterations ahead of time and prefetch 20 iterations ahead of time and we will do it because if we do it this we will do it because if we do it this we will do it because if we do it this way the prefetch will arrive just in way the prefetch will arrive just in way the prefetch will arrive just in time for B to get its value now we need time for B to get its value now we need time for B to get its value now we need to figure out what to prefetch here and to figure out what to prefetch here and to figure out what to prefetch here and whether we should be doing the whether we should be doing the whether we should be doing the prefetching we should not be doing the prefetching we should not be doing the prefetching we should not be doing the prefetching if by the time we we reach prefetching if by the time we we reach prefetching if by the time we we reach the axis to that element of a know that the axis to that element of a know that the axis to that element of a know that this should be I + 1 or something larger this should be I + 1 or something larger this should be I + 1 or something larger this eliminates that element from the this eliminates that element from the this eliminates that element from the cache so the question is how much cache cache so the question is how much cache cache so the question is how much cache stuff does each iteration of this outer stuff does each iteration of this outer stuff does each iteration of this outer loop bring in and the answer is it loop bring in and the answer is it loop bring in and the answer is it brings 1,00 8 by elements here and an brings 1,00 8 by elements here and an brings 1,00 8 by elements here and an element of a so if we prefetch with a element of a so if we prefetch with a element of a so if we prefetch with a distance of one what we get is that our distance of one what we get is that our distance of one what we get is that our pre prefetch is done after 200 Cycles pre prefetch is done after 200 Cycles pre prefetch is done after 200 Cycles there are 10,000 Cycles here so it's in there are 10,000 Cycles here so it's in there are 10,000 Cycles here so it's in time to be accessed in the next time to be accessed in the next time to be accessed in the next iteration of the outer loop the question iteration of the outer loop the question iteration of the outer loop the question is just does this element get kicked out is just does this element get kicked out is just does this element get kicked out before we can use it and the answer is before we can use it and the answer is before we can use it and the answer is no because 8 times this 1,000 ends up no because 8 times this 1,000 ends up no because 8 times this 1,000 ends up being 8,000 the cache is 16 kiloby so being 8,000 the cache is 16 kiloby so being 8,000 the cache is 16 kiloby so basically if we prefet here the element basically if we prefet here the element basically if we prefet here the element of the AR array that we prefet will of the AR array that we prefet will of the AR array that we prefet will still be there when we eventually get to still be there when we eventually get to still be there when we eventually get to the next iteration of the outer loop the next iteration of the outer loop the next iteration of the outer loop where we get to use it so the answer is where we get to use it so the answer is where we get to use it so the answer is we should prefetch with I + 1 and we we should prefetch with I + 1 and we we should prefetch with I + 1 and we should not be selecting this note that should not be selecting this note that should not be selecting this note that we should not be prefetching with I + 20 we should not be prefetching with I + 20 we should not be prefetching with I + 20 here if we do that there are 20 Loops in here if we do that there are 20 Loops in here if we do that there are 20 Loops in between when we prefetch and when we between when we prefetch and when we between when we prefetch and when we actually use it and each of these Loops actually use it and each of these Loops actually use it and each of these Loops is bringing in 8,000 bytes worth of B so is bringing in 8,000 bytes worth of B so is bringing in 8,000 bytes worth of B so if we prefetch that early if we prefetch that early if we prefetch that early then 20 * 8 is larger than our cash and then 20 * 8 is larger than our cash and then 20 * 8 is larger than our cash and the element we prefetch would be kicked the element we prefetch would be kicked the element we prefetch would be kicked out from the cash so that would be an out from the cash so that would be an out from the cash so that would be an example of premature prefetching

## Hardware Prefetching： Part 4
Kind: captions Language: en another example of prefetching would be another example of prefetching would be another example of prefetching would be the so-called Hardware prefetching where the so-called Hardware prefetching where the so-called Hardware prefetching where the program doesn't change and the the program doesn't change and the the program doesn't change and the hardware itself either the processor or hardware itself either the processor or hardware itself either the processor or the cache is trying to guess what will the cache is trying to guess what will the cache is trying to guess what will be accessed soon there are a number of be accessed soon there are a number of be accessed soon there are a number of Hardware preatures that work reasonably Hardware preatures that work reasonably Hardware preatures that work reasonably well for example many current processors well for example many current processors well for example many current processors include what's called a stream buffer include what's called a stream buffer include what's called a stream buffer prefecture other popular types of prefecture other popular types of prefecture other popular types of prefectures include a stride prefecture prefectures include a stride prefecture prefectures include a stride prefecture and what is called a correlating and what is called a correlating and what is called a correlating prefecture all of these try to guess prefecture all of these try to guess prefecture all of these try to guess what is going to be accessed next based what is going to be accessed next based what is going to be accessed next based on some sort of locality property so a on some sort of locality property so a on some sort of locality property so a stream buffer is sequential in nature stream buffer is sequential in nature stream buffer is sequential in nature it's trying to see whether once we it's trying to see whether once we it's trying to see whether once we access a block and then the next block access a block and then the next block access a block and then the next block that follows it we are likely to access that follows it we are likely to access that follows it we are likely to access the next next block and so on so it's the next next block and so on so it's the next next block and so on so it's just trying to fetch several blocks in just trying to fetch several blocks in just trying to fetch several blocks in advance in order to not be too late a advance in order to not be too late a advance in order to not be too late a stride based prefecture is monitoring stride based prefecture is monitoring stride based prefecture is monitoring accesses to see if memory accesses that accesses to see if memory accesses that accesses to see if memory accesses that we doing are at a fixed distance from we doing are at a fixed distance from we doing are at a fixed distance from each other so if their addresses differ each other so if their addresses differ each other so if their addresses differ by the same amount it's going to guess by the same amount it's going to guess by the same amount it's going to guess that we will just continue doing that that we will just continue doing that that we will just continue doing that and it starts prefetching with a and it starts prefetching with a and it starts prefetching with a distance of D * something and it distance of D * something and it distance of D * something and it determines how many of these in advance determines how many of these in advance determines how many of these in advance such that it's just in time a such that it's just in time a such that it's just in time a correlating prefecture is trying to correlating prefecture is trying to correlating prefecture is trying to predict sequences of axises that are not predict sequences of axises that are not predict sequences of axises that are not sequential or at the same distance from sequential or at the same distance from sequential or at the same distance from each other the way it does this is when each other the way it does this is when each other the way it does this is when we see a and then B we see a and then B we see a and then B accessed it remembers in some sort of a accessed it remembers in some sort of a accessed it remembers in some sort of a table that when a comes then B comes table that when a comes then B comes table that when a comes then B comes then when C comes it remembers that then when C comes it remembers that then when C comes it remembers that after B comes C and so on and then later after B comes C and so on and then later after B comes C and so on and then later on if we see a it's going to look here on if we see a it's going to look here on if we see a it's going to look here and issue a prefet for B if we do access and issue a prefet for B if we do access and issue a prefet for B if we do access B it's going to look here and issue a B it's going to look here and issue a B it's going to look here and issue a prefetch for C and so on so it can fetch prefetch for C and so on so it can fetch prefetch for C and so on so it can fetch an arbitrary sequence of accesses as an arbitrary sequence of accesses as an arbitrary sequence of accesses as long as that sequence repeats so it's long as that sequence repeats so it's long as that sequence repeats so it's very good for example for link list very good for example for link list very good for example for link list which are not necessarily sequential in which are not necessarily sequential in which are not necessarily sequential in memory and they're not at fixed memory and they're not at fixed memory and they're not at fixed sides but if you access the first node sides but if you access the first node sides but if you access the first node and the second node at the turn node and and the second node at the turn node and and the second node at the turn node and then you do that again it's going to then you do that again it's going to then you do that again it's going to prefetch correctly

## Loop Interchange： Part 4
Kind: captions Language: en the final type of optimization for the final type of optimization for the final type of optimization for reducing the Miss rate that we will look reducing the Miss rate that we will look reducing the Miss rate that we will look at is a so-called loop interchange which at is a so-called loop interchange which at is a so-called loop interchange which is one of the compiler optimizations is one of the compiler optimizations is one of the compiler optimizations that can be done to transform the code that can be done to transform the code that can be done to transform the code into code that has better locality so into code that has better locality so into code that has better locality so suppose that we are initializing a suppose that we are initializing a suppose that we are initializing a matrix like this by having an outer loop matrix like this by having an outer loop matrix like this by having an outer loop and an inner loop and then we initialize and an inner loop and then we initialize and an inner loop and then we initialize the element and then loop like this the the element and then loop like this the the element and then loop like this the layout of this matrix in memory if it's layout of this matrix in memory if it's layout of this matrix in memory if it's a CRA is such that it begins with a zero a CRA is such that it begins with a zero a CRA is such that it begins with a zero zero the next element in memory will be zero the next element in memory will be zero the next element in memory will be a zero one and so on a zero one and so on a zero one and so on eventually we have the last element of eventually we have the last element of eventually we have the last element of this row and only now we will have a one this row and only now we will have a one this row and only now we will have a one zero and so on so now let's look at how zero and so on so now let's look at how zero and so on so now let's look at how this loop accesses these elements I and this loop accesses these elements I and this loop accesses these elements I and J are both zero at first so we will J are both zero at first so we will J are both zero at first so we will access this element next J will be access this element next J will be access this element next J will be incremented so we will access this incremented so we will access this incremented so we will access this element and then this element and so on element and then this element and so on element and then this element and so on we will access all of the rows of the we will access all of the rows of the we will access all of the rows of the matrix before we come back and start matrix before we come back and start matrix before we come back and start accessing the second element in each row accessing the second element in each row accessing the second element in each row so the problem is when this is accessed so the problem is when this is accessed so the problem is when this is accessed we fetched an entire cache block worth we fetched an entire cache block worth we fetched an entire cache block worth of stuff into the cache just to use this of stuff into the cache just to use this of stuff into the cache just to use this one element then we're going to fetch a one element then we're going to fetch a one element then we're going to fetch a block here to use this one element and block here to use this one element and block here to use this one element and by the time we reach the end of the by the time we reach the end of the by the time we reach the end of the matrix and look back here we might have matrix and look back here we might have matrix and look back here we might have run out of space in the cache and the run out of space in the cache and the run out of space in the cache and the block that we would now use has been block that we would now use has been block that we would now use has been kicked out of the cache so we fetch it kicked out of the cache so we fetch it kicked out of the cache so we fetch it again use this one element and so on so again use this one element and so on so again use this one element and so on so we have one miss per access here so a we have one miss per access here so a we have one miss per access here so a good compiler will detect that the order good compiler will detect that the order good compiler will detect that the order of these loops doesn't match the layout of these loops doesn't match the layout of these loops doesn't match the layout in memory and it's going to perform what in memory and it's going to perform what in memory and it's going to perform what is called a loop interchange which is called a loop interchange which is called a loop interchange which amounts to simply swapping the two loops amounts to simply swapping the two loops amounts to simply swapping the two loops so now the inner loop is the one that so now the inner loop is the one that so now the inner loop is the one that moves to the next element in memory so moves to the next element in memory so moves to the next element in memory so once we fetch a block from memory we end once we fetch a block from memory we end once we fetch a block from memory we end up using the whole block only then we up using the whole block only then we up using the whole block only then we move to the next block and so on once we move to the next block and so on once we move to the next block and so on once we have done with the row we move on to the have done with the row we move on to the have done with the row we move on to the next row so now we are nicely next row so now we are nicely next row so now we are nicely sequentially accessing this may sequentially accessing this may sequentially accessing this may not only does it improve locality not only does it improve locality not only does it improve locality because now we end up accessing the because now we end up accessing the because now we end up accessing the entire cache block at a time but also it entire cache block at a time but also it entire cache block at a time but also it makes the access nicely sequential so makes the access nicely sequential so makes the access nicely sequential so one of the pre fetchers can do a good one of the pre fetchers can do a good one of the pre fetchers can do a good job on it so this works really well it job on it so this works really well it job on it so this works really well it dramatically improves the hit rate that dramatically improves the hit rate that dramatically improves the hit rate that we're getting but it is not always we're getting but it is not always we're getting but it is not always possible you cannot just take any two possible you cannot just take any two possible you cannot just take any two nested loops and transform in this way nested loops and transform in this way nested loops and transform in this way the compiler has to prove that this code the compiler has to prove that this code the compiler has to prove that this code and this code are equivalent which it and this code are equivalent which it and this code are equivalent which it does by proving that there are no does by proving that there are no does by proving that there are no dependencies between iterations of these dependencies between iterations of these dependencies between iterations of these loops

## Overlap Misses： Part 4
Kind: captions Language: en so going back so going back so going back to our choices for reducing the amat we to our choices for reducing the amat we to our choices for reducing the amat we have seen that we can reduce the hit have seen that we can reduce the hit have seen that we can reduce the hit time we have now seen that there are time we have now seen that there are time we have now seen that there are techniques that reduce the miss rate and techniques that reduce the miss rate and techniques that reduce the miss rate and the final set of techniques are those the final set of techniques are those the final set of techniques are those that reduce the miss penalty so when we that reduce the miss penalty so when we that reduce the miss penalty so when we miss we don't suffer as much as before miss we don't suffer as much as before miss we don't suffer as much as before so the first technique for reducing the so the first technique for reducing the so the first technique for reducing the miss penalty is to overlap multiple miss penalty is to overlap multiple miss penalty is to overlap multiple misses if time goes in this direction misses if time goes in this direction misses if time goes in this direction our processor does a lot of activity our processor does a lot of activity our processor does a lot of activity multiple instructions per cycle and at multiple instructions per cycle and at multiple instructions per cycle and at some point some point some point it does a load for example and now that it does a load for example and now that it does a load for example and now that load tries to be found in the cache load tries to be found in the cache load tries to be found in the cache and if not we're going to go to memory and if not we're going to go to memory and if not we're going to go to memory and it eventually comes back now if you and it eventually comes back now if you and it eventually comes back now if you have a fancy out of order processor it have a fancy out of order processor it have a fancy out of order processor it doesn't stop here and wait what it does doesn't stop here and wait what it does doesn't stop here and wait what it does is it finds other instructions to do is it finds other instructions to do is it finds other instructions to do so so so even after even after even after we start fetching the data from memory we start fetching the data from memory we start fetching the data from memory the processor is continuing but the processor is continuing but the processor is continuing but eventually it starts running out of eventually it starts running out of eventually it starts running out of things to do and probably before the things to do and probably before the things to do and probably before the load comes back from memory the load comes back from memory the load comes back from memory the processor runs out of resources remember processor runs out of resources remember processor runs out of resources remember that it cannot commit this load so that it cannot commit this load so that it cannot commit this load so eventually for example it will fill up eventually for example it will fill up eventually for example it will fill up the rob or maybe even sooner than that the rob or maybe even sooner than that the rob or maybe even sooner than that it will run out of reservation stations it will run out of reservation stations it will run out of reservation stations if a lot of things depend on this load if a lot of things depend on this load if a lot of things depend on this load so some part of this miss latency is so some part of this miss latency is so some part of this miss latency is going to be directly added to the going to be directly added to the going to be directly added to the execution time but some part of it is execution time but some part of it is execution time but some part of it is actually overlapping with the processor actually overlapping with the processor actually overlapping with the processor activity during the time between trying activity during the time between trying activity during the time between trying to execute this load to execute this load to execute this load and running out of things to do and running out of things to do and running out of things to do this processor might actually issue this processor might actually issue this processor might actually issue another load that would be a cache miss another load that would be a cache miss another load that would be a cache miss so if we have what is called a blocking so if we have what is called a blocking so if we have what is called a blocking cache then this load cannot be done cache then this load cannot be done cache then this load cannot be done until the first load is until the first load is until the first load is finished and only at this point this finished and only at this point this finished and only at this point this load can really be tried in the cache we load can really be tried in the cache we load can really be tried in the cache we realize it's a miss we suffer the miss realize it's a miss we suffer the miss realize it's a miss we suffer the miss latency and meanwhile because the latency and meanwhile because the latency and meanwhile because the processor can commit these instructions processor can commit these instructions processor can commit these instructions here the processor can overlap some of here the processor can overlap some of here the processor can overlap some of this mislatency with some other activity this mislatency with some other activity this mislatency with some other activity we can also have a non-blocking cache we can also have a non-blocking cache we can also have a non-blocking cache and a non-blocking cache and a non-blocking cache and a non-blocking cache can support things like can support things like can support things like hit under a miss hit under a miss hit under a miss meaning meaning meaning while we are having a cache miss while we are having a cache miss while we are having a cache miss hits to other blocks in the cache that hits to other blocks in the cache that hits to other blocks in the cache that are sent by the processor will be are sent by the processor will be are sent by the processor will be serviced and returned to the processor serviced and returned to the processor serviced and returned to the processor with data and also we can have what is with data and also we can have what is with data and also we can have what is called a miss under a miss in which case called a miss under a miss in which case called a miss under a miss in which case while we are having a miss we can send while we are having a miss we can send while we are having a miss we can send another request to memory so let's look another request to memory so let's look another request to memory so let's look at what that looks like so our processor at what that looks like so our processor at what that looks like so our processor is happily chugging along it has this is happily chugging along it has this is happily chugging along it has this first load that suffers a miss we check first load that suffers a miss we check first load that suffers a miss we check in the cache we wait for it for to come in the cache we wait for it for to come in the cache we wait for it for to come back from memory we continue working and back from memory we continue working and back from memory we continue working and eventually run out of things to do eventually run out of things to do eventually run out of things to do because they depend on this first load because they depend on this first load because they depend on this first load but the idea is that now this load here but the idea is that now this load here but the idea is that now this load here that also suffers a miss will have its that also suffers a miss will have its that also suffers a miss will have its own check in the cache and when we own check in the cache and when we own check in the cache and when we realize it's a miss we will send it to realize it's a miss we will send it to realize it's a miss we will send it to memory so it will come back here now memory so it will come back here now memory so it will come back here now what we have is when the first load what we have is when the first load what we have is when the first load comes back from memory there is a burst comes back from memory there is a burst comes back from memory there is a burst of activity it starts dying out because of activity it starts dying out because of activity it starts dying out because we are still waiting for the second load we are still waiting for the second load we are still waiting for the second load but then the second load comes back and but then the second load comes back and but then the second load comes back and we are very quickly back to our normal we are very quickly back to our normal we are very quickly back to our normal operation so now as you can see operation so now as you can see operation so now as you can see the inactivity in the processor used to the inactivity in the processor used to the inactivity in the processor used to be this and this and now it's just this be this and this and now it's just this be this and this and now it's just this and maybe a small amount here so by and maybe a small amount here so by and maybe a small amount here so by overlapping overlapping overlapping the miss time of the two loads we have the miss time of the two loads we have the miss time of the two loads we have almost cut the penalty to on performance almost cut the penalty to on performance almost cut the penalty to on performance to half of what it was because before we to half of what it was because before we to half of what it was because before we had to wait twice had to wait twice had to wait twice this this this now we really wait now we really wait now we really wait once and maybe some little more if we once and maybe some little more if we once and maybe some little more if we managed to find three or four loads that managed to find three or four loads that managed to find three or four loads that overlapped overlapped overlapped then with a blocking cash we would pay then with a blocking cash we would pay then with a blocking cash we would pay the penalty three or four times the penalty three or four times the penalty three or four times here we might be paying here we might be paying here we might be paying one penalty plus a little bit more the one penalty plus a little bit more the one penalty plus a little bit more the property that the processor is property that the processor is property that the processor is exploiting here is called exploiting here is called exploiting here is called memory level parallelism memory level parallelism memory level parallelism here the memory never gets more than one here the memory never gets more than one here the memory never gets more than one access at a time access at a time access at a time here the memory gets accesses to process here the memory gets accesses to process here the memory gets accesses to process them in parallel so of course our memory them in parallel so of course our memory them in parallel so of course our memory needs to be able to do this but if it needs to be able to do this but if it needs to be able to do this but if it can then a non-blocking cache that can can then a non-blocking cache that can can then a non-blocking cache that can do miss under miss can dramatically cut do miss under miss can dramatically cut do miss under miss can dramatically cut down on the cost of misses instead of down on the cost of misses instead of down on the cost of misses instead of seeing the full miss latency being added seeing the full miss latency being added seeing the full miss latency being added to the memory access time we are really to the memory access time we are really to the memory access time we are really seeing that the penalties of the two seeing that the penalties of the two seeing that the penalties of the two misses overlap so really we pay the misses overlap so really we pay the misses overlap so really we pay the penalty once and we get one two penalty once and we get one two penalty once and we get one two 15 misses 15 misses 15 misses in exchange

## Miss Under Miss Support in Caches： Part 4
Kind: captions Language: en so what do we have to have in our cash so what do we have to have in our cash so what do we have to have in our cash in order to support a Miss under miss in order to support a Miss under miss in order to support a Miss under miss operation before the cash would simply operation before the cash would simply operation before the cash would simply block when it has a Miss and not do block when it has a Miss and not do block when it has a Miss and not do anything until the Miss comes so it anything until the Miss comes so it anything until the Miss comes so it doesn't really need much support for doesn't really need much support for doesn't really need much support for that with Miss under miss we have a Miss that with Miss under miss we have a Miss that with Miss under miss we have a Miss but accesses keep coming to the cash if but accesses keep coming to the cash if but accesses keep coming to the cash if they're hits we handle them normally by they're hits we handle them normally by they're hits we handle them normally by just finding those blocks and returning just finding those blocks and returning just finding those blocks and returning to the processor if they are mrs. we to the processor if they are mrs. we to the processor if they are mrs. we have to do more now now we need to have have to do more now now we need to have have to do more now now we need to have the so-called miss status handling the so-called miss status handling the so-called miss status handling registers or ms hrs what they do is they registers or ms hrs what they do is they registers or ms hrs what they do is they remember what we request it from memory remember what we request it from memory remember what we request it from memory so they keep information about the so they keep information about the so they keep information about the missus that we currently have in missus that we currently have in missus that we currently have in progress when we have a Miss we have to progress when we have a Miss we have to progress when we have a Miss we have to check the MS hrs to see if there is any check the MS hrs to see if there is any check the MS hrs to see if there is any match basically if you have a Miss we match basically if you have a Miss we match basically if you have a Miss we want to know whether it's a new Miss or want to know whether it's a new Miss or want to know whether it's a new Miss or something that we didn't find in the something that we didn't find in the something that we didn't find in the cash but it has already been ordered cash but it has already been ordered cash but it has already been ordered from memory because of a previous access from memory because of a previous access from memory because of a previous access that was a miss so if it's not a match that was a miss so if it's not a match that was a miss so if it's not a match that means it's a Miss to a different that means it's a Miss to a different that means it's a Miss to a different block in that case we allocate a new msh block in that case we allocate a new msh block in that case we allocate a new msh are in the msh are we remember which are in the msh are we remember which are in the msh are we remember which instruction the processor to wake up instruction the processor to wake up instruction the processor to wake up when the data comes back and that's it when the data comes back and that's it when the data comes back and that's it if there is a match that means that we if there is a match that means that we if there is a match that means that we try to find data from a block and that try to find data from a block and that try to find data from a block and that block had a previous miss that was block had a previous miss that was block had a previous miss that was already sent to memory but didn't come already sent to memory but didn't come already sent to memory but didn't come back yet so really if we did things one back yet so really if we did things one back yet so really if we did things one at a time this would not be amiss so we at a time this would not be amiss so we at a time this would not be amiss so we call this a Miss and this is sometimes call this a Miss and this is sometimes call this a Miss and this is sometimes called half miss because this is a true called half miss because this is a true called half miss because this is a true miss that we would have even if we did miss that we would have even if we did miss that we would have even if we did blocking caches this is a mess that blocking caches this is a mess that blocking caches this is a mess that would be a hit if we did blocking but would be a hit if we did blocking but would be a hit if we did blocking but because we allowed the processor to because we allowed the processor to because we allowed the processor to check the cash before the previous mrs. check the cash before the previous mrs. check the cash before the previous mrs. came back some of the new accesses are came back some of the new accesses are came back some of the new accesses are now not finding the data in the cache now not finding the data in the cache now not finding the data in the cache but the data is already on the way back but the data is already on the way back but the data is already on the way back to the processor now because the request to the processor now because the request to the processor now because the request for this block has already been sent to for this block has already been sent to for this block has already been sent to memory we shouldn't send it again memory we shouldn't send it again memory we shouldn't send it again instead we simply add that instruction instead we simply add that instruction instead we simply add that instruction to the MS HR and that's it when the data to the MS HR and that's it when the data to the MS HR and that's it when the data finally comes back from memory we find finally comes back from memory we find finally comes back from memory we find the MS a chart that corresponds to that the MS a chart that corresponds to that the MS a chart that corresponds to that request and we wake up all the request and we wake up all the request and we wake up all the structions that were added to this msh structions that were added to this msh structions that were added to this msh are the first one and all of the are the first one and all of the are the first one and all of the subsequent ones that also wanted the subsequent ones that also wanted the subsequent ones that also wanted the data keep in mind that the Miss and the data keep in mind that the Miss and the data keep in mind that the Miss and the half miss to the same block need not half miss to the same block need not half miss to the same block need not necessarily be to the same word actually necessarily be to the same word actually necessarily be to the same word actually it's quite common that for example you it's quite common that for example you it's quite common that for example you access the first word and then very soon access the first word and then very soon access the first word and then very soon reacts is the second word in a block the reacts is the second word in a block the reacts is the second word in a block the first word had a miss the second world first word had a miss the second world first word had a miss the second world will have a half miss and after waking will have a half miss and after waking will have a half miss and after waking up the instructions we release the msh up the instructions we release the msh up the instructions we release the msh are so it can be used by another mess so are so it can be used by another mess so are so it can be used by another mess so how many ms hrs do we want to have it how many ms hrs do we want to have it how many ms hrs do we want to have it turns out there is a huge benefit even turns out there is a huge benefit even turns out there is a huge benefit even if you only have two so you can handle if you only have two so you can handle if you only have two so you can handle two different blocks in progress at the two different blocks in progress at the two different blocks in progress at the same time for is even better and there same time for is even better and there same time for is even better and there are benefits to be gained even if you are benefits to be gained even if you are benefits to be gained even if you have 16 or even 32 ms hrs so we want to have 16 or even 32 ms hrs so we want to have 16 or even 32 ms hrs so we want to have a few tens of ms hrs if we can this have a few tens of ms hrs if we can this have a few tens of ms hrs if we can this is because memory latencies are is because memory latencies are is because memory latencies are relatively long so if we can keep relatively long so if we can keep relatively long so if we can keep sending requests to memory during that sending requests to memory during that sending requests to memory during that time we can be achieving the memory time we can be achieving the memory time we can be achieving the memory level parallelism of 16 or even 32 level parallelism of 16 or even 32 level parallelism of 16 or even 32 cutting down dramatically on what the cutting down dramatically on what the cutting down dramatically on what the overall performance penalty of cache overall performance penalty of cache overall performance penalty of cache misses and the being

## Miss Under Miss Quiz： Part 4
Kind: captions Language: en so let's see if we learned how Miss so let's see if we learned how Miss so let's see if we learned how Miss under Miss and mhrs work what kind of under Miss and mhrs work what kind of under Miss and mhrs work what kind of application gets no benefit from Miss application gets no benefit from Miss application gets no benefit from Miss under Miss support you need to select under Miss support you need to select under Miss support you need to select all correct answers an application that all correct answers an application that all correct answers an application that always hits in the cache an application always hits in the cache an application always hits in the cache an application that has a Miss every 1,000 that has a Miss every 1,000 that has a Miss every 1,000 instructions an application that has a instructions an application that has a instructions an application that has a Miss every 10 instructions and an Miss every 10 instructions and an Miss every 10 instructions and an application that has a Miss every two application that has a Miss every two application that has a Miss every two instructions

## Miss Under Miss Quiz Quiz Solution： Part 4
Kind: captions Language: en let's look at the answer to our quiz let's look at the answer to our quiz let's look at the answer to our quiz what kind of application gets no benefit what kind of application gets no benefit what kind of application gets no benefit from miss under miss support from miss under miss support from miss under miss support one that always hits in the cache gets one that always hits in the cache gets one that always hits in the cache gets no benefit it never has misses so it no benefit it never has misses so it no benefit it never has misses so it cannot benefit from misunder miss cannot benefit from misunder miss cannot benefit from misunder miss support support support an application that misses every 1000 an application that misses every 1000 an application that misses every 1000 instructions will have a miss and the instructions will have a miss and the instructions will have a miss and the processor will run out of rob entries processor will run out of rob entries processor will run out of rob entries because it has fewer than 1000 because it has fewer than 1000 because it has fewer than 1000 until it finds the next miss so that until it finds the next miss so that until it finds the next miss so that application will suffer the first miss application will suffer the first miss application will suffer the first miss and only then the processor can continue and only then the processor can continue and only then the processor can continue and find the next miss so we never and find the next miss so we never and find the next miss so we never actually need to do miss under miss so actually need to do miss under miss so actually need to do miss under miss so this application will not benefit this application will not benefit this application will not benefit an application that has a miss every 10 an application that has a miss every 10 an application that has a miss every 10 instructions will have a miss and we'll instructions will have a miss and we'll instructions will have a miss and we'll find the next miss so it can benefit find the next miss so it can benefit find the next miss so it can benefit from miss under miss support so this from miss under miss support so this from miss under miss support so this should not be checked and then an should not be checked and then an should not be checked and then an application that has a miss every two application that has a miss every two application that has a miss every two instructions of course will also benefit instructions of course will also benefit instructions of course will also benefit from miss undermiss so we don't check from miss undermiss so we don't check from miss undermiss so we don't check this either

## Cache Hierarchies： Part 4
Kind: captions Language: en so back to our overall reducing the amat so back to our overall reducing the amat so back to our overall reducing the amat we've seen some techniques to reduce the we've seen some techniques to reduce the we've seen some techniques to reduce the hit time hit time hit time we've seen techniques to reduce the miss we've seen techniques to reduce the miss we've seen techniques to reduce the miss rate we've seen a technique for using rate we've seen a technique for using rate we've seen a technique for using the miss penalty which is to overlap the miss penalty which is to overlap the miss penalty which is to overlap multiple misses multiple misses multiple misses and we will now see a technique called and we will now see a technique called and we will now see a technique called multi-level caches or having a cache multi-level caches or having a cache multi-level caches or having a cache hierarchy instead of just one cache hierarchy instead of just one cache hierarchy instead of just one cache with this technique a miss in the first with this technique a miss in the first with this technique a miss in the first cache that the processor accesses which cache that the processor accesses which cache that the processor accesses which is called now a level one cache we'll is called now a level one cache we'll is called now a level one cache we'll just go to another cache instead of just go to another cache instead of just go to another cache instead of memory memory memory so now so now so now the miss penalty from our level one the miss penalty from our level one the miss penalty from our level one cache is not equal to the memory latency cache is not equal to the memory latency cache is not equal to the memory latency the miss penalty in level one cache now the miss penalty in level one cache now the miss penalty in level one cache now is the hit time of the level two cache is the hit time of the level two cache is the hit time of the level two cache plus when the level two cache misses plus when the level two cache misses plus when the level two cache misses then it's gonna be level two miss rate then it's gonna be level two miss rate then it's gonna be level two miss rate times the level two miss penalty times the level two miss penalty times the level two miss penalty and this level two miss penalty can be and this level two miss penalty can be and this level two miss penalty can be the actual memory latency but we can the actual memory latency but we can the actual memory latency but we can have a level 3 cache and if we miss have a level 3 cache and if we miss have a level 3 cache and if we miss their level 4 cache and so on

## AMAT With Cache Hierarchies： Part 4
Kind: captions Language: en so let's look at the aat equation when so let's look at the aat equation when so let's look at the aat equation when we have a cache hierarchy the aat is we have a cache hierarchy the aat is we have a cache hierarchy the aat is still equal to L1 hit time plus the L1 still equal to L1 hit time plus the L1 still equal to L1 hit time plus the L1 Miss rate times the L1 Miss penalty but Miss rate times the L1 Miss penalty but Miss rate times the L1 Miss penalty but it used to stop here and the L1 Miss it used to stop here and the L1 Miss it used to stop here and the L1 Miss penalty would simply be the memory penalty would simply be the memory penalty would simply be the memory access time but now we have that the L1 access time but now we have that the L1 access time but now we have that the L1 Miss penalty is the L2 hit time plus the Miss penalty is the L2 hit time plus the Miss penalty is the L2 hit time plus the L2 Miss rate times the L2 Miss penalty L2 Miss rate times the L2 Miss penalty L2 Miss rate times the L2 Miss penalty and the L2 Miss penalty is the L3 hit and the L2 Miss penalty is the L3 hit and the L2 Miss penalty is the L3 hit time plus the level three Miss rate time plus the level three Miss rate time plus the level three Miss rate times the level three Miss penalty and times the level three Miss penalty and times the level three Miss penalty and so on until at some point we have a cash so on until at some point we have a cash so on until at some point we have a cash whose Miss whose Miss whose Miss penalty is equal to the main memory penalty is equal to the main memory penalty is equal to the main memory latencies because there is no more latencies because there is no more latencies because there is no more caches beneath it this cash whose misses caches beneath it this cash whose misses caches beneath it this cash whose misses go directly to memory is called the last go directly to memory is called the last go directly to memory is called the last level cach or level cach or level cach or LLC so it used to be that the L1 cash LLC so it used to be that the L1 cash LLC so it used to be that the L1 cash was the only cash so L1 cach was the LLC was the only cash so L1 cach was the LLC was the only cash so L1 cach was the LLC then level two caches were introduced so then level two caches were introduced so then level two caches were introduced so level two Miss penalty was the main level two Miss penalty was the main level two Miss penalty was the main memory latency because level two went memory latency because level two went memory latency because level two went directly to memory and that was the last directly to memory and that was the last directly to memory and that was the last level cach and then level three caches level cach and then level three caches level cach and then level three caches were introduced so they became the LLC were introduced so they became the LLC were introduced so they became the LLC and so on

## L1 Vs L2 Quiz： Part 4
Kind: captions Language: en so now that we have seen the concept of so now that we have seen the concept of so now that we have seen the concept of multi-level caches let's see how l2 and multi-level caches let's see how l2 and multi-level caches let's see how l2 and l1 compare so which of these statements l1 compare so which of these statements l1 compare so which of these statements are true the l1 capacity is less than l2 are true the l1 capacity is less than l2 are true the l1 capacity is less than l2 capacity the l1 latency is less than l2 capacity the l1 latency is less than l2 capacity the l1 latency is less than l2 latency the number of accesses that go latency the number of accesses that go latency the number of accesses that go to l1 is less than the number of to l1 is less than the number of to l1 is less than the number of accesses that go to l2 and the l1 accesses that go to l2 and the l1 accesses that go to l2 and the l1 associativity has to be equal to l2 associativity has to be equal to l2 associativity has to be equal to l2 associativity so again check all of the associativity so again check all of the associativity so again check all of the statements that should be true



---

# Playlist 3 Module 6 Raw Transcript

## L1vsL2 Quiz Solution： Part 4
Kind: captions Language: en let's look at the answer to our quiz let's look at the answer to our quiz let's look at the answer to our quiz we were asked which of these are true l1 we were asked which of these are true l1 we were asked which of these are true l1 capacity capacity capacity is less than l2 capacity is less than l2 capacity is less than l2 capacity this should be true this should be true this should be true because the l2 cache because the l2 cache because the l2 cache needs to have hits for things that were needs to have hits for things that were needs to have hits for things that were l1 misses so if l2 l1 misses so if l2 l1 misses so if l2 has the same or lower capacity than l1 has the same or lower capacity than l1 has the same or lower capacity than l1 then not many things that miss in l1 then not many things that miss in l1 then not many things that miss in l1 will actually be hits in l2 and we want will actually be hits in l2 and we want will actually be hits in l2 and we want the l2 to still have a lot of hits so the l2 to still have a lot of hits so the l2 to still have a lot of hits so that things don't go that things don't go that things don't go to even slower caches or even to memory to even slower caches or even to memory to even slower caches or even to memory the l1 latency should it be less than l2 the l1 latency should it be less than l2 the l1 latency should it be less than l2 latency and the answer is yes latency and the answer is yes latency and the answer is yes we look first in l1 cache we look first in l1 cache we look first in l1 cache not because not because not because it has a better it has a better it has a better chance of finding the data than the l2 chance of finding the data than the l2 chance of finding the data than the l2 cache but because it has a lower hit cache but because it has a lower hit cache but because it has a lower hit latency the number of accesses in level latency the number of accesses in level latency the number of accesses in level one cache one cache one cache is it lower than the number of accesses is it lower than the number of accesses is it lower than the number of accesses to l2 cache to l2 cache to l2 cache this is not true this is not true this is not true all of the accesses loads and stores all of the accesses loads and stores all of the accesses loads and stores go to l1 cache but only those that miss go to l1 cache but only those that miss go to l1 cache but only those that miss in l1 cache in l1 cache in l1 cache go to l2 cache so the l2 cache usually go to l2 cache so the l2 cache usually go to l2 cache so the l2 cache usually has a lot fewer accesses has a lot fewer accesses has a lot fewer accesses than l1 than l1 than l1 and finally l1 of sensitivity needs to and finally l1 of sensitivity needs to and finally l1 of sensitivity needs to be equal to l2 associativity this is be equal to l2 associativity this is be equal to l2 associativity this is potentially true but potentially true but potentially true but usually it isn't usually it isn't usually it isn't why well because the l1 cache needs to why well because the l1 cache needs to why well because the l1 cache needs to have a low hit latency have a low hit latency have a low hit latency and need not have the capacity of the l2 and need not have the capacity of the l2 and need not have the capacity of the l2 cache cache cache similarly for associativity the l1 cache similarly for associativity the l1 cache similarly for associativity the l1 cache because it needs to be fast because it needs to be fast because it needs to be fast can have a lower associativity whereas can have a lower associativity whereas can have a lower associativity whereas the l2 cache can be slower so we can the l2 cache can be slower so we can the l2 cache can be slower so we can maybe afford a higher sensitivity so maybe afford a higher sensitivity so maybe afford a higher sensitivity so typically that's what happens l one has typically that's what happens l one has typically that's what happens l one has a lower associativity than l2 so this is a lower associativity than l2 so this is a lower associativity than l2 so this is also not true

## Multilevel Cache Performance： Part 4
Kind: captions Language: en let's look at a single cache that is 16 let's look at a single cache that is 16 let's look at a single cache that is 16 kilobytes in size kilobytes in size kilobytes in size let's look also at a single cache that let's look also at a single cache that let's look also at a single cache that is a hundred twenty eight kilobytes in is a hundred twenty eight kilobytes in is a hundred twenty eight kilobytes in size let's look at no cache simply we size let's look at no cache simply we size let's look at no cache simply we will just access the main memory and will just access the main memory and will just access the main memory and let's look at a hierarchy that has this let's look at a hierarchy that has this let's look at a hierarchy that has this kind of cache is the first level cache kind of cache is the first level cache kind of cache is the first level cache and this kind of cache is the second and this kind of cache is the second and this kind of cache is the second level cache we will look at the hit time level cache we will look at the hit time level cache we will look at the hit time for this the hit rate for all of these for this the hit rate for all of these for this the hit rate for all of these and the overall aim at so 16 kilobyte and the overall aim at so 16 kilobyte and the overall aim at so 16 kilobyte cache might have a hit time of two cache might have a hit time of two cache might have a hit time of two cycles a hit rate of 90% so it gives us cycles a hit rate of 90% so it gives us cycles a hit rate of 90% so it gives us an overall a mat of two plus 10% of the an overall a mat of two plus 10% of the an overall a mat of two plus 10% of the time we have the memory latency let's time we have the memory latency let's time we have the memory latency let's say the memory latency is a hundred say the memory latency is a hundred say the memory latency is a hundred cycles in that case we end up with an cycles in that case we end up with an cycles in that case we end up with an overall amount of 12 to from the hit overall amount of 12 to from the hit overall amount of 12 to from the hit time and 10 on average from the Miss time and 10 on average from the Miss time and 10 on average from the Miss penalties now let's look at a larger penalties now let's look at a larger penalties now let's look at a larger cache this cache might have a hit time cache this cache might have a hit time cache this cache might have a hit time of 10 cycles but a hit rate of maybe of 10 cycles but a hit rate of maybe of 10 cycles but a hit rate of maybe 97.5% 97.5% 97.5% this is a bit high for such a cache but this is a bit high for such a cache but this is a bit high for such a cache but let's say it is the a mat when this is let's say it is the a mat when this is let's say it is the a mat when this is used alone would be 10 plus the miss used alone would be 10 plus the miss used alone would be 10 plus the miss rate times the Miss penalty and we end rate times the Miss penalty and we end rate times the Miss penalty and we end up with 12.5 which is a little bit worse up with 12.5 which is a little bit worse up with 12.5 which is a little bit worse than with a smaller cache so having a than with a smaller cache so having a than with a smaller cache so having a larger cache alone increases the hit larger cache alone increases the hit larger cache alone increases the hit time improves the hit rate so it may or time improves the hit rate so it may or time improves the hit rate so it may or may not improve the a mat but either way may not improve the a mat but either way may not improve the a mat but either way the a mat is not going to improve a lot the a mat is not going to improve a lot the a mat is not going to improve a lot of course having a cache is still lots of course having a cache is still lots of course having a cache is still lots better than not having a cache not better than not having a cache not better than not having a cache not having a cache means that our memory has having a cache means that our memory has having a cache means that our memory has a hit time of 100 cycles basically the a hit time of 100 cycles basically the a hit time of 100 cycles basically the memory latency it hits a hundred percent memory latency it hits a hundred percent memory latency it hits a hundred percent of the time so aim at ends up being 100 of the time so aim at ends up being 100 of the time so aim at ends up being 100 plus 0 times the penalty because it plus 0 times the penalty because it plus 0 times the penalty because it never misses but we still end up with a never misses but we still end up with a never misses but we still end up with a hundred cycles per access which is way hundred cycles per access which is way hundred cycles per access which is way too big so obviously having a cache is too big so obviously having a cache is too big so obviously having a cache is better than not having it so now let's better than not having it so now let's better than not having it so now let's look at the cache hierarchy the hit time look at the cache hierarchy the hit time look at the cache hierarchy the hit time is different is different is different pending on which of the levels we hit in pending on which of the levels we hit in pending on which of the levels we hit in the hit time is still going to be two the hit time is still going to be two the hit time is still going to be two cycles for a level one hit for a level cycles for a level one hit for a level cycles for a level one hit for a level two hit however we first check in level two hit however we first check in level two hit however we first check in level one so we paid two cycles to check then one so we paid two cycles to check then one so we paid two cycles to check then we access l2 and have a hit we access l2 and have a hit we access l2 and have a hit so overall now it's going to be 12 so overall now it's going to be 12 so overall now it's going to be 12 cycles for a level to hit the hit rate cycles for a level to hit the hit rate cycles for a level to hit the hit rate is going to be 90 percent for l1 it's is going to be 90 percent for l1 it's is going to be 90 percent for l1 it's still the same cache as if it was alone still the same cache as if it was alone still the same cache as if it was alone and of all the accesses that go from and of all the accesses that go from and of all the accesses that go from level 1 to level 2 we will have some level 1 to level 2 we will have some level 1 to level 2 we will have some hits because the l2 cache has this hit hits because the l2 cache has this hit hits because the l2 cache has this hit rate when working alone of all the rate when working alone of all the rate when working alone of all the processor accesses it would have a hit processor accesses it would have a hit processor accesses it would have a hit on this many but of all the processor on this many but of all the processor on this many but of all the processor accesses those that hit in l1 probably accesses those that hit in l1 probably accesses those that hit in l1 probably would also have been hits in l2 would also have been hits in l2 would also have been hits in l2 so this cache really has a 75% hit rate so this cache really has a 75% hit rate so this cache really has a 75% hit rate of all the things that go into it it of all the things that go into it it of all the things that go into it it would have this hit rate if it was alone would have this hit rate if it was alone would have this hit rate if it was alone but the l1 cache is filtering all of the but the l1 cache is filtering all of the but the l1 cache is filtering all of the easy to hit accesses so the l2 only get easy to hit accesses so the l2 only get easy to hit accesses so the l2 only get 75% basically it only gets to see kind 75% basically it only gets to see kind 75% basically it only gets to see kind of the worst ten percent of the accesses of the worst ten percent of the accesses of the worst ten percent of the accesses and for those it's only having a 3/4 hit and for those it's only having a 3/4 hit and for those it's only having a 3/4 hit rate but the a matte is now going to be rate but the a matte is now going to be rate but the a matte is now going to be 2 for l1 hits plus 10% of the time we 2 for l1 hits plus 10% of the time we 2 for l1 hits plus 10% of the time we have an l1 miss when we do we have ten have an l1 miss when we do we have ten have an l1 miss when we do we have ten cycles to access l2 plus 25% of those cycles to access l2 plus 25% of those cycles to access l2 plus 25% of those also end up accessing memory so when we also end up accessing memory so when we also end up accessing memory so when we compute what this is it's 10 plus 25 so compute what this is it's 10 plus 25 so compute what this is it's 10 plus 25 so the overall miss penalty for a level one the overall miss penalty for a level one the overall miss penalty for a level one miss is just 35 cycles it's much better miss is just 35 cycles it's much better miss is just 35 cycles it's much better than what it used to be when the l1 was than what it used to be when the l1 was than what it used to be when the l1 was working alone and we finally end up with working alone and we finally end up with working alone and we finally end up with two plus three point five which gives us two plus three point five which gives us two plus three point five which gives us an overall aim at of five point five an overall aim at of five point five an overall aim at of five point five cycles this is much better than either cycles this is much better than either cycles this is much better than either of the two of the two of the two she's working alone and it's much better she's working alone and it's much better she's working alone and it's much better because really we're having most of the because really we're having most of the because really we're having most of the accesses hit using the hit latency of accesses hit using the hit latency of accesses hit using the hit latency of the fast cache some accesses have a the fast cache some accesses have a the fast cache some accesses have a higher hit latency in the second cache higher hit latency in the second cache higher hit latency in the second cache and fewer of them pay this huge memory and fewer of them pay this huge memory and fewer of them pay this huge memory latency so this is why cache hierarchies latency so this is why cache hierarchies latency so this is why cache hierarchies work better than individual caches and work better than individual caches and work better than individual caches and why we have them it is not enough to why we have them it is not enough to why we have them it is not enough to simply have a large cache if it's also simply have a large cache if it's also simply have a large cache if it's also slow in that case you can combine their slow in that case you can combine their slow in that case you can combine their good properties so that most of the time good properties so that most of the time good properties so that most of the time we get this hit time but overall as far we get this hit time but overall as far we get this hit time but overall as far as the memory access is concerned we as the memory access is concerned we as the memory access is concerned we really have this hit rate in the really have this hit rate in the really have this hit rate in the combined cache

## Hit Rate： Part 4
Kind: captions Language: en so we have observed that when the cash so we have observed that when the cash so we have observed that when the cash that we use as the l2 cache is used that we use as the l2 cache is used that we use as the l2 cache is used alone it has a higher hit rate let's alone it has a higher hit rate let's alone it has a higher hit rate let's look back at our example here where we look back at our example here where we look back at our example here where we see dat a hundred twenty-eight kilobyte see dat a hundred twenty-eight kilobyte see dat a hundred twenty-eight kilobyte cache when used alone would have a cache when used alone would have a cache when used alone would have a relatively high hit rate but when we use relatively high hit rate but when we use relatively high hit rate but when we use it after the level one cache has already it after the level one cache has already it after the level one cache has already been accessed it only gets a 75% hit been accessed it only gets a 75% hit been accessed it only gets a 75% hit rate so it looks like the l2 cache has a rate so it looks like the l2 cache has a rate so it looks like the l2 cache has a lower hit rate than the l1 cache which lower hit rate than the l1 cache which lower hit rate than the l1 cache which can be misleading so this particular can be misleading so this particular can be misleading so this particular type of hit rate we call local hit rate type of hit rate we call local hit rate type of hit rate we call local hit rate for this cache a local hit rate is a hit for this cache a local hit rate is a hit for this cache a local hit rate is a hit rate that the cache actually observes of rate that the cache actually observes of rate that the cache actually observes of all the accesses to that cache this many all the accesses to that cache this many all the accesses to that cache this many our hits but note that all the accesses our hits but note that all the accesses our hits but note that all the accesses to a level 2 level 3 etc cache are not to a level 2 level 3 etc cache are not to a level 2 level 3 etc cache are not all the accesses that the processor all the accesses that the processor all the accesses that the processor makes those accesses that have a lot of makes those accesses that have a lot of makes those accesses that have a lot of locality get to be l1 hit and never locality get to be l1 hit and never locality get to be l1 hit and never reach these caches so somehow their reach these caches so somehow their reach these caches so somehow their local hit rate is lower because they local hit rate is lower because they local hit rate is lower because they never see the easy accesses if such a never see the easy accesses if such a never see the easy accesses if such a cache was alone it would see this which cache was alone it would see this which cache was alone it would see this which we call a global hit rate essentially if we call a global hit rate essentially if we call a global hit rate essentially if we care only about how many accesses we care only about how many accesses we care only about how many accesses that the processor make go beyond a that the processor make go beyond a that the processor make go beyond a level 2 cache then we would really be level 2 cache then we would really be level 2 cache then we would really be seeing this but note that this hit rate seeing this but note that this hit rate seeing this but note that this hit rate really includes the hit rate of the really includes the hit rate of the really includes the hit rate of the level 1 cache and what are the actual level 1 cache and what are the actual level 1 cache and what are the actual hits in the level 2 cache so when we hits in the level 2 cache so when we hits in the level 2 cache so when we talk about the cache size and how bigger talk about the cache size and how bigger talk about the cache size and how bigger caches behave better and so on as far as caches behave better and so on as far as caches behave better and so on as far as hit rate is concerned usually we think hit rate is concerned usually we think hit rate is concerned usually we think about the global hit rate because the about the global hit rate because the about the global hit rate because the local hit rate heavily depends on what local hit rate heavily depends on what local hit rate heavily depends on what do you have as the level 1 cache if it's do you have as the level 1 cache if it's do you have as the level 1 cache if it's a level 3 cache then it's local hit rate a level 3 cache then it's local hit rate a level 3 cache then it's local hit rate would depend on what is the level 1 hit would depend on what is the level 1 hit would depend on what is the level 1 hit rate what is the level 2 hit rate and rate what is the level 2 hit rate and rate what is the level 2 hit rate and only then we even get to access the only then we even get to access the only then we even get to access the level 3 cache level 3 cache level 3 cache it's global hit rate is going to be it's global hit rate is going to be it's global hit rate is going to be similar to a hit rate of that type of a similar to a hit rate of that type of a similar to a hit rate of that type of a cache if it was used alone

## Global VS Local Hit Rate： Part 4
Kind: captions Language: en so let's define the global and local hit so let's define the global and local hit so let's define the global and local hit rate the global hit rate can be defined rate the global hit rate can be defined rate the global hit rate can be defined as simply one minus the global miss rate as simply one minus the global miss rate as simply one minus the global miss rate and the global miss rate can be defined and the global miss rate can be defined and the global miss rate can be defined as the number of misses in this cache as the number of misses in this cache as the number of misses in this cache divided by the number of all memory divided by the number of all memory divided by the number of all memory references that the processor makes not references that the processor makes not references that the processor makes not just those that reach this cache in just those that reach this cache in just those that reach this cache in contrast the local hit rate and the contrast the local hit rate and the contrast the local hit rate and the local miss rate can be defined in terms local miss rate can be defined in terms local miss rate can be defined in terms of normal cache behavior local to this of normal cache behavior local to this of normal cache behavior local to this cache so the local hit rate for example cache so the local hit rate for example cache so the local hit rate for example is the number of hits divided by the is the number of hits divided by the is the number of hits divided by the number of accesses to this cache and number of accesses to this cache and number of accesses to this cache and remember that not all of the memory remember that not all of the memory remember that not all of the memory references the processor makes actually references the processor makes actually references the processor makes actually reach this cache the local miss rate reach this cache the local miss rate reach this cache the local miss rate simply can be the number of misses simply can be the number of misses simply can be the number of misses divided by the number of accesses and divided by the number of accesses and divided by the number of accesses and they of course still add up to 1 another they of course still add up to 1 another they of course still add up to 1 another popular metric of how often the cache popular metric of how often the cache popular metric of how often the cache hits that tries to capture the behavior hits that tries to capture the behavior hits that tries to capture the behavior of caches that are not the level 1 cache of caches that are not the level 1 cache of caches that are not the level 1 cache is misses per thousand instructions or is misses per thousand instructions or is misses per thousand instructions or MP ki where K stands for kilo MP ki where K stands for kilo MP ki where K stands for kilo instructions it is very similar to the instructions it is very similar to the instructions it is very similar to the global miss rate except that it doesn't global miss rate except that it doesn't global miss rate except that it doesn't normalize the misses with the number of normalize the misses with the number of normalize the misses with the number of just memory accesses it normalizes with just memory accesses it normalizes with just memory accesses it normalizes with the number of instructions

## Global and Local Miss Rate Quiz： Part 4
Kind: captions Language: en so let's see if we can compute the so let's see if we can compute the so let's see if we can compute the global and local miss rate suppose we global and local miss rate suppose we global and local miss rate suppose we have a level 1 cache that has a 90% hit have a level 1 cache that has a 90% hit have a level 1 cache that has a 90% hit rate what is the local miss rate for rate what is the local miss rate for rate what is the local miss rate for this cache and what is the global miss this cache and what is the global miss this cache and what is the global miss rate for this cache and then let's rate for this cache and then let's rate for this cache and then let's suppose that we also have a level 2 suppose that we also have a level 2 suppose that we also have a level 2 cache where we have a hit for half of cache where we have a hit for half of cache where we have a hit for half of the l1 misses then the local miss rate the l1 misses then the local miss rate the l1 misses then the local miss rate of this cache is what and the global of this cache is what and the global of this cache is what and the global miss rate of this cache is what

## Global and Local Miss Rate Quiz Quiz Solution： Part 4
Kind: captions Language: en let's look at the solution to our global let's look at the solution to our global let's look at the solution to our global and local miss rate quiz for a level 1 and local miss rate quiz for a level 1 and local miss rate quiz for a level 1 cache when we say that it has a 90 cache when we say that it has a 90 cache when we say that it has a 90 percent hit rate we don't have to percent hit rate we don't have to percent hit rate we don't have to specify whether it's a global or local specify whether it's a global or local specify whether it's a global or local hit rate because it's the same it's hit rate because it's the same it's hit rate because it's the same it's local miss rate is the number of misses local miss rate is the number of misses local miss rate is the number of misses divided by the number of accesses to divided by the number of accesses to divided by the number of accesses to this cache its global miss rate is the this cache its global miss rate is the this cache its global miss rate is the number of misses divided by all the number of misses divided by all the number of misses divided by all the accesses that the processor makes but accesses that the processor makes but accesses that the processor makes but because all these accesses go to this because all these accesses go to this because all these accesses go to this cache they are the same and they're both cache they are the same and they're both cache they are the same and they're both 10% because that's what percentage of 10% because that's what percentage of 10% because that's what percentage of all processor accesses misses in this all processor accesses misses in this all processor accesses misses in this cache and that is also the percentage of cache and that is also the percentage of cache and that is also the percentage of things that reach this cache that Miss things that reach this cache that Miss things that reach this cache that Miss in it for a level 2 cache things are in it for a level 2 cache things are in it for a level 2 cache things are different different different it hits for 50% of l1 misses that means it hits for 50% of l1 misses that means it hits for 50% of l1 misses that means that it misses for half of the l1 misses that it misses for half of the l1 misses that it misses for half of the l1 misses so it's local miss rate is 50% of all so it's local miss rate is 50% of all so it's local miss rate is 50% of all the things that reach this cache and the things that reach this cache and the things that reach this cache and those are only l1 misses half of them those are only l1 misses half of them those are only l1 misses half of them hit and half of them miss but it's hit and half of them miss but it's hit and half of them miss but it's global miss rate is only 5% because only global miss rate is only 5% because only global miss rate is only 5% because only 10% of the accesses get to this cache 10% of the accesses get to this cache 10% of the accesses get to this cache and half of that misses so when we and half of that misses so when we and half of that misses so when we express the misses in terms of all the express the misses in terms of all the express the misses in terms of all the accesses that the processor made only 5% accesses that the processor made only 5% accesses that the processor made only 5% of all processor accesses end up missing of all processor accesses end up missing of all processor accesses end up missing in the l2 cache

## Inclusion Property： Part 4
Kind: captions Language: en in addition to different Mis rates in addition to different Mis rates in addition to different Mis rates depending on whether we are looking depending on whether we are looking depending on whether we are looking globally or locally at a cach in a globally or locally at a cach in a globally or locally at a cach in a multi-level cache we have what is called multi-level cache we have what is called multi-level cache we have what is called an inclusion property the inclusion an inclusion property the inclusion an inclusion property the inclusion property has to do with assuming that property has to do with assuming that property has to do with assuming that the block is in level one cache is it the block is in level one cache is it the block is in level one cache is it that it may or may not be in the level that it may or may not be in the level that it may or may not be in the level two cache or it has to be in the L2 two cache or it has to be in the L2 two cache or it has to be in the L2 cache and it is also possible to make a cache and it is also possible to make a cache and it is also possible to make a system in which if a block is in one it system in which if a block is in one it system in which if a block is in one it cannot be also in L2 this is often cannot be also in L2 this is often cannot be also in L2 this is often called exclusion this is what we usually called exclusion this is what we usually called exclusion this is what we usually call call call inclusion and this doesn't have a name inclusion and this doesn't have a name inclusion and this doesn't have a name it's neither exclusion nor it's neither exclusion nor it's neither exclusion nor inclusion so if we just have a normal inclusion so if we just have a normal inclusion so if we just have a normal cach hierarchy where processor accesses cach hierarchy where processor accesses cach hierarchy where processor accesses go to level one cache and then the go to level one cache and then the go to level one cache and then the misses from that go to a larger level misses from that go to a larger level misses from that go to a larger level two cache the question is what will we two cache the question is what will we two cache the question is what will we get and the answer is unless we get and the answer is unless we get and the answer is unless we explicitly try to en Force inclusion or explicitly try to en Force inclusion or explicitly try to en Force inclusion or exclusion we will get this most of the exclusion we will get this most of the exclusion we will get this most of the stuff that is in L1 will also be found stuff that is in L1 will also be found stuff that is in L1 will also be found in L2 but there is no guarantee for that in L2 but there is no guarantee for that in L2 but there is no guarantee for that so there could be blocks in L1 that have so there could be blocks in L1 that have so there could be blocks in L1 that have been replaced from L2 let's look at a been replaced from L2 let's look at a been replaced from L2 let's look at a simple example when the L1 cache has two simple example when the L1 cache has two simple example when the L1 cache has two blocks and is fully associative and the blocks and is fully associative and the blocks and is fully associative and the larger level two cache has four blocks larger level two cache has four blocks larger level two cache has four blocks and is fully associative and both of and is fully associative and both of and is fully associative and both of them use lru policy now let's suppose them use lru policy now let's suppose them use lru policy now let's suppose that the procor access is block a it's that the procor access is block a it's that the procor access is block a it's going to be a miss here and a miss here going to be a miss here and a miss here going to be a miss here and a miss here it's brought from memory placed here and it's brought from memory placed here and it's brought from memory placed here and then that is fed to the L1 cache so it then that is fed to the L1 cache so it then that is fed to the L1 cache so it places it here so so far we have places it here so so far we have places it here so so far we have inclusion let's now access Block B it inclusion let's now access Block B it inclusion let's now access Block B it gets brought from memory here and here gets brought from memory here and here gets brought from memory here and here we still have inclusion if we now access we still have inclusion if we now access we still have inclusion if we now access Block C it will be here and here and we Block C it will be here and here and we Block C it will be here and here and we still have inclusion but let's suppose still have inclusion but let's suppose still have inclusion but let's suppose that when we had the blocks A and B that when we had the blocks A and B that when we had the blocks A and B brought in we reais a that is going to brought in we reais a that is going to brought in we reais a that is going to be a level one hit so now the lru be a level one hit so now the lru be a level one hit so now the lru counters in level one are this but counters in level one are this but counters in level one are this but because there has been no access when a because there has been no access when a because there has been no access when a was re accessed to level two cuz it was was re accessed to level two cuz it was was re accessed to level two cuz it was a level one hit so we don't go to a level one hit so we don't go to a level one hit so we don't go to L2 here we still have that b is the most L2 here we still have that b is the most L2 here we still have that b is the most recently accessed a is the next most recently accessed a is the next most recently accessed a is the next most recently accessed and let's say these recently accessed and let's say these recently accessed and let's say these two are like this now let's say we two are like this now let's say we two are like this now let's say we access C what's going to happen is we access C what's going to happen is we access C what's going to happen is we have a Miss in the L1 cache we have a have a Miss in the L1 cache we have a have a Miss in the L1 cache we have a Miss in the L2 cache we bring in C and Miss in the L2 cache we bring in C and Miss in the L2 cache we bring in C and replace the least recently used block replace the least recently used block replace the least recently used block which is this the counters now get which is this the counters now get which is this the counters now get updated to be three 2 1 and zero and the updated to be three 2 1 and zero and the updated to be three 2 1 and zero and the counters and then C is brought into this counters and then C is brought into this counters and then C is brought into this level one cache and replaces B because level one cache and replaces B because level one cache and replaces B because that's the least recently used block and that's the least recently used block and that's the least recently used block and the counters become one and zero here the counters become one and zero here the counters become one and zero here now let's say a is accessed again that now let's say a is accessed again that now let's say a is accessed again that changes the counters back to this and changes the counters back to this and changes the counters back to this and there is no access to the level two there is no access to the level two there is no access to the level two cache let's say now we access block D cache let's say now we access block D cache let's say now we access block D it's going to be fetched here the it's going to be fetched here the it's going to be fetched here the counters are updated to three 2 1 and z counters are updated to three 2 1 and z counters are updated to three 2 1 and z and d is also brought into the level one and d is also brought into the level one and d is also brought into the level one cache and placed here of course that cache and placed here of course that cache and placed here of course that updates the counters this way now let's updates the counters this way now let's updates the counters this way now let's say a is accessed again now we have the say a is accessed again now we have the say a is accessed again now we have the counters again one Zer let's say now e counters again one Zer let's say now e counters again one Zer let's say now e is accessed is accessed is accessed it's a Miss in both caches we bring e in it's a Miss in both caches we bring e in it's a Miss in both caches we bring e in and now in the L2 cache it replaces the and now in the L2 cache it replaces the and now in the L2 cache it replaces the least recently used block which is a least recently used block which is a least recently used block which is a however in the L1 cache it will replace however in the L1 cache it will replace however in the L1 cache it will replace the least recently used block which is D the least recently used block which is D the least recently used block which is D so at this point we no longer have so at this point we no longer have so at this point we no longer have inclusion because a is in the L1 cache inclusion because a is in the L1 cache inclusion because a is in the L1 cache but is no longer found in the L2 cache but is no longer found in the L2 cache but is no longer found in the L2 cache so inclusion does not necessarily hold so inclusion does not necessarily hold so inclusion does not necessarily hold when you have a cash hierarchy because when you have a cash hierarchy because when you have a cash hierarchy because things that are frequently accessed in things that are frequently accessed in things that are frequently accessed in the L1 cache will not be frequently the L1 cache will not be frequently the L1 cache will not be frequently maybe accessed in L2 because all of the maybe accessed in L2 because all of the maybe accessed in L2 because all of the accesses to that block are going to L1 accesses to that block are going to L1 accesses to that block are going to L1 and the L2 never sees them So eventually and the L2 never sees them So eventually and the L2 never sees them So eventually that block gets replaced from L2 to that block gets replaced from L2 to that block gets replaced from L2 to maintain inclusion properly we have to maintain inclusion properly we have to maintain inclusion properly we have to have a so-called inclusion bit added to have a so-called inclusion bit added to have a so-called inclusion bit added to L2 which is one if that block is also in L2 which is one if that block is also in L2 which is one if that block is also in L1 and when we replace things from L2 to L1 and when we replace things from L2 to L1 and when we replace things from L2 to we prevent blocks whose inclusion bit is we prevent blocks whose inclusion bit is we prevent blocks whose inclusion bit is one from being replaced that ensures one from being replaced that ensures one from being replaced that ensures that we never replace something that is that we never replace something that is that we never replace something that is still in L1 so we can maintain inclusion still in L1 so we can maintain inclusion still in L1 so we can maintain inclusion but again that requires an additional but again that requires an additional but again that requires an additional bit

## Inclusion Quiz： Part 4
Kind: captions Language: en so let's see if we understood inclusion so let's see if we understood inclusion so let's see if we understood inclusion let's say that we have a level 1 and let's say that we have a level 1 and let's say that we have a level 1 and level 2 cache level 2 cache level 2 cache that maintain inclusion property let's that maintain inclusion property let's that maintain inclusion property let's say that a dirty block is replaced from say that a dirty block is replaced from say that a dirty block is replaced from the level 1 cache by something else the level 1 cache by something else the level 1 cache by something else and we need to write it back and we need to write it back and we need to write it back this right back axis can it be a level 2 this right back axis can it be a level 2 this right back axis can it be a level 2 hit hit hit and can it be a level 2 miss and can it be a level 2 miss and can it be a level 2 miss so check here so check here so check here if the right back axis if the right back axis if the right back axis from level 1 can be a hit in l2 from level 1 can be a hit in l2 from level 1 can be a hit in l2 check here check here check here if the right back axis from l1 can be a if the right back axis from l1 can be a if the right back axis from l1 can be a level two miss level two miss level two miss now let's look at the level one and now let's look at the level one and now let's look at the level one and level two cache but this time level two cache but this time level two cache but this time they make no attempt they make no attempt they make no attempt to maintain inclusion to maintain inclusion to maintain inclusion again if we have a dirty block replaced again if we have a dirty block replaced again if we have a dirty block replaced from the level one cache from the level one cache from the level one cache the level one cache has to write it back the level one cache has to write it back the level one cache has to write it back and this right back can it be a level and this right back can it be a level and this right back can it be a level two hit two hit two hit and can it be a level two miss

## Inclusion Quiz Quiz Solution： Part 4
Kind: captions Language: en And it's going to be crazy, it's certain, time, And it's going to be crazy, it's certain, time, And it's going to be crazy, it's certain, time, together with that if of yours that thinks together with that if of yours that thinks together with that if of yours that thinks this of a whole, including the very this of a whole, including the very this of a whole, including the very blog of the current scene. One, they're high, it's blog of the current scene. One, they're high, it's blog of the current scene. One, they're high, it's John, I'm fine, and keep the cow's milk, and John, I'm fine, and keep the cow's milk, and John, I'm fine, and keep the cow's milk, and you, I love how to read and Straw, and you, I love how to read and Straw, and you, I love how to read and Straw, and take your Messi, bicose, includes Hangar, take your Messi, bicose, includes Hangar, take your Messi, bicose, includes Hangar, before the date, Blocks, Pillars, Enelton. before the date, Blocks, Pillars, Enelton. before the date, Blocks, Pillars, Enelton. After we don't, we Break, we strive to After we don't, we Break, we strive to After we don't, we Break, we strive to bring the new Black Swan, and time and bring the new Black Swan, and time and bring the new Black Swan, and time and place is Blue from broad out here on place is Blue from broad out here on place is Blue from broad out here on YouTube, but still you throw it back to YouTube, but still you throw it back to YouTube, but still you throw it back to Block The Time, just in one. Sweet, I hear Block The Time, just in one. Sweet, I hear Block The Time, just in one. Sweet, I hear Israel Tool, dose of Ryback, seven Israel Tool, dose of Ryback, seven Israel Tool, dose of Ryback, seven encounters, and food, McDonald's, I try, encounters, and food, McDonald's, I try, encounters, and food, McDonald's, I try, Nintendo, includes on-way, that Block Strike, Nintendo, includes on-way, that Block Strike, Nintendo, includes on-way, that Block Strike, Play from the level of Cash, the blog, May Play from the level of Cash, the blog, May Play from the level of Cash, the blog, May or May not, or May not, or May not, Being The Love rockers, wow, I am, Being The Love rockers, wow, I am, Being The Love rockers, wow, I am, I have already back, Beatles, Love I have already back, Beatles, Love I have already back, Beatles, Love To Ride At War, with the block, is not To Ride At War, with the block, is not To Ride At War, with the block, is not well, tell you, Any more YouTube, MS, I am well, tell you, Any more YouTube, MS, I am well, tell you, Any more YouTube, MS, I am Rider, Black ending becomes Light laser, and Rider, Black ending becomes Light laser, and Rider, Black ending becomes Light laser, and son of man has, including Profit in

## Advanced Caches Outro： Part 4
Kind: captions Language: en in this lesson we learn how modern in this lesson we learn how modern in this lesson we learn how modern processors use multi-level caches and processors use multi-level caches and processors use multi-level caches and other cache tricks to make up for main other cache tricks to make up for main other cache tricks to make up for main memory being so slow but why is main memory being so slow but why is main memory being so slow but why is main memory so slow memory so slow memory so slow let's see in our next lesson

## Memory Intro： Part 4
Kind: captions Language: en in this lesson we will learn how in this lesson we will learn how in this lesson we will learn how different types of memory work and why different types of memory work and why different types of memory work and why we cannot have a memory that is large we cannot have a memory that is large we cannot have a memory that is large fast and cheap at the same time we will fast and cheap at the same time we will fast and cheap at the same time we will also learn how memory chips can pack so also learn how memory chips can pack so also learn how memory chips can pack so many bits in such a small space

## How Memory Works： Part 4
Kind: captions Language: en in this lesson we will see how memory in this lesson we will see how memory in this lesson we will see how memory works we will look at memory technology works we will look at memory technology works we will look at memory technology and look how SRAM and how Dam works and and look how SRAM and how Dam works and and look how SRAM and how Dam works and what do these mean we will see why is what do these mean we will see why is what do these mean we will see why is the main memory so slow we will see why the main memory so slow we will see why the main memory so slow we will see why don't we just use the cach like type of don't we just use the cach like type of don't we just use the cach like type of memory for the main memory to make it memory for the main memory to make it memory for the main memory to make it faster we will see what happens on a faster we will see what happens on a faster we will see what happens on a cach m when we once access main memory cach m when we once access main memory cach m when we once access main memory and we will see some techniques for and we will see some techniques for and we will see some techniques for making things faster so that once you making things faster so that once you making things faster so that once you have a cach miss the memory doesn't take have a cach miss the memory doesn't take have a cach miss the memory doesn't take that long to respond it's still going to that long to respond it's still going to that long to respond it's still going to be ridiculously long but not as bad as be ridiculously long but not as bad as be ridiculously long but not as bad as before

## Memory Technology  SRAM and DRAM： Part 4
Kind: captions Language: en there are two memory Technologies sram there are two memory Technologies sram there are two memory Technologies sram and dram and dram and dram sram stands for static Random Access sram stands for static Random Access sram stands for static Random Access memory dram stands for Dynamic Random memory dram stands for Dynamic Random memory dram stands for Dynamic Random Access memory The Random Access Part in Access memory The Random Access Part in Access memory The Random Access Part in both names Simply refers to the fact both names Simply refers to the fact both names Simply refers to the fact that We can Access Any memory location that We can Access Any memory location that We can Access Any memory location by Address Without needing to go through by Address Without needing to go through by Address Without needing to go through all the memory locations so Random all the memory locations so Random all the memory locations so Random Access is as opposed to sequential Access is as opposed to sequential Access is as opposed to sequential Access Like a Tape Where You have to Access Like a Tape Where You have to Access Like a Tape Where You have to actually Scan through the whole Tape to actually Scan through the whole Tape to actually Scan through the whole Tape to get some get some get some with memory We can arbitrarily Access with memory We can arbitrarily Access with memory We can arbitrarily Access Any Part of the memory Without really Any Part of the memory Without really Any Part of the memory Without really needing to get to it so the only needing to get to it so the only needing to get to it so the only difference between sram and dram is  this difference between sram and dram is  this difference between sram and dram is  this static or Dynamic behavior static in static or Dynamic behavior static in static or Dynamic behavior static in sram refers to the fact that sram sram refers to the fact that sram sram refers to the fact that sram retains Its Data while the power is retains Its Data while the power is retains Its Data while the power is supplied so As long as you Connect It To supplied so As long as you Connect It To supplied so As long as you Connect It To A Power Source It's going to retain the A Power Source It's going to retain the A Power Source It's going to retain the Data that you wrote to it It's Not going Data that you wrote to it It's Not going Data that you wrote to it It's Not going to Lose It Dynamic in dram means that It to Lose It Dynamic in dram means that It to Lose It Dynamic in dram means that It Will Lose Data Even while connected To A Will Lose Data Even while connected To A Will Lose Data Even while connected To A Power Source unless We Refresh The Data Power Source unless We Refresh The Data Power Source unless We Refresh The Data so Pretty much We need to read Out The so Pretty much We need to read Out The so Pretty much We need to read Out The Data and write it Back In And keep doing Data and write it Back In And keep doing Data and write it Back In And keep doing that on a regular basis otherwise dram that on a regular basis otherwise dram that on a regular basis otherwise dram will Simply  Lose our Data not that both will Simply  Lose our Data not that both will Simply  Lose our Data not that both of  these types of memory will Lose Data of  these types of memory will Lose Data of  these types of memory will Lose Data When the power is not supplied Such as When the power is not supplied Such as When the power is not supplied Such as When we Turn them off so it seems like When we Turn them off so it seems like When we Turn them off so it seems like sram is better We like it use We don't sram is better We like it use We don't sram is better We like it use We don't have to do anything in Order for It To have to do anything in Order for It To have to do anything in Order for It To keep The Data whereas witham We actually keep The Data whereas witham We actually keep The Data whereas witham We actually have to Access The Data and write it have to Access The Data and write it have to Access The Data and write it back in in Order for It To not Lose It back in in Order for It To not Lose It back in in Order for It To not Lose It so Why do We want to have dram the so Why do We want to have dram the so Why do We want to have dram the answer is that for sram several answer is that for sram several answer is that for sram several transistors are needed for each bit of transistors are needed for each bit of transistors are needed for each bit of sram whereas for dram We only need One sram whereas for dram We only need One sram whereas for dram We only need One transistor Per bit  so obviously We can transistor Per bit  so obviously We can transistor Per bit  so obviously We can get a lot more diram Per Unit Area than get a lot more diram Per Unit Area than get a lot more diram Per Unit Area than We can We can We can SR so SR is good it retains Data but Bad SR so SR is good it retains Data but Bad SR so SR is good it retains Data but Bad Because It's more Expensive Here We have Because It's more Expensive Here We have Because It's more Expensive Here We have to Refresh The Data to keep it alive but to Refresh The Data to keep it alive but to Refresh The Data to keep it alive but We can cram A lot of these Bits on a We can cram A lot of these Bits on a We can cram A lot of these Bits on a Chip it turns Out There is Another Chip it turns Out There is Another Chip it turns Out There is Another difference Which is sram is typically difference Which is sram is typically difference Which is sram is typically faster and dram is typically slower so faster and dram is typically slower so faster and dram is typically slower so We Get Speed ​​and simplicity at more Cost We Get Speed ​​and simplicity at more Cost We Get Speed ​​and simplicity at more Cost or We can go slower and need to work at or We can go slower and need to work at or We can go slower and need to work at it but have more Bits

## One Memory Bit   SRAM： Part 4
Kind: captions Language: en so let's look at what one memory bit so let's look at what one memory bit so let's look at what one memory bit looks like in SRAM and dram technology looks like in SRAM and dram technology looks like in SRAM and dram technology the bit sits at the intersection of a the bit sits at the intersection of a the bit sits at the intersection of a word line that passes by many bits and a word line that passes by many bits and a word line that passes by many bits and a bit line that also passes by many bits bit line that also passes by many bits bit line that also passes by many bits so a memory is a matrix of cells like so a memory is a matrix of cells like so a memory is a matrix of cells like this and theem will also have a word this and theem will also have a word this and theem will also have a word line and a bit line what the word line line and a bit line what the word line line and a bit line what the word line does is it controls a transistor here does is it controls a transistor here does is it controls a transistor here that can open or close itself to connect that can open or close itself to connect that can open or close itself to connect the cell to the bit line so the idea is the cell to the bit line so the idea is the cell to the bit line so the idea is that if we want to access this cell we that if we want to access this cell we that if we want to access this cell we activate this word line which opens this activate this word line which opens this activate this word line which opens this transistor and now the memory cell is transistor and now the memory cell is transistor and now the memory cell is connected to the bit line so if we want connected to the bit line so if we want connected to the bit line so if we want to write data we put the bit line at the to write data we put the bit line at the to write data we put the bit line at the value we want and the cell gets that value we want and the cell gets that value we want and the cell gets that value if we want to read the data we let value if we want to read the data we let value if we want to read the data we let go of the bit lines and then the cell go of the bit lines and then the cell go of the bit lines and then the cell here will get a new value on the bit here will get a new value on the bit here will get a new value on the bit line so we can sense what it is in SRAM line so we can sense what it is in SRAM line so we can sense what it is in SRAM the actual memory cell consist consists the actual memory cell consist consists the actual memory cell consist consists of two inverters each inverter has two of two inverters each inverter has two of two inverters each inverter has two transistors that's how you build an transistors that's how you build an transistors that's how you build an inverter and the idea is that if this is inverter and the idea is that if this is inverter and the idea is that if this is one here then this inverter flips it and one here then this inverter flips it and one here then this inverter flips it and outputs a zero here this other inverter outputs a zero here this other inverter outputs a zero here this other inverter flips that and outputs one here so once flips that and outputs one here so once flips that and outputs one here so once we disconnect this transistor this cell we disconnect this transistor this cell we disconnect this transistor this cell will keep its value it's going to will keep its value it's going to will keep its value it's going to maintain a zero here because that zero maintain a zero here because that zero maintain a zero here because that zero is inverted and output as a one here is inverted and output as a one here is inverted and output as a one here which amplifies this Zer here so we have which amplifies this Zer here so we have which amplifies this Zer here so we have a feedback loop here that keeps the data a feedback loop here that keeps the data a feedback loop here that keeps the data that we want so how do we write to this that we want so how do we write to this that we want so how do we write to this cell so if we try to put a one here this cell so if we try to put a one here this cell so if we try to put a one here this inverter here is working against us inverter here is working against us inverter here is working against us because it says a one it wants to Output because it says a one it wants to Output because it says a one it wants to Output a zero and the answer to that is that a zero and the answer to that is that a zero and the answer to that is that this transistor here connects this cell this transistor here connects this cell this transistor here connects this cell to the bit line if we put a stronger one to the bit line if we put a stronger one to the bit line if we put a stronger one here because these are small inverters here because these are small inverters here because these are small inverters we can defeat them so that even though we can defeat them so that even though we can defeat them so that even though this inverter is trying to put a zero this inverter is trying to put a zero this inverter is trying to put a zero here we defeat it with a one and then here we defeat it with a one and then here we defeat it with a one and then what happens is now that this is a one what happens is now that this is a one what happens is now that this is a one this inverter outputs a zero here this this inverter outputs a zero here this this inverter outputs a zero here this inverter now starts outputting a one and inverter now starts outputting a one and inverter now starts outputting a one and the cell becomes a one to make that the cell becomes a one to make that the cell becomes a one to make that easier typically we have two of these easier typically we have two of these easier typically we have two of these transistors and two bit lines for the transistors and two bit lines for the transistors and two bit lines for the same cell that have the opposite values same cell that have the opposite values same cell that have the opposite values so for this cell to become a one we put so for this cell to become a one we put so for this cell to become a one we put one here and zero here that puts a one one here and zero here that puts a one one here and zero here that puts a one here at the same time when we are here at the same time when we are here at the same time when we are putting a zero here so these are more putting a zero here so these are more putting a zero here so these are more easily defeated when we want to read we easily defeated when we want to read we easily defeated when we want to read we can connect both of them to the bit can connect both of them to the bit can connect both of them to the bit lines and if we wrote a one here we will lines and if we wrote a one here we will lines and if we wrote a one here we will have this one be one this one be zero so have this one be one this one be zero so have this one be one this one be zero so this line really outputs the bit we want this line really outputs the bit we want this line really outputs the bit we want and this one just outputs the opposite and this one just outputs the opposite and this one just outputs the opposite value of the bit we want by looking at value of the bit we want by looking at value of the bit we want by looking at the difference between these bit lines the difference between these bit lines the difference between these bit lines we can more quickly detect what the cell we can more quickly detect what the cell we can more quickly detect what the cell actually has as we said these are weak actually has as we said these are weak actually has as we said these are weak transistors here so once we connect this transistors here so once we connect this transistors here so once we connect this cell to the bit lines the bit line is cell to the bit lines the bit line is cell to the bit lines the bit line is long and in order to for example raise long and in order to for example raise long and in order to for example raise the voltage on the bit line to a value the voltage on the bit line to a value the voltage on the bit line to a value that corresponds to one these that corresponds to one these that corresponds to one these transistors here are weak and cannot transistors here are weak and cannot transistors here are weak and cannot pull it to one very fast they also will pull it to one very fast they also will pull it to one very fast they also will try to lower this one down to zero and try to lower this one down to zero and try to lower this one down to zero and they may not be able to do that very they may not be able to do that very they may not be able to do that very fast so at the other end here where fast so at the other end here where fast so at the other end here where we're trying to detect what in this cell we're trying to detect what in this cell we're trying to detect what in this cell instead of looking at when will this instead of looking at when will this instead of looking at when will this cross a threshold between a zero and a cross a threshold between a zero and a cross a threshold between a zero and a one what we look at is in what direction one what we look at is in what direction one what we look at is in what direction is the difference of these two going we is the difference of these two going we is the difference of these two going we make them initially about halfway make them initially about halfway make them initially about halfway between 0 and one we then activate the between 0 and one we then activate the between 0 and one we then activate the word line the cell now starts let's say word line the cell now starts let's say word line the cell now starts let's say pulling this one up and lowering this pulling this one up and lowering this pulling this one up and lowering this one down if we look at what's the one down if we look at what's the one down if we look at what's the difference very quickly we will detect difference very quickly we will detect difference very quickly we will detect that this one here because it's drawn that this one here because it's drawn that this one here because it's drawn towards one is getting to be larger towards one is getting to be larger towards one is getting to be larger voltage than the voltage on this one so voltage than the voltage on this one so voltage than the voltage on this one so we can now say that the cell had a one we can now say that the cell had a one we can now say that the cell had a one before it actually manages to pull this before it actually manages to pull this before it actually manages to pull this fully to one and lower this fully to fully to one and lower this fully to fully to one and lower this fully to zero

## One Memory Bit DRAM： Part 4
Kind: captions Language: en in Dam we also have a transistor that is in Dam we also have a transistor that is in Dam we also have a transistor that is activated by the word line and that activated by the word line and that activated by the word line and that connects the cell to the bit line but we connects the cell to the bit line but we connects the cell to the bit line but we said that's the only transistor we have said that's the only transistor we have said that's the only transistor we have the cell is made out of a simple the cell is made out of a simple the cell is made out of a simple capacitor here so the idea is that to capacitor here so the idea is that to capacitor here so the idea is that to write to this cell we will activate the write to this cell we will activate the write to this cell we will activate the word line we put let's say A1 into the word line we put let's say A1 into the word line we put let's say A1 into the bit Line This transistor connects the bit Line This transistor connects the bit Line This transistor connects the capacitor to the bit line now what capacitor to the bit line now what capacitor to the bit line now what happens is R1 effectively charges this happens is R1 effectively charges this happens is R1 effectively charges this capacitor once we deactivate the word capacitor once we deactivate the word capacitor once we deactivate the word line the capacitor retains the value line the capacitor retains the value line the capacitor retains the value that corresponds to a one if we want to that corresponds to a one if we want to that corresponds to a one if we want to write a zero we activate the word line write a zero we activate the word line write a zero we activate the word line the capacitor is now connected to the the capacitor is now connected to the the capacitor is now connected to the bit line that is connected to a zero bit line that is connected to a zero bit line that is connected to a zero voltage and we empty this capacitor into voltage and we empty this capacitor into voltage and we empty this capacitor into the bit line thus now our capacitor the bit line thus now our capacitor the bit line thus now our capacitor becomes a zero value now when we becomes a zero value now when we becomes a zero value now when we disconnect the word line the capacitor disconnect the word line the capacitor disconnect the word line the capacitor has nothing in it so it retains the has nothing in it so it retains the has nothing in it so it retains the value of zero so as you can see the bit value of zero so as you can see the bit value of zero so as you can see the bit is really stored in this capacitor when is really stored in this capacitor when is really stored in this capacitor when it's charged we are storing a one when it's charged we are storing a one when it's charged we are storing a one when it's not charged we are storing a zero it's not charged we are storing a zero it's not charged we are storing a zero so what's the problem here well the so what's the problem here well the so what's the problem here well the problem is that this transistor is not a problem is that this transistor is not a problem is that this transistor is not a perfect switch it's a little bit leaky perfect switch it's a little bit leaky perfect switch it's a little bit leaky here it's not a problem because if it's here it's not a problem because if it's here it's not a problem because if it's a little bit leaky these two are strong a little bit leaky these two are strong a little bit leaky these two are strong enough to retain the voltage at you know enough to retain the voltage at you know enough to retain the voltage at you know let's say 1 and zero or Z and one while let's say 1 and zero or Z and one while let's say 1 and zero or Z and one while these are not trying to defeat them but these are not trying to defeat them but these are not trying to defeat them but here our capacitor is slowly leaking here our capacitor is slowly leaking here our capacitor is slowly leaking into the bit line and eventually gets into the bit line and eventually gets into the bit line and eventually gets empty so one problem with DM is that it empty so one problem with DM is that it empty so one problem with DM is that it will lose this bit over time and that will lose this bit over time and that will lose this bit over time and that means that periodically we have to means that periodically we have to means that periodically we have to actually read out this bit figure out actually read out this bit figure out actually read out this bit figure out what it was and write it back in at full what it was and write it back in at full what it was and write it back in at full Voltage so that it can again leak for a Voltage so that it can again leak for a Voltage so that it can again leak for a while before it gets lost another while before it gets lost another while before it gets lost another problem with dram that you don't have problem with dram that you don't have problem with dram that you don't have with SRAM is that once we open the word with SRAM is that once we open the word with SRAM is that once we open the word line the capacitor drains into the bit line the capacitor drains into the bit line the capacitor drains into the bit line we can sense what the value was but line we can sense what the value was but line we can sense what the value was but the capacitor is no longer fully charged the capacitor is no longer fully charged the capacitor is no longer fully charged this is called destructive read when you this is called destructive read when you this is called destructive read when you read a DRM cell you have to write it read a DRM cell you have to write it read a DRM cell you have to write it back in it loses the value as it gets back in it loses the value as it gets back in it loses the value as it gets red so this is what is usually called a red so this is what is usually called a red so this is what is usually called a 6t cell or six transistor cell there are 6t cell or six transistor cell there are 6t cell or six transistor cell there are two transistors that are there for two transistors that are there for two transistors that are there for connecting to the cell and there are connecting to the cell and there are connecting to the cell and there are four transist s in the cell itself four transist s in the cell itself four transist s in the cell itself because each of the inverters has two because each of the inverters has two because each of the inverters has two transistors this is of course a 1T cell transistors this is of course a 1T cell transistors this is of course a 1T cell now the area occupied by an SRAM cell now the area occupied by an SRAM cell now the area occupied by an SRAM cell obviously is the area occupied by about obviously is the area occupied by about obviously is the area occupied by about six six six transistors the area of the dram cell it transistors the area of the dram cell it transistors the area of the dram cell it looks like it's the area of one looks like it's the area of one looks like it's the area of one transistor and the area of a capacitor transistor and the area of a capacitor transistor and the area of a capacitor the bigger this capacitor the longer it the bigger this capacitor the longer it the bigger this capacitor the longer it can retain something that corresponds to can retain something that corresponds to can retain something that corresponds to a one or a zero before it gets lost so a one or a zero before it gets lost so a one or a zero before it gets lost so we want this capacitor to be big a we want this capacitor to be big a we want this capacitor to be big a capacitor can be viewed as two metal capacitor can be viewed as two metal capacitor can be viewed as two metal plates placed very close to each other plates placed very close to each other plates placed very close to each other the bigger the area of the plates the the bigger the area of the plates the the bigger the area of the plates the bigger the capacitance of the capacitor bigger the capacitance of the capacitor bigger the capacitance of the capacitor it can keep more charge so it looks like it can keep more charge so it looks like it can keep more charge so it looks like to make a good dram cell we will have to to make a good dram cell we will have to to make a good dram cell we will have to make a big cell because this capacitor make a big cell because this capacitor make a big cell because this capacitor will need a lot of metal area to build will need a lot of metal area to build will need a lot of metal area to build large plates but this is not how we large plates but this is not how we large plates but this is not how we build this capacitor the transistor and build this capacitor the transistor and build this capacitor the transistor and the capacitor itself are actually built the capacitor itself are actually built the capacitor itself are actually built as a single transistor in a technology as a single transistor in a technology as a single transistor in a technology that is called trench cell so this end that is called trench cell so this end that is called trench cell so this end of this transistor is simply buried of this transistor is simply buried of this transistor is simply buried deeply into the Silicon substrate when deeply into the Silicon substrate when deeply into the Silicon substrate when we manufacture transistors so really the we manufacture transistors so really the we manufacture transistors so really the capacitor is kind of buried under the capacitor is kind of buried under the capacitor is kind of buried under the transistor so we really get the area of transistor so we really get the area of transistor so we really get the area of the transistor itself and not much the transistor itself and not much the transistor itself and not much around it you can see also that the dram around it you can see also that the dram around it you can see also that the dram cell can be smaller because it doesn't cell can be smaller because it doesn't cell can be smaller because it doesn't need this second bit line we just need need this second bit line we just need need this second bit line we just need one so that also makes it slightly one so that also makes it slightly one so that also makes it slightly smaller we don't need as much wiring smaller we don't need as much wiring smaller we don't need as much wiring there



---


# Module 1: Shared Memory Multiprocessors & Synchronization

## 1. Shared Memory Machine Model
* **Parallelism**: Fundamental to modern computer systems. Any general-purpose CPU has multiple cores, and general-purpose operating systems are designed to take advantage of hardware parallelism.
* **Shared Memory Machine**: A multi-processor architecture where the entire address space defined by the memories is accessible from any of the CPUs.
* **Common Components**:
  * CPUs
  * Memory
  * Interconnection Network
  * Private cache associated with each CPU

### Three Structural Styles:
1. **Dance Hall Architecture**:
   * CPUs are on one side of an interconnection network, and memory is on the other side.
2. **Symmetric Multiprocessor (SMP) Architecture**:
   * All CPUs are connected to the main memory via a simple system bus.
   * **Symmetric**: Access time from any CPU to memory is exactly the same.
3. **Distributed Shared Memory (DSM) Architecture**:
   * A piece of memory is physically associated with each CPU.
   * Each CPU can access all memories through the interconnection network.
   * Access to local memory is faster than access to remote memory.

## 2. Shared Memory and Private Caches
* **Cache Functionality**:
  * Similar to uniprocessors, caches hold frequently accessed memory locations (data or instructions) to speed up subsequent accesses.
  * When a CPU accesses memory, it checks the cache first. If absent, it fetches from main memory and stores it in the cache for future reuse.
* **The Cache Coherence Problem**:
  * A unique issue in multiprocessors. Since caches are private to each CPU but memory is shared, multiple CPUs may cache the same memory location (e.g., variable `Y`).
  * If one CPU modifies its cached copy to `Y'`, the other CPUs still have the stale value `Y` in their private caches.
  * **Solution**: Requires a contract between hardware and software to ensure consistency.

## 3. Memory Consistency Model
* **Definition**: A contract between hardware and software that dictates the behavior a programmer can expect when multiple threads/processors access shared memory.
* **Goal**: To prevent non-intuitive results (e.g., messages arriving out of order causing one variable to be updated while another is not, leading to impossible program states) so programmers avoid surprises.

### Sequential Consistency Memory Model
* Proposed by Leslie Lamport (1977).
* Two main properties:
  1. **Program Order**: The sequence of memory accesses (reads/writes) generated by a single processor is maintained in the exact textual order they were written.
  2. **Arbitrary Interleaving**: The order in which memory accesses from different processors are interleaved and satisfied by main memory can be arbitrary (analogous to a dealer performing a merge shuffle of two halves of a card deck).
* Guarantees intuitive results by disallowing impossible outcomes that violate program order or strict interleaving.
* **Other Models**: Release consistency, lazy release consistency, and eventual consistency (to be covered in distributed systems).

## 4. Cache Coherence Implementation
* **Memory Consistency vs. Cache Coherence**:
  * *Memory Consistency Model*: The model presented to the programmer (expectation).
  * *Cache Coherence*: How the system (hardware/software) actually implements this model in the presence of private caches.
* **Hardware/Software Trade-off**:
  * **Non-Cache Coherent (NCC) Shared Multiprocessor**: Hardware provides shared address space, but system software must maintain cache coherence.
  * **Cache Coherent (CC) Multiprocessor**: Hardware maintains cache coherence and shared address space entirely.

### Hardware Cache Coherence Mechanisms
1. **Write Invalidate Scheme**:
   * When a processor modifies a shared memory location, hardware broadcasts an "invalidate" signal on the bus.
   * Other processor caches "snoop" on the bus. If they have the memory location, they invalidate their cached copy.
2. **Write Update Scheme**:
   * When a processor modifies a shared memory location, hardware sends the new value on the bus.
   * Other caches snooping on the bus will update their local copy to the new value.
* **Overhead**: Both schemes introduce communication overhead (invalidation or update messages), which grows with the number of processors, network complexity, and the amount of data sharing.

## 5. Scalability
* **Definition**: The expectation that as you add more processors to a system, performance increases.
* **The Reality**: Actual delivered performance is often not linear.
  * **Pro**: Exploits parallelism.
  * **Con**: Increased overhead for cache coherence and communication as processors are added.
* **Chuck Thacker's Quote**: *"Shared memory machines scale well when you don't share memory."*
* **Implication**: To achieve good scalability, minimize the sharing of data structures across threads. This principle is vital when designing OS synchronization, communication, and scheduling algorithms.

## 6. Synchronization Primitives
* Essential tools for coordinating multi-threaded programs.

### Locks
* **Definition**: A mechanism to protect shared data from concurrent access, ensuring that threads do not interfere with each other's work.
* **Types of Locks**:
  1. **Exclusive Lock (Mutual Exclusion Lock / Mutex)**: Only one thread can hold the lock at a time. Used when modifying data (e.g., kids taking turns hitting a single ball).
  2. **Shared Lock**: Allows multiple threads to acquire the lock simultaneously for reading data, ensuring no one modifies it while it's being read (e.g., multiple people reading a newspaper simultaneously).

### Barrier Synchronization
* **Definition**: A coordination point where multiple threads must wait until all participating threads have reached the barrier before any can proceed to the next phase of computation.
* **Analogy**: Waiting for an entire party to arrive at a restaurant before the host will seat anyone.

## 7. Implementing Synchronization: Atomic Operations
* **Programmer's Intent**: A common pattern is producer-consumer, where one thread modifies data and signals another thread when done.
* **Simple Implementation**: Can be achieved using basic atomic reads/writes (loads/stores) using a shared flag variable. The producer modifies data, sets `flag = 1`. The consumer spins until `flag == 1`, then reads the data.
* **Problem for Locks**: Implementing a Mutual Exclusion lock requires checking if a lock is free (`0`) and setting it to taken (`1`) *atomically*. Simple reads and writes cannot do this in one uninterruptible step.

### Read-Modify-Write (RMW) Instructions
* Hardware must provide special atomic instructions (Fetch-and-Phi) to implement locks.
* **Examples**:
  1. **Test-and-Set**: Atomically returns the current value of a memory location and sets it to `1`.
  2. **Fetch-and-Increment**: Atomically fetches the old value and increments the memory location by a specified amount (e.g., `1`).
  3. **Others**: Fetch-and-Store, Fetch-and-Decrement, Compare-and-Swap.

## 8. Scalability Issues With Synchronization
* **Sources of Inefficiency in Locks/Barriers**:
  1. **Latency**: Time spent by a thread to acquire a free lock.
  2. **Waiting Time**: Time spent waiting for a lock currently held by another thread (mostly determined by application logic; OS has little control over this).
  3. **Contention**: When a lock is released, how long it takes for one of the multiple waiting threads to successfully acquire it, and the network traffic generated in the process.

## 9. Spinlock Implementations

### Native Spinlock (Spin on Test-and-Set)
* **Algorithm**: Threads repeatedly execute the `Test-and-Set` instruction until it returns `0` (unlocked).
* **Unlock**: Set lock to `0`.
* **Problems**:
  1. **Too Much Contention**: When the lock is released, all waiting processors simultaneously execute `Test-and-Set`.
  2. **Cannot Exploit Caches**: `Test-and-Set` must bypass the cache and go to main memory to guarantee atomicity.
  3. **Disrupts Useful Work**: Heavy bus traffic from waiting processors slows down the processor that actually holds the lock and is trying to do useful work.

### Caching Spinlock (Spin on Read)
* **Algorithm**: Uses cache coherence.
  * Waiters first do a normal read to check the lock status.
  * If locked, they spin locally on the cached value without going to the bus.
  * When the lock is released, the hardware cache coherence invalidates the local copy.
  * Waiters notice the change, break the spin, and attempt a `Test-and-Set`.
* **Problem**: When the lock is released, all waiters break out and attempt `Test-and-Set` simultaneously, causing an $O(N^2)$ burst of bus transactions (where $N$ is the number of processors), again disrupting useful work.

### Spinlocks With Delay
* **Goal**: Procrastinate checking for the lock after noticing it's released to reduce bus contention.
* **Static Delay**:
  * Each processor waits a pre-assigned, unique amount of time based on its ID before attempting `Test-and-Set`.
  * **Drawback**: Static delays can be inefficient if the lock is lightly contended.
* **Dynamic Delay (Exponential Back-off)**:
  * Delay time increases dynamically.
  * If a thread fails to acquire the lock, it delays a small amount before trying again.
  * If it fails repeatedly, the delay doubles (exponential back-off), reducing frequency of checks under high contention.
  * Works well even on Non-Cache Coherent (NCC) multiprocessors since it limits the frequency of `Test-and-Set` memory accesses.

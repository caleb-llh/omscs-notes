# Playlist 4 Module 9 Raw Transcript

## Lock Variable Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out what a so let's see if we can figure out what a so let's see if we can figure out what a lock variable really is suppose we have lock variable really is suppose we have lock variable really is suppose we have a code like this this code is similar to a code like this this code is similar to a code like this this code is similar to the previous one it's just written in C the previous one it's just written in C the previous one it's just written in C so we have lock the lock that so we have lock the lock that so we have lock the lock that corresponds to elemental increment the corresponds to elemental increment the corresponds to elemental increment the count for L unlock the question for you count for L unlock the question for you count for L unlock the question for you is what is count local fell is it just is what is count local fell is it just is what is count local fell is it just another location in shared memory a another location in shared memory a another location in shared memory a location in some sort of special location in some sort of special location in some sort of special synchronization memory or a special synchronization memory or a special synchronization memory or a special variable that doesn't really have a variable that doesn't really have a variable that doesn't really have a memory address at all

## Lock Variable Quiz Solution： Part 5
Kind: captions Language: en let's look at the answer to our log let's look at the answer to our log let's look at the answer to our log variable quiz variable quiz variable quiz in this code we are using the lock in this code we are using the lock in this code we are using the lock count lock and it's really an array of count lock and it's really an array of count lock and it's really an array of lock that has elements so we are lock that has elements so we are lock that has elements so we are indexing into the array and getting that indexing into the array and getting that indexing into the array and getting that one element of the array and that's our one element of the array and that's our one element of the array and that's our lock that we lock to access count of l lock that we lock to access count of l lock that we lock to access count of l and then we unlock that log so the and then we unlock that log so the and then we unlock that log so the question was what is really this lock question was what is really this lock question was what is really this lock is it just another location in shared is it just another location in shared is it just another location in shared memory so count lock of l really is just memory so count lock of l really is just memory so count lock of l really is just a memory word a memory word a memory word or is it a location in some sort of or is it a location in some sort of or is it a location in some sort of special synchronization memory so there special synchronization memory so there special synchronization memory so there is a sort of special memory where this is a sort of special memory where this is a sort of special memory where this array of locks is residing or is it that array of locks is residing or is it that array of locks is residing or is it that locks are special variables that we can locks are special variables that we can locks are special variables that we can refer to like this in code but really refer to like this in code but really refer to like this in code but really they don't have memory addresses they don't have memory addresses they don't have memory addresses whatsoever the correct answer here is whatsoever the correct answer here is whatsoever the correct answer here is that a lock that a lock that a lock is just is just is just another location in shared memory so a another location in shared memory so a another location in shared memory so a lock is a variable just like any other lock is a variable just like any other lock is a variable just like any other variable it has a memory address we can variable it has a memory address we can variable it has a memory address we can load load load to get its value we can store to change to get its value we can store to change to get its value we can store to change its value and so on so now we will see its value and so on so now we will see its value and so on so now we will see what that variable looks like and what what that variable looks like and what what that variable looks like and what the lock and unlock functions are the lock and unlock functions are the lock and unlock functions are supposed to look like

## Lock Synchronization： Part 5
Kind: captions Language: en so let's look at what log so let's look at what log so let's look at what log synchronization looks like so we said synchronization looks like so we said synchronization looks like so we said that a lock or mutex is just a location that a lock or mutex is just a location that a lock or mutex is just a location in memory so let's say that we are using in memory so let's say that we are using in memory so let's say that we are using integers to represent the log structure integers to represent the log structure integers to represent the log structure so really a lock is just a number so really a lock is just a number so really a lock is just a number we initialize a lock variable so you we initialize a lock variable so you we initialize a lock variable so you pass a lock variable to initialize to pass a lock variable to initialize to pass a lock variable to initialize to let's say a lock init function and what let's say a lock init function and what let's say a lock init function and what it's going to do is set the lock to an it's going to do is set the lock to an it's going to do is set the lock to an unlocked value which let's say that's 0 unlocked value which let's say that's 0 unlocked value which let's say that's 0 so pretty much lock initialization is so pretty much lock initialization is so pretty much lock initialization is just write 0 to that memory location just write 0 to that memory location just write 0 to that memory location that represents the lock zero means that represents the lock zero means that represents the lock zero means unlocked in this case and now the lock unlocked in this case and now the lock unlocked in this case and now the lock function the function two tries to function the function two tries to function the function two tries to acquire the lock and make sure that we acquire the lock and make sure that we acquire the lock and make sure that we are alone in doing that we'll do are alone in doing that we'll do are alone in doing that we'll do something like this it will spin as long something like this it will spin as long something like this it will spin as long as the lock that is 1 which is the as the lock that is 1 which is the as the lock that is 1 which is the locked value and once it sees that lock locked value and once it sees that lock locked value and once it sees that lock var is 0 not 1 it's going to put 1 in var is 0 not 1 it's going to put 1 in var is 0 not 1 it's going to put 1 in that value to mark it as locked and then that value to mark it as locked and then that value to mark it as locked and then we can enter the critical section we can enter the critical section we can enter the critical section because others will now see our 1 and because others will now see our 1 and because others will now see our 1 and spin there and then to unlock we will spin there and then to unlock we will spin there and then to unlock we will just set the lock variable back to being just set the lock variable back to being just set the lock variable back to being free once we do this free once we do this free once we do this coherence ensures that whoever is coherence ensures that whoever is coherence ensures that whoever is waiting here now sees a 0 and acquires waiting here now sees a 0 and acquires waiting here now sees a 0 and acquires the lock themselves so we let them go the lock themselves so we let them go the lock themselves so we let them go into the critical section into the critical section into the critical section but this lock function doesn't really but this lock function doesn't really but this lock function doesn't really work suppose you have two threads the work suppose you have two threads the work suppose you have two threads the purple and the green that come to this purple and the green that come to this purple and the green that come to this point at the same time the lock was point at the same time the lock was point at the same time the lock was initialized to 0 both of them read the initialized to 0 both of them read the initialized to 0 both of them read the lock var for the first time cz row of lock var for the first time cz row of lock var for the first time cz row of them exit this loop because the lock var them exit this loop because the lock var them exit this loop because the lock var is not 1 so both of them come here right is not 1 so both of them come here right is not 1 so both of them come here right 1 to the lock var and then leave this 1 to the lock var and then leave this 1 to the lock var and then leave this lock function and now both of them are lock function and now both of them are lock function and now both of them are in the critical section which is what we in the critical section which is what we in the critical section which is what we were trying to prevent what we really were trying to prevent what we really were trying to prevent what we really need to correctly implement the lock need to correctly implement the lock need to correctly implement the lock function is that the looking at lock var function is that the looking at lock var function is that the looking at lock var and sing 0 and the right thing to lock and sing 0 and the right thing to lock and sing 0 and the right thing to lock var of 1 needs to be atomic which means var of 1 needs to be atomic which means var of 1 needs to be atomic which means we really need to do this in a critical we really need to do this in a critical we really need to do this in a critical section of its own to make sure that section of its own to make sure that section of its own to make sure that when we see a 0 we end up writing one when we see a 0 we end up writing one when we see a 0 we end up writing one before anybody else gets to see a 0 or before anybody else gets to see a 0 or before anybody else gets to see a 0 or at least when we see a 0 we try to write at least when we see a 0 we try to write at least when we see a 0 we try to write 1 but we know if we were the one to 1 but we know if we were the one to 1 but we know if we were the one to write 1 write 1 write 1 or if somebody did it before us we or if somebody did it before us we or if somebody did it before us we should not write one if somebody should not write one if somebody should not write one if somebody succeeded in writing one before us so we succeeded in writing one before us so we succeeded in writing one before us so we have some sort of a paradox here in that have some sort of a paradox here in that have some sort of a paradox here in that we need logs here in order to implement we need logs here in order to implement we need logs here in order to implement the log function so how do we ever the log function so how do we ever the log function so how do we ever support the log function because in support the log function because in support the log function because in order to have a working log function we order to have a working log function we order to have a working log function we really need to have a log in and unlock really need to have a log in and unlock really need to have a log in and unlock here

## Implementing Lock： Part 5
Kind: captions Language: en so let's see how we can implement the so let's see how we can implement the so let's see how we can implement the log function our log function needs to log function our log function needs to log function our log function needs to do something like this we need some sort do something like this we need some sort do something like this we need some sort of a magical lock that works without of a magical lock that works without of a magical lock that works without using locks that protects a critical using locks that protects a critical using locks that protects a critical section in which we check if the lock section in which we check if the lock section in which we check if the lock variable is zero we put one in the lock variable is zero we put one in the lock variable is zero we put one in the lock variable and we leave because we have variable and we leave because we have variable and we leave because we have just successfully acquired the lock if just successfully acquired the lock if just successfully acquired the lock if the lock variable is one we repeat all the lock variable is one we repeat all the lock variable is one we repeat all of this and we need to let others also of this and we need to let others also of this and we need to let others also be checking so really we need to insert be checking so really we need to insert be checking so really we need to insert the unlock magic here and here so the unlock magic here and here so the unlock magic here and here so basically we're just trying to do this basically we're just trying to do this basically we're just trying to do this check and if it's zero setting to one check and if it's zero setting to one check and if it's zero setting to one atomically with each other using some atomically with each other using some atomically with each other using some sort of magic obviously there is no sort of magic obviously there is no sort of magic obviously there is no magic so if there is no magic what do we magic so if there is no magic what do we magic so if there is no magic what do we do there are several ways to get the do there are several ways to get the do there are several ways to get the effect that lock magic would give us one effect that lock magic would give us one effect that lock magic would give us one way is to use what's called lamport's way is to use what's called lamport's way is to use what's called lamport's bakery algorithm or some other algorithm bakery algorithm or some other algorithm bakery algorithm or some other algorithm that actually is able to use normal load that actually is able to use normal load that actually is able to use normal load and store instructions to replace lock and store instructions to replace lock and store instructions to replace lock magic but these algorithms tend to be magic but these algorithms tend to be magic but these algorithms tend to be relatively complicated and just a simple relatively complicated and just a simple relatively complicated and just a simple log function would take tens of log function would take tens of log function would take tens of instructions to do so doing this is instructions to do so doing this is instructions to do so doing this is expensive it makes the log function slow expensive it makes the log function slow expensive it makes the log function slow the other option is to use some sort of the other option is to use some sort of the other option is to use some sort of special atomic read and write special atomic read and write special atomic read and write instructions so there would be an instructions so there would be an instructions so there would be an instruction that does this check and set instruction that does this check and set instruction that does this check and set to one or something similar to that that to one or something similar to that that to one or something similar to that that would allow us to in one instruction do would allow us to in one instruction do would allow us to in one instruction do this without needing the magic

## Atomic Instructions Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out what so let's see if we can figure out what so let's see if we can figure out what kind of atomic instruction do we need so kind of atomic instruction do we need so kind of atomic instruction do we need so we want to implement logs easily and for we want to implement logs easily and for we want to implement logs easily and for that we need what type of instruction we that we need what type of instruction we that we need what type of instruction we need a load that reads memory or we need need a load that reads memory or we need need a load that reads memory or we need a store that writes memory or we need an a store that writes memory or we need an a store that writes memory or we need an instruction that both reads and writes instruction that both reads and writes instruction that both reads and writes memory or we need an instruction that memory or we need an instruction that memory or we need an instruction that doesn't Access Memory at all so what do doesn't Access Memory at all so what do doesn't Access Memory at all so what do we need in order to implement logs we need in order to implement logs we need in order to implement logs relatively easily without resorting to relatively easily without resorting to relatively easily without resorting to complicated algorithms

## Atomic Instructions Quiz Solution： Part 5
Kind: captions Language: en let's look at the answer to our atomic let's look at the answer to our atomic let's look at the answer to our atomic instruction quiz which asks what do we instruction quiz which asks what do we instruction quiz which asks what do we need to implement locks easily an need to implement locks easily an need to implement locks easily an instruction that loads memory stores to instruction that loads memory stores to instruction that loads memory stores to memory or one that reads and writes memory or one that reads and writes memory or one that reads and writes memory or one that doesn't access memory memory or one that doesn't access memory memory or one that doesn't access memory at all we already saw that we need an at all we already saw that we need an at all we already saw that we need an instruction that does something like instruction that does something like instruction that does something like this without interference from others so this without interference from others so this without interference from others so we need an instruction that atomically we need an instruction that atomically we need an instruction that atomically does these two operations one is a read does these two operations one is a read does these two operations one is a read one is a write so an instruction that one is a write so an instruction that one is a write so an instruction that just reads memory will not give us what just reads memory will not give us what just reads memory will not give us what we want it's capable of doing this but we want it's capable of doing this but we want it's capable of doing this but it's not going to let us do this a store it's not going to let us do this a store it's not going to let us do this a store might be able to do this but it cannot might be able to do this but it cannot might be able to do this but it cannot first check the lock variable to see first check the lock variable to see first check the lock variable to see whether it's available so what we need whether it's available so what we need whether it's available so what we need really is an instruction that both reads really is an instruction that both reads really is an instruction that both reads and then writes memory and certainly we and then writes memory and certainly we and then writes memory and certainly we don't need an instruction that doesn't don't need an instruction that doesn't don't need an instruction that doesn't access memory because the lock bar is a access memory because the lock bar is a access memory because the lock bar is a memory location so the correct answer is memory location so the correct answer is memory location so the correct answer is this

## Atomic Instructions： Part 5
Kind: captions Language: en we said that to implement logs easily we we said that to implement logs easily we we said that to implement logs easily we need Atomic instructions that are going need Atomic instructions that are going need Atomic instructions that are going to both read and write memory there are to both read and write memory there are to both read and write memory there are three main types of such instructions three main types of such instructions three main types of such instructions the first type is an atomic exchange the first type is an atomic exchange the first type is an atomic exchange instruction something like this the instruction something like this the instruction something like this the parameters for this instruction look parameters for this instruction look parameters for this instruction look like the parameters for a load or a like the parameters for a load or a like the parameters for a load or a store but what an exchange does it does store but what an exchange does it does store but what an exchange does it does both a load and a store at the same time both a load and a store at the same time both a load and a store at the same time so that it swaps the content of R1 with so that it swaps the content of R1 with so that it swaps the content of R1 with the contents of the memory location that the contents of the memory location that the contents of the memory location that IT addresses so a load would read this IT addresses so a load would read this IT addresses so a load would read this and put it in R1 a store would take and put it in R1 a store would take and put it in R1 a store would take what's in R1 and put it here an exchange what's in R1 and put it here an exchange what's in R1 and put it here an exchange swaps the contents of the tool so the swaps the contents of the tool so the swaps the contents of the tool so the register gets what was in the memory register gets what was in the memory register gets what was in the memory location and the memory location gets location and the memory location gets location and the memory location gets what was in the register if we have an what was in the register if we have an what was in the register if we have an atomic exchange what we can do is put atomic exchange what we can do is put atomic exchange what we can do is put one into R1 and then repeatedly as long one into R1 and then repeatedly as long one into R1 and then repeatedly as long as R1 is equal to one exchange R1 with as R1 is equal to one exchange R1 with as R1 is equal to one exchange R1 with our lock variable so this is the idea R1 our lock variable so this is the idea R1 our lock variable so this is the idea R1 being one we are exchanging our R1 with being one we are exchanging our R1 with being one we are exchanging our R1 with the lock variable we start out with one the lock variable we start out with one the lock variable we start out with one in R1 when we swap them if the memory in R1 when we swap them if the memory in R1 when we swap them if the memory location for the lock had a zero it location for the lock had a zero it location for the lock had a zero it becomes one in the same instruction that becomes one in the same instruction that becomes one in the same instruction that gets us a zero if we do get a zero we gets us a zero if we do get a zero we gets us a zero if we do get a zero we exit this Loop and the lock variable has exit this Loop and the lock variable has exit this Loop and the lock variable has become one if however the lock was become one if however the lock was become one if however the lock was already occupied then we swap one with already occupied then we swap one with already occupied then we swap one with one I R1 stays one so we keep looping so one I R1 stays one so we keep looping so one I R1 stays one so we keep looping so until we succeed in obtaining a zero we until we succeed in obtaining a zero we until we succeed in obtaining a zero we will Loop here just as before but also will Loop here just as before but also will Loop here just as before but also as we are getting our zero we are as we are getting our zero we are as we are getting our zero we are automically setting the lock variable to automically setting the lock variable to automically setting the lock variable to one so nobody else can also observe a one so nobody else can also observe a one so nobody else can also observe a zero pretty much if we get a z in our R1 zero pretty much if we get a z in our R1 zero pretty much if we get a z in our R1 that means that everybody else who try that means that everybody else who try that means that everybody else who try to do this at about the same time we'll to do this at about the same time we'll to do this at about the same time we'll get our one if somebody else did this get our one if somebody else did this get our one if somebody else did this before us then we would be getting their before us then we would be getting their before us then we would be getting their one in our R1 so the fact that we are one in our R1 so the fact that we are one in our R1 so the fact that we are reading zero here means that we are the reading zero here means that we are the reading zero here means that we are the ones who acquir the lock so Atomic ones who acquir the lock so Atomic ones who acquir the lock so Atomic exchange gives us this read and write at exchange gives us this read and write at exchange gives us this read and write at the same time behavior that we want to the same time behavior that we want to the same time behavior that we want to implement our locks and as you can see implement our locks and as you can see implement our locks and as you can see the log function can be now very the log function can be now very the log function can be now very simple the drawback of the atomic simple the drawback of the atomic simple the drawback of the atomic exchange operation is that it keeps exchange operation is that it keeps exchange operation is that it keeps writing to the memory location all the writing to the memory location all the writing to the memory location all the time even while the lock is busy while time even while the lock is busy while time even while the lock is busy while the lock has been locked by some the lock has been locked by some the lock has been locked by some somebody else so as long as we are doing somebody else so as long as we are doing somebody else so as long as we are doing this we will be doing these rights even this we will be doing these rights even this we will be doing these rights even though we're just getting a one all the though we're just getting a one all the though we're just getting a one all the time so the second main type of atomic time so the second main type of atomic time so the second main type of atomic instructions is a whole family of things instructions is a whole family of things instructions is a whole family of things that can be classified as test and then that can be classified as test and then that can be classified as test and then write so the idea is that we test the write so the idea is that we test the write so the idea is that we test the location and if it satisfies some location and if it satisfies some location and if it satisfies some conditions then we're going to write but conditions then we're going to write but conditions then we're going to write but we don't write all the time so let's say we don't write all the time so let's say we don't write all the time so let's say we have some sort of a test and store we have some sort of a test and store we have some sort of a test and store instruction which behaves like this we instruction which behaves like this we instruction which behaves like this we read the memory location that we are read the memory location that we are read the memory location that we are addressing and if that location is zero addressing and if that location is zero addressing and if that location is zero then we store R1 into it and in that then we store R1 into it and in that then we store R1 into it and in that case R1 becomes one but if the location case R1 becomes one but if the location case R1 becomes one but if the location wasn't zero then we don't store anything wasn't zero then we don't store anything wasn't zero then we don't store anything to that location we just return a z so to that location we just return a z so to that location we just return a z so the idea here is that we're testing the idea here is that we're testing the idea here is that we're testing whether the lock is free if it is free whether the lock is free if it is free whether the lock is free if it is free then we try to write a one in it if it's then we try to write a one in it if it's then we try to write a one in it if it's not free then we simply continue There not free then we simply continue There not free then we simply continue There is no point in trying to write to a lock is no point in trying to write to a lock is no point in trying to write to a lock that we know is not free so we are that we know is not free so we are that we know is not free so we are avoiding the rights that happen in the avoiding the rights that happen in the avoiding the rights that happen in the exchange while the lock is actually not exchange while the lock is actually not exchange while the lock is actually not free so this behaves very similarly to free so this behaves very similarly to free so this behaves very similarly to The Exchange except that it only does The Exchange except that it only does The Exchange except that it only does the right if the lock has the unlocked the right if the lock has the unlocked the right if the lock has the unlocked state

## Test And Set Quiz： Part 5
Kind: captions Language: en so let's see if we can use the test and so let's see if we can use the test and so let's see if we can use the test and right family of instructions to right family of instructions to right family of instructions to implement locks the instruction we will implement locks the instruction we will implement locks the instruction we will be using will be the test and set be using will be the test and set be using will be the test and set instruction and its parameters are a instruction and its parameters are a instruction and its parameters are a register and a memory address usually in register and a memory address usually in register and a memory address usually in the form of offset plus a register what the form of offset plus a register what the form of offset plus a register what test and set does is very similar to our test and set does is very similar to our test and set does is very similar to our test and right in that it checks the test and right in that it checks the test and right in that it checks the memory location at this address if the memory location at this address if the memory location at this address if the memory relation is zero we put a 1 there memory relation is zero we put a 1 there memory relation is zero we put a 1 there and we set our 1 to 1 if the memory and we set our 1 to 1 if the memory and we set our 1 to 1 if the memory location was not 0 then we just set our location was not 0 then we just set our location was not 0 then we just set our 1 to 0 without writing to memory if we 1 to 0 without writing to memory if we 1 to 0 without writing to memory if we have such a test and set instruction the have such a test and set instruction the have such a test and set instruction the lock function for a mutex lock wire can lock function for a mutex lock wire can lock function for a mutex lock wire can be implemented by setting our 1 to 0 and be implemented by setting our 1 to 0 and be implemented by setting our 1 to 0 and then as long as our 1 is equal to 0 then as long as our 1 is equal to 0 then as long as our 1 is equal to 0 doing what

## Test And Set Quiz Solution： Part 5
Kind: captions Language: en let's look at the solution to our test let's look at the solution to our test let's look at the solution to our test and set quiz we have a test and set and set quiz we have a test and set and set quiz we have a test and set instruction that checks the memory instruction that checks the memory instruction that checks the memory location at the address defined in the location at the address defined in the location at the address defined in the instruction if that location is zero instruction if that location is zero instruction if that location is zero then we write a one there and set the then we write a one there and set the then we write a one there and set the register to one if the location is not register to one if the location is not register to one if the location is not zero then we just set the register to zero then we just set the register to zero then we just set the register to zero without writing to the memory zero without writing to the memory zero without writing to the memory location and we want to use this to location and we want to use this to location and we want to use this to implement a log function the way we do implement a log function the way we do implement a log function the way we do that is we set R1 to zero and then as that is we set R1 to zero and then as that is we set R1 to zero and then as long as R1 is equal to zero we test and long as R1 is equal to zero we test and long as R1 is equal to zero we test and set lockar and get the result of that set lockar and get the result of that set lockar and get the result of that instruction in R1 so what happens is our instruction in R1 so what happens is our instruction in R1 so what happens is our R1 starts out at zero so we will enter R1 starts out at zero so we will enter R1 starts out at zero so we will enter this first iteration of the loop if the this first iteration of the loop if the this first iteration of the loop if the test and set finds the lock busy it's test and set finds the lock busy it's test and set finds the lock busy it's going to read lockware see that it's not going to read lockware see that it's not going to read lockware see that it's not zero returns zero and R1 stays zero and zero returns zero and R1 stays zero and zero returns zero and R1 stays zero and we keep looping if however the test and we keep looping if however the test and we keep looping if however the test and set finds the lockware to be free that set finds the lockware to be free that set finds the lockware to be free that means its memory location is zero then means its memory location is zero then means its memory location is zero then we do this we set lock V to one thus we do this we set lock V to one thus we do this we set lock V to one thus changing the state of the lock to locked changing the state of the lock to locked changing the state of the lock to locked and we return one in R1 so that this and we return one in R1 so that this and we return one in R1 so that this Loop will exit so we will only exit this Loop will exit so we will only exit this Loop will exit so we will only exit this log function once we acquire the lock log function once we acquire the lock log function once we acquire the lock and we will wait here as long as the and we will wait here as long as the and we will wait here as long as the lock is not available

## Atomic Instructions 2： Part 5
Kind: captions Language: en so coming back to our Atomic so coming back to our Atomic so coming back to our Atomic instructions we have seen that we can instructions we have seen that we can instructions we have seen that we can have an atomic exchange where the have an atomic exchange where the have an atomic exchange where the problem was that it keeps writing to the problem was that it keeps writing to the problem was that it keeps writing to the lock bar even when it's not available lock bar even when it's not available lock bar even when it's not available our test and right class of instructions our test and right class of instructions our test and right class of instructions is trying to avoid that by testing the is trying to avoid that by testing the is trying to avoid that by testing the memory location and only writing to it memory location and only writing to it memory location and only writing to it if they find a zero there and we might if they find a zero there and we might if they find a zero there and we might implement the log with a test and store implement the log with a test and store implement the log with a test and store for example instruction we will for example instruction we will for example instruction we will Implement a log by setting R1 to 1 then Implement a log by setting R1 to 1 then Implement a log by setting R1 to 1 then testing lock while and storing R1 there testing lock while and storing R1 there testing lock while and storing R1 there and the result we get from this testing and the result we get from this testing and the result we get from this testing store let's say 0 in R1 means that the store let's say 0 in R1 means that the store let's say 0 in R1 means that the lockvar was occupied in which case we lockvar was occupied in which case we lockvar was occupied in which case we will just repeat this try this again and will just repeat this try this again and will just repeat this try this again and again but the idea is that as long as again but the idea is that as long as again but the idea is that as long as the lock is occupied this test and store the lock is occupied this test and store the lock is occupied this test and store is only reading the lock variable it is is only reading the lock variable it is is only reading the lock variable it is not writing to it this is good because not writing to it this is good because not writing to it this is good because remember coherence if you keep writing remember coherence if you keep writing remember coherence if you keep writing you keep invalidating all the other you keep invalidating all the other you keep invalidating all the other copies so everybody that is waiting copies so everybody that is waiting copies so everybody that is waiting keeps generating bus traffic because keeps generating bus traffic because keeps generating bus traffic because they're all trying to write and US they're all trying to write and US they're all trying to write and US invalidating each other in contrast the invalidating each other in contrast the invalidating each other in contrast the test and store everybody that is waiting test and store everybody that is waiting test and store everybody that is waiting while the lock is occupied simply gets while the lock is occupied simply gets while the lock is occupied simply gets to share the lock variable to share the lock variable to share the lock variable they're all having it in the shared they're all having it in the shared they're all having it in the shared State and simply and simply iterating on State and simply and simply iterating on State and simply and simply iterating on their cached copy once the lock has been their cached copy once the lock has been their cached copy once the lock has been freed those shared copies are freed those shared copies are freed those shared copies are invalidated because the unlock writes to invalidated because the unlock writes to invalidated because the unlock writes to the lock variable at this point the lock variable at this point the lock variable at this point everybody sees the new value and tries everybody sees the new value and tries everybody sees the new value and tries to grab the lock so there is really only to grab the lock so there is really only to grab the lock so there is really only communication on the lock when the log communication on the lock when the log communication on the lock when the log becomes free until somebody acquires it becomes free until somebody acquires it becomes free until somebody acquires it again so this test and write approach again so this test and write approach again so this test and write approach solve the problem of continuously solve the problem of continuously solve the problem of continuously writing to the log variable but doesn't writing to the log variable but doesn't writing to the log variable but doesn't really solve the problem of this is a really solve the problem of this is a really solve the problem of this is a really strange instruction it's neither really strange instruction it's neither really strange instruction it's neither a store nor a load it would be ideal for a store nor a load it would be ideal for a store nor a load it would be ideal for processor design if we could do processor design if we could do processor design if we could do something that is very much like a load something that is very much like a load something that is very much like a load and very much like a store yet behaves and very much like a store yet behaves and very much like a store yet behaves this way and that brings us to the third this way and that brings us to the third this way and that brings us to the third type of atomic instructions which are type of atomic instructions which are type of atomic instructions which are really now two instructions they special really now two instructions they special really now two instructions they special load and a special store that are load and a special store that are load and a special store that are somehow linked together to create an somehow linked together to create an somehow linked together to create an atomic instruction

## Load Linked Store Conditional： Part 5
Kind: captions Language: en so let's look at the load-linked store so let's look at the load-linked store so let's look at the load-linked store conditional instructions which usually conditional instructions which usually conditional instructions which usually we call ll and SC instruction we have we call ll and SC instruction we have we call ll and SC instruction we have them because atomic reading and writing them because atomic reading and writing them because atomic reading and writing in the same instruction even if we just in the same instruction even if we just in the same instruction even if we just do the right if the read has tested do the right if the read has tested do the right if the read has tested positive for something it's really bad positive for something it's really bad positive for something it's really bad for pipelining or processor let's look for pipelining or processor let's look for pipelining or processor let's look at the classical five stage pipeline at the classical five stage pipeline at the classical five stage pipeline where we fetch decode and read registers where we fetch decode and read registers where we fetch decode and read registers do the early operation this is where we do the early operation this is where we do the early operation this is where we compute the address for memory compute the address for memory compute the address for memory instructions then we have a memory instructions then we have a memory instructions then we have a memory access stage and finally we write the access stage and finally we write the access stage and finally we write the result to a register if a register result to a register if a register result to a register if a register result exists in instruction loads would result exists in instruction loads would result exists in instruction loads would be fetched decoded and read the register be fetched decoded and read the register be fetched decoded and read the register compute the address using that register compute the address using that register compute the address using that register access memory to get the value from that access memory to get the value from that access memory to get the value from that address and then write the result to a address and then write the result to a address and then write the result to a register an atomic read write register an atomic read write register an atomic read write instruction such as a swap or test and instruction such as a swap or test and instruction such as a swap or test and set or test and write would compute the set or test and write would compute the set or test and write would compute the address and read registers and then when address and read registers and then when address and read registers and then when it gets to the memory stage it cannot do it gets to the memory stage it cannot do it gets to the memory stage it cannot do everything it needs to do in one access everything it needs to do in one access everything it needs to do in one access to memory because loads and stores do to memory because loads and stores do to memory because loads and stores do either a read or a write a read write either a read or a write a read write either a read or a write a read write instruction needs to do both and that instruction needs to do both and that instruction needs to do both and that cannot be done in the same cycle for the cannot be done in the same cycle for the cannot be done in the same cycle for the same memory location without really same memory location without really same memory location without really complicating the memory stage a lot so complicating the memory stage a lot so complicating the memory stage a lot so just for the sake of atomic read write just for the sake of atomic read write just for the sake of atomic read write instructions we would need to add memory instructions we would need to add memory instructions we would need to add memory to stage possibly memory tree so that to stage possibly memory tree so that to stage possibly memory tree so that they can read the memory do some they can read the memory do some they can read the memory do some checking and then I finally write the checking and then I finally write the checking and then I finally write the memory so we would be adding to our memory so we would be adding to our memory so we would be adding to our pipeline several stages possibly just pipeline several stages possibly just pipeline several stages possibly just for these instructions but remember that for these instructions but remember that for these instructions but remember that every instruction flows through the every instruction flows through the every instruction flows through the pipeline the same way so if we add these pipeline the same way so if we add these pipeline the same way so if we add these two stages for these instructions we two stages for these instructions we two stages for these instructions we really have added two stages for all really have added two stages for all really have added two stages for all instructions which is not very good instructions which is not very good instructions which is not very good because these are actually not going to because these are actually not going to because these are actually not going to execute that frequently to justify this execute that frequently to justify this execute that frequently to justify this so we will separate the atomic read and so we will separate the atomic read and so we will separate the atomic read and write into two instructions the first write into two instructions the first write into two instructions the first will be a load linked instruction each will be a load linked instruction each will be a load linked instruction each purpose is to implement the read part of purpose is to implement the read part of purpose is to implement the read part of the atomic operation what it does is it the atomic operation what it does is it the atomic operation what it does is it behaves just like a normal load it just behaves just like a normal load it just behaves just like a normal load it just reads from a memory location and puts reads from a memory location and puts reads from a memory location and puts the value in a register but in addition the value in a register but in addition the value in a register but in addition to that to that to that it saves the address from which it it saves the address from which it it saves the address from which it loaded into a special link register so loaded into a special link register so loaded into a special link register so that's the only thing that it does that that's the only thing that it does that that's the only thing that it does that is different from a normal load the is different from a normal load the is different from a normal load the store instruction that we will use to store instruction that we will use to store instruction that we will use to implement the right part of the atomic implement the right part of the atomic implement the right part of the atomic read/write operation is called a store read/write operation is called a store read/write operation is called a store conditional what it does is to check conditional what it does is to check conditional what it does is to check first if the address that it computes is first if the address that it computes is first if the address that it computes is the same as the one in the link register the same as the one in the link register the same as the one in the link register if the address is the same then it if the address is the same then it if the address is the same then it doesn't normal store to that memory doesn't normal store to that memory doesn't normal store to that memory location and returns a 1 in its register location and returns a 1 in its register location and returns a 1 in its register if the address that the store computes if the address that the store computes if the address that the store computes is not the same as the one in the link is not the same as the one in the link is not the same as the one in the link register then it just returns 0 in the register then it just returns 0 in the register then it just returns 0 in the register without storing anything these register without storing anything these register without storing anything these two are behaving like a single atomic two are behaving like a single atomic two are behaving like a single atomic operation because of the link registers operation because of the link registers operation because of the link registers so let's see in more detail how that so let's see in more detail how that so let's see in more detail how that works

## How is LL SC Atomic： Part 5
Kind: captions Language: en so remember that we need the load-linked so remember that we need the load-linked so remember that we need the load-linked and the store conditional to behave like and the store conditional to behave like and the store conditional to behave like a single atomic instruction so that a single atomic instruction so that a single atomic instruction so that writes by other processors cannot come writes by other processors cannot come writes by other processors cannot come in between them without us knowing well in between them without us knowing well in between them without us knowing well this is how it happens we do a load link this is how it happens we do a load link this is how it happens we do a load link from a lock variable to r1 and then we from a lock variable to r1 and then we from a lock variable to r1 and then we do a store conditional of r2 into the do a store conditional of r2 into the do a store conditional of r2 into the lock variable and what we need is that lock variable and what we need is that lock variable and what we need is that if the load link finds the lock if the load link finds the lock if the load link finds the lock available and we try to do a store available and we try to do a store available and we try to do a store conditional then the store conditional conditional then the store conditional conditional then the store conditional should only succeed if the lock is still should only succeed if the lock is still should only succeed if the lock is still available at the time we try that if available at the time we try that if available at the time we try that if somebody else has written into the lock somebody else has written into the lock somebody else has written into the lock then store conditional needs to fail and then store conditional needs to fail and then store conditional needs to fail and the key for that is that if we snoop a the key for that is that if we snoop a the key for that is that if we snoop a right to lock var at any time we put a right to lock var at any time we put a right to lock var at any time we put a zero in the link register so if the load zero in the link register so if the load zero in the link register so if the load link loads the lock variable and we see link loads the lock variable and we see link loads the lock variable and we see that it's free and we try to do a store that it's free and we try to do a store that it's free and we try to do a store conditional but somebody beats us to it conditional but somebody beats us to it conditional but somebody beats us to it then the store will fail because it will then the store will fail because it will then the store will fail because it will observe that the link register doesn't observe that the link register doesn't observe that the link register doesn't match the address of the lock wire so match the address of the lock wire so match the address of the lock wire so the load-linked store conditional really the load-linked store conditional really the load-linked store conditional really are relying on the coherence where we are relying on the coherence where we are relying on the coherence where we observe right by others to make sure observe right by others to make sure observe right by others to make sure that if the store conditional succeeds that if the store conditional succeeds that if the store conditional succeeds it can guarantee that nobody else it can guarantee that nobody else it can guarantee that nobody else grabbed a lock between when we check the grabbed a lock between when we check the grabbed a lock between when we check the lock and when we think we grabbed it lock and when we think we grabbed it lock and when we think we grabbed it really by writing a busy value into it really by writing a busy value into it really by writing a busy value into it now note that the load-linked store now note that the load-linked store now note that the load-linked store conditional are atomic by themselves we conditional are atomic by themselves we conditional are atomic by themselves we don't need actually to use a lock to don't need actually to use a lock to don't need actually to use a lock to make this atomic so some of the critical make this atomic so some of the critical make this atomic so some of the critical sections that would just lock something sections that would just lock something sections that would just lock something increment a variable and then unlock the increment a variable and then unlock the increment a variable and then unlock the lock can now directly be implemented lock can now directly be implemented lock can now directly be implemented using load link and store conditional on using load link and store conditional on using load link and store conditional on the variable itself by load linking the the variable itself by load linking the the variable itself by load linking the variable itself into r1 incrementing r1 variable itself into r1 incrementing r1 variable itself into r1 incrementing r1 store conditional of r1 back into the store conditional of r1 back into the store conditional of r1 back into the variable and now we need to check if the variable and now we need to check if the variable and now we need to check if the r1 as a result of the store conditional r1 as a result of the store conditional r1 as a result of the store conditional became a 0 in which case somebody else became a 0 in which case somebody else became a 0 in which case somebody else was trying to do the same thing was trying to do the same thing was trying to do the same thing and we didn't succeed in doing this and we didn't succeed in doing this and we didn't succeed in doing this atomically in which case we just try atomically in which case we just try atomically in which case we just try again so eventually we will succeed in again so eventually we will succeed in again so eventually we will succeed in this we will load and we will manage to this we will load and we will manage to this we will load and we will manage to store before somebody else beats us to store before somebody else beats us to store before somebody else beats us to it so if another thread is doing the it so if another thread is doing the it so if another thread is doing the exact same thing here let's say we both exact same thing here let's say we both exact same thing here let's say we both load now both of us linked to the load now both of us linked to the load now both of us linked to the variable we both increment we both variable we both increment we both variable we both increment we both stores whoever does the first store will stores whoever does the first store will stores whoever does the first store will succeed whoever does the second store is succeed whoever does the second store is succeed whoever does the second store is going to fail because by that time their going to fail because by that time their going to fail because by that time their link is broken by the first store so link is broken by the first store so link is broken by the first store so that's how for example we can do atomic that's how for example we can do atomic that's how for example we can do atomic increments and all sorts of other things increments and all sorts of other things increments and all sorts of other things that involve only one memory location that involve only one memory location that involve only one memory location so very simple critical sections like so very simple critical sections like so very simple critical sections like this can actually be directly implement this can actually be directly implement this can actually be directly implement it with load-linked store conditional it with load-linked store conditional it with load-linked store conditional without even needing locks anymore we without even needing locks anymore we without even needing locks anymore we can use load link and store conditional can use load link and store conditional can use load link and store conditional directly on the variable itself and we directly on the variable itself and we directly on the variable itself and we no longer need an actual lock around it no longer need an actual lock around it no longer need an actual lock around it for more complicated critical sections for more complicated critical sections for more complicated critical sections such as those that access multiple such as those that access multiple such as those that access multiple variables however load-linked store variables however load-linked store variables however load-linked store conditional doesn't work well but we can conditional doesn't work well but we can conditional doesn't work well but we can still use load-linked store conditional still use load-linked store conditional still use load-linked store conditional to actually implement the lock variable to actually implement the lock variable to actually implement the lock variable here without needing complicated here without needing complicated here without needing complicated algorithms

## LL SC Lock Quiz： Part 5
Kind: captions Language: en so let's see if we can use load Link so let's see if we can use load Link so let's see if we can use load Link store conditional to implement a log store conditional to implement a log store conditional to implement a log function the log function the log function the log function takes a reference to variable function takes a reference to variable function takes a reference to variable lockar and then in assembler and then it lockar and then in assembler and then it lockar and then in assembler and then it will do something like this put one in will do something like this put one in will do something like this put one in R1 load the current value of lockware R1 load the current value of lockware R1 load the current value of lockware into R2 using a load link and then store into R2 using a load link and then store into R2 using a load link and then store R1 into lockar using store conditional R1 into lockar using store conditional R1 into lockar using store conditional so the idea is that we load the current so the idea is that we load the current so the idea is that we load the current value of the log and then we store a one value of the log and then we store a one value of the log and then we store a one there so the question for you is what do there so the question for you is what do there so the question for you is what do we put here to complete this log we put here to complete this log we put here to complete this log function in a way that works correctly function in a way that works correctly function in a way that works correctly that is if we leave this function we that is if we leave this function we that is if we leave this function we know that we have acquired the lock and know that we have acquired the lock and know that we have acquired the lock and we are the only ones that did so the we are the only ones that did so the we are the only ones that did so the first option for where the question mark first option for where the question mark first option for where the question mark is is simply to do a branch on equal Z is is simply to do a branch on equal Z is is simply to do a branch on equal Z R1 trilock the second choice here is R1 trilock the second choice here is R1 trilock the second choice here is branch on equal Z R2 branch on equal Z R2 branch on equal Z R2 trog the third choice is to do Branch trog the third choice is to do Branch trog the third choice is to do Branch not equal 0 R2 trog and then branch on not equal 0 R2 trog and then branch on not equal 0 R2 trog and then branch on equals z link trilock the fourth option equals z link trilock the fourth option equals z link trilock the fourth option is brancher not equal 0 R2 trilock and is brancher not equal 0 R2 trilock and is brancher not equal 0 R2 trilock and then branch on equal 0 R1 trock and the then branch on equal 0 R1 trock and the then branch on equal 0 R1 trock and the fifth final option is to do branch on fifth final option is to do branch on fifth final option is to do branch on not equal 0 and R2 to trock followed by not equal 0 and R2 to trock followed by not equal 0 and R2 to trock followed by a branch or not equal Z link trilock so a branch or not equal Z link trilock so a branch or not equal Z link trilock so select which one of this five is the select which one of this five is the select which one of this five is the correct thing to put here

## LL SC Lock Quiz Quiz Solution： Part 5
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our load-linked store conditional lock load-linked store conditional lock load-linked store conditional lock implementation quiz this is what this implementation quiz this is what this implementation quiz this is what this code is trying to do it's putting one in code is trying to do it's putting one in code is trying to do it's putting one in r1 and then it's loading the current r1 and then it's loading the current r1 and then it's loading the current value of the lock var into r2 and then value of the lock var into r2 and then value of the lock var into r2 and then trying to put a 1 into the lock var all trying to put a 1 into the lock var all trying to put a 1 into the lock var all of these choices are repeating the try of these choices are repeating the try of these choices are repeating the try to acquire the lock under some to acquire the lock under some to acquire the lock under some conditions so the question is what are conditions so the question is what are conditions so the question is what are the correct conditions for retrying the the correct conditions for retrying the the correct conditions for retrying the lock well there are two conditions that lock well there are two conditions that lock well there are two conditions that need to be checked one is did we need to be checked one is did we need to be checked one is did we actually see a lock that was free the actually see a lock that was free the actually see a lock that was free the lock is free when we read a zero in that lock is free when we read a zero in that lock is free when we read a zero in that case we don't go back we actually saw a case we don't go back we actually saw a case we don't go back we actually saw a log that was free if the lock was busy log that was free if the lock was busy log that was free if the lock was busy we go back so if r2 is zero then we we go back so if r2 is zero then we we go back so if r2 is zero then we actually don't go back to trial log that actually don't go back to trial log that actually don't go back to trial log that means that we are actually acquiring the means that we are actually acquiring the means that we are actually acquiring the lock successfully so far so this is lock successfully so far so this is lock successfully so far so this is definitely not a correct solution we definitely not a correct solution we definitely not a correct solution we need to check our two for not equals need to check our two for not equals need to check our two for not equals zero meaning we found a nonzero value zero meaning we found a nonzero value zero meaning we found a nonzero value here that means that the lock was busy here that means that the lock was busy here that means that the lock was busy and we retry so one of these three are and we retry so one of these three are and we retry so one of these three are going to be the correct answer because going to be the correct answer because going to be the correct answer because they include the correct check for they include the correct check for they include the correct check for whether the lock was available the whether the lock was available the whether the lock was available the second thing that we need to check is if second thing that we need to check is if second thing that we need to check is if we found a lock available which means we found a lock available which means we found a lock available which means we're past this point we need to check we're past this point we need to check we're past this point we need to check whether we successfully stored a one whether we successfully stored a one whether we successfully stored a one without somebody else beating us to it without somebody else beating us to it without somebody else beating us to it we successfully store a 1 if our one we successfully store a 1 if our one we successfully store a 1 if our one after the store conditional has a value after the store conditional has a value after the store conditional has a value of 1 so we fail to store our 1 of 1 so we fail to store our 1 of 1 so we fail to store our 1 successfully if r1 is equal to 0 that successfully if r1 is equal to 0 that successfully if r1 is equal to 0 that means that this is really the correct means that this is really the correct means that this is really the correct solution note that these two are solution note that these two are solution note that these two are checking the link register directly and checking the link register directly and checking the link register directly and we said that the link register is a we said that the link register is a we said that the link register is a hidden register it can really only be hidden register it can really only be hidden register it can really only be accessed implicitly through load link accessed implicitly through load link accessed implicitly through load link and store conditional instructions there and store conditional instructions there and store conditional instructions there is no instruction that can directly read is no instruction that can directly read is no instruction that can directly read the link register so that's why these the link register so that's why these the link register so that's why these two two two are not correct choices so in this lock are not correct choices so in this lock are not correct choices so in this lock function basically what we are doing is function basically what we are doing is function basically what we are doing is if we saw the lock that was free and we if we saw the lock that was free and we if we saw the lock that was free and we managed to store a 1 into it without managed to store a 1 into it without managed to store a 1 into it without anybody interfering then we acquired the anybody interfering then we acquired the anybody interfering then we acquired the lock if we saw the lock was busy we try lock if we saw the lock was busy we try lock if we saw the lock was busy we try again again again if the lock was free but somebody beat if the lock was free but somebody beat if the lock was free but somebody beat us to it us to it us to it then again we try again

## Locks and Performance： Part 5
Kind: captions Language: en so now let's consider how different lock so now let's consider how different lock so now let's consider how different lock implementations implementations implementations interact with the coherence protocol and interact with the coherence protocol and interact with the coherence protocol and what performance do they achieve what performance do they achieve what performance do they achieve let's look at three cores let's look at three cores let's look at three cores 0 1 and 2. let's first try to do this 0 1 and 2. let's first try to do this 0 1 and 2. let's first try to do this with atomic exchange instructions core 0 with atomic exchange instructions core 0 with atomic exchange instructions core 0 let's say tries to grab the lock first let's say tries to grab the lock first let's say tries to grab the lock first and it succeeds it does an atomic and it succeeds it does an atomic and it succeeds it does an atomic exchange gets that the lock was free the exchange gets that the lock was free the exchange gets that the lock was free the lock var is now going to be in the lock var is now going to be in the lock var is now going to be in the modified state in the cache of course 0 modified state in the cache of course 0 modified state in the cache of course 0 and it's going to not be and it's going to not be and it's going to not be present in core 1 and core 2's caches present in core 1 and core 2's caches present in core 1 and core 2's caches now eventually core 0 which grab the now eventually core 0 which grab the now eventually core 0 which grab the lock lock lock will unlock by writing 0 to the lock var will unlock by writing 0 to the lock var will unlock by writing 0 to the lock var but the question is what happens but the question is what happens but the question is what happens meanwhile on core 1 and core 2. meanwhile on core 1 and core 2. meanwhile on core 1 and core 2. both core 1 and core 2 might want to both core 1 and core 2 might want to both core 1 and core 2 might want to also enter the same critical section also enter the same critical section also enter the same critical section let's say core 1 tries first it does an let's say core 1 tries first it does an let's say core 1 tries first it does an exchange of r1 and lock war exchange of r1 and lock war exchange of r1 and lock war see that lock war is one so it didn't see that lock war is one so it didn't see that lock war is one so it didn't grab the lock but because there was a grab the lock but because there was a grab the lock but because there was a right to lock var right to lock var right to lock var there is a there is a there is a transfer of lock wire into this cache transfer of lock wire into this cache transfer of lock wire into this cache where it's now modified and now where it's now modified and now where it's now modified and now lockvar is invalid in core zeros cache lockvar is invalid in core zeros cache lockvar is invalid in core zeros cache let's say that core zero now does its let's say that core zero now does its let's say that core zero now does its own exchange does the same thing own exchange does the same thing own exchange does the same thing tries to grab the lock sees that it's tries to grab the lock sees that it's tries to grab the lock sees that it's one we'll try again but because it did one we'll try again but because it did one we'll try again but because it did the right to lock where now it grabs the the right to lock where now it grabs the the right to lock where now it grabs the block in the modified state and core one block in the modified state and core one block in the modified state and core one gets it in invalid state and in core gets it in invalid state and in core gets it in invalid state and in core zero it stays invalid and now as long as zero it stays invalid and now as long as zero it stays invalid and now as long as the lock is busy these two will be the lock is busy these two will be the lock is busy these two will be spinning in that loop so what's going to spinning in that loop so what's going to spinning in that loop so what's going to happen is this block will move many many happen is this block will move many many happen is this block will move many many times between their caches each time times between their caches each time times between their caches each time causing communication on the shared bus causing communication on the shared bus causing communication on the shared bus and each time spending a lot of power and each time spending a lot of power and each time spending a lot of power none of this activity here will actually none of this activity here will actually none of this activity here will actually succeed in grabbing the lock until the succeed in grabbing the lock until the succeed in grabbing the lock until the lock var becomes zero lock var becomes zero lock var becomes zero so at some point so at some point so at some point let's say that core one was the last one let's say that core one was the last one let's say that core one was the last one to grab the lock at the point where to grab the lock at the point where to grab the lock at the point where clockwise written to zero this right clockwise written to zero this right clockwise written to zero this right will cause yet another movement of the will cause yet another movement of the will cause yet another movement of the block it becomes modified in core zeros block it becomes modified in core zeros block it becomes modified in core zeros cache now and now whoever tries to grab cache now and now whoever tries to grab cache now and now whoever tries to grab the lock first will succeed the lock first will succeed the lock first will succeed by obtaining the block by obtaining the block by obtaining the block writing to it but this time grabbing the writing to it but this time grabbing the writing to it but this time grabbing the lock so if core 1 now succeeds in lock so if core 1 now succeeds in lock so if core 1 now succeeds in grabbing the lock after it became grabbing the lock after it became grabbing the lock after it became available core 2 continues this activity available core 2 continues this activity available core 2 continues this activity further further further so there is a lot of this going on as so there is a lot of this going on as so there is a lot of this going on as long as anybody is waiting on a lock long as anybody is waiting on a lock long as anybody is waiting on a lock this is very power hungry because these this is very power hungry because these this is very power hungry because these movements of data between caches are movements of data between caches are movements of data between caches are going to cost us a lot of energy per going to cost us a lot of energy per going to cost us a lot of energy per transfer transfer transfer and also and also and also this activity here keeps the this activity here keeps the this activity here keeps the interconnect between the cores busy for interconnect between the cores busy for interconnect between the cores busy for example the shared bus example the shared bus example the shared bus so the one core that is actually doing so the one core that is actually doing so the one core that is actually doing useful work here while this is going on useful work here while this is going on useful work here while this is going on if it has a cache miss will now be if it has a cache miss will now be if it has a cache miss will now be slower in handling this cache miss slower in handling this cache miss slower in handling this cache miss because it has to wait for the busy bus because it has to wait for the busy bus because it has to wait for the busy bus that these two are keeping busy so not that these two are keeping busy so not that these two are keeping busy so not only is it power hungry to do this it only is it power hungry to do this it only is it power hungry to do this it also also also by busy waiting in such an active way by busy waiting in such an active way by busy waiting in such an active way we're slowing down the useful work that we're slowing down the useful work that we're slowing down the useful work that is the only thing that can really result is the only thing that can really result is the only thing that can really result in us actually getting the lock in us actually getting the lock in us actually getting the lock eventually


# Playlist 1 Module 1 Raw Transcript

## ILP Intro： Part 2
Kind: captions Language: en Branch fiction and if conversion help us Branch fiction and if conversion help us Branch fiction and if conversion help us eliminate most of the pipeline problems eliminate most of the pipeline problems eliminate most of the pipeline problems caused by control hazards. But data caused by control hazards. But data caused by control hazards. But data dependencies can also prevent us from dependencies can also prevent us from dependencies can also prevent us from finishing one instruction every single finishing one instruction every single finishing one instruction every single cycle. So what can we do about data cycle. So what can we do about data cycle. So what can we do about data dependencies? And why stop at only one dependencies? And why stop at only one dependencies? And why stop at only one instruction per cycle? In this lesson, instruction per cycle? In this lesson, instruction per cycle? In this lesson, we will learn about instruction level we will learn about instruction level we will learn about instruction level parallelism, which tells us how many parallelism, which tells us how many parallelism, which tells us how many instructions could be possibly execute instructions could be possibly execute instructions could be possibly execute in any given cycle.

## ILP All in the Same Cycle： Part 2
Kind: captions Language: en okay so the most ideal situation would okay so the most ideal situation would okay so the most ideal situation would be if all instructions that we need to be if all instructions that we need to be if all instructions that we need to execute could just go through the execute could just go through the execute could just go through the pipeline in the same stage so basically pipeline in the same stage so basically pipeline in the same stage so basically although all of them will end up although all of them will end up although all of them will end up executing in the same cycle let's see an executing in the same cycle let's see an executing in the same cycle let's see an example of this so we have a nice table example of this so we have a nice table example of this so we have a nice table here and now we're going to have some here and now we're going to have some here and now we're going to have some five instructions here so let's have five instructions here so let's have five instructions here so let's have instruction so here's where we're going instruction so here's where we're going instruction so here's where we're going to basically just have our instructions to basically just have our instructions to basically just have our instructions and let's say that you know the first and let's say that you know the first and let's say that you know the first instruction is an add instruction our instruction is an add instruction our instruction is an add instruction our one equals R two plus three I'm gonna one equals R two plus three I'm gonna one equals R two plus three I'm gonna draw it like this so that we don't have draw it like this so that we don't have draw it like this so that we don't have to actually have the opcodes and to actually have the opcodes and to actually have the opcodes and everything else clutter the screen and everything else clutter the screen and everything else clutter the screen and then we have our four equals R 1 minus R then we have our four equals R 1 minus R then we have our four equals R 1 minus R five and then we have X or R six equals five and then we have X or R six equals five and then we have X or R six equals r 7x or 8 and then we will have our five r 7x or 8 and then we will have our five r 7x or 8 and then we will have our five equals R eight times R nine and finally equals R eight times R nine and finally equals R eight times R nine and finally we will have an ad of r 4 equals r 8 + r we will have an ad of r 4 equals r 8 + r we will have an ad of r 4 equals r 8 + r 9 so remember these are instructions not 9 so remember these are instructions not 9 so remember these are instructions not actual C operations it's just a time actual C operations it's just a time actual C operations it's just a time kind of showing them like this because kind of showing them like this because kind of showing them like this because it's a more compact representations of it's a more compact representations of it's a more compact representations of them and now let's see what happens in a them and now let's see what happens in a them and now let's see what happens in a pipeline when we do this for a five pipeline when we do this for a five pipeline when we do this for a five stage pipeline so in the first cycle so stage pipeline so in the first cycle so stage pipeline so in the first cycle so this is cycle 1 this will be fetched and this is cycle 1 this will be fetched and this is cycle 1 this will be fetched and this will be fetched and this will be this will be fetched and this will be this will be fetched and this will be fetched and this will be fetched and fetched and this will be fetched and fetched and this will be fetched and this will be fetched and then in cycle 2 this will be fetched and then in cycle 2 this will be fetched and then in cycle 2 we will decode this and read registers we will decode this and read registers we will decode this and read registers and decode this and read registers and and decode this and read registers and and decode this and read registers and decode this and read registers and so on decode this and read registers and so on decode this and read registers and so on and now if you remember your five stage and now if you remember your five stage and now if you remember your five stage pipeline the time comes to execute these pipeline the time comes to execute these pipeline the time comes to execute these things so if we just get all five of things so if we just get all five of things so if we just get all five of them here and execute them we will have them here and execute them we will have them here and execute them we will have a very good CPI in reality what's going a very good CPI in reality what's going a very good CPI in reality what's going to happen is that we are gonna to happen is that we are gonna to happen is that we are gonna eventually write the results for all of eventually write the results for all of eventually write the results for all of these things and if we had more these things and if we had more these things and if we had more instructions here it wouldn't matter all instructions here it wouldn't matter all instructions here it wouldn't matter all of this will be done in basically five of this will be done in basically five of this will be done in basically five cycles so if we have five cycles for a cycles so if we have five cycles for a cycles so if we have five cycles for a very large number of instructions the very large number of instructions the very large number of instructions the CPI is going to be CPI is going to be CPI is going to be five cycles for pretty much an infinite five cycles for pretty much an infinite five cycles for pretty much an infinite number of instructions which is gonna be number of instructions which is gonna be number of instructions which is gonna be very close to zero very close to zero very close to zero so we get a CPI for zero which is very so we get a CPI for zero which is very so we get a CPI for zero which is very good you know you are getting basically good you know you are getting basically good you know you are getting basically zero almost zero cycles per instruction zero almost zero cycles per instruction zero almost zero cycles per instruction so that's wonderful so what's wrong with so that's wonderful so what's wrong with so that's wonderful so what's wrong with something like this well what's wrong something like this well what's wrong something like this well what's wrong with it is that when we execute these with it is that when we execute these with it is that when we execute these instructions what has happened is this instructions what has happened is this instructions what has happened is this instruction in this instruction have instruction in this instruction have instruction in this instruction have read registers at the same time remember read registers at the same time remember read registers at the same time remember that the decode stage is also the read that the decode stage is also the read that the decode stage is also the read register stage so what happens here is register stage so what happens here is register stage so what happens here is we are here adding the R 2 plus R 3 and we are here adding the R 2 plus R 3 and we are here adding the R 2 plus R 3 and here when we are writing we are really here when we are writing we are really here when we are writing we are really writing R 1 ok but when we are adding writing R 1 ok but when we are adding writing R 1 ok but when we are adding here I mean we are subtracting R 5 from here I mean we are subtracting R 5 from here I mean we are subtracting R 5 from R 1 this R 1 here is not the R 1 that R 1 this R 1 here is not the R 1 that R 1 this R 1 here is not the R 1 that this instruction is writing it's the R this instruction is writing it's the R this instruction is writing it's the R one that we read before that instruction one that we read before that instruction one that we read before that instruction managed to write anything so what's managed to write anything so what's managed to write anything so what's wrong with this is that this first wrong with this is that this first wrong with this is that this first instruction is going to execute instruction is going to execute instruction is going to execute correctly but the second instruction correctly but the second instruction correctly but the second instruction because we are executing it too early is because we are executing it too early is because we are executing it too early is gonna basically read the R one from gonna basically read the R one from gonna basically read the R one from before the first instruction so it before the first instruction so it before the first instruction so it doesn't get the result of the first doesn't get the result of the first doesn't get the result of the first instruction so the programmer who wrote instruction so the programmer who wrote instruction so the programmer who wrote this wanted this R want to be this R 1 this wanted this R want to be this R 1 this wanted this R want to be this R 1 it was supposed to be like this but it it was supposed to be like this but it it was supposed to be like this but it isn't and that means that we will have isn't and that means that we will have isn't and that means that we will have to do something about this we cannot to do something about this we cannot to do something about this we cannot just let instructions execute in the just let instructions execute in the just let instructions execute in the same cycle we actually have to figure same cycle we actually have to figure same cycle we actually have to figure out which ones can execute and which out which ones can execute and which out which ones can execute and which ones cannot execute in the same cycle

## The Execute Stage： Part 2
Kind: captions Language: en okay so we have seen that there is a okay so we have seen that there is a okay so we have seen that there is a problem when multiple instructions problem when multiple instructions problem when multiple instructions execute in the same cycle and the execute in the same cycle and the execute in the same cycle and the problem occurs because we need to read problem occurs because we need to read problem occurs because we need to read registers before the previous registers before the previous registers before the previous instructions have written them because instructions have written them because instructions have written them because those previous instructions now execute those previous instructions now execute those previous instructions now execute at the same time as our instruction and at the same time as our instruction and at the same time as our instruction and we have seen that the problem really we have seen that the problem really we have seen that the problem really occurs in the execute stage because we occurs in the execute stage because we occurs in the execute stage because we are just operating on wrong values so are just operating on wrong values so are just operating on wrong values so let's see in the execute stage if let's see in the execute stage if let's see in the execute stage if forwarding can help us with this if you forwarding can help us with this if you forwarding can help us with this if you remember your pipelining what forwarding remember your pipelining what forwarding remember your pipelining what forwarding does is it feeds the value from the does is it feeds the value from the does is it feeds the value from the previous instruction to the subsequent previous instruction to the subsequent previous instruction to the subsequent instructions before those values have instructions before those values have instructions before those values have actually been written to registers so actually been written to registers so actually been written to registers so let's see if that helps if you remember let's see if that helps if you remember let's see if that helps if you remember we had our instructions and now we are we had our instructions and now we are we had our instructions and now we are just gonna look at the execute stage and just gonna look at the execute stage and just gonna look at the execute stage and we're gonna have our instruction just be we're gonna have our instruction just be we're gonna have our instruction just be I 1 I 2 I 3 I 4 I 5 and remember that I 1 I 2 I 3 I 4 I 5 and remember that I 1 I 2 I 3 I 4 I 5 and remember that there was a dependence from i1 to i2 so there was a dependence from i1 to i2 so there was a dependence from i1 to i2 so now what would happen is we execute i1 now what would happen is we execute i1 now what would happen is we execute i1 at some point and in the same cycle we at some point and in the same cycle we at some point and in the same cycle we execute I 2 so the problem with execute I 2 so the problem with execute I 2 so the problem with forwarding is that it will be able to forwarding is that it will be able to forwarding is that it will be able to feed this value to the instruction in feed this value to the instruction in feed this value to the instruction in the next cycle but not to an instruction the next cycle but not to an instruction the next cycle but not to an instruction in the same cycle cause basically if you in the same cycle cause basically if you in the same cycle cause basically if you look at a time line for the cycle this look at a time line for the cycle this look at a time line for the cycle this is the kind of beginning of the execute is the kind of beginning of the execute is the kind of beginning of the execute stage this is the end of the execute stage this is the end of the execute stage this is the end of the execute stage in here somewhere we are kind of stage in here somewhere we are kind of stage in here somewhere we are kind of taking two values operating on them taking two values operating on them taking two values operating on them producing a result this result is producing a result this result is producing a result this result is available kind of here at the end of the available kind of here at the end of the available kind of here at the end of the cycle meanwhile the other instruction is cycle meanwhile the other instruction is cycle meanwhile the other instruction is taking its values doing a subtraction taking its values doing a subtraction taking its values doing a subtraction producing a result and this is where producing a result and this is where producing a result and this is where forwarding can provide this value if forwarding can provide this value if forwarding can provide this value if forwarding worked it would have to forwarding worked it would have to forwarding worked it would have to provide the value here and any arrow provide the value here and any arrow provide the value here and any arrow that goes back what here means basically that goes back what here means basically that goes back what here means basically time travels so pretty much it would time travels so pretty much it would time travels so pretty much it would produce a result in you know one cycle produce a result in you know one cycle produce a result in you know one cycle send it back in time one nanosecond send it back in time one nanosecond send it back in time one nanosecond earlier so that it can be used here earlier so that it can be used here earlier so that it can be used here which is of course impossible so we which is of course impossible so we which is of course impossible so we cannot do that what we will do is not cannot do that what we will do is not cannot do that what we will do is not execute this instruction here we will execute this instruction here we will execute this instruction here we will have to stall meaning this instruction have to stall meaning this instruction have to stall meaning this instruction has to be delayed it just cannot execute has to be delayed it just cannot execute has to be delayed it just cannot execute in this cycle it will execute in the in this cycle it will execute in the in this cycle it will execute in the next cycle if I 3 I 4 and I 5 don't have next cycle if I 3 I 4 and I 5 don't have next cycle if I 3 I 4 and I 5 don't have any dependencies with any dependencies with any dependencies with one they can still execute in you know one they can still execute in you know one they can still execute in you know they have read the right register values they have read the right register values they have read the right register values they're gonna be able to execute so now they're gonna be able to execute so now they're gonna be able to execute so now what we have is really we spent two what we have is really we spent two what we have is really we spent two cycles executing five instructions okay cycles executing five instructions okay cycles executing five instructions okay so if we ignore the filling of the so if we ignore the filling of the so if we ignore the filling of the pipeline and so on basically what we pipeline and so on basically what we pipeline and so on basically what we have now is the CPI is equal to two over have now is the CPI is equal to two over have now is the CPI is equal to two over five in this case which is approximately five in this case which is approximately five in this case which is approximately 0.4 actually it is 0.4 so what we have 0.4 actually it is 0.4 so what we have 0.4 actually it is 0.4 so what we have is a CP of you know instead of it being is a CP of you know instead of it being is a CP of you know instead of it being 0.2 for these five instructions it would 0.2 for these five instructions it would 0.2 for these five instructions it would be 0.4 if we have a lot of instructions be 0.4 if we have a lot of instructions be 0.4 if we have a lot of instructions there will be a lot of these there will be a lot of these there will be a lot of these dependencies so we will actually have to dependencies so we will actually have to dependencies so we will actually have to spend you know we will have a CPI that spend you know we will have a CPI that spend you know we will have a CPI that is not zero even if we had an infinite is not zero even if we had an infinite is not zero even if we had an infinite number of execution units

## RAW Dependencies： Part 2
Kind: captions Language: en Okay, so what we have seen just now is Okay, so what we have seen just now is Okay, so what we have seen just now is that basically even the ideal processor that basically even the ideal processor that basically even the ideal processor that can execute any number of that can execute any number of that can execute any number of instructions per cycle still has to obey instructions per cycle still has to obey instructions per cycle still has to obey raw dependencies meaning it still has to raw dependencies meaning it still has to raw dependencies meaning it still has to wait for results to be produced before wait for results to be produced before wait for results to be produced before they can be used by instructions that they can be used by instructions that they can be used by instructions that need those results and uh that really need those results and uh that really need those results and uh that really creates uh some of the delays that the creates uh some of the delays that the creates uh some of the delays that the ideal processors are going to have and ideal processors are going to have and ideal processors are going to have and that's why the ILP is not zero. it's that's why the ILP is not zero. it's that's why the ILP is not zero. it's going to be something larger than zero going to be something larger than zero going to be something larger than zero even for an ideal processor. So let us even for an ideal processor. So let us even for an ideal processor. So let us now look at uh some instructions. So now look at uh some instructions. So now look at uh some instructions. So let's say that I1 is producing a value let's say that I1 is producing a value let's say that I1 is producing a value that is used by I2 and then I3 is that is used by I2 and then I3 is that is used by I2 and then I3 is producing a value for I4 and then I4 producing a value for I4 and then I4 producing a value for I4 and then I4 produces a value that is used for I5. So produces a value that is used for I5. So produces a value that is used for I5. So what is going to be the CPI for this what is going to be the CPI for this what is going to be the CPI for this particular program? particular program? particular program? Well, it's going to be CPI is equal how Well, it's going to be CPI is equal how Well, it's going to be CPI is equal how many cycles do we need for these three many cycles do we need for these three many cycles do we need for these three for these five instructions? Well, for these five instructions? Well, for these five instructions? Well, here's what's going to happen. I1 can here's what's going to happen. I1 can here's what's going to happen. I1 can execute immediately in the first cycle execute immediately in the first cycle execute immediately in the first cycle and so can I3 because it doesn't have to and so can I3 because it doesn't have to and so can I3 because it doesn't have to wait for anything. So, we spend one wait for anything. So, we spend one wait for anything. So, we spend one cycle executing I1 and I3. The next cycle executing I1 and I3. The next cycle executing I1 and I3. The next cycle, I2 can now go and I4 can also go cycle, I2 can now go and I4 can also go cycle, I2 can now go and I4 can also go cuz I3 is now done. And then we still cuz I3 is now done. And then we still cuz I3 is now done. And then we still need a third cycle to execute I5, which need a third cycle to execute I5, which need a third cycle to execute I5, which has to wait for I4. So, we have spent has to wait for I4. So, we have spent has to wait for I4. So, we have spent three cycles for five instructions, three cycles for five instructions, three cycles for five instructions, which is going to be a CPI of 0.6. So which is going to be a CPI of 0.6. So which is going to be a CPI of 0.6. So the CPI is still lower than one but uh the CPI is still lower than one but uh the CPI is still lower than one but uh now it's even larger. now it's even larger. now it's even larger. And finally if we did have this And finally if we did have this And finally if we did have this dependence as well. Now we have to do I1 dependence as well. Now we have to do I1 dependence as well. Now we have to do I1 and then I2 and then I3 and then I4 and and then I2 and then I3 and then I4 and and then I2 and then I3 and then I4 and then I5. So now the CPI with this is then I5. So now the CPI with this is then I5. So now the CPI with this is going to be one. Why? Well, because we going to be one. Why? Well, because we going to be one. Why? Well, because we need five cycles to execute five need five cycles to execute five need five cycles to execute five instructions. So the CPI is going to be instructions. So the CPI is going to be instructions. So the CPI is going to be one. So basically in the program the one. So basically in the program the one. So basically in the program the dependencies the the raw dependencies dependencies the the raw dependencies dependencies the the raw dependencies are going to determine basically what is are going to determine basically what is are going to determine basically what is the possible CPI even if the processor the possible CPI even if the processor the possible CPI even if the processor can do everything else ideally meaning can do everything else ideally meaning can do everything else ideally meaning it can fetch any number of instructions. it can fetch any number of instructions. it can fetch any number of instructions. It can decode and read registers in any It can decode and read registers in any It can decode and read registers in any number of uh instructions but it simply number of uh instructions but it simply number of uh instructions but it simply cannot provide time travel which uh cannot provide time travel which uh cannot provide time travel which uh which uh we which is kind of the only which uh we which is kind of the only which uh we which is kind of the only consideration that we really consider in consideration that we really consider in consideration that we really consider in this case is just that you know we we this case is just that you know we we this case is just that you know we we cannot do time travel. Everything else cannot do time travel. Everything else cannot do time travel. Everything else we can do we still have a CPI that is we can do we still have a CPI that is we can do we still have a CPI that is somewhere between one and zero. You somewhere between one and zero. You somewhere between one and zero. You would have a CPI of zero if you have a would have a CPI of zero if you have a would have a CPI of zero if you have a very large number of instructions with very large number of instructions with very large number of instructions with no dependencies. We have seen that we no dependencies. We have seen that we no dependencies. We have seen that we have a CPI of one. If you have any have a CPI of one. If you have any have a CPI of one. If you have any number of instructions that are all number of instructions that are all number of instructions that are all dependent on the previous instructions, dependent on the previous instructions, dependent on the previous instructions, you really cannot do anything in you really cannot do anything in you really cannot do anything in parallel.

## WAW Dependencies： Part 2
Kind: captions Language: en so let's say that we have these five so let's say that we have these five so let's say that we have these five instructions and for now all that instructions and for now all that instructions and for now all that matters really for right after I matters really for right after I matters really for right after I dependencies which registers are we dependencies which registers are we dependencies which registers are we writing to so let's say that the first writing to so let's say that the first writing to so let's say that the first instruction is writing to r1 it's instruction is writing to r1 it's instruction is writing to r1 it's computing some result it doesn't really computing some result it doesn't really computing some result it doesn't really matter what it is and then the second matter what it is and then the second matter what it is and then the second one is writing to r4 then we have our one is writing to r4 then we have our one is writing to r4 then we have our seven and our eight being written and seven and our eight being written and seven and our eight being written and then the fifth instruction is also then the fifth instruction is also then the fifth instruction is also supposed to write to our four so we will supposed to write to our four so we will supposed to write to our four so we will see that the problem will be with the see that the problem will be with the see that the problem will be with the second and the fifth instruction writing second and the fifth instruction writing second and the fifth instruction writing to r4 so what can happen is let's say to r4 so what can happen is let's say to r4 so what can happen is let's say that there is a read after write that there is a read after write that there is a read after write dependence between the first and the dependence between the first and the dependence between the first and the second instructions so what's now going second instructions so what's now going second instructions so what's now going to happen is there will be some cycle to happen is there will be some cycle to happen is there will be some cycle let's call it cycle five in which r1 is let's call it cycle five in which r1 is let's call it cycle five in which r1 is going to execute our four cannot execute going to execute our four cannot execute going to execute our four cannot execute it has to wait and it will execute here it has to wait and it will execute here it has to wait and it will execute here in cycle six meanwhile these in cycle six meanwhile these in cycle six meanwhile these instructions will execute because they instructions will execute because they instructions will execute because they don't have any dependencies now in our don't have any dependencies now in our don't have any dependencies now in our five stage pipeline what's going to five stage pipeline what's going to five stage pipeline what's going to happen is basically this instruction is happen is basically this instruction is happen is basically this instruction is then going to move to the mem stage then going to move to the mem stage then going to move to the mem stage where it doesn't do anything because where it doesn't do anything because where it doesn't do anything because none of them are really memory none of them are really memory none of them are really memory instructions and then so will these and instructions and then so will these and instructions and then so will these and then what's going to happen is in the then what's going to happen is in the then what's going to happen is in the next cycle this instruction will write next cycle this instruction will write next cycle this instruction will write its result its result its result so now r1 gets written this instruction so now r1 gets written this instruction so now r1 gets written this instruction is also gonna write its results so r7 is is also gonna write its results so r7 is is also gonna write its results so r7 is going to be written this one writes r8 going to be written this one writes r8 going to be written this one writes r8 and this one writes are four however the and this one writes are four however the and this one writes are four however the second instruction has executed here so second instruction has executed here so second instruction has executed here so it's going to go through the mem stage it's going to go through the mem stage it's going to go through the mem stage here and write the result here and it's here and write the result here and it's here and write the result here and it's going to be r4 so if you look at what's going to be r4 so if you look at what's going to be r4 so if you look at what's the final result in our for the final the final result in our for the final the final result in our for the final result is in r4 is whatever was the last result is in r4 is whatever was the last result is in r4 is whatever was the last thing written to it which is going to be thing written to it which is going to be thing written to it which is going to be this value but it should have been this this value but it should have been this this value but it should have been this value because when the programmer wrote value because when the programmer wrote value because when the programmer wrote this they wrote it this way so they this they wrote it this way so they this they wrote it this way so they would expect that our four would retain would expect that our four would retain would expect that our four would retain the value from this fifth instruction the value from this fifth instruction the value from this fifth instruction not from the instruction so basically not from the instruction so basically not from the instruction so basically the programmer wanted this to become the programmer wanted this to become the programmer wanted this to become temporarily this but then take a new temporarily this but then take a new temporarily this but then take a new value here and that's kind of the last value here and that's kind of the last value here and that's kind of the last value after these five instructions but value after these five instructions but value after these five instructions but that didn't happen so that's a problem that didn't happen so that's a problem that didn't happen so that's a problem why is it a problem why did it occur why is it a problem why did it occur why is it a problem why did it occur because this instruction got delayed so because this instruction got delayed so because this instruction got delayed so we have executed these two instructions we have executed these two instructions we have executed these two instructions out of order this is the second out of order this is the second out of order this is the second instruction and it writes in the eighth instruction and it writes in the eighth instruction and it writes in the eighth cycle this is the fifth instruction but cycle this is the fifth instruction but cycle this is the fifth instruction but it writes in the seventh cycle so it writes in the seventh cycle so it writes in the seventh cycle so basically we have actually written the basically we have actually written the basically we have actually written the result in the wrong order result in the wrong order result in the wrong order I mean it's it's an order different from I mean it's it's an order different from I mean it's it's an order different from the order in which they were supposed to the order in which they were supposed to the order in which they were supposed to execute in the program so in order to execute in the program so in order to execute in the program so in order to fix this one solution would be so to fix fix this one solution would be so to fix fix this one solution would be so to fix this one solution would be to not have this one solution would be to not have this one solution would be to not have this instruction right the result here this instruction right the result here this instruction right the result here instead it would have to be stalled here instead it would have to be stalled here instead it would have to be stalled here and here and finally write its result and here and finally write its result and here and finally write its result here so a real processor might in some here so a real processor might in some here so a real processor might in some way needs to figure out how to do this way needs to figure out how to do this way needs to figure out how to do this because these writes need to happen in because these writes need to happen in because these writes need to happen in the correct order

## Dependency Quiz： Part 2
Kind: captions Language: en okay so now that we have seen two types okay so now that we have seen two types okay so now that we have seen two types of dependencies and how can they affect of dependencies and how can they affect of dependencies and how can they affect the scheduling of instructions in a the scheduling of instructions in a the scheduling of instructions in a process that is trying to do more than process that is trying to do more than process that is trying to do more than an instruction per cycle let's do a an instruction per cycle let's do a an instruction per cycle let's do a dependency quiz let's say we have a dependency quiz let's say we have a dependency quiz let's say we have a processor with a classical five stage processor with a classical five stage processor with a classical five stage pipeline meaning that in one cycle it pipeline meaning that in one cycle it pipeline meaning that in one cycle it fetches then reads registers and decodes fetches then reads registers and decodes fetches then reads registers and decodes and executes then performs a memory and executes then performs a memory and executes then performs a memory access and finally writes registers access and finally writes registers access and finally writes registers let's also say that the processor has let's also say that the processor has let's also say that the processor has forwarding that can if the result has forwarding that can if the result has forwarding that can if the result has been produced it's gonna feed it to the been produced it's gonna feed it to the been produced it's gonna feed it to the instruction correctly even though maybe instruction correctly even though maybe instruction correctly even though maybe the result has not been written to a the result has not been written to a the result has not been written to a register yet but the result has to have register yet but the result has to have register yet but the result has to have been produced meaning that the been produced meaning that the been produced meaning that the instruction that produces the result had instruction that produces the result had instruction that produces the result had to either finish the execute stage if to either finish the execute stage if to either finish the execute stage if the result is something like a plus or the result is something like a plus or the result is something like a plus or had to finish the memory stage if this had to finish the memory stage if this had to finish the memory stage if this is a load instruction so we can is a load instruction so we can is a load instruction so we can basically then feed that result to the basically then feed that result to the basically then feed that result to the instruction that executes in the next instruction that executes in the next instruction that executes in the next cycle without having to wait for the cycle without having to wait for the cycle without having to wait for the producing instruction to get to the producing instruction to get to the producing instruction to get to the right register stage just as we right register stage just as we right register stage just as we discussed before and finally let's say discussed before and finally let's say discussed before and finally let's say that this processor can execute ten that this processor can execute ten that this processor can execute ten instructions in its stage so instructions in its stage so instructions in its stage so theoretically it could in one cycle theoretically it could in one cycle theoretically it could in one cycle fetch ten instructions then the Codel fetch ten instructions then the Codel fetch ten instructions then the Codel ten then execute all ten if there are no ten then execute all ten if there are no ten then execute all ten if there are no dependencies and so on so in five cycles dependencies and so on so in five cycles dependencies and so on so in five cycles is going to execute all ten instructions is going to execute all ten instructions is going to execute all ten instructions if we have 20 instructions the first ten if we have 20 instructions the first ten if we have 20 instructions the first ten will be fetch in the first cycle will be fetch in the first cycle will be fetch in the first cycle it would immediately in the next cycle it would immediately in the next cycle it would immediately in the next cycle fetch the next and so theoretically it fetch the next and so theoretically it fetch the next and so theoretically it would be able to finish all 20 in six would be able to finish all 20 in six would be able to finish all 20 in six cycles because we have to still get to cycles because we have to still get to cycles because we have to still get to the right track stage the question is the right track stage the question is the right track stage the question is when do we execute and write back each when do we execute and write back each when do we execute and write back each of these instructions so we have these of these instructions so we have these of these instructions so we have these six instructions and the question is six instructions and the question is six instructions and the question is when do we execute and to help you get when do we execute and to help you get when do we execute and to help you get started let's say that the very first started let's say that the very first started let's say that the very first cyclic cycles zero that is when we fetch cyclic cycles zero that is when we fetch cyclic cycles zero that is when we fetch them all them all them all it reads registers in cycle 1 so it will it reads registers in cycle 1 so it will it reads registers in cycle 1 so it will execute definitely in cycle 2 also execute definitely in cycle 2 also execute definitely in cycle 2 also because the add here is using r2 it has because the add here is using r2 it has because the add here is using r2 it has to wait for the mode to execute and then to wait for the mode to execute and then to wait for the mode to execute and then forwarding will produce the most value forwarding will produce the most value forwarding will produce the most value to the add so the add has to wait an to the add so the add has to wait an to the add so the add has to wait an execute in cycle 3 if you look at the execute in cycle 3 if you look at the execute in cycle 3 if you look at the mouse because it has executed in cycle 2 mouse because it has executed in cycle 2 mouse because it has executed in cycle 2 it will go through the main stage in it will go through the main stage in it will go through the main stage in cycle 3 and write its result in cycle 4 cycle 3 and write its result in cycle 4 cycle 3 and write its result in cycle 4 the question is the question is the question is when are these others in what cycles are when are these others in what cycles are when are these others in what cycles are they producing and writing their results

## Dependency Quiz Solution： Part 2
Kind: captions Language: en so the ad is here producing a value of so the ad is here producing a value of so the ad is here producing a value of r1 that is not a problem because the r1 that is not a problem because the r1 that is not a problem because the previous instruction is just producing previous instruction is just producing previous instruction is just producing r2 so the ad can write its result when r2 so the ad can write its result when r2 so the ad can write its result when it's time and when it executed in cycle it's time and when it executed in cycle it's time and when it executed in cycle three so it's gonna write in cycle five three so it's gonna write in cycle five three so it's gonna write in cycle five now let's look at this multiplication now let's look at this multiplication now let's look at this multiplication instruction over here remember we can instruction over here remember we can instruction over here remember we can execute any number of instructions in execute any number of instructions in execute any number of instructions in the in the same cycle the reason why we the in the same cycle the reason why we the in the same cycle the reason why we had to delay the ad is because it had to delay the ad is because it had to delay the ad is because it actually has this dependence from r2 to actually has this dependence from r2 to actually has this dependence from r2 to r2 the multiplication is using only r3 r2 the multiplication is using only r3 r2 the multiplication is using only r3 and producing r3 using r3 has no problem and producing r3 using r3 has no problem and producing r3 using r3 has no problem because neither the man nor the other because neither the man nor the other because neither the man nor the other producing r3 so this multiplication can producing r3 so this multiplication can producing r3 so this multiplication can actually execute in cycle two and then actually execute in cycle two and then actually execute in cycle two and then there is no problem with r3 here because there is no problem with r3 here because there is no problem with r3 here because there is only r1 and r2 written by the there is only r1 and r2 written by the there is only r1 and r2 written by the previous instructions so this previous instructions so this previous instructions so this multiplication can actually write r3 in multiplication can actually write r3 in multiplication can actually write r3 in cycle four because it's like two three cycle four because it's like two three cycle four because it's like two three four now let's look at this ad here it four now let's look at this ad here it four now let's look at this ad here it uses r1 and r3 the r1 is supposed to be uses r1 and r3 the r1 is supposed to be uses r1 and r3 the r1 is supposed to be using is produced by this ad which using is produced by this ad which using is produced by this ad which executes is cycle three the r3 is executes is cycle three the r3 is executes is cycle three the r3 is produced in cycle two by the produced in cycle two by the produced in cycle two by the multiplication so this instruction multiplication so this instruction multiplication so this instruction actually has to wait for cycle four actually has to wait for cycle four actually has to wait for cycle four because of this then it's going to be because of this then it's going to be because of this then it's going to be cycle five when it's in the mem stage cycle five when it's in the mem stage cycle five when it's in the mem stage and it's able to write its result in and it's able to write its result in and it's able to write its result in cycle six it's going to overwrite the cycle six it's going to overwrite the cycle six it's going to overwrite the results from the ad here but because the results from the ad here but because the results from the ad here but because the ad is writing in cycle five it's okay if ad is writing in cycle five it's okay if ad is writing in cycle five it's okay if you write in cycle six so everything is you write in cycle six so everything is you write in cycle six so everything is fine here we can write in cycle six now fine here we can write in cycle six now fine here we can write in cycle six now let's look at the multiplication here let's look at the multiplication here let's look at the multiplication here it's using our four none of the other it's using our four none of the other it's using our four none of the other instructions are producing r4 so it can instructions are producing r4 so it can instructions are producing r4 so it can also execute in the second cycle it also execute in the second cycle it also execute in the second cycle it produces r4 none of the previous produces r4 none of the previous produces r4 none of the previous instructions are writing is so we have instructions are writing is so we have instructions are writing is so we have don't have to wait for anything so we don't have to wait for anything so we don't have to wait for anything so we can actually write our r4 in cycle four can actually write our r4 in cycle four can actually write our r4 in cycle four as well just like this small here and as well just like this small here and as well just like this small here and then finally let's look at this ad here then finally let's look at this ad here then finally let's look at this ad here it's using r1 from here it's using r4 it's using r1 from here it's using r4 it's using r1 from here it's using r4 from here so our execution has to be in from here so our execution has to be in from here so our execution has to be in the cycle that follows the larger of the cycle that follows the larger of the cycle that follows the larger of these two the larger is the cycle four these two the larger is the cycle four these two the larger is the cycle four so we have to begin our execution in so we have to begin our execution in so we have to begin our execution in cycle five here at the end of cycle five cycle five here at the end of cycle five cycle five here at the end of cycle five day we will have the result than the day we will have the result than the day we will have the result than the moment we can write the result in cycle moment we can write the result in cycle moment we can write the result in cycle seven but we have to check seven but we have to check seven but we have to check is somebody else gonna overwrite it is somebody else gonna overwrite it is somebody else gonna overwrite it later and that's not going to happen later and that's not going to happen later and that's not going to happen because if we right here in cycle seven because if we right here in cycle seven because if we right here in cycle seven we are safe this is the latest we are safe this is the latest we are safe this is the latest instruction it should do the right of instruction it should do the right of instruction it should do the right of the r1 at the latest time among all the the r1 at the latest time among all the the r1 at the latest time among all the other instructions and if you look at other instructions and if you look at other instructions and if you look at the previous instructions have written the previous instructions have written the previous instructions have written the results in cycle five and cycle six the results in cycle five and cycle six the results in cycle five and cycle six so it's okay if we here write it in so it's okay if we here write it in so it's okay if we here write it in cycle seven so this is the solution to cycle seven so this is the solution to cycle seven so this is the solution to the quiz

## Removing False Dependencies： Part 2
Kind: captions Language: en okay so we have seen that okay so we have seen that okay so we have seen that we have to worry about the read after we have to worry about the read after we have to worry about the read after right dependencies because we actually right dependencies because we actually right dependencies because we actually have to feed the value that the have to feed the value that the have to feed the value that the instruction needs from a previous instruction needs from a previous instruction needs from a previous instruction we also have to worry about instruction we also have to worry about instruction we also have to worry about write after write dependencies possibly write after write dependencies possibly write after write dependencies possibly because we don't want we want the last because we don't want we want the last because we don't want we want the last instruction in the program order to be instruction in the program order to be instruction in the program order to be the last one to actually write the the last one to actually write the the last one to actually write the result and also anti-dependencies we result and also anti-dependencies we result and also anti-dependencies we need to worry about them too because we need to worry about them too because we need to worry about them too because we don't want to overwrite results before don't want to overwrite results before don't want to overwrite results before the instructions that need them had had the instructions that need them had had the instructions that need them had had a chance to use them a chance to use them a chance to use them so now we're going to talk about so now we're going to talk about so now we're going to talk about removing false dependencies removing false dependencies removing false dependencies what are false dependencies well we have what are false dependencies well we have what are false dependencies well we have read after write we have write after read after write we have write after read after write we have write after read and we can also have a write after read and we can also have a write after read and we can also have a write after a write these a write these a write these are called true dependencies are called true dependencies are called true dependencies because you actually have to obey them because you actually have to obey them because you actually have to obey them that's how the program computes some that's how the program computes some that's how the program computes some instructions produce values that are instructions produce values that are instructions produce values that are then used by other instructions and so then used by other instructions and so then used by other instructions and so on so the instructions that use values on so the instructions that use values on so the instructions that use values really depend on instructions that really depend on instructions that really depend on instructions that previously wrote the results previously wrote the results previously wrote the results in contrast in contrast in contrast these these these are called are called are called false false false or name or name or name dependencies dependencies dependencies why why why well because there is nothing well because there is nothing well because there is nothing fundamental about them they are fundamental about them they are fundamental about them they are dependencies just because we are using dependencies just because we are using dependencies just because we are using the same register for two different the same register for two different the same register for two different results so for example in a right after results so for example in a right after results so for example in a right after right dependence we have seen that two right dependence we have seen that two right dependence we have seen that two different instructions would write to different instructions would write to different instructions would write to the same register and that creates the the same register and that creates the the same register and that creates the right after eye dependence if the second right after eye dependence if the second right after eye dependence if the second of those two instructions of those two instructions of those two instructions we're using another register to write we're using another register to write we're using another register to write this value this value this value then there would not be no dependence we then there would not be no dependence we then there would not be no dependence we will see an example of this a little bit will see an example of this a little bit will see an example of this a little bit later later later but but but the idea is that while true dependencies the idea is that while true dependencies the idea is that while true dependencies you actually you actually you actually have to delay instructions in order to have to delay instructions in order to have to delay instructions in order to satisfy them with the name dependencies satisfy them with the name dependencies satisfy them with the name dependencies you could actually execute instructions you could actually execute instructions you could actually execute instructions earlier if only you could take care of earlier if only you could take care of earlier if only you could take care of you know kind of multiple values for the you know kind of multiple values for the you know kind of multiple values for the same register as we will see just in same register as we will see just in same register as we will see just in case just in a couple of seconds

## Duplicating Register Values： Part 2
Kind: captions Language: en so one of the approaches for eliminating so one of the approaches for eliminating so one of the approaches for eliminating false dependencies is to duplicate false dependencies is to duplicate false dependencies is to duplicate register values let's see how that would register values let's see how that would register values let's see how that would work we have an instruction here that work we have an instruction here that work we have an instruction here that adds R2 and R3 and puts the result in R1 adds R2 and R3 and puts the result in R1 adds R2 and R3 and puts the result in R1 then we have R1 minus R5 goes into R4 then we have R1 minus R5 goes into R4 then we have R1 minus R5 goes into R4 then we have R4 + 1 goes into R3 then we then we have R4 + 1 goes into R3 then we then we have R4 + 1 goes into R3 then we have R8 - R9 goes into R4 and sometime have R8 - R9 goes into R4 and sometime have R8 - R9 goes into R4 and sometime later we have an instruction that uses later we have an instruction that uses later we have an instruction that uses R4 here we have what happens in some R4 here we have what happens in some R4 here we have what happens in some cycle let's call it cycle 100 and in the cycle let's call it cycle 100 and in the cycle let's call it cycle 100 and in the next few cycles and let's assume that next few cycles and let's assume that next few cycles and let's assume that cycle 100 is when this instruction can cycle 100 is when this instruction can cycle 100 is when this instruction can execute now what happens is after we execute now what happens is after we execute now what happens is after we execute this instruction it can supply execute this instruction it can supply execute this instruction it can supply the register value R1 through forwarding the register value R1 through forwarding the register value R1 through forwarding to the next instruction so we have that to the next instruction so we have that to the next instruction so we have that this instruction can execute here and this instruction can execute here and this instruction can execute here and then R4 can be supplied to the next then R4 can be supplied to the next then R4 can be supplied to the next instruction so it can execute here how instruction so it can execute here how instruction so it can execute here how however this instruction which is doing however this instruction which is doing however this instruction which is doing R8 minus R9 could actually execute here R8 minus R9 could actually execute here R8 minus R9 could actually execute here so the problem with false dependencies so the problem with false dependencies so the problem with false dependencies occurs in that R4 is written by this and occurs in that R4 is written by this and occurs in that R4 is written by this and also this instruction so let's see which also this instruction so let's see which also this instruction so let's see which final value of R4 ends up in the final value of R4 ends up in the final value of R4 ends up in the register so that it is used much later register so that it is used much later register so that it is used much later here well if this is in execution stage here well if this is in execution stage here well if this is in execution stage let's say that we have the memory stage let's say that we have the memory stage let's say that we have the memory stage next and the right register stage next next and the right register stage next next and the right register stage next meanwhile this instruction goes through meanwhile this instruction goes through meanwhile this instruction goes through the m stage here and then write register the m stage here and then write register the m stage here and then write register here so as you can see we first write here so as you can see we first write here so as you can see we first write this R4 and only then write this R4 this R4 and only then write this R4 this R4 and only then write this R4 which means that the final value of which means that the final value of which means that the final value of register R4 and that is the value that register R4 and that is the value that register R4 and that is the value that will be read by this instruction is will be read by this instruction is will be read by this instruction is actually going to come from this actually going to come from this actually going to come from this instruction and that should not be instruction and that should not be instruction and that should not be happening so in order to fix this one happening so in order to fix this one happening so in order to fix this one way is to again duplicate register way is to again duplicate register way is to again duplicate register values so the idea is that when this values so the idea is that when this values so the idea is that when this instruction writes to the register R4 it instruction writes to the register R4 it instruction writes to the register R4 it writes to a version of register R4 and writes to a version of register R4 and writes to a version of register R4 and then another value is stored to another then another value is stored to another then another value is stored to another version of R4 but we remember both version of R4 but we remember both version of R4 but we remember both versions so that's why it's called versions so that's why it's called versions so that's why it's called duplication pretty much we don't store duplication pretty much we don't store duplication pretty much we don't store only one value for R4 we store all the only one value for R4 we store all the only one value for R4 we store all the possible values that R4 had and then an possible values that R4 had and then an possible values that R4 had and then an instruction that wants R4 will have to instruction that wants R4 will have to instruction that wants R4 will have to search among all the possible versions search among all the possible versions search among all the possible versions of R4 to find the one that is before it of R4 to find the one that is before it of R4 to find the one that is before it but the latest one before it so in this but the latest one before it so in this but the latest one before it so in this case we will search for R4 and find this case we will search for R4 and find this case we will search for R4 and find this one and this one and this is the latest one and this one and this is the latest one and this one and this is the latest one that we should be using that one that we should be using that one that we should be using that requires two versions of R4 to exist requires two versions of R4 to exist requires two versions of R4 to exist meanwhile this instruction here when it meanwhile this instruction here when it meanwhile this instruction here when it executes we'll look for versions of R4 executes we'll look for versions of R4 executes we'll look for versions of R4 and again find that there are really two and again find that there are really two and again find that there are really two versions that it might be using R4 here versions that it might be using R4 here versions that it might be using R4 here and R4 here but because this this one is and R4 here but because this this one is and R4 here but because this this one is coming after this instruction it should coming after this instruction it should coming after this instruction it should be ignored and thus we use this value so be ignored and thus we use this value so be ignored and thus we use this value so everything will be correct but it everything will be correct but it everything will be correct but it requires us to keep multiple versions of requires us to keep multiple versions of requires us to keep multiple versions of each registers which is really each registers which is really each registers which is really complicated

## Register Renaming： Part 2
Kind: captions Language: en so we have seen that the duplication of so we have seen that the duplication of so we have seen that the duplication of all the register values that have all all the register values that have all all the register values that have all ever been produced is a problem uh so we ever been produced is a problem uh so we ever been produced is a problem uh so we will look at registry naming which is will look at registry naming which is will look at registry naming which is another scheme that uh can provide uh another scheme that uh can provide uh another scheme that uh can provide uh relief from false dependencies but in a relief from false dependencies but in a relief from false dependencies but in a way that actually is more manageable how way that actually is more manageable how way that actually is more manageable how does the register naming work so does the register naming work so does the register naming work so registry naming separates the concept of registry naming separates the concept of registry naming separates the concept of architectural registers which are the architectural registers which are the architectural registers which are the registers that the programmers and the registers that the programmers and the registers that the programmers and the compilers use so this is when you write compilers use so this is when you write compilers use so this is when you write an instruction like add R1 R2 R3 the R1 an instruction like add R1 R2 R3 the R1 an instruction like add R1 R2 R3 the R1 R2 and R3 that you're referring to are R2 and R3 that you're referring to are R2 and R3 that you're referring to are the architectural registers in register the architectural registers in register the architectural registers in register naming there is also a concept of naming there is also a concept of naming there is also a concept of physical registers which is all the physical registers which is all the physical registers which is all the places that we can actually put a value places that we can actually put a value places that we can actually put a value in the processor so there is of course a in the processor so there is of course a in the processor so there is of course a place for kind of the latest value of R1 place for kind of the latest value of R1 place for kind of the latest value of R1 for example but there could be for example but there could be for example but there could be additional places where we could additional places where we could additional places where we could potentially store values that should be potentially store values that should be potentially store values that should be going to R1 and then what we do is as going to R1 and then what we do is as going to R1 and then what we do is as the processor reads instructions and the processor reads instructions and the processor reads instructions and figures out which registers they they're figures out which registers they they're figures out which registers they they're using and remember the program is using and remember the program is using and remember the program is referring to architecture registers as referring to architecture registers as referring to architecture registers as it's fetching instructions and decoding it's fetching instructions and decoding it's fetching instructions and decoding them it also does registry naming which them it also does registry naming which them it also does registry naming which pretty much means that it rewrites the pretty much means that it rewrites the pretty much means that it rewrites the program to use the physical registers so program to use the physical registers so program to use the physical registers so basically if it sees add R1 R2 R3 it's basically if it sees add R1 R2 R3 it's basically if it sees add R1 R2 R3 it's going to rename That Into You Know add R going to rename That Into You Know add R going to rename That Into You Know add R something else and so on in order to something else and so on in order to something else and so on in order to basically refer to the places where it basically refer to the places where it basically refer to the places where it has actually put those values and how has actually put those values and how has actually put those values and how does it know where the values are does it know where the values are does it know where the values are well it uses a table called register well it uses a table called register well it uses a table called register allocation table or rat and you should allocation table or rat and you should allocation table or rat and you should remember this because we will be very remember this because we will be very remember this because we will be very frequently referring to the rat because frequently referring to the rat because frequently referring to the rat because it's used very very often what the rat it's used very very often what the rat it's used very very often what the rat is is a table that says which physical is is a table that says which physical is is a table that says which physical registers has the value for which registers has the value for which registers has the value for which architecture register so for each of the architecture register so for each of the architecture register so for each of the architecture registers there will be an architecture registers there will be an architecture registers there will be an entry in this table and it will say what entry in this table and it will say what entry in this table and it will say what is the current location in the physical is the current location in the physical is the current location in the physical registers where the value for that registers where the value for that registers where the value for that architectural registers can be found

## RAT Example： Part 2
Kind: captions Language: en okay so this is what a rat looks like okay so this is what a rat looks like okay so this is what a rat looks like it's a table where for each it's a table where for each it's a table where for each architectural register for example for architectural register for example for architectural register for example for r0 it will say where do we keep r0 r0 it will say where do we keep r0 r0 it will say where do we keep r0 currently then for R1 there is another currently then for R1 there is another currently then for R1 there is another entry and so on so so this table really entry and so on so so this table really entry and so on so so this table really just stores where things are this is just stores where things are this is just stores where things are this is just for our convenience you know we just for our convenience you know we just for our convenience you know we kind of Number the entries but basically kind of Number the entries but basically kind of Number the entries but basically doesn't need to store this zero here doesn't need to store this zero here doesn't need to store this zero here it's simply that the entry number Z is it's simply that the entry number Z is it's simply that the entry number Z is for r0 the entry number one is for R1 for r0 the entry number one is for R1 for r0 the entry number one is for R1 Etc so now will have some instructions Etc so now will have some instructions Etc so now will have some instructions let's first look at this first let's first look at this first let's first look at this first instruction so let's say the processer instruction so let's say the processer instruction so let's say the processer is fetching this instruction what it is fetching this instruction what it is fetching this instruction what it does is for all the registers that this does is for all the registers that this does is for all the registers that this instruction needs to read it looks in instruction needs to read it looks in instruction needs to read it looks in the rat where those register can the rat where those register can the rat where those register can currently be found let's say that for R2 currently be found let's say that for R2 currently be found let's say that for R2 this is just saying P2 for now so this this is just saying P2 for now so this this is just saying P2 for now so this is physical register 2 which corresponds is physical register 2 which corresponds is physical register 2 which corresponds to R2 initially and let's say that it's to R2 initially and let's say that it's to R2 initially and let's say that it's P3 here in fact let's say that all of P3 here in fact let's say that all of P3 here in fact let's say that all of them are just like this so what the them are just like this so what the them are just like this so what the processor does does is it renames this processor does does is it renames this processor does does is it renames this instruction so it fetches it decodes it instruction so it fetches it decodes it instruction so it fetches it decodes it and then rewrites it so that the actual and then rewrites it so that the actual and then rewrites it so that the actual operation that will be done is going to operation that will be done is going to operation that will be done is going to be on physical registers P2 and P3 and be on physical registers P2 and P3 and be on physical registers P2 and P3 and now the question is where are we going now the question is where are we going now the question is where are we going to put our result well we're going to to put our result well we're going to to put our result well we're going to put our result in a new place for R1 so put our result in a new place for R1 so put our result in a new place for R1 so not in P1 so we will change the rat to not in P1 so we will change the rat to not in P1 so we will change the rat to put the result in some p17 or whatever put the result in some p17 or whatever put the result in some p17 or whatever okay so now we're going to say that this okay so now we're going to say that this okay so now we're going to say that this is this out goes to p7 and then we're is this out goes to p7 and then we're is this out goes to p7 and then we're going to use P2 and P3 for the inputs going to use P2 and P3 for the inputs going to use P2 and P3 for the inputs let's say that the next instruction is let's say that the next instruction is let's say that the next instruction is subtract R1 and R5 and put the result in subtract R1 and R5 and put the result in subtract R1 and R5 and put the result in R4 what the processor again will do is R4 what the processor again will do is R4 what the processor again will do is so it always follows the same procedure so it always follows the same procedure so it always follows the same procedure look at the rat for R1 and R5 to see look at the rat for R1 and R5 to see look at the rat for R1 and R5 to see where those values currently are and where those values currently are and where those values currently are and then rename the so we're going to look then rename the so we're going to look then rename the so we're going to look at R1 and it says p7 so this subtract is at R1 and it says p7 so this subtract is at R1 and it says p7 so this subtract is renamed into read p7 renamed into read p7 renamed into read p7 and P5 and put the result into a new and P5 and put the result into a new and P5 and put the result into a new place for R4 so we're going to change place for R4 so we're going to change place for R4 so we're going to change the r from now on to put the result in the r from now on to put the result in the r from now on to put the result in let's say let's say let's say p8 okay so I've added three more p8 okay so I've added three more p8 okay so I've added three more instructions and now we can rename them instructions and now we can rename them instructions and now we can rename them to so this xor will be renamed into an to so this xor will be renamed into an to so this xor will be renamed into an xor of R7 and R8 where are they well xor of R7 and R8 where are they well xor of R7 and R8 where are they well let's say they're in p7 and p8 and then let's say they're in p7 and p8 and then let's say they're in p7 and p8 and then we're going to write to R six we're not we're going to write to R six we're not we're going to write to R six we're not going to write to P5 that's where the pr going to write to P5 that's where the pr going to write to P5 that's where the pr previous value of R6 is we're going to previous value of R6 is we're going to previous value of R6 is we're going to write it to a new register p19 then write it to a new register p19 then write it to a new register p19 then we're going to take this multiply and we're going to take this multiply and we're going to take this multiply and remember you need to update the rat remember you need to update the rat remember you need to update the rat every time you produce a new value every time you produce a new value every time you produce a new value because that's where subsequent because that's where subsequent because that's where subsequent instructions will find the value for instructions will find the value for instructions will find the value for that register so we're going to again R8 that register so we're going to again R8 that register so we're going to again R8 and R9 still not change so they're going and R9 still not change so they're going and R9 still not change so they're going to be p8 and to be p8 and to be p8 and P9 the new value for R5 is going to go P9 the new value for R5 is going to go P9 the new value for R5 is going to go to P20 and then we have this add R8 and to P20 and then we have this add R8 and to P20 and then we have this add R8 and and R9 are still in p8 and P9 the new and R9 are still in p8 and P9 the new and R9 are still in p8 and P9 the new value of R4 is now going to be p21 now value of R4 is now going to be p21 now value of R4 is now going to be p21 now this is an already renamed value so this is an already renamed value so this is an already renamed value so we're going to rename it again and say we're going to rename it again and say we're going to rename it again and say p21 later on if there is an instruction p21 later on if there is an instruction p21 later on if there is an instruction that uses R4 so something something R4 that uses R4 so something something R4 that uses R4 so something something R4 it's going to be renamed into whatever it's going to be renamed into whatever it's going to be renamed into whatever p21 so the trick here is that we have p21 so the trick here is that we have p21 so the trick here is that we have seen that this instruction writes to R4 seen that this instruction writes to R4 seen that this instruction writes to R4 and this one also writes to R4 so there and this one also writes to R4 so there and this one also writes to R4 so there is a register name dependence between is a register name dependence between is a register name dependence between these two because they're both trying to these two because they're both trying to these two because they're both trying to write to R4 but when we rename them this write to R4 but when we rename them this write to R4 but when we rename them this way the dependence is gone because this way the dependence is gone because this way the dependence is gone because this is producing p18 this is producing p21 is producing p18 this is producing p21 is producing p18 this is producing p21 instructions among these that need to instructions among these that need to instructions among these that need to write the value so basically if write the value so basically if write the value so basically if something here was reading R4 it would something here was reading R4 it would something here was reading R4 it would get the value p18 instead because it's get the value p18 instead because it's get the value p18 instead because it's been it would be renamed to read p8 been it would be renamed to read p8 been it would be renamed to read p8 anything that follows this instruction anything that follows this instruction anything that follows this instruction that wants to read R4 would be renamed that wants to read R4 would be renamed that wants to read R4 would be renamed so that it reads the p21 so simply the so that it reads the p21 so simply the so that it reads the p21 so simply the use of R4 as a name for the value use of R4 as a name for the value use of R4 as a name for the value produced by this instruction and also produced by this instruction and also produced by this instruction and also for the value that produced by this for the value that produced by this for the value that produced by this instruction is what was causing for instruction is what was causing for instruction is what was causing for example the right after right dependence example the right after right dependence example the right after right dependence we eliminate that dependence because we eliminate that dependence because we eliminate that dependence because here the name will be different this here the name will be different this here the name will be different this value is going to be named p18 this one value is going to be named p18 this one value is going to be named p18 this one will be p21 there is no more dependence

## Register Renaming Quiz： Part 2
Kind: captions Language: en okay so now that we have seen how okay so now that we have seen how okay so now that we have seen how register naming Works let's try it register naming Works let's try it register naming Works let's try it ourselves so let's have a register ourselves so let's have a register ourselves so let's have a register naming naming naming quiz okay so we will have a rat here quiz okay so we will have a rat here quiz okay so we will have a rat here that will tell us where the values for that will tell us where the values for that will tell us where the values for R1 R2 R3 R4 R5 and R6 are and over here R1 R2 R3 R4 R5 and R6 are and over here R1 R2 R3 R4 R5 and R6 are and over here we will have a sequence of instructions we will have a sequence of instructions we will have a sequence of instructions as they're fetched and then we will need as they're fetched and then we will need as they're fetched and then we will need to write in what's renamed okay so here to write in what's renamed okay so here to write in what's renamed okay so here we have the initial state of the Rat we have the initial state of the Rat we have the initial state of the Rat which is simply R1 is mapped to P1 R2 to which is simply R1 is mapped to P1 R2 to which is simply R1 is mapped to P1 R2 to P2 Etc and here are our instructions in P2 Etc and here are our instructions in P2 Etc and here are our instructions in order to help you get started I'm going order to help you get started I'm going order to help you get started I'm going to rename the first instruction so after to rename the first instruction so after to rename the first instruction so after renaming it's still a renaming it's still a renaming it's still a multiplication R2 and R2 are P2 and P2 multiplication R2 and R2 are P2 and P2 multiplication R2 and R2 are P2 and P2 R2 is where we should put our value and R2 is where we should put our value and R2 is where we should put our value and remember we need to rename it in the red remember we need to rename it in the red remember we need to rename it in the red so it's going to be in so it's going to be in so it's going to be in p7 so this is what this renamed p7 so this is what this renamed p7 so this is what this renamed instruction looks like read registers instruction looks like read registers instruction looks like read registers physical registers P2 and P2 and put the physical registers P2 and P2 and put the physical registers P2 and P2 and put the result in p7 now it's your turn you need result in p7 now it's your turn you need result in p7 now it's your turn you need to both tell me what's renamed and also to both tell me what's renamed and also to both tell me what's renamed and also update the rat update the rat update the rat accordingly

## Register Renaming Quiz Solution： Part 2
Kind: captions Language: en Okay, so let's look at a register naming Okay, so let's look at a register naming Okay, so let's look at a register naming quiz solution. So we have seen the first quiz solution. So we have seen the first quiz solution. So we have seen the first instruction renamed. Let's now rename instruction renamed. Let's now rename instruction renamed. Let's now rename the second one. It's going to be an ad. the second one. It's going to be an ad. the second one. It's going to be an ad. R1 and R2 are going to be P1 and P7. R1 R1 and R2 are going to be P1 and P7. R1 R1 and R2 are going to be P1 and P7. R1 is where we put the result. Remember, is where we put the result. Remember, is where we put the result. Remember, you don't use this. You rename it to the you don't use this. You rename it to the you don't use this. You rename it to the next physical register. So we have P8. next physical register. So we have P8. next physical register. So we have P8. Then we have the multiplication of R4 Then we have the multiplication of R4 Then we have the multiplication of R4 and R4. This is P4 P4. You write the new and R4. This is P4 P4. You write the new and R4. This is P4 P4. You write the new value of R2 and that means that this P7 value of R2 and that means that this P7 value of R2 and that means that this P7 now becomes P9. Then we have the add of now becomes P9. Then we have the add of now becomes P9. Then we have the add of R3 and R2 which are P3 and P9 right as a R3 and R2 which are P3 and P9 right as a R3 and R2 which are P3 and P9 right as a result to R3 which becomes P10 before result to R3 which becomes P10 before result to R3 which becomes P10 before that happens. And then this that happens. And then this that happens. And then this multiplication will read R six and R multiplication will read R six and R multiplication will read R six and R six. That's our P6 and P6 here. produce six. That's our P6 and P6 here. produce six. That's our P6 and P6 here. produce a result and put it in R2. So now R2 a result and put it in R2. So now R2 a result and put it in R2. So now R2 gets to become P1. And finally we have gets to become P1. And finally we have gets to become P1. And finally we have this ad of R5 and R2. This is P5 and P1. this ad of R5 and R2. This is P5 and P1. this ad of R5 and R2. This is P5 and P1. Produce a result and put it in R5. So R5 Produce a result and put it in R5. So R5 Produce a result and put it in R5. So R5 now instead of P5 becomes P12. And now instead of P5 becomes P12. And now instead of P5 becomes P12. And that's the solution to the quiz.

## False Dependencies After Renaming： Part 2
Kind: captions Language: en so in the previous quiz we have seen so in the previous quiz we have seen so in the previous quiz we have seen that we can fetch these instructions that we can fetch these instructions that we can fetch these instructions then we rename them to eliminate false then we rename them to eliminate false then we rename them to eliminate false dependencies so let's see whether we dependencies so let's see whether we dependencies so let's see whether we have any false dependencies after have any false dependencies after have any false dependencies after renaming and what does that do to our renaming and what does that do to our renaming and what does that do to our IPC or instructions per cycle which is IPC or instructions per cycle which is IPC or instructions per cycle which is the opposite of CPI well here we had a the opposite of CPI well here we had a the opposite of CPI well here we had a true dependence and that true dependence true dependence and that true dependence true dependence and that true dependence is still there in a renamed program is still there in a renamed program is still there in a renamed program similarly here we have a true dependence similarly here we have a true dependence similarly here we have a true dependence and it is still there in our renamed and it is still there in our renamed and it is still there in our renamed program and then here we have another program and then here we have another program and then here we have another one and it is still there however here one and it is still there however here one and it is still there however here we also have an output dependence from we also have an output dependence from we also have an output dependence from R2 here to R2 here and then another one R2 here to R2 here and then another one R2 here to R2 here and then another one to here and we have an anti- dependence to here and we have an anti- dependence to here and we have an anti- dependence here and one here so if we look at all here and one here so if we look at all here and one here so if we look at all of the dependencies then basically this of the dependencies then basically this of the dependencies then basically this is a sequential program so for our is a sequential program so for our is a sequential program so for our fetched program we have the cpf1 which fetched program we have the cpf1 which fetched program we have the cpf1 which is is also the IPC of1 for a renamed is is also the IPC of1 for a renamed is is also the IPC of1 for a renamed program however this anti- dependence is program however this anti- dependence is program however this anti- dependence is no longer there this output dependence no longer there this output dependence no longer there this output dependence is no longer there there is no anti- is no longer there there is no anti- is no longer there there is no anti- dependence here and there is no output dependence here and there is no output dependence here and there is no output dependence here so really here we can dependence here so really here we can dependence here so really here we can execute this this and this instruction execute this this and this instruction execute this this and this instruction in the first cycle and this this and in the first cycle and this this and in the first cycle and this this and this instruction in the second cycle so this instruction in the second cycle so this instruction in the second cycle so for a rename program now we have that we for a rename program now we have that we for a rename program now we have that we can execute this this and this can execute this this and this can execute this this and this instruction three instructions in the instruction three instructions in the instruction three instructions in the first cycle first cycle first cycle these three can be done in the second these three can be done in the second these three can be done in the second cycle so overall we have six cycle so overall we have six cycle so overall we have six instructions over two cycles the cycles instructions over two cycles the cycles instructions over two cycles the cycles per instruction is two cycles for six per instruction is two cycles for six per instruction is two cycles for six instructions giving us a much lower cpfs instructions giving us a much lower cpfs instructions giving us a much lower cpfs only only only 0.33 or we can write it as IPC of three 0.33 or we can write it as IPC of three 0.33 or we can write it as IPC of three meaning we execute three instructions meaning we execute three instructions meaning we execute three instructions per cycle as you can see our renaming per cycle as you can see our renaming per cycle as you can see our renaming gave us a much better potential for gave us a much better potential for gave us a much better potential for executing this program quickly

## Instruction Level Parallelism (ILP)： Part 2
Kind: captions Language: en Okay, now that we have seen how to do Okay, now that we have seen how to do Okay, now that we have seen how to do registry naming and how to follow the registry naming and how to follow the registry naming and how to follow the true dependencies and so on, we we are true dependencies and so on, we we are true dependencies and so on, we we are ready to learn what the instruction ready to learn what the instruction ready to learn what the instruction level parallelism or ILP is. So we will level parallelism or ILP is. So we will level parallelism or ILP is. So we will now define what it is. So the ILP really now define what it is. So the ILP really now define what it is. So the ILP really can be defined as this IPC instructions can be defined as this IPC instructions can be defined as this IPC instructions per cycle when we have a processor that per cycle when we have a processor that per cycle when we have a processor that can do an entire instruction in one can do an entire instruction in one can do an entire instruction in one cycle. So it doesn't need a pipeline. It cycle. So it doesn't need a pipeline. It cycle. So it doesn't need a pipeline. It can fetch, decode, execute, write can fetch, decode, execute, write can fetch, decode, execute, write result, everything in just a single result, everything in just a single result, everything in just a single cycle for one instruction. Also, the cycle for one instruction. Also, the cycle for one instruction. Also, the processor can do any number of processor can do any number of processor can do any number of instructions in the same cycle. So instructions in the same cycle. So instructions in the same cycle. So theoretically, it could just fetch and theoretically, it could just fetch and theoretically, it could just fetch and execute your entire program in the one execute your entire program in the one execute your entire program in the one cycle, but it still has to obey true cycle, but it still has to obey true cycle, but it still has to obey true dependencies. So if one instruction dependencies. So if one instruction dependencies. So if one instruction depends on another, then they cannot be depends on another, then they cannot be depends on another, then they cannot be done in the same cycle. You have to done in the same cycle. You have to done in the same cycle. You have to first do one and then the one that needs first do one and then the one that needs first do one and then the one that needs its result. So as you can see ILP really its result. So as you can see ILP really its result. So as you can see ILP really is kind of what an ideal processor can is kind of what an ideal processor can is kind of what an ideal processor can do subject only to obeying true do subject only to obeying true do subject only to obeying true dependencies. How do we compute the ILP dependencies. How do we compute the ILP dependencies. How do we compute the ILP for a program? Well, we first rename all for a program? Well, we first rename all for a program? Well, we first rename all the registers. So we do register the registers. So we do register the registers. So we do register renaming first and renaming first and renaming first and then we pretend we execute. So what we then we pretend we execute. So what we then we pretend we execute. So what we do is we say okay in the first cycle do is we say okay in the first cycle do is we say okay in the first cycle these are the instructions that don't these are the instructions that don't these are the instructions that don't depend on anything so they can go. So in depend on anything so they can go. So in depend on anything so they can go. So in the next cycle we go through the same the next cycle we go through the same the next cycle we go through the same thing and so on until we run out of thing and so on until we run out of thing and so on until we run out of instructions to do at which point we instructions to do at which point we instructions to do at which point we know how many cycles it is. Now the one know how many cycles it is. Now the one know how many cycles it is. Now the one thing that you should remember about the thing that you should remember about the thing that you should remember about the ILP and that will help you realize what ILP and that will help you realize what ILP and that will help you realize what it is and how to do it is that ILP is a it is and how to do it is that ILP is a it is and how to do it is that ILP is a property of a program not of any property of a program not of any property of a program not of any particular processor. So basically we particular processor. So basically we particular processor. So basically we define the LP to be what would this define the LP to be what would this define the LP to be what would this program do as far as the IPC is program do as far as the IPC is program do as far as the IPC is concerned on an ideal processor. So it concerned on an ideal processor. So it concerned on an ideal processor. So it has nothing to do with what kind of has nothing to do with what kind of has nothing to do with what kind of processor you actually run it on. It has processor you actually run it on. It has processor you actually run it on. It has everything to do with just the everything to do with just the everything to do with just the dependencies in the program. So be dependencies in the program. So be dependencies in the program. So be mindful of that because for example it mindful of that because for example it mindful of that because for example it doesn't make really sense to say doesn't make really sense to say doesn't make really sense to say something like what's the ILP on this something like what's the ILP on this something like what's the ILP on this program on the I don't know Intel Core program on the I don't know Intel Core program on the I don't know Intel Core i7 processor because the ILP on the i7 processor because the ILP on the i7 processor because the ILP on the Intel Core i7 processor is beside the Intel Core i7 processor is beside the Intel Core i7 processor is beside the point because the LP is really the point because the LP is really the point because the LP is really the performance of this program on an ideal performance of this program on an ideal performance of this program on an ideal processor that has nothing to do with processor that has nothing to do with processor that has nothing to do with Intel i7. Okay. So just just remember Intel i7. Okay. So just just remember Intel i7. Okay. So just just remember that when you're asked to produce the that when you're asked to produce the that when you're asked to produce the LP, you should just consider the LP, you should just consider the LP, you should just consider the program. It doesn't matter what the program. It doesn't matter what the program. It doesn't matter what the processor looks like because the LP is processor looks like because the LP is processor looks like because the LP is computed for a perfect processor anyway. computed for a perfect processor anyway. computed for a perfect processor anyway. In contrast, you could say that the IPC In contrast, you could say that the IPC In contrast, you could say that the IPC of this program on a given processor is of this program on a given processor is of this program on a given processor is something else. But be mindful of this. something else. But be mindful of this. something else. But be mindful of this. Basically, the LP you only need to see Basically, the LP you only need to see Basically, the LP you only need to see the program. You don't really need to the program. You don't really need to the program. You don't really need to know what the processor looks like.


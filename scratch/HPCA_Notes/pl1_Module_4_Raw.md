# Playlist 1 Module 4 Raw Transcript

## One Cycle Quiz Part 3 Quiz Solution： Part 2
Kind: captions Language: en let's look at the solution for the part let's look at the solution for the part let's look at the solution for the part 30 for one cycle quiz which is about if 30 for one cycle quiz which is about if 30 for one cycle quiz which is about if this is the state at the beginning of this is the state at the beginning of this is the state at the beginning of the cycle then during this cycle which the cycle then during this cycle which the cycle then during this cycle which if any instruction will dispatch into if any instruction will dispatch into if any instruction will dispatch into the Lu here and this instruction we the Lu here and this instruction we the Lu here and this instruction we already saw in part two is going to free already saw in part two is going to free already saw in part two is going to free its reservation station and it's already its reservation station and it's already its reservation station and it's already executing so it will definitely not executing so it will definitely not executing so it will definitely not dispatch again this instruction here dispatch again this instruction here dispatch again this instruction here will capture its last remaining operon will capture its last remaining operon will capture its last remaining operon during this cycle so it becomes eligible during this cycle so it becomes eligible during this cycle so it becomes eligible for dispatch after that we said that in for dispatch after that we said that in for dispatch after that we said that in the same cycle capture and then this the same cycle capture and then this the same cycle capture and then this patch is allowed so this instruction patch is allowed so this instruction patch is allowed so this instruction here is a candidate for dispatching we here is a candidate for dispatching we here is a candidate for dispatching we are also going to issue this instruction are also going to issue this instruction are also going to issue this instruction here into a reservation station but here into a reservation station but here into a reservation station but because the issue and then this patch is because the issue and then this patch is because the issue and then this patch is not allowed in the same cycle that means not allowed in the same cycle that means not allowed in the same cycle that means that this instruction even if its that this instruction even if its that this instruction even if its operands were ready would not be operands were ready would not be operands were ready would not be eligible for dispatch now so the only eligible for dispatch now so the only eligible for dispatch now so the only instruction eligible for dispatch is instruction eligible for dispatch is instruction eligible for dispatch is this one which means that that's the one this one which means that that's the one this one which means that that's the one that gets to dispatch if more than one that gets to dispatch if more than one that gets to dispatch if more than one instruction were eligible for dispatch instruction were eligible for dispatch instruction were eligible for dispatch then we would have to pick which one then we would have to pick which one then we would have to pick which one dispatch is because we only have one dispatch is because we only have one dispatch is because we only have one execution unit and in order to do that execution unit and in order to do that execution unit and in order to do that we would actually need to be told what we would actually need to be told what we would actually need to be told what are the rules for picking among are the rules for picking among are the rules for picking among instructions fortunately we didn't need instructions fortunately we didn't need instructions fortunately we didn't need that because only one is eligible for that because only one is eligible for that because only one is eligible for dispatch

## Tomasulo's Algorithm Quiz： Part 2
Kind: captions Language: en so let's do another tomasulo's algorithm so let's do another tomasulo's algorithm so let's do another tomasulo's algorithm quiz which of the following is not true quiz which of the following is not true quiz which of the following is not true about tomasulo's algorithm it issues about tomasulo's algorithm it issues about tomasulo's algorithm it issues instructions in program order select instructions in program order select instructions in program order select this if it's not true it dispatches this if it's not true it dispatches this if it's not true it dispatches instructions in program order select instructions in program order select instructions in program order select this if this is not true it writes this if this is not true it writes this if this is not true it writes results in program order select this if results in program order select this if results in program order select this if this is not true

## Tomasulo's Algorithm Quiz Solution： Part 2
Kind: captions Language: en so let's look at our tomasulo's so let's look at our tomasulo's so let's look at our tomasulo's algorithm quiz solution does it issue algorithm quiz solution does it issue algorithm quiz solution does it issue instructions in program order yes it instructions in program order yes it instructions in program order yes it does instructions come from the does instructions come from the does instructions come from the instruction buffer in program order and instruction buffer in program order and instruction buffer in program order and that's the order in which we're going to that's the order in which we're going to that's the order in which we're going to issue them you cannot issue instructions issue them you cannot issue instructions issue them you cannot issue instructions out of order so this is true we don't out of order so this is true we don't out of order so this is true we don't select it because the question is which select it because the question is which select it because the question is which is not true it dispatches instructions is not true it dispatches instructions is not true it dispatches instructions in program order no it doesn't in program order no it doesn't in program order no it doesn't instructions can they be dispatched instructions can they be dispatched instructions can they be dispatched outside of program order that's why this outside of program order that's why this outside of program order that's why this is an out of order processor so this is is an out of order processor so this is is an out of order processor so this is definitely not true and then it writes definitely not true and then it writes definitely not true and then it writes results in program order results in program order results in program order not necessarily again results can be not necessarily again results can be not necessarily again results can be written in the order in which they are written in the order in which they are written in the order in which they are produced and it works for that so this produced and it works for that so this produced and it works for that so this is not true so basically the only one is not true so basically the only one is not true so basically the only one that is true is one the other two are that is true is one the other two are that is true is one the other two are not true so we selected the two and not true so we selected the two and not true so we selected the two and three as not true

## Load and Store Instructions： Part 2
Kind: captions Language: en now let's briefly consider just what now let's briefly consider just what now let's briefly consider just what happens to load and store instructions happens to load and store instructions happens to load and store instructions just like we had data dependencies that just like we had data dependencies that just like we had data dependencies that go through registers this is for example go through registers this is for example go through registers this is for example why we did register raming to eliminate why we did register raming to eliminate why we did register raming to eliminate the false dependencies through registers the false dependencies through registers the false dependencies through registers and why we are doing uh these and why we are doing uh these and why we are doing uh these reservation stations and so on in order reservation stations and so on in order reservation stations and so on in order to kind of obey properly the true to kind of obey properly the true to kind of obey properly the true dependencies there can be dependencies dependencies there can be dependencies dependencies there can be dependencies through memory because loads and stores through memory because loads and stores through memory because loads and stores in this case are the only instructions in this case are the only instructions in this case are the only instructions that can have dependencies through that can have dependencies through that can have dependencies through memory but there can be dependencies a memory but there can be dependencies a memory but there can be dependencies a read after write dependence occurs read after write dependence occurs read after write dependence occurs if there is for example a store word to if there is for example a store word to if there is for example a store word to some address in memory and then we do a some address in memory and then we do a some address in memory and then we do a load word from that address then there load word from that address then there load word from that address then there is definitely a read after write is definitely a read after write is definitely a read after write dependence between the store and the dependence between the store and the dependence between the store and the load basically the load uses the value load basically the load uses the value load basically the load uses the value put there by the store we can also have put there by the store we can also have put there by the store we can also have a right after read false dependence for a right after read false dependence for a right after read false dependence for example we can have the program that example we can have the program that example we can have the program that first needs to load something then store first needs to load something then store first needs to load something then store it if we reorder these then the load it if we reorder these then the load it if we reorder these then the load gets a value from the store not the gets a value from the store not the gets a value from the store not the value from before and finally we can value from before and finally we can value from before and finally we can have a right after right depend if there have a right after right depend if there have a right after right depend if there are two stores that store to the same are two stores that store to the same are two stores that store to the same address the last one should kind of address the last one should kind of address the last one should kind of leave the value at the end of that leave the value at the end of that leave the value at the end of that sequence but if we reorder them then the sequence but if we reorder them then the sequence but if we reorder them then the value in the memory ends up being the value in the memory ends up being the value in the memory ends up being the kind of stale value from the first of kind of stale value from the first of kind of stale value from the first of the stores so obviously dependencies the stores so obviously dependencies the stores so obviously dependencies through memory need to be obeyed or through memory need to be obeyed or through memory need to be obeyed or eliminated just like the registered ones eliminated just like the registered ones eliminated just like the registered ones do so what do we do about this do so what do we do about this do so what do we do about this dependencies in tomasulo's algorithm we dependencies in tomasulo's algorithm we dependencies in tomasulo's algorithm we do loads and stores in order basically do loads and stores in order basically do loads and stores in order basically we don't try to reorder them when as we we don't try to reorder them when as we we don't try to reorder them when as we issue these instructions we put them in issue these instructions we put them in issue these instructions we put them in the load or a sore queue and then we the load or a sore queue and then we the load or a sore queue and then we just follow the order of these just follow the order of these just follow the order of these instructions so for example a load instructions so for example a load instructions so for example a load doesn't execute if there is a previous doesn't execute if there is a previous doesn't execute if there is a previous store pending even though maybe the load store pending even though maybe the load store pending even though maybe the load is ready to go but the store still is is ready to go but the store still is is ready to go but the store still is waiting for something the second option waiting for something the second option waiting for something the second option is to actually identify dependencies is to actually identify dependencies is to actually identify dependencies between load stores Etc and then reorder between load stores Etc and then reorder between load stores Etc and then reorder them just like we did normal them just like we did normal them just like we did normal instructions Etc but this turns out to instructions Etc but this turns out to instructions Etc but this turns out to be more complicated than doing it for be more complicated than doing it for be more complicated than doing it for register BAS dependencies so we're not register BAS dependencies so we're not register BAS dependencies so we're not going to do that and that's why for going to do that and that's why for going to do that and that's why for tomasulo's algorithm they chose not to tomasulo's algorithm they chose not to tomasulo's algorithm they chose not to do this for memory instructions but they do this for memory instructions but they do this for memory instructions but they chose to do do it for other instructions chose to do do it for other instructions chose to do do it for other instructions modern processors actually do identify modern processors actually do identify modern processors actually do identify dependencies reorder Etc even for loads dependencies reorder Etc even for loads dependencies reorder Etc even for loads and stores but uh we will consider later and stores but uh we will consider later and stores but uh we will consider later how that is done so so for tomasulo's how that is done so so for tomasulo's how that is done so so for tomasulo's algorithm this was really kind of the algorithm this was really kind of the algorithm this was really kind of the you know option that they chose


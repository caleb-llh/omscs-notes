# Playlist 2 Module 4 Raw Transcript

## ROB Timing Quiz Solution 3： Part 3
Kind: captions Language: en okay let's work on the solution for the okay let's work on the solution for the okay let's work on the solution for the drop timing quiz the final question was drop timing quiz the final question was drop timing quiz the final question was when does when does when does the add here the last instruction commit the add here the last instruction commit the add here the last instruction commit to do that we have to figure out what to do that we have to figure out what to do that we have to figure out what happens with the last two instructions happens with the last two instructions happens with the last two instructions so can we issue the subtracting cycle so can we issue the subtracting cycle so can we issue the subtracting cycle five five five yes we can only one of the add subtract yes we can only one of the add subtract yes we can only one of the add subtract reservation stations is used at that reservation stations is used at that reservation stations is used at that time time time in cycle five it actually will be freed in cycle five it actually will be freed in cycle five it actually will be freed so from cycle six we actually have all so from cycle six we actually have all so from cycle six we actually have all the reservation stations in ads attract the reservation stations in ads attract the reservation stations in ads attract available but for sure one is available available but for sure one is available available but for sure one is available in cycle five in cycle five in cycle five and then the third one we can use it in and then the third one we can use it in and then the third one we can use it in cycle six in fact by that time this one cycle six in fact by that time this one cycle six in fact by that time this one is also available so if we can just is also available so if we can just is also available so if we can just issue these instructions issue these instructions issue these instructions the subtract here uses r2 and r5 r5 the subtract here uses r2 and r5 r5 the subtract here uses r2 and r5 r5 is not produced by any of the previous is not produced by any of the previous is not produced by any of the previous instructions but r2 is so we actually instructions but r2 is so we actually instructions but r2 is so we actually have to wait until cycle 7 before we can have to wait until cycle 7 before we can have to wait until cycle 7 before we can proceed to execution proceed to execution proceed to execution now now now because in cycle 7 other instructions because in cycle 7 other instructions because in cycle 7 other instructions are also beginning execution we have to are also beginning execution we have to are also beginning execution we have to figure out can we actually run this figure out can we actually run this figure out can we actually run this subtract now or do we have to wait for subtract now or do we have to wait for subtract now or do we have to wait for the next cycle so we want to run it in the next cycle so we want to run it in the next cycle so we want to run it in cycle 7 cycle 7 cycle 7 but remember this is a multiplication but remember this is a multiplication but remember this is a multiplication this is a subtract so they're using this is a subtract so they're using this is a subtract so they're using different execution units and they can different execution units and they can different execution units and they can begin in the same cycle if another for begin in the same cycle if another for begin in the same cycle if another for example add or subtract was beginning at example add or subtract was beginning at example add or subtract was beginning at seven then we would actually have to seven then we would actually have to seven then we would actually have to wait until eight but because these are wait until eight but because these are wait until eight but because these are different units we can begin at seven different units we can begin at seven different units we can begin at seven and we're going to try to broadcast our and we're going to try to broadcast our and we're going to try to broadcast our result at eight nobody else is trying to result at eight nobody else is trying to result at eight nobody else is trying to broadcast at eight so we will succeed at broadcast at eight so we will succeed at broadcast at eight so we will succeed at that that that and finally and finally and finally for the add here it's waiting for r4 and for the add here it's waiting for r4 and for the add here it's waiting for r4 and r3 r3 r3 r4 is produced by this subtract r3 is r4 is produced by this subtract r3 is r4 is produced by this subtract r3 is produced here so we need to wait for produced here so we need to wait for produced here so we need to wait for cycles five and eight and then begin in cycles five and eight and then begin in cycles five and eight and then begin in cycle nine cycle nine cycle nine we will try to broadcast in cycle ten we will try to broadcast in cycle ten we will try to broadcast in cycle ten and now the question is when do they and now the question is when do they and now the question is when do they commit commit commit remember that we can commit up to two remember that we can commit up to two remember that we can commit up to two instructions per cycle so what's going instructions per cycle so what's going instructions per cycle so what's going to happen is in cycle 10 we are going to to happen is in cycle 10 we are going to to happen is in cycle 10 we are going to also try to commit the subtract we are also try to commit the subtract we are also try to commit the subtract we are only committing this multiplication so only committing this multiplication so only committing this multiplication so far we can commit two instructions we far we can commit two instructions we far we can commit two instructions we will try to commit the next instruction will try to commit the next instruction will try to commit the next instruction so in cycle 10 we will actually commit so in cycle 10 we will actually commit so in cycle 10 we will actually commit this and now interestingly enough this this and now interestingly enough this this and now interestingly enough this ad is done just in time to try to commit ad is done just in time to try to commit ad is done just in time to try to commit it in cycle 11 so it's going to commit it in cycle 11 so it's going to commit it in cycle 11 so it's going to commit in cycle 11. in cycle 11. in cycle 11. so the final answer for the final part so the final answer for the final part so the final answer for the final part of the quiz is that the commit is of the quiz is that the commit is of the quiz is that the commit is happening in cycle 11 here

## Unified Reservation Stations： Part 3
Kind: captions Language: en now that we have seen how the arob based now that we have seen how the arob based now that we have seen how the arob based processor works when we have reservation processor works when we have reservation processor works when we have reservation stations for separate units separately stations for separate units separately stations for separate units separately let's talk about unified reservation let's talk about unified reservation let's talk about unified reservation stations so this is what we had so far stations so this is what we had so far stations so this is what we had so far we had three reservation stations for we had three reservation stations for we had three reservation stations for the ad unit and we had two for the the ad unit and we had two for the the ad unit and we had two for the multiply unit this also does subtracts multiply unit this also does subtracts multiply unit this also does subtracts this also does divides and we have seen this also does divides and we have seen this also does divides and we have seen it in at least one of our examples that it in at least one of our examples that it in at least one of our examples that we can run out of these reservation we can run out of these reservation we can run out of these reservation stations while we still have plenty of stations while we still have plenty of stations while we still have plenty of these left however we still could don't these left however we still could don't these left however we still could don't issue instructions basically once we issue instructions basically once we issue instructions basically once we cannot issue an instruction because it cannot issue an instruction because it cannot issue an instruction because it needs one of these reservation stations needs one of these reservation stations needs one of these reservation stations because issue needs to happen in order because issue needs to happen in order because issue needs to happen in order we cannot issue any of these either so we cannot issue any of these either so we cannot issue any of these either so until these reservation stations become until these reservation stations become until these reservation stations become available we cannot really use the available we cannot really use the available we cannot really use the available ones here note that these available ones here note that these available ones here note that these reservation stations are exactly the reservation stations are exactly the reservation stations are exactly the same except that this one is feeding same except that this one is feeding same except that this one is feeding this unit and this one is feeding this this unit and this one is feeding this this unit and this one is feeding this unit but the logic in them is exactly unit but the logic in them is exactly unit but the logic in them is exactly the same they're monitoring all of the the same they're monitoring all of the the same they're monitoring all of the results that are broadcast capturing results that are broadcast capturing results that are broadcast capturing results and then going to execution results and then going to execution results and then going to execution units so to improve Arab ability to use units so to improve Arab ability to use units so to improve Arab ability to use the expensive reservation stations we the expensive reservation stations we the expensive reservation stations we can go with the so-called unified can go with the so-called unified can go with the so-called unified reservation stations approach where all reservation stations approach where all reservation stations approach where all of the reservation stations are in one of the reservation stations are in one of the reservation stations are in one big array now when we need to issue we big array now when we need to issue we big array now when we need to issue we need to grab one of these reservation need to grab one of these reservation need to grab one of these reservation stations but it doesn't matter which stations but it doesn't matter which stations but it doesn't matter which unit it is for basically all of the unit it is for basically all of the unit it is for basically all of the reservation stations are equal so the reservation stations are equal so the reservation stations are equal so the benefit of this is that as long as there benefit of this is that as long as there benefit of this is that as long as there are any available reservation stations are any available reservation stations are any available reservation stations we can issue instructions the drawback we can issue instructions the drawback we can issue instructions the drawback here is that the logic for dispatching here is that the logic for dispatching here is that the logic for dispatching instructions into these execution units instructions into these execution units instructions into these execution units becomes a little bit more becomes a little bit more becomes a little bit more complicated here for issue into the complicated here for issue into the complicated here for issue into the multiplication we only needed to see multiplication we only needed to see multiplication we only needed to see whether one of these is available and If whether one of these is available and If whether one of these is available and If one or more are available select one one or more are available select one one or more are available select one meanwhile here we're selecting one of meanwhile here we're selecting one of meanwhile here we're selecting one of these three and sending it to the ad these three and sending it to the ad these three and sending it to the ad unit and if more than one are available unit and if more than one are available unit and if more than one are available we have to figure out just which one of we have to figure out just which one of we have to figure out just which one of the three here however every cycle we the three here however every cycle we the three here however every cycle we need to select one of these for the add need to select one of these for the add need to select one of these for the add and one of these for the multiply so we and one of these for the multiply so we and one of these for the multiply so we need to actually consider which one of need to actually consider which one of need to actually consider which one of these are ads to go here which one of these are ads to go here which one of these are ads to go here which one of these are multiplies to go here and then these are multiplies to go here and then these are multiplies to go here and then every cycle we need to select one of the every cycle we need to select one of the every cycle we need to select one of the ads and one of the multiplies to go ads and one of the multiplies to go ads and one of the multiplies to go there so the logic for dispatching there so the logic for dispatching there so the logic for dispatching becomes more complicated here because we becomes more complicated here because we becomes more complicated here because we have more reservation stations to look have more reservation stations to look have more reservation stations to look at and we also for each unit need to at and we also for each unit need to at and we also for each unit need to look at reservation stations that just look at reservation stations that just look at reservation stations that just have wrong things for that unit so we have wrong things for that unit so we have wrong things for that unit so we have to ignore them but again have to ignore them but again have to ignore them but again reservation stations are very very reservation stations are very very reservation stations are very very expensive so usually processors will use expensive so usually processors will use expensive so usually processors will use some variant of this organization rather some variant of this organization rather some variant of this organization rather than this one basically we don't have than this one basically we don't have than this one basically we don't have each unit having its own reservation each unit having its own reservation each unit having its own reservation stations typically a group of units will stations typically a group of units will stations typically a group of units will have a reservation station so that we have a reservation station so that we have a reservation station so that we can benefit from our reservation can benefit from our reservation can benefit from our reservation stations if we have them

## Superscalar： Part 3
Kind: captions Language: en we have seen a road based processor but we have seen a road based processor but we have seen a road based processor but this processor has not been a this processor has not been a this processor has not been a superscalar processor it was only superscalar processor it was only superscalar processor it was only issuing one instruction per cycle and issuing one instruction per cycle and issuing one instruction per cycle and even though we have seen a variant in even though we have seen a variant in even though we have seen a variant in one of the quizzes that commits up to one of the quizzes that commits up to one of the quizzes that commits up to two instructions per cycle the two instructions per cycle the two instructions per cycle the bottleneck will still be the issue so bottleneck will still be the issue so bottleneck will still be the issue so now let's look at the real superscalar now let's look at the real superscalar now let's look at the real superscalar processor what does it have to do first processor what does it have to do first processor what does it have to do first it needs to fetch more than one it needs to fetch more than one it needs to fetch more than one instruction per cycle instruction per cycle instruction per cycle when we are fetching we are really when we are fetching we are really when we are fetching we are really fetching more than one instructions fetching more than one instructions fetching more than one instructions worth of memory per cycle so if our worth of memory per cycle so if our worth of memory per cycle so if our instructions are for example four bytes instructions are for example four bytes instructions are for example four bytes and we want to fetch to our fetch really and we want to fetch to our fetch really and we want to fetch to our fetch really needs to supply us with eight bytes needs to supply us with eight bytes needs to supply us with eight bytes every cycle then we need to decode more every cycle then we need to decode more every cycle then we need to decode more than one instruction per cycle this than one instruction per cycle this than one instruction per cycle this amounts to basically having more than amounts to basically having more than amounts to basically having more than one decoder so every cycle the first one decoder so every cycle the first one decoder so every cycle the first decoder will be looking at the first decoder will be looking at the first decoder will be looking at the first instruction while the second one it will instruction while the second one it will instruction while the second one it will be looking at the second instruction we be looking at the second instruction we be looking at the second instruction we fetch etc next we have to issue more fetch etc next we have to issue more fetch etc next we have to issue more than one instruction per cycle remember than one instruction per cycle remember than one instruction per cycle remember that we need to issue instructions in that we need to issue instructions in that we need to issue instructions in order so every cycle we will consider order so every cycle we will consider order so every cycle we will consider the next instruction that should issue the next instruction that should issue the next instruction that should issue for issuing and if it can issue we will for issuing and if it can issue we will for issuing and if it can issue we will also consider the next instruction and also consider the next instruction and also consider the next instruction and if that one can issue we will consider if that one can issue we will consider if that one can issue we will consider the next one and so on so for example if the next one and so on so for example if the next one and so on so for example if you are trying to issue up to three you are trying to issue up to three you are trying to issue up to three instructions per cycle they still need instructions per cycle they still need instructions per cycle they still need to be three consecutive instructions and to be three consecutive instructions and to be three consecutive instructions and if one of them cannot issue then the if one of them cannot issue then the if one of them cannot issue then the following ones cannot issue either we following ones cannot issue either we following ones cannot issue either we want to be able to dispatch more than want to be able to dispatch more than want to be able to dispatch more than one instruction per cycle we have seen one instruction per cycle we have seen one instruction per cycle we have seen that if we have more than one execution that if we have more than one execution that if we have more than one execution unit we actually can dispatch one unit we actually can dispatch one unit we actually can dispatch one instruction to each of the units instruction to each of the units instruction to each of the units effectively we will already able to effectively we will already able to effectively we will already able to dispatch more than one instruction per dispatch more than one instruction per dispatch more than one instruction per cycle cycle cycle now the problem usually is that you have now the problem usually is that you have now the problem usually is that you have many more ads than multiplies for many more ads than multiplies for many more ads than multiplies for example so if you really want to finish example so if you really want to finish example so if you really want to finish more than one instruction per cycle and more than one instruction per cycle and more than one instruction per cycle and do that over many cycles probably we do that over many cycles probably we do that over many cycles probably we would have to have something like to add would have to have something like to add would have to have something like to add units and a multiplication unit so that units and a multiplication unit so that units and a multiplication unit so that even if we have only ads to do we would even if we have only ads to do we would even if we have only ads to do we would be able to issue and dispatch to per be able to issue and dispatch to per be able to issue and dispatch to per cycle on average then we need to be able cycle on average then we need to be able cycle on average then we need to be able to broadcast more than one result per to broadcast more than one result per to broadcast more than one result per cycle this involves not only having to cycle this involves not only having to cycle this involves not only having to broadcast buses or three broadcast buses broadcast buses or three broadcast buses broadcast buses or three broadcast buses but also every reservation station has but also every reservation station has but also every reservation station has to compare its tags that its waiting for to compare its tags that its waiting for to compare its tags that its waiting for to every one of the buses because in any to every one of the buses because in any to every one of the buses because in any given cycle we could have any of the given cycle we could have any of the given cycle we could have any of the buses producing our results so this buses producing our results so this buses producing our results so this immensely complicates reservation immensely complicates reservation immensely complicates reservation station basically the cost of each station basically the cost of each station basically the cost of each reservation station is proportional to reservation station is proportional to reservation station is proportional to the number of broadcasts we have to the number of broadcasts we have to the number of broadcasts we have to monitor monitor monitor if we had one let's say that some cost if we had one let's say that some cost if we had one let's say that some cost having three broadcasting said is going having three broadcasting said is going having three broadcasting said is going to be at least three times as costly and to be at least three times as costly and to be at least three times as costly and finally our commit logic needs to be finally our commit logic needs to be finally our commit logic needs to be capable of committing more than one capable of committing more than one capable of committing more than one instruction per cycle we have already instruction per cycle we have already instruction per cycle we have already seen in one of our quizzes that this is seen in one of our quizzes that this is seen in one of our quizzes that this is very similar to issuing in that for very similar to issuing in that for very similar to issuing in that for committing with we consider the first committing with we consider the first committing with we consider the first instruction that is not committed for instruction that is not committed for instruction that is not committed for commit if that one can commit we can commit if that one can commit we can commit if that one can commit we can consider the next one to commit in the consider the next one to commit in the consider the next one to commit in the same cycle and so on so it's not only same cycle and so on so it's not only same cycle and so on so it's not only that we have to consider three that we have to consider three that we have to consider three instructions for commit and check instructions for commit and check instructions for commit and check whether they are done we also have to whether they are done we also have to whether they are done we also have to make sure that we don't commit for make sure that we don't commit for make sure that we don't commit for example the third instruction we are example the third instruction we are example the third instruction we are considering unless both of the previous considering unless both of the previous considering unless both of the previous ones are committed and among these we ones are committed and among these we ones are committed and among these we really have to be worried about the really have to be worried about the really have to be worried about the weakest link meaning if we can fetch up weakest link meaning if we can fetch up weakest link meaning if we can fetch up to let's say four instructions per cycle to let's say four instructions per cycle to let's say four instructions per cycle the code up to four instructions per the code up to four instructions per the code up to four instructions per cycle is shown only one per cycle cycle is shown only one per cycle cycle is shown only one per cycle dispatch up to four broadcast up to four dispatch up to four broadcast up to four dispatch up to four broadcast up to four and commit up to four we will still be and commit up to four we will still be and commit up to four we will still be limited by the issue of only one so on limited by the issue of only one so on limited by the issue of only one so on average we can never produce more than average we can never produce more than average we can never produce more than one instruction finishing per cycle and one instruction finishing per cycle and one instruction finishing per cycle and committing because simply they have to committing because simply they have to committing because simply they have to go to issue one at a time if all of go to issue one at a time if all of go to issue one at a time if all of these are very very large and one of these are very very large and one of these are very very large and one of them is limited to let's say three then them is limited to let's say three then them is limited to let's say three then again we will not be able to average again we will not be able to average again we will not be able to average more than three instructions per cycle more than three instructions per cycle more than three instructions per cycle no matter how well the program is no matter how well the program is no matter how well the program is matching this organization so pretty matching this organization so pretty matching this organization so pretty much this is kind of like a pipe where much this is kind of like a pipe where much this is kind of like a pipe where the width of the pipe in each of the the width of the pipe in each of the the width of the pipe in each of the stage is determining the flow of the stage is determining the flow of the stage is determining the flow of the entire pipe pretty much the narrowest entire pipe pretty much the narrowest entire pipe pretty much the narrowest place in the pipe is what limits the place in the pipe is what limits the place in the pipe is what limits the flow of the instructions through it

## Terminology Confusion： Part 3
Kind: captions Language: en one of the things we also should discuss one of the things we also should discuss one of the things we also should discuss is the terminology confusion about the is the terminology confusion about the is the terminology confusion about the auto for the processors and how we name auto for the processors and how we name auto for the processors and how we name different parts of them we have been different parts of them we have been different parts of them we have been calling the stages of the execution calling the stages of the execution calling the stages of the execution issue was when we obtained the issue was when we obtained the issue was when we obtained the reservation station in the aerobie entry reservation station in the aerobie entry reservation station in the aerobie entry dispatch was when the instruction was dispatch was when the instruction was dispatch was when the instruction was selected for execution and commit was selected for execution and commit was selected for execution and commit was when the instruction has committed this when the instruction has committed this when the instruction has committed this terminology was introduced in the terminology was introduced in the terminology was introduced in the original tomasulo's algorithm and then original tomasulo's algorithm and then original tomasulo's algorithm and then commit was introduced in the first paper commit was introduced in the first paper commit was introduced in the first paper they talked about arabes so most of the they talked about arabes so most of the they talked about arabes so most of the academics are going to use this academics are going to use this academics are going to use this terminology so most of the research terminology so most of the research terminology so most of the research papers that are written will use papers that are written will use papers that are written will use something similar to this however some something similar to this however some something similar to this however some companies and some of the papers will companies and some of the papers will companies and some of the papers will for issue use the term not only issue for issue use the term not only issue for issue use the term not only issue but also allocate because this is when but also allocate because this is when but also allocate because this is when we allocate resources for the we allocate resources for the we allocate resources for the instructions so pretty much we allocate instructions so pretty much we allocate instructions so pretty much we allocate an reservation station and so on so an reservation station and so on so an reservation station and so on so they're going to call this allocate they're going to call this allocate they're going to call this allocate instead of issue and to confuse the instead of issue and to confuse the instead of issue and to confuse the exhibit of even further some call the exhibit of even further some call the exhibit of even further some call the issue part dispatch so we dispatch the issue part dispatch so we dispatch the issue part dispatch so we dispatch the instruction to reservation stations and instruction to reservation stations and instruction to reservation stations and the aerobie entries similarly what we the aerobie entries similarly what we the aerobie entries similarly what we have been calling dispatch some will have been calling dispatch some will have been calling dispatch some will call execute some will call it issue so call execute some will call it issue so call execute some will call it issue so we issued the instruction from a we issued the instruction from a we issued the instruction from a reservation station to execution as well reservation station to execution as well reservation station to execution as well as a residual dispatch which is we as a residual dispatch which is we as a residual dispatch which is we dispatch the instruction to execution dispatch the instruction to execution dispatch the instruction to execution and finally the comment is most often and finally the comment is most often and finally the comment is most often called commit but sometimes it's been called commit but sometimes it's been called commit but sometimes it's been called complete which introduces some called complete which introduces some called complete which introduces some confusion because actually the confusion because actually the confusion because actually the instruction completes execution well instruction completes execution well instruction completes execution well before it commits it's just that now the before it commits it's just that now the before it commits it's just that now the term complete would refer to commit if term complete would refer to commit if term complete would refer to commit if that's the nomenclature that they're that's the nomenclature that they're that's the nomenclature that they're using and then commit is also often using and then commit is also often using and then commit is also often called retire so we retire the called retire so we retire the called retire so we retire the instruction from the processor and instruction from the processor and instruction from the processor and because a lot of papers have been because a lot of papers have been because a lot of papers have been written by academics especially graduate written by academics especially graduate written by academics especially graduate students commit has also been called students commit has also been called students commit has also been called graduate you can guess why

## Out of Order： Part 3
Kind: captions Language: en finally let us discuss in our OB based finally let us discuss in our OB based finally let us discuss in our OB based out of order execution processor what out of order execution processor what out of order execution processor what really is out of order here so this is a really is out of order here so this is a really is out of order here so this is a processor pipeline let's say this is the processor pipeline let's say this is the processor pipeline let's say this is the fetch stage there is a decode stage we fetch stage there is a decode stage we fetch stage there is a decode stage we issue then we dispatch and then we have issue then we dispatch and then we have issue then we dispatch and then we have execution and then the write of the execution and then the write of the execution and then the write of the result which is the broadcast we have result which is the broadcast we have result which is the broadcast we have and then finally the commit so in this and then finally the commit so in this and then finally the commit so in this pipeline if this was an out of order pipeline if this was an out of order pipeline if this was an out of order processor keep in mind that not all of processor keep in mind that not all of processor keep in mind that not all of these stages are processing instructions these stages are processing instructions these stages are processing instructions out of order out of order out of order in fact from fetch up to issue we are in fact from fetch up to issue we are in fact from fetch up to issue we are processing instructions in order we have processing instructions in order we have processing instructions in order we have to fetch them in the order that they to fetch them in the order that they to fetch them in the order that they should appear in the program within should appear in the program within should appear in the program within decode them in the correct order and we decode them in the correct order and we decode them in the correct order and we fetch them if you remember in order this fetch them if you remember in order this fetch them if you remember in order this ensures that any dependencies we figure ensures that any dependencies we figure ensures that any dependencies we figure out here are the dependencies that are out here are the dependencies that are out here are the dependencies that are compatible with the original program compatible with the original program compatible with the original program order now that we have issued order now that we have issued order now that we have issued instructions to reservation stations and instructions to reservation stations and instructions to reservation stations and they're waiting for execution they're they're waiting for execution they're they're waiting for execution they're gonna execute in the order of data gonna execute in the order of data gonna execute in the order of data dependencies not necessarily in program dependencies not necessarily in program dependencies not necessarily in program order so the execution and writing of order so the execution and writing of order so the execution and writing of results are happening out of order and results are happening out of order and results are happening out of order and then again commit needs to do then again commit needs to do then again commit needs to do instructions in the proper program order instructions in the proper program order instructions in the proper program order because if you remember the order of because if you remember the order of because if you remember the order of committing is really the order in which committing is really the order in which committing is really the order in which we tell the outside world that we are we tell the outside world that we are we tell the outside world that we are finishing instructions so this has to finishing instructions so this has to finishing instructions so this has to happen in order to give the appearance happen in order to give the appearance happen in order to give the appearance that we are executing instructions in that we are executing instructions in that we are executing instructions in program order so when we have an out of program order so when we have an out of program order so when we have an out of order processor it really is doing many order processor it really is doing many order processor it really is doing many stages in order and only the execution stages in order and only the execution stages in order and only the execution and broadcast and things in between are and broadcast and things in between are and broadcast and things in between are happening out of order so the happening out of order so the happening out of order so the dispatching the execution the writing of dispatching the execution the writing of dispatching the execution the writing of results everything else is still in results everything else is still in results everything else is still in order in contrast what we would call an order in contrast what we would call an order in contrast what we would call an inorder processor just as these in order inorder processor just as these in order inorder processor just as these in order to but even an out of order processor to but even an out of order processor to but even an out of order processor does a lot of things in order

## In Order vs Out of Order Quiz： Part 3
Kind: captions Language: en so let's do an inorder versus so let's do an inorder versus so let's do an inorder versus out-of-order quiz so let's say that out-of-order quiz so let's say that out-of-order quiz so let's say that these are the stages of our processor we these are the stages of our processor we these are the stages of our processor we have fetch decode issue instructions have fetch decode issue instructions have fetch decode issue instructions then we dispatch them to execution we then we dispatch them to execution we then we dispatch them to execution we have two stages of execution so have two stages of execution so have two stages of execution so everything takes two cycles to execute everything takes two cycles to execute everything takes two cycles to execute followed by a broadcast of the result followed by a broadcast of the result followed by a broadcast of the result then we have a commit stage and then we then we have a commit stage and then we then we have a commit stage and then we have a separate stage after commit in have a separate stage after commit in have a separate stage after commit in which we release the Rob entry the which we release the Rob entry the which we release the Rob entry the question for you is to mark which of question for you is to mark which of question for you is to mark which of these stages are happening in program these stages are happening in program these stages are happening in program order and which one of these are out of order and which one of these are out of order and which one of these are out of order in out of order processor

## In Order vs Out of Order Quiz Solution： Part 3
Kind: captions Language: en execution

## ROB Outro： Part 3
Kind: captions Language: en in this lesson we discussed the reorder in this lesson we discussed the reorder in this lesson we discussed the reorder buffer which lets us correctly handle buffer which lets us correctly handle buffer which lets us correctly handle exceptions and branch mispredictions in exceptions and branch mispredictions in exceptions and branch mispredictions in out of order processors out of order processors out of order processors this is very important for actually this is very important for actually this is very important for actually making real programs work so all high making real programs work so all high making real programs work so all high performance processors today include performance processors today include performance processors today include such a reorder buffer

## Memory Ordering Intro： Part 3
Kind: captions Language: en we have seen that an out-of-order we have seen that an out-of-order we have seen that an out-of-order processor will track when one processor will track when one processor will track when one instruction uses a register produced by instruction uses a register produced by instruction uses a register produced by another but load and store instructions another but load and store instructions another but load and store instructions can access the same memory location can access the same memory location can access the same memory location without using the same register for it without using the same register for it without using the same register for it so let's see how to do them correctly in so let's see how to do them correctly in so let's see how to do them correctly in an out-of-order processor

## Memory Access Ordering： Part 3
Kind: captions Language: en so we have seen that using the RFP and so we have seen that using the RFP and so we have seen that using the RFP and something like tomasulo's algorithm we something like tomasulo's algorithm we something like tomasulo's algorithm we can enforce the order of dependencies on can enforce the order of dependencies on can enforce the order of dependencies on registers between instructions the registers between instructions the registers between instructions the question still remains what about memory question still remains what about memory question still remains what about memory access ordering we have load and store access ordering we have load and store access ordering we have load and store instructions do they really need to be instructions do they really need to be instructions do they really need to be done strictly in program order or can we done strictly in program order or can we done strictly in program order or can we do something about reordering them too do something about reordering them too do something about reordering them too so far we have eliminated control so far we have eliminated control so far we have eliminated control dependencies using branch prediction dependencies using branch prediction dependencies using branch prediction eliminated false dependencies on eliminated false dependencies on eliminated false dependencies on registers that these dependencies that registers that these dependencies that registers that these dependencies that go through registers and we have go through registers and we have go through registers and we have eliminated the false dependencies eliminated the false dependencies eliminated the false dependencies through register renaming next we learn through register renaming next we learn through register renaming next we learn how to obey read after write register how to obey read after write register how to obey read after write register dependencies or through dependencies and dependencies or through dependencies and dependencies or through dependencies and we do that through Tomasulo like we do that through Tomasulo like we do that through Tomasulo like scheduling so we have instructions scheduling so we have instructions scheduling so we have instructions waiting reservation stations until their waiting reservation stations until their waiting reservation stations until their dependencies have been satisfied and dependencies have been satisfied and dependencies have been satisfied and then they can proceed to execution out then they can proceed to execution out then they can proceed to execution out of program order note that the data of program order note that the data of program order note that the data dependence handling that we have done so dependence handling that we have done so dependence handling that we have done so far however is only for register far however is only for register far however is only for register dependencies that is when one dependencies that is when one dependencies that is when one instruction is producing a value in a instruction is producing a value in a instruction is producing a value in a register that another is going to be register that another is going to be register that another is going to be using but what about memory dependencies using but what about memory dependencies using but what about memory dependencies if we have a store and then allowed if we have a store and then allowed if we have a store and then allowed there could also be a dependence between there could also be a dependence between there could also be a dependence between the memory value written by the store the memory value written by the store the memory value written by the store and then the value read by the load if and then the value read by the load if and then the value read by the load if these two addresses are the same address these two addresses are the same address these two addresses are the same address then actually the load needs to get the then actually the load needs to get the then actually the load needs to get the value from the store and in which case value from the store and in which case value from the store and in which case we should be doing them in order however we should be doing them in order however we should be doing them in order however if these two addresses are not the same if these two addresses are not the same if these two addresses are not the same just like with registers we actually just like with registers we actually just like with registers we actually don't need to maintain the program order don't need to maintain the program order don't need to maintain the program order between these accesses however we have between these accesses however we have between these accesses however we have not seen anything that can handle out of not seen anything that can handle out of not seen anything that can handle out of order execution of memory instructions

## When does Memory Write Happen： Part 3
Kind: captions Language: en the first point we need to consider is the first point we need to consider is the first point we need to consider is for store instructions when does the for store instructions when does the for store instructions when does the memory right actually happen and it memory right actually happen and it memory right actually happen and it happens at commit we have to do rights happens at commit we have to do rights happens at commit we have to do rights to memory at commit because it is unsafe to memory at commit because it is unsafe to memory at commit because it is unsafe to update memory at any point before the to update memory at any point before the to update memory at any point before the instruction commits an instruction that instruction commits an instruction that instruction commits an instruction that has not yet committed is subject to has not yet committed is subject to has not yet committed is subject to being canceled for example because of a being canceled for example because of a being canceled for example because of a branch misprediction or exception so branch misprediction or exception so branch misprediction or exception so doing a memory right prematurely might doing a memory right prematurely might doing a memory right prematurely might mean that later we have to undo accesses mean that later we have to undo accesses mean that later we have to undo accesses to memory meaning we have to kind of put to memory meaning we have to kind of put to memory meaning we have to kind of put back the old value of a memory location back the old value of a memory location back the old value of a memory location which is extremely difficult to do so which is extremely difficult to do so which is extremely difficult to do so what we will do is we will delay memory what we will do is we will delay memory what we will do is we will delay memory rights and stores until commit but does rights and stores until commit but does rights and stores until commit but does it mean that the load has to also wait it mean that the load has to also wait it mean that the load has to also wait until commit before it can get data until commit before it can get data until commit before it can get data because only then the data is in memory because only then the data is in memory because only then the data is in memory so the question is if we write values on so the question is if we write values on so the question is if we write values on stores that commit where does the load stores that commit where does the load stores that commit where does the load get data and we want our loads to get get data and we want our loads to get get data and we want our loads to get data as early as possible so that we can data as early as possible so that we can data as early as possible so that we can finish those loads and supply that data finish those loads and supply that data finish those loads and supply that data to the subsequent instructions that to the subsequent instructions that to the subsequent instructions that depend on the load for this purpose we depend on the load for this purpose we depend on the load for this purpose we introduce a structure called load/store introduce a structure called load/store introduce a structure called load/store queue and that is where we will keep all queue and that is where we will keep all queue and that is where we will keep all loads and stores and we will next see loads and stores and we will next see loads and stores and we will next see how loads get data from the stores

## Load Store Queue Part 1： Part 3
Kind: captions Language: en So because the loads in our processor So because the loads in our processor So because the loads in our processor need to be done as soon as possible need to be done as soon as possible need to be done as soon as possible while the stores do not write to memory while the stores do not write to memory while the stores do not write to memory until commit. We said we need a loadster until commit. We said we need a loadster until commit. We said we need a loadster Q so that we can supply the values from Q so that we can supply the values from Q so that we can supply the values from stores to loads. stores to loads. stores to loads. This loster Q or LSQ is a structure just This loster Q or LSQ is a structure just This loster Q or LSQ is a structure just like the ROB meaning we put things there like the ROB meaning we put things there like the ROB meaning we put things there in order and remove them at commit but in order and remove them at commit but in order and remove them at commit but we only put load and store instructions we only put load and store instructions we only put load and store instructions there. So there is a bit that says which there. So there is a bit that says which there. So there is a bit that says which instruction do we have load or store. instruction do we have load or store. instruction do we have load or store. There is a field for what is the address There is a field for what is the address There is a field for what is the address this instruction is accessing and there this instruction is accessing and there this instruction is accessing and there is a field for the value that the is a field for the value that the is a field for the value that the instruction should be storing or instruction should be storing or instruction should be storing or loading. There is also a field for this loading. There is also a field for this loading. There is also a field for this instruction has been completed. Now instruction has been completed. Now instruction has been completed. Now let's suppose we have this program and let's suppose we have this program and let's suppose we have this program and suppose that the loq is initially empty. suppose that the loq is initially empty. suppose that the loq is initially empty. So as we said things are going to be So as we said things are going to be So as we said things are going to be placed here in program order. So the placed here in program order. So the placed here in program order. So the first instruction here is going to be a first instruction here is going to be a first instruction here is going to be a load. Let's say that we quickly compute load. Let's say that we quickly compute load. Let's say that we quickly compute the address and it's the address and it's the address and it's 104. This load because there are no 104. This load because there are no 104. This load because there are no previous stores will go to memory. The previous stores will go to memory. The previous stores will go to memory. The next instruction here is a store. Let's next instruction here is a store. Let's next instruction here is a store. Let's say that it computes the address of 204. say that it computes the address of 204. say that it computes the address of 204. Let's say that the value is 15 and we Let's say that the value is 15 and we Let's say that the value is 15 and we mark it done. And let's say that this mark it done. And let's say that this mark it done. And let's say that this instruction is kind of delayed, so it instruction is kind of delayed, so it instruction is kind of delayed, so it doesn't return yet. So we still have all doesn't return yet. So we still have all doesn't return yet. So we still have all of these in the loster Q. The next load of these in the loster Q. The next load of these in the loster Q. The next load let's say it eventually computes the let's say it eventually computes the let's say it eventually computes the address of 204. For every load when it address of 204. For every load when it address of 204. For every load when it computes the address, we check in the computes the address, we check in the computes the address, we check in the load store if any store matches that load store if any store matches that load store if any store matches that address. If there is no matching store address. If there is no matching store address. If there is no matching store like equals case for this load because like equals case for this load because like equals case for this load because there were no stores, we go to memory. there were no stores, we go to memory. there were no stores, we go to memory. If there is a matching store, we do not If there is a matching store, we do not If there is a matching store, we do not go to memory. Instead we do what is go to memory. Instead we do what is go to memory. Instead we do what is called the store to load forwarding called the store to load forwarding called the store to load forwarding where we simply take the value from the where we simply take the value from the where we simply take the value from the store and that is the value that the store and that is the value that the store and that is the value that the load produces and we never go to memory. load produces and we never go to memory. load produces and we never go to memory. So again this is called store to load So again this is called store to load So again this is called store to load forwarding. Now, of course, this assumes forwarding. Now, of course, this assumes forwarding. Now, of course, this assumes that at the time we want to do this that at the time we want to do this that at the time we want to do this load, the stores have already been load, the stores have already been load, the stores have already been completed and we now know what the completed and we now know what the completed and we now know what the addresses are and if we need to get the addresses are and if we need to get the addresses are and if we need to get the value, we know what the value This

## Load Store Queue Part 2： Part 3
Kind: captions Language: en it is entirely possible that for example it is entirely possible that for example it is entirely possible that for example for this store and this load the store for this store and this load the store for this store and this load the store doesn't have the address at the time doesn't have the address at the time doesn't have the address at the time when the load produces the address and when the load produces the address and when the load produces the address and now what do we do the options for this now what do we do the options for this now what do we do the options for this situation are make loads and stores situation are make loads and stores situation are make loads and stores execute in order so this load is not execute in order so this load is not execute in order so this load is not allowed to execute until all previous allowed to execute until all previous allowed to execute until all previous instructions have been completed that instructions have been completed that instructions have been completed that guarantees that at the time when we want guarantees that at the time when we want guarantees that at the time when we want to figure out what to do with this load to figure out what to do with this load to figure out what to do with this load we have all the store addresses and we have all the store addresses and we have all the store addresses and values so we can have a situation just values so we can have a situation just values so we can have a situation just like this one here but of course that like this one here but of course that like this one here but of course that orders these instructions so for example orders these instructions so for example orders these instructions so for example loads cannot really proceed so for loads cannot really proceed so for loads cannot really proceed so for example if this load has been a cache example if this load has been a cache example if this load has been a cache miss and takes a long time all of these miss and takes a long time all of these miss and takes a long time all of these instructions will have to wait for that instructions will have to wait for that instructions will have to wait for that the waiting can be reduced by not the waiting can be reduced by not the waiting can be reduced by not waiting for all of these instructions to waiting for all of these instructions to waiting for all of these instructions to complete instead we just wait for all complete instead we just wait for all complete instead we just wait for all previous store addresses to be known previous store addresses to be known previous store addresses to be known once we know all the store addresses our once we know all the store addresses our once we know all the store addresses our load address can be compared with them load address can be compared with them load address can be compared with them and then we know what to do we either go and then we know what to do we either go and then we know what to do we either go to memory if nothing matches we get the to memory if nothing matches we get the to memory if nothing matches we get the value from the store if it is available value from the store if it is available value from the store if it is available or we wait for the store to produce the or we wait for the store to produce the or we wait for the store to produce the value if the address matches but the value if the address matches but the value if the address matches but the store is not done yet with its value the store is not done yet with its value the store is not done yet with its value the third option is the most aggressive and third option is the most aggressive and third option is the most aggressive and it is to just let the load go anyway so it is to just let the load go anyway so it is to just let the load go anyway so pretty much when we produce the address pretty much when we produce the address pretty much when we produce the address of the load we check the addresses of of the load we check the addresses of of the load we check the addresses of the previous stores if one of them does the previous stores if one of them does the previous stores if one of them does match we wait for it because clearly match we wait for it because clearly match we wait for it because clearly going to memory will be wrong but if going to memory will be wrong but if going to memory will be wrong but if none of them match we ignore the fact none of them match we ignore the fact none of them match we ignore the fact that some of them might match once we that some of them might match once we that some of them might match once we lose all their addresses so in this lose all their addresses so in this lose all their addresses so in this situation this load will end up going to situation this load will end up going to situation this load will end up going to memory now two things can happen one is memory now two things can happen one is memory now two things can happen one is this store resolves to something like 74 this store resolves to something like 74 this store resolves to something like 74 in which case we confirm that this was in which case we confirm that this was in which case we confirm that this was done ok or the store might actually get done ok or the store might actually get done ok or the store might actually get the address of 174 in which case this the address of 174 in which case this the address of 174 in which case this load loaded the wrong value from memory load loaded the wrong value from memory load loaded the wrong value from memory in that case we need to recover this in that case we need to recover this in that case we need to recover this load loaded the wrong value and possibly load loaded the wrong value and possibly load loaded the wrong value and possibly supplied it to other instructions so in supplied it to other instructions so in supplied it to other instructions so in this case the stores this case the stores this case the stores when they produce the address would when they produce the address would when they produce the address would check whether any of the loads that check whether any of the loads that check whether any of the loads that follow them match the address and we're follow them match the address and we're follow them match the address and we're done and if so then request a redoing of done and if so then request a redoing of done and if so then request a redoing of the load and all of the subsequent the load and all of the subsequent the load and all of the subsequent instructions to fix the problem of the instructions to fix the problem of the instructions to fix the problem of the load loading the wrong value most modern load loading the wrong value most modern load loading the wrong value most modern processors today actually go with the go processors today actually go with the go processors today actually go with the go anyway option because it produces the anyway option because it produces the anyway option because it produces the best performance if we let the loads go best performance if we let the loads go best performance if we let the loads go without waiting for store addresses it without waiting for store addresses it without waiting for store addresses it turns out that most of the time we are turns out that most of the time we are turns out that most of the time we are right so this most of the time right so this most of the time right so this most of the time successfully loads the value earlier successfully loads the value earlier successfully loads the value earlier then if we waited but occasionally we then if we waited but occasionally we then if we waited but occasionally we have a cost of the recovery because we have a cost of the recovery because we have a cost of the recovery because we were wrong about this and then there are were wrong about this and then there are were wrong about this and then there are entire schemes that try to predict when entire schemes that try to predict when entire schemes that try to predict when is this going to happen is this going to happen is this going to happen let the loads go if it's unlikely and let the loads go if it's unlikely and let the loads go if it's unlikely and for loads that often conflict with some for loads that often conflict with some for loads that often conflict with some store wait for that store to produce the store wait for that store to produce the store wait for that store to produce the address before we proceed

## In Order Load Store Execution： Part 3
Kind: captions Language: en our first solution to this problem is our first solution to this problem is our first solution to this problem is that we don't do out of order load store that we don't do out of order load store that we don't do out of order load store execution instead we do in order load execution instead we do in order load execution instead we do in order load store execution this is what happens in store execution this is what happens in store execution this is what happens in that case the load is a miss the ad is that case the load is a miss the ad is that case the load is a miss the ad is stalled the store is stalled because of stalled the store is stalled because of stalled the store is stalled because of that cuz they're both both waiting for that cuz they're both both waiting for that cuz they're both both waiting for the load to eventually finish the the load to eventually finish the the load to eventually finish the subtraction gets done we now know the subtraction gets done we now know the subtraction gets done we now know the address for this load at that point in address for this load at that point in address for this load at that point in the load store Q we will check whether the load store Q we will check whether the load store Q we will check whether there are any preceding instructions there are any preceding instructions there are any preceding instructions that might eventually resolve to the that might eventually resolve to the that might eventually resolve to the same address or even worse we check same address or even worse we check same address or even worse we check whether there is just all preceding whether there is just all preceding whether there is just all preceding instructions are done because this load instructions are done because this load instructions are done because this load is not done we're not going to consider is not done we're not going to consider is not done we're not going to consider this store for execution that is for this store for execution that is for this store for execution that is for producing the value for this address and producing the value for this address and producing the value for this address and as a result we're also not going to do as a result we're also not going to do as a result we're also not going to do this load so pretty much what now this load so pretty much what now this load so pretty much what now happens is this load here doesn't go to happens is this load here doesn't go to happens is this load here doesn't go to memory although it knows where the memory although it knows where the memory although it knows where the address is eventually this Miss is address is eventually this Miss is address is eventually this Miss is resolved we compute R7 and then very resolved we compute R7 and then very resolved we compute R7 and then very quickly quickly quickly afterwards we we're going to compute the afterwards we we're going to compute the afterwards we we're going to compute the address for the store so now what we address for the store so now what we address for the store so now what we have is this load gets done this have is this load gets done this have is this load gets done this instruction was already done eventually instruction was already done eventually instruction was already done eventually the ad gets done and eventually the the ad gets done and eventually the the ad gets done and eventually the store gets done the store is considered store gets done the store is considered store gets done the store is considered done not when it commits but when it done not when it commits but when it done not when it commits but when it actually knows the address and knows the actually knows the address and knows the actually knows the address and knows the value that is going to go to that value that is going to go to that value that is going to go to that address at that point only this load is address at that point only this load is address at that point only this load is allowed to proceed because at that point allowed to proceed because at that point allowed to proceed because at that point it can just go up and check whether any it can just go up and check whether any it can just go up and check whether any stores produce the value for the address stores produce the value for the address stores produce the value for the address that we are interested in and if so just that we are interested in and if so just that we are interested in and if so just grab the value so the load can only grab the value so the load can only grab the value so the load can only finish after this store has finished and finish after this store has finished and finish after this store has finished and this store will not do anything until this store will not do anything until this store will not do anything until this load is done Etc so really we are this load is done Etc so really we are this load is done Etc so really we are executing most of the instructions out executing most of the instructions out executing most of the instructions out of order here but loads and stores are of order here but loads and stores are of order here but loads and stores are still proceeding in order of course you still proceeding in order of course you still proceeding in order of course you can see that this is suboptimal if this can see that this is suboptimal if this can see that this is suboptimal if this store doesn't store to the same address store doesn't store to the same address store doesn't store to the same address we have been waiting here for a very we have been waiting here for a very we have been waiting here for a very long time and thus we have delayed all long time and thus we have delayed all long time and thus we have delayed all of the destructions that depend on the of the destructions that depend on the of the destructions that depend on the load worse if the load would have been a load worse if the load would have been a load worse if the load would have been a cash Miss we were sitting there not cash Miss we were sitting there not cash Miss we were sitting there not trying to order things from memory until trying to order things from memory until trying to order things from memory until many many cycles later potentially and many many cycles later potentially and many many cycles later potentially and only then we realize we have a cash Miss only then we realize we have a cash Miss only then we realize we have a cash Miss and have to wait a many many cycles more and have to wait a many many cycles more and have to wait a many many cycles more so this is of course not a very high so this is of course not a very high so this is of course not a very high performance solution but it is a correct performance solution but it is a correct performance solution but it is a correct solution

## Memory Ordering Quiz： Part 3
Kind: captions Language: en so now that we have seen an example of so now that we have seen an example of so now that we have seen an example of memory ordering let's do a memory memory ordering let's do a memory memory ordering let's do a memory ordering quiz let's say we have a load a ordering quiz let's say we have a load a ordering quiz let's say we have a load a store another pair of floating store store another pair of floating store store another pair of floating store let's say that all of the loads are let's say that all of the loads are let's say that all of the loads are cache misses so what happens is in cycle cache misses so what happens is in cycle cache misses so what happens is in cycle 1 we begin executing this load and send 1 we begin executing this load and send 1 we begin executing this load and send it to memory and it's gonna take 40 it to memory and it's gonna take 40 it to memory and it's gonna take 40 cycles for it to return a value so in cycles for it to return a value so in cycles for it to return a value so in cycle 1 we send the request to memory in cycle 1 we send the request to memory in cycle 1 we send the request to memory in cycle 41 we finally get the value and cycle 41 we finally get the value and cycle 41 we finally get the value and now we have our r1 let's say that we are now we have our r1 let's say that we are now we have our r1 let's say that we are doing stores and loads out of order so doing stores and loads out of order so doing stores and loads out of order so although this store really cannot get although this store really cannot get although this store really cannot get the value until cycle 41 at least we the value until cycle 41 at least we the value until cycle 41 at least we proceed with this load so in cycle 2 we proceed with this load so in cycle 2 we proceed with this load so in cycle 2 we send it to memory in cycle 42 we'll get send it to memory in cycle 42 we'll get send it to memory in cycle 42 we'll get the value this store waits for that the value this store waits for that the value this store waits for that value but this load is sent to memory in value but this load is sent to memory in value but this load is sent to memory in cycle 3 and in cycle 43 it gets the cycle 3 and in cycle 43 it gets the cycle 3 and in cycle 43 it gets the value now what happens in cycle 42 is value now what happens in cycle 42 is value now what happens in cycle 42 is this store is finally done and then in this store is finally done and then in this store is finally done and then in cycle 43 this store is done and in cycle cycle 43 this store is done and in cycle cycle 43 this store is done and in cycle 44 this cycle is done so overall we have 44 this cycle is done so overall we have 44 this cycle is done so overall we have taken 44 cycles to do this but we have taken 44 cycles to do this but we have taken 44 cycles to do this but we have been doing loads prematurely as far as been doing loads prematurely as far as been doing loads prematurely as far as the stores are concerned because if this the stores are concerned because if this the stores are concerned because if this r2 + 4 is equal to r3 + 0 then actually r2 + 4 is equal to r3 + 0 then actually r2 + 4 is equal to r3 + 0 then actually this load should have waited so we this load should have waited so we this load should have waited so we decided not to be totally safe and go decided not to be totally safe and go decided not to be totally safe and go and then we are assuming that there will and then we are assuming that there will and then we are assuming that there will be some recovery mechanism your task is be some recovery mechanism your task is be some recovery mechanism your task is to figure out when is each of these to figure out when is each of these to figure out when is each of these instructions sent to memory and when do instructions sent to memory and when do instructions sent to memory and when do we get the result from memory and what's we get the result from memory and what's we get the result from memory and what's the final cycle count if we do things in the final cycle count if we do things in the final cycle count if we do things in order order order so if loads and stores have to happen in so if loads and stores have to happen in so if loads and stores have to happen in order


# Playlist 1 Module 2 Raw Transcript

## ILP Example： Part 2
Kind: captions Language: en so now that we know what ilp is let's so now that we know what ilp is let's so now that we know what ilp is let's look at an example of computing the lp look at an example of computing the lp look at an example of computing the lp just so that we can be on the same page just so that we can be on the same page just so that we can be on the same page about that first we need a program about that first we need a program about that first we need a program so let's say we have this program we so let's say we have this program we so let's say we have this program we have already renamed it so that only the have already renamed it so that only the have already renamed it so that only the true dependencies remain so now let's true dependencies remain so now let's true dependencies remain so now let's look for those true dependencies look for those true dependencies look for those true dependencies we write to p10 we write to p10 we write to p10 this instruction doesn't use p10 this instruction doesn't use p10 this instruction doesn't use p10 actually the first one that uses p10 is actually the first one that uses p10 is actually the first one that uses p10 is this one this one this one we write to p6 nothing uses p6 we write to p6 nothing uses p6 we write to p6 nothing uses p6 we write to p5 we write to p5 we write to p5 this one uses p5 so there is a this one uses p5 so there is a this one uses p5 so there is a dependence basically here dependence basically here dependence basically here and another one here and another one here and another one here and then here we produce p4 so we only and then here we produce p4 so we only and then here we produce p4 so we only have really these two true dependencies have really these two true dependencies have really these two true dependencies so now let's look at like what can so now let's look at like what can so now let's look at like what can execute in which cycle to do that the execute in which cycle to do that the execute in which cycle to do that the best way is to just kind of check off best way is to just kind of check off best way is to just kind of check off instructions and counter cycles so in instructions and counter cycles so in instructions and counter cycles so in the first cycle we can do this the first cycle we can do this the first cycle we can do this this this this this this this and this because none of these and this because none of these and this because none of these instructions has any incoming instructions has any incoming instructions has any incoming dependencies yet in the second cycle dependencies yet in the second cycle dependencies yet in the second cycle okay this one cannot be done in the okay this one cannot be done in the okay this one cannot be done in the first cycle because it has a dependence first cycle because it has a dependence first cycle because it has a dependence from this and this and that's not been from this and this and that's not been from this and this and that's not been done in the first cycle yet so after the done in the first cycle yet so after the done in the first cycle yet so after the first cycle all of these are done so now first cycle all of these are done so now first cycle all of these are done so now we can do this so what we have what we we can do this so what we have what we we can do this so what we have what we get is now the ilp get is now the ilp get is now the ilp is equal to how many instructions is equal to how many instructions is equal to how many instructions five five five over how many cycles two we got an ilp over how many cycles two we got an ilp over how many cycles two we got an ilp of 2.5 again we didn't need to know what of 2.5 again we didn't need to know what of 2.5 again we didn't need to know what the processor looks like the processor looks like the processor looks like now some convenient tricks for computing now some convenient tricks for computing now some convenient tricks for computing the lp first the lp first the lp first i didn't actually need to rename the i didn't actually need to rename the i didn't actually need to rename the program if this program had real program if this program had real program if this program had real registers with some anti and output registers with some anti and output registers with some anti and output dependencies dependencies dependencies i could just fake renaming but just i could just fake renaming but just i could just fake renaming but just looking at where the true dependencies looking at where the true dependencies looking at where the true dependencies are and completely ignoring the anti and are and completely ignoring the anti and are and completely ignoring the anti and output dependencies why well because the output dependencies why well because the output dependencies why well because the renaming would eliminate them anyway all renaming would eliminate them anyway all renaming would eliminate them anyway all that would be left is the true that would be left is the true that would be left is the true dependencies so you actually when you're dependencies so you actually when you're dependencies so you actually when you're computing the lp don't really need to computing the lp don't really need to computing the lp don't really need to look at a rename program you can do it look at a rename program you can do it look at a rename program you can do it directly on the program as it was directly on the program as it was directly on the program as it was written you just need to follow only the written you just need to follow only the written you just need to follow only the true dependencies and completely ignore true dependencies and completely ignore true dependencies and completely ignore the the antenna output dependencies the the antenna output dependencies the the antenna output dependencies second you know when checking this off second you know when checking this off second you know when checking this off be mindful to kind of remember how many be mindful to kind of remember how many be mindful to kind of remember how many cycles you're computing and also what cycles you're computing and also what cycles you're computing and also what has been done and so on and then you has been done and so on and then you has been done and so on and then you know just be careful also to kind of know just be careful also to kind of know just be careful also to kind of divide the right thing with the right divide the right thing with the right divide the right thing with the right thing so it's five instructions over two thing so it's five instructions over two thing so it's five instructions over two cycles not the other way around

## ILP Quiz Solution： Part 2
Kind: captions Language: en as we said before we have to figure out as we said before we have to figure out as we said before we have to figure out the dependencies even though this the dependencies even though this the dependencies even though this program has output an anti dependences program has output an anti dependences program has output an anti dependences again for LT all we care about is the again for LT all we care about is the again for LT all we care about is the true dependencies so we could rename true dependencies so we could rename true dependencies so we could rename this programming J get just the true this programming J get just the true this programming J get just the true dependencies or we could just look at dependencies or we could just look at dependencies or we could just look at where the true dependencies are here so where the true dependencies are here so where the true dependencies are here so we have here this is using r1 this is we have here this is using r1 this is we have here this is using r1 this is using our 2 and r1 but it doesn't matter using our 2 and r1 but it doesn't matter using our 2 and r1 but it doesn't matter because this is going to be a longer r7 because this is going to be a longer r7 because this is going to be a longer r7 and r8 are not depending on anything yet and r8 are not depending on anything yet and r8 are not depending on anything yet r3 and r 7 there is a dependence here R r3 and r 7 there is a dependence here R r3 and r 7 there is a dependence here R 1 and R 1 we are dependent on this 1 and R 1 we are dependent on this 1 and R 1 we are dependent on this instruction here for both of them really instruction here for both of them really instruction here for both of them really and then R 7 and r 7 we are not using and then R 7 and r 7 we are not using and then R 7 and r 7 we are not using any of them again we are overwriting R 1 any of them again we are overwriting R 1 any of them again we are overwriting R 1 here we are overwriting R 1 here and so here we are overwriting R 1 here and so here we are overwriting R 1 here and so on but that doesn't matter because after on but that doesn't matter because after on but that doesn't matter because after renaming all of that would be gone so renaming all of that would be gone so renaming all of that would be gone so now it's time to figure out what can now it's time to figure out what can now it's time to figure out what can execute in which cycle in the first execute in which cycle in the first execute in which cycle in the first cycle we can execute this one we cannot cycle we can execute this one we cannot cycle we can execute this one we cannot execute this one because it depends on execute this one because it depends on execute this one because it depends on that we cannot execute this one because that we cannot execute this one because that we cannot execute this one because it has to wait for the second it has to wait for the second it has to wait for the second instruction we can execute this one it instruction we can execute this one it instruction we can execute this one it doesn't depend on anything we can not doesn't depend on anything we can not doesn't depend on anything we can not execute this one yet because it has a execute this one yet because it has a execute this one yet because it has a dependence this one also but this one dependence this one also but this one dependence this one also but this one can execute so in the first cycle we can execute so in the first cycle we can execute so in the first cycle we managed to do our three instructions now managed to do our three instructions now managed to do our three instructions now this instruction only depends on this instruction only depends on this instruction only depends on instruction that is done after cycle one instruction that is done after cycle one instruction that is done after cycle one so this can be done in second cycle this so this can be done in second cycle this so this can be done in second cycle this one actually depends on instruction that one actually depends on instruction that one actually depends on instruction that is done in second cycle so it will have is done in second cycle so it will have is done in second cycle so it will have to wait until the third cycle this one to wait until the third cycle this one to wait until the third cycle this one depends on an instruction that is done depends on an instruction that is done depends on an instruction that is done in the third cycle so this is going to in the third cycle so this is going to in the third cycle so this is going to be executed in the fourth cycle this one be executed in the fourth cycle this one be executed in the fourth cycle this one can actually execute in the second cycle can actually execute in the second cycle can actually execute in the second cycle it depends on the instruction that it depends on the instruction that it depends on the instruction that executes in the first cycle and that's executes in the first cycle and that's executes in the first cycle and that's it so it can be done in the second cycle it so it can be done in the second cycle it so it can be done in the second cycle so what we have is the last instruction so what we have is the last instruction so what we have is the last instruction that finishes in cycle four and we that finishes in cycle four and we that finishes in cycle four and we managed to execute seven instructions managed to execute seven instructions managed to execute seven instructions over that time so we have seven over over that time so we have seven over over that time so we have seven over four which if you want to calculate it four which if you want to calculate it four which if you want to calculate it out it's gonna end up being 1.75

## ILP with Control Dependencies： Part 2
Kind: captions Language: en so we have seen that when Computing the so we have seen that when Computing the so we have seen that when Computing the ILP we completely ignore false ILP we completely ignore false ILP we completely ignore false dependencies among data dependencies and dependencies among data dependencies and dependencies among data dependencies and only consider true or flow or read after only consider true or flow or read after only consider true or flow or read after write data write data write data dependencies so ILP really only looks at dependencies so ILP really only looks at dependencies so ILP really only looks at those data dependencies but there are those data dependencies but there are those data dependencies but there are two other types of dependencies in two other types of dependencies in two other types of dependencies in addition to data dependencies there are addition to data dependencies there are addition to data dependencies there are structural dependencies and control structural dependencies and control structural dependencies and control dependencies how do they affect ILP and dependencies how do they affect ILP and dependencies how do they affect ILP and the answer is that first of all when the answer is that first of all when the answer is that first of all when looking at ILP there are no structural looking at ILP there are no structural looking at ILP there are no structural dependencies structural dependencies dependencies structural dependencies dependencies structural dependencies occur when we don't have enough Hardware occur when we don't have enough Hardware occur when we don't have enough Hardware to do things in the same cycle when to do things in the same cycle when to do things in the same cycle when Computing the ILP we are assuming ideal Computing the ILP we are assuming ideal Computing the ILP we are assuming ideal hardware and as part of being ideal it hardware and as part of being ideal it hardware and as part of being ideal it has no structural dependencies meaning has no structural dependencies meaning has no structural dependencies meaning any instruction that could possibly any instruction that could possibly any instruction that could possibly execute in the same cycle will execute execute in the same cycle will execute execute in the same cycle will execute in that cycle and not have to wait on in that cycle and not have to wait on in that cycle and not have to wait on some resource like for example there some resource like for example there some resource like for example there being only one Adder so we can do only being only one Adder so we can do only being only one Adder so we can do only one add in a cycle we can assume that we one add in a cycle we can assume that we one add in a cycle we can assume that we have an infinite number of adders so if have an infinite number of adders so if have an infinite number of adders so if we want to do an ad we do it for control we want to do an ad we do it for control we want to do an ad we do it for control dependencies we assume that we have dependencies we assume that we have dependencies we assume that we have perfect same cycle Branch prediction perfect same cycle Branch prediction perfect same cycle Branch prediction meaning that branches are predicted in meaning that branches are predicted in meaning that branches are predicted in the same cycle in which we fetch them so the same cycle in which we fetch them so the same cycle in which we fetch them so we see all the correct instructions we see all the correct instructions we see all the correct instructions after the branch in the same cycle in after the branch in the same cycle in after the branch in the same cycle in which we see a branch let's see an which we see a branch let's see an which we see a branch let's see an example of this where we have a program example of this where we have a program example of this where we have a program that has a flow dependence and also an that has a flow dependence and also an that has a flow dependence and also an output dependence and then we have a output dependence and then we have a output dependence and then we have a branch that has a flow dependence on R1 branch that has a flow dependence on R1 branch that has a flow dependence on R1 and branches to label if the condition and branches to label if the condition and branches to label if the condition is satisfied otherwise continues on and is satisfied otherwise continues on and is satisfied otherwise continues on and then after the branch in the program we then after the branch in the program we then after the branch in the program we have an instruction and at the label we have an instruction and at the label we have an instruction and at the label we have another instruction so now let's have another instruction so now let's have another instruction so now let's see how we compute ILP here and these see how we compute ILP here and these see how we compute ILP here and these are again Cycles so what happens is in are again Cycles so what happens is in are again Cycles so what happens is in the first cycle we can definitely do the first cycle we can definitely do the first cycle we can definitely do this we cannot do them all in the first this we cannot do them all in the first this we cannot do them all in the first cycle because it it depends on the ad so cycle because it it depends on the ad so cycle because it it depends on the ad so we will have to wait until the second we will have to wait until the second we will have to wait until the second cycle to do it the branch can actually cycle to do it the branch can actually cycle to do it the branch can actually not be done until the third cycle not be done until the third cycle not be done until the third cycle because that's where we compare this now because that's where we compare this now because that's where we compare this now let's say that this branch is actually let's say that this branch is actually let's say that this branch is actually taken what we can assume is that taken what we can assume is that taken what we can assume is that although the branch cannot execute until although the branch cannot execute until although the branch cannot execute until the third cycle from the beginning since the third cycle from the beginning since the third cycle from the beginning since we started fetching the program we knew we started fetching the program we knew we started fetching the program we knew what the branch is going to do so we what the branch is going to do so we what the branch is going to do so we knew that the branch is going to go to knew that the branch is going to go to knew that the branch is going to go to the label and thus this instruction the label and thus this instruction the label and thus this instruction because it has no data dependencies on because it has no data dependencies on because it has no data dependencies on any other instruction will actually any other instruction will actually any other instruction will actually execute in the first cycle although it execute in the first cycle although it execute in the first cycle although it has a control dependence so as you can has a control dependence so as you can has a control dependence so as you can see not only are control dependencies see not only are control dependencies see not only are control dependencies not resulting in any delays after the not resulting in any delays after the not resulting in any delays after the branch branch branch happens actually we are assuming perfect happens actually we are assuming perfect happens actually we are assuming perfect Branch Branch Branch prediction and that means that this prediction and that means that this prediction and that means that this branch is predicted even before it's branch is predicted even before it's branch is predicted even before it's executed so that we can just do executed so that we can just do executed so that we can just do dependent instructions dependent instructions dependent instructions pretty much we can completely ignore pretty much we can completely ignore pretty much we can completely ignore control dependencies the branch acts as control dependencies the branch acts as control dependencies the branch acts as an instruction that simply produces no an instruction that simply produces no an instruction that simply produces no results so it eventually has to execute results so it eventually has to execute results so it eventually has to execute but it has really no impact as far as but it has really no impact as far as but it has really no impact as far as delaying other instructions is concerned delaying other instructions is concerned delaying other instructions is concerned so in this case not only does the so in this case not only does the so in this case not only does the multiply instruction execute even before multiply instruction execute even before multiply instruction execute even before the branch on which it has a control the branch on which it has a control the branch on which it has a control dependence executes also the dependence executes also the dependence executes also the instructions that is small will feed instructions that is small will feed instructions that is small will feed values to can execute before the branch values to can execute before the branch values to can execute before the branch so pretty much a branch itself might be so pretty much a branch itself might be so pretty much a branch itself might be delayed because of true dependencies but delayed because of true dependencies but delayed because of true dependencies but its control dependencies on that branch its control dependencies on that branch its control dependencies on that branch have absolutely no impact on what the have absolutely no impact on what the have absolutely no impact on what the ILP will be because we are assuming ILP will be because we are assuming ILP will be because we are assuming perfect Branch piction where all the perfect Branch piction where all the perfect Branch piction where all the branches in the program are perfectly branches in the program are perfectly branches in the program are perfectly predicted in the same cycle as soon as predicted in the same cycle as soon as predicted in the same cycle as soon as we begin fetching the program and from we begin fetching the program and from we begin fetching the program and from the beginning of the program we knew the beginning of the program we knew the beginning of the program we knew exactly which instructions will execute exactly which instructions will execute exactly which instructions will execute so that we can just do them according to so that we can just do them according to so that we can just do them according to data dependencies

## ILP vs IPC： Part 2
Kind: captions Language: en so we already know that ILP is not equal so we already know that ILP is not equal so we already know that ILP is not equal to ie PC on any real processor actually to ie PC on any real processor actually to ie PC on any real processor actually it can be but it's not necessarily so it can be but it's not necessarily so it can be but it's not necessarily so and that is because ILP is really the and that is because ILP is really the and that is because ILP is really the IPC on a perfect processor that does IPC on a perfect processor that does IPC on a perfect processor that does perfect branch prediction and only has perfect branch prediction and only has perfect branch prediction and only has to follow data dependencies so now let's to follow data dependencies so now let's to follow data dependencies so now let's look at an example of a program in the look at an example of a program in the look at an example of a program in the real processor and see basically how do real processor and see basically how do real processor and see basically how do we determine LP and how do we determine we determine LP and how do we determine we determine LP and how do we determine the IPC so this is our program and now the IPC so this is our program and now the IPC so this is our program and now let's look at what the processor looks let's look at what the processor looks let's look at what the processor looks like sir processor is to issue meaning like sir processor is to issue meaning like sir processor is to issue meaning it can handle two instructions per cycle it can handle two instructions per cycle it can handle two instructions per cycle out-of-order superscalar which means out-of-order superscalar which means out-of-order superscalar which means that it doesn't need to execute that it doesn't need to execute that it doesn't need to execute instructions exactly in program order instructions exactly in program order instructions exactly in program order and let's say that it only has one and let's say that it only has one and let's say that it only has one multiplication unit and two units that multiplication unit and two units that multiplication unit and two units that can do add two tracks X or Z etc for our can do add two tracks X or Z etc for our can do add two tracks X or Z etc for our ILP we can just ignore all of this ILP we can just ignore all of this ILP we can just ignore all of this because they LP again is just the because they LP again is just the because they LP again is just the property of a program so we have to just property of a program so we have to just property of a program so we have to just obey dependencies there is a data obey dependencies there is a data obey dependencies there is a data dependence here and that's only really dependence here and that's only really dependence here and that's only really the only dependence that we have so as the only dependence that we have so as the only dependence that we have so as far as ILP is concerned we can do this far as ILP is concerned we can do this far as ILP is concerned we can do this in two cycles this this this and this in two cycles this this this and this in two cycles this this this and this would be done in the first cycle would be done in the first cycle would be done in the first cycle and then this would be done in the and then this would be done in the and then this would be done in the second cycle because it depends on the second cycle because it depends on the second cycle because it depends on the instruction that is done in the first instruction that is done in the first instruction that is done in the first cycle so we have five instructions over cycle so we have five instructions over cycle so we have five instructions over two cycles gives us 2.5 in contrast the two cycles gives us 2.5 in contrast the two cycles gives us 2.5 in contrast the IPC for this processor needs to consider IPC for this processor needs to consider IPC for this processor needs to consider all the limitations of this processor so all the limitations of this processor so all the limitations of this processor so we have this instruction can be done in we have this instruction can be done in we have this instruction can be done in the first cycle this one cannot because the first cycle this one cannot because the first cycle this one cannot because of a dependence so before the IPC we of a dependence so before the IPC we of a dependence so before the IPC we still have to obey all the dependencies still have to obey all the dependencies still have to obey all the dependencies plus we have to also worry about the plus we have to also worry about the plus we have to also worry about the other limitations of the processor this other limitations of the processor this other limitations of the processor this is an out-of-order processor so we are is an out-of-order processor so we are is an out-of-order processor so we are not doing this but we can still look not doing this but we can still look not doing this but we can still look further because we can do instructions further because we can do instructions further because we can do instructions out of order the XOR can be done because out of order the XOR can be done because out of order the XOR can be done because we have to add subtract etc units so we we have to add subtract etc units so we we have to add subtract etc units so we can do this in the first cycle to this can do this in the first cycle to this can do this in the first cycle to this instruction can be done in the first instruction can be done in the first instruction can be done in the first cycle because we can use the multiplier cycle because we can use the multiplier cycle because we can use the multiplier but for example this instruction here but for example this instruction here but for example this instruction here cannot be done in the first cycle unlike cannot be done in the first cycle unlike cannot be done in the first cycle unlike when we computed the ILP it cannot be when we computed the ILP it cannot be when we computed the ILP it cannot be done because we only have to add done because we only have to add done because we only have to add subtract XOR etc subtract XOR etc subtract XOR etc and we have already consumed both of and we have already consumed both of and we have already consumed both of them in the first cycle for the ad in them in the first cycle for the ad in them in the first cycle for the ad in the expects or so in the second cycle the expects or so in the second cycle the expects or so in the second cycle now we can do this and this so it turns now we can do this and this so it turns now we can do this and this so it turns out that I our IPC is still five out that I our IPC is still five out that I our IPC is still five instructions over two cycles equals to instructions over two cycles equals to instructions over two cycles equals to 0.5 but it doesn't have to be that way 0.5 but it doesn't have to be that way 0.5 but it doesn't have to be that way for example if this processor only had for example if this processor only had for example if this processor only had one add subtract X or unit we would now one add subtract X or unit we would now one add subtract X or unit we would now have the IPC 1/2 in the first cycle we have the IPC 1/2 in the first cycle we have the IPC 1/2 in the first cycle we can do this we cannot do this because of can do this we cannot do this because of can do this we cannot do this because of a dependence we cannot do this because a dependence we cannot do this because a dependence we cannot do this because we are already using our one unit that we are already using our one unit that we are already using our one unit that we have for add taxes etc we can do the we have for add taxes etc we can do the we have for add taxes etc we can do the multiply and we cannot do this at either multiply and we cannot do this at either multiply and we cannot do this at either in the second cycle we can do this we in the second cycle we can do this we in the second cycle we can do this we cannot do any of these other two because cannot do any of these other two because cannot do any of these other two because again we are using where our add again we are using where our add again we are using where our add subtract X or unit this would be in our subtract X or unit this would be in our subtract X or unit this would be in our third cycle and this would be our fourth third cycle and this would be our fourth third cycle and this would be our fourth cycle so now we have five instructions cycle so now we have five instructions cycle so now we have five instructions over four cycles and that's equal to over four cycles and that's equal to over four cycles and that's equal to 1.25 so this IPC is half of this IPC and 1.25 so this IPC is half of this IPC and 1.25 so this IPC is half of this IPC and half of this LP so what we can say is half of this LP so what we can say is half of this LP so what we can say is that the IPC has to be less than or that the IPC has to be less than or that the IPC has to be less than or equal to the ILP the ILP is for a equal to the ILP the ILP is for a equal to the ILP the ILP is for a perfect processor a real processor will perfect processor a real processor will perfect processor a real processor will have some limitations that will maybe have some limitations that will maybe have some limitations that will maybe allow it to achieve the same IPC as the allow it to achieve the same IPC as the allow it to achieve the same IPC as the ILP available in the application but ILP available in the application but ILP available in the application but sometimes it will not so what we can say sometimes it will not so what we can say sometimes it will not so what we can say is that the ILP is always greater than is that the ILP is always greater than is that the ILP is always greater than or equal to the IPC on any real or equal to the IPC on any real or equal to the IPC on any real processor and again we didn't really processor and again we didn't really processor and again we didn't really need to know what the processor is to need to know what the processor is to need to know what the processor is to compute the LP but we do need to know compute the LP but we do need to know compute the LP but we do need to know what the processor looks like when we what the processor looks like when we what the processor looks like when we are computing its IPC and we have seen are computing its IPC and we have seen are computing its IPC and we have seen that the IPC can be equal to the LP or that the IPC can be equal to the LP or that the IPC can be equal to the LP or it can be lower than that but it cannot it can be lower than that but it cannot it can be lower than that but it cannot be larger than that

## IPC & ILP Quiz： Part 2
Kind: captions Language: en okay so now that we have seen how ILP okay so now that we have seen how ILP okay so now that we have seen how ILP and IPC differ let's do a little IPC and and IPC differ let's do a little IPC and and IPC differ let's do a little IPC and ILP quiz let's say that we have a three ILP quiz let's say that we have a three ILP quiz let's say that we have a three issue in order processor that has three issue in order processor that has three issue in order processor that has three Al meaning that for everything that we Al meaning that for everything that we Al meaning that for everything that we can issue it can always execute it and can issue it can always execute it and can issue it can always execute it and these are general purpose s use that can these are general purpose s use that can these are general purpose s use that can just execute any type of instruction so just execute any type of instruction so just execute any type of instruction so we probably won't have any issues with we probably won't have any issues with we probably won't have any issues with the limited number of units the program the limited number of units the program the limited number of units the program we are going to execute is so for this we are going to execute is so for this we are going to execute is so for this program what we want to know is what is program what we want to know is what is program what we want to know is what is the ILP and what is the IPC

## IPC & ILP Solution Quiz： Part 2
Kind: captions Language: en so let's look at our solution so let's look at our solution so let's look at our solution when we have a problem like this we when we have a problem like this we when we have a problem like this we first figure out the dependencies there first figure out the dependencies there first figure out the dependencies there is one from here to here and another one is one from here to here and another one is one from here to here and another one from here to here and there is also from here to here and there is also from here to here and there is also dependence here from r7 to r7 so now dependence here from r7 to r7 so now dependence here from r7 to r7 so now let's look at our ilp so for ilp we can let's look at our ilp so for ilp we can let's look at our ilp so for ilp we can do this and this in the first cycle they do this and this in the first cycle they do this and this in the first cycle they don't depend on anything this one needs don't depend on anything this one needs don't depend on anything this one needs to be done in the second cycle because to be done in the second cycle because to be done in the second cycle because it depends on the first two instructions it depends on the first two instructions it depends on the first two instructions this one however can be done in the this one however can be done in the this one however can be done in the first cycle this one cannot it needs the first cycle this one cannot it needs the first cycle this one cannot it needs the r7 that is produced by this one r7 that is produced by this one r7 that is produced by this one so this gets done in the second cycle so this gets done in the second cycle so this gets done in the second cycle and then this one actually doesn't and then this one actually doesn't and then this one actually doesn't depend on anything so it can also be depend on anything so it can also be depend on anything so it can also be done in the first cycle so as far as ilp done in the first cycle so as far as ilp done in the first cycle so as far as ilp is concerned we have is concerned we have is concerned we have a total of six instructions executed a total of six instructions executed a total of six instructions executed over two cycles so the ilp is going to over two cycles so the ilp is going to over two cycles so the ilp is going to be three for the ipc we have to worry be three for the ipc we have to worry be three for the ipc we have to worry not only about these dependencies but not only about these dependencies but not only about these dependencies but also also also that this is a three issue processor and that this is a three issue processor and that this is a three issue processor and that it's in order that it's in order that it's in order so this can still be done in the first so this can still be done in the first so this can still be done in the first cycle cycle cycle this can be done in the first cycle as this can be done in the first cycle as this can be done in the first cycle as far as three issue is concerned this far as three issue is concerned this far as three issue is concerned this could be done in the first cycle but could be done in the first cycle but could be done in the first cycle but because it has a dependence we still because it has a dependence we still because it has a dependence we still have to delay it by one cycle and now have to delay it by one cycle and now have to delay it by one cycle and now the problem is that in an inorder the problem is that in an inorder the problem is that in an inorder processor once we don't execute an processor once we don't execute an processor once we don't execute an instruction we cannot execute instruction we cannot execute instruction we cannot execute instructions past it because that would instructions past it because that would instructions past it because that would be out of order so this is all that be out of order so this is all that be out of order so this is all that happens in the first cycle in this happens in the first cycle in this happens in the first cycle in this processor processor processor in the second cycle we are able to in the second cycle we are able to in the second cycle we are able to execute this instruction because now the execute this instruction because now the execute this instruction because now the first two are done first two are done first two are done we can execute this one we can execute this one we can execute this one because now that this one is done this because now that this one is done this because now that this one is done this is eligible for execution and then we is eligible for execution and then we is eligible for execution and then we have to stop here because of this have to stop here because of this have to stop here because of this dependence so this would have to be done dependence so this would have to be done dependence so this would have to be done in the third cycle and again we cannot in the third cycle and again we cannot in the third cycle and again we cannot do this so in the last third cycle we do this so in the last third cycle we do this so in the last third cycle we will be able to finally do this and then will be able to finally do this and then will be able to finally do this and then this because they don't depend on each this because they don't depend on each this because they don't depend on each other so they can be done in the same other so they can be done in the same other so they can be done in the same cycle so now we have cycle so now we have cycle so now we have six instructions over three cycles six instructions over three cycles six instructions over three cycles for an ipc of for an ipc of for an ipc of two two two it turns out that even if this was a two it turns out that even if this was a two it turns out that even if this was a two issue processor the inorder property issue processor the inorder property issue processor the inorder property really would still limit us to a ipc of really would still limit us to a ipc of really would still limit us to a ipc of two so sometimes one of the things is two so sometimes one of the things is two so sometimes one of the things is going to be in limitations sometimes going to be in limitations sometimes going to be in limitations sometimes another limitation is going to play a another limitation is going to play a another limitation is going to play a dominant role sometimes all of the dominant role sometimes all of the dominant role sometimes all of the limitations are going to play a role in limitations are going to play a role in limitations are going to play a role in limiting our ipc

## ILP & IPC Discussion： Part 2
Kind: captions Language: en so now that we have seen what ilp is and so now that we have seen what ilp is and so now that we have seen what ilp is and also how it relates to ipc we can have a also how it relates to ipc we can have a also how it relates to ipc we can have a little discussion about the lp and the little discussion about the lp and the little discussion about the lp and the ipc ipc ipc we have defined our ilp as basically the we have defined our ilp as basically the we have defined our ilp as basically the ipc on an ideal out of order processor ipc on an ideal out of order processor ipc on an ideal out of order processor that has perfect branch friction enough that has perfect branch friction enough that has perfect branch friction enough resources for everything etc and we saw resources for everything etc and we saw resources for everything etc and we saw that ilp is larger than or equal to the that ilp is larger than or equal to the that ilp is larger than or equal to the ipc that can be achieved on any real ipc that can be achieved on any real ipc that can be achieved on any real processor so if we consider a narrow processor so if we consider a narrow processor so if we consider a narrow issuing order processor narrow issue issuing order processor narrow issue issuing order processor narrow issue meaning that it can only issue about meaning that it can only issue about meaning that it can only issue about maybe one two maybe three instructions maybe one two maybe three instructions maybe one two maybe three instructions per cycle and it's in order per cycle and it's in order per cycle and it's in order usually the limitation for the ipc will usually the limitation for the ipc will usually the limitation for the ipc will be this basically the in order property be this basically the in order property be this basically the in order property is not going to hurt us too much is not going to hurt us too much is not going to hurt us too much so this processor is going to be limited so this processor is going to be limited so this processor is going to be limited mostly by the narrow issue mostly by the narrow issue mostly by the narrow issue however if we have a wide issue in order however if we have a wide issue in order however if we have a wide issue in order processor meaning it can do more processor meaning it can do more processor meaning it can do more instructions per cycle instructions per cycle instructions per cycle it's going to be limited mostly by it's going to be limited mostly by it's going to be limited mostly by the inorder property because this is the inorder property because this is the inorder property because this is going to allow it to do a lot of going to allow it to do a lot of going to allow it to do a lot of instructions per cycle and if it can do instructions per cycle and if it can do instructions per cycle and if it can do closely to what the ilp already is for closely to what the ilp already is for closely to what the ilp already is for the application then basically we're the application then basically we're the application then basically we're already kind of close to ideal as far as already kind of close to ideal as far as already kind of close to ideal as far as the ipc the ipc the ipc is concerned for for the wide issue is concerned for for the wide issue is concerned for for the wide issue problem but because of the inorder problem but because of the inorder problem but because of the inorder property we just cannot find those property we just cannot find those property we just cannot find those instructions to keep our wide issue instructions to keep our wide issue instructions to keep our wide issue slots busy so if we have a wide issue slots busy so if we have a wide issue slots busy so if we have a wide issue processor we also need for it to be out processor we also need for it to be out processor we also need for it to be out of order so that it can benefit from the of order so that it can benefit from the of order so that it can benefit from the wider issue by finding instructions wider issue by finding instructions wider issue by finding instructions anywhere where they are and not anywhere where they are and not anywhere where they are and not basically stopping on the first basically stopping on the first basically stopping on the first dependence dependence dependence so such a processor will need to fetch so such a processor will need to fetch so such a processor will need to fetch execute etc more than one instruction execute etc more than one instruction execute etc more than one instruction per cycle preferably a lot more than one per cycle preferably a lot more than one per cycle preferably a lot more than one so it cannot be just like two it should so it cannot be just like two it should so it cannot be just like two it should be four maybe or more be four maybe or more be four maybe or more that's why we call it wide issue that's why we call it wide issue that's why we call it wide issue it should be able to eliminate false it should be able to eliminate false it should be able to eliminate false dependencies between because if we have dependencies between because if we have dependencies between because if we have to obey all sorts of dependencies then to obey all sorts of dependencies then to obey all sorts of dependencies then that's going to limit our ipc2 that's going to limit our ipc2 that's going to limit our ipc2 and bring us far away from the ideal ilp and bring us far away from the ideal ilp and bring us far away from the ideal ilp and finally should be able to reorder and finally should be able to reorder and finally should be able to reorder instructions so that we can execute instructions so that we can execute instructions so that we can execute instructions out of order meaning that instructions out of order meaning that instructions out of order meaning that our execution order should be kind of our execution order should be kind of our execution order should be kind of similar to what we did for the ilp and similar to what we did for the ilp and similar to what we did for the ilp and not not not for what we did for the in-order for what we did for the in-order for what we did for the in-order processors

## ILP Outro： Part 2
Kind: captions Language: en in this lesson we have seen that we in this lesson we have seen that we in this lesson we have seen that we should be able to get excellent should be able to get excellent should be able to get excellent performance even when the program has a performance even when the program has a performance even when the program has a lot of data dependencies in the next lot of data dependencies in the next lot of data dependencies in the next several lessons we will learn how a real several lessons we will learn how a real several lessons we will learn how a real processor can actually do this only with processor can actually do this only with processor can actually do this only with Hardware structures that have limited Hardware structures that have limited Hardware structures that have limited size and complexity

## Tomasulo Intro： Part 2
Kind: captions Language: en in this lesson we will see how in this lesson we will see how in this lesson we will see how instruction scheduling can let us instruction scheduling can let us instruction scheduling can let us execute programs faster even when these execute programs faster even when these execute programs faster even when these programs have data dependencies and even programs have data dependencies and even programs have data dependencies and even how to execute more than one instruction how to execute more than one instruction how to execute more than one instruction every cycle

## Improving IPC： Part 2
Kind: captions Language: en so in this lesson we're going to be so in this lesson we're going to be so in this lesson we're going to be looking at how to improve the IPC looking at how to improve the IPC looking at how to improve the IPC without a further execution and how to without a further execution and how to without a further execution and how to design actual Hardware that does that design actual Hardware that does that design actual Hardware that does that for improving the IPC we have seen that for improving the IPC we have seen that for improving the IPC we have seen that the ILP can be good it can be the ILP can be good it can be the ILP can be good it can be significantly larger than one usually significantly larger than one usually significantly larger than one usually it's way over four but to achieve it's way over four but to achieve it's way over four but to achieve something close to that ILP we need to something close to that ILP we need to something close to that ILP we need to handle the control dependencies and we handle the control dependencies and we handle the control dependencies and we have seen how Branch prediction can help have seen how Branch prediction can help have seen how Branch prediction can help eliminate control dependencies if branch eliminate control dependencies if branch eliminate control dependencies if branch prediction is correct so if our Branch prediction is correct so if our Branch prediction is correct so if our Branch prediction is very good which on today's prediction is very good which on today's prediction is very good which on today's processor it is then as far as control processor it is then as far as control processor it is then as far as control dependencies are concerned our IPC will dependencies are concerned our IPC will dependencies are concerned our IPC will be very close to the ILP then we have to be very close to the ILP then we have to be very close to the ILP then we have to consider the right after right and right consider the right after right and right consider the right after right and right after read data dependencies also known after read data dependencies also known after read data dependencies also known as false dependencies and we have seen as false dependencies and we have seen as false dependencies and we have seen that a technique called register that a technique called register that a technique called register renaming can completely eliminate these renaming can completely eliminate these renaming can completely eliminate these dependencies so that you simply don't dependencies so that you simply don't dependencies so that you simply don't have them anymore in your program at have them anymore in your program at have them anymore in your program at which point they're not going to be a which point they're not going to be a which point they're not going to be a problem for our IPC and then we have problem for our IPC and then we have problem for our IPC and then we have seen that there are these right after seen that there are these right after seen that there are these right after read also known as through data read also known as through data read also known as through data dependencies and we have seen that it dependencies and we have seen that it dependencies and we have seen that it helps a lot when you can execute helps a lot when you can execute helps a lot when you can execute instructions out of order meaning you instructions out of order meaning you instructions out of order meaning you don't strictly follow the program order don't strictly follow the program order don't strictly follow the program order you simply find instructions whose you simply find instructions whose you simply find instructions whose inputs are ready and you execute them inputs are ready and you execute them inputs are ready and you execute them then that is how ILP mostly was good and then that is how ILP mostly was good and then that is how ILP mostly was good and finally there are these structural finally there are these structural finally there are these structural dependencies which is when you cannot dependencies which is when you cannot dependencies which is when you cannot execute things simply because you don't execute things simply because you don't execute things simply because you don't have enough resources in the processor have enough resources in the processor have enough resources in the processor but that can be solved simply by but that can be solved simply by but that can be solved simply by investing in a in to a wider issue investing in a in to a wider issue investing in a in to a wider issue processor meaning we need a processor processor meaning we need a processor processor meaning we need a processor that can handle a bunch of instructions that can handle a bunch of instructions that can handle a bunch of instructions in each cycle and not have to delay in each cycle and not have to delay in each cycle and not have to delay instructions just because it doesn't instructions just because it doesn't instructions just because it doesn't have resources for it so among these we have resources for it so among these we have resources for it so among these we really need to figure out how to do really need to figure out how to do really need to figure out how to do these three and first we will focus on these three and first we will focus on these three and first we will focus on this so how do we actually do register this so how do we actually do register this so how do we actually do register naming and outof Order execution in a naming and outof Order execution in a naming and outof Order execution in a way that can be amenable to Hardware way that can be amenable to Hardware way that can be amenable to Hardware implementation so we know how to do it implementation so we know how to do it implementation so we know how to do it kind of manually on paper but how does a kind of manually on paper but how does a kind of manually on paper but how does a processor actually do it so this will processor actually do it so this will processor actually do it so this will really focus on how to do this for real

## Tomasulo's Algorithm： Part 2
Kind: captions Language: en so the first technique that we will so the first technique that we will so the first technique that we will consider is tomasulo's algorithm it's consider is tomasulo's algorithm it's consider is tomasulo's algorithm it's really one of the first techniques for really one of the first techniques for really one of the first techniques for outof order execution and now it's more outof order execution and now it's more outof order execution and now it's more than 40 years old it was used in the old than 40 years old it was used in the old than 40 years old it was used in the old IBM 360 machines which are now again IBM 360 machines which are now again IBM 360 machines which are now again more than 40 years old what it does is more than 40 years old what it does is more than 40 years old what it does is it determines which instructions have it determines which instructions have it determines which instructions have inputs ready so that they can go in the inputs ready so that they can go in the inputs ready so that they can go in the next cycle and which instructions still next cycle and which instructions still next cycle and which instructions still have to wait for their inputs to be have to wait for their inputs to be have to wait for their inputs to be produced it also includes a form of produced it also includes a form of produced it also includes a form of register raming and it is surprisingly register raming and it is surprisingly register raming and it is surprisingly similar to what we actually use today as similar to what we actually use today as similar to what we actually use today as far as outof order execution is far as outof order execution is far as outof order execution is concerned so although this is more than concerned so although this is more than concerned so although this is more than 40 years old it actually is very useful 40 years old it actually is very useful 40 years old it actually is very useful to know how it works because it's kind to know how it works because it's kind to know how it works because it's kind of like the first approach so it's of like the first approach so it's of like the first approach so it's simpler than what we use today but it simpler than what we use today but it simpler than what we use today but it really well explains what's going on the really well explains what's going on the really well explains what's going on the differences between Tomaso's algorithm differences between Tomaso's algorithm differences between Tomaso's algorithm and what is used today are only that in and what is used today are only that in and what is used today are only that in the olden days when tomasulo's algorithm the olden days when tomasulo's algorithm the olden days when tomasulo's algorithm was implemented they did that only for was implemented they did that only for was implemented they did that only for floating Point instructions while today floating Point instructions while today floating Point instructions while today we do it for all instructions also they we do it for all instructions also they we do it for all instructions also they had relatively few instructions in the had relatively few instructions in the had relatively few instructions in the window that they looking at meaning they window that they looking at meaning they window that they looking at meaning they only really looked over some small only really looked over some small only really looked over some small number of instructions into the you know number of instructions into the you know number of instructions into the you know future instructions that need to be future instructions that need to be future instructions that need to be executed while today we look at hundreds executed while today we look at hundreds executed while today we look at hundreds of instructions past the one we are of instructions past the one we are of instructions past the one we are still executing in the olden days still executing in the olden days still executing in the olden days exception handling for floating Point exception handling for floating Point exception handling for floating Point instructions was not such a big problem instructions was not such a big problem instructions was not such a big problem because they were only considering because they were only considering because they were only considering really you know execution of kind of really you know execution of kind of really you know execution of kind of floating Point intensive applications floating Point intensive applications floating Point intensive applications and uh you know those would be and uh you know those would be and uh you know those would be programmed and so on and you would programmed and so on and you would programmed and so on and you would usually run one program at a time if usually run one program at a time if usually run one program at a time if there is an exception you could do some there is an exception you could do some there is an exception you could do some drastic measures today process include drastic measures today process include drastic measures today process include explicit support for exception handling explicit support for exception handling explicit support for exception handling that we will see that we will see that we will see later so we will first again look at how later so we will first again look at how later so we will first again look at how the tomasulo algorithm did it and then the tomasulo algorithm did it and then the tomasulo algorithm did it and then we will consider how today's processors we will consider how today's processors we will consider how today's processors do these additional things

## The Picture： Part 2
Kind: captions Language: en so before we go into how exactly so before we go into how exactly so before we go into how exactly tomasulo's algorithm Works let's kind of tomasulo's algorithm Works let's kind of tomasulo's algorithm Works let's kind of see an overview picture of what's going see an overview picture of what's going see an overview picture of what's going on in there just like what the hardware on in there just like what the hardware on in there just like what the hardware structur looks like so first there is an structur looks like so first there is an structur looks like so first there is an instruction Quee which I labeled here IQ instruction Quee which I labeled here IQ instruction Quee which I labeled here IQ instructions come from the fetch unit in instructions come from the fetch unit in instructions come from the fetch unit in the order in which they were fetched and the order in which they were fetched and the order in which they were fetched and again these are only floating Point again these are only floating Point again these are only floating Point instruction and then they're queued up instruction and then they're queued up instruction and then they're queued up here so this is basically like this is here so this is basically like this is here so this is basically like this is the kind of oldest instruction the next the kind of oldest instruction the next the kind of oldest instruction the next one the next one the next one and we one the next one the next one and we one the next one the next one and we will be kind of you know dropping these will be kind of you know dropping these will be kind of you know dropping these instructions into the Tomaso's ALG instructions into the Tomaso's ALG instructions into the Tomaso's ALG machine in the order in which they came machine in the order in which they came machine in the order in which they came we always grab the next available we always grab the next available we always grab the next available instruction from the instruction queue instruction from the instruction queue instruction from the instruction queue and we want to put it into a reservation and we want to put it into a reservation and we want to put it into a reservation station there are a number of station there are a number of station there are a number of reservation stations that basically when reservation stations that basically when reservation stations that basically when one becomes available we will put an one becomes available we will put an one becomes available we will put an instruction into one of those and instruction into one of those and instruction into one of those and reservation stations are where reservation stations are where reservation stations are where instructions basically sit for their instructions basically sit for their instructions basically sit for their parameters to become ready there is a parameters to become ready there is a parameters to become ready there is a floating Point register file so this is floating Point register file so this is floating Point register file so this is where your floating Point registers are where your floating Point registers are where your floating Point registers are and when an instruction is inserted into and when an instruction is inserted into and when an instruction is inserted into a reservation station the values that a reservation station the values that a reservation station the values that are already in registers meaning the are already in registers meaning the are already in registers meaning the values that are actually already present values that are actually already present values that are actually already present and ready for execution are going to be and ready for execution are going to be and ready for execution are going to be simply entered into the reservation simply entered into the reservation simply entered into the reservation station from the register file once an station from the register file once an station from the register file once an instruction is actually ready to execute instruction is actually ready to execute instruction is actually ready to execute it goes to an execution unit and you can it goes to an execution unit and you can it goes to an execution unit and you can have different types of execution units have different types of execution units have different types of execution units so for example this is a adder and this so for example this is a adder and this so for example this is a adder and this is a multiplier so simple floating Point is a multiplier so simple floating Point is a multiplier so simple floating Point instructions would be done in the kind instructions would be done in the kind instructions would be done in the kind of Adder unit and the more complicated of Adder unit and the more complicated of Adder unit and the more complicated like multiply and divide would be done like multiply and divide would be done like multiply and divide would be done in the multiplier unit in the multiplier unit in the multiplier unit each of these units really had separate each of these units really had separate each of these units really had separate reservation stations where instructions reservation stations where instructions reservation stations where instructions that are going to that unit would wait that are going to that unit would wait that are going to that unit would wait once such a unit has produced a result once such a unit has produced a result once such a unit has produced a result the result would be broadcast on a bus the result would be broadcast on a bus the result would be broadcast on a bus so of course the result would go to the so of course the result would go to the so of course the result would go to the register file so prettyy much all the register file so prettyy much all the register file so prettyy much all the results that are ready would be results that are ready would be results that are ready would be available in the register file so that available in the register file so that available in the register file so that instructions that come in would simply instructions that come in would simply instructions that come in would simply grab them from there and we not don't grab them from there and we not don't grab them from there and we not don't have to worry about where those values have to worry about where those values have to worry about where those values are going to come from but these results are going to come from but these results are going to come from but these results would also be broadcast to the would also be broadcast to the would also be broadcast to the reservation stations why well because reservation stations why well because reservation stations why well because the are instructions here that are the are instructions here that are the are instructions here that are waiting for values that they need to be waiting for values that they need to be waiting for values that they need to be produced and as values are produced we produced and as values are produced we produced and as values are produced we need to tell these instructions that you need to tell these instructions that you need to tell these instructions that you know now your value has been produced so know now your value has been produced so know now your value has been produced so they need to also go here so pretty much they need to also go here so pretty much they need to also go here so pretty much here you have some values that you here you have some values that you here you have some values that you already have in an instruction some already have in an instruction some already have in an instruction some values that you're waiting for and you values that you're waiting for and you values that you're waiting for and you know what you're waiting for and then know what you're waiting for and then know what you're waiting for and then when the value is finally broadcast you when the value is finally broadcast you when the value is finally broadcast you check for each of the value that it's check for each of the value that it's check for each of the value that it's broadcast whether this is the one we are broadcast whether this is the one we are broadcast whether this is the one we are waiting for or not now I have two of waiting for or not now I have two of waiting for or not now I have two of these per reservation station because a these per reservation station because a these per reservation station because a typ instruction will actually have two typ instruction will actually have two typ instruction will actually have two inputs like for example you're adding inputs like for example you're adding inputs like for example you're adding two numbers and when you broadcast a two numbers and when you broadcast a two numbers and when you broadcast a result either of the two or both of them result either of the two or both of them result either of the two or both of them might need that result so for example if might need that result so for example if might need that result so for example if you're doing something like add R1 R1 you're doing something like add R1 R1 you're doing something like add R1 R1 and the instruction that is producing R1 and the instruction that is producing R1 and the instruction that is producing R1 is currently in the multiplier when it is currently in the multiplier when it is currently in the multiplier when it broadcast the result and says this is R1 broadcast the result and says this is R1 broadcast the result and says this is R1 we want to latch it into maybe both of we want to latch it into maybe both of we want to latch it into maybe both of the operants for this instruction so you the operants for this instruction so you the operants for this instruction so you need to compare basically what's being need to compare basically what's being need to compare basically what's being broadcast its name needs to be compared broadcast its name needs to be compared broadcast its name needs to be compared to both of the inputs that you are to both of the inputs that you are to both of the inputs that you are waiting for and then you select the one waiting for and then you select the one waiting for and then you select the one or both of them or none of them if this or both of them or none of them if this or both of them or none of them if this is not the value you're waiting for and is not the value you're waiting for and is not the value you're waiting for and finally if the instruction is not an finally if the instruction is not an finally if the instruction is not an arithmetic instruction if it's actually arithmetic instruction if it's actually arithmetic instruction if it's actually a load into the floating Point register a load into the floating Point register a load into the floating Point register file or a store from it then the file or a store from it then the file or a store from it then the instruction would go to the address instruction would go to the address instruction would go to the address generation unit that's an integer generation unit that's an integer generation unit that's an integer operation so it's not going going to go operation so it's not going going to go operation so it's not going going to go through that so basically we just through that so basically we just through that so basically we just compute the address and then we insert compute the address and then we insert compute the address and then we insert the instruction into the load buffer or the instruction into the load buffer or the instruction into the load buffer or a store buffer where the instructions a store buffer where the instructions a store buffer where the instructions are going to be queued up for going to are going to be queued up for going to are going to be queued up for going to memory and the load buffer only provides memory and the load buffer only provides memory and the load buffer only provides a data address the store buffer provides a data address the store buffer provides a data address the store buffer provides both the address and the data to memory both the address and the data to memory both the address and the data to memory when a load comes back from memory its when a load comes back from memory its when a load comes back from memory its value is also broadcast on the cdb and value is also broadcast on the cdb and value is also broadcast on the cdb and goes to the appropriate register and goes to the appropriate register and goes to the appropriate register and also all the values that are broadcast also all the values that are broadcast also all the values that are broadcast from the bus are going to go here so from the bus are going to go here so from the bus are going to go here so that stores can get their values when that stores can get their values when that stores can get their values when they become available so a store they become available so a store they become available so a store instruction that needs to put a value in instruction that needs to put a value in instruction that needs to put a value in memory is going to wait for its floating memory is going to wait for its floating memory is going to wait for its floating point value that needs to be placed to point value that needs to be placed to point value that needs to be placed to memory memory memory until it becomes available and only then until it becomes available and only then until it becomes available and only then be sent to the memory unlike arithmetic be sent to the memory unlike arithmetic be sent to the memory unlike arithmetic instructions which can execute out of instructions which can execute out of instructions which can execute out of order basically they only wait for order basically they only wait for order basically they only wait for inputs to become available and then they inputs to become available and then they inputs to become available and then they can go for loads and stores they were can go for loads and stores they were can go for loads and stores they were done in order so basically they didn't done in order so basically they didn't done in order so basically they didn't reorder loads and stores and it will reorder loads and stores and it will reorder loads and stores and it will later consider how to do that modern later consider how to do that modern later consider how to do that modern processors will actually reorder loads processors will actually reorder loads processors will actually reorder loads and stores too so this is a lot simpler and stores too so this is a lot simpler and stores too so this is a lot simpler than in modern processors but this here than in modern processors but this here than in modern processors but this here is pretty much the way it's done now is pretty much the way it's done now is pretty much the way it's done now modulo some small adjustment modulo some small adjustment modulo some small adjustment so before we go into kind of the details so before we go into kind of the details so before we go into kind of the details of tomasulo algorithm let's just kind of of tomasulo algorithm let's just kind of of tomasulo algorithm let's just kind of give names to some things on this give names to some things on this give names to some things on this picture and then we will kind of discuss picture and then we will kind of discuss picture and then we will kind of discuss what each of these things really needs what each of these things really needs what each of these things really needs to do so this part here where we send to do so this part here where we send to do so this part here where we send the instruction from the instruction the instruction from the instruction the instruction from the instruction queue into the reservation stations and queue into the reservation stations and queue into the reservation stations and the Lo store unit is called issue when the Lo store unit is called issue when the Lo store unit is called issue when the instruction is finally sent to the instruction is finally sent to the instruction is finally sent to execution from a reservation station is execution from a reservation station is execution from a reservation station is called dispatch and when the called dispatch and when the called dispatch and when the instruction is ready to broadcast its instruction is ready to broadcast its instruction is ready to broadcast its result we call that right result or result we call that right result or result we call that right result or simply broadcast so basically every simply broadcast so basically every simply broadcast so basically every instruction will be issued meaning get instruction will be issued meaning get instruction will be issued meaning get to the resing station eventually it gets to the resing station eventually it gets to the resing station eventually it gets to dispatch because all of its inputs to dispatch because all of its inputs to dispatch because all of its inputs are ready and uh it's kind of gets to go are ready and uh it's kind of gets to go are ready and uh it's kind of gets to go to the unit once it's done it's going to to the unit once it's done it's going to to the unit once it's done it's going to write the result and that is when we write the result and that is when we write the result and that is when we consider the instruction completed so consider the instruction completed so consider the instruction completed so now let's see what happens when you now let's see what happens when you now let's see what happens when you issue when you dispatch and when you issue when you dispatch and when you issue when you dispatch and when you write result

## Issue： Part 2
Kind: captions Language: en so the first step of Tomas Sol's so the first step of Tomas Sol's so the first step of Tomas Sol's algorithm that we will consider is issue algorithm that we will consider is issue algorithm that we will consider is issue so what happens during issue we take the so what happens during issue we take the so what happens during issue we take the next instruction in program order from next instruction in program order from next instruction in program order from the instruction queue this has to be the instruction queue this has to be the instruction queue this has to be done in program order for register done in program order for register done in program order for register naming to work correctly we then need to naming to work correctly we then need to naming to work correctly we then need to determine where the inputs of the determine where the inputs of the determine where the inputs of the instructions are going to come from are instructions are going to come from are instructions are going to come from are they already in the register file or are they already in the register file or are they already in the register file or are they going to be produced by some of the they going to be produced by some of the they going to be produced by some of the instructions that still hasn't broadcast instructions that still hasn't broadcast instructions that still hasn't broadcast its result and uh if we need to wait for its result and uh if we need to wait for its result and uh if we need to wait for an instruction which one so there will an instruction which one so there will an instruction which one so there will be some sort of rat table there we're be some sort of rat table there we're be some sort of rat table there we're then going to get a free or available then going to get a free or available then going to get a free or available reservation station of the correct kind reservation station of the correct kind reservation station of the correct kind not that there are some reservation not that there are some reservation not that there are some reservation stations that are for the adders some of stations that are for the adders some of stations that are for the adders some of them are for the multiplier so if this them are for the multiplier so if this them are for the multiplier so if this is an add instruction then we really is an add instruction then we really is an add instruction then we really need to find an available reservation need to find an available reservation need to find an available reservation station in front of the other if all of station in front of the other if all of station in front of the other if all of the reservation stations are busy at the reservation stations are busy at the reservation stations are busy at this point meaning they're already used this point meaning they're already used this point meaning they're already used by some other instructions and those by some other instructions and those by some other instructions and those instructions haven't left yet then we instructions haven't left yet then we instructions haven't left yet then we simply don't issue anything this cycle simply don't issue anything this cycle simply don't issue anything this cycle and in tomasulo's algorithm they were and in tomasulo's algorithm they were and in tomasulo's algorithm they were issuing one instruction at a time so so issuing one instruction at a time so so issuing one instruction at a time so so they they would just issue one they they would just issue one they they would just issue one instruction per cycle we would put the instruction per cycle we would put the instruction per cycle we would put the instruction in the reservation station instruction in the reservation station instruction in the reservation station then and we will tag the destination then and we will tag the destination then and we will tag the destination register of the instructions so that the register of the instructions so that the register of the instructions so that the result when it's produced goes there and result when it's produced goes there and result when it's produced goes there and also so that instructions that want that also so that instructions that want that also so that instructions that want that register in the future will know which register in the future will know which register in the future will know which instruction is going to produce this instruction is going to produce this instruction is going to produce this file if it has already not been produced

## Issue Example： Part 2
Kind: captions Language: en so we will have a running example of how so we will have a running example of how so we will have a running example of how the Tomaso algorithm works so whenever the Tomaso algorithm works so whenever the Tomaso algorithm works so whenever we do something kind of to explain it we we do something kind of to explain it we we do something kind of to explain it we will also show how it works so this is will also show how it works so this is will also show how it works so this is an example of how the issue is working an example of how the issue is working an example of how the issue is working first this is going to be our issue Q first this is going to be our issue Q first this is going to be our issue Q I'm showing four instructions here this I'm showing four instructions here this I'm showing four instructions here this is the first instruction this is the is the first instruction this is the is the first instruction this is the second one third one and so on so we second one third one and so on so we second one third one and so on so we will issue in program order which means will issue in program order which means will issue in program order which means the first instruction that we're going the first instruction that we're going the first instruction that we're going to be issue is going to be this F2 = F4 to be issue is going to be this F2 = F4 to be issue is going to be this F2 = F4 + fub1 this is really an ad but I'm + fub1 this is really an ad but I'm + fub1 this is really an ad but I'm writing it this way cuz it's more common writing it this way cuz it's more common writing it this way cuz it's more common next for each of the floting point next for each of the floting point next for each of the floting point registers which we are going to name registers which we are going to name registers which we are going to name fub1 F2 F3 and F4 there is a register fub1 F2 F3 and F4 there is a register fub1 F2 F3 and F4 there is a register Alias table or rat where we're going to Alias table or rat where we're going to Alias table or rat where we're going to keep which instruction is supposed to keep which instruction is supposed to keep which instruction is supposed to produce that register if there is produce that register if there is produce that register if there is nothing here that actually points to the nothing here that actually points to the nothing here that actually points to the register file so pretty much if there is register file so pretty much if there is register file so pretty much if there is nothing here in F1 that's going to mean nothing here in F1 that's going to mean nothing here in F1 that's going to mean that you can just find the value for F1 that you can just find the value for F1 that you can just find the value for F1 in the actual F1 in the register file in the actual F1 in the register file in the actual F1 in the register file this is our register file we have four this is our register file we have four this is our register file we have four registers and these are the their actual registers and these are the their actual registers and these are the their actual values when we go through the example we values when we go through the example we values when we go through the example we will actually compute with them so again will actually compute with them so again will actually compute with them so again this is the register file again the rat this is the register file again the rat this is the register file again the rat when it's empty says that basically this when it's empty says that basically this when it's empty says that basically this means F1 can just be read from the means F1 can just be read from the means F1 can just be read from the register file you don't have to wait for register file you don't have to wait for register file you don't have to wait for anything and finally we're going to have anything and finally we're going to have anything and finally we're going to have a reservation stations we're going to a reservation stations we're going to a reservation stations we're going to name them rs1 2 3 for the ad reservation name them rs1 2 3 for the ad reservation name them rs1 2 3 for the ad reservation stations and four and five for the stations and four and five for the stations and four and five for the multiplication reservation station so we multiplication reservation station so we multiplication reservation station so we really have five reservation stations really have five reservation stations really have five reservation stations but note that you can use only these but note that you can use only these but note that you can use only these three for ads and only these three for three for ads and only these three for three for ads and only these three for multiplies divides now we are ready to multiplies divides now we are ready to multiplies divides now we are ready to actually issue an instruction so again actually issue an instruction so again actually issue an instruction so again this is going to be the first one we this is going to be the first one we this is going to be the first one we issue the first step is to take it from issue the first step is to take it from issue the first step is to take it from the instruction buffer the next step is the instruction buffer the next step is the instruction buffer the next step is to look at the rat and find whether our to look at the rat and find whether our to look at the rat and find whether our inputs are ready or not and in this case inputs are ready or not and in this case inputs are ready or not and in this case both of the inputs are ready F1 and F4 both of the inputs are ready F1 and F4 both of the inputs are ready F1 and F4 are both are both are both available so we're going to say that available so we're going to say that available so we're going to say that this is an ADD this is an ADD this is an ADD and we're going to take the value for in and we're going to take the value for in and we're going to take the value for in this case F4 and F1 and that's going to this case F4 and F1 and that's going to this case F4 and F1 and that's going to be be be 0.71 and it's going to be 3.14 and place 0.71 and it's going to be 3.14 and place 0.71 and it's going to be 3.14 and place the instruction in the reservation the instruction in the reservation the instruction in the reservation station of course that assumes that station of course that assumes that station of course that assumes that there is a reservation station available there is a reservation station available there is a reservation station available we will see eventually what happens when we will see eventually what happens when we will see eventually what happens when there isn't any and the final step for there isn't any and the final step for there isn't any and the final step for register naming is to say that the register naming is to say that the register naming is to say that the result of the instruction F2 is going to result of the instruction F2 is going to result of the instruction F2 is going to come from now on from reservation come from now on from reservation come from now on from reservation station one so here we will write station one so here we will write station one so here we will write rs1 so future instructions that want F2 rs1 so future instructions that want F2 rs1 so future instructions that want F2 are not going to read minus one they're are not going to read minus one they're are not going to read minus one they're going to wait for this result to be going to wait for this result to be going to wait for this result to be produced so this is the renaming of the produced so this is the renaming of the produced so this is the renaming of the very first instruction one final step very first instruction one final step very first instruction one final step that we will do of course is remove this that we will do of course is remove this that we will do of course is remove this instruction from the issue queue and instruction from the issue queue and instruction from the issue queue and what's going to happen now is this is what's going to happen now is this is what's going to happen now is this is going to shift down but for my example going to shift down but for my example going to shift down but for my example that's going to be very difficult to do that's going to be very difficult to do that's going to be very difficult to do so I'm just going to kind of cross out so I'm just going to kind of cross out so I'm just going to kind of cross out this instructions so this is this just this instructions so this is this just this instructions so this is this just means that you know this instruction has means that you know this instruction has means that you know this instruction has been removed from the issue Cube been removed from the issue Cube been removed from the issue Cube to make things more interesting for to make things more interesting for to make things more interesting for remaining instructions and also to see remaining instructions and also to see remaining instructions and also to see what happens when an instruction is what happens when an instruction is what happens when an instruction is renamed let's issue one or two more renamed let's issue one or two more renamed let's issue one or two more instructions so let's issue the next instructions so let's issue the next instructions so let's issue the next instruction which is this one this is a instruction which is this one this is a instruction which is this one this is a divide we need to find a free divide we need to find a free divide we need to find a free reservation station here say it's a reservation station here say it's a reservation station here say it's a divide F2 and F3 are going to be the divide F2 and F3 are going to be the divide F2 and F3 are going to be the parameters for this instruction F2 comes parameters for this instruction F2 comes parameters for this instruction F2 comes from rs1 so we cannot simply read this from rs1 so we cannot simply read this from rs1 so we cannot simply read this value here we're going to say here that value here we're going to say here that value here we're going to say here that rs1 rs1 rs1 is what we're waiting for but F3 is is what we're waiting for but F3 is is what we're waiting for but F3 is available so we're going to get available so we're going to get available so we're going to get 2.72 from the register F and we now Mark 2.72 from the register F and we now Mark 2.72 from the register F and we now Mark this instruction as we're going to this instruction as we're going to this instruction as we're going to basically take it out of the issue q and basically take it out of the issue q and basically take it out of the issue q and this is so very easy to forget but when this is so very easy to forget but when this is so very easy to forget but when we do that we need to mark that the we do that we need to mark that the we do that we need to mark that the result the F1 is from now on not going result the F1 is from now on not going result the F1 is from now on not going to be coming from here it's going to be to be coming from here it's going to be to be coming from here it's going to be coming from reservation station 4 and coming from reservation station 4 and coming from reservation station 4 and we're going to issue this instruction we're going to issue this instruction we're going to issue this instruction two it's a subtraction so it's going to two it's a subtraction so it's going to two it's a subtraction so it's going to go here fub1 and FS2 are the go here fub1 and FS2 are the go here fub1 and FS2 are the parameters we see that neither of those parameters we see that neither of those parameters we see that neither of those two can be read from the register file two can be read from the register file two can be read from the register file so what we're going to do is we're going so what we're going to do is we're going so what we're going to do is we're going to Mark here that that the first to Mark here that that the first to Mark here that that the first parameter is going to be produced by RS4 parameter is going to be produced by RS4 parameter is going to be produced by RS4 the second one is going to be produced the second one is going to be produced the second one is going to be produced by by by rs1 we're going to then Mark that F4 is rs1 we're going to then Mark that F4 is rs1 we're going to then Mark that F4 is coming from coming from coming from rs2 and we're going to remove this rs2 and we're going to remove this rs2 and we're going to remove this instruction from the isq of course more instruction from the isq of course more instruction from the isq of course more instructions are probably going to come instructions are probably going to come instructions are probably going to come this way but we're not going to look at this way but we're not going to look at this way but we're not going to look at that because again that that too many that because again that that too many that because again that that too many instructions for this example now let's instructions for this example now let's instructions for this example now let's say that there wasn't an issue slot here say that there wasn't an issue slot here say that there wasn't an issue slot here we then couldn't issue the next we then couldn't issue the next we then couldn't issue the next instruction in the next cycle until we instruction in the next cycle until we instruction in the next cycle until we are actually done executing one one of are actually done executing one one of are actually done executing one one of these two I'm not going to do that but these two I'm not going to do that but these two I'm not going to do that but just just keep in mind that when you just just keep in mind that when you just just keep in mind that when you want to issue an instruction you first want to issue an instruction you first want to issue an instruction you first have to check whether a reservation have to check whether a reservation have to check whether a reservation station is available if not then we station is available if not then we station is available if not then we simply cannot issue that in ruction simply cannot issue that in ruction simply cannot issue that in ruction let's issue it now because we do have let's issue it now because we do have let's issue it now because we do have room for it it's an addition of FS2 and room for it it's an addition of FS2 and room for it it's an addition of FS2 and fs3 F2 comes from reservation station fs3 F2 comes from reservation station fs3 F2 comes from reservation station one F3 comes from the register file so one F3 comes from the register file so one F3 comes from the register file so we're just going to take the we're just going to take the we're just going to take the 2.72 we're going to then rename F1 to 2.72 we're going to then rename F1 to 2.72 we're going to then rename F1 to point to this result and now we see that point to this result and now we see that point to this result and now we see that F1 is already renamed to reservation F1 is already renamed to reservation F1 is already renamed to reservation station 4 that's station 4 that's station 4 that's basically if you want to use F1 basically if you want to use F1 basically if you want to use F1 since this instruction you would be since this instruction you would be since this instruction you would be waiting for its result but now F1 is waiting for its result but now F1 is waiting for its result but now F1 is being basically renamed to another being basically renamed to another being basically renamed to another reservation station so this is how reservation station so this is how reservation station so this is how register naming here Works in tomasulo's register naming here Works in tomasulo's register naming here Works in tomasulo's algorithm so what we're going to do is algorithm so what we're going to do is algorithm so what we're going to do is we're going to basically overwrite this we're going to basically overwrite this we're going to basically overwrite this with the name of this reservation with the name of this reservation with the name of this reservation station which is station which is station which is RS3 and now we have issued all of our RS3 and now we have issued all of our RS3 and now we have issued all of our four instructions in a real four instructions in a real four instructions in a real processor you would issue one processor you would issue one processor you would issue one instruction each cycle and probably some instruction each cycle and probably some instruction each cycle and probably some of this instructions would have executed of this instructions would have executed of this instructions would have executed by the time we issue all of them but I by the time we issue all of them but I by the time we issue all of them but I just wanted to show you how issue works just wanted to show you how issue works just wanted to show you how issue works if for example nothing could execute for if for example nothing could execute for if for example nothing could execute for some reason now that we have gone some reason now that we have gone some reason now that we have gone through an example of issuing let's see through an example of issuing let's see through an example of issuing let's see what the next step is

## Issue Quiz： Part 2
Kind: captions Language: en before we move on to the next stage before we move on to the next stage before we move on to the next stage which would be dis patch let's do a which would be dis patch let's do a which would be dis patch let's do a issue quiz so in this quiz we have our issue quiz so in this quiz we have our issue quiz so in this quiz we have our instruction queue we have a rat we have instruction queue we have a rat we have instruction queue we have a rat we have a register file we have a reservation a register file we have a reservation a register file we have a reservation stations these two instructions are stations these two instructions are stations these two instructions are already reservation stations there are already reservation stations there are already reservation stations there are two left to issue and these are the two left to issue and these are the two left to issue and these are the values of the register file of the Rat values of the register file of the Rat values of the register file of the Rat Etc so your task is to issue these two Etc so your task is to issue these two Etc so your task is to issue these two instructions or at least issue those instructions or at least issue those instructions or at least issue those that can be issued at this time you that can be issued at this time you that can be issued at this time you don't need to consider when these don't need to consider when these don't need to consider when these extractions are going to finish so just extractions are going to finish so just extractions are going to finish so just issue whatever can be issued in the next issue whatever can be issued in the next issue whatever can be issued in the next several Cycles assuming that these are several Cycles assuming that these are several Cycles assuming that these are just not going to leave from here just not going to leave from here just not going to leave from here anytime soon


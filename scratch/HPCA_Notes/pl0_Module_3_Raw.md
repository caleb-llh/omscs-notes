# Playlist 0 Module 3 Raw Transcript

## Performance Comparison Quiz Solution： Part 1
Kind: captions Language: en let's see the solution for the let's see the solution for the let's see the solution for the performance comparison quiz we had a performance comparison quiz we had a performance comparison quiz we had a laptop that takes four hours to compress laptop that takes four hours to compress laptop that takes four hours to compress this video this is a latency so the this video this is a latency so the this video this is a latency so the speed-up of new versus old will be the speed-up of new versus old will be the speed-up of new versus old will be the old latency divided by the new latency old latency divided by the new latency old latency divided by the new latency four hours is 240 minutes the new four hours is 240 minutes the new four hours is 240 minutes the new latency is 10 minutes so the speed-up is latency is 10 minutes so the speed-up is latency is 10 minutes so the speed-up is 24 so the correct answer here is 24 24 so the correct answer here is 24 24 so the correct answer here is 24 intuitively a speed-up larger than 1 intuitively a speed-up larger than 1 intuitively a speed-up larger than 1 means that the new thing is actually means that the new thing is actually means that the new thing is actually faster than the old thing speed ups less faster than the old thing speed ups less faster than the old thing speed ups less than 1 mean that the new thing is than 1 mean that the new thing is than 1 mean that the new thing is actually slower so if you divide the actually slower so if you divide the actually slower so if you divide the wrong things here if you took for wrong things here if you took for wrong things here if you took for example 10 over 240 you would have example 10 over 240 you would have example 10 over 240 you would have gotten something like 0.04 and at that gotten something like 0.04 and at that gotten something like 0.04 and at that point you can check the new thing is point you can check the new thing is point you can check the new thing is actually significantly faster so if actually significantly faster so if actually significantly faster so if you're getting a number that is much you're getting a number that is much you're getting a number that is much lower than 1 that probably means you're lower than 1 that probably means you're lower than 1 that probably means you're dividing the numbers wrongly

## Performance Comparison Quiz 2： Part 1
Kind: captions Language: en let's do another performance comparison let's do another performance comparison let's do another performance comparison quiz let's say now we have the new quiz let's say now we have the new quiz let's say now we have the new laptop that can compress this video in laptop that can compress this video in laptop that can compress this video in 10 minutes but we dropped this laptop 10 minutes but we dropped this laptop 10 minutes but we dropped this laptop and it falls down the storm drain so we and it falls down the storm drain so we and it falls down the storm drain so we have to use the old laptop they took have to use the old laptop they took have to use the old laptop they took four hours to do this so what is the four hours to do this so what is the four hours to do this so what is the speed-up of using the old laptop instead speed-up of using the old laptop instead speed-up of using the old laptop instead of the new one that fell down the drain

## Performance Comparison Quiz Solution 2： Part 1
Kind: captions Language: en let's discuss the solution for the let's discuss the solution for the let's discuss the solution for the second performance comparison quiz now second performance comparison quiz now second performance comparison quiz now the laptop that we had took 10 minutes the laptop that we had took 10 minutes the laptop that we had took 10 minutes and the new one takes 4 hours because we and the new one takes 4 hours because we and the new one takes 4 hours because we are actually using the old laptop to do are actually using the old laptop to do are actually using the old laptop to do this the speed up is equal to the this the speed up is equal to the this the speed up is equal to the latency of the previous thing which is latency of the previous thing which is latency of the previous thing which is 10 minutes divided by the latency of the 10 minutes divided by the latency of the 10 minutes divided by the latency of the new thing we're using which is 240 so new thing we're using which is 240 so new thing we're using which is 240 so this time the speed up is 0.04 so the this time the speed up is 0.04 so the this time the speed up is 0.04 so the correct answer here is 0.04 correct answer here is 0.04 correct answer here is 0.04 again if there is an actual slowdown again if there is an actual slowdown again if there is an actual slowdown from the previous thing to the new thing from the previous thing to the new thing from the previous thing to the new thing we're using then we can expect the speed we're using then we can expect the speed we're using then we can expect the speed up to be lower than one and if we got up to be lower than one and if we got up to be lower than one and if we got instead 24 as our answer that should instead 24 as our answer that should instead 24 as our answer that should mean that whatever we are using now is mean that whatever we are using now is mean that whatever we are using now is faster than before so that should tell faster than before so that should tell faster than before so that should tell us that something is wrong so pretty us that something is wrong so pretty us that something is wrong so pretty much by kind of checking if it makes much by kind of checking if it makes much by kind of checking if it makes sense you know is the new thing actually sense you know is the new thing actually sense you know is the new thing actually having a speed up as opposed to slowdown having a speed up as opposed to slowdown having a speed up as opposed to slowdown if it is a speed up it should be above if it is a speed up it should be above if it is a speed up it should be above one a speed up lower than one is an one a speed up lower than one is an one a speed up lower than one is an actual Slowdown

## Speedup： Part 1
Kind: captions Language: en so let's discuss the speed up numbers we so let's discuss the speed up numbers we so let's discuss the speed up numbers we can get we have already seen in the can get we have already seen in the can get we have already seen in the previous quizzes that a speed up larger previous quizzes that a speed up larger previous quizzes that a speed up larger than one means improve performance than one means improve performance than one means improve performance either through shorter execution time it either through shorter execution time it either through shorter execution time it takes less to compute something than takes less to compute something than takes less to compute something than before or higher throughput we can do before or higher throughput we can do before or higher throughput we can do more of something per second than before more of something per second than before more of something per second than before a speed up of less than one means that a speed up of less than one means that a speed up of less than one means that we have worse performance than before we have worse performance than before we have worse performance than before either it takes longer to execute either it takes longer to execute either it takes longer to execute something or we get lower throughput something or we get lower throughput something or we get lower throughput than before and finally when Computing than before and finally when Computing than before and finally when Computing speed up remember that performance is speed up remember that performance is speed up remember that performance is proportional to throughput so when proportional to throughput so when proportional to throughput so when Computing speed up we divide the Computing speed up we divide the Computing speed up we divide the throughput of the new thing with the throughput of the new thing with the throughput of the new thing with the throughput of the old thing however throughput of the old thing however throughput of the old thing however performance is proportional to one over performance is proportional to one over performance is proportional to one over latency so if we have something like it latency so if we have something like it latency so if we have something like it takes two seconds to do something that's takes two seconds to do something that's takes two seconds to do something that's a latency performance in this case is a latency performance in this case is a latency performance in this case is one over that so when Computing speed one over that so when Computing speed one over that so when Computing speed UPS we actually divide the old latency UPS we actually divide the old latency UPS we actually divide the old latency with the new latency

## Measuring Performance： Part 1
Kind: captions Language: en now let's talk about measuring now let's talk about measuring now let's talk about measuring performance we have seen that we can performance we have seen that we can performance we have seen that we can express performance as one over express performance as one over express performance as one over execution time but the execution time is execution time but the execution time is execution time but the execution time is on what workload so if we want to on what workload so if we want to on what workload so if we want to measure performance on two machines and measure performance on two machines and measure performance on two machines and compare them the question is what do we compare them the question is what do we compare them the question is what do we run on them to tell which one is faster run on them to tell which one is faster run on them to tell which one is faster than the other and by how much the best than the other and by how much the best than the other and by how much the best assessment of how do they compare is if assessment of how do they compare is if assessment of how do they compare is if we run actual workloads from a we run actual workloads from a we run actual workloads from a particular user but this requires us to particular user but this requires us to particular user but this requires us to run many different programs what we get run many different programs what we get run many different programs what we get is not repres representative of other is not repres representative of other is not repres representative of other users who might be using this machine users who might be using this machine users who might be using this machine completely differently and there is the completely differently and there is the completely differently and there is the question of how do we get the worklow question of how do we get the worklow question of how do we get the worklow data even if we found a user whose usage data even if we found a user whose usage data even if we found a user whose usage patterns are representative of many patterns are representative of many patterns are representative of many others how do we persuade that person to others how do we persuade that person to others how do we persuade that person to give us exactly what they were doing give us exactly what they were doing give us exactly what they were doing with the machine over a course of let's with the machine over a course of let's with the machine over a course of let's say a day so when we compare performance say a day so when we compare performance say a day so when we compare performance of different machines we usually don't of different machines we usually don't of different machines we usually don't do this instead we use so-called do this instead we use so-called do this instead we use so-called benchmark workloads

## Benchmarks： Part 1
Kind: captions Language: en so what are these benchmarks they're so what are these benchmarks they're so what are these benchmarks they're programs an input data that users or programs an input data that users or programs an input data that users or companies have agreed upon will be used companies have agreed upon will be used companies have agreed upon will be used for performance measurements so pretty for performance measurements so pretty for performance measurements so pretty much either a consortium of companies much either a consortium of companies much either a consortium of companies meets and decides that these are the meets and decides that these are the meets and decides that these are the programs and input data that we will use programs and input data that we will use programs and input data that we will use for performance measurements or a group for performance measurements or a group for performance measurements or a group of people who is concerned about some of people who is concerned about some of people who is concerned about some performance issue will design programs performance issue will design programs performance issue will design programs and data and allow others to measure and data and allow others to measure and data and allow others to measure performance on that type of a problem performance on that type of a problem performance on that type of a problem using those programs and input data using those programs and input data using those programs and input data usually we don't have just one benchmark usually we don't have just one benchmark usually we don't have just one benchmark program we have a benchmark suite it program we have a benchmark suite it program we have a benchmark suite it consists of multiple programs along with consists of multiple programs along with consists of multiple programs along with their input data each of these programs their input data each of these programs their input data each of these programs is usually chosen to be representative is usually chosen to be representative is usually chosen to be representative of some type of applications so for of some type of applications so for of some type of applications so for example we will not have ten different example we will not have ten different example we will not have ten different compression programs in a benchmark we compression programs in a benchmark we compression programs in a benchmark we will usually have one or two because we will usually have one or two because we will usually have one or two because we think that those one or two are probably think that those one or two are probably think that those one or two are probably accurately representing the behavior of accurately representing the behavior of accurately representing the behavior of the other compression programs that we the other compression programs that we the other compression programs that we didn't put in the benchmark suite

## Types of Benchmarks： Part 1
Kind: captions Language: en so let's discuss what types of so let's discuss what types of so let's discuss what types of benchmarks do we have out there we can benchmarks do we have out there we can benchmarks do we have out there we can have real applications they are the most have real applications they are the most have real applications they are the most representative of real applications but representative of real applications but representative of real applications but they're also the most difficult to set they're also the most difficult to set they're also the most difficult to set up on a new machine so for example if up on a new machine so for example if up on a new machine so for example if we're building a new computer system we we're building a new computer system we we're building a new computer system we may not be able to run a full fledged may not be able to run a full fledged may not be able to run a full fledged operating system there and have all the operating system there and have all the operating system there and have all the you know graphics cards and hard drives you know graphics cards and hard drives you know graphics cards and hard drives etcetera etcetera connected so it may etcetera etcetera connected so it may etcetera etcetera connected so it may not be possible to run for example a web not be possible to run for example a web not be possible to run for example a web browser or a database on this machine browser or a database on this machine browser or a database on this machine yet next we can have application kernels yet next we can have application kernels yet next we can have application kernels we find the most time-consuming part of we find the most time-consuming part of we find the most time-consuming part of an application usually a loop of some an application usually a loop of some an application usually a loop of some sort and we isolate just that loop and sort and we isolate just that loop and sort and we isolate just that loop and say that this is what are we gonna use say that this is what are we gonna use say that this is what are we gonna use for testing performance of machines that for testing performance of machines that for testing performance of machines that way we don't have to run a full-blown way we don't have to run a full-blown way we don't have to run a full-blown application all we have to do is find a application all we have to do is find a application all we have to do is find a way to run just the kernels on the new way to run just the kernels on the new way to run just the kernels on the new machine that we're testing but sometimes machine that we're testing but sometimes machine that we're testing but sometimes running a reasonable sized kernel is running a reasonable sized kernel is running a reasonable sized kernel is still too difficult for example in the still too difficult for example in the still too difficult for example in the early stages of prototyping a machine early stages of prototyping a machine early stages of prototyping a machine when we may not even have a compiler for when we may not even have a compiler for when we may not even have a compiler for example so the next class of benchmarks example so the next class of benchmarks example so the next class of benchmarks are the so-called synthetic benchmarks are the so-called synthetic benchmarks are the so-called synthetic benchmarks they're usually designed to behave they're usually designed to behave they're usually designed to behave similarly to some type of a kernel but similarly to some type of a kernel but similarly to some type of a kernel but to be much simpler to compile pretty to be much simpler to compile pretty to be much simpler to compile pretty much they're kind of the abstract of a much they're kind of the abstract of a much they're kind of the abstract of a kernel and finally we can compare kernel and finally we can compare kernel and finally we can compare machines not based on how well they machines not based on how well they machines not based on how well they perform on some actual code that they're perform on some actual code that they're perform on some actual code that they're running but purely based on peak running but purely based on peak running but purely based on peak performance in theory how many performance in theory how many performance in theory how many instructions per second should this instructions per second should this instructions per second should this machine be able to execute usually peak machine be able to execute usually peak machine be able to execute usually peak performance is only good for marketing performance is only good for marketing performance is only good for marketing doesn't tell us very much about the doesn't tell us very much about the doesn't tell us very much about the actual performance of the machine actual performance of the machine actual performance of the machine synthetic benchmarks are usually very synthetic benchmarks are usually very synthetic benchmarks are usually very good for design studies of potential new good for design studies of potential new good for design studies of potential new machines but they are not very good for machines but they are not very good for machines but they are not very good for actually reporting performance to others actually reporting performance to others actually reporting performance to others so we can use this just to see how well so we can use this just to see how well so we can use this just to see how well do we expect a design to behave so that do we expect a design to behave so that do we expect a design to behave so that we can choose among multiple possible we can choose among multiple possible we can choose among multiple possible designs once we actually build a machine designs once we actually build a machine designs once we actually build a machine we can use for example kernels to test we can use for example kernels to test we can use for example kernels to test the performance of a prototype and the performance of a prototype and the performance of a prototype and finally we should be using real finally we should be using real finally we should be using real applications if we are comparing our applications if we are comparing our applications if we are comparing our machines that we're trying to for machines that we're trying to for machines that we're trying to for example sell to somebody

## Performance Reporting Quiz： Part 1
Kind: captions Language: en so let's do a quick check of our so let's do a quick check of our so let's do a quick check of our knowledge of benchmarks and performance knowledge of benchmarks and performance knowledge of benchmarks and performance reporting which of the following uses reporting which of the following uses reporting which of the following uses code from real applications but does not code from real applications but does not code from real applications but does not use all of the code from any of the use all of the code from any of the use all of the code from any of the applications the choices are the actual applications the choices are the actual applications the choices are the actual workload mix from the user and workload mix from the user and workload mix from the user and application suite and application kernel application suite and application kernel application suite and application kernel synthetic benchmarks and peak synthetic benchmarks and peak synthetic benchmarks and peak performance reporting select all of performance reporting select all of performance reporting select all of those that satisfy this

## Performance Reporting Quiz Solution： Part 1
Kind: captions Language: en let's discuss the solution for our let's discuss the solution for our let's discuss the solution for our performance reporting quiz we need to performance reporting quiz we need to performance reporting quiz we need to check among these all of those that use check among these all of those that use check among these all of those that use quotes from real applications but not quotes from real applications but not quotes from real applications but not all of the code peak performance doesn't all of the code peak performance doesn't all of the code peak performance doesn't use any code synthetic benchmarks do not use any code synthetic benchmarks do not use any code synthetic benchmarks do not use code from real applications this is use code from real applications this is use code from real applications this is code specifically designed for testing code specifically designed for testing code specifically designed for testing application kernels do use code from application kernels do use code from application kernels do use code from real applications but that application real applications but that application real applications but that application code has been reduced to form a kernel code has been reduced to form a kernel code has been reduced to form a kernel so this is definitely true an so this is definitely true an so this is definitely true an application suite uses the code from application suite uses the code from application suite uses the code from real applications and uses all of the real applications and uses all of the real applications and uses all of the code for all of these applications so code for all of these applications so code for all of these applications so this is not true and then an actual this is not true and then an actual this is not true and then an actual workload mix is similar to an workload mix is similar to an workload mix is similar to an application suite except that it comes application suite except that it comes application suite except that it comes from usage patterns of a specific user from usage patterns of a specific user from usage patterns of a specific user so the answer to this quiz is that so the answer to this quiz is that so the answer to this quiz is that application kernels use code from your application kernels use code from your application kernels use code from your applications but not all of their code

## Benchmark Standards： Part 1
Kind: captions Language: en benchmarks are very important to benchmarks are very important to benchmarks are very important to computer manufacturers so usually there computer manufacturers so usually there computer manufacturers so usually there are standards organizations that put are standards organizations that put are standards organizations that put together benchmark suites for use in together benchmark suites for use in together benchmark suites for use in comparing performance between machines comparing performance between machines comparing performance between machines so usually there is a benchmarking so usually there is a benchmarking so usually there is a benchmarking organization that takes input from organization that takes input from organization that takes input from manufacturers of systems from user manufacturers of systems from user manufacturers of systems from user groups from experts in academia and so groups from experts in academia and so groups from experts in academia and so on and produces a standard benchmark on and produces a standard benchmark on and produces a standard benchmark suite along with instructions on how to suite along with instructions on how to suite along with instructions on how to produce representative measurements so produce representative measurements so produce representative measurements so this is how we got tpc this is how we got tpc this is how we got tpc benchmarks benchmarks benchmarks which are used for databases web servers which are used for databases web servers which are used for databases web servers data mining and other transaction data mining and other transaction data mining and other transaction processing so-called embassy benchmarks processing so-called embassy benchmarks processing so-called embassy benchmarks which are used in embedded processing which are used in embedded processing which are used in embedded processing such as cars video players printers such as cars video players printers such as cars video players printers phones etc so if we're testing a phones etc so if we're testing a phones etc so if we're testing a processor for for example potentially processor for for example potentially processor for for example potentially using a car using a car using a car we would use the embassy benchmarks not we would use the embassy benchmarks not we would use the embassy benchmarks not the tpc the tpc the tpc and the spec benchmarks which are and the spec benchmarks which are and the spec benchmarks which are typically used to evaluate engineering typically used to evaluate engineering typically used to evaluate engineering workstations and also raw processors workstations and also raw processors workstations and also raw processors because these benchmarks unlike the because these benchmarks unlike the because these benchmarks unlike the other two other two other two are not very i o intensive they're are not very i o intensive they're are not very i o intensive they're mostly mostly mostly processor oriented processor oriented processor oriented so the spec benchmark suite includes a so the spec benchmark suite includes a so the spec benchmark suite includes a number of applications such as gcc number of applications such as gcc number of applications such as gcc which represents software development which represents software development which represents software development workloads like a compiler workloads like a compiler workloads like a compiler several applications like b waves and several applications like b waves and several applications like b waves and lbm that are representative of fluid lbm that are representative of fluid lbm that are representative of fluid dynamics workloads perl dynamics workloads perl dynamics workloads perl which is representative of string which is representative of string which is representative of string processing applications catus adm this processing applications catus adm this processing applications catus adm this is a physics simulation related to is a physics simulation related to is a physics simulation related to relativity this is a parser for xml relativity this is a parser for xml relativity this is a parser for xml several solvers of differential several solvers of differential several solvers of differential equations equations equations b zip a compression application two game b zip a compression application two game b zip a compression application two game ai applications and so on as you can see ai applications and so on as you can see ai applications and so on as you can see the spec benchmarks really try to cover the spec benchmarks really try to cover the spec benchmarks really try to cover a variety of potential uses for a variety of potential uses for a variety of potential uses for processors and high performance systems

## Summarizing Performance： Part 1
Kind: captions Language: en so let's say we have something like 26 so let's say we have something like 26 so let's say we have something like 26 applications in a benchmark Suite how do applications in a benchmark Suite how do applications in a benchmark Suite how do we summarize the overall performance of we summarize the overall performance of we summarize the overall performance of a machine on the entire Suite so we want a machine on the entire Suite so we want a machine on the entire Suite so we want a single number that represents the a single number that represents the a single number that represents the performance of the machine usually we performance of the machine usually we performance of the machine usually we want to use something like the average want to use something like the average want to use something like the average execution time so let's say we have a execution time so let's say we have a execution time so let's say we have a computer X and a computer Y A Benchmark computer X and a computer Y A Benchmark computer X and a computer Y A Benchmark application a b and c and let's say that application a b and c and let's say that application a b and c and let's say that computer X takes 9 seconds here 10 computer X takes 9 seconds here 10 computer X takes 9 seconds here 10 seconds here and 5 Seconds here whereas seconds here and 5 Seconds here whereas seconds here and 5 Seconds here whereas the computer y takes 18 seconds here 7 the computer y takes 18 seconds here 7 the computer y takes 18 seconds here 7 Seconds here and 11 seconds here the Seconds here and 11 seconds here the Seconds here and 11 seconds here the speed up of machine X over machine y on speed up of machine X over machine y on speed up of machine X over machine y on each of these applications is two for each of these applications is two for each of these applications is two for application a because X is doing it in application a because X is doing it in application a because X is doing it in half the time as y 0.7 on application B half the time as y 0.7 on application B half the time as y 0.7 on application B Because X is actually slower than y Because X is actually slower than y Because X is actually slower than y there and here it's 2.2 because X is there and here it's 2.2 because X is there and here it's 2.2 because X is more than twice as fast as y now let's more than twice as fast as y now let's more than twice as fast as y now let's compute the average compute the average compute the average execution time here we have 9 + 10 + 5 execution time here we have 9 + 10 + 5 execution time here we have 9 + 10 + 5 over 3 gives us 8 seconds so on average over 3 gives us 8 seconds so on average over 3 gives us 8 seconds so on average computer X finishes one of these three computer X finishes one of these three computer X finishes one of these three applications in 8 seconds on average applications in 8 seconds on average applications in 8 seconds on average computer y finishes one of these three computer y finishes one of these three computer y finishes one of these three in 18 + 7 + 11 over 3 12 seconds if we in 18 + 7 + 11 over 3 12 seconds if we in 18 + 7 + 11 over 3 12 seconds if we now compute the speed up of X over y on now compute the speed up of X over y on now compute the speed up of X over y on average it will be 1.5 when comp average it will be 1.5 when comp average it will be 1.5 when comp computed this way but if we average the computed this way but if we average the computed this way but if we average the speed UPS here we get 1.63 so if we want speed UPS here we get 1.63 so if we want speed UPS here we get 1.63 so if we want to use average execution times we cannot to use average execution times we cannot to use average execution times we cannot simply average the speed UPS on simply average the speed UPS on simply average the speed UPS on individual applications to get the individual applications to get the individual applications to get the average speed up pretty much the speed average speed up pretty much the speed average speed up pretty much the speed up using average execution times is not up using average execution times is not up using average execution times is not the same as the average of speed UPS on the same as the average of speed UPS on the same as the average of speed UPS on these individual applications if we want these individual applications if we want these individual applications if we want to be able to average speed UPS then we to be able to average speed UPS then we to be able to average speed UPS then we need to use geometric means for both of need to use geometric means for both of need to use geometric means for both of our application execution times and for our application execution times and for our application execution times and for our speed UPS in that case we get a our speed UPS in that case we get a our speed UPS in that case we get a geometric mean of 7.66 here geometric mean of 7.66 here geometric mean of 7.66 here 11.15 here and 1.4 56 here so we have 11.15 here and 1.4 56 here so we have 11.15 here and 1.4 56 here so we have used the geometric mean for the used the geometric mean for the used the geometric mean for the execution times for each of the computer execution times for each of the computer execution times for each of the computer if we use the geometric mean for these if we use the geometric mean for these if we use the geometric mean for these speedups we will get the same value here speedups we will get the same value here speedups we will get the same value here the geometric mean is computed by the geometric mean is computed by the geometric mean is computed by multiplying out for example 9 * 10 * 5 multiplying out for example 9 * 10 * 5 multiplying out for example 9 * 10 * 5 and then taking the nth root in this and then taking the nth root in this and then taking the nth root in this case third root of the value to get the case third root of the value to get the case third root of the value to get the geometric mean and because things are geometric mean and because things are geometric mean and because things are being multiplied out and then the nend being multiplied out and then the nend being multiplied out and then the nend through take and you can see how if we through take and you can see how if we through take and you can see how if we divide these things we will get that the divide these things we will get that the divide these things we will get that the speed up computed geometric means of speed up computed geometric means of speed up computed geometric means of execution times will be the same as the execution times will be the same as the execution times will be the same as the mean of the speed UPS because we have 3 mean of the speed UPS because we have 3 mean of the speed UPS because we have 3 root of 18 * 7 * 11 these two divided is root of 18 * 7 * 11 these two divided is root of 18 * 7 * 11 these two divided is the same as thir root of 9 18 * 10/ 7 * the same as thir root of 9 18 * 10/ 7 * the same as thir root of 9 18 * 10/ 7 * 5 11 so as you can see this is the 5 11 so as you can see this is the 5 11 so as you can see this is the geometric mean of execution times then geometric mean of execution times then geometric mean of execution times then we can compute the speed up using them we can compute the speed up using them we can compute the speed up using them and that's the same as the geometric and that's the same as the geometric and that's the same as the geometric mean of individual speedups however this mean of individual speedups however this mean of individual speedups however this only works for geometric means we do only works for geometric means we do only works for geometric means we do prefer average execution time when we prefer average execution time when we prefer average execution time when we are Computing the average times however are Computing the average times however are Computing the average times however just you must be careful not to compute just you must be careful not to compute just you must be careful not to compute individual speedups and then a a them individual speedups and then a a them individual speedups and then a a them instead you should be Computing the instead you should be Computing the instead you should be Computing the average execution times and then average execution times and then average execution times and then Computing the speed up using those Computing the speed up using those Computing the speed up using those averages you should also remember that averages you should also remember that averages you should also remember that simple average should not be used on simple average should not be used on simple average should not be used on ratios of times so basically a speed up ratios of times so basically a speed up ratios of times so basically a speed up here is a ratio of two times we should here is a ratio of two times we should here is a ratio of two times we should not be using a simple arithmetic mean on not be using a simple arithmetic mean on not be using a simple arithmetic mean on these values because they are ratios these values because they are ratios these values because they are ratios geometric means on the other hand can be geometric means on the other hand can be geometric means on the other hand can be used on ratios but for for this to work used on ratios but for for this to work used on ratios but for for this to work we also have to use geometric mean on we also have to use geometric mean on we also have to use geometric mean on times too

## Speedup Averaging Quiz： Part 1
Kind: captions Language: en let's do a speed up averaging quiz let's let's do a speed up averaging quiz let's let's do a speed up averaging quiz let's say we have an old laptop and a new say we have an old laptop and a new say we have an old laptop and a new laptop let's say that we have two laptop let's say that we have two laptop let's say that we have two applications that are representative for applications that are representative for applications that are representative for our workloads a homework formatting our workloads a homework formatting our workloads a homework formatting application and a virus scan application application and a virus scan application application and a virus scan application for homework formatting going from the for homework formatting going from the for homework formatting going from the old laptop to the new laptop on homework old laptop to the new laptop on homework old laptop to the new laptop on homework formatting gives us a speed up of two formatting gives us a speed up of two formatting gives us a speed up of two for virus scanning going from the old for virus scanning going from the old for virus scanning going from the old laptop to the new laptop gives us a laptop to the new laptop gives us a laptop to the new laptop gives us a speed up of eight so overall going from speed up of eight so overall going from speed up of eight so overall going from the old laptop to the new laptop gives the old laptop to the new laptop gives the old laptop to the new laptop gives us a speed up of how much

## Speedup Averaging Quiz Solution： Part 1
Kind: captions Language: en so let's discuss the solution for our so let's discuss the solution for our so let's discuss the solution for our speed-up averaging quiz we only have the speed-up averaging quiz we only have the speed-up averaging quiz we only have the speed ups for the two applications and speed ups for the two applications and speed ups for the two applications and we have said previously that an we have said previously that an we have said previously that an arithmetic average of speed ups does not arithmetic average of speed ups does not arithmetic average of speed ups does not make sense you should not use arithmetic make sense you should not use arithmetic make sense you should not use arithmetic averaging for speed ups because there averaging for speed ups because there averaging for speed ups because there are ratios of execution times so the are ratios of execution times so the are ratios of execution times so the only thing that does make sense to use only thing that does make sense to use only thing that does make sense to use on speed ups in this case is a geometric on speed ups in this case is a geometric on speed ups in this case is a geometric mean thus the speed up we need is a 2 mean thus the speed up we need is a 2 mean thus the speed up we need is a 2 times 8 and then there are 2 values so times 8 and then there are 2 values so times 8 and then there are 2 values so we take the second root of that and we we take the second root of that and we we take the second root of that and we get a number 4 so the correct answer get a number 4 so the correct answer get a number 4 so the correct answer here is the speed up is 4 again doing here is the speed up is 4 again doing here is the speed up is 4 again doing something like 2 plus 8 over 2 is not something like 2 plus 8 over 2 is not something like 2 plus 8 over 2 is not correct here because these are ratios correct here because these are ratios correct here because these are ratios you should not use the normal arithmetic you should not use the normal arithmetic you should not use the normal arithmetic mean for that

## Iron Law of Performance： Part 1
Kind: captions Language: en the next thing we will discuss is the so the next thing we will discuss is the so the next thing we will discuss is the so called iron law of performance for a called iron law of performance for a called iron law of performance for a large part of this class we will be large part of this class we will be large part of this class we will be looking only at the processor itself so looking only at the processor itself so looking only at the processor itself so the processor time can be expressed as the processor time can be expressed as the processor time can be expressed as the number of instructions per program the number of instructions per program the number of instructions per program times the number of cycles we spend per times the number of cycles we spend per times the number of cycles we spend per instruction times the clock cycle time instruction times the clock cycle time instruction times the clock cycle time or the number of seconds per clock cycle or the number of seconds per clock cycle or the number of seconds per clock cycle to understand why this is CPU time we to understand why this is CPU time we to understand why this is CPU time we can do the following the number of can do the following the number of can do the following the number of instructions per program let's say this instructions per program let's say this instructions per program let's say this is the number of instructions per is the number of instructions per is the number of instructions per program times the cycles per instruction program times the cycles per instruction program times the cycles per instruction times number of seconds we spend per times number of seconds we spend per times number of seconds we spend per cycle and then the cycle here we can cycle and then the cycle here we can cycle and then the cycle here we can cross them out the instruction here we cross them out the instruction here we cross them out the instruction here we can cross them out and we get how many can cross them out and we get how many can cross them out and we get how many seconds per program which is the CPU seconds per program which is the CPU seconds per program which is the CPU time so why do we want to think of time so why do we want to think of time so why do we want to think of processor time in terms of instructions processor time in terms of instructions processor time in terms of instructions per program cycles per instruction and per program cycles per instruction and per program cycles per instruction and clock cycle time instead of just clock cycle time instead of just clock cycle time instead of just measuring the overall time well because measuring the overall time well because measuring the overall time well because these three components allow us to think these three components allow us to think these three components allow us to think about different aspects of the computer about different aspects of the computer about different aspects of the computer architecture and surrounding areas that architecture and surrounding areas that architecture and surrounding areas that we can change so the number of we can change so the number of we can change so the number of instructions per program is generally instructions per program is generally instructions per program is generally affected by the algorithm we use and by affected by the algorithm we use and by affected by the algorithm we use and by the compiler we use if we have a better the compiler we use if we have a better the compiler we use if we have a better compiler it might result in a more compiler it might result in a more compiler it might result in a more efficient program that needs fewer efficient program that needs fewer efficient program that needs fewer instructions to do the same thing and instructions to do the same thing and instructions to do the same thing and also our instruction set might affect also our instruction set might affect also our instruction set might affect how many instructions we need to execute how many instructions we need to execute how many instructions we need to execute in a program for example if you have in a program for example if you have in a program for example if you have very very simple instructions it might very very simple instructions it might very very simple instructions it might take more of them to execute the same take more of them to execute the same take more of them to execute the same thing the number of cycles per thing the number of cycles per thing the number of cycles per instruction is also affected by the instruction is also affected by the instruction is also affected by the instruction set this time the simpler instruction set this time the simpler instruction set this time the simpler instructions may take fewer cycles to do instructions may take fewer cycles to do instructions may take fewer cycles to do but also our processor design can affect but also our processor design can affect but also our processor design can affect how many cycles we need per instruction how many cycles we need per instruction how many cycles we need per instruction we will see several techniques that we will see several techniques that we will see several techniques that allow a processor design to spend fewer allow a processor design to spend fewer allow a processor design to spend fewer cycles per instruction without changing cycles per instruction without changing cycles per instruction without changing the instruction set and finally clock the instruction set and finally clock the instruction set and finally clock cycle time is also affected by processor cycle time is also affected by processor cycle time is also affected by processor design we can design a processor so that design we can design a processor so that design we can design a processor so that it does very little per clock cycle the it does very little per clock cycle the it does very little per clock cycle the spending less time per clock cycle and spending less time per clock cycle and spending less time per clock cycle and also clock cycle time can be affected by also clock cycle time can be affected by also clock cycle time can be affected by circuit design if we have faster circuit design if we have faster circuit design if we have faster circuits they will need less time to do circuits they will need less time to do circuits they will need less time to do the same thing that needs to be done in the same thing that needs to be done in the same thing that needs to be done in the clock cycle and also by transistor the clock cycle and also by transistor the clock cycle and also by transistor physics physics physics because they allow us to build faster because they allow us to build faster because they allow us to build faster circuits so really computer architecture circuits so really computer architecture circuits so really computer architecture will affect the instruction set and here will affect the instruction set and here will affect the instruction set and here we have a choice of having complex we have a choice of having complex we have a choice of having complex instructions where I spend fewer of them instructions where I spend fewer of them instructions where I spend fewer of them per program but takes more cycles to do per program but takes more cycles to do per program but takes more cycles to do it or we can have simpler instructions it or we can have simpler instructions it or we can have simpler instructions where we have more of them in a program where we have more of them in a program where we have more of them in a program but we spend fewer cycles per but we spend fewer cycles per but we spend fewer cycles per instruction and also computer instruction and also computer instruction and also computer architecture is affected by processor architecture is affected by processor architecture is affected by processor design so in processor design we have a design so in processor design we have a design so in processor design we have a choice of making a processor that has choice of making a processor that has choice of making a processor that has very short clock cycle at the expense very short clock cycle at the expense very short clock cycle at the expense possibly of spending more cycles per possibly of spending more cycles per possibly of spending more cycles per instruction or we can have a processor instruction or we can have a processor instruction or we can have a processor that spends a lot of work in a cycle that spends a lot of work in a cycle that spends a lot of work in a cycle thus reducing the number of cycles it thus reducing the number of cycles it thus reducing the number of cycles it needs per instruction a good design in needs per instruction a good design in needs per instruction a good design in terms of processor design will balance terms of processor design will balance terms of processor design will balance the tool a good design in an instruction the tool a good design in an instruction the tool a good design in an instruction set will balance the two

## Iron Law Quiz： Part 1
Kind: captions Language: en now that we have seen the iron law of now that we have seen the iron law of now that we have seen the iron law of performance let's do a quiz on the iron performance let's do a quiz on the iron performance let's do a quiz on the iron law of performance let's say we have a law of performance let's say we have a law of performance let's say we have a program that has three billion program that has three billion program that has three billion instructions executing from start to instructions executing from start to instructions executing from start to completion let's say that our processor completion let's say that our processor completion let's say that our processor spends two cycles on each instruction spends two cycles on each instruction spends two cycles on each instruction and the processor is clocked at 3 and the processor is clocked at 3 and the processor is clocked at 3 gigahertz so we can say that the gigahertz so we can say that the gigahertz so we can say that the execution time is put your answer here


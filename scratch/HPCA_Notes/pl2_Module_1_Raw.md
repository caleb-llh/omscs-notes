# Playlist 2 Module 1 Raw Transcript

## ROB Intro： Part 3
Kind: captions Language: en we have seen that a processor can we have seen that a processor can we have seen that a processor can improve performance a lot by reordering improve performance a lot by reordering improve performance a lot by reordering instructions but things are not that instructions but things are not that instructions but things are not that Rosy in the instruction reorder land in Rosy in the instruction reorder land in Rosy in the instruction reorder land in real programs we can have exceptions real programs we can have exceptions real programs we can have exceptions such as divide by zero that can be such as divide by zero that can be such as divide by zero that can be messed up if we don't execute messed up if we don't execute messed up if we don't execute instructions exactly in program order instructions exactly in program order instructions exactly in program order this lesson explains how to clean up the this lesson explains how to clean up the this lesson explains how to clean up the reordering mass when exceptions do reordering mass when exceptions do reordering mass when exceptions do happen

## Exceptions in Out Of Order Execution： Part 3
Kind: captions Language: en son hauer dono cbot exceptions in out of son hauer dono cbot exceptions in out of son hauer dono cbot exceptions in out of order to share with this part of order to share with this part of order to share with this part of its first data 7x forti cycles foro di white data 7x forti cycles foro di white data 7x forti cycles foro di white fashion writer exotic fortitu di dispersi 3 mb forti ed expert mv fini 0 0 0 0 0 0 i data di the forty workshop mendes professor workshop mendes professor workshop mendes professor program counter for the sims 3 wii shop program counter for the sims 3 wii shop program counter for the sims 3 wii shop l'exception amber heard it comes back to l'exception amber heard it comes back to l'exception amber heard it comes back to this entry in psycho forti red hat security rinaudo destruction you used here 0 produced by destruction you used here 0 produced by destruction you used here 0 produced by the show never get the the show never get the the show never get the fixer di scampo i  bonfire house facebook di scampo i  bonfire house facebook di scampo i  bonfire house facebook page back front desk goes by page back front desk goes by page back front desk goes by data sud problem of gambling exceptions prix sud problem of gambling exceptions prix sud problem of gambling exceptions prix size league and so on too big match size league and so on too big match size league and so on too big match drugs drugs drugs for months twas implemented

## Branch Misprediction： Part 3
Kind: captions Language: en another thing that is a problem with another thing that is a problem with another thing that is a problem with tomasulo's algorithm is when we try to tomasulo's algorithm is when we try to tomasulo's algorithm is when we try to do branch mispredictions so how do we do branch mispredictions so how do we do branch mispredictions so how do we recover from a branch misprediction so recover from a branch misprediction so recover from a branch misprediction so let's say we have a program like this let's say we have a program like this let's say we have a program like this where we have a divide it takes where we have a divide it takes where we have a divide it takes something like 40 cycles and once we get something like 40 cycles and once we get something like 40 cycles and once we get that result we can resolve this branch that result we can resolve this branch that result we can resolve this branch over here so this branch can be over here so this branch can be over here so this branch can be predicted but it can take a long time to predicted but it can take a long time to predicted but it can take a long time to realize that it's been miss predicted so realize that it's been miss predicted so realize that it's been miss predicted so let's say meanwhile we have fetched this let's say meanwhile we have fetched this let's say meanwhile we have fetched this instruction because the branch should instruction because the branch should instruction because the branch should have been taken have been taken have been taken we have fetched this instruction and we have fetched this instruction and we have fetched this instruction and this instruction is already done in that this instruction is already done in that this instruction is already done in that case our tree is already written once we case our tree is already written once we case our tree is already written once we realize that we have miss please take realize that we have miss please take realize that we have miss please take this branch possibly 40 50 cycles later this branch possibly 40 50 cycles later this branch possibly 40 50 cycles later we should behave like we have never we should behave like we have never we should behave like we have never executed this instruction and instead executed this instruction and instead executed this instruction and instead start fetching instructions from the start fetching instructions from the start fetching instructions from the label that the branch is pointing to but label that the branch is pointing to but label that the branch is pointing to but that it becomes impossible because our that it becomes impossible because our that it becomes impossible because our tree by that time has already been tree by that time has already been tree by that time has already been updated so instead of having the old updated so instead of having the old updated so instead of having the old value of our tree now we have the new value of our tree now we have the new value of our tree now we have the new value of our tree when we jump to the value of our tree when we jump to the value of our tree when we jump to the label so again the problem is very label so again the problem is very label so again the problem is very similar to precise exceptions in that similar to precise exceptions in that similar to precise exceptions in that pretty much an instruction can complete pretty much an instruction can complete pretty much an instruction can complete and write two registers before and write two registers before and write two registers before proceeding instructions have fully been proceeding instructions have fully been proceeding instructions have fully been verified and before we even know that verified and before we even know that verified and before we even know that this instruction should have been this instruction should have been this instruction should have been executed a final problem occurs because executed a final problem occurs because executed a final problem occurs because of so-called phantom exceptions so what of so-called phantom exceptions so what of so-called phantom exceptions so what we can have is let's say that there is a we can have is let's say that there is a we can have is let's say that there is a divide here let's say that we have miss divide here let's say that we have miss divide here let's say that we have miss predicted this branch let's say that predicted this branch let's say that predicted this branch let's say that this instruction doesn't execute too this instruction doesn't execute too this instruction doesn't execute too soon and so on but the problem is if soon and so on but the problem is if soon and so on but the problem is if there is a divide here and it generates there is a divide here and it generates there is a divide here and it generates an exception that the exception will be an exception that the exception will be an exception that the exception will be triggered even though this instruction triggered even though this instruction triggered even though this instruction was never supposed to be executed so was never supposed to be executed so was never supposed to be executed so when we come back we have now a problem when we come back we have now a problem when we come back we have now a problem of basically we have already started of basically we have already started of basically we have already started exception processing for this we have exception processing for this we have exception processing for this we have you know done everything for this you know done everything for this you know done everything for this exception by the time we realize that exception by the time we realize that exception by the time we realize that it's too late so again we have to it's too late so again we have to it's too late so again we have to somehow take care of not having somehow take care of not having somehow take care of not having exceptions until we are sure that they exceptions until we are sure that they exceptions until we are sure that they should happen

## Correct Out of Order Execution： Part 3
Kind: captions Language: en so let's see how should we do the so let's see how should we do the so let's see how should we do the correct out of order execution we should correct out of order execution we should correct out of order execution we should execute out of order we can broadcast execute out of order we can broadcast execute out of order we can broadcast out of order but we should deposit out of order but we should deposit out of order but we should deposit values to registers in order this is values to registers in order this is values to registers in order this is needed because if we deposit register needed because if we deposit register needed because if we deposit register values out of order then if subsequently values out of order then if subsequently values out of order then if subsequently we discover that one of the earlier we discover that one of the earlier we discover that one of the earlier instruction shouldn't have been done instruction shouldn't have been done instruction shouldn't have been done like it has an exception or it's a like it has an exception or it's a like it has an exception or it's a branch misprediction then this branch misprediction then this branch misprediction then this instruction has already deposited it and instruction has already deposited it and instruction has already deposited it and it shouldn't have so if we deposit in it shouldn't have so if we deposit in it shouldn't have so if we deposit in order this cannot happen we will order this cannot happen we will order this cannot happen we will basically deposit results in order so basically deposit results in order so basically deposit results in order so once we get to deposit that means that once we get to deposit that means that once we get to deposit that means that all of the previous instructions have all of the previous instructions have all of the previous instructions have finished successfully and everything is finished successfully and everything is finished successfully and everything is fine in tomasulo's algorithm this is not fine in tomasulo's algorithm this is not fine in tomasulo's algorithm this is not happening in order this is happening out happening in order this is happening out happening in order this is happening out of order which is why we have a problem of order which is why we have a problem of order which is why we have a problem so what we need is a structure called so what we need is a structure called so what we need is a structure called reorder buffer even after issue it reorder buffer even after issue it reorder buffer even after issue it remembers the program order and it keeps remembers the program order and it keeps remembers the program order and it keeps the results of instructions until the results of instructions until the results of instructions until they're safe to write to registers so they're safe to write to registers so they're safe to write to registers so basically instead of writing two basically instead of writing two basically instead of writing two registers whenever the result is registers whenever the result is registers whenever the result is produced it would end up in the reorder produced it would end up in the reorder produced it would end up in the reorder buffer and then we will go in order buffer and then we will go in order buffer and then we will go in order through the reorder buffer and deposit through the reorder buffer and deposit through the reorder buffer and deposit results into the registers and once the results into the registers and once the results into the registers and once the result is deposited to the register only result is deposited to the register only result is deposited to the register only dead the instruction is considered fully dead the instruction is considered fully dead the instruction is considered fully done

## ROB Part 1： Part 3
Kind: captions Language: en now that we know that we need the now that we know that we need the now that we know that we need the reorder buffer to hold the results of reorder buffer to hold the results of reorder buffer to hold the results of instructions until they're ready to instructions until they're ready to instructions until they're ready to finish in program order let's see what finish in program order let's see what finish in program order let's see what it looks like the reorder buffer is a it looks like the reorder buffer is a it looks like the reorder buffer is a table of entries in each entry we keep table of entries in each entry we keep table of entries in each entry we keep at least three at least three at least three Fields one says what the value produced Fields one says what the value produced Fields one says what the value produced by the instruction is this is the value by the instruction is this is the value by the instruction is this is the value that we are not writing to the register that we are not writing to the register that we are not writing to the register when the instruction finishes it will when the instruction finishes it will when the instruction finishes it will now write its value to the reorder now write its value to the reorder now write its value to the reorder buffer when it broadcasts buffer when it broadcasts buffer when it broadcasts we need a bit here to tell us whether we we need a bit here to tell us whether we we need a bit here to tell us whether we actually have a valid value here or not actually have a valid value here or not actually have a valid value here or not because we will give a reorder buffer because we will give a reorder buffer because we will give a reorder buffer entry to the instruction before it entry to the instruction before it entry to the instruction before it actually writes the result and finally actually writes the result and finally actually writes the result and finally in the end we have to write this value in the end we have to write this value in the end we have to write this value to an actual register and for that we to an actual register and for that we to an actual register and for that we need to know when we do such a write need to know when we do such a write need to know when we do such a write when we move this value from the reorder when we move this value from the reorder when we move this value from the reorder buffer to a register what register are buffer to a register what register are buffer to a register what register are we going to write we going to write we going to write to this is a reorder buffer that can to this is a reorder buffer that can to this is a reorder buffer that can hold up to eight instructions hold up to eight instructions hold up to eight instructions and we keep instructions in the reorder and we keep instructions in the reorder and we keep instructions in the reorder buffer in program order so we need two buffer in program order so we need two buffer in program order so we need two pointers one pointer tells us when we pointers one pointer tells us when we pointers one pointer tells us when we issue the next instruction where does it issue the next instruction where does it issue the next instruction where does it go another one tells us when we finally go another one tells us when we finally go another one tells us when we finally complete in program order the next complete in program order the next complete in program order the next instruction which instruction is it instruction which instruction is it instruction which instruction is it going to be so in this particular going to be so in this particular going to be so in this particular reorder buffer the instructions that are reorder buffer the instructions that are reorder buffer the instructions that are actually valid actually valid actually valid are starting from the oldest are starting from the oldest are starting from the oldest one to the newest one this the even one to the newest one this the even one to the newest one this the even newer instructions are going to be added newer instructions are going to be added newer instructions are going to be added here and the oldest one will complete here and the oldest one will complete here and the oldest one will complete from here so these five instructions are from here so these five instructions are from here so these five instructions are actually in program order in the reorder actually in program order in the reorder actually in program order in the reorder buffer

## ROB Part 2： Part 3
Kind: captions Language: en now that we know what the reorder buffer now that we know what the reorder buffer now that we know what the reorder buffer is supposed to look like let's see how is supposed to look like let's see how is supposed to look like let's see how it's used for that we need the reorder it's used for that we need the reorder it's used for that we need the reorder buffer with its commit and issue points buffer with its commit and issue points buffer with its commit and issue points and we need the rest of the processor and we need the rest of the processor and we need the rest of the processor that we have already seen the that we have already seen the that we have already seen the instruction queue reservation stations instruction queue reservation stations instruction queue reservation stations registers and the rat let's say that we registers and the rat let's say that we registers and the rat let's say that we have this add instruction here in the have this add instruction here in the have this add instruction here in the instruction queue so now we want to see instruction queue so now we want to see instruction queue so now we want to see what happens once we start issuing this what happens once we start issuing this what happens once we start issuing this all the way to once we eventually Commit all the way to once we eventually Commit all the way to once we eventually Commit This instruction in a that now has a This instruction in a that now has a This instruction in a that now has a reorder buffer so let's first see what reorder buffer so let's first see what reorder buffer so let's first see what happens on instruction issue as before happens on instruction issue as before happens on instruction issue as before when we didn't have the reorder buffer when we didn't have the reorder buffer when we didn't have the reorder buffer we get a free reservation station and we we get a free reservation station and we we get a free reservation station and we put our instruction in it now that we do put our instruction in it now that we do put our instruction in it now that we do have a reorder buffer we will also get have a reorder buffer we will also get have a reorder buffer we will also get one of those unlike reservation station one of those unlike reservation station one of those unlike reservation station where we just need a free one it doesn't where we just need a free one it doesn't where we just need a free one it doesn't matter which one it is here we need to matter which one it is here we need to matter which one it is here we need to get the entry that is the next one at get the entry that is the next one at get the entry that is the next one at the issue point so our instruction will the issue point so our instruction will the issue point so our instruction will go here in the reorder buffer that of go here in the reorder buffer that of go here in the reorder buffer that of course also means that our issue point course also means that our issue point course also means that our issue point will now move and points to the next will now move and points to the next will now move and points to the next entry in the reorder buffer so now we entry in the reorder buffer so now we entry in the reorder buffer so now we have seven instructions that have been have seven instructions that have been have seven instructions that have been issued and have not yet committed issued and have not yet committed issued and have not yet committed another thing that we had to do when we another thing that we had to do when we another thing that we had to do when we were issuing instructions in maso's were issuing instructions in maso's were issuing instructions in maso's algorithm is that the rat entry for R1 algorithm is that the rat entry for R1 algorithm is that the rat entry for R1 the destination register of the the destination register of the the destination register of the instruction needed to be point pointed instruction needed to be point pointed instruction needed to be point pointed to the reservation station for the to the reservation station for the to the reservation station for the instruction we will still need to do instruction we will still need to do instruction we will still need to do this pointing but instead of the this pointing but instead of the this pointing but instead of the reservation station now we point the r reservation station now we point the r reservation station now we point the r entry to the Rob entry for the entry to the Rob entry for the entry to the Rob entry for the instruction that means that this R entry instruction that means that this R entry instruction that means that this R entry will be now instead of pointing to the will be now instead of pointing to the will be now instead of pointing to the reservation reservation reservation station be pointing to this entry here station be pointing to this entry here station be pointing to this entry here so overall at the end of the cycle when so overall at the end of the cycle when so overall at the end of the cycle when we issue this instruction we will occupy we issue this instruction we will occupy we issue this instruction we will occupy a reservation station a reservation station a reservation station with the values that we found for these with the values that we found for these with the values that we found for these registers we will make the r point to registers we will make the r point to registers we will make the r point to the Rob six entry and in this entry we the Rob six entry and in this entry we the Rob six entry and in this entry we will say that R1 will need to be written will say that R1 will need to be written will say that R1 will need to be written for this instruction and we don't still for this instruction and we don't still for this instruction and we don't still have the value so the done bit is zero have the value so the done bit is zero have the value so the done bit is zero here for this instruction now this ad here for this instruction now this ad here for this instruction now this ad here might wait for operants to become here might wait for operants to become here might wait for operants to become ready and that happens exactly as before ready and that happens exactly as before ready and that happens exactly as before with tomasulo's algorithm we just with tomasulo's algorithm we just with tomasulo's algorithm we just capture results that we need and we are capture results that we need and we are capture results that we need and we are ready to dispatch once we have all the ready to dispatch once we have all the ready to dispatch once we have all the operants eventually the ad has all the operants eventually the ad has all the operants eventually the ad has all the things that it needs to dispatch and the things that it needs to dispatch and the things that it needs to dispatch and the dispatching works exactly the same as dispatching works exactly the same as dispatching works exactly the same as before so we check whether the operants before so we check whether the operants before so we check whether the operants are ready and when they are we send the are ready and when they are we send the are ready and when they are we send the instruction for execution but now we can instruction for execution but now we can instruction for execution but now we can also free the reservation station as also free the reservation station as also free the reservation station as soon as we dispatch previously with soon as we dispatch previously with soon as we dispatch previously with tomasulo's algorithm we needed to wait tomasulo's algorithm we needed to wait tomasulo's algorithm we needed to wait for the reservation station release all for the reservation station release all for the reservation station release all the way until this instruction the way until this instruction the way until this instruction broadcasts its result broadcasts its result broadcasts its result that was needed because the reservation that was needed because the reservation that was needed because the reservation station serves as the name for the station serves as the name for the station serves as the name for the result now that the name for the result result now that the name for the result result now that the name for the result is the Rob entry we no longer need the is the Rob entry we no longer need the is the Rob entry we no longer need the reservation stations to play that role reservation stations to play that role reservation stations to play that role so the reservation station now does only so the reservation station now does only so the reservation station now does only its primary job which is wait for its primary job which is wait for its primary job which is wait for operant and capture them and then figure operant and capture them and then figure operant and capture them and then figure out which instructions to dispatch after out which instructions to dispatch after out which instructions to dispatch after that the reservation station for the that the reservation station for the that the reservation station for the instruction is done so we can free it so instruction is done so we can free it so instruction is done so we can free it so now our at exit Utes and carries with it now our at exit Utes and carries with it now our at exit Utes and carries with it the tag of Rob 6 which is the name for the tag of Rob 6 which is the name for the tag of Rob 6 which is the name for its result eventually it produces a its result eventually it produces a its result eventually it produces a result

## Free Reservation Stations Quiz： Part 3
Kind: captions Language: en we have seen that the robbed changes we have seen that the robbed changes we have seen that the robbed changes when we freed the reservation stations when we freed the reservation stations when we freed the reservation stations let's see if we understand that suppose let's see if we understand that suppose let's see if we understand that suppose that we have a situation where an that we have a situation where an that we have a situation where an instruction cannot issue because there instruction cannot issue because there instruction cannot issue because there is no available reservation station for is no available reservation station for is no available reservation station for it it it when is this more likely to happen when when is this more likely to happen when when is this more likely to happen when we have a processor with normal we have a processor with normal we have a processor with normal tomasulo's algorithm nor reorder buffer tomasulo's algorithm nor reorder buffer tomasulo's algorithm nor reorder buffer and we have to add and to multiply and we have to add and to multiply and we have to add and to multiply reservation stations or when we have a reservation stations or when we have a reservation stations or when we have a processor that does have a Rob and has processor that does have a Rob and has processor that does have a Rob and has the same number of reservation stations

## Free Reservation Stations Quiz Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our when can let's look at the answer to our when can let's look at the answer to our when can we free the reservation stations quiz if we free the reservation stations quiz if we free the reservation stations quiz if the instruction cannot issue because the instruction cannot issue because the instruction cannot issue because there is no available reservation there is no available reservation there is no available reservation station for it that means that all of station for it that means that all of station for it that means that all of our reservation stations are busy with our reservation stations are busy with our reservation stations are busy with the processor that doesn't have a Rob we the processor that doesn't have a Rob we the processor that doesn't have a Rob we hold on to our reservation stations from hold on to our reservation stations from hold on to our reservation stations from the time when we issue the instruction the time when we issue the instruction the time when we issue the instruction until the time the instruction until the time the instruction until the time the instruction broadcasts the result so issuing of an broadcasts the result so issuing of an broadcasts the result so issuing of an instruction once the reservation instruction once the reservation instruction once the reservation stations are busy we'll have to wait for stations are busy we'll have to wait for stations are busy we'll have to wait for the next instruction to broadcast the the next instruction to broadcast the the next instruction to broadcast the result in contrast in a row based result in contrast in a row based result in contrast in a row based processor with the same number of processor with the same number of processor with the same number of reservation stations we grab them at the reservation stations we grab them at the reservation stations we grab them at the same time when we issue the instruction same time when we issue the instruction same time when we issue the instruction but we free them as soon as we dispatch but we free them as soon as we dispatch but we free them as soon as we dispatch the instruction for execution the instruction for execution the instruction for execution so we free the reservation stations so we free the reservation stations so we free the reservation stations sooner thus the next instruction might sooner thus the next instruction might sooner thus the next instruction might find the reservation station free over find the reservation station free over find the reservation station free over here where over here it was still not here where over here it was still not here where over here it was still not available so this situation is more available so this situation is more available so this situation is more likely to happen when we do not have the likely to happen when we do not have the likely to happen when we do not have the Rob then when we do

## ROB Part 3： Part 3
Kind: captions Language: en eventually our ad produces a result and eventually our ad produces a result and eventually our ad produces a result and when that happens we will broadcast it when that happens we will broadcast it when that happens we will broadcast it the broadcast of the result happens the broadcast of the result happens the broadcast of the result happens exactly as before intimacies algorithm exactly as before intimacies algorithm exactly as before intimacies algorithm except that the tag we carry for the except that the tag we carry for the except that the tag we carry for the result is the Rob entry not the result is the Rob entry not the result is the Rob entry not the reservation station number because we reservation station number because we reservation station number because we have already freed the reservation have already freed the reservation have already freed the reservation station so what we do is we now station so what we do is we now station so what we do is we now broadcast the tag of Rob 6 with the broadcast the tag of Rob 6 with the broadcast the tag of Rob 6 with the value let's say 15 value let's say 15 value let's say 15 just like with tomasulo's algorithm this just like with tomasulo's algorithm this just like with tomasulo's algorithm this value and the tag are now given to the value and the tag are now given to the value and the tag are now given to the reservation stations and they normally reservation stations and they normally reservation stations and they normally captured the result based on the tag as captured the result based on the tag as captured the result based on the tag as before this result is not broadcast to before this result is not broadcast to before this result is not broadcast to the reservation stations and they the reservation stations and they the reservation stations and they capture this result even if they need it capture this result even if they need it capture this result even if they need it and those that need the result of this and those that need the result of this and those that need the result of this instruction will capture the value 15 instruction will capture the value 15 instruction will capture the value 15 just like we did in tomasulo's algorithm just like we did in tomasulo's algorithm just like we did in tomasulo's algorithm so this is not changed without the Rob so this is not changed without the Rob so this is not changed without the Rob we would now also capture this result in we would now also capture this result in we would now also capture this result in the register and update the right to the register and update the right to the register and update the right to point to that register with the Rob we point to that register with the Rob we point to that register with the Rob we don't want to write to the register yet don't want to write to the register yet don't want to write to the register yet so we write to the Rob instead so this so we write to the Rob instead so this so we write to the Rob instead so this broadcast value now goes to the Rob and broadcast value now goes to the Rob and broadcast value now goes to the Rob and we write 15 here and mark the we write 15 here and mark the we write 15 here and mark the instruction is done also the result of instruction is done also the result of instruction is done also the result of the instruction is not going to be in the instruction is not going to be in the instruction is not going to be in the registers so we do not update the the registers so we do not update the the registers so we do not update the rat to point to the registers in fact rat to point to the registers in fact rat to point to the registers in fact the rat is still pointing to the correct the rat is still pointing to the correct the rat is still pointing to the correct place the result of the instruction is place the result of the instruction is place the result of the instruction is in the Rob now so we don't do this here in the Rob now so we don't do this here in the Rob now so we don't do this here after broadcasting its result the after broadcasting its result the after broadcasting its result the instruction would be completed in instruction would be completed in instruction would be completed in tomasulo's algorithm with the Rob we are tomasulo's algorithm with the Rob we are tomasulo's algorithm with the Rob we are still not done with it yes we have still not done with it yes we have still not done with it yes we have computed the result and mark the computed the result and mark the computed the result and mark the computation is done but we have not computation is done but we have not computation is done but we have not deposited this result in the registers deposited this result in the registers deposited this result in the registers for that there is an additional step for that there is an additional step for that there is an additional step called commit to commit this instruction called commit to commit this instruction called commit to commit this instruction we must first commit all of the previous we must first commit all of the previous we must first commit all of the previous instructions so each cycle we test instructions so each cycle we test instructions so each cycle we test whether the next instruction at the whether the next instruction at the whether the next instruction at the commit point is done in which case the commit point is done in which case the commit point is done in which case the commit point can move on and commit commit point can move on and commit commit point can move on and commit instruction so eventually the commit instruction so eventually the commit instruction so eventually the commit point will reach here and now we will point will reach here and now we will point will reach here and now we will test whether this instruction is done test whether this instruction is done test whether this instruction is done find out that it's done and committed find out that it's done and committed find out that it's done and committed the committing of the instruction the committing of the instruction the committing of the instruction consists of actually taking this value consists of actually taking this value consists of actually taking this value 15 and writing it to the register that 15 and writing it to the register that 15 and writing it to the register that the instruction is supposed to write to the instruction is supposed to write to the instruction is supposed to write to in this case r1 so 15 will be written in this case r1 so 15 will be written in this case r1 so 15 will be written here and also we need to update the rat here and also we need to update the rat here and also we need to update the rat because it's pointing to the entry rob6 because it's pointing to the entry rob6 because it's pointing to the entry rob6 that entry will be freed after we commit that entry will be freed after we commit that entry will be freed after we commit this instruction so instead of pointing this instruction so instead of pointing this instruction so instead of pointing to rob6 now we can point to r1 so really to rob6 now we can point to r1 so really to rob6 now we can point to r1 so really the updating of the rat for writing to the updating of the rat for writing to the updating of the rat for writing to the register has moved from the the register has moved from the the register has moved from the broadcast time to the commit time broadcast time to the commit time broadcast time to the commit time because that's when we are actually because that's when we are actually because that's when we are actually updating the registers and after updating the registers and after updating the registers and after committing this instruction the commit committing this instruction the commit committing this instruction the commit point will move even further so that the point will move even further so that the point will move even further so that the next instruction to commit will be here next instruction to commit will be here next instruction to commit will be here and that completes our reorder buffer and that completes our reorder buffer and that completes our reorder buffer process or step by step for one process or step by step for one process or step by step for one instruction as you can see many of the instruction as you can see many of the instruction as you can see many of the things are happening exactly the same as things are happening exactly the same as things are happening exactly the same as in tomasulo's algorithm some of the in tomasulo's algorithm some of the in tomasulo's algorithm some of the things have changed like pointing the things have changed like pointing the things have changed like pointing the right to the Rob and instead of the right to the Rob and instead of the right to the Rob and instead of the reservation station writing to the Rob reservation station writing to the Rob reservation station writing to the Rob on broadcast instead of the register on broadcast instead of the register on broadcast instead of the register file and also we have added a step here file and also we have added a step here file and also we have added a step here for committing all so we can free the for committing all so we can free the for committing all so we can free the reservation station on this patch not on reservation station on this patch not on reservation station on this patch not on broadcast and finally we need a new step broadcast and finally we need a new step broadcast and finally we need a new step for actually committing the instruction

## Hardware Organization with ROB： Part 3
Kind: captions Language: en so let's look at what kinds of so let's look at what kinds of so let's look at what kinds of structures and what do we have in the structures and what do we have in the structures and what do we have in the hardware with the Rob so we still have hardware with the Rob so we still have hardware with the Rob so we still have the instruction queue from which we the instruction queue from which we the instruction queue from which we dispatch into the reservation stations dispatch into the reservation stations dispatch into the reservation stations we still have the rat that can point to we still have the rat that can point to we still have the rat that can point to a register file or to the renamed name a register file or to the renamed name a register file or to the renamed name for the instruction until it wres to the for the instruction until it wres to the for the instruction until it wres to the register file but rat entries that point register file but rat entries that point register file but rat entries that point to not to register file in in normal to not to register file in in normal to not to register file in in normal tomasulo algorithm would point to the tomasulo algorithm would point to the tomasulo algorithm would point to the reservation stations now however we have reservation stations now however we have reservation stations now however we have a new structure called the reorder a new structure called the reorder a new structure called the reorder buffer Rob it has a T and tail pointer buffer Rob it has a T and tail pointer buffer Rob it has a T and tail pointer and basically the instructions that are and basically the instructions that are and basically the instructions that are currently in execution are between these currently in execution are between these currently in execution are between these two pointers the head pointer is the two pointers the head pointer is the two pointers the head pointer is the pointer to where are we going to put the pointer to where are we going to put the pointer to where are we going to put the next instruction that issues the tail next instruction that issues the tail next instruction that issues the tail pointer is where we are considering pointer is where we are considering pointer is where we are considering instructions for commit and the rat instructions for commit and the rat instructions for commit and the rat entries that are not pointing to the entries that are not pointing to the entries that are not pointing to the register file are now pointing to the register file are now pointing to the register file are now pointing to the Rob entries for the instructions uh that Rob entries for the instructions uh that Rob entries for the instructions uh that are going to produce that value

## ROB Quiz： Part 3
Kind: captions Language: en so now that we know what the Rob's so now that we know what the Rob's so now that we know what the Rob's purpose is let's do a rob quiz so we purpose is let's do a rob quiz so we purpose is let's do a rob quiz so we need a Rob because we want to remember need a Rob because we want to remember need a Rob because we want to remember the program order until commit because the program order until commit because the program order until commit because we need a place to temporarily store the we need a place to temporarily store the we need a place to temporarily store the instructional result until commit to instructional result until commit to instructional result until commit to serve as the name or tag for the result serve as the name or tag for the result serve as the name or tag for the result of the instruction to store the source of the instruction to store the source of the instruction to store the source operands until dispatch of the operands until dispatch of the operands until dispatch of the instruction and to determine which instruction and to determine which instruction and to determine which instruction goes to which execution unit instruction goes to which execution unit instruction goes to which execution unit you should select all of the correct you should select all of the correct you should select all of the correct answers here there could be more than answers here there could be more than answers here there could be more than one that are correct

## ROB Quiz Solution： Part 3
Kind: captions Language: en okay so let's see the rob quiz solution okay so let's see the rob quiz solution okay so let's see the rob quiz solution so we do need the rob tool remember the so we do need the rob tool remember the so we do need the rob tool remember the program order that's the only place program order that's the only place program order that's the only place where the program order is preserved where the program order is preserved where the program order is preserved between the issue which is done in between the issue which is done in between the issue which is done in program order and the commit which is program order and the commit which is program order and the commit which is also done in program order because also done in program order because also done in program order because things in between are they're out of things in between are they're out of things in between are they're out of order so we definitely need the Rob for order so we definitely need the Rob for order so we definitely need the Rob for that to temporarily store the that to temporarily store the that to temporarily store the instructions result yes the Rob holds instructions result yes the Rob holds instructions result yes the Rob holds the instructions result between the time the instructions result between the time the instructions result between the time when it is produced meaning when we are when it is produced meaning when we are when it is produced meaning when we are actually broadcasting it on the bus and actually broadcasting it on the bus and actually broadcasting it on the bus and the time when the instructions result is the time when the instructions result is the time when the instructions result is committed to the register file so we committed to the register file so we committed to the register file so we definitely have that role for the Rob to definitely have that role for the Rob to definitely have that role for the Rob to serve as the name or the tag for the serve as the name or the tag for the serve as the name or the tag for the result yes with tomasulo's algorithm the result yes with tomasulo's algorithm the result yes with tomasulo's algorithm the reservation station was playing this reservation station was playing this reservation station was playing this role but with the Rob the Rob entry is role but with the Rob the Rob entry is role but with the Rob the Rob entry is really going to serve this role for an really going to serve this role for an really going to serve this role for an instruction to store the source operands instruction to store the source operands instruction to store the source operands until dispatch know the reservation until dispatch know the reservation until dispatch know the reservation station still will retain this role station still will retain this role station still will retain this role basically the reservation station still basically the reservation station still basically the reservation station still stores the source operands until stores the source operands until stores the source operands until dispatch so this answer is not correct dispatch so this answer is not correct dispatch so this answer is not correct and to determine which instruction goes and to determine which instruction goes and to determine which instruction goes to which execution unit again Rob to which execution unit again Rob to which execution unit again Rob doesn't play this role the Rob is doesn't play this role the Rob is doesn't play this role the Rob is usually unified meaning all of the usually unified meaning all of the usually unified meaning all of the instructions go to the same Rob they instructions go to the same Rob they instructions go to the same Rob they just get different entries in it we know just get different entries in it we know just get different entries in it we know which execution unit the instruction which execution unit the instruction which execution unit the instruction should go to because different execution should go to because different execution should go to because different execution units have different reservation units have different reservation units have different reservation stations so when we issue an instruction stations so when we issue an instruction stations so when we issue an instruction we send it to the right set of we send it to the right set of we send it to the right set of reservation stations and that determines reservation stations and that determines reservation stations and that determines what is going to be the execution unit what is going to be the execution unit what is going to be the execution unit so this answer is also not correct

## Branch Misprediction Recovery Part 1： Part 3
Kind: captions Language: en we said we need a Rob in order to have we said we need a Rob in order to have we said we need a Rob in order to have precise exceptions and also to be able precise exceptions and also to be able precise exceptions and also to be able to easily recover from Branch to easily recover from Branch to easily recover from Branch mispredictions so let's see how that mispredictions so let's see how that mispredictions so let's see how that happens suppose we have a program that happens suppose we have a program that happens suppose we have a program that reaches a branch and suppose that our reaches a branch and suppose that our reaches a branch and suppose that our prediction was that the program will prediction was that the program will prediction was that the program will continue here and fetch these three continue here and fetch these three continue here and fetch these three instructions although in fact the instructions although in fact the instructions although in fact the program branches so these three program branches so these three program branches so these three instructions will be executed in a instructions will be executed in a instructions will be executed in a branch misprediction let's say what branch misprediction let's say what branch misprediction let's say what happens in this program in our Rob and happens in this program in our Rob and happens in this program in our Rob and the registers first we will issue the the registers first we will issue the the registers first we will issue the load here its output is the R1 so at the load here its output is the R1 so at the load here its output is the R1 so at the beginning of all of this the Rob is beginning of all of this the Rob is beginning of all of this the Rob is empty the issue and commit Point are the empty the issue and commit Point are the empty the issue and commit Point are the same and that let's say that there are same and that let's say that there are same and that let's say that there are no no no instructions at the beginning of all of instructions at the beginning of all of instructions at the beginning of all of this let's say that there are no this let's say that there are no this let's say that there are no instructions so the rat is just pointing instructions so the rat is just pointing instructions so the rat is just pointing to the registers and the issue and to the registers and the issue and to the registers and the issue and commit point in the Rob are the same commit point in the Rob are the same commit point in the Rob are the same meaning that the Rob is empty all of the meaning that the Rob is empty all of the meaning that the Rob is empty all of the entries are free now let's see what entries are free now let's see what entries are free now let's see what happens when we issue the load we move happens when we issue the load we move happens when we issue the load we move the issue pointer here here the load the issue pointer here here the load the issue pointer here here the load produces produces produces R1 and R1 here in the rat is made to R1 and R1 here in the rat is made to R1 and R1 here in the rat is made to point to the Rob one entry when we issue point to the Rob one entry when we issue point to the Rob one entry when we issue the branch it produces no the branch it produces no the branch it produces no output and thus we don't need to rename output and thus we don't need to rename output and thus we don't need to rename anything and our issue pointer moves anything and our issue pointer moves anything and our issue pointer moves here next what happens we will issue here next what happens we will issue here next what happens we will issue instructions that shouldn't have been instructions that shouldn't have been instructions that shouldn't have been issued but the processor doesn't know issued but the processor doesn't know issued but the processor doesn't know that yet so it's just going to do it that yet so it's just going to do it that yet so it's just going to do it anyway so this side here writes to anyway so this side here writes to anyway so this side here writes to R2 and it gets the next Rob entry here R2 and it gets the next Rob entry here R2 and it gets the next Rob entry here so the R2 entry is going to point to Rob so the R2 entry is going to point to Rob so the R2 entry is going to point to Rob three and then this multiply three and then this multiply three and then this multiply here is going to write to R3 and thus R3 here is going to write to R3 and thus R3 here is going to write to R3 and thus R3 is going to point to Rob four and then is going to point to Rob four and then is going to point to Rob four and then the Divide is going to again write to R2 the Divide is going to again write to R2 the Divide is going to again write to R2 and that means that the entry for R2 in and that means that the entry for R2 in and that means that the entry for R2 in the red will be changed from pointing to the red will be changed from pointing to the red will be changed from pointing to the add to pointing to the Divide which the add to pointing to the Divide which the add to pointing to the Divide which is Rob five and of course after all of is Rob five and of course after all of is Rob five and of course after all of this issuing is done our issue pointer this issuing is done our issue pointer this issuing is done our issue pointer is now here and we have five is now here and we have five is now here and we have five instructions in our Rob now suppose that instructions in our Rob now suppose that instructions in our Rob now suppose that this load takes a long time to produce a this load takes a long time to produce a this load takes a long time to produce a value for example because it's a cash M value for example because it's a cash M value for example because it's a cash M this Branch depends on the load so it this Branch depends on the load so it this Branch depends on the load so it cannot complete until the load is done cannot complete until the load is done cannot complete until the load is done and this add also depends on the load and this add also depends on the load and this add also depends on the load and it cannot complete until the load is and it cannot complete until the load is and it cannot complete until the load is done but this multiply here can begin done but this multiply here can begin done but this multiply here can begin because R3 and R4 are not produced by because R3 and R4 are not produced by because R3 and R4 are not produced by any of these instructions so this multip any of these instructions so this multip any of these instructions so this multip might produce a value for example 15 and might produce a value for example 15 and might produce a value for example 15 and originally it would write it to the originally it would write it to the originally it would write it to the registers and at that point after the registers and at that point after the registers and at that point after the branch misprediction but this multiply branch misprediction but this multiply branch misprediction but this multiply can compute its value because R3 and R4 can compute its value because R3 and R4 can compute its value because R3 and R4 might be ready they're not produced by might be ready they're not produced by might be ready they're not produced by any of the previous any of the previous any of the previous instructions so the value of 15 when the instructions so the value of 15 when the instructions so the value of 15 when the multiply multiply multiply finishes would have been written to the finishes would have been written to the finishes would have been written to the registers and then we don't know how to registers and then we don't know how to registers and then we don't know how to undo the multiplication once we discover undo the multiplication once we discover undo the multiplication once we discover that the branch has been mispredicted in that the branch has been mispredicted in that the branch has been mispredicted in a raw based a raw based a raw based processor the result of the processor the result of the processor the result of the multiplication goes into its Rob entry multiplication goes into its Rob entry multiplication goes into its Rob entry and its marked is done once this and its marked is done once this and its marked is done once this multiply produces a value The Divide can multiply produces a value The Divide can multiply produces a value The Divide can begin and let's say that the Divide is begin and let's say that the Divide is begin and let's say that the Divide is producing a value of two and it might producing a value of two and it might producing a value of two and it might also be done before the load returns so also be done before the load returns so also be done before the load returns so now we have a situation where these two now we have a situation where these two now we have a situation where these two instructions would have updated instructions would have updated instructions would have updated registers and done permanent damage but registers and done permanent damage but registers and done permanent damage but in a raw based processor all they did is in a raw based processor all they did is in a raw based processor all they did is update the robw update the robw update the robw entries the registers are still entries the registers are still entries the registers are still unmodified

## Branch Misprediction Recovery Part 2： Part 3
Kind: captions Language: en eventually this load does complete and eventually this load does complete and eventually this load does complete and let's say it brings us a value of 700 let's say it brings us a value of 700 let's say it brings us a value of 700 and it's marked is done now we're going and it's marked is done now we're going and it's marked is done now we're going to do the branch and the add meanwhile to do the branch and the add meanwhile to do the branch and the add meanwhile we are going to be checking whether we we are going to be checking whether we we are going to be checking whether we can commit this instruction and we can can commit this instruction and we can can commit this instruction and we can because it's marked is done and it's the because it's marked is done and it's the because it's marked is done and it's the next instruction to commit so we will next instruction to commit so we will next instruction to commit so we will commit this instruction which will commit this instruction which will commit this instruction which will result in moving the commit pointer here result in moving the commit pointer here result in moving the commit pointer here and writing this value of 700 to and writing this value of 700 to and writing this value of 700 to register r1 and also making the rat now register r1 and also making the rat now register r1 and also making the rat now point here now let's suppose that the point here now let's suppose that the point here now let's suppose that the branch takes longer to figure out than branch takes longer to figure out than branch takes longer to figure out than the add the add the add so this add completes produces a value so this add completes produces a value so this add completes produces a value of let's say 3 and it's done and that's of let's say 3 and it's done and that's of let's say 3 and it's done and that's all that happens here again this result all that happens here again this result all that happens here again this result would have been written to the registers would have been written to the registers would have been written to the registers thus permanently changing r2 and that's thus permanently changing r2 and that's thus permanently changing r2 and that's all that happens now know that this all that happens now know that this all that happens now know that this result of tree would not have been result of tree would not have been result of tree would not have been written to the registers even with written to the registers even with written to the registers even with tomasulo's algorithm because the tomasulo's algorithm because the tomasulo's algorithm because the renaming for r2 is pointing not to this renaming for r2 is pointing not to this renaming for r2 is pointing not to this instruction meaning that we would have instruction meaning that we would have instruction meaning that we would have kept in the registers the value of 2 kept in the registers the value of 2 kept in the registers the value of 2 that we would have written for the that we would have written for the that we would have written for the divide because that's the latest r2 but divide because that's the latest r2 but divide because that's the latest r2 but in this case we don't do any of that we in this case we don't do any of that we in this case we don't do any of that we just keep the values in the Rob as is just keep the values in the Rob as is just keep the values in the Rob as is eventually we get to resolve this branch eventually we get to resolve this branch eventually we get to resolve this branch and figure out that it's been and figure out that it's been and figure out that it's been mispredicted so now the branch will be mispredicted so now the branch will be mispredicted so now the branch will be marked as done we will now start marked as done we will now start marked as done we will now start fetching from the correct instructions fetching from the correct instructions fetching from the correct instructions but the question is how do we get rid of but the question is how do we get rid of but the question is how do we get rid of the ones that we had wrong well what we the ones that we had wrong well what we the ones that we had wrong well what we do is we annotate here that there's has do is we annotate here that there's has do is we annotate here that there's has been a misprediction of this branch and been a misprediction of this branch and been a misprediction of this branch and that's all that happens as a result of that's all that happens as a result of that's all that happens as a result of its execution and we don't really fix its execution and we don't really fix its execution and we don't really fix the misprediction we continue fetching the misprediction we continue fetching the misprediction we continue fetching the wrong instructions because we don't the wrong instructions because we don't the wrong instructions because we don't know how to get rid of these until the know how to get rid of these until the know how to get rid of these until the commit reach is the point of the branch commit reach is the point of the branch commit reach is the point of the branch so when we commit this branch we realize so when we commit this branch we realize so when we commit this branch we realize that after this branch we were fetching that after this branch we were fetching that after this branch we were fetching the wrong instructions the PC that the the wrong instructions the PC that the the wrong instructions the PC that the branch should have created is different branch should have created is different branch should have created is different from the PC we have actually used at from the PC we have actually used at from the PC we have actually used at that point what we do is we commit the that point what we do is we commit the that point what we do is we commit the branch normally it doesn't write to any branch normally it doesn't write to any branch normally it doesn't write to any registers so the commit just moves this registers so the commit just moves this registers so the commit just moves this but if it's been mispredicted and it was but if it's been mispredicted and it was but if it's been mispredicted and it was what we do is we do the recovery before what we do is we do the recovery before what we do is we do the recovery before we restart the fetch from the correct we restart the fetch from the correct we restart the fetch from the correct place how do we do this recovery well place how do we do this recovery well place how do we do this recovery well with the Rob based processor at the with the Rob based processor at the with the Rob based processor at the point where the commit has reached the point where the commit has reached the point where the commit has reached the branch the registers contain exactly the branch the registers contain exactly the branch the registers contain exactly the values they need to contain at the point values they need to contain at the point values they need to contain at the point of the branch all of the instructions of the branch all of the instructions of the branch all of the instructions prior to the branch have been committed prior to the branch have been committed prior to the branch have been committed which means that their updates have been which means that their updates have been which means that their updates have been reflected in the registers and none of reflected in the registers and none of reflected in the registers and none of the instructions after the branch have the instructions after the branch have the instructions after the branch have updated the registers so the registers updated the registers so the registers updated the registers so the registers contain exactly the correct value at the contain exactly the correct value at the contain exactly the correct value at the point of the branch to undo these point of the branch to undo these point of the branch to undo these instructions what we need to do is two instructions what we need to do is two instructions what we need to do is two things one is reverse the issuing of things one is reverse the issuing of things one is reverse the issuing of these instructions by simply making the these instructions by simply making the these instructions by simply making the Rob empty at this point so after we have Rob empty at this point so after we have Rob empty at this point so after we have committed the branch we make the Rob committed the branch we make the Rob committed the branch we make the Rob ante by simply putting the issue point ante by simply putting the issue point ante by simply putting the issue point in the same place so we will issue in the same place so we will issue in the same place so we will issue instructions again into these slots we instructions again into these slots we instructions again into these slots we of course as we issue them on mark this of course as we issue them on mark this of course as we issue them on mark this done bit also the rat now contains the done bit also the rat now contains the done bit also the rat now contains the wrong values but the question is what wrong values but the question is what wrong values but the question is what are the right values for the rat well are the right values for the rat well are the right values for the rat well because the registers contain exactly because the registers contain exactly because the registers contain exactly the correct values as of this point the correct values as of this point the correct values as of this point undoing the damage in the rat simply undoing the damage in the rat simply undoing the damage in the rat simply consists of rewriting whatever is in the consists of rewriting whatever is in the consists of rewriting whatever is in the rat so that each rat entry points rat so that each rat entry points rat so that each rat entry points directly to its corresponding register directly to its corresponding register directly to its corresponding register so now it looks like these instructions so now it looks like these instructions so now it looks like these instructions have never executed or issued for that have never executed or issued for that have never executed or issued for that matter and we can begin fetching from matter and we can begin fetching from matter and we can begin fetching from the correct place updating the renaming the correct place updating the renaming the correct place updating the renaming as a result of that and updating the as a result of that and updating the as a result of that and updating the issue pointed as a result of that as you issue pointed as a result of that as you issue pointed as a result of that as you can see with the raw based processor we can see with the raw based processor we can see with the raw based processor we can do instructions out of order but we can do instructions out of order but we can do instructions out of order but we still can figure out what the correct still can figure out what the correct still can figure out what the correct order should have been if we did the order should have been if we did the order should have been if we did the right thing at the point of the branch right thing at the point of the branch right thing at the point of the branch so as you can see here we have executed so as you can see here we have executed so as you can see here we have executed these instructions before we figured out these instructions before we figured out these instructions before we figured out the branch but we are still able to undo the branch but we are still able to undo the branch but we are still able to undo this damage overall the recovery with a this damage overall the recovery with a this damage overall the recovery with a Rob based processor consists of making Rob based processor consists of making Rob based processor consists of making the right entry point to their the right entry point to their the right entry point to their corresponding registers who can erase corresponding registers who can erase corresponding registers who can erase all of the renaming emptying out all of the renaming emptying out all of the renaming emptying out whatever is in the Rob after we commit whatever is in the Rob after we commit whatever is in the Rob after we commit the offending branch instruction the offending branch instruction the offending branch instruction and of course getting rid of whatever is and of course getting rid of whatever is and of course getting rid of whatever is left over in the reservation stations in left over in the reservation stations in left over in the reservation stations in the area so basically free all the the area so basically free all the the area so basically free all the reservation stations and stop the areas reservation stations and stop the areas reservation stations and stop the areas from broadcasting results in the future from broadcasting results in the future from broadcasting results in the future so if one of these instructions is still so if one of these instructions is still so if one of these instructions is still in the alias or reservation stations in the alias or reservation stations in the alias or reservation stations it's going to be just dropped

## ROB and Exceptions： Part 3
Kind: captions Language: en now let's see how the rob is fixing our now let's see how the rob is fixing our now let's see how the rob is fixing our exception handling problems remember exception handling problems remember exception handling problems remember there are two of those problems one of there are two of those problems one of there are two of those problems one of the problems occurs because this divide the problems occurs because this divide the problems occurs because this divide can be delayed and realize that r2 is can be delayed and realize that r2 is can be delayed and realize that r2 is zero much much later while the add for zero much much later while the add for zero much much later while the add for example can be quick and be done so if example can be quick and be done so if example can be quick and be done so if you remember with tomasulo's algorithm you remember with tomasulo's algorithm you remember with tomasulo's algorithm the ad would deposit the result to the the ad would deposit the result to the the ad would deposit the result to the destination register of the ad long destination register of the ad long destination register of the ad long before the divide had a chance to before the divide had a chance to before the divide had a chance to determine that r2 is zero and that determine that r2 is zero and that determine that r2 is zero and that really we should have jumped to the really we should have jumped to the really we should have jumped to the exception handler here and never exception handler here and never exception handler here and never executed the add executed the add executed the add so how does the rob help so how does the rob help so how does the rob help well what we do is we treat the well what we do is we treat the well what we do is we treat the exception just like any other result so exception just like any other result so exception just like any other result so basically when we determine that r2 is basically when we determine that r2 is basically when we determine that r2 is zero instead of producing a result for zero instead of producing a result for zero instead of producing a result for r0 we mark the r0 in the rob as i mean r0 we mark the r0 in the rob as i mean r0 we mark the r0 in the rob as i mean the result in the rob is now going to be the result in the rob is now going to be the result in the rob is now going to be exception instead of a value when the exception instead of a value when the exception instead of a value when the divide reaches the commit point at that divide reaches the commit point at that divide reaches the commit point at that point the ad still hasn't committed and point the ad still hasn't committed and point the ad still hasn't committed and everything before divide did so at that everything before divide did so at that everything before divide did so at that point we can just do the kind of wait point we can just do the kind of wait point we can just do the kind of wait for drain you know kind of flush for drain you know kind of flush for drain you know kind of flush everything including the divide at this everything including the divide at this everything including the divide at this point and then jump to the exception point and then jump to the exception point and then jump to the exception handler and now we have a stable state handler and now we have a stable state handler and now we have a stable state for the handler which is here basically for the handler which is here basically for the handler which is here basically everything before the divide finished everything before the divide finished everything before the divide finished the divide and everything after it the divide and everything after it the divide and everything after it didn't finish which is exactly the state didn't finish which is exactly the state didn't finish which is exactly the state that the divide by zero exception that the divide by zero exception that the divide by zero exception handler should be seeing handler should be seeing handler should be seeing similarly for a load that would here similarly for a load that would here similarly for a load that would here have a page fault we would have the same have a page fault we would have the same have a page fault we would have the same situation of when the page fault reaches situation of when the page fault reaches situation of when the page fault reaches the commit we have committed everything the commit we have committed everything the commit we have committed everything before the page vote and we haven't before the page vote and we haven't before the page vote and we haven't committed the page fault itself or committed the page fault itself or committed the page fault itself or anything after it so there is a very anything after it so there is a very anything after it so there is a very nice resume point for the page fault nice resume point for the page fault nice resume point for the page fault exception handler when we go back there exception handler when we go back there exception handler when we go back there and load the page from the disk we can and load the page from the disk we can and load the page from the disk we can jump back to the load or the store and jump back to the load or the store and jump back to the load or the store and then start executing from here and then start executing from here and then start executing from here and because nothing here has already because nothing here has already because nothing here has already executed everything is fine the second executed everything is fine the second executed everything is fine the second problem with exceptions that we had were problem with exceptions that we had were problem with exceptions that we had were those phantom exceptions basically if we those phantom exceptions basically if we those phantom exceptions basically if we predicted that the branch here is not predicted that the branch here is not predicted that the branch here is not taken we would execute this divide and taken we would execute this divide and taken we would execute this divide and maybe get the exception like divide by maybe get the exception like divide by maybe get the exception like divide by zero and at the time when we get this zero and at the time when we get this zero and at the time when we get this exception maybe the branch is still not exception maybe the branch is still not exception maybe the branch is still not resolved so when we finally resolve the resolved so when we finally resolve the resolved so when we finally resolve the branch it is too late because the divide branch it is too late because the divide branch it is too late because the divide by zero has already been triggered so by zero has already been triggered so by zero has already been triggered so how does an rob handle this well the how does an rob handle this well the how does an rob handle this well the result of this instruction is now going result of this instruction is now going result of this instruction is now going to be marked as exception in its rob to be marked as exception in its rob to be marked as exception in its rob as the commit reaches the branch at that as the commit reaches the branch at that as the commit reaches the branch at that point or before it depending on what point or before it depending on what point or before it depending on what kind of branch kind of branch kind of branch strategy we have we will figure out that strategy we have we will figure out that strategy we have we will figure out that the branch has been mispredicted and the branch has been mispredicted and the branch has been mispredicted and that we really wanted to jump to this that we really wanted to jump to this that we really wanted to jump to this label here label here label here at that point the divide and anything at that point the divide and anything at that point the divide and anything over here is not committed basically we over here is not committed basically we over here is not committed basically we haven't committed anything after the haven't committed anything after the haven't committed anything after the branch so we can just cancel these branch so we can just cancel these branch so we can just cancel these instructions they never reach commit and instructions they never reach commit and instructions they never reach commit and thus the exception here is never thus the exception here is never thus the exception here is never triggered so the idea with exception triggered so the idea with exception triggered so the idea with exception handling is simply treat the exception handling is simply treat the exception handling is simply treat the exception just as any other result and delay the just as any other result and delay the just as any other result and delay the actual handling of exceptions until the actual handling of exceptions until the actual handling of exceptions until the instruction that is triggering the instruction that is triggering the instruction that is triggering the exception commits at that point we know exception commits at that point we know exception commits at that point we know exactly what the resume point is for the exactly what the resume point is for the exactly what the resume point is for the exception handler and we know that we exception handler and we know that we exception handler and we know that we won't have any phantom exceptions won't have any phantom exceptions won't have any phantom exceptions because because because we would never reach the commit of this we would never reach the commit of this we would never reach the commit of this divide unless this divide was on the divide unless this divide was on the divide unless this divide was on the correct path for all the branches correct path for all the branches correct path for all the branches basically if there was a misprediction basically if there was a misprediction basically if there was a misprediction we would have cancelled this divide long we would have cancelled this divide long we would have cancelled this divide long before it reaches the commit point


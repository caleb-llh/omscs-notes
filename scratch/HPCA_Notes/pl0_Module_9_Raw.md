# Playlist 0 Module 9 Raw Transcript

## 1 Bit History Solution Quiz： Part 1
Kind: captions Language: en let's look at the solution for our one let's look at the solution for our one let's look at the solution for our one bit history quiz we have a one bit bit history quiz we have a one bit bit history quiz we have a one bit history that starts at zero and two bit history that starts at zero and two bit history that starts at zero and two bit counter for each possible history all counter for each possible history all counter for each possible history all counterstart it's strong not taken and counterstart it's strong not taken and counterstart it's strong not taken and the pattern we're trying to predict is the pattern we're trying to predict is the pattern we're trying to predict is the not taken not taken taken etc after the not taken not taken taken etc after the not taken not taken taken etc after 100 repetitions of this pattern what is 100 repetitions of this pattern what is 100 repetitions of this pattern what is the total number of mispredictions that the total number of mispredictions that the total number of mispredictions that we will have seen one way to approach we will have seen one way to approach we will have seen one way to approach this problem is simply to work through this problem is simply to work through this problem is simply to work through the table like we have seen before so we the table like we have seen before so we the table like we have seen before so we will have our state the prediction will have our state the prediction will have our state the prediction the outcome the outcome the outcome and whether we are correct and whether we are correct and whether we are correct and now we will just work through the and now we will just work through the and now we will just work through the outcomes the outcomes will be outcomes the outcomes will be outcomes the outcomes will be n n n n n n t t t n n n n n n t etc and of course we will not go t etc and of course we will not go t etc and of course we will not go through 100 repetitions of this pretty through 100 repetitions of this pretty through 100 repetitions of this pretty soon the predictor will start to be very soon the predictor will start to be very soon the predictor will start to be very consistent about what it's predicting in consistent about what it's predicting in consistent about what it's predicting in each round of the pattern the state each round of the pattern the state each round of the pattern the state initially is zero strong not taken initially is zero strong not taken initially is zero strong not taken strong not taken strong not taken strong not taken the prediction as a result of that will the prediction as a result of that will the prediction as a result of that will be be be use the zeroth counter which is saying use the zeroth counter which is saying use the zeroth counter which is saying strong not taken so the prediction will strong not taken so the prediction will strong not taken so the prediction will be not taken be not taken be not taken this agrees with the outcome so we are this agrees with the outcome so we are this agrees with the outcome so we are correct correct correct update the predictor update the predictor update the predictor so the state will be zero because the so the state will be zero because the so the state will be zero because the outcome was not taken strong not taken outcome was not taken strong not taken outcome was not taken strong not taken will be updated with a not taken outcome will be updated with a not taken outcome will be updated with a not taken outcome so it stays strong not taken and the so it stays strong not taken and the so it stays strong not taken and the other counter just stays the same now other counter just stays the same now other counter just stays the same now the predictor again will predict not the predictor again will predict not the predictor again will predict not taken using zero and strong not taken taken using zero and strong not taken taken using zero and strong not taken and it will again be correct so the and it will again be correct so the and it will again be correct so the history stays at zero the strong note history stays at zero the strong note history stays at zero the strong note taken stays at strong note taken and the taken stays at strong note taken and the taken stays at strong note taken and the other counter just stays the same and at other counter just stays the same and at other counter just stays the same and at this point we will again with a history this point we will again with a history this point we will again with a history of zero use this predict not taken of zero use this predict not taken of zero use this predict not taken strongly and be wrong this time so after strongly and be wrong this time so after strongly and be wrong this time so after one round we have a single misprediction one round we have a single misprediction one round we have a single misprediction the history will be the history will be the history will be one now one now one now and the predictor here and the predictor here and the predictor here will be updated will be updated will be updated to weak not taken because there has been to weak not taken because there has been to weak not taken because there has been a taken outcome and the other predictor a taken outcome and the other predictor a taken outcome and the other predictor just stays the same now we will use a just stays the same now we will use a just stays the same now we will use a history of one history of one history of one to index say strong not taken to index say strong not taken to index say strong not taken so the outcome will be not taken and be so the outcome will be not taken and be so the outcome will be not taken and be correct next correct next correct next we will we will we will keep the strong note taken here the keep the strong note taken here the keep the strong note taken here the other counter stays the same other counter stays the same other counter stays the same the history becomes zero because of this the history becomes zero because of this the history becomes zero because of this and we will use and we will use and we will use a history of zero to index into the weak a history of zero to index into the weak a history of zero to index into the weak not taken state and thus predict not not taken state and thus predict not not taken state and thus predict not taken again taken again taken again again we are correct in our decision again we are correct in our decision again we are correct in our decision next next next the history stays zero because the the history stays zero because the the history stays zero because the decision was not taken the update of the decision was not taken the update of the decision was not taken the update of the weak knock taken goes back to the strong weak knock taken goes back to the strong weak knock taken goes back to the strong not taken because the decision was not not taken because the decision was not not taken because the decision was not taken the other counter stays the same taken the other counter stays the same taken the other counter stays the same and using the history of zero we will and using the history of zero we will and using the history of zero we will index and use this counter index and use this counter index and use this counter this counter says truck not taken so we this counter says truck not taken so we this counter says truck not taken so we will predict not taken this will will predict not taken this will will predict not taken this will disagree with the taken prediction so we disagree with the taken prediction so we disagree with the taken prediction so we will have a misprediction will have a misprediction will have a misprediction this concludes round two as you can see this concludes round two as you can see this concludes round two as you can see at the end of round two we have the same at the end of round two we have the same at the end of round two we have the same state that we had at the end of round state that we had at the end of round state that we had at the end of round one so now pretty much what happened in one so now pretty much what happened in one so now pretty much what happened in round two will repeat more times round two will repeat more times round two will repeat more times overall in the hundred repetitions of overall in the hundred repetitions of overall in the hundred repetitions of this in each repetition we have a single this in each repetition we have a single this in each repetition we have a single misprediction so the correct answer is misprediction so the correct answer is misprediction so the correct answer is that we have a hundred mispredictions or that we have a hundred mispredictions or that we have a hundred mispredictions or one third one third one third of our decisions are wrong as you can of our decisions are wrong as you can of our decisions are wrong as you can see the one bit history predictor is not see the one bit history predictor is not see the one bit history predictor is not a great predictor for this type of a a great predictor for this type of a a great predictor for this type of a pattern

## 2 Bit History Predictor： Part 1
Kind: captions Language: en so we have seen in the previous quiz so we have seen in the previous quiz so we have seen in the previous quiz that the one bit predictor cannot really that the one bit predictor cannot really that the one bit predictor cannot really predict well the pattern n NT that predict well the pattern n NT that predict well the pattern n NT that repeats we will see that a 2bit history repeats we will see that a 2bit history repeats we will see that a 2bit history will be successfully predicting this will be successfully predicting this will be successfully predicting this pattern how does a two-bit history pattern how does a two-bit history pattern how does a two-bit history predictor work same as before we take predictor work same as before we take predictor work same as before we take the PC and we index into the branch the PC and we index into the branch the PC and we index into the branch history table from which we get an entry history table from which we get an entry history table from which we get an entry now however the entry we get has five now however the entry we get has five now however the entry we get has five pieces it has two bits of History so pieces it has two bits of History so pieces it has two bits of History so these are the last two outcomes of this these are the last two outcomes of this these are the last two outcomes of this branch and then we have four times a two branch and then we have four times a two branch and then we have four times a two bit counter one for each possible bit counter one for each possible bit counter one for each possible history so with two bits we will index history so with two bits we will index history so with two bits we will index into this array of counters and use the into this array of counters and use the into this array of counters and use the appropriate counter for the given appropriate counter for the given appropriate counter for the given history so as you can see the overall history so as you can see the overall history so as you can see the overall cost of this predictor now is two bits cost of this predictor now is two bits cost of this predictor now is two bits of History plus 4 * 2 so it ends up of History plus 4 * 2 so it ends up of History plus 4 * 2 so it ends up costing us 10 bits per Branch instead of costing us 10 bits per Branch instead of costing us 10 bits per Branch instead of going through a lengthy example of how going through a lengthy example of how going through a lengthy example of how this works when we try to predict this this works when we try to predict this this works when we try to predict this pattern let's try to figure it out the pattern let's try to figure it out the pattern let's try to figure it out the sequence of outcomes is n n t n and T sequence of outcomes is n n t n and T sequence of outcomes is n n t n and T Etc when we are seeing the outcome of T Etc when we are seeing the outcome of T Etc when we are seeing the outcome of T and trying to predict it that is we're and trying to predict it that is we're and trying to predict it that is we're trying to predict what will become T our trying to predict what will become T our trying to predict what will become T our 2bit history will be 0 0 it's saying 2bit history will be 0 0 it's saying 2bit history will be 0 0 it's saying that the last two outcomes we have seen that the last two outcomes we have seen that the last two outcomes we have seen have been both not taken this history have been both not taken this history have been both not taken this history will make us use this counter here so will make us use this counter here so will make us use this counter here so that counter which we will call counter that counter which we will call counter that counter which we will call counter number zero will be counting up when number zero will be counting up when number zero will be counting up when we're trying to predict this particular we're trying to predict this particular we're trying to predict this particular outcome our history will be 01 we will outcome our history will be 01 we will outcome our history will be 01 we will be using counter number one and we will be using counter number one and we will be using counter number one and we will decrement it because we seeing a non-t decrement it because we seeing a non-t decrement it because we seeing a non-t taken decision when we're trying to taken decision when we're trying to taken decision when we're trying to predict this the history will be 1 Z predict this the history will be 1 Z predict this the history will be 1 Z this this this corresponds to counter number two which corresponds to counter number two which corresponds to counter number two which will also be decremented for this now we will also be decremented for this now we will also be decremented for this now we will again use the history of 0 0 will again use the history of 0 0 will again use the history of 0 0 because the last two outcomes have been because the last two outcomes have been because the last two outcomes have been not taken this means that the counter not taken this means that the counter not taken this means that the counter number zero because of this taken number zero because of this taken number zero because of this taken decision will be incremented and so on decision will be incremented and so on decision will be incremented and so on as you can see counter zero always as you can see counter zero always as you can see counter zero always counts up so very soon it will start counts up so very soon it will start counts up so very soon it will start predicting taken which means every time predicting taken which means every time predicting taken which means every time we see a history of 0 0 we will start we see a history of 0 0 we will start we see a history of 0 0 we will start predicting strong taken counter one predicting strong taken counter one predicting strong taken counter one keeps counting down so very very soon keeps counting down so very very soon keeps counting down so very very soon after seeing a history of 01 we will after seeing a history of 01 we will after seeing a history of 01 we will predict strong not taken and similarly predict strong not taken and similarly predict strong not taken and similarly counter two is always counting down so counter two is always counting down so counter two is always counting down so very soon we will start predicting not very soon we will start predicting not very soon we will start predicting not taken when we see a history of one Zer taken when we see a history of one Zer taken when we see a history of one Zer as you can see what's now going to as you can see what's now going to as you can see what's now going to happen is pretty much this counter zero happen is pretty much this counter zero happen is pretty much this counter zero here will start to predict strong taken here will start to predict strong taken here will start to predict strong taken counter one will start to predict strong counter one will start to predict strong counter one will start to predict strong not taken and counter two is going to not taken and counter two is going to not taken and counter two is going to predict strong not taken counter number predict strong not taken counter number predict strong not taken counter number three which is is the last counter we three which is is the last counter we three which is is the last counter we have actually never gets used because we have actually never gets used because we have actually never gets used because we never see a history of 1 one that would never see a history of 1 one that would never see a history of 1 one that would mean that we have seen a taken followed mean that we have seen a taken followed mean that we have seen a taken followed by a taken which doesn't happen in this by a taken which doesn't happen in this by a taken which doesn't happen in this particular pattern so as you can see particular pattern so as you can see particular pattern so as you can see this predictor starts being a perfect this predictor starts being a perfect this predictor starts being a perfect predictor after a while after these predictor after a while after these predictor after a while after these counters have been appropriately counters have been appropriately counters have been appropriately modified depending on their starting modified depending on their starting modified depending on their starting state so unlike a single bit history state so unlike a single bit history state so unlike a single bit history predictor a two- bit history can predict predictor a two- bit history can predict predictor a two- bit history can predict this pattern accurately

## 2 Bit History Predictor 2： Part 1
Kind: captions Language: en so after the initial warmup period we so after the initial warmup period we so after the initial warmup period we have seen that the two-bit history have seen that the two-bit history have seen that the two-bit history predictor will start predicting this predictor will start predicting this predictor will start predicting this with a hundred percent accuracy we also with a hundred percent accuracy we also with a hundred percent accuracy we also see that this predictor is really see that this predictor is really see that this predictor is really wasting one of its four two bit counters wasting one of its four two bit counters wasting one of its four two bit counters because it's only using three of the because it's only using three of the because it's only using three of the four to predict this pattern and the four to predict this pattern and the four to predict this pattern and the fourth one simply never gets used so we fourth one simply never gets used so we fourth one simply never gets used so we have it in the entry but we really have it in the entry but we really have it in the entry but we really didn't need it now let's see what didn't need it now let's see what didn't need it now let's see what happens if this same two-bit history happens if this same two-bit history happens if this same two-bit history predictor is trying to predict the predictor is trying to predict the predictor is trying to predict the alternating pattern not taken taken not alternating pattern not taken taken not alternating pattern not taken taken not taken taken and so on for that pattern taken taken and so on for that pattern taken taken and so on for that pattern when we are trying to predict this not when we are trying to predict this not when we are trying to predict this not taken here we are seeing a history of 0 taken here we are seeing a history of 0 taken here we are seeing a history of 0 1 that means the counter one will be 1 that means the counter one will be 1 that means the counter one will be counting down when we are trying to counting down when we are trying to counting down when we are trying to predict this we have a history of 1 0 predict this we have a history of 1 0 predict this we have a history of 1 0 and when we have 1 0 that means counter and when we have 1 0 that means counter and when we have 1 0 that means counter 2 is counting up for this we again have 2 is counting up for this we again have 2 is counting up for this we again have a history of 0 1 so counter 1 counts a history of 0 1 so counter 1 counts a history of 0 1 so counter 1 counts down and when we try to predict this we down and when we try to predict this we down and when we try to predict this we have 1 0 in history so counter 2 is have 1 0 in history so counter 2 is have 1 0 in history so counter 2 is counting up as you can see for this counting up as you can see for this counting up as you can see for this pattern counter 1 and counter 2 will pattern counter 1 and counter 2 will pattern counter 1 and counter 2 will very soon start predicting correctly and very soon start predicting correctly and very soon start predicting correctly and there are really only two of these there are really only two of these there are really only two of these counters used 1 because the history can counters used 1 because the history can counters used 1 because the history can be 0 1 and 1 because the history can be be 0 1 and 1 because the history can be be 0 1 and 1 because the history can be 1 0 we never can see the history of 0 0 1 0 we never can see the history of 0 0 1 0 we never can see the history of 0 0 or 1 1 which means after the initial or 1 1 which means after the initial or 1 1 which means after the initial warmup period this predictor will also warmup period this predictor will also warmup period this predictor will also predict this with a hundred percent predict this with a hundred percent predict this with a hundred percent accuracy we have already seen that the accuracy we have already seen that the accuracy we have already seen that the one bit history predictor can predict one bit history predictor can predict one bit history predictor can predict this with a 100 percent accuracy which this with a 100 percent accuracy which this with a 100 percent accuracy which is good but this predictor is not using is good but this predictor is not using is good but this predictor is not using half of its 2 bit predictor entries so half of its 2 bit predictor entries so half of its 2 bit predictor entries so basically in the entry that corresponds basically in the entry that corresponds basically in the entry that corresponds to this branch we are really using only to this branch we are really using only to this branch we are really using only two of the four two bit counters so the two of the four two bit counters so the two of the four two bit counters so the waste is larger in general an n-bit waste is larger in general an n-bit waste is larger in general an n-bit history predictor will correctly predict history predictor will correctly predict history predictor will correctly predict all patterns of length less than or all patterns of length less than or all patterns of length less than or equal to n plus one we have seen that a equal to n plus one we have seen that a equal to n plus one we have seen that a single bit predictor can predict single bit predictor can predict single bit predictor can predict correctly the pattern that is of length correctly the pattern that is of length correctly the pattern that is of length two a two bit predictor can predict two a two bit predictor can predict two a two bit predictor can predict correctly a pattern of length three and correctly a pattern of length three and correctly a pattern of length three and also a pattern of length two both of also a pattern of length two both of also a pattern of length two both of them of course can predict them of course can predict them of course can predict a pattern of length one which amounts to a pattern of length one which amounts to a pattern of length one which amounts to always taken or always not taken in always taken or always not taken in always taken or always not taken in general an n-bit history predictor will general an n-bit history predictor will general an n-bit history predictor will predict all patterns of length its predict all patterns of length its predict all patterns of length its history size plus one or less than that history size plus one or less than that history size plus one or less than that however the n-bit history predictor will however the n-bit history predictor will however the n-bit history predictor will cost us the N bit of history plus two to cost us the N bit of history plus two to cost us the N bit of history plus two to the end two bit counters per entry each the end two bit counters per entry each the end two bit counters per entry each branch will have an entry that we index branch will have an entry that we index branch will have an entry that we index into that will be of this size so the into that will be of this size so the into that will be of this size so the entries become very large for example if entries become very large for example if entries become very large for example if it's a 10 bit history we're talking it's a 10 bit history we're talking it's a 10 bit history we're talking about several kilobits worth of stuff about several kilobits worth of stuff about several kilobits worth of stuff for a single branch to be predicted if for a single branch to be predicted if for a single branch to be predicted if we have for example a loop that has we have for example a loop that has we have for example a loop that has something like a loop out branch and a something like a loop out branch and a something like a loop out branch and a loop back branch that will spend two of loop back branch that will spend two of loop back branch that will spend two of these entries and how about the waste these entries and how about the waste these entries and how about the waste most of the two bit counters that we most of the two bit counters that we most of the two bit counters that we have in our entry will be wasted why have in our entry will be wasted why have in our entry will be wasted why well because if we have for example a 10 well because if we have for example a 10 well because if we have for example a 10 bit history there are a thousand 24 bit history there are a thousand 24 bit history there are a thousand 24 entries we can only predict patterns entries we can only predict patterns entries we can only predict patterns that are up to 11th in length and a that are up to 11th in length and a that are up to 11th in length and a pattern of length 11 will have 11 pattern of length 11 will have 11 pattern of length 11 will have 11 possible histories at most so of the possible histories at most so of the possible histories at most so of the more than a thousand histories we will more than a thousand histories we will more than a thousand histories we will really be using only up to 11 of them really be using only up to 11 of them really be using only up to 11 of them for accurate prediction the rest of the for accurate prediction the rest of the for accurate prediction the rest of the histories are mainly waste so as we can histories are mainly waste so as we can histories are mainly waste so as we can see by increasing the end here which is see by increasing the end here which is see by increasing the end here which is the length of the history we are the length of the history we are the length of the history we are improving our ability to predict longer improving our ability to predict longer improving our ability to predict longer and longer patterns but our predictor and longer patterns but our predictor and longer patterns but our predictor becomes very expensive very quickly and becomes very expensive very quickly and becomes very expensive very quickly and much of the cost that we are adding is much of the cost that we are adding is much of the cost that we are adding is waste so the question is why would we waste so the question is why would we waste so the question is why would we want to have n large why not just settle want to have n large why not just settle want to have n large why not just settle for something like patterns of length 3 for something like patterns of length 3 for something like patterns of length 3 well it turns out that a very common well it turns out that a very common well it turns out that a very common type of a pattern is a loop where we type of a pattern is a loop where we type of a pattern is a loop where we have something like this would be don't have something like this would be don't have something like this would be don't exit the loop don't exit the loop exit exit the loop don't exit the loop exit exit the loop don't exit the loop exit the loop so it's a traitor ation loop the loop so it's a traitor ation loop the loop so it's a traitor ation loop and then we repeat that loop many times and then we repeat that loop many times and then we repeat that loop many times so for example in order to do something so for example in order to do something so for example in order to do something like a 8 iteration loop we have to have like a 8 iteration loop we have to have like a 8 iteration loop we have to have a pattern of 8 or 9 lengths a pattern of 8 or 9 lengths a pattern of 8 or 9 lengths which means that we need in seven or which means that we need in seven or which means that we need in seven or eight bit history in order to predict eight bit history in order to predict eight bit history in order to predict that successfully so we do want to have that successfully so we do want to have that successfully so we do want to have a long history but we'd rather have it a long history but we'd rather have it a long history but we'd rather have it without this huge cost and without the without this huge cost and without the without this huge cost and without the huge waste that the cost comes with

## N Bit History Predictor Quiz： Part 1
Kind: captions Language: en so now that we have seen how to or more so now that we have seen how to or more so now that we have seen how to or more bit histories work let's do a quiz about bit histories work let's do a quiz about bit histories work let's do a quiz about them let's say we have an n-bit history them let's say we have an n-bit history them let's say we have an n-bit history with a two bit counter per history in with a two bit counter per history in with a two bit counter per history in each predictor entry let's say that we each predictor entry let's say that we each predictor entry let's say that we need a thousand twenty four entries in need a thousand twenty four entries in need a thousand twenty four entries in order for each branch to and each other order for each branch to and each other order for each branch to and each other instruction to have its own entry so instruction to have its own entry so instruction to have its own entry so that they don't interfere with each that they don't interfere with each that they don't interfere with each other so for the length of the history other so for the length of the history other so for the length of the history of one four eight and 16 what is the of one four eight and 16 what is the of one four eight and 16 what is the cost of the entire predictor in bits I'm cost of the entire predictor in bits I'm cost of the entire predictor in bits I'm going to tell you that the cost with a going to tell you that the cost with a going to tell you that the cost with a 1-bit history is the one bit of history 1-bit history is the one bit of history 1-bit history is the one bit of history and two two bit counters per entries and two two bit counters per entries and two two bit counters per entries it's going to be five bits times 1,024 it's going to be five bits times 1,024 it's going to be five bits times 1,024 is this predictor good for each of the is this predictor good for each of the is this predictor good for each of the predictors for a pattern of N and NT predictors for a pattern of N and NT predictors for a pattern of N and NT that repeats and we know already that that repeats and we know already that that repeats and we know already that the one bit history predictor is not the one bit history predictor is not the one bit history predictor is not good for this pattern because it's too good for this pattern because it's too good for this pattern because it's too long for it and finally what is the long for it and finally what is the long for it and finally what is the number of two bit counters in an entry number of two bit counters in an entry number of two bit counters in an entry that we actually use when trying to that we actually use when trying to that we actually use when trying to predict the repeating NT pattern meaning predict the repeating NT pattern meaning predict the repeating NT pattern meaning not taken taken not taken taken and so not taken taken not taken taken and so not taken taken not taken taken and so on we already know that in the N equals on we already know that in the N equals on we already know that in the N equals 1 predictor we need to use both of our 1 predictor we need to use both of our 1 predictor we need to use both of our counters so the answer is to to further counters so the answer is to to further counters so the answer is to to further help you I'm going to tell you that the help you I'm going to tell you that the help you I'm going to tell you that the cost of an N equals 4 predictor should cost of an N equals 4 predictor should cost of an N equals 4 predictor should be the history is 4 plus 2 bit predictor be the history is 4 plus 2 bit predictor be the history is 4 plus 2 bit predictor x' I need 2 to the 4th of them times a x' I need 2 to the 4th of them times a x' I need 2 to the 4th of them times a thousand 24 you can compute what this is thousand 24 you can compute what this is thousand 24 you can compute what this is you should put a real number here so you you should put a real number here so you you should put a real number here so you should multiply things out for these two should multiply things out for these two should multiply things out for these two here you should just say yes or no yes here you should just say yes or no yes here you should just say yes or no yes if the predictor will predict this if the predictor will predict this if the predictor will predict this successfully after the initial warmup successfully after the initial warmup successfully after the initial warmup period no if it won't and then how many period no if it won't and then how many period no if it won't and then how many two bit counters are we using for the two bit counters are we using for the two bit counters are we using for the not taken taken not taken taken pattern not taken taken not taken taken pattern not taken taken not taken taken pattern in this particular predictor

## N Bit History Predictor Solution Quiz： Part 1
Kind: captions Language: en let's now look at the solution for our let's now look at the solution for our let's now look at the solution for our nbit history predictor quiz we said that nbit history predictor quiz we said that nbit history predictor quiz we said that we want to have an nbit history we want to have an nbit history we want to have an nbit history predictor with two bit counters per predictor with two bit counters per predictor with two bit counters per possible history and we need 1,24 possible history and we need 1,24 possible history and we need 1,24 entries like that and now we want to entries like that and now we want to entries like that and now we want to compute the cost for these predictors compute the cost for these predictors compute the cost for these predictors how well they do on the NN NT pattern how well they do on the NN NT pattern how well they do on the NN NT pattern and also how many of the two bit and also how many of the two bit and also how many of the two bit counters do they actually use for the NT counters do they actually use for the NT counters do they actually use for the NT repeating pattern the cost can be repeating pattern the cost can be repeating pattern the cost can be computed as simply the size of the computed as simply the size of the computed as simply the size of the history plus we need 2 to the nth 2bit history plus we need 2 to the nth 2bit history plus we need 2 to the nth 2bit counters for each entry and if you counters for each entry and if you counters for each entry and if you compute what this is it amounts to 532,250multifamily 532,250multifamily 532,250multifamily 234,00 234,00 234,00 1112 bits so this is many many megabits 1112 bits so this is many many megabits 1112 bits so this is many many megabits worth of predictor stuff again for the worth of predictor stuff again for the worth of predictor stuff again for the same number of entries as you can see same number of entries as you can see same number of entries as you can see this is probably not a realistic this is probably not a realistic this is probably not a realistic predictor at all and even this one predictor at all and even this one predictor at all and even this one probably you don't want to spend that probably you don't want to spend that probably you don't want to spend that much Hardware on just Branch prediction much Hardware on just Branch prediction much Hardware on just Branch prediction let's now see how well they do on the NN let's now see how well they do on the NN let's now see how well they do on the NN NT pattern this is a pattern of length NT pattern this is a pattern of length NT pattern this is a pattern of length four so anything that has n equal three four so anything that has n equal three four so anything that has n equal three or more will handle it well so from that or more will handle it well so from that or more will handle it well so from that perspective all three of these will do perspective all three of these will do perspective all three of these will do well these two will do well on even well these two will do well on even well these two will do well on even longer patterns and finally how many of longer patterns and finally how many of longer patterns and finally how many of those two bit counters that we have are those two bit counters that we have are those two bit counters that we have are we actually using for the not taken we actually using for the not taken we actually using for the not taken taken not taken taken think about it taken not taken taken think about it taken not taken taken think about it this way for the N equals 1 when we need this way for the N equals 1 when we need this way for the N equals 1 when we need to predict the taken Branch we have a to predict the taken Branch we have a to predict the taken Branch we have a history of just zero because it's a history of just zero because it's a history of just zero because it's a single bit history when we need to single bit history when we need to single bit history when we need to predict the not Branch we have a history predict the not Branch we have a history predict the not Branch we have a history of just one so we're using both of these of just one so we're using both of these of just one so we're using both of these when we have n equal 4 and trying to when we have n equal 4 and trying to when we have n equal 4 and trying to predict this not taken Branch our predict this not taken Branch our predict this not taken Branch our history is 0 1 01 when we're trying to history is 0 1 01 when we're trying to history is 0 1 01 when we're trying to predict the taken Branch the history predict the taken Branch the history predict the taken Branch the history will be 1 1 0 when we're trying to will be 1 1 0 when we're trying to will be 1 1 0 when we're trying to predict the next not taken Branch the predict the next not taken Branch the predict the next not taken Branch the history again will be 0 1 01 so as you history again will be 0 1 01 so as you history again will be 0 1 01 so as you can see really we will be only using can see really we will be only using can see really we will be only using entries 0 1 01 and 1 0 1 0 so we're entries 0 1 01 and 1 0 1 0 so we're entries 0 1 01 and 1 0 1 0 so we're still using only only still using only only still using only only two of the 16 2bit counters if you look two of the 16 2bit counters if you look two of the 16 2bit counters if you look at the 8bit history the history will at the 8bit history the history will at the 8bit history the history will just be 0 1 0 1 0 1 01 and 1 0 1 0 1 0 1 just be 0 1 0 1 0 1 01 and 1 0 1 0 1 0 1 just be 0 1 0 1 0 1 01 and 1 0 1 0 1 0 1 0 here so we will still be using only 0 here so we will still be using only 0 here so we will still be using only two of now two of now two of now 256 2bit counters so we are now using 256 2bit counters so we are now using 256 2bit counters so we are now using less than 1% of the counters we have in less than 1% of the counters we have in less than 1% of the counters we have in each entry and finally for this each entry and finally for this each entry and finally for this history you can see the trend here we history you can see the trend here we history you can see the trend here we will be using only two 2bit counters of will be using only two 2bit counters of will be using only two 2bit counters of more than 65,000 that we have in each more than 65,000 that we have in each more than 65,000 that we have in each entry

## History Predictor Quiz： Part 1
Kind: captions Language: en let's do another quiz about history let's do another quiz about history let's do another quiz about history predictors let's say we have a program predictors let's say we have a program predictors let's say we have a program in which we have two nested Loops one I in which we have two nested Loops one I in which we have two nested Loops one I goes from0 through 7 and then within it goes from0 through 7 and then within it goes from0 through 7 and then within it we have a nested Loop of J goes from 0 we have a nested Loop of J goes from 0 we have a nested Loop of J goes from 0 through 7 let's say we do something that through 7 let's say we do something that through 7 let's say we do something that doesn't have much branching behavior in doesn't have much branching behavior in doesn't have much branching behavior in it this type of nested Loop structure is it this type of nested Loop structure is it this type of nested Loop structure is very common when you have things like very common when you have things like very common when you have things like matrices or even image processing in matrices or even image processing in matrices or even image processing in particular for image processing it's particular for image processing it's particular for image processing it's very common that for example compression very common that for example compression very common that for example compression or decompression are working in blocks or decompression are working in blocks or decompression are working in blocks of let's say 8 by8 pixels in this case of let's say 8 by8 pixels in this case of let's say 8 by8 pixels in this case which will mean that there are Loops which will mean that there are Loops which will mean that there are Loops like this all over the code let's say like this all over the code let's say like this all over the code let's say that the code when it does this nested that the code when it does this nested that the code when it does this nested Loop just does something else and then Loop just does something else and then Loop just does something else and then very quickly comes back to it to do very quickly comes back to it to do very quickly comes back to it to do another round of this so This Is Not another round of this so This Is Not another round of this so This Is Not Just executed once the whole thing is Just executed once the whole thing is Just executed once the whole thing is executed many many times if we are executed many many times if we are executed many many times if we are building a history based predictor that building a history based predictor that building a history based predictor that should do well on this code it needs at should do well on this code it needs at should do well on this code it needs at least how many entries and each of these least how many entries and each of these least how many entries and each of these entries should have how many bits of entries should have how many bits of entries should have how many bits of history and how many two bit counters do history and how many two bit counters do history and how many two bit counters do we need per entry as a result of this we need per entry as a result of this we need per entry as a result of this history

## History Predictor Solution Quiz： Part 1
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our history predictor quiz in c a loop exits history predictor quiz in c a loop exits history predictor quiz in c a loop exits at the beginning so the first loop will at the beginning so the first loop will at the beginning so the first loop will do something like initialize i to zero do something like initialize i to zero do something like initialize i to zero if i is equal to eight jump to done if i is equal to eight jump to done if i is equal to eight jump to done otherwise do everything that is needed otherwise do everything that is needed otherwise do everything that is needed for an iteration of the outer loop for an iteration of the outer loop for an iteration of the outer loop and then branch back here and then branch back here and then branch back here the inner loop will just do j equals the inner loop will just do j equals the inner loop will just do j equals zero zero zero and then if j is equal to 8 and then if j is equal to 8 and then if j is equal to 8 go to some label d1 go to some label d1 go to some label d1 do something do something do something jump back here and the label d1 would jump back here and the label d1 would jump back here and the label d1 would really be here really be here really be here after which we are going like this after which we are going like this after which we are going like this so how many entries do we need we need so how many entries do we need we need so how many entries do we need we need at least one entry per branch although at least one entry per branch although at least one entry per branch although in fact we need one entry for each in fact we need one entry for each in fact we need one entry for each instruction but at least we need to have instruction but at least we need to have instruction but at least we need to have entries for branches so we have a branch entries for branches so we have a branch entries for branches so we have a branch that does this here we have a branch that does this here we have a branch that does this here we have a branch that does this here we have a loopback that does this here we have a loopback that does this here we have a loopback branch here and we have a loopback branch here and we have a loopback branch here and we have a loopback branch here so we're talking about at branch here so we're talking about at branch here so we're talking about at least four entries least four entries least four entries in reality we need many many more in in reality we need many many more in in reality we need many many more in order to do well on all four of these order to do well on all four of these order to do well on all four of these branches we need to see what is the branches we need to see what is the branches we need to see what is the longest history that any one of them longest history that any one of them longest history that any one of them will have the pack branch is always will have the pack branch is always will have the pack branch is always taken so really we don't even need a taken so really we don't even need a taken so really we don't even need a history for it even a single two bit history for it even a single two bit history for it even a single two bit counter will do well on these two but counter will do well on these two but counter will do well on these two but this one we'll be testing eight times this one we'll be testing eight times this one we'll be testing eight times for zero through seven for zero through seven for zero through seven and stay in the loop and then the ninth and stay in the loop and then the ninth and stay in the loop and then the ninth time it tests it jumps out so really it time it tests it jumps out so really it time it tests it jumps out so really it has a pattern of length nine has a pattern of length nine has a pattern of length nine which means we need an eight bit history which means we need an eight bit history which means we need an eight bit history with an eighth bit history with an eighth bit history with an eighth bit history we need two to the 8th we need two to the 8th we need two to the 8th 2-bit counters 2-bit counters 2-bit counters so that leads to 256 2-bit counters now so that leads to 256 2-bit counters now so that leads to 256 2-bit counters now what is the pattern of this branch here what is the pattern of this branch here what is the pattern of this branch here it really is 8 times it's not taken it really is 8 times it's not taken it really is 8 times it's not taken followed by a taken and that repeats followed by a taken and that repeats followed by a taken and that repeats the inner branch does exactly the same the inner branch does exactly the same the inner branch does exactly the same thing except it does it many more times thing except it does it many more times thing except it does it many more times so if you look at how many possible so if you look at how many possible so if you look at how many possible patterns we actually have patterns we actually have patterns we actually have of length eight we have a pattern of of length eight we have a pattern of of length eight we have a pattern of eight not takens eight not takens eight not takens we have a pattern of seven not takens we have a pattern of seven not takens we have a pattern of seven not takens followed by a taken we have a pattern of followed by a taken we have a pattern of followed by a taken we have a pattern of six not taken then taken not taken etc six not taken then taken not taken etc six not taken then taken not taken etc so we are really using only nine of so we are really using only nine of so we are really using only nine of these two bit counters which again shows these two bit counters which again shows these two bit counters which again shows that that that a predictor of this type with a very a predictor of this type with a very a predictor of this type with a very long history will waste a lot of its long history will waste a lot of its long history will waste a lot of its many many entries

## History With Shared Counters Part 1： Part 1
Kind: captions Language: en so now let's talk about how to reduce so now let's talk about how to reduce so now let's talk about how to reduce the waste how to have fewer than two to the waste how to have fewer than two to the waste how to have fewer than two to the nth counters and yet have a long the nth counters and yet have a long the nth counters and yet have a long history so we will talk about the history so we will talk about the history so we will talk about the history based predictor with shared history based predictor with shared history based predictor with shared counters so again the motivation for counters so again the motivation for counters so again the motivation for this is that with nbit history the way this is that with nbit history the way this is that with nbit history the way we just seen it we will need two to the we just seen it we will need two to the we just seen it we will need two to the nth counters per entry if we want to do nth counters per entry if we want to do nth counters per entry if we want to do things the way we've just done them and things the way we've just done them and things the way we've just done them and we also know that for repeating patterns we also know that for repeating patterns we also know that for repeating patterns only about n counters out of two to the only about n counters out of two to the only about n counters out of two to the are really used in reality we're using are really used in reality we're using are really used in reality we're using about n plus one but basically the about n plus one but basically the about n plus one but basically the important thing is that we are using the important thing is that we are using the important thing is that we are using the number of counters that is proportional number of counters that is proportional number of counters that is proportional to the size of the history but we need to the size of the history but we need to the size of the history but we need to have two to the end counters because to have two to the end counters because to have two to the end counters because we don't know which histories will be we don't know which histories will be we don't know which histories will be important so the idea here is to share important so the idea here is to share important so the idea here is to share these two bit counters between entries these two bit counters between entries these two bit counters between entries instead of dedicating two to the nth two instead of dedicating two to the nth two instead of dedicating two to the nth two bit counters to each entry we will have bit counters to each entry we will have bit counters to each entry we will have a pool of two bit counters that entries a pool of two bit counters that entries a pool of two bit counters that entries will then use but it is possible that will then use but it is possible that will then use but it is possible that different entries with different program different entries with different program different entries with different program counters and different histories end up counters and different histories end up counters and different histories end up using the same two bit counter so there using the same two bit counter so there using the same two bit counter so there is a possibility of conflict but if we is a possibility of conflict but if we is a possibility of conflict but if we have enough of these two bit counters have enough of these two bit counters have enough of these two bit counters because each entry really doesn't need because each entry really doesn't need because each entry really doesn't need too many of them usually we will have too many of them usually we will have too many of them usually we will have very few conflicts

## History With Shared Counters part 2： Part 1
Kind: captions Language: en So this is how our history with shared So this is how our history with shared So this is how our history with shared counters would work. We have the program counters would work. We have the program counters would work. We have the program counter. We take some of the lowermost counter. We take some of the lowermost counter. We take some of the lowermost bits. Use that to index into what we bits. Use that to index into what we bits. Use that to index into what we will call PHT or pattern history table. will call PHT or pattern history table. will call PHT or pattern history table. This is a table that simply keeps the This is a table that simply keeps the This is a table that simply keeps the history bits alone for that branch. So history bits alone for that branch. So history bits alone for that branch. So if we have an 11 bit history, this table if we have an 11 bit history, this table if we have an 11 bit history, this table will have 11 bits per entry. So it will have 11 bits per entry. So it will have 11 bits per entry. So it doesn't have the two bit counters. But doesn't have the two bit counters. But doesn't have the two bit counters. But we do need the two bit counter to we do need the two bit counter to we do need the two bit counter to eventually tell us whether the branch is eventually tell us whether the branch is eventually tell us whether the branch is supposed to be taken or not taken. So supposed to be taken or not taken. So supposed to be taken or not taken. So what we do is we take this history from what we do is we take this history from what we do is we take this history from this table combine it in some way this table combine it in some way this table combine it in some way usually using an exor with the bits of usually using an exor with the bits of usually using an exor with the bits of the PC and use that to index into what the PC and use that to index into what the PC and use that to index into what we still will call the branch history we still will call the branch history we still will call the branch history table. This branch history table has table. This branch history table has table. This branch history table has entries. Each of these entries is just a entries. Each of these entries is just a entries. Each of these entries is just a single twobit counter. So each entry is single twobit counter. So each entry is single twobit counter. So each entry is very small and that entry will tell us very small and that entry will tell us very small and that entry will tell us whether we should be predicting taken or whether we should be predicting taken or whether we should be predicting taken or not taken. When the branch outcome is not taken. When the branch outcome is not taken. When the branch outcome is known, we use the same history and PC known, we use the same history and PC known, we use the same history and PC combination to index back into this two combination to index back into this two combination to index back into this two bit counter increment or decrement it bit counter increment or decrement it bit counter increment or decrement it based on the current decision on a based on the current decision on a based on the current decision on a branch and then we shift in that pattern branch and then we shift in that pattern branch and then we shift in that pattern also into the PHT entry so that this also into the PHT entry so that this also into the PHT entry so that this history is ready for the next prediction history is ready for the next prediction history is ready for the next prediction on this particular branch. So the idea on this particular branch. So the idea on this particular branch. So the idea is that the program counter here tells is that the program counter here tells is that the program counter here tells us which branch we are talking about. We us which branch we are talking about. We us which branch we are talking about. We hope that the pattern history table has hope that the pattern history table has hope that the pattern history table has one history per branch that we have. one history per branch that we have. one history per branch that we have. Different histories and different pieces Different histories and different pieces Different histories and different pieces will result in different two-bit will result in different two-bit will result in different two-bit counters being used and thus the counters being used and thus the counters being used and thus the decisions will be made for kind of like decisions will be made for kind of like decisions will be made for kind of like a combination of the history and the PC. a combination of the history and the PC. a combination of the history and the PC. Note that it is possible for another PC Note that it is possible for another PC Note that it is possible for another PC to map somewhere here. But that history to map somewhere here. But that history to map somewhere here. But that history when exor with that PC gives us maybe when exor with that PC gives us maybe when exor with that PC gives us maybe the same two bit counter. So it is the same two bit counter. So it is the same two bit counter. So it is possible to have some overlap here. But possible to have some overlap here. But possible to have some overlap here. But the idea is that this way for each PC we the idea is that this way for each PC we the idea is that this way for each PC we have a single history of all the have a single history of all the have a single history of all the possible histories where there is like possible histories where there is like possible histories where there is like two to the nth of them. We're really two to the nth of them. We're really two to the nth of them. We're really using only a very small number of using only a very small number of using only a very small number of counters because very few histories are counters because very few histories are counters because very few histories are actually happening. And that means that actually happening. And that means that actually happening. And that means that if we have a relatively large array of if we have a relatively large array of if we have a relatively large array of two- bit counters, the possibility of two- bit counters, the possibility of two- bit counters, the possibility of conflict is very low. But this allows us conflict is very low. But this allows us conflict is very low. But this allows us to have a lot of bits of history. For to have a lot of bits of history. For to have a lot of bits of history. For example, if we take 11 bits of PC and we example, if we take 11 bits of PC and we example, if we take 11 bits of PC and we index into the PhD with them, we will index into the PhD with them, we will index into the PhD with them, we will need 2 to the 11th histories times let's need 2 to the 11th histories times let's need 2 to the 11th histories times let's say 11 bits of history. We exert those say 11 bits of history. We exert those say 11 bits of history. We exert those 11 bits with these 11 bits. So we will 11 bits with these 11 bits. So we will 11 bits with these 11 bits. So we will have 2 to the 11th two bit counters. So have 2 to the 11th two bit counters. So have 2 to the 11th two bit counters. So the overall cost of this predictor will the overall cost of this predictor will the overall cost of this predictor will be 2 to the 11th * 11 for the histories be 2 to the 11th * 11 for the histories be 2 to the 11th * 11 for the histories plus 2 to the 11 * 2 for the counters plus 2 to the 11 * 2 for the counters plus 2 to the 11 * 2 for the counters which ends up costing us 26 kilo bits. which ends up costing us 26 kilo bits. which ends up costing us 26 kilo bits. This is much less than what the cost This is much less than what the cost This is much less than what the cost would be if we had an array of two bit would be if we had an array of two bit would be if we had an array of two bit counters for each possible history in counters for each possible history in counters for each possible history in each entry over here. pretty much these each entry over here. pretty much these each entry over here. pretty much these 2 to the 11* 2 would need to be in each 2 to the 11* 2 would need to be in each 2 to the 11* 2 would need to be in each entry here if we didn't do the ex entry here if we didn't do the ex entry here if we didn't do the ex sorting like this. Note also that we sorting like this. Note also that we sorting like this. Note also that we don't have to have these numbers here don't have to have these numbers here don't have to have these numbers here and here equal. We can for example take and here equal. We can for example take and here equal. We can for example take something like 10 bits of the PC to something like 10 bits of the PC to something like 10 bits of the PC to index into the PHT which gives us only 2 index into the PHT which gives us only 2 index into the PHT which gives us only 2 to the 10th histories and have a 16 bit to the 10th histories and have a 16 bit to the 10th histories and have a 16 bit history so that we can have two to the history so that we can have two to the history so that we can have two to the 16th entries here and so on. It still 16th entries here and so on. It still 16th entries here and so on. It still will keep the overall cost in the you will keep the overall cost in the you will keep the overall cost in the you know tens of kilobits not megabits or know tens of kilobits not megabits or know tens of kilobits not megabits or gigabits that we would get if we try to gigabits that we would get if we try to gigabits that we would get if we try to put together long histories with put together long histories with put together long histories with dedicating two-bit counters for each dedicating two-bit counters for each dedicating two-bit counters for each possible history in each entry.

## History With Shared Counters part 3： Part 1
Kind: captions Language: en Now let's see how well this works when Now let's see how well this works when Now let's see how well this works when we have something like a branch that is we have something like a branch that is we have something like a branch that is just always taken. In that case the just always taken. In that case the just always taken. In that case the entry for that branch will just be all entry for that branch will just be all entry for that branch will just be all ones in the history. That branch has a ones in the history. That branch has a ones in the history. That branch has a fixed PC. It always has the same fixed PC. It always has the same fixed PC. It always has the same history. When we extore them we will be history. When we extore them we will be history. When we extore them we will be using only one two bit counter. So using only one two bit counter. So using only one two bit counter. So pretty much although we have a long pretty much although we have a long pretty much although we have a long history if the branch is just a history if the branch is just a history if the branch is just a repetition of the same thing we are repetition of the same thing we are repetition of the same thing we are ending up using only one of the two bit ending up using only one of the two bit ending up using only one of the two bit counters. So the additional cost counters. So the additional cost counters. So the additional cost relative to just having an array of two relative to just having an array of two relative to just having an array of two bit counters is that now we're keeping bit counters is that now we're keeping bit counters is that now we're keeping an 11 bit history for this branch. So an 11 bit history for this branch. So an 11 bit history for this branch. So still the cost is not just two bits. still the cost is not just two bits. still the cost is not just two bits. It's really let's say 11 or 16 bits plus It's really let's say 11 or 16 bits plus It's really let's say 11 or 16 bits plus two. But it's much less than a long two. But it's much less than a long two. But it's much less than a long history plus 2 to the n times a two bit history plus 2 to the n times a two bit history plus 2 to the n times a two bit counter. If we have a branch that is counter. If we have a branch that is counter. If we have a branch that is never taken, we can do the same never taken, we can do the same never taken, we can do the same reasoning and end up using a single reasoning and end up using a single reasoning and end up using a single counter. If we have a branch that has counter. If we have a branch that has counter. If we have a branch that has something like NT NT, it really has only something like NT NT, it really has only something like NT NT, it really has only two possible histories 0 1 0 1 0 1 etc. two possible histories 0 1 0 1 0 1 etc. two possible histories 0 1 0 1 0 1 etc. or 1 0 1 0 1 0 etc. So it ends up using or 1 0 1 0 1 0 etc. So it ends up using or 1 0 1 0 1 0 etc. So it ends up using two counters. So if we have a lot of two counters. So if we have a lot of two counters. So if we have a lot of these counters as you can see most these counters as you can see most these counters as you can see most branches are going to be like these. We branches are going to be like these. We branches are going to be like these. We will be using very very few entries by will be using very very few entries by will be using very very few entries by these branches which leaves us plenty of these branches which leaves us plenty of these branches which leaves us plenty of counters for branches where we have a counters for branches where we have a counters for branches where we have a very long pattern where we will be using very long pattern where we will be using very long pattern where we will be using something like you know 16 histories and something like you know 16 histories and something like you know 16 histories and thus use something like 16 counters. As thus use something like 16 counters. As thus use something like 16 counters. As you can see in this organization pretty you can see in this organization pretty you can see in this organization pretty much the branches that need more much the branches that need more much the branches that need more counters get them. The branches that counters get them. The branches that counters get them. The branches that need relatively few counters get them. need relatively few counters get them. need relatively few counters get them. The only really additional cost relative The only really additional cost relative The only really additional cost relative to just having the counters is the to just having the counters is the to just having the counters is the histories. The downside is that again histories. The downside is that again histories. The downside is that again some branches with particular histories some branches with particular histories some branches with particular histories might overlap in which counter they use might overlap in which counter they use might overlap in which counter they use with other branches and other histories. with other branches and other histories. with other branches and other histories. Pretty much my branch exord with its Pretty much my branch exord with its Pretty much my branch exord with its history might map to the same counter as history might map to the same counter as history might map to the same counter as some other branch and its own history some other branch and its own history some other branch and its own history when they are exort. But this happens when they are exort. But this happens when they are exort. But this happens very rarely if the BHT is large. And now very rarely if the BHT is large. And now very rarely if the BHT is large. And now it can be large because each entry is it can be large because each entry is it can be large because each entry is just two bits.

## PShare： Part 1
Kind: captions Language: en What we have just seen is called a What we have just seen is called a What we have just seen is called a Pshare predictor. It has a private Pshare predictor. It has a private Pshare predictor. It has a private history for each branch. That is each history for each branch. That is each history for each branch. That is each branch should have its own history in branch should have its own history in branch should have its own history in the branch history table and it has the branch history table and it has the branch history table and it has shared counters so that different shared counters so that different shared counters so that different histories and different branches might histories and different branches might histories and different branches might map to the same counters. This predictor map to the same counters. This predictor map to the same counters. This predictor is good for things like even odd is good for things like even odd is good for things like even odd behavior like taken not taken loops with behavior like taken not taken loops with behavior like taken not taken loops with few iterations. For example, those eight few iterations. For example, those eight few iterations. For example, those eight iteration loops that we have seen and so iteration loops that we have seen and so iteration loops that we have seen and so on. So pretty much whenever the branch's on. So pretty much whenever the branch's on. So pretty much whenever the branch's own previous behavior is predictive of own previous behavior is predictive of own previous behavior is predictive of its future behavior, Pshare tends to do its future behavior, Pshare tends to do its future behavior, Pshare tends to do well. Another type of similar predictor well. Another type of similar predictor well. Another type of similar predictor that we can make is called G-share. It that we can make is called G-share. It that we can make is called G-share. It has a global history and shared has a global history and shared has a global history and shared counters. So a global history means that counters. So a global history means that counters. So a global history means that there is only a single global history there is only a single global history there is only a single global history that we use to predict all branches. So that we use to predict all branches. So that we use to predict all branches. So what we have is now the PC a single what we have is now the PC a single what we have is now the PC a single history that we exert together and index history that we exert together and index history that we exert together and index into the array of two bit counters. This into the array of two bit counters. This into the array of two bit counters. This history every branch decision regardless history every branch decision regardless history every branch decision regardless of what the PC of the branch is gets of what the PC of the branch is gets of what the PC of the branch is gets shifted in here. So what is Gshshire shifted in here. So what is Gshshire shifted in here. So what is Gshshire good for? It's good for so-called good for? It's good for so-called good for? It's good for so-called correlated branches. That is branches correlated branches. That is branches correlated branches. That is branches whose decision is related to what the whose decision is related to what the whose decision is related to what the other branches in the program were other branches in the program were other branches in the program were doing. Now I would think when would that doing. Now I would think when would that doing. Now I would think when would that happen and it turns out that it happens happen and it turns out that it happens happen and it turns out that it happens in programs quite often. Think about in programs quite often. Think about in programs quite often. Think about something like this. If my shape is a something like this. If my shape is a something like this. If my shape is a square then do something. Then do square then do something. Then do square then do something. Then do something that you do either way something that you do either way something that you do either way regardless of what the shape is. Then if regardless of what the shape is. Then if regardless of what the shape is. Then if the shape is not square I have something the shape is not square I have something the shape is not square I have something else to do. This branch and this branch else to do. This branch and this branch else to do. This branch and this branch are entirely correlated. If this one was are entirely correlated. If this one was are entirely correlated. If this one was taken this one will not be taken and the taken this one will not be taken and the taken this one will not be taken and the other way around. So if this branch is other way around. So if this branch is other way around. So if this branch is in the history when this one is being in the history when this one is being in the history when this one is being predicted then we can perfectly predict predicted then we can perfectly predict predicted then we can perfectly predict this branch. Note that however if we this branch. Note that however if we this branch. Note that however if we look at this branch individually or this look at this branch individually or this look at this branch individually or this branch individually the history of this branch individually the history of this branch individually the history of this branch might be completely random branch might be completely random branch might be completely random because it really depends on our data because it really depends on our data because it really depends on our data whether shapes are squares. So if we whether shapes are squares. So if we whether shapes are squares. So if we randomly have shapes that are squares or randomly have shapes that are squares or randomly have shapes that are squares or not, we cannot really predict much here. not, we cannot really predict much here. not, we cannot really predict much here. So this branch might be mispredicted So this branch might be mispredicted So this branch might be mispredicted often because we simply don't know at often because we simply don't know at often because we simply don't know at that point. But this branch becomes that point. But this branch becomes that point. But this branch becomes perfectly predictable if it's using the perfectly predictable if it's using the perfectly predictable if it's using the history produced by this branch.

## PShare vs GShare Quiz： Part 1
Kind: captions Language: en now that we have seen pshare and gshare now that we have seen pshare and gshare now that we have seen pshare and gshare let's do a quiz about them let's say let's do a quiz about them let's say let's do a quiz about them let's say that we have this fragment of C code that we have this fragment of C code that we have this fragment of C code where I goes from 1,000 down to zero and where I goes from 1,000 down to zero and where I goes from 1,000 down to zero and for odd numbered eyes we are going to for odd numbered eyes we are going to for odd numbered eyes we are going to add the I to n otherwise we just don't add the I to n otherwise we just don't add the I to n otherwise we just don't do it this code might translate into do it this code might translate into do it this code might translate into assembler program like this this Branch assembler program like this this Branch assembler program like this this Branch here is the test for exiting this Loop here is the test for exiting this Loop here is the test for exiting this Loop when R1 is zero this is testing the when R1 is zero this is testing the when R1 is zero this is testing the least significant bit of R1 least significant bit of R1 least significant bit of R1 and if that bit is zero that means we and if that bit is zero that means we and if that bit is zero that means we have an even number so we jump to the have an even number so we jump to the have an even number so we jump to the even label if the number is odd then we even label if the number is odd then we even label if the number is odd then we add it to R3 which in this case is add it to R3 which in this case is add it to R3 which in this case is representing n either way we decrement representing n either way we decrement representing n either way we decrement the R1 this is the IUS minus and we the R1 this is the IUS minus and we the R1 this is the IUS minus and we unconditionally Branch back to the loop unconditionally Branch back to the loop unconditionally Branch back to the loop and the exit label for exiting the loop and the exit label for exiting the loop and the exit label for exiting the loop is here if we want to get good accuracy is here if we want to get good accuracy is here if we want to get good accuracy on all of the branches in this code then on all of the branches in this code then on all of the branches in this code then the history should be how much for the history should be how much for the history should be how much for pshare right the number here and for pshare right the number here and for pshare right the number here and for gshare write the number here so we're gshare write the number here so we're gshare write the number here so we're just talking about how many bits of just talking about how many bits of just talking about how many bits of History we need to get good prediction History we need to get good prediction History we need to get good prediction accuracy for all of these branches

## PShare vs GShare Solution Quiz： Part 1
Kind: captions Language: en let's have the solution for our pshare let's have the solution for our pshare let's have the solution for our pshare versus GX share quiz the question is for versus GX share quiz the question is for versus GX share quiz the question is for all of the branches in this code how all of the branches in this code how all of the branches in this code how much history do we need to make them all much history do we need to make them all much history do we need to make them all very well predicted we have one two very well predicted we have one two very well predicted we have one two three branches this Branch here is three branches this Branch here is three branches this Branch here is easily predictable even without any easily predictable even without any easily predictable even without any history so for this one any history will history so for this one any history will history so for this one any history will work the exit Branch we do a thousand work the exit Branch we do a thousand work the exit Branch we do a thousand iteration each time it's not taken iteration each time it's not taken iteration each time it's not taken except the last time so for this Branch except the last time so for this Branch except the last time so for this Branch again we don't need much history even again we don't need much history even again we don't need much history even with no history using just a two bit with no history using just a two bit with no history using just a two bit counter we would predict this Branch counter we would predict this Branch counter we would predict this Branch accurately a thousand times followed by accurately a thousand times followed by accurately a thousand times followed by one misprediction so it's a very good one misprediction so it's a very good one misprediction so it's a very good prediction rate still this Branch prediction rate still this Branch prediction rate still this Branch however requires some history this is however requires some history this is however requires some history this is the even odd Branch so we really need to the even odd Branch so we really need to the even odd Branch so we really need to know whether the previous outcome was know whether the previous outcome was know whether the previous outcome was even or odd so that we can do the even or odd so that we can do the even or odd so that we can do the opposite for pshare the even odd opposite for pshare the even odd opposite for pshare the even odd handling can be done with just a history handling can be done with just a history handling can be done with just a history length of one and the history length of length of one and the history length of length of one and the history length of one will work well for these two as well one will work well for these two as well one will work well for these two as well so we just need a history length of one so we just need a history length of one so we just need a history length of one for gshare when we're doing this Branch for gshare when we're doing this Branch for gshare when we're doing this Branch we want its previous outcome to be part we want its previous outcome to be part we want its previous outcome to be part of the global history if we look at what of the global history if we look at what of the global history if we look at what the global history looks like this the global history looks like this the global history looks like this branch is not taken so the global branch is not taken so the global branch is not taken so the global history will be zero this branch in the history will be zero this branch in the history will be zero this branch in the very first iteration because I is 1,000 very first iteration because I is 1,000 very first iteration because I is 1,000 that's divisible by two is taken because that's divisible by two is taken because that's divisible by two is taken because the number is even this branch is then the number is even this branch is then the number is even this branch is then taken then we have this Branch zero now taken then we have this Branch zero now taken then we have this Branch zero now it's going to be not taken it's going to be not taken it's going to be not taken here followed by a taken here and this here followed by a taken here and this here followed by a taken here and this pattern will repeat so really we need pattern will repeat so really we need pattern will repeat so really we need this to be part of the history when this to be part of the history when this to be part of the history when we're predicting this we need this to be we're predicting this we need this to be we're predicting this we need this to be part of the history and we're predicting part of the history and we're predicting part of the history and we're predicting this and so on in order to do that we this and so on in order to do that we this and so on in order to do that we need the history to contain at least need the history to contain at least need the history to contain at least this much which is three bits and this much which is three bits and this much which is three bits and similarly here we need this to be our similarly here we need this to be our similarly here we need this to be our history so it catches this bit not that history so it catches this bit not that history so it catches this bit not that really there are only two possible really there are only two possible really there are only two possible histories for this Branch one is the 1 1 histories for this Branch one is the 1 1 histories for this Branch one is the 1 1 0 and the other is 0 1 0 because the 0 and the other is 0 1 0 because the 0 and the other is 0 1 0 because the other two branches are just having the other two branches are just having the other two branches are just having the same outcome every time so we need a same outcome every time so we need a same outcome every time so we need a three bit history as you can see gshare three bit history as you can see gshare three bit history as you can see gshare can do a similar job to pshare except can do a similar job to pshare except can do a similar job to pshare except that it needs a longer history even for that it needs a longer history even for that it needs a longer history even for branches that are actually kind of self branches that are actually kind of self branches that are actually kind of self determining note that gshare can also do determining note that gshare can also do determining note that gshare can also do those correlative branches that pshare those correlative branches that pshare those correlative branches that pshare cannot do

## GShare or PShare： Part 1
Kind: captions Language: en So now that we have seen that G-share So now that we have seen that G-share So now that we have seen that G-share can do some correlated branches that can do some correlated branches that can do some correlated branches that Pshare cannot do. But on the other hand, Pshare cannot do. But on the other hand, Pshare cannot do. But on the other hand, Pshare can do things with shorter Pshare can do things with shorter Pshare can do things with shorter history that require Gshare to have more history that require Gshare to have more history that require Gshare to have more of a history. The question is if we need of a history. The question is if we need of a history. The question is if we need to choose one, which one should we to choose one, which one should we to choose one, which one should we choose in our processor? Many early choose in our processor? Many early choose in our processor? Many early processors would choose either G-Share processors would choose either G-Share processors would choose either G-Share or Pshare. But very quickly people or Pshare. But very quickly people or Pshare. But very quickly people figured out that you really want to have figured out that you really want to have figured out that you really want to have both of them. You want to have a G-share both of them. You want to have a G-share both of them. You want to have a G-share so that you can do correlated branches so that you can do correlated branches so that you can do correlated branches and you want to do Pshare because it and you want to do Pshare because it and you want to do Pshare because it does better figuring out those self- does better figuring out those self- does better figuring out those self- similar branches even if it has a similar branches even if it has a similar branches even if it has a shorter history.

## Tournament Predictor： Part 1
Kind: captions Language: en so that brings us to a so-called so that brings us to a so-called so that brings us to a so-called tournament predictor the idea is that tournament predictor the idea is that tournament predictor the idea is that you have two predictors one is better you have two predictors one is better you have two predictors one is better for some branches the other is better for some branches the other is better for some branches the other is better for other branches now what you want to for other branches now what you want to for other branches now what you want to do is to get the best behavior for all do is to get the best behavior for all do is to get the best behavior for all types of branches so what you want to do types of branches so what you want to do types of branches so what you want to do is when you have these branches use the is when you have these branches use the is when you have these branches use the first predictor when you have these first predictor when you have these first predictor when you have these branches use the second predictor but branches use the second predictor but branches use the second predictor but you don't know which branches are going you don't know which branches are going you don't know which branches are going to benefit from which of your two to benefit from which of your two to benefit from which of your two predictors ahead of time so what you predictors ahead of time so what you predictors ahead of time so what you really have is your two predictors let's really have is your two predictors let's really have is your two predictors let's say that they're G share and pshare you say that they're G share and pshare you say that they're G share and pshare you will use your PC of the branch to index will use your PC of the branch to index will use your PC of the branch to index into both of the predictors you will into both of the predictors you will into both of the predictors you will take the decisions that they generate take the decisions that they generate take the decisions that they generate and now you need to decide which one to and now you need to decide which one to and now you need to decide which one to use how do you do that and the idea of a use how do you do that and the idea of a use how do you do that and the idea of a tournament predictor is to use a tournament predictor is to use a tournament predictor is to use a so-called meta predictor which is just so-called meta predictor which is just so-called meta predictor which is just an array of two bit counters you also an array of two bit counters you also an array of two bit counters you also use the PC2 index there and the entry of use the PC2 index there and the entry of use the PC2 index there and the entry of the meta the meta the meta predictor does not tell you a prediction predictor does not tell you a prediction predictor does not tell you a prediction for the branch it tells you for the branch it tells you for the branch it tells you which of the two predictors is the more which of the two predictors is the more which of the two predictors is the more likely to give an accurate prediction likely to give an accurate prediction likely to give an accurate prediction for that Branch so what you do is you for that Branch so what you do is you for that Branch so what you do is you use this to choose among the two use this to choose among the two use this to choose among the two decisions so pretty much this meta decisions so pretty much this meta decisions so pretty much this meta predictor gives you a way to select predictor gives you a way to select predictor gives you a way to select among the two decisions so that the among the two decisions so that the among the two decisions so that the final outcome is the decision that the final outcome is the decision that the final outcome is the decision that the meta predictor thinks is a better meta predictor thinks is a better meta predictor thinks is a better prediction you train the individual prediction you train the individual prediction you train the individual predictors just like before on every predictors just like before on every predictors just like before on every Branch outcome you train each of the Branch outcome you train each of the Branch outcome you train each of the predictors you train The Meta predictor predictors you train The Meta predictor predictors you train The Meta predictor a little bit differently instead of a little bit differently instead of a little bit differently instead of counting up when taken and counting down counting up when taken and counting down counting up when taken and counting down when not taken the meta predictor is when not taken the meta predictor is when not taken the meta predictor is instead trained on how well these two instead trained on how well these two instead trained on how well these two are doing if your two predictors are are doing if your two predictors are are doing if your two predictors are both correct then your meta predictor both correct then your meta predictor both correct then your meta predictor doesn't change anything if your first doesn't change anything if your first doesn't change anything if your first predictor like in this case your gshare predictor like in this case your gshare predictor like in this case your gshare is correct but the other one is not then is correct but the other one is not then is correct but the other one is not then you count down counting down will result you count down counting down will result you count down counting down will result in selecting the gshare result more in selecting the gshare result more in selecting the gshare result more often if your first predictor is wrong often if your first predictor is wrong often if your first predictor is wrong but your second predictor is right then but your second predictor is right then but your second predictor is right then you count up counting up will result in you count up counting up will result in you count up counting up will result in choosing this prediction more often and choosing this prediction more often and choosing this prediction more often and finally if both of them are wrong then finally if both of them are wrong then finally if both of them are wrong then the meta predictor doesn't change the meta predictor doesn't change the meta predictor doesn't change because again there is no benefit in because again there is no benefit in because again there is no benefit in trying to select one over the other so trying to select one over the other so trying to select one over the other so the idea of the meta predictor is that the idea of the meta predictor is that the idea of the meta predictor is that the prediction bit of the 2bit counter the prediction bit of the 2bit counter the prediction bit of the 2bit counter is telling you really which of these to is telling you really which of these to is telling you really which of these to select the hysteresis is there just in select the hysteresis is there just in select the hysteresis is there just in case gshare is overall the more accurate case gshare is overall the more accurate case gshare is overall the more accurate but sometimes pshare beats it or the but sometimes pshare beats it or the but sometimes pshare beats it or the other way around and again each branch other way around and again each branch other way around and again each branch has its own meta predictor entry so this has its own meta predictor entry so this has its own meta predictor entry so this type of decision and this type of type of decision and this type of type of decision and this type of selection is done dependent on branches selection is done dependent on branches selection is done dependent on branches so pretty much for your for example Loop so pretty much for your for example Loop so pretty much for your for example Loop exit Branch it might be that the pshare exit Branch it might be that the pshare exit Branch it might be that the pshare works better for your correlated works better for your correlated works better for your correlated branches it might be that the G share branches it might be that the G share branches it might be that the G share works works better and they will have works works better and they will have works works better and they will have different meta predictor entries so that different meta predictor entries so that different meta predictor entries so that they will use different predictors for they will use different predictors for they will use different predictors for their outcomes


# Playlist 3 Module 2 Raw Transcript

## Multi Level Page Tables： Part 4
Kind: captions Language: en so now that we have seen that flat page so now that we have seen that flat page so now that we have seen that flat page tables can be quite large even for tables can be quite large even for tables can be quite large even for 32-bit address spaces and are too big to 32-bit address spaces and are too big to 32-bit address spaces and are too big to even fit in memory for 64-bit address even fit in memory for 64-bit address even fit in memory for 64-bit address spaces let's talk about multi-level page spaces let's talk about multi-level page spaces let's talk about multi-level page tables which is how we reduce the size tables which is how we reduce the size tables which is how we reduce the size of page tables when we have a large of page tables when we have a large of page tables when we have a large address space so the problem with flat address space so the problem with flat address space so the problem with flat page tables is that their size is page tables is that their size is page tables is that their size is proportional to the address space proportional to the address space proportional to the address space meaning how much memory can the meaning how much memory can the meaning how much memory can the application possibly address not how application possibly address not how application possibly address not how much memory it is actually using for a much memory it is actually using for a much memory it is actually using for a 32-bit address space we have seen that 32-bit address space we have seen that 32-bit address space we have seen that we have a reasonably sized page table so we have a reasonably sized page table so we have a reasonably sized page table so for 32-bit virtual addresses the flat for 32-bit virtual addresses the flat for 32-bit virtual addresses the flat page table size is several megabytes page table size is several megabytes page table size is several megabytes note that we can have a very small note that we can have a very small note that we can have a very small application that only uses a couple of application that only uses a couple of application that only uses a couple of kilobytes of memory yet it still needs a kilobytes of memory yet it still needs a kilobytes of memory yet it still needs a page table of this size because the page page table of this size because the page page table of this size because the page table size has nothing to do with how table size has nothing to do with how table size has nothing to do with how much memory we are using it has to do much memory we are using it has to do much memory we are using it has to do with how much memory can we possibly with how much memory can we possibly with how much memory can we possibly address for 64-bit virtual addresses a address for 64-bit virtual addresses a address for 64-bit virtual addresses a flat page table would simply be too big flat page table would simply be too big flat page table would simply be too big the page table would be many many the page table would be many many the page table would be many many gigabytes in size so which simply cannot gigabytes in size so which simply cannot gigabytes in size so which simply cannot use it because it wouldn't fit in the use it because it wouldn't fit in the use it because it wouldn't fit in the amount of physical memory we actually amount of physical memory we actually amount of physical memory we actually have so multi-level page tables are have so multi-level page tables are have so multi-level page tables are designed to try to overcome both of the designed to try to overcome both of the designed to try to overcome both of the problems we want the overall size of the problems we want the overall size of the problems we want the overall size of the page table to be proportional to how page table to be proportional to how page table to be proportional to how much memory we are using and we want our much memory we are using and we want our much memory we are using and we want our page table to work even for 64-bit page table to work even for 64-bit page table to work even for 64-bit virtual addresses as long as the program virtual addresses as long as the program virtual addresses as long as the program is actually not using the entire 64-bit is actually not using the entire 64-bit is actually not using the entire 64-bit virtual address space which no program virtual address space which no program virtual address space which no program really does the idea of why multi-level really does the idea of why multi-level really does the idea of why multi-level page tables work has to do with how the page tables work has to do with how the page tables work has to do with how the virtual address space is usually used by virtual address space is usually used by virtual address space is usually used by the applications recall that the the applications recall that the the applications recall that the application thinks it can address memory application thinks it can address memory application thinks it can address memory with 64-bit addresses so it has this with 64-bit addresses so it has this with 64-bit addresses so it has this many potential memory locations it can many potential memory locations it can many potential memory locations it can access but in reality the application access but in reality the application access but in reality the application only allocates memory like this at the only allocates memory like this at the only allocates memory like this at the beginning of the virtual memory there is beginning of the virtual memory there is beginning of the virtual memory there is the code the static variables and the the code the static variables and the the code the static variables and the heap and at the top of the virtual heap and at the top of the virtual heap and at the top of the virtual address space is the stack so we're address space is the stack so we're address space is the stack so we're really talking about really talking about really talking about two contiguous regions of virtual memory two contiguous regions of virtual memory two contiguous regions of virtual memory that are actually used by the that are actually used by the that are actually used by the application and even if these two are application and even if these two are application and even if these two are several gigabytes each in a 64-bit several gigabytes each in a 64-bit several gigabytes each in a 64-bit address space there will be many many address space there will be many many address space there will be many many terabytes of unused address space here terabytes of unused address space here terabytes of unused address space here but in a flat page table we had to have but in a flat page table we had to have but in a flat page table we had to have a page table entry for each page in this a page table entry for each page in this a page table entry for each page in this huge space here so a multi-level page huge space here so a multi-level page huge space here so a multi-level page table combines the idea of a flat page table combines the idea of a flat page table combines the idea of a flat page table where we use some bits from the table where we use some bits from the table where we use some bits from the virtual address to index the table this virtual address to index the table this virtual address to index the table this works really well in hardware hardware works really well in hardware hardware works really well in hardware hardware really likes using some bits to index really likes using some bits to index really likes using some bits to index some sort of an array but we want to some sort of an array but we want to some sort of an array but we want to avoid the use of table entries that avoid the use of table entries that avoid the use of table entries that correspond to this meaning we want to correspond to this meaning we want to correspond to this meaning we want to have a pretty normal page table like have a pretty normal page table like have a pretty normal page table like thing here and here but somehow not have thing here and here but somehow not have thing here and here but somehow not have anything for this so that we can simply anything for this so that we can simply anything for this so that we can simply omit those page table entries now that omit those page table entries now that omit those page table entries now that we have the idea for what we want let's we have the idea for what we want let's we have the idea for what we want let's see how the multi-level page table is see how the multi-level page table is see how the multi-level page table is actually organized

## Multi Level Page Table Structure： Part 4
Kind: captions Language: en so the multi-level page table still so the multi-level page table still so the multi-level page table still looks at the virtual address in terms of looks at the virtual address in terms of looks at the virtual address in terms of page offset and page number but instead page offset and page number but instead page offset and page number but instead of using the entire page number to of using the entire page number to of using the entire page number to Access one huge table it partitions the Access one huge table it partitions the Access one huge table it partitions the page number into the so-called inner page number into the so-called inner page number into the so-called inner page number and the outer page number page number and the outer page number page number and the outer page number the outer page number tells us which the outer page number tells us which the outer page number tells us which part of the large pay table we would be part of the large pay table we would be part of the large pay table we would be using the inner part tells us which using the inner part tells us which using the inner part tells us which specific entry in that large page table specific entry in that large page table specific entry in that large page table we would be using instead of having the we would be using instead of having the we would be using instead of having the large page table the outer page number large page table the outer page number large page table the outer page number is now used to index into the outer page is now used to index into the outer page is now used to index into the outer page table and each entry in the outer page table and each entry in the outer page table and each entry in the outer page table tells us where to find the table tells us where to find the table tells us where to find the so-called inner page table once we find so-called inner page table once we find so-called inner page table once we find the inner page table we take the inner the inner page table we take the inner the inner page table we take the inner page number and use it to index into the page number and use it to index into the page number and use it to index into the inner page table and what we find there inner page table and what we find there inner page table and what we find there is the actual frame number to AIS is the actual frame number to AIS is the actual frame number to AIS another entry in the outer page page another entry in the outer page page another entry in the outer page page table will point to a different inner table will point to a different inner table will point to a different inner page table so pretty much what we have page table so pretty much what we have page table so pretty much what we have is the outer page number tells us which is the outer page number tells us which is the outer page number tells us which of the small inner page tables to use of the small inner page tables to use of the small inner page tables to use and the inner page number tells us where and the inner page number tells us where and the inner page number tells us where in each of the page tables we need to in each of the page tables we need to in each of the page tables we need to look at so it seems like this is just a look at so it seems like this is just a look at so it seems like this is just a more complicated way to access the page more complicated way to access the page more complicated way to access the page table but still for every possible page table but still for every possible page table but still for every possible page number we will have a frame number in number we will have a frame number in number we will have a frame number in one of the inner page tables the total one of the inner page tables the total one of the inner page tables the total size of the inner page tables will be size of the inner page tables will be size of the inner page tables will be the size of the original flat page table the size of the original flat page table the size of the original flat page table and we still have added the outer page and we still have added the outer page and we still have added the outer page table which we didn't have before so table which we didn't have before so table which we didn't have before so pretty much we still have the same pretty much we still have the same pretty much we still have the same number of entries total here and we have number of entries total here and we have number of entries total here and we have added something so where are the savings

## Two Level Page Table Example： Part 4
Kind: captions Language: en to understand how the multi-level page to understand how the multi-level page to understand how the multi-level page table save space let's look at an table save space let's look at an table save space let's look at an example of a simple and small twole page example of a simple and small twole page example of a simple and small twole page table in this example we will use a very table in this example we will use a very table in this example we will use a very small virtual address where we have an small virtual address where we have an small virtual address where we have an offset and the page number and each of offset and the page number and each of offset and the page number and each of these is four bits so using a flat page these is four bits so using a flat page these is four bits so using a flat page table we would need 2 to the 4th entries table we would need 2 to the 4th entries table we would need 2 to the 4th entries in it one for each possible page number in it one for each possible page number in it one for each possible page number so the entries would be numbered 0 so the entries would be numbered 0 so the entries would be numbered 0 through 15 and we would index with our through 15 and we would index with our through 15 and we would index with our page number here and get our frame page number here and get our frame page number here and get our frame number with a two level page table let's number with a two level page table let's number with a two level page table let's say we split this 4bit page number into say we split this 4bit page number into say we split this 4bit page number into two 2bit pieces so the same virtual two 2bit pieces so the same virtual two 2bit pieces so the same virtual address still has a 4-bit page number address still has a 4-bit page number address still has a 4-bit page number that we split into two 2bit pieces the that we split into two 2bit pieces the that we split into two 2bit pieces the outer part of the page number is used to outer part of the page number is used to outer part of the page number is used to index into a four entry outer page table index into a four entry outer page table index into a four entry outer page table and each entry in this outer page table and each entry in this outer page table and each entry in this outer page table can point to a four entry inner page can point to a four entry inner page can point to a four entry inner page table so in total we have four inner table so in total we have four inner table so in total we have four inner page tables once we find the pointer to page tables once we find the pointer to page tables once we find the pointer to the correct one we can use the inner the correct one we can use the inner the correct one we can use the inner part of the page number to tell us which part of the page number to tell us which part of the page number to tell us which entry to use and that gives us our frame entry to use and that gives us our frame entry to use and that gives us our frame number so as indicated number so as indicated number so as indicated previously the total size of all the previously the total size of all the previously the total size of all the inner page tables here is the same as inner page tables here is the same as inner page tables here is the same as the original size of the flat page table the original size of the flat page table the original size of the flat page table here we had 16 entries here we have four here we had 16 entries here we have four here we had 16 entries here we have four * 4 entries here Plus we added the space * 4 entries here Plus we added the space * 4 entries here Plus we added the space for the outer page table so where are for the outer page table so where are for the outer page table so where are the savings the savings are in that if the savings the savings are in that if the savings the savings are in that if there is an unused large part of the there is an unused large part of the there is an unused large part of the address space such that all of these address space such that all of these address space such that all of these entries do not correspond to physical entries do not correspond to physical entries do not correspond to physical memory then we don't need this and we memory then we don't need this and we memory then we don't need this and we can Mark here in the outer page table can Mark here in the outer page table can Mark here in the outer page table that the entry is not necessary and then that the entry is not necessary and then that the entry is not necessary and then we can eliminate this inner page table we can eliminate this inner page table we can eliminate this inner page table in a large address space the outer page in a large address space the outer page in a large address space the outer page table table will have many pointers most table table will have many pointers most table table will have many pointers most of which will point to Inner page tables of which will point to Inner page tables of which will point to Inner page tables like this so we eliminate them so we end like this so we eliminate them so we end like this so we eliminate them so we end up having one reasonably large outer up having one reasonably large outer up having one reasonably large outer page table but one that we can fit in page table but one that we can fit in page table but one that we can fit in memory and a small number of inner page memory and a small number of inner page memory and a small number of inner page tables Each of which is reasonably sized tables Each of which is reasonably sized tables Each of which is reasonably sized and fits in memory but we don't have and fits in memory but we don't have and fits in memory but we don't have most of the inner page tables so we save most of the inner page tables so we save most of the inner page tables so we save a lot of space

## Two Level Page Table Size： Part 4
Kind: captions Language: en so now let's talk about how big the so now let's talk about how big the so now let's talk about how big the twole page table gets say we have a twole page table gets say we have a twole page table gets say we have a 32-bit address space and a 4 kiloby Page 32-bit address space and a 4 kiloby Page 32-bit address space and a 4 kiloby Page let's say we're using a 1024 entry outer let's say we're using a 1024 entry outer let's say we're using a 1024 entry outer page table and 1,24 entry inner tables page table and 1,24 entry inner tables page table and 1,24 entry inner tables there can be many of them up to 10,24 of there can be many of them up to 10,24 of there can be many of them up to 10,24 of them and let's say that a page table them and let's say that a page table them and let's say that a page table entry is 8 bytes assuming that our page entry is 8 bytes assuming that our page entry is 8 bytes assuming that our page table entry is 8 bytes and that we have table entry is 8 bytes and that we have table entry is 8 bytes and that we have a program that uses virtual memory at a program that uses virtual memory at a program that uses virtual memory at the beginning of memory from addresses the beginning of memory from addresses the beginning of memory from addresses zero through this and at the top of the zero through this and at the top of the zero through this and at the top of the memory from address this to the end memory from address this to the end memory from address this to the end let's look at what the flat page table let's look at what the flat page table let's look at what the flat page table size will be and what our two level page size will be and what our two level page size will be and what our two level page table size will be with this split table size will be with this split table size will be with this split between the inner and outer page tables between the inner and outer page tables between the inner and outer page tables the flat page table size will have one the flat page table size will have one the flat page table size will have one entry for each possible page so we have entry for each possible page so we have entry for each possible page so we have 2 to the 32nd Pages 2 to the 12 bytes in 2 to the 32nd Pages 2 to the 12 bytes in 2 to the 32nd Pages 2 to the 12 bytes in each page so we have 2 to the 20th each page so we have 2 to the 20th each page so we have 2 to the 20th entries and the total size will be 8 * entries and the total size will be 8 * entries and the total size will be 8 * this which ends up being 8 megabytes for this which ends up being 8 megabytes for this which ends up being 8 megabytes for the two- level page table we need to the two- level page table we need to the two- level page table we need to figure out how many inner page tables do figure out how many inner page tables do figure out how many inner page tables do we have here and here our address is now we have here and here our address is now we have here and here our address is now composed of a 12bit offset to index into composed of a 12bit offset to index into composed of a 12bit offset to index into the 4 kilobyte page a 10 bit outer page the 4 kilobyte page a 10 bit outer page the 4 kilobyte page a 10 bit outer page number and the 10 bit inner page number number and the 10 bit inner page number number and the 10 bit inner page number they need to add up to 3 two which they they need to add up to 3 two which they they need to add up to 3 two which they do so we know that we need to have the do so we know that we need to have the do so we know that we need to have the outer page table and its size will be 2 outer page table and its size will be 2 outer page table and its size will be 2 to the 10th entries times 8 bytes per to the 10th entries times 8 bytes per to the 10th entries times 8 bytes per entry here which ends up being 8 entry here which ends up being 8 entry here which ends up being 8 kilobytes for the outer page table now kilobytes for the outer page table now kilobytes for the outer page table now the question is which of these entries the question is which of these entries the question is which of these entries need to point to actual inner page need to point to actual inner page need to point to actual inner page tables and which ones can simply say tables and which ones can simply say tables and which ones can simply say that there is no inner page table to that there is no inner page table to that there is no inner page table to figure that out we need to see which of figure that out we need to see which of figure that out we need to see which of the inner page table entries do we need the inner page table entries do we need the inner page table entries do we need to actually point to pages of memory and to actually point to pages of memory and to actually point to pages of memory and which ones can simply be empty thus we which ones can simply be empty thus we which ones can simply be empty thus we will decompose these addresses this part will decompose these addresses this part will decompose these addresses this part of the address is the page offset the of the address is the page offset the of the address is the page offset the upper part is the page number and we upper part is the page number and we upper part is the page number and we need to split this 20 bit page number need to split this 20 bit page number need to split this 20 bit page number into 10bit groups so let's just write it into 10bit groups so let's just write it into 10bit groups so let's just write it out we have 0 0 0 0 these are the two out we have 0 0 0 0 these are the two out we have 0 0 0 0 these are the two zeros another hex digit for Z zeros another hex digit for Z zeros another hex digit for Z the one and the zero this is the outer the one and the zero this is the outer the one and the zero this is the outer page number this is the inner page page number this is the inner page page number this is the inner page number as you can see all of the number as you can see all of the number as you can see all of the addresses in this range have the same addresses in this range have the same addresses in this range have the same outer page number we start at zero we outer page number we start at zero we outer page number we start at zero we end at zero so only the entry number end at zero so only the entry number end at zero so only the entry number zero here needs to point to an actual zero here needs to point to an actual zero here needs to point to an actual inner page table for this address range inner page table for this address range inner page table for this address range the page number goes from the page number goes from the page number goes from F0 to fffff F these page number numbers F0 to fffff F these page number numbers F0 to fffff F these page number numbers also have the same outer page number also have the same outer page number also have the same outer page number it's all ones so only the last outer it's all ones so only the last outer it's all ones so only the last outer page table entry needs to point to an page table entry needs to point to an page table entry needs to point to an actual inner page table note that there actual inner page table note that there actual inner page table note that there are 1,24 entries here only two of which are 1,24 entries here only two of which are 1,24 entries here only two of which point to Inner page tables so there is point to Inner page tables so there is point to Inner page tables so there is 1,22 entries that simply say nothing so 1,22 entries that simply say nothing so 1,22 entries that simply say nothing so we have two inner page table times we have two inner page table times we have two inner page table times what's the size of one of them well the what's the size of one of them well the what's the size of one of them well the size is 1,24 entries times 8 bytes per size is 1,24 entries times 8 bytes per size is 1,24 entries times 8 bytes per entry which gives us a total of 16 entry which gives us a total of 16 entry which gives us a total of 16 kilobytes adding the size of the outer kilobytes adding the size of the outer kilobytes adding the size of the outer page table we get only 24 kiloby so the page table we get only 24 kiloby so the page table we get only 24 kiloby so the size of a twole page table for this size of a twole page table for this size of a twole page table for this application is only 24 kiloby compared application is only 24 kiloby compared application is only 24 kiloby compared to 8 megabytes for the flat page table to 8 megabytes for the flat page table to 8 megabytes for the flat page table and this is why multi-level page tables and this is why multi-level page tables and this is why multi-level page tables are almost exclusively used in today's are almost exclusively used in today's are almost exclusively used in today's processors especially when we have a processors especially when we have a processors especially when we have a 64-bit address space because for a 64 64-bit address space because for a 64 64-bit address space because for a 64 address space the flat page table size address space the flat page table size address space the flat page table size would be too large to fit in memory but would be too large to fit in memory but would be too large to fit in memory but a multi-level table will fit mostly a multi-level table will fit mostly a multi-level table will fit mostly because the outermost page table will because the outermost page table will because the outermost page table will have many empty entries so we will need have many empty entries so we will need have many empty entries so we will need very few of these inner page tables

## 4 Level PT Quiz： Part 4
Kind: captions Language: en so now that we have seen a two level so now that we have seen a two level so now that we have seen a two level page table let's see if we can work with page table let's see if we can work with page table let's see if we can work with a four level page table a four level page table a four level page table this time let's assume we have a 64-bit this time let's assume we have a 64-bit this time let's assume we have a 64-bit virtual address space a 64 kilobyte page virtual address space a 64 kilobyte page virtual address space a 64 kilobyte page size size size an 8 byte page table entry and we have a an 8 byte page table entry and we have a an 8 byte page table entry and we have a relatively large program that is only relatively large program that is only relatively large program that is only using addresses zero through four using addresses zero through four using addresses zero through four gigabytes from this huge address space gigabytes from this huge address space gigabytes from this huge address space the flat page table size is very large the flat page table size is very large the flat page table size is very large the question for you is it's a power of the question for you is it's a power of the question for you is it's a power of two what power of two so it's 2 to what two what power of two so it's 2 to what two what power of two so it's 2 to what and if you have a four level page table and if you have a four level page table and if you have a four level page table where the page number is split equally where the page number is split equally where the page number is split equally so the bits of the page number are so the bits of the page number are so the bits of the page number are divided into four equal groups to access divided into four equal groups to access divided into four equal groups to access these page tables this page table uses these page tables this page table uses these page tables this page table uses how many kilobytes of memory

## 4 Level PT Solution Quiz： Part 4
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our four- level page table quiz we have 2 to four- level page table quiz we have 2 to four- level page table quiz we have 2 to the 64th bytes of address space we have the 64th bytes of address space we have the 64th bytes of address space we have 2 to the 16th bytes in each page we get 2 to the 16th bytes in each page we get 2 to the 16th bytes in each page we get that there are 2 to the that there are 2 to the that there are 2 to the 48th pages in our address space for a 48th pages in our address space for a 48th pages in our address space for a flat page table the page table has 2 to flat page table the page table has 2 to flat page table the page table has 2 to the 48 entries times the size of each the 48 entries times the size of each the 48 entries times the size of each entry is 2 to the 3r so the overall size entry is 2 to the 3r so the overall size entry is 2 to the 3r so the overall size is two to the 51st bytes so the answer is two to the 51st bytes so the answer is two to the 51st bytes so the answer is here 51 this is an enormous page is here 51 this is an enormous page is here 51 this is an enormous page table many many terabytes of page table table many many terabytes of page table table many many terabytes of page table now let's see what the four level page now let's see what the four level page now let's see what the four level page table looks like the page number in this table looks like the page number in this table looks like the page number in this page table is split across four levels page table is split across four levels page table is split across four levels so the page number is split into four so the page number is split into four so the page number is split into four pieces of 12 bits the easiest way to pieces of 12 bits the easiest way to pieces of 12 bits the easiest way to figure out how large the overall page figure out how large the overall page figure out how large the overall page table is is to figure out how many table is is to figure out how many table is is to figure out how many innermost page tables we have 4 GB worth innermost page tables we have 4 GB worth innermost page tables we have 4 GB worth of memory is 2 to the 32nd bytes which of memory is 2 to the 32nd bytes which of memory is 2 to the 32nd bytes which is 2 to the 16th pages of this size so is 2 to the 16th pages of this size so is 2 to the 16th pages of this size so we have two to the 16th pages one we have two to the 16th pages one we have two to the 16th pages one innermost page table has two to the 12 innermost page table has two to the 12 innermost page table has two to the 12 entries so it covers 2 to the 12th pages entries so it covers 2 to the 12th pages entries so it covers 2 to the 12th pages to cover 2 to the 16th consecutive Pages to cover 2 to the 16th consecutive Pages to cover 2 to the 16th consecutive Pages we need 2 to the 4th of these innermost we need 2 to the 4th of these innermost we need 2 to the 4th of these innermost page tables so now we know that we have page tables so now we know that we have page tables so now we know that we have innermost page tables how many page innermost page tables how many page innermost page tables how many page tables at the next level we need to tables at the next level we need to tables at the next level we need to point to them well each table at that point to them well each table at that point to them well each table at that entry can also have 2 to the 12th entry can also have 2 to the 12th entry can also have 2 to the 12th entries and this is only 2 to the 4th so entries and this is only 2 to the 4th so entries and this is only 2 to the 4th so we need only one so this is the page we need only one so this is the page we need only one so this is the page table situation we have the outermost table situation we have the outermost table situation we have the outermost page table has 2 to the 12th or 496 page table has 2 to the 12th or 496 page table has 2 to the 12th or 496 entries but only one of them the first entries but only one of them the first entries but only one of them the first one actually has a a page table at the one actually has a a page table at the one actually has a a page table at the next level that page table at the next next level that page table at the next next level that page table at the next level also has two to the 12 entries but level also has two to the 12 entries but level also has two to the 12 entries but only one of them the first one points to only one of them the first one points to only one of them the first one points to the next level of page table that next the next level of page table that next the next level of page table that next level page table has 16 entries that level page table has 16 entries that level page table has 16 entries that point to innermost page tables so point to innermost page tables so point to innermost page tables so overall the number of these page tables overall the number of these page tables overall the number of these page tables is one here one here one here and 16 of is one here one here one here and 16 of is one here one here one here and 16 of these for a total of 19 of them the size these for a total of 19 of them the size these for a total of 19 of them the size of each of these small page tables is of each of these small page tables is of each of these small page tables is the number of entries 2 to the 12th the number of entries 2 to the 12th the number of entries 2 to the 12th times the size of an entry 2 to the3 so times the size of an entry 2 to the3 so times the size of an entry 2 to the3 so the size of each one of these is 2 to the size of each one of these is 2 to the size of each one of these is 2 to the 15th bytes which corresponds to 32 the 15th bytes which corresponds to 32 the 15th bytes which corresponds to 32 kiloby so overall we have 19 page tables kiloby so overall we have 19 page tables kiloby so overall we have 19 page tables at various levels Each of which is 32 at various levels Each of which is 32 at various levels Each of which is 32 Koby so the solution here is 19 * 32 so Koby so the solution here is 19 * 32 so Koby so the solution here is 19 * 32 so the final answer is 68 kiloby as you can the final answer is 68 kiloby as you can the final answer is 68 kiloby as you can see this is only 68 kiloby less than a see this is only 68 kiloby less than a see this is only 68 kiloby less than a megabyte compare that to many many megabyte compare that to many many megabyte compare that to many many terabytes of page table for a flat page terabytes of page table for a flat page terabytes of page table for a flat page table organization this is why all 64bit table organization this is why all 64bit table organization this is why all 64bit x86 processors nowadays use multi-level x86 processors nowadays use multi-level x86 processors nowadays use multi-level page tables of at least three page tables of at least three page tables of at least three levels

## Choosing The Page Size： Part 4
Kind: captions Language: en Yesterday morning's soldiers on Examples are Yesterday morning's soldiers on Examples are Yesterday morning's soldiers on Examples are used for boys and demonstrations were used for boys and demonstrations were used for boys and demonstrations were happy with hard 2302 Bichhu subscribe happy with hard 2302 Bichhu subscribe happy with hard 2302 Bichhu subscribe support channel Professor Shonku Rahasya pages support channel Professor Shonku Rahasya pages support channel Professor Shonku Rahasya pages ₹2 lakh pages Big pages subscribe and subscribe subscribe subscribe Seems Like a Path Which Can Give Seems Like a Path Which Can Give Seems Like a Path Which Can Give application Do application Do application Do n't forget to subscribe and subscribe Bluetooth Bluetooth Bluetooth pages Vaidya pages Vaidya pages Vaidya subscribe The Amazing subscribe The Amazing subscribe The Amazing in it's going to do subscribe my channel channel channel subscribe subscribe subscribe The Channel Ko subscribe kare subscribe 12345 Mudda ne Bigg Boss wapsi le mode ko switch 12345 Mudda ne Bigg Boss wapsi le mode ko switch 12345 Mudda ne Bigg Boss wapsi le mode ko switch press the subscribe button on the west side

## Memory Access Time With V-P Translation： Part 4
Kind: captions Language: en So now that we have seen how paging and So now that we have seen how paging and So now that we have seen how paging and virtual to physical translation works, virtual to physical translation works, virtual to physical translation works, let's talk about the memory access time let's talk about the memory access time let's talk about the memory access time when we have this translation. So our when we have this translation. So our when we have this translation. So our program does something like this. It program does something like this. It program does something like this. It says load R1 from the address computed says load R1 from the address computed says load R1 from the address computed by adding four to what is in R2. This by adding four to what is in R2. This by adding four to what is in R2. This when computed will be the virtual when computed will be the virtual when computed will be the virtual address that we want to access in the address that we want to access in the address that we want to access in the program. So to do a load or restore, the program. So to do a load or restore, the program. So to do a load or restore, the processor has to compute the virtual processor has to compute the virtual processor has to compute the virtual address. This is quick. Usually it's address. This is quick. Usually it's address. This is quick. Usually it's just addition of two numbers that are just addition of two numbers that are just addition of two numbers that are already in the processor. One is a already in the processor. One is a already in the processor. One is a constant fetch with the instruction. The constant fetch with the instruction. The constant fetch with the instruction. The other one is the register that is inside other one is the register that is inside other one is the register that is inside the processor. Next, the processor has the processor. Next, the processor has the processor. Next, the processor has to compute the page number which is to compute the page number which is to compute the page number which is extremely quick because it just means extremely quick because it just means extremely quick because it just means that we take some bits from the virtual that we take some bits from the virtual that we take some bits from the virtual address and without any address address and without any address address and without any address translation. The next thing that the translation. The next thing that the translation. The next thing that the processor will do is access the cache processor will do is access the cache processor will do is access the cache and sometimes if there is a cache miss and sometimes if there is a cache miss and sometimes if there is a cache miss the actual memory. So again these two the actual memory. So again these two the actual memory. So again these two are extremely quick. Most of the time are extremely quick. Most of the time are extremely quick. Most of the time will be spent accessing the cache and will be spent accessing the cache and will be spent accessing the cache and occasionally we will spend more cycles occasionally we will spend more cycles occasionally we will spend more cycles because we need to access the memory because we need to access the memory because we need to access the memory with virtual to physical translation. with virtual to physical translation. with virtual to physical translation. After computing the page number the After computing the page number the After computing the page number the processor has to compute the physical processor has to compute the physical processor has to compute the physical address of the page table entry where address of the page table entry where address of the page table entry where the translation for the page number is. the translation for the page number is. the translation for the page number is. This is done by adding the page number This is done by adding the page number This is done by adding the page number that we computed to the beginning that we computed to the beginning that we computed to the beginning address of the page table. Then we read address of the page table. Then we read address of the page table. Then we read the actual page table entry and compute the actual page table entry and compute the actual page table entry and compute the physical address by combining the the physical address by combining the the physical address by combining the physical frame number from the page physical frame number from the page physical frame number from the page table entry with the page offset from table entry with the page offset from table entry with the page offset from the virtual address. So how fast is the virtual address. So how fast is the virtual address. So how fast is this? Well, this is fast because it just this? Well, this is fast because it just this? Well, this is fast because it just means adding the page number to some means adding the page number to some means adding the page number to some sort of address. This is fast because it sort of address. This is fast because it sort of address. This is fast because it only means combining two numbers that we only means combining two numbers that we only means combining two numbers that we already have. But how fast is this? And already have. But how fast is this? And already have. But how fast is this? And for that we need to ask ourselves so for that we need to ask ourselves so for that we need to ask ourselves so where is the page table? Is it inside where is the page table? Is it inside where is the page table? Is it inside the processor so it can be quickly the processor so it can be quickly the processor so it can be quickly accessed or is it in memory? We have accessed or is it in memory? We have accessed or is it in memory? We have seen that the page table can be fairly seen that the page table can be fairly seen that the page table can be fairly large. Yes, for a multi-level page table large. Yes, for a multi-level page table large. Yes, for a multi-level page table we can have it be less than a megabyte we can have it be less than a megabyte we can have it be less than a megabyte for some programs but in theory it can for some programs but in theory it can for some programs but in theory it can be extremely large. So we cannot be extremely large. So we cannot be extremely large. So we cannot guarantee that the page table for even guarantee that the page table for even guarantee that the page table for even one process fits on the processor chip one process fits on the processor chip one process fits on the processor chip which means that the page tables really which means that the page tables really which means that the page tables really need to be in memory. And that means need to be in memory. And that means need to be in memory. And that means that for each load or store, we now need that for each load or store, we now need that for each load or store, we now need to have a memory access, which is just to have a memory access, which is just to have a memory access, which is just as slow as when we had a cache miss that as slow as when we had a cache miss that as slow as when we had a cache miss that we were trying to avoid before we got to we were trying to avoid before we got to we were trying to avoid before we got to page tables. Things are even worse page tables. Things are even worse page tables. Things are even worse because for a multi-level page table, because for a multi-level page table, because for a multi-level page table, for example, a four-level page table, we for example, a four-level page table, we for example, a four-level page table, we would need to do this multiple times. would need to do this multiple times. would need to do this multiple times. For example, four times. Compute the For example, four times. Compute the For example, four times. Compute the physical address of the outermost page physical address of the outermost page physical address of the outermost page table. Read that entry. Compute the table. Read that entry. Compute the table. Read that entry. Compute the physical address of the next page table. physical address of the next page table. physical address of the next page table. Go ahead and do that four times. So we Go ahead and do that four times. So we Go ahead and do that four times. So we need four memory accesses to read the need four memory accesses to read the need four memory accesses to read the four page table entries until we get the four page table entries until we get the four page table entries until we get the actual translation after which we can actual translation after which we can actual translation after which we can quickly access the cache. So somehow we quickly access the cache. So somehow we quickly access the cache. So somehow we have a situation where the virtual to have a situation where the virtual to have a situation where the virtual to physical address translation is costing physical address translation is costing physical address translation is costing us more than the memory accesses that we us more than the memory accesses that we us more than the memory accesses that we try to avoid by having the caches.

## V to P Translation Quiz： Part 4
Kind: captions Language: en So to see the impact of virtual to So to see the impact of virtual to So to see the impact of virtual to physical translation on performance, physical translation on performance, physical translation on performance, let's do a quiz about that. In a let's do a quiz about that. In a let's do a quiz about that. In a processor that takes a single cycle to processor that takes a single cycle to processor that takes a single cycle to compute the virtual address for a load compute the virtual address for a load compute the virtual address for a load or store instruction needs one cycle to or store instruction needs one cycle to or store instruction needs one cycle to access the cache on a hit, takes 10 access the cache on a hit, takes 10 access the cache on a hit, takes 10 cycles to access memory. For example, cycles to access memory. For example, cycles to access memory. For example, when we have a cache miss has a 90% hit when we have a cache miss has a 90% hit when we have a cache miss has a 90% hit rate for data that it is trying to rate for data that it is trying to rate for data that it is trying to access and the page table entries that access and the page table entries that access and the page table entries that we need to access cannot be in the we need to access cannot be in the we need to access cannot be in the cache. So we need to access memory cache. So we need to access memory cache. So we need to access memory directly for them. So how many cycles do directly for them. So how many cycles do directly for them. So how many cycles do we need for a load instruction such as we need for a load instruction such as we need for a load instruction such as this?

## V P Translation Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our virtual to physical translation speed virtual to physical translation speed virtual to physical translation speed quiz we were asked how many cycles do we quiz we were asked how many cycles do we quiz we were asked how many cycles do we need for a load instruction if we use a need for a load instruction if we use a need for a load instruction if we use a three level page table we need one cycle three level page table we need one cycle three level page table we need one cycle to compute the virtual address we need to compute the virtual address we need to compute the virtual address we need ten cycles to access the outermost page ten cycles to access the outermost page ten cycles to access the outermost page table 10 cycles flux is the next level table 10 cycles flux is the next level table 10 cycles flux is the next level of page table 10 cycles talks is the of page table 10 cycles talks is the of page table 10 cycles talks is the innermost page table because neither of innermost page table because neither of innermost page table because neither of those can be cached plus we need to do those can be cached plus we need to do those can be cached plus we need to do the actual cache access and maybe the the actual cache access and maybe the the actual cache access and maybe the memory so overall we need one plus three memory so overall we need one plus three memory so overall we need one plus three levels of page tables times memory levels of page tables times memory levels of page tables times memory latency plus one cycle to have a hit in latency plus one cycle to have a hit in latency plus one cycle to have a hit in our cache plus ten percent of the time our cache plus ten percent of the time our cache plus ten percent of the time we have a miss penalty to access the we have a miss penalty to access the we have a miss penalty to access the memory this ends up being 33 cycles memory this ends up being 33 cycles memory this ends up being 33 cycles thirty of which are to access the page thirty of which are to access the page thirty of which are to access the page tables

## V to P Translation Quiz 2： Part 4
Kind: captions Language: en let us repeat the previous quiz accepted let us repeat the previous quiz accepted let us repeat the previous quiz accepted now the page table entries can be cashed now the page table entries can be cashed now the page table entries can be cashed just like data and have the same hit just like data and have the same hit just like data and have the same hit rate like data

## V P Translation Solution 2 Quiz： Part 4
Kind: captions Language: en let's look at the solution to our latest let's look at the solution to our latest let's look at the solution to our latest virtual to physical translation quiz virtual to physical translation quiz virtual to physical translation quiz where we had the memory access times and where we had the memory access times and where we had the memory access times and the hit rates for the cache and we were the hit rates for the cache and we were the hit rates for the cache and we were assuming that the three level page table assuming that the three level page table assuming that the three level page table now can be cached just like data with now can be cached just like data with now can be cached just like data with the same hit rate the same hit rate the same hit rate now we have one cycle to compute the now we have one cycle to compute the now we have one cycle to compute the virtual address virtual address virtual address followed by for each level of the page followed by for each level of the page followed by for each level of the page table table table three times we need to do three times we need to do three times we need to do one cycle to access the cache one cycle to access the cache one cycle to access the cache plus plus plus 10 of the time for each of those 10 of the time for each of those 10 of the time for each of those we will have a miss and go to memory to we will have a miss and go to memory to we will have a miss and go to memory to fetch the page table entry fetch the page table entry fetch the page table entry and then after we do that and then after we do that and then after we do that we can access the data we can access the data we can access the data which takes one cycle plus which takes one cycle plus which takes one cycle plus ten percent of the time it takes ten ten percent of the time it takes ten ten percent of the time it takes ten more cycles to access memory more cycles to access memory more cycles to access memory so now we have so now we have so now we have nine cycles all together nine cycles all together nine cycles all together six of which six of which six of which are used are used are used by our virtual to physical translation by our virtual to physical translation by our virtual to physical translation and only three and only three and only three for for for the instruction itself and the memory the instruction itself and the memory the instruction itself and the memory access access access including the misses so we can see that including the misses so we can see that including the misses so we can see that the virtual to physical translation is the virtual to physical translation is the virtual to physical translation is expensive even if we allow page table expensive even if we allow page table expensive even if we allow page table entries to be treated as data and cached

## Translation Look Aside Buffer (TLB)： Part 4
Kind: captions Language: en so to speed up virtual to physical so to speed up virtual to physical so to speed up virtual to physical translation the processor includes a translation the processor includes a translation the processor includes a structure called the translation structure called the translation structure called the translation lookaside buffer or TLB the TLB is a lookaside buffer or TLB the TLB is a lookaside buffer or TLB the TLB is a cache for translations so now you would cache for translations so now you would cache for translations so now you would ask yourself well how is the TLB better ask yourself well how is the TLB better ask yourself well how is the TLB better than just using the cache itself well than just using the cache itself well than just using the cache itself well first the cache is relatively big it first the cache is relatively big it first the cache is relatively big it could store a lot of translations if we could store a lot of translations if we could store a lot of translations if we only held translations there but we only held translations there but we only held translations there but we don't most of the stuff in the cache is don't most of the stuff in the cache is don't most of the stuff in the cache is data one translation in the cache covers data one translation in the cache covers data one translation in the cache covers a whole page worth of data so the TLB a whole page worth of data so the TLB a whole page worth of data so the TLB that cache is only translations can be that cache is only translations can be that cache is only translations can be extremely small which means extremely extremely small which means extremely extremely small which means extremely fast let's look for example at a program fast let's look for example at a program fast let's look for example at a program that accesses 16 kilobytes of data for that accesses 16 kilobytes of data for that accesses 16 kilobytes of data for that to work well we need about 16 that to work well we need about 16 that to work well we need about 16 kilobytes of cache but this is only 4 kilobytes of cache but this is only 4 kilobytes of cache but this is only 4 pages if we have a 4 kilobyte page so pages if we have a 4 kilobyte page so pages if we have a 4 kilobyte page so the TLB that corresponds to that only the TLB that corresponds to that only the TLB that corresponds to that only needs four entries to cover the same needs four entries to cover the same needs four entries to cover the same amount of memory and with only 4 entries amount of memory and with only 4 entries amount of memory and with only 4 entries we can make the deal be extremely we can make the deal be extremely we can make the deal be extremely extremely fast much less than a cycle extremely fast much less than a cycle extremely fast much less than a cycle for each translation next we have seen for each translation next we have seen for each translation next we have seen that in a multi-level page table we that in a multi-level page table we that in a multi-level page table we access our cache for each level of the access our cache for each level of the access our cache for each level of the page table because we need to look up page table because we need to look up page table because we need to look up all those entries in order to find the all those entries in order to find the all those entries in order to find the final translation in contrast till the final translation in contrast till the final translation in contrast till the only stores the final translation even only stores the final translation even only stores the final translation even if we have a 4 level page table if we have a 4 level page table if we have a 4 level page table eventually we find what a given page eventually we find what a given page eventually we find what a given page number Maps do and we just store the number Maps do and we just store the number Maps do and we just store the frame number in the TLB not the frame number in the TLB not the frame number in the TLB not the intermediate page table entries that we intermediate page table entries that we intermediate page table entries that we have been using to finally come up with have been using to finally come up with have been using to finally come up with the translation so in a cache we'll have the translation so in a cache we'll have the translation so in a cache we'll have for a 4 level page table for accesses to for a 4 level page table for accesses to for a 4 level page table for accesses to do the translation in a TLB we do this do the translation in a TLB we do this do the translation in a TLB we do this in one axis so now a load or a store in one axis so now a load or a store in one axis so now a load or a store just needs to form the address access just needs to form the address access just needs to form the address access the TLB to find the translation and then the TLB to find the translation and then the TLB to find the translation and then access the cache to get the data if we access the cache to get the data if we access the cache to get the data if we have both the TLB hit and the cache hit have both the TLB hit and the cache hit have both the TLB hit and the cache hit this can be done in one or two cycles this can be done in one or two cycles this can be done in one or two cycles but what if we have a TLB miss what if but what if we have a TLB miss what if but what if we have a TLB miss what if we do not find a translation in the TLB we do not find a translation in the TLB we do not find a translation in the TLB in that case we need to perform the in that case we need to perform the in that case we need to perform the translation using the page table or page translation using the page table or page translation using the page table or page tables if we have a multiple level page tables if we have a multiple level page tables if we have a multiple level page table and then put that translation in table and then put that translation in table and then put that translation in our TLB so that it can be used later our TLB so that it can be used later our TLB so that it can be used later reiax is the same page again

## What If We Have A TLB Miss Quiz： Part 4
Kind: captions Language: en but who's gonna do these two things but who's gonna do these two things but who's gonna do these two things should it be the operating system so should it be the operating system so should it be the operating system so when we have a TLB miss we go back into when we have a TLB miss we go back into when we have a TLB miss we go back into the operating system and let it figure the operating system and let it figure the operating system and let it figure out what the page tables have using some out what the page tables have using some out what the page tables have using some sort of software or should the processor sort of software or should the processor sort of software or should the processor automatically read the page table or automatically read the page table or automatically read the page table or page tables and then update the TLB page tables and then update the TLB page tables and then update the TLB without any operating system without any operating system without any operating system intervention which one of these two do intervention which one of these two do intervention which one of these two do you think is correct

## What If We Have A TLB Miss Quiz Solution： Part 4
Kind: captions Language: en to answer a question both are okay to answer a question both are okay to answer a question both are okay options this option is called software options this option is called software options this option is called software TLB miss handling and it has the TLB miss handling and it has the TLB miss handling and it has the advantage of letting the operating advantage of letting the operating advantage of letting the operating system use any sort of page table that system use any sort of page table that system use any sort of page table that it wants because the hardware doesn't it wants because the hardware doesn't it wants because the hardware doesn't really need to access the page table the really need to access the page table the really need to access the page table the hardware simply has the TLB and the job hardware simply has the TLB and the job hardware simply has the TLB and the job of the operating system is to put the of the operating system is to put the of the operating system is to put the correct translations into the TLB but if correct translations into the TLB but if correct translations into the TLB but if you use any organization for the page you use any organization for the page you use any organization for the page table it wants so the operating system table it wants so the operating system table it wants so the operating system for example may not even have the page for example may not even have the page for example may not even have the page table in form of a table it can use a table in form of a table it can use a table in form of a table it can use a binary tree or a hash table or anything binary tree or a hash table or anything binary tree or a hash table or anything else it wants the second option where else it wants the second option where else it wants the second option where the processor automatically reads page the processor automatically reads page the processor automatically reads page tables and updates it will be is called tables and updates it will be is called tables and updates it will be is called hardware TLB miss handling in this case hardware TLB miss handling in this case hardware TLB miss handling in this case the page tables need to be in a form the page tables need to be in a form the page tables need to be in a form that is easily accessible by the that is easily accessible by the that is easily accessible by the hardware so we will have a flat or a hardware so we will have a flat or a hardware so we will have a flat or a multi-level page table like we have seen multi-level page table like we have seen multi-level page table like we have seen already in our previous videos this already in our previous videos this already in our previous videos this approach does require more Hardware approach does require more Hardware approach does require more Hardware because the hardware now needs to because the hardware now needs to because the hardware now needs to actually be able to access the page actually be able to access the page actually be able to access the page tables and sequence the accesses to the tables and sequence the accesses to the tables and sequence the accesses to the multiple levels and so on but it is multiple levels and so on but it is multiple levels and so on but it is faster than software handling this is faster than software handling this is faster than software handling this is treated kind of like a cache miss almost treated kind of like a cache miss almost treated kind of like a cache miss almost whereas here we need to execute a little whereas here we need to execute a little whereas here we need to execute a little program to fill the TLB so because this program to fill the TLB so because this program to fill the TLB so because this is faster and because hardware is cheap is faster and because hardware is cheap is faster and because hardware is cheap these days most high performance these days most high performance these days most high performance processors like x86 will use this processors like x86 will use this processors like x86 will use this approach of hardware TLB miss handling approach of hardware TLB miss handling approach of hardware TLB miss handling but some embedded processors will use but some embedded processors will use but some embedded processors will use the software TLB miss handling because the software TLB miss handling because the software TLB miss handling because they are concerned about the hardware they are concerned about the hardware they are concerned about the hardware cost so they want to be as simple as cost so they want to be as simple as cost so they want to be as simple as possible and also because in these possible and also because in these possible and also because in these embedded processors TLB misses occur embedded processors TLB misses occur embedded processors TLB misses occur less frequently because they are running less frequently because they are running less frequently because they are running more regular applications


# Playlist 2 Module 9 Raw Transcript

## Miss Rate Quiz： Part 3
Kind: captions Language: en let's also do a quiz about the Miss rate let's also do a quiz about the Miss rate let's also do a quiz about the Miss rate keep in mind that the hit rate is equal keep in mind that the hit rate is equal keep in mind that the hit rate is equal to 1 minus the Miss rate the Miss rate to 1 minus the Miss rate the Miss rate to 1 minus the Miss rate the Miss rate is how often do we have a cache miss the is how often do we have a cache miss the is how often do we have a cache miss the hit rate is how often do we have a cache hit rate is how often do we have a cache hit rate is how often do we have a cache hit and the two add up to 1 because we hit and the two add up to 1 because we hit and the two add up to 1 because we either have one or the other so which of either have one or the other so which of either have one or the other so which of the following are true for a the following are true for a the following are true for a well-designed cache is it true that in a well-designed cache is it true that in a well-designed cache is it true that in a well-designed cache the hit rate should well-designed cache the hit rate should well-designed cache the hit rate should be larger than the Miss rate the hit be larger than the Miss rate the hit be larger than the Miss rate the hit rate should be less than the Miss rate rate should be less than the Miss rate rate should be less than the Miss rate that the hit rate and the Miss rate that the hit rate and the Miss rate that the hit rate and the Miss rate should be balanced as close to equal as should be balanced as close to equal as should be balanced as close to equal as possible that the hit rate should be possible that the hit rate should be possible that the hit rate should be almost 1 that the Miss rate should be almost 1 that the Miss rate should be almost 1 that the Miss rate should be almost 1 again more than one possibly or almost 1 again more than one possibly or almost 1 again more than one possibly or possibly none of them might be true so possibly none of them might be true so possibly none of them might be true so check all of those that are true for a check all of those that are true for a check all of those that are true for a well-designed cache

## Miss Rate Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our miss let's look at the answer to our miss let's look at the answer to our miss rate quiz in a well-designed cache we rate quiz in a well-designed cache we rate quiz in a well-designed cache we want the hit rate to be as high as want the hit rate to be as high as want the hit rate to be as high as possible and the Miss rate to be as low possible and the Miss rate to be as low possible and the Miss rate to be as low as possible because the hit rate as possible because the hit rate as possible because the hit rate determines how often do we have just a determines how often do we have just a determines how often do we have just a hit latency and the Miss rate is how hit latency and the Miss rate is how hit latency and the Miss rate is how often we have to pay the full memory often we have to pay the full memory often we have to pay the full memory latency so we definitely want the hit latency so we definitely want the hit latency so we definitely want the hit rate to be larger than the Miss rate we rate to be larger than the Miss rate we rate to be larger than the Miss rate we definitely do not want the hitter to be definitely do not want the hitter to be definitely do not want the hitter to be smaller than the Miss rate we don't want smaller than the Miss rate we don't want smaller than the Miss rate we don't want them to be equal to each other because them to be equal to each other because them to be equal to each other because that means that half of the time we're that means that half of the time we're that means that half of the time we're going to memory we want to hit much more going to memory we want to hit much more going to memory we want to hit much more than that than that than that we want the hit rate to be almost 1 we want the hit rate to be almost 1 we want the hit rate to be almost 1 because that means that almost always we because that means that almost always we because that means that almost always we are going to have a cache hit and we are going to have a cache hit and we are going to have a cache hit and we definitely do not want the Miss rate to definitely do not want the Miss rate to definitely do not want the Miss rate to be almost 1 because that means that we be almost 1 because that means that we be almost 1 because that means that we are almost never hitting in our cache

## Cache Size in Real Processors： Part 3
Kind: captions Language: en so we have seen that we want the cash to so we have seen that we want the cash to so we have seen that we want the cash to be small and fast but not too small be small and fast but not too small be small and fast but not too small because then it keeps very little stuff because then it keeps very little stuff because then it keeps very little stuff so that it has very few hits so what are so that it has very few hits so what are so that it has very few hits so what are the cache sizes that we have actually the cache sizes that we have actually the cache sizes that we have actually observed in real processors there is a observed in real processors there is a observed in real processors there is a complication there in that modern complication there in that modern complication there in that modern processors usually have several caches processors usually have several caches processors usually have several caches not just one so depending on which cash not just one so depending on which cash not just one so depending on which cash are we talking about it might be larger are we talking about it might be larger are we talking about it might be larger or smaller or smaller or smaller so let's look at caches that we call l1 so let's look at caches that we call l1 so let's look at caches that we call l1 or level 1 caches which are the caches or level 1 caches which are the caches or level 1 caches which are the caches that directly serve is the read and that directly serve is the read and that directly serve is the read and write requests from the processor so write requests from the processor so write requests from the processor so when the processor wants to read a write when the processor wants to read a write when the processor wants to read a write it first goes to the l1 cache to check it first goes to the l1 cache to check it first goes to the l1 cache to check if it has a hit if the l1 cache has a if it has a hit if the l1 cache has a if it has a hit if the l1 cache has a mess then things get complicated because mess then things get complicated because mess then things get complicated because before we go to the main memory we go to before we go to the main memory we go to before we go to the main memory we go to other caches and in several lessons we other caches and in several lessons we other caches and in several lessons we will actually see how that works but for will actually see how that works but for will actually see how that works but for now let you see how big are these level now let you see how big are these level now let you see how big are these level 1 caches in recent processors typical 1 caches in recent processors typical 1 caches in recent processors typical sizes of the l1 cache have been 16 to 64 sizes of the l1 cache have been 16 to 64 sizes of the l1 cache have been 16 to 64 kilobytes this is large enough to get kilobytes this is large enough to get kilobytes this is large enough to get about 90% hit rate meaning only 1/10 of about 90% hit rate meaning only 1/10 of about 90% hit rate meaning only 1/10 of all the accesses from the processor end all the accesses from the processor end all the accesses from the processor end up going beyond this cache yet these are up going beyond this cache yet these are up going beyond this cache yet these are still small enough to have the hit time still small enough to have the hit time still small enough to have the hit time that is equal to 1 2 3 processor cycles that is equal to 1 2 3 processor cycles that is equal to 1 2 3 processor cycles so we spend very few processor cycles so we spend very few processor cycles so we spend very few processor cycles waiting for the data to come back from waiting for the data to come back from waiting for the data to come back from this cache if it's a hit this cache if it's a hit this cache if it's a hit recall that memories have hundreds of recall that memories have hundreds of recall that memories have hundreds of cycles so this is very very fast cycles so this is very very fast cycles so this is very very fast compared to the memory

## Cache Organization： Part 3
Kind: captions Language: en now that we know what the purpose of the now that we know what the purpose of the now that we know what the purpose of the cache is and how big approximately it cache is and how big approximately it cache is and how big approximately it needs to be let's look at how it is needs to be let's look at how it is needs to be let's look at how it is organized internally the questions we organized internally the questions we organized internally the questions we need to ask ourselves is how do we need to ask ourselves is how do we need to ask ourselves is how do we determine if we have a hit or miss so we determine if we have a hit or miss so we determine if we have a hit or miss so we need to know what data do we have in the need to know what data do we have in the need to know what data do we have in the cache and then when the processor gives cache and then when the processor gives cache and then when the processor gives us an address we need to determine us an address we need to determine us an address we need to determine whether that data is or is not in the whether that data is or is not in the whether that data is or is not in the cache and we need to do that very cache and we need to do that very cache and we need to do that very quickly and the other important question quickly and the other important question quickly and the other important question is how do we determine what to kick out is how do we determine what to kick out is how do we determine what to kick out from the cache once we are out of room from the cache once we are out of room from the cache once we are out of room in it and we need to put more stuff in in it and we need to put more stuff in in it and we need to put more stuff in it because we are just having a cache it because we are just having a cache it because we are just having a cache miss so for the first question of how do miss so for the first question of how do miss so for the first question of how do we know whether we have a kid or a Miss we know whether we have a kid or a Miss we know whether we have a kid or a Miss we want something that is very fast that we want something that is very fast that we want something that is very fast that means it needs to be a table of some means it needs to be a table of some means it needs to be a table of some sort that we can quickly index with some sort that we can quickly index with some sort that we can quickly index with some bits of the address and it tells us what bits of the address and it tells us what bits of the address and it tells us what we have in the cache and if we do have we have in the cache and if we do have we have in the cache and if we do have what we are looking for then it gives us what we are looking for then it gives us what we are looking for then it gives us the data so conceptually the cache is a the data so conceptually the cache is a the data so conceptually the cache is a table which we index with some bits that table which we index with some bits that table which we index with some bits that we take from the address of the data so we take from the address of the data so we take from the address of the data so some stuff here needs to tell us whether some stuff here needs to tell us whether some stuff here needs to tell us whether we have hit and the rest of the stuff we have hit and the rest of the stuff we have hit and the rest of the stuff here here here is the data we want if we do have the is the data we want if we do have the is the data we want if we do have the hit how much data we have in each cache hit how much data we have in each cache hit how much data we have in each cache entry is called the block size or the entry is called the block size or the entry is called the block size or the line size because the entry in the cache line size because the entry in the cache line size because the entry in the cache is also called a line and they tell us is also called a line and they tell us is also called a line and they tell us how many bytes are in each of these how many bytes are in each of these how many bytes are in each of these entries if we have only one byte per entries if we have only one byte per entries if we have only one byte per entry then every address will map to a entry then every address will map to a entry then every address will map to a different entry if we have a block size different entry if we have a block size different entry if we have a block size of 1 that means the entry in the cache of 1 that means the entry in the cache of 1 that means the entry in the cache is only one byte every single byte is only one byte every single byte is only one byte every single byte address will have a different entry that address will have a different entry that address will have a different entry that creates several problems one usually the creates several problems one usually the creates several problems one usually the processor can issue accesses not only to processor can issue accesses not only to processor can issue accesses not only to a single byte but also for example load a single byte but also for example load a single byte but also for example load word and store word will access a four word and store word will access a four word and store word will access a four byte location so for a single access now byte location so for a single access now byte location so for a single access now we would need to look up four different we would need to look up four different we would need to look up four different lines in the cache that complicates and lines in the cache that complicates and lines in the cache that complicates and slows down the cache a lot so we slows down the cache a lot so we slows down the cache a lot so we definitely don't want a block size to be definitely don't want a block size to be definitely don't want a block size to be this small we want the block size to be this small we want the block size to be this small we want the block size to be at least as large as the largest single at least as large as the largest single at least as large as the largest single axis that we can do in the cache and axis that we can do in the cache and axis that we can do in the cache and hopefully slightly larger than that just hopefully slightly larger than that just hopefully slightly larger than that just so so so that when we do a load word or store that when we do a load word or store that when we do a load word or store word we find the data in the same cache word we find the data in the same cache word we find the data in the same cache block most of the time the next thing block most of the time the next thing block most of the time the next thing that we want to consider when deciding that we want to consider when deciding that we want to consider when deciding the block size is spatial locality if we the block size is spatial locality if we the block size is spatial locality if we Mason the cache on one axis we will Mason the cache on one axis we will Mason the cache on one axis we will bring in an entire block worth of memory bring in an entire block worth of memory bring in an entire block worth of memory stuff into the cache so if there is no stuff into the cache so if there is no stuff into the cache so if there is no spatial locality we want to bring in spatial locality we want to bring in spatial locality we want to bring in just what we are currently accessing but just what we are currently accessing but just what we are currently accessing but if there is any spatial locality we want if there is any spatial locality we want if there is any spatial locality we want to bring more than what we are just to bring more than what we are just to bring more than what we are just accessing so typically block sizes of 32 accessing so typically block sizes of 32 accessing so typically block sizes of 32 to 128 bytes work well both from the to 128 bytes work well both from the to 128 bytes work well both from the perspective of they're larger than a perspective of they're larger than a perspective of they're larger than a typical access and also they capture typical access and also they capture typical access and also they capture much of the spatial locality that exists much of the spatial locality that exists much of the spatial locality that exists in programs now let's look at block in programs now let's look at block in programs now let's look at block sizes of something like one kilobyte in sizes of something like one kilobyte in sizes of something like one kilobyte in that case what happens is we fetch a lot that case what happens is we fetch a lot that case what happens is we fetch a lot of data from memory every time we have a of data from memory every time we have a of data from memory every time we have a Miss and if there is not enough locality Miss and if there is not enough locality Miss and if there is not enough locality a lot of the data will not be used so we a lot of the data will not be used so we a lot of the data will not be used so we are occupying space in our cache are occupying space in our cache are occupying space in our cache remember that the cache is only 16 to 64 remember that the cache is only 16 to 64 remember that the cache is only 16 to 64 of these kilobytes so if we bring in a of these kilobytes so if we bring in a of these kilobytes so if we bring in a block like this we are consuming a block like this we are consuming a block like this we are consuming a significant part of the overall space in significant part of the overall space in significant part of the overall space in the cache and we are doing it probably the cache and we are doing it probably the cache and we are doing it probably with data that we won't use much so it with data that we won't use much so it with data that we won't use much so it turns out that for level 1 cache is at turns out that for level 1 cache is at turns out that for level 1 cache is at least we don't want the block size to be least we don't want the block size to be least we don't want the block size to be too large so block sizes of 32 to 128 too large so block sizes of 32 to 128 too large so block sizes of 32 to 128 kilobytes are a balance between getting kilobytes are a balance between getting kilobytes are a balance between getting use of spatial locality when we bring use of spatial locality when we bring use of spatial locality when we bring stuff in versus not being dependent too stuff in versus not being dependent too stuff in versus not being dependent too much on there being a lot of spatial much on there being a lot of spatial much on there being a lot of spatial locality because some programs don't locality because some programs don't locality because some programs don't have that much of it

## Block Size Quiz： Part 3
Kind: captions Language: en so let's do a quiz on block sizes now so let's do a quiz on block sizes now so let's do a quiz on block sizes now consider a cache that is 32 kilobytes in consider a cache that is 32 kilobytes in consider a cache that is 32 kilobytes in size and has a 64 byte block size so size and has a 64 byte block size so size and has a 64 byte block size so let's consider a program that accesses let's consider a program that accesses let's consider a program that accesses variables X 1 X 2 etc up to some X N and variables X 1 X 2 etc up to some X N and variables X 1 X 2 etc up to some X N and these are scalar variables not part of these are scalar variables not part of these are scalar variables not part of an array and let's assume that the an array and let's assume that the an array and let's assume that the program has lots of temporal locality program has lots of temporal locality program has lots of temporal locality when accessing these variables but no when accessing these variables but no when accessing these variables but no spatial locality the question for you is spatial locality the question for you is spatial locality the question for you is what is the largest n the number of what is the largest n the number of what is the largest n the number of these variables that still results in a these variables that still results in a these variables that still results in a high hit rate in this cache put the high hit rate in this cache put the high hit rate in this cache put the number here

## Block Size Quiz Solution： Part 3
Kind: captions Language: en let's look at the solution to our block let's look at the solution to our block let's look at the solution to our block size quiz we have a 32 kilobyte cache size quiz we have a 32 kilobyte cache size quiz we have a 32 kilobyte cache with 64 byte block size and the program with 64 byte block size and the program with 64 byte block size and the program that accesses n variables that have lots that accesses n variables that have lots that accesses n variables that have lots of temporal locality meaning each of of temporal locality meaning each of of temporal locality meaning each of these variables is accessed often in the these variables is accessed often in the these variables is accessed often in the program but no spatial locality meaning program but no spatial locality meaning program but no spatial locality meaning they are nowhere close to each other the they are nowhere close to each other the they are nowhere close to each other the question is what is the largest n the question is what is the largest n the question is what is the largest n the number of these variables that still number of these variables that still number of these variables that still results in a high hit rate now x1 when results in a high hit rate now x1 when results in a high hit rate now x1 when we bring it into the cache we will bring we bring it into the cache we will bring we bring it into the cache we will bring an entire block worth of stuff around it an entire block worth of stuff around it an entire block worth of stuff around it and because there is no spatial locality and because there is no spatial locality and because there is no spatial locality that means that none of the other that means that none of the other that means that none of the other variables will be in that block when we variables will be in that block when we variables will be in that block when we access x2 same thing we will bring 64 access x2 same thing we will bring 64 access x2 same thing we will bring 64 bytes worth of stuff that doesn't bytes worth of stuff that doesn't bytes worth of stuff that doesn't include any of the other variables so include any of the other variables so include any of the other variables so every time we access one of these every time we access one of these every time we access one of these variables we will need 64 bytes worth of variables we will need 64 bytes worth of variables we will need 64 bytes worth of space in the cache if we want to have a space in the cache if we want to have a space in the cache if we want to have a high hit rate because these variables high hit rate because these variables high hit rate because these variables have lots of temporal locality we want have lots of temporal locality we want have lots of temporal locality we want all of them to remain in the cache that all of them to remain in the cache that all of them to remain in the cache that is we want all of them to fit in the is we want all of them to fit in the is we want all of them to fit in the cache so the largest n that still cache so the largest n that still cache so the largest n that still results in the high hit rate is how many results in the high hit rate is how many results in the high hit rate is how many blocks can we fit in your cache really blocks can we fit in your cache really blocks can we fit in your cache really it's a 32 kilobyte cache there are 64 it's a 32 kilobyte cache there are 64 it's a 32 kilobyte cache there are 64 byte blocks how many blocks do we have byte blocks how many blocks do we have byte blocks how many blocks do we have so the answer is 32 kilobytes divided by so the answer is 32 kilobytes divided by so the answer is 32 kilobytes divided by 64 bytes which gives us 512 so the 64 bytes which gives us 512 so the 64 bytes which gives us 512 so the answer here is 512 if we have 512 or answer here is 512 if we have 512 or answer here is 512 if we have 512 or fewer variables then the fact that each fewer variables then the fact that each fewer variables then the fact that each one of them has its own cache block in one of them has its own cache block in one of them has its own cache block in the cache although of the variables the cache although of the variables the cache although of the variables probably only 4 bytes in size is not probably only 4 bytes in size is not probably only 4 bytes in size is not gonna hurt us in terms of hit rate but gonna hurt us in terms of hit rate but gonna hurt us in terms of hit rate but if we have more than 512 variables then if we have more than 512 variables then if we have more than 512 variables then even though there is plenty of space in even though there is plenty of space in even though there is plenty of space in the cache if it's 4 byte variables 512 the cache if it's 4 byte variables 512 the cache if it's 4 byte variables 512 variables is 2 kilobytes worth of actual variables is 2 kilobytes worth of actual variables is 2 kilobytes worth of actual stuff that we are using the block is stuff that we are using the block is stuff that we are using the block is much larger than that but because we are much larger than that but because we are much larger than that but because we are fetching an entire block when we have a fetching an entire block when we have a fetching an entire block when we have a Miss we will not have a good hit rate if Miss we will not have a good hit rate if Miss we will not have a good hit rate if you have you have you have more than 512 of these variables in more than 512 of these variables in more than 512 of these variables in order to be able to have more variables order to be able to have more variables order to be able to have more variables than this we really need them to be than this we really need them to be than this we really need them to be close to each other so that we can close to each other so that we can close to each other so that we can benefit from spatial locality meaning benefit from spatial locality meaning benefit from spatial locality meaning that a block we fetch for one variable that a block we fetch for one variable that a block we fetch for one variable needs to include some of the other needs to include some of the other needs to include some of the other variables

## Cache Block Start Addr： Part 3
Kind: captions Language: en so now that we know that when we have a so now that we know that when we have a so now that we know that when we have a cache miss we fetch an entire block cache miss we fetch an entire block cache miss we fetch an entire block worth of stuff from memory let's talk worth of stuff from memory let's talk worth of stuff from memory let's talk about where can these blocks begin in about where can these blocks begin in about where can these blocks begin in memory one option is a block can begin memory one option is a block can begin memory one option is a block can begin anywhere so let's say we have 64 byte anywhere so let's say we have 64 byte anywhere so let's say we have 64 byte blocks that means that a block for blocks that means that a block for blocks that means that a block for example might go from address 0 through example might go from address 0 through example might go from address 0 through 63 bytes or it can be from 1 through 64 63 bytes or it can be from 1 through 64 63 bytes or it can be from 1 through 64 or it can go from 2 to 65 and so on so or it can go from 2 to 65 and so on so or it can go from 2 to 65 and so on so pretty much if the starting address of pretty much if the starting address of pretty much if the starting address of the block can be anywhere then these are the block can be anywhere then these are the block can be anywhere then these are the possible blocks that we might fetch the possible blocks that we might fetch the possible blocks that we might fetch in our cache this complicates things a in our cache this complicates things a in our cache this complicates things a lot first our cache is a table we want lot first our cache is a table we want lot first our cache is a table we want to index into the table using some bits to index into the table using some bits to index into the table using some bits of the address that we are accessing but of the address that we are accessing but of the address that we are accessing but let's say that what we're accessing is let's say that what we're accessing is let's say that what we're accessing is the address 27 the problem is the block the address 27 the problem is the block the address 27 the problem is the block can actually be found wherever the can actually be found wherever the can actually be found wherever the beginning of address of the block maps beginning of address of the block maps beginning of address of the block maps so block 0 to 63 might map here in the so block 0 to 63 might map here in the so block 0 to 63 might map here in the cache block 1 through 64 might map cache block 1 through 64 might map cache block 1 through 64 might map somewhere else in the cache and so on so somewhere else in the cache and so on so somewhere else in the cache and so on so basically there are many possible places basically there are many possible places basically there are many possible places where the address 27 might be simply where the address 27 might be simply where the address 27 might be simply because each of these blocks might map because each of these blocks might map because each of these blocks might map somewhere else depending on what its somewhere else depending on what its somewhere else depending on what its beginning address is second there is beginning address is second there is beginning address is second there is another complication which is these another complication which is these another complication which is these blocks overlap so the block 0 to 63 blocks overlap so the block 0 to 63 blocks overlap so the block 0 to 63 contains stuff that overlaps with most contains stuff that overlaps with most contains stuff that overlaps with most of what 1 through 64 contains so this of what 1 through 64 contains so this of what 1 through 64 contains so this other than this is the same stuff now other than this is the same stuff now other than this is the same stuff now the problem is when we are reading it's the problem is when we are reading it's the problem is when we are reading it's easy we just need to read one of these easy we just need to read one of these easy we just need to read one of these copies for 27 but when we are writing we copies for 27 but when we are writing we copies for 27 but when we are writing we need to determine which of the copies need to determine which of the copies need to determine which of the copies exist and write to all of them so that exist and write to all of them so that exist and write to all of them so that reading any of them will still be ok so reading any of them will still be ok so reading any of them will still be ok so pretty much we don't want this to happen pretty much we don't want this to happen pretty much we don't want this to happen we don't want to have multiple places we don't want to have multiple places we don't want to have multiple places where the same byte address might be so where the same byte address might be so where the same byte address might be so in order to both reduce the complexity in order to both reduce the complexity in order to both reduce the complexity of accessing the cache and eliminate the of accessing the cache and eliminate the of accessing the cache and eliminate the problem of this repetition of stuff in problem of this repetition of stuff in problem of this repetition of stuff in the cache we will only have caches where the cache we will only have caches where the cache we will only have caches where the blocks start at block aligned the blocks start at block aligned the blocks start at block aligned addresses so for a 64 byte block we will addresses so for a 64 byte block we will addresses so for a 64 byte block we will have a block have a block have a block that contains data from 0 to 63 bytes that contains data from 0 to 63 bytes that contains data from 0 to 63 bytes the next block begins at byte 64 and the next block begins at byte 64 and the next block begins at byte 64 and goes to 127 and so on so pretty much any goes to 127 and so on so pretty much any goes to 127 and so on so pretty much any byte address can be found in only one of byte address can be found in only one of byte address can be found in only one of the possible blocks that also allows us the possible blocks that also allows us the possible blocks that also allows us to simply use some bits of the address to simply use some bits of the address to simply use some bits of the address that tell us which of these blocks are that tell us which of these blocks are that tell us which of these blocks are we talking about an index into the cache we talking about an index into the cache we talking about an index into the cache and find it there so there is only one and find it there so there is only one and find it there so there is only one place in the cache where each of these place in the cache where each of these place in the cache where each of these will fit and so on so for all reasonable will fit and so on so for all reasonable will fit and so on so for all reasonable caches we will really only consider caches we will really only consider caches we will really only consider aligned blocks meaning you cannot fetch aligned blocks meaning you cannot fetch aligned blocks meaning you cannot fetch any 64 bytes or 32 bytes or whatever any 64 bytes or 32 bytes or whatever any 64 bytes or 32 bytes or whatever block sizes from memory into the cache block sizes from memory into the cache block sizes from memory into the cache we need to fetch the 64 byte block that we need to fetch the 64 byte block that we need to fetch the 64 byte block that contains the data and is block aligned

## Blocks in Cache and Memory： Part 3
Kind: captions Language: en so now that we know that cache blocks so now that we know that cache blocks so now that we know that cache blocks need to be at aligned memory addresses need to be at aligned memory addresses need to be at aligned memory addresses let's look at what the memory and cache let's look at what the memory and cache let's look at what the memory and cache look like in terms of blocks our memory look like in terms of blocks our memory look like in terms of blocks our memory can be seen as just a huge array of can be seen as just a huge array of can be seen as just a huge array of memory locations let's say this is memory locations let's say this is memory locations let's say this is location number 0 and let's say that location number 0 and let's say that location number 0 and let's say that each location is 4 bytes wide so the each location is 4 bytes wide so the each location is 4 bytes wide so the next location is at address 4 and then 8 next location is at address 4 and then 8 next location is at address 4 and then 8 and so on let's say that our block size and so on let's say that our block size and so on let's say that our block size is 16 bytes that means that a possible is 16 bytes that means that a possible is 16 bytes that means that a possible block in memory is this this would be block in memory is this this would be block in memory is this this would be the first block this would be the second the first block this would be the second the first block this would be the second block this would be the third block and block this would be the third block and block this would be the third block and so on a cache then can be considered to so on a cache then can be considered to so on a cache then can be considered to be a number of slots where a block can be a number of slots where a block can be a number of slots where a block can fit in this case we have just a two fit in this case we have just a two fit in this case we have just a two block cache so the cache size is just block cache so the cache size is just block cache so the cache size is just equal to two block sizes so our memory equal to two block sizes so our memory equal to two block sizes so our memory has blocks of data and their cache has has blocks of data and their cache has has blocks of data and their cache has lines which are basically slots where a lines which are basically slots where a lines which are basically slots where a block can fit so we can say for example block can fit so we can say for example block can fit so we can say for example that block number zero can be fetched that block number zero can be fetched that block number zero can be fetched into the cache and placed in line number into the cache and placed in line number into the cache and placed in line number zero so when we want to make a precise zero so when we want to make a precise zero so when we want to make a precise distinction between the space in the distinction between the space in the distinction between the space in the cache where we can put a block and the cache where we can put a block and the cache where we can put a block and the actual memory content we put there the actual memory content we put there the actual memory content we put there the space in the cache is called a line and space in the cache is called a line and space in the cache is called a line and the stuff we put there is called a block the stuff we put there is called a block the stuff we put there is called a block the line size and the block size are the the line size and the block size are the the line size and the block size are the same so a line is effectively a slot same so a line is effectively a slot same so a line is effectively a slot where a block might fit

## Cache Line Sizes Quiz： Part 3
Kind: captions Language: en so now that we have seen what the cache so now that we have seen what the cache so now that we have seen what the cache line is and what its size does let's do line is and what its size does let's do line is and what its size does let's do a quiz about that so which of these are a quiz about that so which of these are a quiz about that so which of these are not good line sizes in a two kilobyte not good line sizes in a two kilobyte not good line sizes in a two kilobyte cache one byte 32 bytes 48 bytes 64 cache one byte 32 bytes 48 bytes 64 cache one byte 32 bytes 48 bytes 64 bytes and one kilobyte again we're bytes and one kilobyte again we're bytes and one kilobyte again we're looking for those that are not good line looking for those that are not good line looking for those that are not good line sizes in a cache of two kilobyte size

## Cache Line Sizes Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer for our cache let's look at the answer for our cache let's look at the answer for our cache line size quiz we are asked to determine line size quiz we are asked to determine line size quiz we are asked to determine which of these are not good line sizes which of these are not good line sizes which of these are not good line sizes for a two kilobyte cache a one byte line for a two kilobyte cache a one byte line for a two kilobyte cache a one byte line is not a good one because it doesn't is not a good one because it doesn't is not a good one because it doesn't exploit spatial locality and because exploit spatial locality and because exploit spatial locality and because word size accesses we'll need to access word size accesses we'll need to access word size accesses we'll need to access multiple blocks or multiple lines in the multiple blocks or multiple lines in the multiple blocks or multiple lines in the cache so this is definitely not a good cache so this is definitely not a good cache so this is definitely not a good size 32 bytes is a good size it exploits size 32 bytes is a good size it exploits size 32 bytes is a good size it exploits spatial locality it's not too large and spatial locality it's not too large and spatial locality it's not too large and it's a power of two so we can easily it's a power of two so we can easily it's a power of two so we can easily locate blocks if I give you an address locate blocks if I give you an address locate blocks if I give you an address you can easily find what block does it you can easily find what block does it you can easily find what block does it belong to 48 bytes is a good size as far belong to 48 bytes is a good size as far belong to 48 bytes is a good size as far as it's not too large or too small but as it's not too large or too small but as it's not too large or too small but it's not a power of two so if we are it's not a power of two so if we are it's not a power of two so if we are given an address and we need to find out given an address and we need to find out given an address and we need to find out what is the block that contains that what is the block that contains that what is the block that contains that address we need to divide by 48 which is address we need to divide by 48 which is address we need to divide by 48 which is much more complicated than dividing by a much more complicated than dividing by a much more complicated than dividing by a power of two for example for a 32 byte power of two for example for a 32 byte power of two for example for a 32 byte size the lowest five bits of the address size the lowest five bits of the address size the lowest five bits of the address tell us where we are within the block tell us where we are within the block tell us where we are within the block this is kind of telling us where within this is kind of telling us where within this is kind of telling us where within the 32 bytes of the block we are and the the 32 bytes of the block we are and the the 32 bytes of the block we are and the upper bits of the address basically are upper bits of the address basically are upper bits of the address basically are the block number so we divide by 32 the block number so we divide by 32 the block number so we divide by 32 simply by discarding the lowermost simply by discarding the lowermost simply by discarding the lowermost 32 bytes in contrast dividing by 48 32 bytes in contrast dividing by 48 32 bytes in contrast dividing by 48 bytes would actually need to be a bytes would actually need to be a bytes would actually need to be a division by 48 so this is not a good division by 48 so this is not a good division by 48 so this is not a good line size 64 bytes is also a reasonable line size 64 bytes is also a reasonable line size 64 bytes is also a reasonable blocks as it's larger than 32 but still blocks as it's larger than 32 but still blocks as it's larger than 32 but still a lot of them fit in a two kilobyte a lot of them fit in a two kilobyte a lot of them fit in a two kilobyte cache one kilobyte is good because it's cache one kilobyte is good because it's cache one kilobyte is good because it's a power of two but only two of them fit a power of two but only two of them fit a power of two but only two of them fit in a two kilobyte cache so this is not a in a two kilobyte cache so this is not a in a two kilobyte cache so this is not a good line size because it is too large good line size because it is too large good line size because it is too large given that the cache is only two given that the cache is only two given that the cache is only two kilobytes in size what we want to have kilobytes in size what we want to have kilobytes in size what we want to have as a block size is something that is a as a block size is something that is a as a block size is something that is a power of two exploits spatial locality power of two exploits spatial locality power of two exploits spatial locality but still a significant number of them but still a significant number of them but still a significant number of them fit in our cache

## Block Offset and Number： Part 3
Kind: captions Language: en so let's talk about how we determine so let's talk about how we determine so let's talk about how we determine what the block offset is and what the what the block offset is and what the what the block offset is and what the block number is given an address let's block number is given an address let's block number is given an address let's say that the processor produces a 32-bit say that the processor produces a 32-bit say that the processor produces a 32-bit address with bits numbered from 0 address with bits numbered from 0 address with bits numbered from 0 through 31 so this is the address of the through 31 so this is the address of the through 31 so this is the address of the location the processor wants us to find location the processor wants us to find location the processor wants us to find in the cache the cache can be seen as an in the cache the cache can be seen as an in the cache the cache can be seen as an array of lines each block size in size array of lines each block size in size array of lines each block size in size in this example let's look at 16 as the in this example let's look at 16 as the in this example let's look at 16 as the block size some bits of this address block size some bits of this address block size some bits of this address tell us which block are we talking about tell us which block are we talking about tell us which block are we talking about and some bits are telling us we're in and some bits are telling us we're in and some bits are telling us we're in the block are we looking for our data the block are we looking for our data the block are we looking for our data with the block size of 16 we need to with the block size of 16 we need to with the block size of 16 we need to determine how many bits tell us where in determine how many bits tell us where in determine how many bits tell us where in the block we are the block has 16 bytes the block we are the block has 16 bytes the block we are the block has 16 bytes 2 to the 4th is 16 so we need 4 bits to 2 to the 4th is 16 so we need 4 bits to 2 to the 4th is 16 so we need 4 bits to tell us we're within a block we are that tell us we're within a block we are that tell us we're within a block we are that means that bits 0 through 3 are going to means that bits 0 through 3 are going to means that bits 0 through 3 are going to tell us once we find the block in the tell us once we find the block in the tell us once we find the block in the cache which part of the block to read cache which part of the block to read cache which part of the block to read the remaining bits bits four through 31 the remaining bits bits four through 31 the remaining bits bits four through 31 tell us which block are we trying to tell us which block are we trying to tell us which block are we trying to find so these bits that tell us where in find so these bits that tell us where in find so these bits that tell us where in the block are we going to find our data the block are we going to find our data the block are we going to find our data are called the block offset and the bits are called the block offset and the bits are called the block offset and the bits that tell us which block are we that tell us which block are we that tell us which block are we interested in are called the block interested in are called the block interested in are called the block number so what we do is we access the number so what we do is we access the number so what we do is we access the cache trying to find a block according cache trying to find a block according cache trying to find a block according to the block number if we find a block to the block number if we find a block to the block number if we find a block we use the block offset to get the right we use the block offset to get the right we use the block offset to get the right data from the block because there is data from the block because there is data from the block because there is more than one location in the block

## Block Number Quiz： Part 3
Kind: captions Language: en so let's see if we can put this to work so let's see if we can put this to work so let's see if we can put this to work in a quiz consider a cache with a 32 in a quiz consider a cache with a 32 in a quiz consider a cache with a 32 byte block size and the processor that byte block size and the processor that byte block size and the processor that creates 16-bit addresses if the address creates 16-bit addresses if the address creates 16-bit addresses if the address in binary is this there are sixteen bits in binary is this there are sixteen bits in binary is this there are sixteen bits here so if that is the address what is here so if that is the address what is here so if that is the address what is the block number that corresponds to the block number that corresponds to the block number that corresponds to this address and what is the block this address and what is the block this address and what is the block offset that corresponds to this address offset that corresponds to this address offset that corresponds to this address you can put binary numbers here because you can put binary numbers here because you can put binary numbers here because it's a lot easier that way

## Block Number Quiz Solution： Part 3
Kind: captions Language: en let's look at the solution to our block let's look at the solution to our block let's look at the solution to our block number quiz if we have a 32 byte block number quiz if we have a 32 byte block number quiz if we have a 32 byte block size and a 16-bit address like this the size and a 16-bit address like this the size and a 16-bit address like this the question is what is the block offset and question is what is the block offset and question is what is the block offset and the block number for a 32 byte block the the block number for a 32 byte block the the block number for a 32 byte block the block offset will determine where within block offset will determine where within block offset will determine where within the 32 bytes are we we need 5 bits to the 32 bytes are we we need 5 bits to the 32 bytes are we we need 5 bits to tell us that because 2 to the 5 is 32 so tell us that because 2 to the 5 is 32 so tell us that because 2 to the 5 is 32 so we need 5 bits to tell us which of the we need 5 bits to tell us which of the we need 5 bits to tell us which of the 32 bytes are we interested in accessing 32 bytes are we interested in accessing 32 bytes are we interested in accessing that means that the least significant 5 that means that the least significant 5 that means that the least significant 5 bits of this address are the block bits of this address are the block bits of this address are the block offset so the block offset is this the offset so the block offset is this the offset so the block offset is this the remaining bits of the address are the remaining bits of the address are the remaining bits of the address are the block number they are telling us which block number they are telling us which block number they are telling us which of the 32 byte blocks contains this of the 32 byte blocks contains this of the 32 byte blocks contains this address so this is our block number

## Cache Tags： Part 3
Kind: captions Language: en so now that we know how to find a block so now that we know how to find a block so now that we know how to find a block offset and the block number let's talk offset and the block number let's talk offset and the block number let's talk about the cache tags which is how we about the cache tags which is how we about the cache tags which is how we determine which block do we actually determine which block do we actually determine which block do we actually have in the cache so our cache can have in the cache so our cache can have in the cache so our cache can contain some number of lines in this contain some number of lines in this contain some number of lines in this case we have a four line cache we could case we have a four line cache we could case we have a four line cache we could have something like for example 64 bytes have something like for example 64 bytes have something like for example 64 bytes of data in each line let's say that any of data in each line let's say that any of data in each line let's say that any block from memory can be placed in any block from memory can be placed in any block from memory can be placed in any of these four lines in that case when we of these four lines in that case when we of these four lines in that case when we are given the address by the processor are given the address by the processor are given the address by the processor the least significant bits of the the least significant bits of the the least significant bits of the address will tell us where in the line address will tell us where in the line address will tell us where in the line we are after we find the line but each we are after we find the line but each we are after we find the line but each of these lines in the cache could of these lines in the cache could of these lines in the cache could contain any block number from the memory contain any block number from the memory contain any block number from the memory so the question is how do we find so the question is how do we find so the question is how do we find whether our access is actually a cache whether our access is actually a cache whether our access is actually a cache hit or not and the answer is in addition hit or not and the answer is in addition hit or not and the answer is in addition to the data of the blocks the cache also to the data of the blocks the cache also to the data of the blocks the cache also keeps a so called tag that tells it keeps a so called tag that tells it keeps a so called tag that tells it which block does it have here in this which block does it have here in this which block does it have here in this particular cache the tag will contain particular cache the tag will contain particular cache the tag will contain the block number that the cache has in the block number that the cache has in the block number that the cache has in each of these spots so we need four tags each of these spots so we need four tags each of these spots so we need four tags because there could be four blocks in because there could be four blocks in because there could be four blocks in this cache so given the address we take this cache so given the address we take this cache so given the address we take the block number part of the address and the block number part of the address and the block number part of the address and compare it to each of the tags we have compare it to each of the tags we have compare it to each of the tags we have in our cache if one of these says one in our cache if one of these says one in our cache if one of these says one that means that the tag matches the that means that the tag matches the that means that the tag matches the block number and that means that our block number and that means that our block number and that means that our data is in the corresponding line in the data is in the corresponding line in the data is in the corresponding line in the cache let's say that this is the cache let's say that this is the cache let's say that this is the situation we have so we have a tag match situation we have so we have a tag match situation we have so we have a tag match here that means that we have a hit and here that means that we have a hit and here that means that we have a hit and that our data is here in the cache now that our data is here in the cache now that our data is here in the cache now we can use the block offset to tell us we can use the block offset to tell us we can use the block offset to tell us where in that line our data is and we where in that line our data is and we where in that line our data is and we take the data and supply to the take the data and supply to the take the data and supply to the processor so in this particular cache we processor so in this particular cache we processor so in this particular cache we have the offset part of the address here have the offset part of the address here have the offset part of the address here and the block number is in this case and the block number is in this case and the block number is in this case called the tag part of the address when called the tag part of the address when called the tag part of the address when we have a cache miss we will bring the we have a cache miss we will bring the we have a cache miss we will bring the data here and we will put the block data here and we will put the block data here and we will put the block number in the corresponding tag so that number in the corresponding tag so that number in the corresponding tag so that we can find this block later we will see we can find this block later we will see we can find this block later we will see later that sometimes the tag part of the later that sometimes the tag part of the later that sometimes the tag part of the address address address is not exactly the same as the block is not exactly the same as the block is not exactly the same as the block number but in this cache it is

## Cache Tag Quiz： Part 3
Kind: captions Language: en let's check our knowledge of cach tags let's check our knowledge of cach tags let's check our knowledge of cach tags using a quiz which is always true about using a quiz which is always true about using a quiz which is always true about the cach tags it contains the entire the cach tags it contains the entire the cach tags it contains the entire address of the first bite of the block address of the first bite of the block address of the first bite of the block it contains at least one bit from the it contains at least one bit from the it contains at least one bit from the Block offset it contains at least one Block offset it contains at least one Block offset it contains at least one bit from the block number it contains bit from the block number it contains bit from the block number it contains some bits from the Block offset and some some bits from the Block offset and some some bits from the Block offset and some from the block number select all of from the block number select all of from the block number select all of those that the tag always has


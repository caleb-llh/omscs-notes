# Playlist 2 Module 5 Raw Transcript

## Memory Ordering Quiz Solution： Part 3
Kind: captions Language: en okay let's see our memory or drink quiz okay let's see our memory or drink quiz okay let's see our memory or drink quiz solution this load is still sent to solution this load is still sent to solution this load is still sent to memory in cycle 1 and we'll come back in memory in cycle 1 and we'll come back in memory in cycle 1 and we'll come back in cycle 41 this store however cannot cycle 41 this store however cannot cycle 41 this store however cannot proceed fully because we don't know what proceed fully because we don't know what proceed fully because we don't know what the value is the value is the value is until cycle 42 so in cycle 42 this store until cycle 42 so in cycle 42 this store until cycle 42 so in cycle 42 this store finally figures out what the address and finally figures out what the address and finally figures out what the address and the value are so now this load can be the value are so now this load can be the value are so now this load can be considered done because we need to do considered done because we need to do considered done because we need to do things in order that means that only in things in order that means that only in things in order that means that only in cycle 43 this load can start going to cycle 43 this load can start going to cycle 43 this load can start going to memory it's gonna take 40 cycles to come memory it's gonna take 40 cycles to come memory it's gonna take 40 cycles to come back so in cycle 83 the value comes back back so in cycle 83 the value comes back back so in cycle 83 the value comes back from memory at that point this store can from memory at that point this store can from memory at that point this store can finish at cycle 84 in cycle 85 that finish at cycle 84 in cycle 85 that finish at cycle 84 in cycle 85 that means that only then this load can go to means that only then this load can go to means that only then this load can go to memory it's gonna be cycle 125 by the memory it's gonna be cycle 125 by the memory it's gonna be cycle 125 by the time it come back and finally the final time it come back and finally the final time it come back and finally the final store can be done in cycle 126 as you store can be done in cycle 126 as you store can be done in cycle 126 as you can see it took almost three times as can see it took almost three times as can see it took almost three times as long in this case to do this as it did long in this case to do this as it did long in this case to do this as it did when we were trying to do things out of when we were trying to do things out of when we were trying to do things out of order so there is a huge advantage in order so there is a huge advantage in order so there is a huge advantage in trying to reorder load store trying to reorder load store trying to reorder load store instructions but it carries a risk of instructions but it carries a risk of instructions but it carries a risk of having to recover from basically loading having to recover from basically loading having to recover from basically loading the wrong value from memory because we the wrong value from memory because we the wrong value from memory because we should have loaded the value from the should have loaded the value from the should have loaded the value from the store

## Store to Load Forwarding： Part 3
Kind: captions Language: en before we discuss how the recovery from before we discuss how the recovery from before we discuss how the recovery from loads done too early is happening let's loads done too early is happening let's loads done too early is happening let's first talk about the store to load first talk about the store to load first talk about the store to load forwarding problem for a load we have to forwarding problem for a load we have to forwarding problem for a load we have to ask ourselves which earlier store do we ask ourselves which earlier store do we ask ourselves which earlier store do we get our value from basically there could get our value from basically there could get our value from basically there could be multiple stores to the same address be multiple stores to the same address be multiple stores to the same address that our load is going to and we have to that our load is going to and we have to that our load is going to and we have to figure out which store is the one to figure out which store is the one to figure out which store is the one to supply our value and also if none of supply our value and also if none of supply our value and also if none of them are matching then we need to figure them are matching then we need to figure them are matching then we need to figure that out because then we need to go to that out because then we need to go to that out because then we need to go to memory when a store is finally resolved memory when a store is finally resolved memory when a store is finally resolved we need to ask ourselves which later we need to ask ourselves which later we need to ask ourselves which later load do I give my value to so there load do I give my value to so there load do I give my value to so there might be a load whose address is already might be a load whose address is already might be a load whose address is already known once the store figures out the known once the store figures out the known once the store figures out the address and the value we should be address and the value we should be address and the value we should be waking up the load that wants that value waking up the load that wants that value waking up the load that wants that value note that there could be multiple loads note that there could be multiple loads note that there could be multiple loads that the store needs to give the value that the store needs to give the value that the store needs to give the value to so where do we figure this out in the to so where do we figure this out in the to so where do we figure this out in the load store queue

## LSQ Example： Part 3
Kind: captions Language: en okay to illustrate the operation of the okay to illustrate the operation of the okay to illustrate the operation of the lsq we will do an example so here I have lsq we will do an example so here I have lsq we will do an example so here I have a load/store queue where this is the a load/store queue where this is the a load/store queue where this is the oldest instruction and this is the oldest instruction and this is the oldest instruction and this is the youngest instruction in the queue youngest instruction in the queue youngest instruction in the queue note that the queue is storing note that the queue is storing note that the queue is storing instructions in the correct program instructions in the correct program instructions in the correct program order so basically in program order this order so basically in program order this order so basically in program order this was the first low this was the second was the first low this was the second was the first low this was the second instruction and so on this says whether instruction and so on this says whether instruction and so on this says whether it's a load or a store this tells us it's a load or a store this tells us it's a load or a store this tells us what address the loader store was what address the loader store was what address the loader store was fetched from this is the sequence number fetched from this is the sequence number fetched from this is the sequence number basically just going up and this is the basically just going up and this is the basically just going up and this is the address to which the load or the store address to which the load or the store address to which the load or the store resolves to and this is the value that resolves to and this is the value that resolves to and this is the value that we will get for that loader store and we will get for that loader store and we will get for that loader store and let's say that this is the content of let's say that this is the content of let's say that this is the content of the data cache let's say that the the data cache let's say that the the data cache let's say that the initial content of the data cache is 42 initial content of the data cache is 42 initial content of the data cache is 42 here 1 here 38 here and 1 2 3 4 here here 1 here 38 here and 1 2 3 4 here here 1 here 38 here and 1 2 3 4 here this load goes first this load goes first this load goes first access is address 3 2 9 0 which is this access is address 3 2 9 0 which is this access is address 3 2 9 0 which is this one and loads the value of 42 let's say one and loads the value of 42 let's say one and loads the value of 42 let's say now that this store here computes its now that this store here computes its now that this store here computes its result it basically gets the value it's result it basically gets the value it's result it basically gets the value it's gonna store let's say that it gets 25 gonna store let's say that it gets 25 gonna store let's say that it gets 25 note that this store doesn't really put note that this store doesn't really put note that this store doesn't really put that value yet in the cache that will that value yet in the cache that will that value yet in the cache that will happen when it commits so this is only happen when it commits so this is only happen when it commits so this is only that we have gotten the value from the that we have gotten the value from the that we have gotten the value from the instruction that produces the register instruction that produces the register instruction that produces the register that we will use as our source value to that we will use as our source value to that we will use as our source value to store in memory let's say that this next store in memory let's say that this next store in memory let's say that this next door also gets a value of minus 16 this door also gets a value of minus 16 this door also gets a value of minus 16 this time now let's say that this load time now let's say that this load time now let's say that this load executes snacks access is the address 3 executes snacks access is the address 3 executes snacks access is the address 3 4 1 8 and it tries to see whether any of 4 1 8 and it tries to see whether any of 4 1 8 and it tries to see whether any of the previous stores matches that address the previous stores matches that address the previous stores matches that address they don't so it loads the value from they don't so it loads the value from they don't so it loads the value from memory this is the address in the cache memory this is the address in the cache memory this is the address in the cache that matches this so we're gonna get 1 2 that matches this so we're gonna get 1 2 that matches this so we're gonna get 1 2 3 4 3 4 3 4 next this load tries to load the value next this load tries to load the value next this load tries to load the value it has the address of 3 2 9 0 it's going it has the address of 3 2 9 0 it's going it has the address of 3 2 9 0 it's going to search upward for the latest store to search upward for the latest store to search upward for the latest store that still precedes it that matches the that still precedes it that matches the that still precedes it that matches the address and it finds one this store is address and it finds one this store is address and it finds one this store is having the address of 3 2 9 0 so instead having the address of 3 2 9 0 so instead having the address of 3 2 9 0 so instead of going to the cache we are directly of going to the cache we are directly of going to the cache we are directly going to copy just the value of minus 17 going to copy just the value of minus 17 going to copy just the value of minus 17 here so now we actually had a load that here so now we actually had a load that here so now we actually had a load that never really accesses the cache it just never really accesses the cache it just never really accesses the cache it just gets the value from the gets the value from the gets the value from the store the next load accesses address store the next load accesses address store the next load accesses address three three zero zero it searches the three three zero zero it searches the three three zero zero it searches the previous stores and doesn't find any previous stores and doesn't find any previous stores and doesn't find any that match this address so it's gonna go that match this address so it's gonna go that match this address so it's gonna go to the cache and load from address three to the cache and load from address three to the cache and load from address three three zero zero and it gets one next we three zero zero and it gets one next we three zero zero and it gets one next we have another store two three two nine have another store two three two nine have another store two three two nine zero let's say that the value store zero let's say that the value store zero let's say that the value store there is now zero next we have a load there is now zero next we have a load there is now zero next we have a load from three four one zero it searches the from three four one zero it searches the from three four one zero it searches the previous accesses for a store that previous accesses for a store that previous accesses for a store that matches this address and it finds one matches this address and it finds one matches this address and it finds one over here so it just copies the value of over here so it just copies the value of over here so it just copies the value of twenty five so again we have a load that twenty five so again we have a load that twenty five so again we have a load that never accesses the cache because it never accesses the cache because it never accesses the cache because it found what it needed in the load/store found what it needed in the load/store found what it needed in the load/store queue next we have a low two three two queue next we have a low two three two queue next we have a low two three two nine zero it searches the load/store nine zero it searches the load/store nine zero it searches the load/store queue to find a store that matches this queue to find a store that matches this queue to find a store that matches this address and it finds one over here so it address and it finds one over here so it address and it finds one over here so it just copies the value of zero here note just copies the value of zero here note just copies the value of zero here note that by now there have been several that by now there have been several that by now there have been several stores to three two nine zero we always stores to three two nine zero we always stores to three two nine zero we always want to get the value from the most want to get the value from the most want to get the value from the most recent one that still precedes us recent one that still precedes us recent one that still precedes us because if things were happening in because if things were happening in because if things were happening in order one at a time this is exactly what order one at a time this is exactly what order one at a time this is exactly what the value should be in the memory the value should be in the memory the value should be in the memory location note however that the memory location note however that the memory location note however that the memory location as far as the cache and the location as far as the cache and the location as far as the cache and the memory are concerned still contains the memory are concerned still contains the memory are concerned still contains the value 42 at this time although there value 42 at this time although there value 42 at this time although there have been several modifications why well have been several modifications why well have been several modifications why well because the stores haven't sent the because the stores haven't sent the because the stores haven't sent the values to the data cache yet and finally values to the data cache yet and finally values to the data cache yet and finally we have another low two three three zero we have another low two three three zero we have another low two three three zero zero it searches backward to find any zero it searches backward to find any zero it searches backward to find any stores to the same address note that stores to the same address note that stores to the same address note that this is a load we don't do that so there this is a load we don't do that so there this is a load we don't do that so there are no stores to the same address so we are no stores to the same address so we are no stores to the same address so we actually load the value from the cache actually load the value from the cache actually load the value from the cache and the value is 1 now let's say that at and the value is 1 now let's say that at and the value is 1 now let's say that at some point the loads and stores start some point the loads and stores start some point the loads and stores start committing so we will have this load committing so we will have this load committing so we will have this load commit a load commits by simply copying commit a load commits by simply copying commit a load commits by simply copying the value to the register file and now the value to the register file and now the value to the register file and now the oldest pointer is here this store the oldest pointer is here this store the oldest pointer is here this store commits by putting the value in the commits by putting the value in the commits by putting the value in the cache so now we take the value of 25 and cache so now we take the value of 25 and cache so now we take the value of 25 and put it in address three four one zero so put it in address three four one zero so put it in address three four one zero so the override is 38 with 25 and now the the override is 38 with 25 and now the the override is 38 with 25 and now the store can commit and our pointer moves store can commit and our pointer moves store can commit and our pointer moves here we commit another store here so the here we commit another store here so the here we commit another store here so the value of minus 17 goes to location 3 - 9 value of minus 17 goes to location 3 - 9 value of minus 17 goes to location 3 - 9 0 so we override this 42 with 17 0 so we override this 42 with 17 0 so we override this 42 with 17 now our store is committed we move the now our store is committed we move the now our store is committed we move the pointer here if you remember we said pointer here if you remember we said pointer here if you remember we said that we need to send the values to the that we need to send the values to the that we need to send the values to the memory or the data cache at the time the memory or the data cache at the time the memory or the data cache at the time the store commits not at the time it store commits not at the time it store commits not at the time it executes here's why if at this point we executes here's why if at this point we executes here's why if at this point we decide that we are taking an exception decide that we are taking an exception decide that we are taking an exception we can just flush this from the we can just flush this from the we can just flush this from the load/store queue and the value in the load/store queue and the value in the load/store queue and the value in the data cache in memory is exactly the data cache in memory is exactly the data cache in memory is exactly the value that should be there as of this value that should be there as of this value that should be there as of this point in the program execution so just point in the program execution so just point in the program execution so just like we had our architectural register like we had our architectural register like we had our architectural register file and we were copying values on file and we were copying values on file and we were copying values on commit into it for registers so that at commit into it for registers so that at commit into it for registers so that at any given time we know exactly what the any given time we know exactly what the any given time we know exactly what the committed registers value are similarly committed registers value are similarly committed registers value are similarly at any given time we have in our data at any given time we have in our data at any given time we have in our data cache in memory cache in memory cache in memory exactly what is there as of the commit exactly what is there as of the commit exactly what is there as of the commit point this process will now go on the point this process will now go on the point this process will now go on the load commits and deposits this value in load commits and deposits this value in load commits and deposits this value in its register another load can commit and its register another load can commit and its register another load can commit and another one when the store commits it another one when the store commits it another one when the store commits it overrides this 17 with a zero and then overrides this 17 with a zero and then overrides this 17 with a zero and then the loads commit and the final content the loads commit and the final content the loads commit and the final content of the cache is this

## LSQ, ROB, and RS： Part 3
Kind: captions Language: en let's discuss the relationship between let's discuss the relationship between let's discuss the relationship between lsq entries Robin trees and reservation lsq entries Robin trees and reservation lsq entries Robin trees and reservation stations when we issue a load or a store stations when we issue a load or a store stations when we issue a load or a store instruction we need a robe and three instruction we need a robe and three instruction we need a robe and three every instruction needs a Robin tree so every instruction needs a Robin tree so every instruction needs a Robin tree so when we issue a non load store when we issue a non load store when we issue a non load store instruction we also get a robe and tree instruction we also get a robe and tree instruction we also get a robe and tree and we get for a non load store and we get for a non load store and we get for a non load store instruction a reservation station of the instruction a reservation station of the instruction a reservation station of the right type for loads and stores instead right type for loads and stores instead right type for loads and stores instead we get a lsq entry this act as we get a lsq entry this act as we get a lsq entry this act as reservation stations for loads and reservation stations for loads and reservation stations for loads and stores so note that we cannot issue a stores so note that we cannot issue a stores so note that we cannot issue a load or a store unless we have both a load or a store unless we have both a load or a store unless we have both a Rob entry available and then lsq entry Rob entry available and then lsq entry Rob entry available and then lsq entry available just like we couldn't issue a available just like we couldn't issue a available just like we couldn't issue a non load store instruction if we didn't non load store instruction if we didn't non load store instruction if we didn't have both a Rob enter available and the have both a Rob enter available and the have both a Rob enter available and the reservation station of the right type reservation station of the right type reservation station of the right type available when we execute a load store available when we execute a load store available when we execute a load store instruction there are actually two parts instruction there are actually two parts instruction there are actually two parts to that execution first we need to to that execution first we need to to that execution first we need to compute the address second we need to compute the address second we need to compute the address second we need to produce the value for a load that means produce the value for a load that means produce the value for a load that means we first compute the address then we get we first compute the address then we get we first compute the address then we get the value from memory for a store the value from memory for a store the value from memory for a store actually this can happen in any order we actually this can happen in any order we actually this can happen in any order we are computing the address while we are are computing the address while we are are computing the address while we are also trying to obtain the value of the also trying to obtain the value of the also trying to obtain the value of the register that we need to store to memory register that we need to store to memory register that we need to store to memory we write the result only for loads for a we write the result only for loads for a we write the result only for loads for a store there is no right of the result store there is no right of the result store there is no right of the result the store simply keeps the value and the the store simply keeps the value and the the store simply keeps the value and the address in the lost or queue for other address in the lost or queue for other address in the lost or queue for other loads to get it and for the store to loads to get it and for the store to loads to get it and for the store to finally send it to memory on commit but finally send it to memory on commit but finally send it to memory on commit but the load as soon as it gets the result the load as soon as it gets the result the load as soon as it gets the result will broadcast that result to dependent will broadcast that result to dependent will broadcast that result to dependent instructions so that pretty much all the instructions so that pretty much all the instructions so that pretty much all the reservation stations that are waiting reservation stations that are waiting reservation stations that are waiting for that register value can now proceed for that register value can now proceed for that register value can now proceed so note that our load store unit so note that our load store unit so note that our load store unit although it doesn't have reservation although it doesn't have reservation although it doesn't have reservation stations it has the load/store queue so stations it has the load/store queue so stations it has the load/store queue so when it finally figures out the value when it finally figures out the value when it finally figures out the value for the load it needs to broadcast it to for the load it needs to broadcast it to for the load it needs to broadcast it to subsequent instructions so that they can subsequent instructions so that they can subsequent instructions so that they can be woken up and use that value they will be woken up and use that value they will be woken up and use that value they will get that value from the load/store queue get that value from the load/store queue get that value from the load/store queue to commit a load we move the arrow big to commit a load we move the arrow big to commit a load we move the arrow big head basically we freed our Arobin tree head basically we freed our Arobin tree head basically we freed our Arobin tree and we also move the lsq head that is we and we also move the lsq head that is we and we also move the lsq head that is we freed the lsq entry and we need to do freed the lsq entry and we need to do freed the lsq entry and we need to do the same thing for stores for stores in the same thing for stores for stores in the same thing for stores for stores in addition to this we have to send the addition to this we have to send the addition to this we have to send the right to memory pretty much we've been right to memory pretty much we've been right to memory pretty much we've been holding on to the address in the value holding on to the address in the value holding on to the address in the value until we commit the store so at the time until we commit the store so at the time until we commit the store so at the time when we commit it we need to finally when we commit it we need to finally when we commit it we need to finally send that send that send that to memory

## Memory Ordering Quiz： Part 3
Kind: captions Language: en so let's do a memory ordering quiz if we so let's do a memory ordering quiz if we so let's do a memory ordering quiz if we have these instructions as consecutive have these instructions as consecutive have these instructions as consecutive instructions in the program where do you instructions in the program where do you instructions in the program where do you think the load gets its value from so think the load gets its value from so think the load gets its value from so does the load access cache or memory yes does the load access cache or memory yes does the load access cache or memory yes or no right it here

## Memory Ordering Quiz Solution： Part 3
Kind: captions Language: en and the answer is no the law doesn't and the answer is no the law doesn't and the answer is no the law doesn't access the cache or the memory we know access the cache or the memory we know access the cache or the memory we know that this is the same address so the that this is the same address so the that this is the same address so the load will get its value from the store load will get its value from the store load will get its value from the store so the correct answer here is no

## Memory Ordering Quiz 2： Part 3
Kind: captions Language: en so in the first part of the quiz we have so in the first part of the quiz we have so in the first part of the quiz we have seen that the answer is no the load seen that the answer is no the load seen that the answer is no the load doesn't get the value from cash or doesn't get the value from cash or doesn't get the value from cash or memory it gets it from the store so the memory it gets it from the store so the memory it gets it from the store so the second part of the quiz is second part of the quiz is second part of the quiz is the load gets its value from a result the load gets its value from a result the load gets its value from a result broadcast put a check mark here if this broadcast put a check mark here if this broadcast put a check mark here if this is correct a reservation station is correct a reservation station is correct a reservation station put a check mark here if that is correct put a check mark here if that is correct put a check mark here if that is correct a Rob entry put the check mark here if a Rob entry put the check mark here if a Rob entry put the check mark here if that is correct and an lsq entry put a that is correct and an lsq entry put a that is correct and an lsq entry put a check mark if this is correct note that check mark if this is correct note that check mark if this is correct note that in different situations it might be in different situations it might be in different situations it might be possible that multiple answers are possible that multiple answers are possible that multiple answers are correct so put check marks for correct so put check marks for correct so put check marks for everywhere where we might get the value everywhere where we might get the value everywhere where we might get the value from

## Memory Ordering Quiz Solution 2： Part 3
Kind: captions Language: en so let's discuss the memory or drink so let's discuss the memory or drink so let's discuss the memory or drink waste solution for this in this waste solution for this in this waste solution for this in this situation there is a store and then a situation there is a store and then a situation there is a store and then a load to the same address so the load load to the same address so the load load to the same address so the load obviously doesn't go to cache or memory obviously doesn't go to cache or memory obviously doesn't go to cache or memory to get its value it's gonna get it from to get its value it's gonna get it from to get its value it's gonna get it from the store but where does the store keep the store but where does the store keep the store but where does the store keep its value the store doesn't broadcast its value the store doesn't broadcast its value the store doesn't broadcast the result the result is broadcasts for the result the result is broadcasts for the result the result is broadcasts for instructions that produce register instructions that produce register instructions that produce register results so the store really never results so the store really never results so the store really never broadcast this result so this is broadcast this result so this is broadcast this result so this is definitely not true a reservation definitely not true a reservation definitely not true a reservation station reservation stations never station reservation stations never station reservation stations never provide any results to subsequent provide any results to subsequent provide any results to subsequent instructions they only keep values instructions they only keep values instructions they only keep values captured for the current instruction the captured for the current instruction the captured for the current instruction the store doesn't even have a reservation store doesn't even have a reservation store doesn't even have a reservation station so this is definitely not station so this is definitely not station so this is definitely not correct correct correct a Rob entry would keep a result for a a Rob entry would keep a result for a a Rob entry would keep a result for a registered producing instruction between registered producing instruction between registered producing instruction between the time it's been broadcast and the the time it's been broadcast and the the time it's been broadcast and the time it commits but because the store is time it commits but because the store is time it commits but because the store is not a register value producing not a register value producing not a register value producing instruction it doesn't put its result in instruction it doesn't put its result in instruction it doesn't put its result in a Rob entry in fact technically the a Rob entry in fact technically the a Rob entry in fact technically the store doesn't have a result so this is store doesn't have a result so this is store doesn't have a result so this is not correct the store does keep the not correct the store does keep the not correct the store does keep the value it's going to store in the value it's going to store in the value it's going to store in the load/store queue and if you remember load/store queue and if you remember load/store queue and if you remember that is where the load searches for a that is where the load searches for a that is where the load searches for a value when it's running to match its value when it's running to match its value when it's running to match its address two addresses of previous stores address two addresses of previous stores address two addresses of previous stores so this is the only correct answer

## Memory Ordering Outro： Part 3
Kind: captions Language: en in this lesson we learned that we need a in this lesson we learned that we need a in this lesson we learned that we need a load/store queue to track dependencies load/store queue to track dependencies load/store queue to track dependencies through memory so we can ensure correct through memory so we can ensure correct through memory so we can ensure correct execution of memory instructions in and execution of memory instructions in and execution of memory instructions in and out of order processor modern processors out of order processor modern processors out of order processor modern processors have really fancy Loesser queues which have really fancy Loesser queues which have really fancy Loesser queues which helps them do a lot of reordering even helps them do a lot of reordering even helps them do a lot of reordering even for memory instructions

## Compiler ILP Intro： Part 3
Kind: captions Language: en we have completed our discussions of we have completed our discussions of we have completed our discussions of out-of-order processors that try to out-of-order processors that try to out-of-order processors that try to execute more than one instruction per execute more than one instruction per execute more than one instruction per cycle cycle cycle now we will discuss how the compiler can now we will discuss how the compiler can now we will discuss how the compiler can help these processors

## Can Compilers Help Improve IPC： Part 3
Kind: captions Language: en so let's discuss can compilers help so let's discuss can compilers help so let's discuss can compilers help improve IPC which is the number of improve IPC which is the number of improve IPC which is the number of instructions per cycle that the instructions per cycle that the instructions per cycle that the processor achieves processor achieves processor achieves there are really two things that the there are really two things that the there are really two things that the compiler can help with one is the ILP of compiler can help with one is the ILP of compiler can help with one is the ILP of the program itself may be limited for the program itself may be limited for the program itself may be limited for example due to dependence chains which example due to dependence chains which example due to dependence chains which is when we have instructions that depend is when we have instructions that depend is when we have instructions that depend on each other so really if we have a on each other so really if we have a on each other so really if we have a long chain like this then there would be long chain like this then there would be long chain like this then there would be very limited ILP in this program because very limited ILP in this program because very limited ILP in this program because we can really execute only one we can really execute only one we can really execute only one instruction at a time so the compiler as instruction at a time so the compiler as instruction at a time so the compiler as we will see might help us actually avoid we will see might help us actually avoid we will see might help us actually avoid these kinds of dependent chains and also these kinds of dependent chains and also these kinds of dependent chains and also the hardware has a limited window into the hardware has a limited window into the hardware has a limited window into the program for example there might be the program for example there might be the program for example there might be independent instructions so an ideal independent instructions so an ideal independent instructions so an ideal processor might be able to achieve good processor might be able to achieve good processor might be able to achieve good ILP on that program but because these ILP on that program but because these ILP on that program but because these independent instructions are far apart a independent instructions are far apart a independent instructions are far apart a real processor simply cannot see those real processor simply cannot see those real processor simply cannot see those instructions because for example it runs instructions because for example it runs instructions because for example it runs out of Rob space before it reaches those out of Rob space before it reaches those out of Rob space before it reaches those instructions that are independent so in instructions that are independent so in instructions that are independent so in the same example if we have a long chain the same example if we have a long chain the same example if we have a long chain like this followed by like this followed by like this followed by this instruction doesn't depend on any this instruction doesn't depend on any this instruction doesn't depend on any of those in the chain but if the chain of those in the chain but if the chain of those in the chain but if the chain is too long we run out of Arabic space is too long we run out of Arabic space is too long we run out of Arabic space before we reach this instruction so as before we reach this instruction so as before we reach this instruction so as far as this processor is concerned this far as this processor is concerned this far as this processor is concerned this instruction cannot execute in parallel instruction cannot execute in parallel instruction cannot execute in parallel with those just because we never see it with those just because we never see it with those just because we never see it as we will see the compiler can help us as we will see the compiler can help us as we will see the compiler can help us put this independent instructions closer put this independent instructions closer put this independent instructions closer to each other so that the IPC achieved to each other so that the IPC achieved to each other so that the IPC achieved by the processor is closer to the by the processor is closer to the by the processor is closer to the available ILP

## Tree Height Reduction： Part 3
Kind: captions Language: en One example of a technique to actually One example of a technique to actually One example of a technique to actually improve the ILP of the program is three improve the ILP of the program is three improve the ILP of the program is three height reduction. Let's consider a height reduction. Let's consider a height reduction. Let's consider a program that looks like this. Let's say program that looks like this. Let's say program that looks like this. Let's say that we need a sum of these four that we need a sum of these four that we need a sum of these four registers in R8. One way to get that is registers in R8. One way to get that is registers in R8. One way to get that is to add the first two numbers into R8 and to add the first two numbers into R8 and to add the first two numbers into R8 and then add the second number and then add then add the second number and then add then add the second number and then add the third number and then the fourth the third number and then the fourth the third number and then the fourth number. However, this forms a dependence number. However, this forms a dependence number. However, this forms a dependence chain because we have the dependence chain because we have the dependence chain because we have the dependence here and the dependence here. So really here and the dependence here. So really here and the dependence here. So really these instructions have to be done one these instructions have to be done one these instructions have to be done one after the other. So tree height after the other. So tree height after the other. So tree height reduction works as follows. The compiler reduction works as follows. The compiler reduction works as follows. The compiler figures out that instead of adding one figures out that instead of adding one figures out that instead of adding one number at a time to the sum and thus number at a time to the sum and thus number at a time to the sum and thus creating a chain of dependencies, what creating a chain of dependencies, what creating a chain of dependencies, what we can do is we can group the we can do is we can group the we can do is we can group the computation this way. Thus getting we computation this way. Thus getting we computation this way. Thus getting we first add the first two numbers and put first add the first two numbers and put first add the first two numbers and put that in R8. Then we add the second pair that in R8. Then we add the second pair that in R8. Then we add the second pair of numbers, put that in R seven. And of numbers, put that in R seven. And of numbers, put that in R seven. And then we add up the R8 and R seven into then we add up the R8 and R seven into then we add up the R8 and R seven into our final result R8. The dependencies our final result R8. The dependencies our final result R8. The dependencies here are this and this. So now we have here are this and this. So now we have here are this and this. So now we have two instructions that can execute in two instructions that can execute in two instructions that can execute in parallel with each other because they parallel with each other because they parallel with each other because they don't depend on each other and then a don't depend on each other and then a don't depend on each other and then a third instruction that does depend on third instruction that does depend on third instruction that does depend on them too. So if this needed three cycles them too. So if this needed three cycles them too. So if this needed three cycles to execute, this can actually execute in to execute, this can actually execute in to execute, this can actually execute in two cycles if we have a processor that two cycles if we have a processor that two cycles if we have a processor that can do at least two instructions per can do at least two instructions per can do at least two instructions per cycle. Three height reduction cannot cycle. Three height reduction cannot cycle. Three height reduction cannot always be done. It uses associivity of always be done. It uses associivity of always be done. It uses associivity of the addition operation. And you have to the addition operation. And you have to the addition operation. And you have to keep in mind that not all operations are keep in mind that not all operations are keep in mind that not all operations are associative. So we have to be careful. associative. So we have to be careful. associative. So we have to be careful. We can only do this when the final We can only do this when the final We can only do this when the final result is the same as if we did things result is the same as if we did things result is the same as if we did things in the proper order. Let's say

## Tree Height Reduction Quiz： Part 3
Kind: captions Language: en so now let us try our hand at some tree so now let us try our hand at some tree so now let us try our hand at some tree height reduction suppose we have these height reduction suppose we have these height reduction suppose we have these six instructions we are adding r1 and r2 six instructions we are adding r1 and r2 six instructions we are adding r1 and r2 putting the result in our 10 then putting the result in our 10 then putting the result in our 10 then subtracting r3 from that then adding r4 subtracting r3 from that then adding r4 subtracting r3 from that then adding r4 then subtracting r5 then adding r6 and then subtracting r5 then adding r6 and then subtracting r5 then adding r6 and then subtracting r7 if we now do a 3 then subtracting r7 if we now do a 3 then subtracting r7 if we now do a 3 height reduction on this what do we get height reduction on this what do we get height reduction on this what do we get and I already pre-populated most of the and I already pre-populated most of the and I already pre-populated most of the answer for you so we're still gonna add answer for you so we're still gonna add answer for you so we're still gonna add r1 and r2 and put the result in our 10 r1 and r2 and put the result in our 10 r1 and r2 and put the result in our 10 but then what do we add input in our 11 but then what do we add input in our 11 but then what do we add input in our 11 what do we add our 11 to and put that in what do we add our 11 to and put that in what do we add our 11 to and put that in our 10 what do we then add up and put our 10 what do we then add up and put our 10 what do we then add up and put the result in our 11 and then we add our the result in our 11 and then we add our the result in our 11 and then we add our 7 to what and put the result in our 11 7 to what and put the result in our 11 7 to what and put the result in our 11 and finally we will subtract our 11 from and finally we will subtract our 11 from and finally we will subtract our 11 from our 10 and get the final result in our our 10 and get the final result in our our 10 and get the final result in our 10 and this should be the same result 10 and this should be the same result 10 and this should be the same result that we got here so the question for you that we got here so the question for you that we got here so the question for you is what should be in these five places is what should be in these five places is what should be in these five places and also the ILP here is clearly 6 and also the ILP here is clearly 6 and also the ILP here is clearly 6 cycles for 6 instructions which gets us cycles for 6 instructions which gets us cycles for 6 instructions which gets us a 1 why well because each of the a 1 why well because each of the a 1 why well because each of the instructions depends on the previous one instructions depends on the previous one instructions depends on the previous one with your new tree height reduced code with your new tree height reduced code with your new tree height reduced code what is the LP we still have six what is the LP we still have six what is the LP we still have six instructions how many cycles do we need instructions how many cycles do we need instructions how many cycles do we need to do them now

## Tree Height Reduction Quiz Solution： Part 3
Kind: captions Language: en let's look at the solution let's look at the solution let's look at the solution to our three height reduction quiz we to our three height reduction quiz we to our three height reduction quiz we had this code and we are trying to had this code and we are trying to had this code and we are trying to transform it through three height transform it through three height transform it through three height reduction into something that minimizes reduction into something that minimizes reduction into something that minimizes the overall number of cycles it takes to the overall number of cycles it takes to the overall number of cycles it takes to execute this code here the code takes execute this code here the code takes execute this code here the code takes six cycles to do because each of the six cycles to do because each of the six cycles to do because each of the instructions depends on the previous one instructions depends on the previous one instructions depends on the previous one now when doing three height reduction now when doing three height reduction now when doing three height reduction what we need to do is really figure out what we need to do is really figure out what we need to do is really figure out what is the expression that this is what is the expression that this is what is the expression that this is trying to compute and then rewrite that trying to compute and then rewrite that trying to compute and then rewrite that expression so here's what we have here expression so here's what we have here expression so here's what we have here we have r1 plus r2 we have r1 plus r2 we have r1 plus r2 put the result in r10 then subtract r3 put the result in r10 then subtract r3 put the result in r10 then subtract r3 then add our 4 then subtract our five then add our 4 then subtract our five then add our 4 then subtract our five then add our six then add our six then add our six then subtract our seven so this is what then subtract our seven so this is what then subtract our seven so this is what we are trying to compute how do we we are trying to compute how do we we are trying to compute how do we compute it with three height reduction compute it with three height reduction compute it with three height reduction well the idea is to compute well the idea is to compute well the idea is to compute r1 and r2 that's what we're doing here r1 and r2 that's what we're doing here r1 and r2 that's what we're doing here and then compute and then compute and then compute the rest of the things and then add them the rest of the things and then add them the rest of the things and then add them up in a tree so here what we need to do up in a tree so here what we need to do up in a tree so here what we need to do is we need to add up four numbers and is we need to add up four numbers and is we need to add up four numbers and then subtract then subtract then subtract three numbers from that so first we will three numbers from that so first we will three numbers from that so first we will add up the four numbers we already added add up the four numbers we already added add up the four numbers we already added up r1 and r2 up r1 and r2 up r1 and r2 now we're going to add up r4 and r6 next now we're going to add up r4 and r6 next now we're going to add up r4 and r6 next the result of the two additions the result of the two additions the result of the two additions are gonna be are gonna be are gonna be combined combined combined so we're going to add up what we have so we're going to add up what we have so we're going to add up what we have for r1 and r2 and what we have for r4 for r1 and r2 and what we have for r4 for r1 and r2 and what we have for r4 and r6 so now in r10 we have the r1 plus and r6 so now in r10 we have the r1 plus and r6 so now in r10 we have the r1 plus r2 plus r4 plus r6 r2 plus r4 plus r6 r2 plus r4 plus r6 now we're going to subtract r3 r5 and r7 now we're going to subtract r3 r5 and r7 now we're going to subtract r3 r5 and r7 from it if we do it one at a time from it if we do it one at a time from it if we do it one at a time that's going to be slow so what we're that's going to be slow so what we're that's going to be slow so what we're going to do is we're going to going to do is we're going to going to do is we're going to add up r3 r5 and r7 and then subtract add up r3 r5 and r7 and then subtract add up r3 r5 and r7 and then subtract that from our r10 that's why we have two that from our r10 that's why we have two that from our r10 that's why we have two adds here so clearly what we're doing adds here so clearly what we're doing adds here so clearly what we're doing here is we're simply adding up r3 here is we're simply adding up r3 here is we're simply adding up r3 r5 r5 r5 and then r7 is added to that and then r7 is added to that and then r7 is added to that so this needs to be r11 so now r11 has so this needs to be r11 so now r11 has so this needs to be r11 so now r11 has r3 plus r5 plus r7 r3 plus r5 plus r7 r3 plus r5 plus r7 and now we subtract that from and now we subtract that from and now we subtract that from r10 r10 r10 and we get the final answer in r10 now and we get the final answer in r10 now and we get the final answer in r10 now let us see how many cycles we need to do let us see how many cycles we need to do let us see how many cycles we need to do this to do that let's draw the this to do that let's draw the this to do that let's draw the dependencies here we have the dependence dependencies here we have the dependence dependencies here we have the dependence again again again like this like this like this so each instruction needed to be done so each instruction needed to be done so each instruction needed to be done right after the previous one is done right after the previous one is done right after the previous one is done here there is no dependence between here there is no dependence between here there is no dependence between these two these two these two there is a dependence here there is a dependence here there is a dependence here and here so the third instruction needs and here so the third instruction needs and here so the third instruction needs to wait for the first two to complete to wait for the first two to complete to wait for the first two to complete there is now no dependence here so this there is now no dependence here so this there is now no dependence here so this one can be done right away even though one can be done right away even though one can be done right away even though it's the fourth instruction in our code it's the fourth instruction in our code it's the fourth instruction in our code and then there is a dependence here and then there is a dependence here and then there is a dependence here and then finally there is a dependence and then finally there is a dependence and then finally there is a dependence here and also our final r10 here needs here and also our final r10 here needs here and also our final r10 here needs to be brought this way so if you look at to be brought this way so if you look at to be brought this way so if you look at what can be done in the first cycle this what can be done in the first cycle this what can be done in the first cycle this can be done in the first cycle and so can be done in the first cycle and so can be done in the first cycle and so can this this cannot because it has to can this this cannot because it has to can this this cannot because it has to wait wait wait this can this can this can so this is also done in the first cycle so this is also done in the first cycle so this is also done in the first cycle and then this has to wait for that and and then this has to wait for that and and then this has to wait for that and so on so on so on so in our first cycle we've already done so in our first cycle we've already done so in our first cycle we've already done three instruction half of the three instruction half of the three instruction half of the instructions that we had in the second instructions that we had in the second instructions that we had in the second cycle we can do this because these two cycle we can do this because these two cycle we can do this because these two complete in the first cycle complete in the first cycle complete in the first cycle we can do this because it only waits for we can do this because it only waits for we can do this because it only waits for this and that's been done and finally in this and that's been done and finally in this and that's been done and finally in the third cycle we have to do this the third cycle we have to do this the third cycle we have to do this because it has to wait for the results because it has to wait for the results because it has to wait for the results of the second cycle so we have completed of the second cycle so we have completed of the second cycle so we have completed our six instructions now in three cycles our six instructions now in three cycles our six instructions now in three cycles and we got an ilp of two which is twice and we got an ilp of two which is twice and we got an ilp of two which is twice as good as we had before the three as good as we had before the three as good as we had before the three height reduction

## Make Independent Instructions Easier to Find： Part 3
Kind: captions Language: en next we look at techniques that make next we look at techniques that make next we look at techniques that make independence instructions is solving for this independence instructions is solving for this independence instructions is solving for this types of fans ideal processor types of fans ideal processor types of fans ideal processor must find and because it is able to look must find and because it is able to look must find and because it is able to look at and infinite number of instructions at and infinite number of instructions at and infinite number of instructions potentially bad the rail processor potentially bad the rail processor potentially bad the rail processor canonici sums instructions the c2000 canonici sums instructions the c2000 canonici sums instructions the c2000 sidera in the payment instructions sidera in the payment instructions sidera in the payment instructions further than the code google glasses okay until further than the code google glasses okay until further than the code google glasses okay until I nothing wannes instructions scheduling I nothing wannes instructions scheduling I nothing wannes instructions scheduling for simple branch specimens is of for simple branch specimens is of for simple branch specimens is of instructions instructions instructions dimming we counter technique specific to dimming we counter technique specific to dimming we counter technique specific to run messes loop and growing and how run messes loop and growing and how run messes loop and growing and how they interact with instructions they interact with instructions they interact with instructions scheduling and then we will brief counter scheduling and then we will brief counter scheduling and then we will brief counter the more powerful technique gold race the more powerful technique gold race the more powerful technique gold race scheduling


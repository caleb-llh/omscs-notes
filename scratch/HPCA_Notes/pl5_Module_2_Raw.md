# Playlist 5 Module 2 Raw Transcript

## Consistency Models： Part 6
Kind: captions Language: en So finally let's take a look at what So finally let's take a look at what So finally let's take a look at what kinds of consistency are there. We know kinds of consistency are there. We know kinds of consistency are there. We know that there is sequential consistency and that there is sequential consistency and that there is sequential consistency and then there is a family of relaxed then there is a family of relaxed then there is a family of relaxed consistency models which are allowed to consistency models which are allowed to consistency models which are allowed to violate one or more of those four violate one or more of those four violate one or more of those four ordering rules. And the models that have ordering rules. And the models that have ordering rules. And the models that have actually been proposed and sometimes actually been proposed and sometimes actually been proposed and sometimes used are called weak consistency, used are called weak consistency, used are called weak consistency, processor consistency, release processor consistency, release processor consistency, release consistency, lazy release consistency, consistency, lazy release consistency, consistency, lazy release consistency, scope consistency, etc. There are scope consistency, etc. There are scope consistency, etc. There are actually many many relax consistency actually many many relax consistency actually many many relax consistency models, too many to actually explain models, too many to actually explain models, too many to actually explain here. But the key to all of them is that here. But the key to all of them is that here. But the key to all of them is that although they allow arbitrary although they allow arbitrary although they allow arbitrary reorderings or maybe some reorderings reorderings or maybe some reorderings reorderings or maybe some reorderings among data operations, all of them among data operations, all of them among data operations, all of them support synchronization operations that support synchronization operations that support synchronization operations that allow you to ensure that the ordering allow you to ensure that the ordering allow you to ensure that the ordering that you really want in your program that you really want in your program that you really want in your program gets to happen. Some of them do it by gets to happen. Some of them do it by gets to happen. Some of them do it by doing something like the mync doing something like the mync doing something like the mync operations. Some of them do it by using operations. Some of them do it by using operations. Some of them do it by using more refined memory synchronization more refined memory synchronization more refined memory synchronization operations. Some of them do it by operations. Some of them do it by operations. Some of them do it by actually calling library functions that actually calling library functions that actually calling library functions that are ensuring such ordering and so

## Consistency Outro： Part 6
Kind: captions Language: en in this lesson we learned about memory in this lesson we learned about memory in this lesson we learned about memory consistency which defines what is consistency which defines what is consistency which defines what is allowed to happen when processor cores allowed to happen when processor cores allowed to happen when processor cores are reordering memory accesses are reordering memory accesses are reordering memory accesses we have seen that sequential consistency we have seen that sequential consistency we have seen that sequential consistency gives us the behavior we expect but that gives us the behavior we expect but that gives us the behavior we expect but that it can limit performance and we have it can limit performance and we have it can limit performance and we have seen that more relaxed memory seen that more relaxed memory seen that more relaxed memory consistency models can improve consistency models can improve consistency models can improve performance but make program behavior performance but make program behavior performance but make program behavior more difficult to reason about

## Many Cores Intro： Part 6
Kind: captions Language: en in this final lesson we will discuss in this final lesson we will discuss in this final lesson we will discuss what happens when we put a lot of cores what happens when we put a lot of cores what happens when we put a lot of cores on the same chip on the same chip on the same chip and we will apply and put together a lot and we will apply and put together a lot and we will apply and put together a lot of things we have already learned

## Many Core Challenges： Part 6
Kind: captions Language: en so in this lesson so in this lesson so in this lesson we will go through several challenges we will go through several challenges we will go through several challenges that a many core processor that is a that a many core processor that is a that a many core processor that is a processor with a very large number of processor with a very large number of processor with a very large number of cores will face cores will face cores will face the first challenge is that as we grow the first challenge is that as we grow the first challenge is that as we grow the number of cores the number of cores the number of cores the coherence traffic between this the coherence traffic between this the coherence traffic between this course goes up and that is because we course goes up and that is because we course goes up and that is because we have already seen that rights to shared have already seen that rights to shared have already seen that rights to shared memory locations result in invalidations memory locations result in invalidations memory locations result in invalidations and subsequent misses on those locations and subsequent misses on those locations and subsequent misses on those locations and both invalidations and the resulting and both invalidations and the resulting and both invalidations and the resulting misses misses misses go onto the shared bus and thus add to go onto the shared bus and thus add to go onto the shared bus and thus add to this coherent traffic this coherent traffic this coherent traffic as we increase the number of cores we as we increase the number of cores we as we increase the number of cores we get more writes per second and as a get more writes per second and as a get more writes per second and as a result result result the bus throughput that we need goes up the bus throughput that we need goes up the bus throughput that we need goes up until it eventually exceeds what we can until it eventually exceeds what we can until it eventually exceeds what we can do on a bus do on a bus do on a bus unfortunately the bus unfortunately the bus unfortunately the bus allows us to only do one request at a allows us to only do one request at a allows us to only do one request at a time in part time in part time in part that is needed because we rely on the that is needed because we rely on the that is needed because we rely on the bus to produce the easy ordering that we bus to produce the easy ordering that we bus to produce the easy ordering that we get between writes to maintain coherence get between writes to maintain coherence get between writes to maintain coherence but that means that really the bus but that means that really the bus but that means that really the bus eventually becomes the bottleneck eventually becomes the bottleneck eventually becomes the bottleneck so what we need so what we need so what we need is a scalable is a scalable is a scalable on-chip network that allows the traffic on-chip network that allows the traffic on-chip network that allows the traffic to grow as the number of course grows to grow as the number of course grows to grow as the number of course grows and then we also need to use directory and then we also need to use directory and then we also need to use directory coherence coherence coherence so that we don't rely so that we don't rely so that we don't rely on the bus on the bus on the bus to serialize everything for us

## Network On Chip： Part 6
Kind: captions Language: en so let's look at what a network on chip so let's look at what a network on chip so let's look at what a network on chip might look like let's say we have some might look like let's say we have some might look like let's say we have some sort of a square-ish tile that has a sort of a square-ish tile that has a sort of a square-ish tile that has a core and maybe a level one cache and core and maybe a level one cache and core and maybe a level one cache and such tiles normally will be connected to such tiles normally will be connected to such tiles normally will be connected to a shared bus now let's say that these a shared bus now let's say that these a shared bus now let's say that these four cores are using half of the four cores are using half of the four cores are using half of the available trut on the bus because their available trut on the bus because their available trut on the bus because their cash misses and coherence need to be cash misses and coherence need to be cash misses and coherence need to be taken care of through the bus now let's taken care of through the bus now let's taken care of through the bus now let's say we add another set of those cores say we add another set of those cores say we add another set of those cores pretty much the same thing just pretty much the same thing just pretty much the same thing just replicated now we have an even longer replicated now we have an even longer replicated now we have an even longer thus possibly a slower bus that all thus possibly a slower bus that all thus possibly a slower bus that all traffic from all these eight cores goes traffic from all these eight cores goes traffic from all these eight cores goes to so we got a slower bus probably with to so we got a slower bus probably with to so we got a slower bus probably with less throughput than the original small less throughput than the original small less throughput than the original small one had yet it has twice the demand for one had yet it has twice the demand for one had yet it has twice the demand for traffic so very quickly it will get traffic so very quickly it will get traffic so very quickly it will get saturated now let's consider a type of saturated now let's consider a type of saturated now let's consider a type of onchip network that is called a mesh so onchip network that is called a mesh so onchip network that is called a mesh so here what we have is these core tiles here what we have is these core tiles here what we have is these core tiles are individual are individual are individual connected like this for example now connected like this for example now connected like this for example now there is no more broadcast but this core there is no more broadcast but this core there is no more broadcast but this core can still send a message to any other can still send a message to any other can still send a message to any other core if we send a message to this this core if we send a message to this this core if we send a message to this this is the link that gets used meanwhile is the link that gets used meanwhile is the link that gets used meanwhile this core could be sending a message this core could be sending a message this core could be sending a message here if we need to send a message from here if we need to send a message from here if we need to send a message from here to here then these two links get here to here then these two links get here to here then these two links get used meanwhile we can get these two used meanwhile we can get these two used meanwhile we can get these two links used independently so as you can links used independently so as you can links used independently so as you can see the total available throughput in see the total available throughput in see the total available throughput in the entire network is several times the the entire network is several times the the entire network is several times the throughput of this individual link link throughput of this individual link link throughput of this individual link link and because it's a short link we can and because it's a short link we can and because it's a short link we can have a very large throughput now I'm have a very large throughput now I'm have a very large throughput now I'm going to take this 2x two mesh and copy going to take this 2x two mesh and copy going to take this 2x two mesh and copy it so that I get four of those and it so that I get four of those and it so that I get four of those and create a 4x4 mesh that now supports 16 create a 4x4 mesh that now supports 16 create a 4x4 mesh that now supports 16 cores twice as many as here note how cores twice as many as here note how cores twice as many as here note how many new links are there now if we have many new links are there now if we have many new links are there now if we have neighbor to neighbor communication we neighbor to neighbor communication we neighbor to neighbor communication we can have these two talking these two can have these two talking these two can have these two talking these two talking these two talking all over talking these two talking all over talking these two talking all over independent links so as I increase the independent links so as I increase the independent links so as I increase the number of cores I also increase the number of cores I also increase the number of cores I also increase the number of these links naturally so that number of these links naturally so that number of these links naturally so that the total trut available in this network the total trut available in this network the total trut available in this network grows with the number of cores so the grows with the number of cores so the grows with the number of cores so the number of cores grows the number of number of cores grows the number of number of cores grows the number of links grows and that allows the links grows and that allows the links grows and that allows the available through put to grow and that available through put to grow and that available through put to grow and that means that I can have many more cores means that I can have many more cores means that I can have many more cores than I could on the bus there are many than I could on the bus there are many than I could on the bus there are many types of this so-called point to point types of this so-called point to point types of this so-called point to point networks a mesh is one of those that is networks a mesh is one of those that is networks a mesh is one of those that is very good for chip building because none very good for chip building because none very good for chip building because none of these links intersect each other so of these links intersect each other so of these links intersect each other so because chips are basically made by because chips are basically made by because chips are basically made by printing things on Silicon it's printing things on Silicon it's printing things on Silicon it's naturally good to have something that is naturally good to have something that is naturally good to have something that is kind of flat like this and doesn't have kind of flat like this and doesn't have kind of flat like this and doesn't have links going across different links going across different links going across different nodes but we can build chips with some nodes but we can build chips with some nodes but we can build chips with some amount of links Crossing each other and amount of links Crossing each other and amount of links Crossing each other and thus we can have a mesh which is kind of thus we can have a mesh which is kind of thus we can have a mesh which is kind of like what we have seen we can have have like what we have seen we can have have like what we have seen we can have have a Taurus Network you build a Taurus by a Taurus Network you build a Taurus by a Taurus Network you build a Taurus by taking the mesh and connecting the end taking the mesh and connecting the end taking the mesh and connecting the end points to each other so the Taurus points to each other so the Taurus points to each other so the Taurus really takes the link and kind of wraps really takes the link and kind of wraps really takes the link and kind of wraps it around and then wraps it around and it around and then wraps it around and it around and then wraps it around and so on and then you do that in the other so on and then you do that in the other so on and then you do that in the other dimension tool you can think of these dimension tool you can think of these dimension tool you can think of these processors as basically things on a processors as basically things on a processors as basically things on a donut you would take this wrap it around donut you would take this wrap it around donut you would take this wrap it around you get a cylinder then you take the end you get a cylinder then you take the end you get a cylinder then you take the end points of the cylinder bend it and points of the cylinder bend it and points of the cylinder bend it and create a d so that's why it's called a create a d so that's why it's called a create a d so that's why it's called a Taurus but in reality what you really do Taurus but in reality what you really do Taurus but in reality what you really do is you do this have a long link that is you do this have a long link that is you do this have a long link that crosses here and a long link that crosses here and a long link that crosses here and a long link that crosses here and so on and then you also crosses here and so on and then you also crosses here and so on and then you also take this and create a long link that take this and create a long link that take this and create a long link that goes here so that's how you create a goes here so that's how you create a goes here so that's how you create a Taurus and then there are even fancier Taurus and then there are even fancier Taurus and then there are even fancier networks that are still reasonably good networks that are still reasonably good networks that are still reasonably good for on chip implementation such as for on chip implementation such as for on chip implementation such as flattened butterfly if you're interested flattened butterfly if you're interested flattened butterfly if you're interested in how this work a very good place to in how this work a very good place to in how this work a very good place to start would be an advanced class on on start would be an advanced class on on start would be an advanced class on on chip interconnects

## Mesh vs Bus Throughput Quiz： Part 6
Kind: captions Language: en let's see if we can compare the mesh and let's see if we can compare the mesh and let's see if we can compare the mesh and the bus throughput for a small four core the bus throughput for a small four core the bus throughput for a small four core system where the traffic is evenly system where the traffic is evenly system where the traffic is evenly distributed so that each core sends a distributed so that each core sends a distributed so that each core sends a quarter of all the messages in the quarter of all the messages in the quarter of all the messages in the system and the messages that each core system and the messages that each core system and the messages that each core sends are sent round robbing to all the sends are sent round robbing to all the sends are sent round robbing to all the other three cores so if we are core zero other three cores so if we are core zero other three cores so if we are core zero we send messages to one then two then we send messages to one then two then we send messages to one then two then three then one then two then three Etc three then one then two then three Etc three then one then two then three Etc if we're core two then we send to 0 1 3 if we're core two then we send to 0 1 3 if we're core two then we send to 0 1 3 0 1 3 Etc and you can assume that this 0 1 3 Etc and you can assume that this 0 1 3 Etc and you can assume that this is totally randomly distributed so that is totally randomly distributed so that is totally randomly distributed so that we don't necessarily get any we don't necessarily get any we don't necessarily get any synchronicity among these axises pretty synchronicity among these axises pretty synchronicity among these axises pretty much just like a third of the traffic much just like a third of the traffic much just like a third of the traffic sent by each core goes to each of the sent by each core goes to each of the sent by each core goes to each of the other core but you can assume that it other core but you can assume that it other core but you can assume that it happens willy-nilly each core sends 10 happens willy-nilly each core sends 10 happens willy-nilly each core sends 10 million messages per second the bus can million messages per second the bus can million messages per second the bus can support 20 million messages per second support 20 million messages per second support 20 million messages per second the mesh can support 20 million messages the mesh can support 20 million messages the mesh can support 20 million messages per second per link and obviously the per second per link and obviously the per second per link and obviously the four cores are going to saturate the bus four cores are going to saturate the bus four cores are going to saturate the bus bandwidth in which case they will just bandwidth in which case they will just bandwidth in which case they will just have to slow down proportionally so that have to slow down proportionally so that have to slow down proportionally so that they match the bandwidth of the bus the they match the bandwidth of the bus the they match the bandwidth of the bus the question for you is the speed up question for you is the speed up question for you is the speed up achieved by switching to the mesh from achieved by switching to the mesh from achieved by switching to the mesh from the bus is what

## Mesh vs Bus Thoughput Quiz Solution： Part 6
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our mesh versus bus throughput quiz where we mesh versus bus throughput quiz where we mesh versus bus throughput quiz where we had four cores had four cores had four cores with a totally random and even with a totally random and even with a totally random and even distribution of traffic among the cores distribution of traffic among the cores distribution of traffic among the cores we are given what each core is sending we are given what each core is sending we are given what each core is sending we are getting what the bus can do and we are getting what the bus can do and we are getting what the bus can do and what the link in a mesh can do and now what the link in a mesh can do and now what the link in a mesh can do and now we are asked how much faster is it we are asked how much faster is it we are asked how much faster is it on the bus versus on the mesh our bus on the bus versus on the mesh our bus on the bus versus on the mesh our bus cannot sustain the 40 million messages cannot sustain the 40 million messages cannot sustain the 40 million messages per second per second per second that the cores are trying to give it that the cores are trying to give it that the cores are trying to give it so the cores are really going to slow so the cores are really going to slow so the cores are really going to slow down to half of their speed so that the down to half of their speed so that the down to half of their speed so that the bus bandwidth demand matches what it can bus bandwidth demand matches what it can bus bandwidth demand matches what it can provide provide provide so on the bus we get 2x the execution so on the bus we get 2x the execution so on the bus we get 2x the execution time that each core is capable of time that each core is capable of time that each core is capable of providing on the mesh we can support 20 providing on the mesh we can support 20 providing on the mesh we can support 20 million messages per second per link million messages per second per link million messages per second per link so now the question is do our links get so now the question is do our links get so now the question is do our links get saturated so let's see what we get on a saturated so let's see what we get on a saturated so let's see what we get on a mesh like this in terms of traffic each mesh like this in terms of traffic each mesh like this in terms of traffic each quarter is generating 10 million quarter is generating 10 million quarter is generating 10 million messages 3.3 million are going to each messages 3.3 million are going to each messages 3.3 million are going to each other core other core other core so of the 10 million messages sent here so of the 10 million messages sent here so of the 10 million messages sent here one third are going to this score one one third are going to this score one one third are going to this score one third are going through this link and third are going through this link and third are going through this link and then one third are going here let's say then one third are going here let's say then one third are going here let's say that one sixth go this way one sixth go that one sixth go this way one sixth go that one sixth go this way one sixth go this way so this link will be used for this way so this link will be used for this way so this link will be used for one sixth of the messages sent by this one sixth of the messages sent by this one sixth of the messages sent by this score and this one for another one sixth score and this one for another one sixth score and this one for another one sixth and then we add 1 6 to each of these and then we add 1 6 to each of these and then we add 1 6 to each of these which brings us to one half of 10 which brings us to one half of 10 which brings us to one half of 10 million messages so now we are done with million messages so now we are done with million messages so now we are done with this score what happens from this score this score what happens from this score this score what happens from this score same thing this link gets one half of same thing this link gets one half of same thing this link gets one half of the messages one third are going here the messages one third are going here the messages one third are going here one 6 are going here one 6 are going here one 6 are going here so this link suffers one half of this so this link suffers one half of this so this link suffers one half of this traffic again now this link gets traffic again now this link gets traffic again now this link gets one half of the traffic from this core one half of the traffic from this core one half of the traffic from this core and these two links get one sixth each and these two links get one sixth each and these two links get one sixth each and now we're done with this score now and now we're done with this score now and now we're done with this score now what happens when this score is sending what happens when this score is sending what happens when this score is sending one half of the messages go this way one one half of the messages go this way one one half of the messages go this way one half going go this way half going go this way half going go this way one sixth of them go through this link one sixth of them go through this link one sixth of them go through this link and one six go through this link done and one six go through this link done and one six go through this link done with this score and finally this score with this score and finally this score with this score and finally this score sends messages half here have here sends messages half here have here sends messages half here have here and then one sixth here and then one sixth here and then one sixth here and one sixth here now we're done with and one sixth here now we're done with and one sixth here now we're done with this course so as you can see our links this course so as you can see our links this course so as you can see our links are actually evenly used are actually evenly used are actually evenly used each of them gets two times one-half each of them gets two times one-half each of them gets two times one-half plus two-sixths so we get plus two-sixths so we get plus two-sixths so we get one and one-third one and one-third one and one-third times times times the traffic generated by one core that the traffic generated by one core that the traffic generated by one core that was what the one-half was off the was what the one-half was off the was what the one-half was off the traffic from one core is 10 million traffic from one core is 10 million traffic from one core is 10 million messages a second so we're really messages a second so we're really messages a second so we're really getting 10 getting 10 getting 10 plus 3.3 so this is 13.3 million of plus 3.3 so this is 13.3 million of plus 3.3 so this is 13.3 million of messages per second which is available messages per second which is available messages per second which is available on our mesh so the mesh doesn't slow on our mesh so the mesh doesn't slow on our mesh so the mesh doesn't slow down the processors down the processors down the processors so really the speed up of using the mesh so really the speed up of using the mesh so really the speed up of using the mesh versus the bus versus the bus versus the bus is two because the bus requires the is two because the bus requires the is two because the bus requires the cores to slow down so that cores to slow down so that cores to slow down so that it can support the traffic that they're it can support the traffic that they're it can support the traffic that they're generating generating generating while the mesh allows the cores to while the mesh allows the cores to while the mesh allows the cores to proceed at full speed at 10 million proceed at full speed at 10 million proceed at full speed at 10 million messages per second and still has some messages per second and still has some messages per second and still has some traffic left over traffic left over traffic left over if we went to a larger network than four if we went to a larger network than four if we went to a larger network than four cores the bus would have to cores the bus would have to cores the bus would have to proportionally slow down while the mesh proportionally slow down while the mesh proportionally slow down while the mesh would get more links would get more links would get more links and it will be a while until any of the and it will be a while until any of the and it will be a while until any of the mesh links become saturated so clearly mesh links become saturated so clearly mesh links become saturated so clearly the mesh is far superior to the bus in the mesh is far superior to the bus in the mesh is far superior to the bus in terms of the traffic it can support when terms of the traffic it can support when terms of the traffic it can support when we go to more course

## Many Core Challenges 2： Part 6
Kind: captions Language: en so going back to our many core so going back to our many core so going back to our many core challenges we have seen that when the challenges we have seen that when the challenges we have seen that when the number of cores grows number of cores grows number of cores grows so does the coherence traffic on the so does the coherence traffic on the so does the coherence traffic on the chip chip chip and for that we needed a scalable and for that we needed a scalable and for that we needed a scalable on-chip network such as a mesh on-chip network such as a mesh on-chip network such as a mesh and the directory coherence which we and the directory coherence which we and the directory coherence which we have already seen in our cache coherence have already seen in our cache coherence have already seen in our cache coherence lesson lesson lesson another thing another thing another thing that having more cores that having more cores that having more cores will make more difficult is that as the will make more difficult is that as the will make more difficult is that as the number of cores goes up number of cores goes up number of cores goes up the off-chip traffic that we need goes the off-chip traffic that we need goes the off-chip traffic that we need goes up to have decent performance up to have decent performance up to have decent performance we will as we increase the number of we will as we increase the number of we will as we increase the number of cores increase the number of on-chip cores increase the number of on-chip cores increase the number of on-chip caches so if we have four cores we have caches so if we have four cores we have caches so if we have four cores we have four level one and possibly level 2 four level one and possibly level 2 four level one and possibly level 2 caches when we have 64 cores we'll have caches when we have 64 cores we'll have caches when we have 64 cores we'll have 64 level 1 and level 2 caches 64 level 1 and level 2 caches 64 level 1 and level 2 caches so each so each so each core core core doesn't really generate more misses but doesn't really generate more misses but doesn't really generate more misses but it generates the same number of misses it generates the same number of misses it generates the same number of misses regardless of how many cores we have so regardless of how many cores we have so regardless of how many cores we have so we have the same number of misses per we have the same number of misses per we have the same number of misses per core so as we have more cores the number core so as we have more cores the number core so as we have more cores the number of memory requests of memory requests of memory requests which is what we do when we have a miss which is what we do when we have a miss which is what we do when we have a miss would go up in proportion to the number would go up in proportion to the number would go up in proportion to the number of cores of cores of cores now note that the number of pins the now note that the number of pins the now note that the number of pins the number of those little wires sticking number of those little wires sticking number of those little wires sticking out of a chip out of a chip out of a chip does go up slowly does go up slowly does go up slowly but not in proportion or anywhere close but not in proportion or anywhere close but not in proportion or anywhere close to in proportion to the number of cores to in proportion to the number of cores to in proportion to the number of cores so if we double the number of cores so if we double the number of cores so if we double the number of cores possibly we get maybe 10 percent more possibly we get maybe 10 percent more possibly we get maybe 10 percent more pins but that's it pins but that's it pins but that's it because these pins have to physically be because these pins have to physically be because these pins have to physically be large enough to not break when we are large enough to not break when we are large enough to not break when we are moving the chip around moving the chip around moving the chip around and plugging it into a motherboard so and plugging it into a motherboard so and plugging it into a motherboard so what happens is we get some little what happens is we get some little what happens is we get some little improvement in off-chip throughput while improvement in off-chip throughput while improvement in off-chip throughput while our demand for it basically grows in our demand for it basically grows in our demand for it basically grows in proportion to the number of cores so our proportion to the number of cores so our proportion to the number of cores so our off-chip available throughput becomes a off-chip available throughput becomes a off-chip available throughput becomes a bottleneck so we need to reduce the bottleneck so we need to reduce the bottleneck so we need to reduce the number of memory requests that we number of memory requests that we number of memory requests that we generate per core and the way we do that generate per core and the way we do that generate per core and the way we do that is to make the last level cache which in is to make the last level cache which in is to make the last level cache which in most today's processors is the level most today's processors is the level most today's processors is the level three cache three cache three cache shared so that all cores go to that shared so that all cores go to that shared so that all cores go to that cache and its size is shared equally by cache and its size is shared equally by cache and its size is shared equally by all the cores and the size of that cache all the cores and the size of that cache all the cores and the size of that cache needs to go up in proportion to the needs to go up in proportion to the needs to go up in proportion to the number of cores as it goes up so as we number of cores as it goes up so as we number of cores as it goes up so as we double the number of cores we should at double the number of cores we should at double the number of cores we should at least double the size of the last level least double the size of the last level least double the size of the last level cache but there is a problem to having cache but there is a problem to having cache but there is a problem to having one huge llc like this one huge llc like this one huge llc like this such a large cache would be slow and if such a large cache would be slow and if such a large cache would be slow and if it's really one single cache it would it's really one single cache it would it's really one single cache it would have one entry point where we enter the have one entry point where we enter the have one entry point where we enter the address and expect the data out address and expect the data out address and expect the data out and that entry point needs to be and that entry point needs to be and that entry point needs to be somewhere on our chip that now possibly somewhere on our chip that now possibly somewhere on our chip that now possibly has a mesh or another advanced network has a mesh or another advanced network has a mesh or another advanced network and that means that that one point and that means that that one point and that means that that one point will be where we send all of the level will be where we send all of the level will be where we send all of the level two misses so it would become a two misses so it would become a two misses so it would become a bottleneck pretty much now we don't get bottleneck pretty much now we don't get bottleneck pretty much now we don't get to use all of the links equally this set to use all of the links equally this set to use all of the links equally this set of links that go to this entry point for of links that go to this entry point for of links that go to this entry point for the cache would get much more traffic the cache would get much more traffic the cache would get much more traffic than the other links if we double the than the other links if we double the than the other links if we double the number of cores then those links around number of cores then those links around number of cores then those links around the entry point get double the traffic the entry point get double the traffic the entry point get double the traffic so what we do so what we do so what we do is we don't have just one big last level is we don't have just one big last level is we don't have just one big last level cache we have what is called a cache we have what is called a cache we have what is called a distributed last level cache now let's distributed last level cache now let's distributed last level cache now let's see what that looks like

## Distributed LLC： Part 6
Kind: captions Language: en so a distributed last level cache is so a distributed last level cache is so a distributed last level cache is logically a single cache logically a single cache logically a single cache in that a data block will not be in that a data block will not be in that a data block will not be replicated replicated replicated like it would be in private caches where like it would be in private caches where like it would be in private caches where if two or three or four cores for if two or three or four cores for if two or three or four cores for example read a block each of their example read a block each of their example read a block each of their caches would have that block and thus caches would have that block and thus caches would have that block and thus really the total capacity of the caches really the total capacity of the caches really the total capacity of the caches is not how much memory we are covering is not how much memory we are covering is not how much memory we are covering because some memory appears many times because some memory appears many times because some memory appears many times possibly in these caches possibly in these caches possibly in these caches a last level cache that is distributed a last level cache that is distributed a last level cache that is distributed is logically still one cache is logically still one cache is logically still one cache meaning if its capacity is let's say meaning if its capacity is let's say meaning if its capacity is let's say four megabytes then we really cover four four megabytes then we really cover four four megabytes then we really cover four megabytes of memory without any megabytes of memory without any megabytes of memory without any replication replication replication but this distributed cache is sliced up but this distributed cache is sliced up but this distributed cache is sliced up so that each tile which contains a core so that each tile which contains a core so that each tile which contains a core and possibly local caches now also and possibly local caches now also and possibly local caches now also contains a part of this cache so now contains a part of this cache so now contains a part of this cache so now what we have is what we have is what we have is in our mesh in our mesh in our mesh we have a tile we have a tile we have a tile that contains a core a level one cache a that contains a core a level one cache a that contains a core a level one cache a level two cache and a slice of the l3 level two cache and a slice of the l3 level two cache and a slice of the l3 cache cache cache so if this slice here that we use per so if this slice here that we use per so if this slice here that we use per core is let's say one megabyte core is let's say one megabyte core is let's say one megabyte is really n times one megabytes where n is really n times one megabytes where n is really n times one megabytes where n is the number of cores so now as you can is the number of cores so now as you can is the number of cores so now as you can see see see if we use four cores we get four if we use four cores we get four if we use four cores we get four megabytes with 16 cores we have 16 megabytes with 16 cores we have 16 megabytes with 16 cores we have 16 megabytes so we get what we want megabytes so we get what we want megabytes so we get what we want the big cache the big cache the big cache that is there to prevent memory accesses that is there to prevent memory accesses that is there to prevent memory accesses is growing with the number of cores but is growing with the number of cores but is growing with the number of cores but it doesn't have a single entry point in it doesn't have a single entry point in it doesn't have a single entry point in fact each slice has its own entry point fact each slice has its own entry point fact each slice has its own entry point so if we want the data that is in this so if we want the data that is in this so if we want the data that is in this slice we have to route our message to slice we have to route our message to slice we have to route our message to that slice that slice that slice if we want the data that resides in if we want the data that resides in if we want the data that resides in another slice we send a message there another slice we send a message there another slice we send a message there if the data is spread among the slices if the data is spread among the slices if the data is spread among the slices so that so that so that all of them get equal demands for the all of them get equal demands for the all of them get equal demands for the data then the network traffic would be data then the network traffic would be data then the network traffic would be nicely distributed across our chip so nicely distributed across our chip so nicely distributed across our chip so that none of the links get over utilized that none of the links get over utilized that none of the links get over utilized but if we have a level two miss but if we have a level two miss but if we have a level two miss in a particular core how do we know in a particular core how do we know in a particular core how do we know which slice of the level three cache which slice of the level three cache which slice of the level three cache to ask for the data one simple solution to ask for the data one simple solution to ask for the data one simple solution is to spread the cash round robin among is to spread the cash round robin among is to spread the cash round robin among slices by cash index so if we have our slices by cash index so if we have our slices by cash index so if we have our level 3 cash with let's say 1024 sets level 3 cash with let's say 1024 sets level 3 cash with let's say 1024 sets slice 0 gets set 0 slice 1 gets set 1 slice 0 gets set 0 slice 1 gets set 1 slice 0 gets set 0 slice 1 gets set 1 etc and then let's say that we have 8 etc and then let's say that we have 8 etc and then let's say that we have 8 cores then slice 7 gets set 7. slice 0 cores then slice 7 gets set 7. slice 0 cores then slice 7 gets set 7. slice 0 gets set 8 set 9 is in slice 1 again and gets set 8 set 9 is in slice 1 again and gets set 8 set 9 is in slice 1 again and so on so now we can relatively easily so on so now we can relatively easily so on so now we can relatively easily after we break down the address to after we break down the address to after we break down the address to access the cache we know the set number access the cache we know the set number access the cache we know the set number and and and the least significant the least significant the least significant three bits in this case tell us which three bits in this case tell us which three bits in this case tell us which slice to ask if we have slice to ask if we have slice to ask if we have even more cores then the number of sets even more cores then the number of sets even more cores then the number of sets in the cache will grow and as a result in the cache will grow and as a result in the cache will grow and as a result the number of bits the number of bits the number of bits that we need to tell us which slice we that we need to tell us which slice we that we need to tell us which slice we have is growing and that means that the have is growing and that means that the have is growing and that means that the number of bits in the index are growing number of bits in the index are growing number of bits in the index are growing and we will need additional bits to tell and we will need additional bits to tell and we will need additional bits to tell us in which set to look note that if we us in which set to look note that if we us in which set to look note that if we sequentially access data then we will be sequentially access data then we will be sequentially access data then we will be really looking at you know one set and really looking at you know one set and really looking at you know one set and then the next set and then the next set then the next set and then the next set then the next set and then the next set and this nicely spreads the load among and this nicely spreads the load among and this nicely spreads the load among the slices of the level three cache the slices of the level three cache the slices of the level three cache because sequential accesses are because sequential accesses are because sequential accesses are basically going to be spread across the basically going to be spread across the basically going to be spread across the entire chip however the spreading of entire chip however the spreading of entire chip however the spreading of blocks round robin blocks round robin blocks round robin across the entire chip may not be good across the entire chip may not be good across the entire chip may not be good for locality for locality for locality because a core is just as likely to because a core is just as likely to because a core is just as likely to access something that is in the opposite access something that is in the opposite access something that is in the opposite corner of the chip as it is to access corner of the chip as it is to access corner of the chip as it is to access its own slice in which case there is no its own slice in which case there is no its own slice in which case there is no on-chip network traffic whatsoever on-chip network traffic whatsoever on-chip network traffic whatsoever so another way that can help with that so another way that can help with that so another way that can help with that is that we spread the data around but is that we spread the data around but is that we spread the data around but this time we spread it by page number so this time we spread it by page number so this time we spread it by page number so all of the blocks that belong to the all of the blocks that belong to the all of the blocks that belong to the same page would end up in the same slice same page would end up in the same slice same page would end up in the same slice of the level three that helps because of the level three that helps because of the level three that helps because now the operating system can map pages now the operating system can map pages now the operating system can map pages so that it makes the accesses to l3 more so that it makes the accesses to l3 more so that it makes the accesses to l3 more local for example here this score is local for example here this score is local for example here this score is running a thread that has a stack running a thread that has a stack running a thread that has a stack the pages that belong to that stack the pages that belong to that stack the pages that belong to that stack should be those that end up in this should be those that end up in this should be those that end up in this slice so that if we have a level two slice so that if we have a level two slice so that if we have a level two miss we are very likely to access our miss we are very likely to access our miss we are very likely to access our own local slice of the l3 cache of own local slice of the l3 cache of own local slice of the l3 cache of course course course some of the shared data will be us some of the shared data will be us some of the shared data will be us elsewhere but this dramatically improves elsewhere but this dramatically improves elsewhere but this dramatically improves the locality in terms of how far do we the locality in terms of how far do we the locality in terms of how far do we need to go to get to the correct slice need to go to get to the correct slice need to go to get to the correct slice of l3 of l3 of l3 any data that tends to be accessed by any data that tends to be accessed by any data that tends to be accessed by one core can be now mapped by the one core can be now mapped by the one core can be now mapped by the operating system so that it's usually operating system so that it's usually operating system so that it's usually found in the local slice of the last found in the local slice of the last found in the local slice of the last level cache

## Distributed LLC Quiz： Part 6
Kind: captions Language: en so let's see if we can figure out how so let's see if we can figure out how so let's see if we can figure out how the distributed last level cache works the distributed last level cache works the distributed last level cache works let's say we have a process chip with 16 let's say we have a process chip with 16 let's say we have a process chip with 16 cores organized is a four by four mesh cores organized is a four by four mesh cores organized is a four by four mesh like this the cores along with their like this the cores along with their like this the cores along with their level one and level two caches and a level one and level two caches and a level one and level two caches and a slice of l3 are connected like this so a slice of l3 are connected like this so a slice of l3 are connected like this so a single tile here in this mesh has a core single tile here in this mesh has a core single tile here in this mesh has a core a level one level two cache private to a level one level two cache private to a level one level two cache private to that core and a slice of the shared last that core and a slice of the shared last that core and a slice of the shared last level level three cash and that level level level three cash and that level level level three cash and that level three cash is eight megabytes in size three cash is eight megabytes in size three cash is eight megabytes in size has a 256 byte block size is 16 way set has a 256 byte block size is 16 way set has a 256 byte block size is 16 way set associative and is distributed among the associative and is distributed among the associative and is distributed among the slices round-robin by set number if core slices round-robin by set number if core slices round-robin by set number if core zero issues a load from address one two zero issues a load from address one two zero issues a load from address one two three four five six seven eight hex and three four five six seven eight hex and three four five six seven eight hex and if this load is a missing level one and if this load is a missing level one and if this load is a missing level one and in level two we send the level three in level two we send the level three in level two we send the level three requests to what tile

## Distributed LLC Quiz Solution： Part 6
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our distributed last level cache we have 16 distributed last level cache we have 16 distributed last level cache we have 16 cores in a four by four mesh it doesn't cores in a four by four mesh it doesn't cores in a four by four mesh it doesn't really matter what the organization of really matter what the organization of really matter what the organization of the network is we need to know that the network is we need to know that the network is we need to know that there are really 16 tiles there are really 16 tiles there are really 16 tiles numbered 0 through 15 numbered 0 through 15 numbered 0 through 15 and that we have an 8 megabyte cache and that we have an 8 megabyte cache and that we have an 8 megabyte cache with 256 byte block 16 way set with 256 byte block 16 way set with 256 byte block 16 way set associative distributed round robin by associative distributed round robin by associative distributed round robin by set so set 0 will be in slice 0 set 1 set so set 0 will be in slice 0 set 1 set so set 0 will be in slice 0 set 1 will be in slice 1 etc and then it just will be in slice 1 etc and then it just will be in slice 1 etc and then it just wraps around so let's look at wraps around so let's look at wraps around so let's look at what this address maps to what this address maps to what this address maps to the least significant the least significant the least significant 8 bits are telling us what the block 8 bits are telling us what the block 8 bits are telling us what the block offset is offset is offset is the next so many bits are going to be the next so many bits are going to be the next so many bits are going to be the index and the least significant bits the index and the least significant bits the index and the least significant bits of the index are going to tell us which of the index are going to tell us which of the index are going to tell us which tile to look at tile to look at tile to look at there are 16 tiles there are 16 tiles there are 16 tiles so the next digit is really the tile so the next digit is really the tile so the next digit is really the tile number number number and that means and that means and that means that we go to tile number six that we go to tile number six that we go to tile number six note that we don't really need to even note that we don't really need to even note that we don't really need to even compute what exactly is the index compute what exactly is the index compute what exactly is the index because the least significant bits in because the least significant bits in because the least significant bits in this case are telling us this case are telling us this case are telling us what we need to know

## Many Core Challenges 2： Part 6
Kind: captions Language: en now going back to our many core now going back to our many core now going back to our many core challenges we have seen that the number challenges we have seen that the number challenges we have seen that the number of cores as it grows makes the coherence of cores as it grows makes the coherence of cores as it grows makes the coherence traffic grow and we looked at solutions traffic grow and we looked at solutions traffic grow and we looked at solutions to deal with that we then looked at the to deal with that we then looked at the to deal with that we then looked at the problem of the off-chip traffic also problem of the off-chip traffic also problem of the off-chip traffic also growing with the number of course and we growing with the number of course and we growing with the number of course and we just saw that a large shared but just saw that a large shared but just saw that a large shared but distributed last level cache can help distributed last level cache can help distributed last level cache can help with this problem the next problem we with this problem the next problem we with this problem the next problem we face is that the coherence directory face is that the coherence directory face is that the coherence directory that we need is just way too large to that we need is just way too large to that we need is just way too large to fit on chip because we have a scalable fit on chip because we have a scalable fit on chip because we have a scalable onship network and no longer a bus we onship network and no longer a bus we onship network and no longer a bus we need directory coherence but a need directory coherence but a need directory coherence but a traditional directory has one entry for traditional directory has one entry for traditional directory has one entry for each possible memory block the memory each possible memory block the memory each possible memory block the memory can be many gigabytes in size that might can be many gigabytes in size that might can be many gigabytes in size that might require even billions of directory require even billions of directory require even billions of directory entries and that won't fit on ship so entries and that won't fit on ship so entries and that won't fit on ship so how do we deal with that problem

## On Chip Directory： Part 6
Kind: captions Language: en so we need a directory that will fit on so we need a directory that will fit on so we need a directory that will fit on chip otherwise to maintain coherence chip otherwise to maintain coherence chip otherwise to maintain coherence between our caches that are very very between our caches that are very very between our caches that are very very fast we would need to look up a fast we would need to look up a fast we would need to look up a directory in very very slow memory which directory in very very slow memory which directory in very very slow memory which kind of defeats the purpose of having kind of defeats the purpose of having kind of defeats the purpose of having these caches some of the questions about these caches some of the questions about these caches some of the questions about this directory are easy to answer like this directory are easy to answer like this directory are easy to answer like for example where is the home node where for example where is the home node where for example where is the home node where we go to find the directory information we go to find the directory information we go to find the directory information about the block and the answer to that about the block and the answer to that about the block and the answer to that is the same tile that has the last level is the same tile that has the last level is the same tile that has the last level cache slice where that block would be cache slice where that block would be cache slice where that block would be because if the directory entry for that because if the directory entry for that because if the directory entry for that block indicates that none of the private block indicates that none of the private block indicates that none of the private caches have it then we want to look up caches have it then we want to look up caches have it then we want to look up the last level cache to find that block the last level cache to find that block the last level cache to find that block so we might as well do that in the same so we might as well do that in the same so we might as well do that in the same node that has a directory otherwise node that has a directory otherwise node that has a directory otherwise there will be a lot of traffic between there will be a lot of traffic between there will be a lot of traffic between where the directory for a block is and where the directory for a block is and where the directory for a block is and where the last level cache for the block where the last level cache for the block where the last level cache for the block is so naturally they should be on the is so naturally they should be on the is so naturally they should be on the same node but that doesn't really answer same node but that doesn't really answer same node but that doesn't really answer the question of having a directory entry the question of having a directory entry the question of having a directory entry for every memory block we have seen that for every memory block we have seen that for every memory block we have seen that we only keep the data in the last level we only keep the data in the last level we only keep the data in the last level cache slides for those blocks that we cache slides for those blocks that we cache slides for those blocks that we actually have in our level 3 cache but a actually have in our level 3 cache but a actually have in our level 3 cache but a directory without any changes would need directory without any changes would need directory without any changes would need to be for every possible memory block to be for every possible memory block to be for every possible memory block that might ever come into that slice that might ever come into that slice that might ever come into that slice which is a big problem because there are which is a big problem because there are which is a big problem because there are a lot of memory blocks so instead of a lot of memory blocks so instead of a lot of memory blocks so instead of keeping a director entry for every keeping a director entry for every keeping a director entry for every memory block we need to realize that the memory block we need to realize that the memory block we need to realize that the entries that are not in our last level entries that are not in our last level entries that are not in our last level cache or in any caches on chip really cache or in any caches on chip really cache or in any caches on chip really are not shared by anybody so we don't are not shared by anybody so we don't are not shared by anybody so we don't really have to maintain that information really have to maintain that information really have to maintain that information so we will maintain what is called a so we will maintain what is called a so we will maintain what is called a partial directory we don't maintain a partial directory we don't maintain a partial directory we don't maintain a directory entry for every memory block directory entry for every memory block directory entry for every memory block instead our directory will have a instead our directory will have a instead our directory will have a limited number of entries and then we limited number of entries and then we limited number of entries and then we allocate and enter in this limited allocate and enter in this limited allocate and enter in this limited directory only for such blocks that have directory only for such blocks that have directory only for such blocks that have at least one presence bit at one they at least one presence bit at one they at least one presence bit at one they basically only for blocks that might be basically only for blocks that might be basically only for blocks that might be in at least one of the private caches so in at least one of the private caches so in at least one of the private caches so for a block for which we know that it is for a block for which we know that it is for a block for which we know that it is not in any L1 or L2 cache it's present not in any L1 or L2 cache it's present not in any L1 or L2 cache it's present only in the LLC or only in memory for only in the LLC or only in memory for only in the LLC or only in memory for that matter we don't need a directory that matter we don't need a directory that matter we don't need a directory entry because implicitly we know that entry because implicitly we know that entry because implicitly we know that such a directory entry would just be all such a directory entry would just be all such a directory entry would just be all zeros but let's say that we have decided zeros but let's say that we have decided zeros but let's say that we have decided on some limited number of entries we on some limited number of entries we on some limited number of entries we start allocating those entries when we start allocating those entries when we start allocating those entries when we put blocks in L2 and L1 caches and put blocks in L2 and L1 caches and put blocks in L2 and L1 caches and eventually we will run out of directory eventually we will run out of directory eventually we will run out of directory entries what do we do now

## On Chip Directory Quiz： Part 6
Kind: captions Language: en So now let's see if we can figure out So now let's see if we can figure out So now let's see if we can figure out how to do an onchip directory where we how to do an onchip directory where we how to do an onchip directory where we said that we're going to have a partial said that we're going to have a partial said that we're going to have a partial directory. The directory has a limited directory. The directory has a limited directory. The directory has a limited number of entries in each slice. We number of entries in each slice. We number of entries in each slice. We allocate an entry only for a block that allocate an entry only for a block that allocate an entry only for a block that might have some presence bit set at one. might have some presence bit set at one. might have some presence bit set at one. But as we request blocks from level one But as we request blocks from level one But as we request blocks from level one caches, we will allocate these entries. caches, we will allocate these entries. caches, we will allocate these entries. Eventually we will run out of them. So Eventually we will run out of them. So Eventually we will run out of them. So the question for you is what do we do the question for you is what do we do the question for you is what do we do now? We have run out of directory now? We have run out of directory now? We have run out of directory entries. How do we proceed?

## On Chip Directory Quiz Solution： Part 6
Kind: captions Language: en let's look at the answer to our onship let's look at the answer to our onship let's look at the answer to our onship directory quiz directory quiz directory quiz we said that we might run out of these we said that we might run out of these we said that we might run out of these partial directory entries and the partial directory entries and the partial directory entries and the question is what do we do now and the question is what do we do now and the question is what do we do now and the answer is the same thing we did when we answer is the same thing we did when we answer is the same thing we did when we run out of room in a cache and that is run out of room in a cache and that is run out of room in a cache and that is replace an existing entry possibly using replace an existing entry possibly using replace an existing entry possibly using a replacement policy such as lru the a replacement policy such as lru the a replacement policy such as lru the really interesting question is what do really interesting question is what do really interesting question is what do we need to do when we replace a we need to do when we replace a we need to do when we replace a directory entry


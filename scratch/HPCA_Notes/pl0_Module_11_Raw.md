# Playlist 0 Module 11 Raw Transcript

## MovZ MovN Quiz Solution： Part 1
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our move z move n quiz we have this code move z move n quiz we have this code move z move n quiz we have this code and we want to do if conversion of it so and we want to do if conversion of it so and we want to do if conversion of it so that we get a branch-free code that does that we get a branch-free code that does that we get a branch-free code that does the same work the same work the same work if conversion amounts to doing the work if conversion amounts to doing the work if conversion amounts to doing the work of both parts both the not taken and the of both parts both the not taken and the of both parts both the not taken and the taken path taken path taken path and then selecting between the two and and then selecting between the two and and then selecting between the two and correcting the results so that only one correcting the results so that only one correcting the results so that only one of them of them of them is active is active is active so what we have done is these two so what we have done is these two so what we have done is these two instructions are already doing the work instructions are already doing the work instructions are already doing the work of the of the of the not taken path not taken path not taken path it's just that instead of in r2 which it's just that instead of in r2 which it's just that instead of in r2 which would permanently modify r2 we're would permanently modify r2 we're would permanently modify r2 we're putting the result in r4 putting the result in r4 putting the result in r4 and here and here and here the taken the taken the taken path r3 and 1 are added then we put path r3 and 1 are added then we put path r3 and 1 are added then we put result in r5 instead of r3 so what now result in r5 instead of r3 so what now result in r5 instead of r3 so what now needs to happen needs to happen needs to happen is is is if this was the correct path to take if this was the correct path to take if this was the correct path to take then r2 should get the result of r4 then r2 should get the result of r4 then r2 should get the result of r4 otherwise r3 should get the result from otherwise r3 should get the result from otherwise r3 should get the result from r5 r5 r5 this is a branch on equal this is a branch on equal this is a branch on equal 0 r1 0 r1 0 r1 so so so if r1 is not equal 0 if r1 is not equal 0 if r1 is not equal 0 meaning move n on r1 used as a condition on r1 used as a condition on r1 used as a condition we're gonna be moving r4 we're gonna be moving r4 we're gonna be moving r4 into into into r2 r2 r2 if the branch is branching then r1 is if the branch is branching then r1 is if the branch is branching then r1 is zero zero zero and in that case we want to be moving r5 and in that case we want to be moving r5 and in that case we want to be moving r5 into r3 into r3 into r3 and this is our if converted code that and this is our if converted code that and this is our if converted code that has four instructions worth of work but has four instructions worth of work but has four instructions worth of work but no branches in them in contrast the no branches in them in contrast the no branches in them in contrast the original code has original code has original code has three instructions worth of work in this three instructions worth of work in this three instructions worth of work in this path path path and two instructions worth of work in and two instructions worth of work in and two instructions worth of work in this path but it has this branch that this path but it has this branch that this path but it has this branch that might be difficult to predict

## MovZ MovN Performance： Part 1
Kind: captions Language: en so now let's look at the performance of so now let's look at the performance of so now let's look at the performance of move Z and move and type of in if move Z and move and type of in if move Z and move and type of in if conversion in the previous quiz we have conversion in the previous quiz we have conversion in the previous quiz we have already seen that branch code like this already seen that branch code like this already seen that branch code like this can be if converted into code like this can be if converted into code like this can be if converted into code like this let's suppose that this Branch here is let's suppose that this Branch here is let's suppose that this Branch here is perfectly predicted so all we have to perfectly predicted so all we have to perfectly predicted so all we have to worry about is this one and let's say worry about is this one and let's say worry about is this one and let's say that this branch is 80% accurately that this branch is 80% accurately that this branch is 80% accurately predicted and let's say there is a 40 predicted and let's say there is a 40 predicted and let's say there is a 40 instruction penalty on mispredictions in instruction penalty on mispredictions in instruction penalty on mispredictions in that case we have here this cost us four that case we have here this cost us four that case we have here this cost us four instructions this costs us three instructions this costs us three instructions this costs us three instructions on this path or two instructions on this path or two instructions on this path or two instructions on this path let's say it's instructions on this path let's say it's instructions on this path let's say it's a very difficult to predict Branch so a very difficult to predict Branch so a very difficult to predict Branch so it's not it's not it's not biased in which case we have 2.5 biased in which case we have 2.5 biased in which case we have 2.5 instructions all the time plus 20% of instructions all the time plus 20% of instructions all the time plus 20% of the time we have a 40 instruction the time we have a 40 instruction the time we have a 40 instruction penalty this gives us on average 10.5 penalty this gives us on average 10.5 penalty this gives us on average 10.5 instructions worth of work on this instructions worth of work on this instructions worth of work on this versus four here so obviously the if versus four here so obviously the if versus four here so obviously the if converted code is more optimal

## MovZ MovN Performance Quiz： Part 1
Kind: captions Language: en in fact let's do a quiz on moves in in fact let's do a quiz on moves in in fact let's do a quiz on moves in movement performance when we do if movement performance when we do if movement performance when we do if conversions so this is the original code conversions so this is the original code conversions so this is the original code let's say that this branch here is 50 of let's say that this branch here is 50 of let's say that this branch here is 50 of the time taken so half of the time we the time taken so half of the time we the time taken so half of the time we end up doing this and half of the time end up doing this and half of the time end up doing this and half of the time we end up doing this we end up doing this we end up doing this so we can say then that the if so we can say then that the if so we can say then that the if conversion code works better when the conversion code works better when the conversion code works better when the prediction accuracy is below prediction accuracy is below prediction accuracy is below what assuming a 30 instruction what assuming a 30 instruction what assuming a 30 instruction misprediction penalty for this branch misprediction penalty for this branch misprediction penalty for this branch and assuming of course that this branch and assuming of course that this branch and assuming of course that this branch is always perfectly predicted because is always perfectly predicted because is always perfectly predicted because it's easy

## MovZ MovN Performance Quiz Solution： Part 1
Kind: captions Language: en and in Loto focuses The Solution for All and in Loto focuses The Solution for All and in Loto focuses The Solution for All modes in motion performance crisis da modes in motion performance crisis da modes in motion performance crisis da knife stunning first person to take knife stunning first person to take knife stunning first person to take some Things That on Average o Yakult some Things That on Average o Yakult some Things That on Average o Yakult half of the time Tree structure and a half of the time Tree structure and a half of the time Tree structure and a Half the time to inflation is with Half the time to inflation is with Half the time to inflation is with Perfect liked this will be executing to Perfect liked this will be executing to Perfect liked this will be executing to Point for instructions on the Average Point for instructions on the Average Point for instructions on the Average prediction when discussing finds QR prediction when discussing finds QR prediction when discussing finds QR get for instructions but we can of get for instructions but we can of get for instructions but we can of explorations of Heavy or Miss I predict explorations of Heavy or Miss I predict explorations of Heavy or Miss I predict an Eight times Eight Streisand Penalty an Eight times Eight Streisand Penalty an Eight times Eight Streisand Penalty and Damage protection research This and Damage protection research This and Damage protection research This world is equal to four da aurex de month world is equal to four da aurex de month world is equal to four da aurex de month production right it needs to be  for me production right it needs to be  for me production right it needs to be  for me I'm Stuck On Fire lyrics One in five I'm Stuck On Fire lyrics One in five I'm Stuck On Fire lyrics One in five instructions worthy of the best Year and instructions worthy of the best Year and instructions worthy of the best Year and three strikes with this old vintage and it three strikes with this old vintage and it three strikes with this old vintage and it was me super tip carrots inside was me super tip carrots inside was me super tip carrots inside balance of the month protection witnesses to balance of the month protection witnesses to balance of the month protection witnesses to be European on Live I'm going to call a be European on Live I'm going to call a be European on Live I'm going to call a stew on top Sou black and mate and stew on top Sou black and mate and stew on top Sou black and mate and make present in Carol these be long make present in Carol these be long make present in Carol these be long night five percent and converts got night five percent and converts got night five percent and converts got this better if the predictions in the this better if the predictions in the this better if the predictions in the skies above and Flowers and then The skies above and Flowers and then The skies above and Flowers and then The region Code beat and do spirit Nights region Code beat and do spirit Nights region Code beat and do spirit Nights when we first need to have Cooper films

## MOVc Summary： Part 1
Kind: captions Language: en so to summarize the approach where we so to summarize the approach where we so to summarize the approach where we have the conditional move instructions have the conditional move instructions have the conditional move instructions we need a compiler support to do any if we need a compiler support to do any if we need a compiler support to do any if conversion if the compiler doesn't do conversion if the compiler doesn't do conversion if the compiler doesn't do anything the code will be just left with anything the code will be just left with anything the code will be just left with a branch so this is not a fully a branch so this is not a fully a branch so this is not a fully backwards compatible approach in that if backwards compatible approach in that if backwards compatible approach in that if we add the conditional move instructions we add the conditional move instructions we add the conditional move instructions to the is a we're only allowing the to the is a we're only allowing the to the is a we're only allowing the compiler to take advantage of that but compiler to take advantage of that but compiler to take advantage of that but old code will still not use these old code will still not use these old code will still not use these conditional instructions this approach conditional instructions this approach conditional instructions this approach will remove hard to predict branches so will remove hard to predict branches so will remove hard to predict branches so if we have a hard to predict branch for if we have a hard to predict branch for if we have a hard to predict branch for example through profiling we determine example through profiling we determine example through profiling we determine that a particular branch is difficult to that a particular branch is difficult to that a particular branch is difficult to predict we can perform if conversion to predict we can perform if conversion to predict we can perform if conversion to get rid of it if conversion typically get rid of it if conversion typically get rid of it if conversion typically requires use of more registers than the requires use of more registers than the requires use of more registers than the original code because we need to keep original code because we need to keep original code because we need to keep all the results from both of the paths all the results from both of the paths all the results from both of the paths and also more instructions will be and also more instructions will be and also more instructions will be executed as a result of if conversion executed as a result of if conversion executed as a result of if conversion because we are executing instructions because we are executing instructions because we are executing instructions from both paths not just one and also from both paths not just one and also from both paths not just one and also because we need to add the conditional because we need to add the conditional because we need to add the conditional move instructions to select the actual move instructions to select the actual move instructions to select the actual results the ones we want to keep from results the ones we want to keep from results the ones we want to keep from among the results that we have generated among the results that we have generated among the results that we have generated from both paths some of these are from both paths some of these are from both paths some of these are necessary in order to do if conversion necessary in order to do if conversion necessary in order to do if conversion for example the compiler support is okay for example the compiler support is okay for example the compiler support is okay we need that it will remove the we need that it will remove the we need that it will remove the conditional branches that are hard to conditional branches that are hard to conditional branches that are hard to predict so that we want to stay the way predict so that we want to stay the way predict so that we want to stay the way it is but the need for more registers it is but the need for more registers it is but the need for more registers for example can we do something about for example can we do something about for example can we do something about that more instructions are executed that more instructions are executed that more instructions are executed because we execute both paths this again because we execute both paths this again because we execute both paths this again is okay that's how we do if conversion is okay that's how we do if conversion is okay that's how we do if conversion but do we really have to have additional but do we really have to have additional but do we really have to have additional instructions just to select the results instructions just to select the results instructions just to select the results to get rid of this in this problem we to get rid of this in this problem we to get rid of this in this problem we can change the rice a so that every can change the rice a so that every can change the rice a so that every instruction is now conditional every instruction is now conditional every instruction is now conditional every instruction computes its result and then instruction computes its result and then instruction computes its result and then writes it or doesn't write it to the writes it or doesn't write it to the writes it or doesn't write it to the destination register depending on destination register depending on destination register depending on whether a condition is true or not this whether a condition is true or not this whether a condition is true or not this approach is called full predication and approach is called full predication and approach is called full predication and it requires extensive support in the it requires extensive support in the it requires extensive support in the instruction set

## Full Predication HW Support： Part 1
Kind: captions Language: en so let's compare what we need for the so let's compare what we need for the so let's compare what we need for the conditional moves at what we need for conditional moves at what we need for conditional moves at what we need for full predication so for the move full predication so for the move full predication so for the move instructions that are conditional we instructions that are conditional we instructions that are conditional we need a separate opcode that tells us need a separate opcode that tells us need a separate opcode that tells us this is a conditional instruction this is a conditional instruction this is a conditional instruction usually we have a separate opcode for usually we have a separate opcode for usually we have a separate opcode for each particular condition so we need a each particular condition so we need a each particular condition so we need a number of new opcode number of new opcode number of new opcode for full predication we use the same for full predication we use the same for full predication we use the same instructions that we use without instructions that we use without instructions that we use without conditions conditions conditions we add condition bits for full we add condition bits for full we add condition bits for full predication however we add condition predication however we add condition predication however we add condition bits to every instructions so every bits to every instructions so every bits to every instructions so every instruction word contains some bits that instruction word contains some bits that instruction word contains some bits that tell us what is the condition for tell us what is the condition for tell us what is the condition for writing to the destination register for writing to the destination register for writing to the destination register for example the intel itanium instruction example the intel itanium instruction example the intel itanium instruction set has full predication and this is set has full predication and this is set has full predication and this is what an add instruction would look like what an add instruction would look like what an add instruction would look like it specifies the two source registers it specifies the two source registers it specifies the two source registers the destination registers that this is the destination registers that this is the destination registers that this is an ad and it has a field within the an ad and it has a field within the an ad and it has a field within the instruction to specify the so-called instruction to specify the so-called instruction to specify the so-called qualifying predicate that is where the qualifying predicate that is where the qualifying predicate that is where the condition for doing the right to the r1 condition for doing the right to the r1 condition for doing the right to the r1 register will be found in the attained register will be found in the attained register will be found in the attained iam instruction has 41 bits where the iam instruction has 41 bits where the iam instruction has 41 bits where the least significant six bits specify what least significant six bits specify what least significant six bits specify what the qualifying predicate is the the qualifying predicate is the the qualifying predicate is the predicates in the Itanium are actually predicates in the Itanium are actually predicates in the Itanium are actually small one bit registers so that you can small one bit registers so that you can small one bit registers so that you can do a condition check and store the do a condition check and store the do a condition check and store the result in this one bit register and then result in this one bit register and then result in this one bit register and then really the six bits here tell you which really the six bits here tell you which really the six bits here tell you which of the 64 bit conditional registers are of the 64 bit conditional registers are of the 64 bit conditional registers are you gonna use to determine whether to you gonna use to determine whether to you gonna use to determine whether to write to r1

## Full Predication Example： Part 1
Kind: captions Language: en so let's say we have this code where so let's say we have this code where so let's say we have this code where again r1 is compared to zero any branch again r1 is compared to zero any branch again r1 is compared to zero any branch here if it's equal to zero if it's not here if it's equal to zero if it's not here if it's equal to zero if it's not equal to zero we do this and branch out equal to zero we do this and branch out equal to zero we do this and branch out if it is equal to zero we do this and if it is equal to zero we do this and if it is equal to zero we do this and continue we have seen that this code continue we have seen that this code continue we have seen that this code results in four instructions worth of results in four instructions worth of results in four instructions worth of execution when we have the conditional execution when we have the conditional execution when we have the conditional moves two instructions that actually do moves two instructions that actually do moves two instructions that actually do the work of adye's and two instructions the work of adye's and two instructions the work of adye's and two instructions that conditionally write results back to that conditionally write results back to that conditionally write results back to r2 and r3 with full predication Allah r2 and r3 with full predication Allah r2 and r3 with full predication Allah the Itanium we get the condition check the Itanium we get the condition check the Itanium we get the condition check is done using the predicate set is done using the predicate set is done using the predicate set instruction that compares equals zero on instruction that compares equals zero on instruction that compares equals zero on r1 and sets p1 and p2 accordingly the r1 and sets p1 and p2 accordingly the r1 and sets p1 and p2 accordingly the predicates are always set opposite of predicates are always set opposite of predicates are always set opposite of each other so that if r1 is 0 P 1 is set each other so that if r1 is 0 P 1 is set each other so that if r1 is 0 P 1 is set to true and P 2 to false if r1 is not to true and P 2 to false if r1 is not to true and P 2 to false if r1 is not equal to 0 then P 1 is set to false and equal to 0 then P 1 is set to false and equal to 0 then P 1 is set to false and P 2 is set to true now we can simply do P 2 is set to true now we can simply do P 2 is set to true now we can simply do the add I of 1 plus R 2 into R 2 but we the add I of 1 plus R 2 into R 2 but we the add I of 1 plus R 2 into R 2 but we will predicate it with p2 so that it will predicate it with p2 so that it will predicate it with p2 so that it only gets done if r1 was not equal to 0 only gets done if r1 was not equal to 0 only gets done if r1 was not equal to 0 and then this instruction becomes and then this instruction becomes and then this instruction becomes exactly what it was here except that now exactly what it was here except that now exactly what it was here except that now it's predicated so that it only gets it's predicated so that it only gets it's predicated so that it only gets done if R 1 is equal to 0 so as you can done if R 1 is equal to 0 so as you can done if R 1 is equal to 0 so as you can see with this after we do this initial see with this after we do this initial see with this after we do this initial instruction that simply sets the instruction that simply sets the instruction that simply sets the predicate any code that was here simply predicate any code that was here simply predicate any code that was here simply gets predicated with one of the gets predicated with one of the gets predicated with one of the conditions the opposite condition conditions the opposite condition conditions the opposite condition predicates the other branches code and predicates the other branches code and predicates the other branches code and that means that first of all we can use that means that first of all we can use that means that first of all we can use the same registers that we use before we the same registers that we use before we the same registers that we use before we don't need the additional r4 and r5 that don't need the additional r4 and r5 that don't need the additional r4 and r5 that we had in the previous example and we had in the previous example and we had in the previous example and second we still do have the work of both second we still do have the work of both second we still do have the work of both paths but there is no additional work to paths but there is no additional work to paths but there is no additional work to now move the results into the registers now move the results into the registers now move the results into the registers that have the destination registers so that have the destination registers so that have the destination registers so the overhead here is kind of similar to the overhead here is kind of similar to the overhead here is kind of similar to what we had with branches with branches what we had with branches with branches what we had with branches with branches we needed to have the branch and then we needed to have the branch and then we needed to have the branch and then the code from either path here we have the code from either path here we have the code from either path here we have the condition set followed by the code the condition set followed by the code the condition set followed by the code from both parts so the only really from both parts so the only really from both parts so the only really overhead that we have in terms of overhead that we have in terms of overhead that we have in terms of executing more instructions comes from executing more instructions comes from executing more instructions comes from doing the work of both paths instead of doing the work of both paths instead of doing the work of both paths instead of just one but there are no additional just one but there are no additional just one but there are no additional instructions to select between the instructions to select between the instructions to select between the results

## Full Predication Quiz： Part 1
Kind: captions Language: en let us see if we can do some full let us see if we can do some full let us see if we can do some full predication of our own suppose that this predication of our own suppose that this predication of our own suppose that this is the original code where we check if is the original code where we check if is the original code where we check if r2 is equal to zero if it is we jump r2 is equal to zero if it is we jump r2 is equal to zero if it is we jump here and decrement r1 by one here and decrement r1 by one here and decrement r1 by one if r2 is not 0 then we continue if r2 is not 0 then we continue if r2 is not 0 then we continue increment r1 by 1 increment r1 by 1 increment r1 by 1 and then go out so after if conversion and then go out so after if conversion and then go out so after if conversion with full predication with full predication with full predication we get this we get this we get this first first first we set predicates p1 and p2 according to we set predicates p1 and p2 according to we set predicates p1 and p2 according to whether r2 is equal to 0. remember that whether r2 is equal to 0. remember that whether r2 is equal to 0. remember that this one will be set this one will be set this one will be set if r2 is equal to 0 and this one if r2 is equal to 0 and this one if r2 is equal to 0 and this one is set if r2 is not equal to zero so is set if r2 is not equal to zero so is set if r2 is not equal to zero so they're opposite predicates they're opposite predicates they're opposite predicates this one is set if r2 is equal to zero this one is set if r2 is equal to zero this one is set if r2 is equal to zero the actual question for you is what goes the actual question for you is what goes the actual question for you is what goes into the predicate for the next into the predicate for the next into the predicate for the next instruction and the predicate for the instruction and the predicate for the instruction and the predicate for the next instruction after that and then the next instruction after that and then the next instruction after that and then the two instructions are add r1 r1 1 and add two instructions are add r1 r1 1 and add two instructions are add r1 r1 1 and add r1 r1 r1 r1 -1 so what are the predicates here r1 -1 so what are the predicates here r1 -1 so what are the predicates here also if we assume that this code gets us also if we assume that this code gets us also if we assume that this code gets us a cpf 0.5 without mispredictions a cpf 0.5 without mispredictions a cpf 0.5 without mispredictions and that the misprediction penalty is 10 and that the misprediction penalty is 10 and that the misprediction penalty is 10 cycles then we should do the if cycles then we should do the if cycles then we should do the if conversion if the branch equals zero conversion if the branch equals zero conversion if the branch equals zero here is predicted below what accuracy so here is predicted below what accuracy so here is predicted below what accuracy so doing conversion if branch equals zero doing conversion if branch equals zero doing conversion if branch equals zero prediction is less than what percentage prediction is less than what percentage prediction is less than what percentage correct

## Full Predication Quiz Solution： Part 1
Kind: captions Language: en let us look at the solution to our full let us look at the solution to our full let us look at the solution to our full predication quiz we have this branch predication quiz we have this branch predication quiz we have this branch based if-then-else called we're trying based if-then-else called we're trying based if-then-else called we're trying to if convert it using full predication to if convert it using full predication to if convert it using full predication p1 here is set when r2 is 0 when R 2 is p1 here is set when r2 is 0 when R 2 is p1 here is set when r2 is 0 when R 2 is we branch to the else part so this is we branch to the else part so this is we branch to the else part so this is what gets done if r2 is 0 that means what gets done if r2 is 0 that means what gets done if r2 is 0 that means that we do this when p1 is true and then that we do this when p1 is true and then that we do this when p1 is true and then of course we do this when it's not true of course we do this when it's not true of course we do this when it's not true the next part that we want to do is when the next part that we want to do is when the next part that we want to do is when should we do the if conversion so what should we do the if conversion so what should we do the if conversion so what needs to be the accuracy here let's needs to be the accuracy here let's needs to be the accuracy here let's first see how many instructions we have first see how many instructions we have first see how many instructions we have in each of these here we have 3 in each of these here we have 3 in each of these here we have 3 instructions with the CPI of 0.5 this instructions with the CPI of 0.5 this instructions with the CPI of 0.5 this always takes us one point five cycles always takes us one point five cycles always takes us one point five cycles assuming that there are other things assuming that there are other things assuming that there are other things that can be done in the rest of the that can be done in the rest of the that can be done in the rest of the cycle here when this branch is taken we cycle here when this branch is taken we cycle here when this branch is taken we do two instructions when this branch is do two instructions when this branch is do two instructions when this branch is not-taken not-taken not-taken we do three instructions so let's split we do three instructions so let's split we do three instructions so let's split the difference and say that on average the difference and say that on average the difference and say that on average we do 2.5 instructions at the CPI of 0.5 we do 2.5 instructions at the CPI of 0.5 we do 2.5 instructions at the CPI of 0.5 that means that without a misprediction that means that without a misprediction that means that without a misprediction we get 1.25 cycles a misprediction adds we get 1.25 cycles a misprediction adds we get 1.25 cycles a misprediction adds 10 cycles these two perform equally when 10 cycles these two perform equally when 10 cycles these two perform equally when the cost of mispredictions here is 0.25 the cost of mispredictions here is 0.25 the cost of mispredictions here is 0.25 cycles per branch so pretty much the cycles per branch so pretty much the cycles per branch so pretty much the difference between these two is 0.25 difference between these two is 0.25 difference between these two is 0.25 cycles so on average we can lose this cycles so on average we can lose this cycles so on average we can lose this much to get this to be equal the penalty much to get this to be equal the penalty much to get this to be equal the penalty for a single misprediction is 10 cycles for a single misprediction is 10 cycles for a single misprediction is 10 cycles if we can lose only 0.25 cycles per if we can lose only 0.25 cycles per if we can lose only 0.25 cycles per branch that means that only one in 40 branch that means that only one in 40 branch that means that only one in 40 branches can be mispredicted one in fort branches can be mispredicted one in fort branches can be mispredicted one in fort is 2.5% that means that our correct is 2.5% that means that our correct is 2.5% that means that our correct prediction needs to be at least 97.5% to prediction needs to be at least 97.5% to prediction needs to be at least 97.5% to not do the if conversion if the correct not do the if conversion if the correct not do the if conversion if the correct prediction rate is less than this then prediction rate is less than this then prediction rate is less than this then we are better off with if conversion

## Predication Outro： Part 1
Kind: captions Language: en in this lesson we discussed if in this lesson we discussed if in this lesson we discussed if conversion where we convert difficult to conversion where we convert difficult to conversion where we convert difficult to predict branches predict branches predict branches into code that is longer but has no into code that is longer but has no into code that is longer but has no branches branches branches we also learned about special we also learned about special we also learned about special instructions that we need for if instructions that we need for if instructions that we need for if conversion to work conversion to work conversion to work now that we are up to speed now that we are up to speed now that we are up to speed with how control hazards are handled we with how control hazards are handled we with how control hazards are handled we need to learn how to handle data hazards need to learn how to handle data hazards need to learn how to handle data hazards while keeping a good instruction flow in while keeping a good instruction flow in while keeping a good instruction flow in our pipeline


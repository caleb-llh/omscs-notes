# Playlist 4 Module 10 Raw Transcript

## Test and Atomic OP Lock： Part 5
Kind: captions Language: en so we have seen that performing these so we have seen that performing these so we have seen that performing these atomic read write operations on the lock atomic read write operations on the lock atomic read write operations on the lock repeatedly repeatedly repeatedly even while waiting for the lock is even while waiting for the lock is even while waiting for the lock is inefficient in terms of energy and it inefficient in terms of energy and it inefficient in terms of energy and it actually slows down the thread that is actually slows down the thread that is actually slows down the thread that is in the critical section we want to make in the critical section we want to make in the critical section we want to make this more efficient and one way of doing this more efficient and one way of doing this more efficient and one way of doing that is to do a so-called test and that is to do a so-called test and that is to do a so-called test and atomic op approach in the locking atomic op approach in the locking atomic op approach in the locking function so the original approach to function so the original approach to function so the original approach to using for example an atomic exchange is using for example an atomic exchange is using for example an atomic exchange is to to to set r1 to 1 set r1 to 1 set r1 to 1 and as long as r1 is one and as long as r1 is one and as long as r1 is one try to swap it with lockvar try to swap it with lockvar try to swap it with lockvar eventually we see a zero and we exit eventually we see a zero and we exit eventually we see a zero and we exit but as we now know but as we now know but as we now know this keeps invalidating everybody else this keeps invalidating everybody else this keeps invalidating everybody else who is waiting on the log so we are who is waiting on the log so we are who is waiting on the log so we are getting a lot of cash misses while getting a lot of cash misses while getting a lot of cash misses while waiting and active waiting like this is waiting and active waiting like this is waiting and active waiting like this is not only spending energy on the bus not only spending energy on the bus not only spending energy on the bus transfers all the time but also transfers all the time but also transfers all the time but also keeps the bus busy so that useful work keeps the bus busy so that useful work keeps the bus busy so that useful work in other threads that are not busy in other threads that are not busy in other threads that are not busy waiting is slowed down so this is kind waiting is slowed down so this is kind waiting is slowed down so this is kind of like a repeated atomic op approach to of like a repeated atomic op approach to of like a repeated atomic op approach to make it more efficient what we do is we make it more efficient what we do is we make it more efficient what we do is we do the same type of looping here but we do the same type of looping here but we do the same type of looping here but we don't immediately try to swap what we do don't immediately try to swap what we do don't immediately try to swap what we do first is use normal loads to wait for first is use normal loads to wait for first is use normal loads to wait for lockwork to become zero lockwork to become zero lockwork to become zero so the idea is that we're using normal so the idea is that we're using normal so the idea is that we're using normal loads while lockvar is busy if we loads while lockvar is busy if we loads while lockvar is busy if we observe that it's free we try to grab it observe that it's free we try to grab it observe that it's free we try to grab it by doing an atomic exchange we cannot by doing an atomic exchange we cannot by doing an atomic exchange we cannot try to grab it using the normal store try to grab it using the normal store try to grab it using the normal store that's why we have to do the exchange that's why we have to do the exchange that's why we have to do the exchange but the idea is that there is no point but the idea is that there is no point but the idea is that there is no point in trying to do the exchange unless the in trying to do the exchange unless the in trying to do the exchange unless the lock is actually free and the purpose of lock is actually free and the purpose of lock is actually free and the purpose of the exchange now is solely to ensure the exchange now is solely to ensure the exchange now is solely to ensure that only one of us grabs the lock once that only one of us grabs the lock once that only one of us grabs the lock once it becomes free but the busy waiting it becomes free but the busy waiting it becomes free but the busy waiting while the lock is continuously free is while the lock is continuously free is while the lock is continuously free is done using normal reads done using normal reads done using normal reads so now if we have core 0 1 and 2 and so now if we have core 0 1 and 2 and so now if we have core 0 1 and 2 and core 0 succeeds in doing an exchange and core 0 succeeds in doing an exchange and core 0 succeeds in doing an exchange and gets the lock var in the modified state gets the lock var in the modified state gets the lock var in the modified state core 1 will now reach this point and core 1 will now reach this point and core 1 will now reach this point and read lockvar see that it's 1. read lockvar see that it's 1. read lockvar see that it's 1. it's done using a normal load it's done using a normal load it's done using a normal load so here we move to the own state so here we move to the own state so here we move to the own state here we move to the shared state here we move to the shared state here we move to the shared state and there is a cache to cash transfer and there is a cache to cash transfer and there is a cache to cash transfer here now we will keep doing this here now we will keep doing this here now we will keep doing this but we'll keep getting cash hit so we no but we'll keep getting cash hit so we no but we'll keep getting cash hit so we no longer access the bus longer access the bus longer access the bus while waiting while waiting while waiting similarly core 2 will load the lock bar similarly core 2 will load the lock bar similarly core 2 will load the lock bar it will also get it will also get it will also get the value and have it in the shared the value and have it in the shared the value and have it in the shared state state state and now it also gets cash hits while and now it also gets cash hits while and now it also gets cash hits while waiting so as long as core zero keeps waiting so as long as core zero keeps waiting so as long as core zero keeps the lock busy here these two cores are the lock busy here these two cores are the lock busy here these two cores are no longer generating bus traffic so core no longer generating bus traffic so core no longer generating bus traffic so core zero basically gets the entire coherence zero basically gets the entire coherence zero basically gets the entire coherence bus to itself so you can very quickly bus to itself so you can very quickly bus to itself so you can very quickly service cash misses and cache hits here service cash misses and cache hits here service cash misses and cache hits here are much more efficient than bus are much more efficient than bus are much more efficient than bus accesses and this is going to continue accesses and this is going to continue accesses and this is going to continue until core 0 releases the lock at which until core 0 releases the lock at which until core 0 releases the lock at which point there is an invalidation and these point there is an invalidation and these point there is an invalidation and these two will read again see a value of one two will read again see a value of one two will read again see a value of one try to compete for it using the exchange try to compete for it using the exchange try to compete for it using the exchange and so on but those bursts of activity and so on but those bursts of activity and so on but those bursts of activity really happen only when they're actually really happen only when they're actually really happen only when they're actually trying to acquire the lock as long as trying to acquire the lock as long as trying to acquire the lock as long as the lock is really busy they just keep the lock is really busy they just keep the lock is really busy they just keep using normal loads in this loop so this using normal loads in this loop so this using normal loads in this loop so this is much more energy efficient

## Test and Atomic OP Quiz： Part 5
Kind: captions Language: en so let's see if we understood how test so let's see if we understood how test so let's see if we understood how test an atomic op locks work remember our an atomic op locks work remember our an atomic op locks work remember our original code for load link store original code for load link store original code for load link store conditional locks which loads the conditional locks which loads the conditional locks which loads the previous state of the lock tries to put previous state of the lock tries to put previous state of the lock tries to put one into the lock and then checks one into the lock and then checks one into the lock and then checks whether we observed the unlock state of whether we observed the unlock state of whether we observed the unlock state of the lock and whether we successfully the lock and whether we successfully the lock and whether we successfully grab the lock so if we apply the test grab the lock so if we apply the test grab the lock so if we apply the test and atomic up approach to this amazingly and atomic up approach to this amazingly and atomic up approach to this amazingly we do that by just rearranging these we do that by just rearranging these we do that by just rearranging these instructions the first two instructions instructions the first two instructions instructions the first two instructions are exactly the same and then in what are exactly the same and then in what are exactly the same and then in what order do we use the remaining three order do we use the remaining three order do we use the remaining three instructions instead of writing out the instructions instead of writing out the instructions instead of writing out the instruction just say which of these is instruction just say which of these is instruction just say which of these is the instruction a B and C

## Test and Atomic OP Quiz Solution： Part 5
Kind: captions Language: en let's look at the solution for our test let's look at the solution for our test let's look at the solution for our test and atomic pop quiz using load link and atomic pop quiz using load link and atomic pop quiz using load link store conditional instructions so we store conditional instructions so we store conditional instructions so we said that the first two instructions of said that the first two instructions of said that the first two instructions of the new testing atomic approach are the the new testing atomic approach are the the new testing atomic approach are the same put one in our one and then load same put one in our one and then load same put one in our one and then load from l'acqua to our to the idea of tests from l'acqua to our to the idea of tests from l'acqua to our to the idea of tests and atomic hope is not to try the right and atomic hope is not to try the right and atomic hope is not to try the right until we see that the lock is free so until we see that the lock is free so until we see that the lock is free so before trying the store we should check before trying the store we should check before trying the store we should check whether the lock is free that is done by whether the lock is free that is done by whether the lock is free that is done by using the B instruction here so the using the B instruction here so the using the B instruction here so the correct answer should have been just be correct answer should have been just be correct answer should have been just be here and I'm going to write it out to here and I'm going to write it out to here and I'm going to write it out to show you how it works so there is that show you how it works so there is that show you how it works so there is that if we load a busy value meaning a value if we load a busy value meaning a value if we load a busy value meaning a value of one then r 2 is not equal to 0 and of one then r 2 is not equal to 0 and of one then r 2 is not equal to 0 and we're just going to repeat this so as we're just going to repeat this so as we're just going to repeat this so as long as the lock is busy we just keep long as the lock is busy we just keep long as the lock is busy we just keep doing the load link the load link doing the load link the load link doing the load link the load link without the corresponding store without the corresponding store without the corresponding store conditional behaves just like a normal conditional behaves just like a normal conditional behaves just like a normal load yes it's setting the link register load yes it's setting the link register load yes it's setting the link register every time but we are not checking it so every time but we are not checking it so every time but we are not checking it so it's just the same as if we didn't but it's just the same as if we didn't but it's just the same as if we didn't but things get more interesting once we things get more interesting once we things get more interesting once we actually see an available value of the actually see an available value of the actually see an available value of the lock then we try to do a store lock then we try to do a store lock then we try to do a store conditional so now we should use the conditional so now we should use the conditional so now we should use the instruction so we have seen that the instruction so we have seen that the instruction so we have seen that the lock is free now we try to get it and lock is free now we try to get it and lock is free now we try to get it and after this we need to check using after this we need to check using after this we need to check using instruction see whether we actually got instruction see whether we actually got instruction see whether we actually got it so if r1 is equal to 0 that means it so if r1 is equal to 0 that means it so if r1 is equal to 0 that means that our store conditional didn't that our store conditional didn't that our store conditional didn't succeed we go back to trying to acquire succeed we go back to trying to acquire succeed we go back to trying to acquire the lock again so we have seen that the the lock again so we have seen that the the lock again so we have seen that the lock is free but between our load link lock is free but between our load link lock is free but between our load link and our store conditional somebody else and our store conditional somebody else and our store conditional somebody else managed to write to the lock so we need managed to write to the lock so we need managed to write to the lock so we need to retry the whole thing so after out to retry the whole thing so after out to retry the whole thing so after out load link we check whether the lock was load link we check whether the lock was load link we check whether the lock was free if so we try the store conditional free if so we try the store conditional free if so we try the store conditional if we succeed then we got the lock so if we succeed then we got the lock so if we succeed then we got the lock so the waiting for the lock to become free the waiting for the lock to become free the waiting for the lock to become free is done using the same load link that is done using the same load link that is done using the same load link that will be part of our atomic operation will be part of our atomic operation will be part of our atomic operation once we add the store conditional when once we add the store conditional when once we add the store conditional when we see that the lock is free

## Unlock Quiz： Part 5
Kind: captions Language: en now let's see if we can figure out what now let's see if we can figure out what now let's see if we can figure out what the unlock needs to do so the unlock the unlock needs to do so the unlock the unlock needs to do so the unlock function here does what option 1 use a function here does what option 1 use a function here does what option 1 use a normal store to write 0 to log bar normal store to write 0 to log bar normal store to write 0 to log bar option to load link see if we loaded one option to load link see if we loaded one option to load link see if we loaded one and then store conditional an option 3 and then store conditional an option 3 and then store conditional an option 3 we need additional atomic instructions we need additional atomic instructions we need additional atomic instructions in our I say to do this

## Unlock Quiz Solution： Part 5
Kind: captions Language: en let's look at the solution to our unlock let's look at the solution to our unlock let's look at the solution to our unlock quiz where we were asked really what quiz where we were asked really what quiz where we were asked really what does unlock do does unlock do does unlock do does it just store zero to lockvar does it just store zero to lockvar does it just store zero to lockvar does it check whether the lockvar is one does it check whether the lockvar is one does it check whether the lockvar is one and then do a store conditional and then do a store conditional and then do a store conditional atomically to make it zero or do we need atomically to make it zero or do we need atomically to make it zero or do we need additional atomic instructions that are additional atomic instructions that are additional atomic instructions that are gonna somehow gonna somehow gonna somehow unlock the lock and the answer is that unlock the lock and the answer is that unlock the lock and the answer is that unlike the lock function where we needed unlike the lock function where we needed unlike the lock function where we needed to both to both to both check whether the lock is available and check whether the lock is available and check whether the lock is available and make it busy in one go make it busy in one go make it busy in one go the unlock function the unlock function the unlock function is done only by the thread that actually is done only by the thread that actually is done only by the thread that actually has the lock has the lock has the lock so when we get to unlock we know that so when we get to unlock we know that so when we get to unlock we know that the lock is busy because we made it busy the lock is busy because we made it busy the lock is busy because we made it busy all we have to do is just all we have to do is just all we have to do is just make the log free we can do that by make the log free we can do that by make the log free we can do that by writing zero in any way we want so we writing zero in any way we want so we writing zero in any way we want so we choose the simplest way of just doing a choose the simplest way of just doing a choose the simplest way of just doing a normal store to the lock variable normal store to the lock variable normal store to the lock variable so we don't need to atomically test so we don't need to atomically test so we don't need to atomically test whether it's one and then make it 0 and whether it's one and then make it 0 and whether it's one and then make it 0 and we definitely don't need additional we definitely don't need additional we definitely don't need additional atomic instructions

## Barrier Synchronization： Part 5
Kind: captions Language: en another type of synchronization that we another type of synchronization that we another type of synchronization that we often need in programs is called barrier often need in programs is called barrier often need in programs is called barrier synchronization this often occurs when synchronization this often occurs when synchronization this often occurs when there is a parallel section where there is a parallel section where there is a parallel section where several threads are doing something several threads are doing something several threads are doing something independent of each other for example independent of each other for example independent of each other for example each thread is adding up the numbers in each thread is adding up the numbers in each thread is adding up the numbers in its own part of a shared array its own part of a shared array its own part of a shared array eventually we want to know the total sum eventually we want to know the total sum eventually we want to know the total sum so what needs to happen is we need to so what needs to happen is we need to so what needs to happen is we need to make sure that everybody has finished make sure that everybody has finished make sure that everybody has finished their own adding up and that's what the their own adding up and that's what the their own adding up and that's what the barrier is and only then barrier is and only then barrier is and only then somebody for example thread zero can somebody for example thread zero can somebody for example thread zero can actually form the total sum and for actually form the total sum and for actually form the total sum and for example print it out we don't want it to example print it out we don't want it to example print it out we don't want it to print the sum print the sum print the sum before we know that everybody's before we know that everybody's before we know that everybody's contributions to the sum are complete contributions to the sum are complete contributions to the sum are complete another thing that can happen is that we another thing that can happen is that we another thing that can happen is that we are computing the sum in this parallel are computing the sum in this parallel are computing the sum in this parallel way and then all of the threads need to way and then all of the threads need to way and then all of the threads need to use this sum so again all of them need use this sum so again all of them need use this sum so again all of them need to wait for the sum to be certainly done to wait for the sum to be certainly done to wait for the sum to be certainly done and only then can they proceed to and only then can they proceed to and only then can they proceed to actually use the sum actually use the sum actually use the sum if thread 0 if thread 0 if thread 0 starts using the sum before making sure starts using the sum before making sure starts using the sum before making sure that other threads are done computing that other threads are done computing that other threads are done computing the sum then what happens is we are the sum then what happens is we are the sum then what happens is we are using the sum that may not include all using the sum that may not include all using the sum that may not include all of the numbers that are in the array of the numbers that are in the array of the numbers that are in the array same for any other thread so the barrier same for any other thread so the barrier same for any other thread so the barrier pretty much is a global weight pretty much is a global weight pretty much is a global weight that ensures that all threads that ensures that all threads that ensures that all threads have entered the barrier have entered the barrier have entered the barrier before any of them can proceed past the before any of them can proceed past the before any of them can proceed past the barrier barrier barrier so the barrier synchronization ensures so the barrier synchronization ensures so the barrier synchronization ensures that all threads that need to arrive to that all threads that need to arrive to that all threads that need to arrive to the barrier have arrived the barrier have arrived the barrier have arrived before any can leave the barrier a before any can leave the barrier a before any can leave the barrier a barrier implementation can have two barrier implementation can have two barrier implementation can have two variables a counter that counts how many variables a counter that counts how many variables a counter that counts how many threads have arrived and the flag that threads have arrived and the flag that threads have arrived and the flag that gets set when the counter finally gets set when the counter finally gets set when the counter finally reaches n so what each thread arriving reaches n so what each thread arriving reaches n so what each thread arriving to the barrier needs to do is increment to the barrier needs to do is increment to the barrier needs to do is increment the counter check if the counter is the counter check if the counter is the counter check if the counter is equal to the number of threads that need equal to the number of threads that need equal to the number of threads that need to arrive if it is equal set the flag if to arrive if it is equal set the flag if to arrive if it is equal set the flag if it's not equal that means we are not the it's not equal that means we are not the it's not equal that means we are not the last arriving thread then we spin last arriving thread then we spin last arriving thread then we spin waiting on the flag to finally become waiting on the flag to finally become waiting on the flag to finally become set of course things are not as simple

## Simple Barrier Implementation： Part 5
Kind: captions Language: en So let's look at a relatively simple So let's look at a relatively simple So let's look at a relatively simple implementation of a barrier in a implementation of a barrier in a implementation of a barrier in a program. So this is our barrier program. So this is our barrier program. So this is our barrier implementation. Our counter is a shared implementation. Our counter is a shared implementation. Our counter is a shared variable and everybody's trying to variable and everybody's trying to variable and everybody's trying to increment it when they arrive to the increment it when they arrive to the increment it when they arrive to the barrier. Multiple threads could arrive barrier. Multiple threads could arrive barrier. Multiple threads could arrive to the barrier at about the same time. to the barrier at about the same time. to the barrier at about the same time. So we have to protect the counter using So we have to protect the counter using So we have to protect the counter using a lock. So here what we're really doing a lock. So here what we're really doing a lock. So here what we're really doing is counting the arrivals. Once we are is counting the arrivals. Once we are is counting the arrivals. Once we are done incrementing the counter, we can done incrementing the counter, we can done incrementing the counter, we can check if the counter is equal to the check if the counter is equal to the check if the counter is equal to the total. If it is, that means that we are total. If it is, that means that we are total. If it is, that means that we are either the last thread or that the last either the last thread or that the last either the last thread or that the last thread has arrived. Note that the two thread has arrived. Note that the two thread has arrived. Note that the two are not necessarily the same. We may be are not necessarily the same. We may be are not necessarily the same. We may be able to increment the counter, but we able to increment the counter, but we able to increment the counter, but we are not the last thread, but by the time are not the last thread, but by the time are not the last thread, but by the time we get to check the total, the last we get to check the total, the last we get to check the total, the last thread has arrived and increments the thread has arrived and increments the thread has arrived and increments the counter. So, we read the counter that is counter. So, we read the counter that is counter. So, we read the counter that is not the same as what we left it with. not the same as what we left it with. not the same as what we left it with. Either way, if the counter is equal to Either way, if the counter is equal to Either way, if the counter is equal to the total, then we reset the counter the total, then we reset the counter the total, then we reset the counter because it needs to be reinitialized so because it needs to be reinitialized so because it needs to be reinitialized so that next time we enter the same barrier that next time we enter the same barrier that next time we enter the same barrier variable, we have the count at zero. And variable, we have the count at zero. And variable, we have the count at zero. And we set the release flag to one so that we set the release flag to one so that we set the release flag to one so that the threads who are waiting find out the threads who are waiting find out the threads who are waiting find out that the barrier can now be released. If that the barrier can now be released. If that the barrier can now be released. If the count is not equal to the total, the count is not equal to the total, the count is not equal to the total, then we're going to spin here waiting then we're going to spin here waiting then we're going to spin here waiting for somebody else to find that the count for somebody else to find that the count for somebody else to find that the count is equal to the total and release. After is equal to the total and release. After is equal to the total and release. After we are done going through one instance we are done going through one instance we are done going through one instance of this barrier, the release will be of this barrier, the release will be of this barrier, the release will be one. Before we can reuse this barrier, one. Before we can reuse this barrier, one. Before we can reuse this barrier, the release needs to be set at zero. And the release needs to be set at zero. And the release needs to be set at zero. And that's the purpose of this line here. If that's the purpose of this line here. If that's the purpose of this line here. If we are the first thread to arrive, then we are the first thread to arrive, then we are the first thread to arrive, then we're going to set release to zero. So we're going to set release to zero. So we're going to set release to zero. So the idea is now that as we enter the the idea is now that as we enter the the idea is now that as we enter the barrier, the first thread that arrives barrier, the first thread that arrives barrier, the first thread that arrives and know that this is done in a critical and know that this is done in a critical and know that this is done in a critical section. So exactly one thread will see section. So exactly one thread will see section. So exactly one thread will see this as zero. It sets release to zero. this as zero. It sets release to zero. this as zero. It sets release to zero. That thread then increments the count, That thread then increments the count, That thread then increments the count, it becomes one and then goes on here. it becomes one and then goes on here. it becomes one and then goes on here. That thread and all the other threads That thread and all the other threads That thread and all the other threads now see release as zero. And the only now see release as zero. And the only now see release as zero. And the only way it becomes one is if count reaches way it becomes one is if count reaches way it becomes one is if count reaches the total. So what happens is the first the total. So what happens is the first the total. So what happens is the first thread might actually be delayed and thread might actually be delayed and thread might actually be delayed and read the count after everybody is done read the count after everybody is done read the count after everybody is done in which case it's going to do this or in which case it's going to do this or in which case it's going to do this or it might go here and spin. But either it might go here and spin. But either it might go here and spin. But either way release becoming one means that way release becoming one means that way release becoming one means that somebody got to this point which is when somebody got to this point which is when somebody got to this point which is when the barrier should be released. One or the barrier should be released. One or the barrier should be released. One or more threads that arrive towards the end more threads that arrive towards the end more threads that arrive towards the end are going to do this which will reset are going to do this which will reset are going to do this which will reset the count so that next time we enter we the count so that next time we enter we the count so that next time we enter we see count zero and release is set to one see count zero and release is set to one see count zero and release is set to one which ensures that those threads that which ensures that those threads that which ensures that those threads that did end up here are going to see release did end up here are going to see release did end up here are going to see release as one. Note that the meaning of the as one. Note that the meaning of the as one. Note that the meaning of the spin release equals 1 is actually that spin release equals 1 is actually that spin release equals 1 is actually that we wait for release to be one. It turns we wait for release to be one. It turns we wait for release to be one. It turns out that this barrier code is not out that this barrier code is not out that this barrier code is not entirely correct. If two threads are entirely correct. If two threads are entirely correct. If two threads are synchronizing on this barrier, the total synchronizing on this barrier, the total synchronizing on this barrier, the total will be two and the barrier actually will be two and the barrier actually will be two and the barrier actually works the first time. So the thread one works the first time. So the thread one works the first time. So the thread one reaches the barrier and so does thread reaches the barrier and so does thread reaches the barrier and so does thread two. Now they synchronize on this two. Now they synchronize on this two. Now they synchronize on this barrier and that will work. But if we barrier and that will work. But if we barrier and that will work. But if we try to continue work and then try to continue work and then try to continue work and then synchronize on the same barrier variable synchronize on the same barrier variable synchronize on the same barrier variable again, we would expect that this setting again, we would expect that this setting again, we would expect that this setting of the count to zero and this setting of of the count to zero and this setting of of the count to zero and this setting of the release to zero to work. But it the release to zero to work. But it the release to zero to work. But it doesn't always work. So this is not an doesn't always work. So this is not an doesn't always work. So this is not an entirely correct implementation of a entirely correct implementation of a entirely correct implementation of a barrier that can be used more than once.

## Simple Barrier Implementation Doesn't Work： Part 5
Kind: captions Language: en so the simple barrier operation doesn't so the simple barrier operation doesn't so the simple barrier operation doesn't work entirely and we look now at why it work entirely and we look now at why it work entirely and we look now at why it doesn't work let's consider only two doesn't work let's consider only two doesn't work let's consider only two cores core zero which we will show in cores core zero which we will show in cores core zero which we will show in blue color what it's doing and core one blue color what it's doing and core one blue color what it's doing and core one which will show in this color what it's which will show in this color what it's which will show in this color what it's doing what they're both doing initially doing what they're both doing initially doing what they're both doing initially is doing something before they arrive to is doing something before they arrive to is doing something before they arrive to the barrier and let's say that core zero the barrier and let's say that core zero the barrier and let's say that core zero arrives here first so what core zero arrives here first so what core zero arrives here first so what core zero does is resets release to 0 increments does is resets release to 0 increments does is resets release to 0 increments the count to 1 the count to 1 the count to 1 unlocks the lock unlocks the lock unlocks the lock checks whether the count is equal to 2 checks whether the count is equal to 2 checks whether the count is equal to 2 and starts spinning waiting for the and starts spinning waiting for the and starts spinning waiting for the release to become 1. so now release is release to become 1. so now release is release to become 1. so now release is still 0 because still 0 because still 0 because we just reset it and core zero is gonna we just reset it and core zero is gonna we just reset it and core zero is gonna stay here spinning so what core zero stay here spinning so what core zero stay here spinning so what core zero does is it essentially does is it essentially does is it essentially does a load word of release c0 and will does a load word of release c0 and will does a load word of release c0 and will check again and it keeps doing that check again and it keeps doing that check again and it keeps doing that now let's see what happens when core 1 now let's see what happens when core 1 now let's see what happens when core 1 arrives arrives arrives normally what should happen is core 1 normally what should happen is core 1 normally what should happen is core 1 arrives doesn't do this because count is arrives doesn't do this because count is arrives doesn't do this because count is now 1 now 1 now 1 increments the count exits the critical increments the count exits the critical increments the count exits the critical section sees that the count is equal to section sees that the count is equal to section sees that the count is equal to two two two resets the count resets the count resets the count sets the release to one sets the release to one sets the release to one and now and now and now core one proceeds past the barrier what core one proceeds past the barrier what core one proceeds past the barrier what should happen now is course zero should should happen now is course zero should should happen now is course zero should see this release that we just set to 1 see this release that we just set to 1 see this release that we just set to 1 and also proceed past the barrier so and also proceed past the barrier so and also proceed past the barrier so what should happen is what should happen is what should happen is really that we set the release to 1 here really that we set the release to 1 here really that we set the release to 1 here and core 0 and core 0 and core 0 sees that sees that sees that exits the barrier and from then exits the barrier and from then exits the barrier and from then everything will actually be correct but everything will actually be correct but everything will actually be correct but let's say that core zero has been let's say that core zero has been let's say that core zero has been checking the release checking the release checking the release however when the store happens core zero however when the store happens core zero however when the store happens core zero is delayed for example it's executing is delayed for example it's executing is delayed for example it's executing some sort of an interrupt function so some sort of an interrupt function so some sort of an interrupt function so for a while it's suspended in this place for a while it's suspended in this place for a while it's suspended in this place in the code and cannot read the release in the code and cannot read the release in the code and cannot read the release fast enough in that case we don't have fast enough in that case we don't have fast enough in that case we don't have this this this and core 0 is still stuck here in a and core 0 is still stuck here in a and core 0 is still stuck here in a correct barrier implementation core 0 correct barrier implementation core 0 correct barrier implementation core 0 will eventually leave at some point it will eventually leave at some point it will eventually leave at some point it will return from the interrupt read the will return from the interrupt read the will return from the interrupt read the release that is 1 and proceed release that is 1 and proceed release that is 1 and proceed however however however what happens if core 0 is waiting here what happens if core 0 is waiting here what happens if core 0 is waiting here while core 1 after releasing and exiting while core 1 after releasing and exiting while core 1 after releasing and exiting the barrier the barrier the barrier does very little work does very little work does very little work and then comes back to the barrier again and then comes back to the barrier again and then comes back to the barrier again it sees that the count is zero it sees that the count is zero it sees that the count is zero because it reset it to zero when it was because it reset it to zero when it was because it reset it to zero when it was releasing the barrier and sets the releasing the barrier and sets the releasing the barrier and sets the release to one and now it writes zero to release to one and now it writes zero to release to one and now it writes zero to the release variable and now even if the release variable and now even if the release variable and now even if core zero checks the release here and core zero checks the release here and core zero checks the release here and sees what it is it's gonna see this zero sees what it is it's gonna see this zero sees what it is it's gonna see this zero so core zero is gonna proceed to wait so core zero is gonna proceed to wait so core zero is gonna proceed to wait here for a very long time meanwhile core here for a very long time meanwhile core here for a very long time meanwhile core one one one increments the count to one checks if increments the count to one checks if increments the count to one checks if it's equal to two for this second it's equal to two for this second it's equal to two for this second iteration of the barrier it's not two iteration of the barrier it's not two iteration of the barrier it's not two so court one so court one so court one is now gonna spin waiting for somebody is now gonna spin waiting for somebody is now gonna spin waiting for somebody to release the second instance of the to release the second instance of the to release the second instance of the barrier while core zero is gonna be barrier while core zero is gonna be barrier while core zero is gonna be waiting there waiting there waiting there in the first instance of the barrier and in the first instance of the barrier and in the first instance of the barrier and never being released so the only thing never being released so the only thing never being released so the only thing that can release core one now is if core that can release core one now is if core that can release core one now is if core zero somehow left zero somehow left zero somehow left and enter the second instance of the and enter the second instance of the and enter the second instance of the barrier the only thing that can release barrier the only thing that can release barrier the only thing that can release core zero however is if somebody core zero however is if somebody core zero however is if somebody comes and releases here but that's not comes and releases here but that's not comes and releases here but that's not gonna be core tool so now we have a gonna be core tool so now we have a gonna be core tool so now we have a deadlock situation

## Simple Barrier Quiz： Part 5
Kind: captions Language: en let's see if we understood what the let's see if we understood what the let's see if we understood what the problem is with a simple barrier if we problem is with a simple barrier if we problem is with a simple barrier if we have thread 0 that creates a thread 1 have thread 0 that creates a thread 1 have thread 0 that creates a thread 1 they both do some work they use a they both do some work they use a they both do some work they use a barrier to synchronize and then barrier to synchronize and then barrier to synchronize and then immediately after that thread one end immediately after that thread one end immediately after that thread one end and thread 0 is the only one that and thread 0 is the only one that and thread 0 is the only one that continues does the simple barrier work continues does the simple barrier work continues does the simple barrier work for this code yes or no

## Simple Barrier Quiz Solution： Part 5
Kind: captions Language: en let's look at the answer to a simple let's look at the answer to a simple let's look at the answer to a simple barrier quiz where we had this program barrier quiz where we had this program barrier quiz where we had this program where both threads are going to where both threads are going to where both threads are going to synchronize on a single barrier and then synchronize on a single barrier and then synchronize on a single barrier and then thread one finishes and then thread 0 thread one finishes and then thread 0 thread one finishes and then thread 0 continues alone the question is does our continues alone the question is does our continues alone the question is does our simple barrier implementation work for simple barrier implementation work for simple barrier implementation work for that and the answer is yes it does work that and the answer is yes it does work that and the answer is yes it does work if we only do this barrier once then the if we only do this barrier once then the if we only do this barrier once then the first read that enters this critical first read that enters this critical first read that enters this critical section will set the release 20 and section will set the release 20 and section will set the release 20 and increment the count 21 the second thread increment the count 21 the second thread increment the count 21 the second thread that reaches this will increment the that reaches this will increment the that reaches this will increment the count 22 if the first thread ends up count 22 if the first thread ends up count 22 if the first thread ends up waiting for release the second one will waiting for release the second one will waiting for release the second one will still see that the count is to reset the still see that the count is to reset the still see that the count is to reset the count set the release and eventually due count set the release and eventually due count set the release and eventually due to coherence remember that coherence to coherence remember that coherence to coherence remember that coherence guarantees that when we read the release guarantees that when we read the release guarantees that when we read the release eventually we will see it is just that eventually we will see it is just that eventually we will see it is just that we can be arbitrarily delayed but no we can be arbitrarily delayed but no we can be arbitrarily delayed but no matter how long we are delayed matter how long we are delayed matter how long we are delayed eventually we see it and there is no way eventually we see it and there is no way eventually we see it and there is no way to get stuck here so the thread that to get stuck here so the thread that to get stuck here so the thread that does the releasing leaves the barrier does the releasing leaves the barrier does the releasing leaves the barrier that real easy leave the barrier or it that real easy leave the barrier or it that real easy leave the barrier or it might happen that both of them execute might happen that both of them execute might happen that both of them execute this in very close proximity to each this in very close proximity to each this in very close proximity to each other both of them now see the new count other both of them now see the new count other both of them now see the new count of two and then both of them release and of two and then both of them release and of two and then both of them release and exit the barrier either way nobody gets exit the barrier either way nobody gets exit the barrier either way nobody gets stuck the reason why the simpler barrier stuck the reason why the simpler barrier stuck the reason why the simpler barrier is not entirely correct is not that it is not entirely correct is not that it is not entirely correct is not that it doesn't work on the first try it's that doesn't work on the first try it's that doesn't work on the first try it's that it's not reusable so the problem occurs it's not reusable so the problem occurs it's not reusable so the problem occurs if we try to use this barrier again if we try to use this barrier again if we try to use this barrier again after the first time you use it in which after the first time you use it in which after the first time you use it in which case somebody might be stuck still here case somebody might be stuck still here case somebody might be stuck still here and we reset this so that they never and we reset this so that they never and we reset this so that they never leave the first instance while everybody leave the first instance while everybody leave the first instance while everybody else is in the second instance but else is in the second instance but else is in the second instance but unless we have that second instance of unless we have that second instance of unless we have that second instance of the barrier this barrier works so in the barrier this barrier works so in the barrier this barrier works so in this particular case if we are sure that this particular case if we are sure that this particular case if we are sure that we are going to use this better only we are going to use this better only we are going to use this better only once in the entire run of the program once in the entire run of the program once in the entire run of the program then this barrier does work correctly

## Reusable Barrier： Part 5
Kind: captions Language: en so how do we implement reusable barriers so how do we implement reusable barriers so how do we implement reusable barriers correctly this is how the idea here is correctly this is how the idea here is correctly this is how the idea here is that the value for releasing the barrier that the value for releasing the barrier that the value for releasing the barrier will not be the same for all instances will not be the same for all instances will not be the same for all instances of the barrier even instances when of the barrier even instances when of the barrier even instances when release becomes zero all the instances release becomes zero all the instances release becomes zero all the instances are gonna release when release becomes are gonna release when release becomes are gonna release when release becomes one so the idea is that we never really one so the idea is that we never really one so the idea is that we never really need to reinitialize the release we just need to reinitialize the release we just need to reinitialize the release we just flip the release each thread now has a flip the release each thread now has a flip the release each thread now has a local sense which is kind of what's the local sense which is kind of what's the local sense which is kind of what's the release that we should get in order to release that we should get in order to release that we should get in order to get out so if we have two threads 0 and get out so if we have two threads 0 and get out so if we have two threads 0 and 1 let's say that they started with local 1 let's say that they started with local 1 let's say that they started with local sense being 0 now they're gonna get here sense being 0 now they're gonna get here sense being 0 now they're gonna get here and they're both gonna figure out that and they're both gonna figure out that and they're both gonna figure out that the local sense is 1 local sense is the local sense is 1 local sense is the local sense is 1 local sense is local to each thread so each of them local to each thread so each of them local to each thread so each of them just sets a local variable independently just sets a local variable independently just sets a local variable independently so this is not going to interfere with so this is not going to interfere with so this is not going to interfere with each other now one of the thread will each other now one of the thread will each other now one of the thread will enter the counter LOC increment the enter the counter LOC increment the enter the counter LOC increment the count and here if the count is equal to count and here if the count is equal to count and here if the count is equal to the total they will reset the count and the total they will reset the count and the total they will reset the count and set the release to local sense so it set the release to local sense so it set the release to local sense so it sets the release to what we are waiting sets the release to what we are waiting sets the release to what we are waiting for in this case let's say thread 1 for in this case let's say thread 1 for in this case let's say thread 1 releases it to 1 that means that thread releases it to 1 that means that thread releases it to 1 that means that thread 0 when it incremented account didn't see 0 when it incremented account didn't see 0 when it incremented account didn't see the total add to it so it at 1 and the total add to it so it at 1 and the total add to it so it at 1 and proceeded to exit here and enters this proceeded to exit here and enters this proceeded to exit here and enters this weight here it's waiting now for the weight here it's waiting now for the weight here it's waiting now for the release to become what it thinks local release to become what it thinks local release to become what it thinks local senses which is 1 so it's going to spin senses which is 1 so it's going to spin senses which is 1 so it's going to spin until somebody puts one into the release until somebody puts one into the release until somebody puts one into the release eventually thread 1 does that so now eventually thread 1 does that so now eventually thread 1 does that so now let's say that thread 0 is stuck here let's say that thread 0 is stuck here let's say that thread 0 is stuck here that was the problem with the previous that was the problem with the previous that was the problem with the previous implementation of the barrier let's say implementation of the barrier let's say implementation of the barrier let's say that we just don't read it fast enough that we just don't read it fast enough that we just don't read it fast enough so we want to see a 1 but we don't check so we want to see a 1 but we don't check so we want to see a 1 but we don't check it fast enough thread 1 now proceeds to it fast enough thread 1 now proceeds to it fast enough thread 1 now proceeds to exit finishes the work and comes back to exit finishes the work and comes back to exit finishes the work and comes back to the second instance of the barrier it the second instance of the barrier it the second instance of the barrier it now flips its own local sense to 0 now flips its own local sense to 0 now flips its own local sense to 0 increments the count to 1 because we increments the count to 1 because we increments the count to 1 because we reset the count when we were releasing reset the count when we were releasing reset the count when we were releasing the first instance checks the count sees the first instance checks the count sees the first instance checks the count sees that it is 1 doesn't do this unlocks the that it is 1 doesn't do this unlocks the that it is 1 doesn't do this unlocks the lock lock lock and enters the spin here but thread one and enters the spin here but thread one and enters the spin here but thread one is now checking whether the release has is now checking whether the release has is now checking whether the release has become zero because it flipped the local become zero because it flipped the local become zero because it flipped the local sense now it's waiting for this value sense now it's waiting for this value sense now it's waiting for this value for the release so they're waiting for for the release so they're waiting for for the release so they're waiting for different value different value different value note that the release is still the same note that the release is still the same note that the release is still the same as thread one left it in the first as thread one left it in the first as thread one left it in the first instance instance instance so eventually thread zero will check and so eventually thread zero will check and so eventually thread zero will check and see that the release has become one at see that the release has become one at see that the release has become one at which point it will leave eventually which point it will leave eventually which point it will leave eventually it's gonna come here change its local it's gonna come here change its local it's gonna come here change its local sense to zero increment the count see sense to zero increment the count see sense to zero increment the count see that it's to reset the count set the that it's to reset the count set the that it's to reset the count set the release to zero this time and now thread release to zero this time and now thread release to zero this time and now thread one will be released so by flipping the one will be released so by flipping the one will be released so by flipping the value of the release instead of value of the release instead of value of the release instead of reinitializing it in each iteration of reinitializing it in each iteration of reinitializing it in each iteration of the loop we avoid the problem that we the loop we avoid the problem that we the loop we avoid the problem that we had so this barrier is reusable we can had so this barrier is reusable we can had so this barrier is reusable we can synchronize on the same barrier over and synchronize on the same barrier over and synchronize on the same barrier over and over without risking deadlock situations

## Synchronization Outro： Part 5
Kind: captions Language: en Invest leave Don't ever forget synchronize Invest leave Don't ever forget synchronize Invest leave Don't ever forget synchronize and in operation such as take and in operation such as take and in operation such as take Laughing accent would need to coordinate activities am Laughing accent would need to coordinate activities am Laughing accent would need to coordinate activities am I kiss you Friends and special Atomic I kiss you Friends and special Atomic I kiss you Friends and special Atomic instructions damn You implement to instructions damn You implement to instructions damn You implement to exceptional In The next to breast website exceptional In The next to breast website exceptional In The next to breast website synchronization shin when messed up When a synchronization shin when messed up When a synchronization shin when messed up When a processor conveyor loading Station orce processor conveyor loading Station orce processor conveyor loading Station orce an when how to Guard against Bet


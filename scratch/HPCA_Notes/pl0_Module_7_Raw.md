# Playlist 0 Module 7 Raw Transcript

## Predict Not Taken： Part 1
Kind: captions Language: en so let's talk more about the predict so let's talk more about the predict so let's talk more about the predict not-taken predictor which is the not-taken predictor which is the not-taken predictor which is the simplest predictor we can have it simplest predictor we can have it simplest predictor we can have it amounts to simply incrementing the PC so amounts to simply incrementing the PC so amounts to simply incrementing the PC so this predictor is very simple because this predictor is very simple because this predictor is very simple because all it requires us is to increment the all it requires us is to increment the all it requires us is to increment the PC we know the PC from which we fetch PC we know the PC from which we fetch PC we know the PC from which we fetch the branch we know how big the the branch we know how big the the branch we know how big the instruction is we can just increment the instruction is we can just increment the instruction is we can just increment the PC so there is really no memory to this PC so there is really no memory to this PC so there is really no memory to this predictor or anything else we have to do predictor or anything else we have to do predictor or anything else we have to do the increment of the PC anyway once we the increment of the PC anyway once we the increment of the PC anyway once we figure out it's not a branch so this figure out it's not a branch so this figure out it's not a branch so this predictor pretty much uses the hardware predictor pretty much uses the hardware predictor pretty much uses the hardware we have to have anyway but the question we have to have anyway but the question we have to have anyway but the question is how accurate is it a rule of thumb is is how accurate is it a rule of thumb is is how accurate is it a rule of thumb is that about 20% of all instructions are that about 20% of all instructions are that about 20% of all instructions are branches so 80% of the time this branches so 80% of the time this branches so 80% of the time this predictor will be correct because it's predictor will be correct because it's predictor will be correct because it's simply not a branch instruction for simply not a branch instruction for simply not a branch instruction for branches a little more over half of the branches a little more over half of the branches a little more over half of the branches are taken so let's say that 60% branches are taken so let's say that 60% branches are taken so let's say that 60% of branches are taken so what we get is of branches are taken so what we get is of branches are taken so what we get is that this predictor is correct 80% of that this predictor is correct 80% of that this predictor is correct 80% of the time for non branches and also the time for non branches and also the time for non branches and also another 8% of all the instructions another 8% of all the instructions another 8% of all the instructions because of the 40% of branches that are because of the 40% of branches that are because of the 40% of branches that are not taken it is incorrect 12% of the not taken it is incorrect 12% of the not taken it is incorrect 12% of the time it is incorrect 60 percent of 20 time it is incorrect 60 percent of 20 time it is incorrect 60 percent of 20 percent which amounts to 12% now if we percent which amounts to 12% now if we percent which amounts to 12% now if we know the misprediction penalty we can know the misprediction penalty we can know the misprediction penalty we can easily compute what is the impact on CPI easily compute what is the impact on CPI easily compute what is the impact on CPI of branch mispredictions the CPI will be of branch mispredictions the CPI will be of branch mispredictions the CPI will be 1 plus 12% times the penalty in a 5 1 plus 12% times the penalty in a 5 1 plus 12% times the penalty in a 5 stage pipeline this would be 2 so the stage pipeline this would be 2 so the stage pipeline this would be 2 so the CPI would be one point 24

## Why We Need Better Prediction： Part 1
Kind: captions Language: en So we have seen that our predict not So we have seen that our predict not So we have seen that our predict not taken predictor that is very simple to taken predictor that is very simple to taken predictor that is very simple to implement is pretty accurate for about implement is pretty accurate for about implement is pretty accurate for about 88% of all instructions. It predicts 88% of all instructions. It predicts 88% of all instructions. It predicts correctly. So why do we even need any correctly. So why do we even need any correctly. So why do we even need any better predictors because anything better predictors because anything better predictors because anything better than this will require some better than this will require some better than this will require some investment of hardware to do the investment of hardware to do the investment of hardware to do the prediction better. We will answer this prediction better. We will answer this prediction better. We will answer this question in two ways. First we will question in two ways. First we will question in two ways. First we will compare the CPI with the not taken compare the CPI with the not taken compare the CPI with the not taken predictor that is let's say 88% accurate predictor that is let's say 88% accurate predictor that is let's say 88% accurate and with a better predictor that is and with a better predictor that is and with a better predictor that is let's say 99% accurate. And we will do let's say 99% accurate. And we will do let's say 99% accurate. And we will do this for a five- stage pipeline that this for a five- stage pipeline that this for a five- stage pipeline that resolves branches in the third stage. So resolves branches in the third stage. So resolves branches in the third stage. So it has a two cycle penalty whenever it has a two cycle penalty whenever it has a two cycle penalty whenever there is a misprediction. So it has a there is a misprediction. So it has a there is a misprediction. So it has a two cycle penalty when there is a two cycle penalty when there is a two cycle penalty when there is a misprediction. misprediction. misprediction. We will also do it for a 14 stage We will also do it for a 14 stage We will also do it for a 14 stage pipeline that resolves branches in the pipeline that resolves branches in the pipeline that resolves branches in the 11th stage. So it has a 10 cycle penalty 11th stage. So it has a 10 cycle penalty 11th stage. So it has a 10 cycle penalty for a for a for a misprediction. And for another pipeline misprediction. And for another pipeline misprediction. And for another pipeline that has 14 stages, resolves branches in that has 14 stages, resolves branches in that has 14 stages, resolves branches in 11th stage. But this one executes four 11th stage. But this one executes four 11th stage. But this one executes four instructions per cycle. So this is very instructions per cycle. So this is very instructions per cycle. So this is very close to what modern processors look close to what modern processors look close to what modern processors look like. So for a not taken predictor and a like. So for a not taken predictor and a like. So for a not taken predictor and a five-stage pipe, the CPI we get is 1 five-stage pipe, the CPI we get is 1 five-stage pipe, the CPI we get is 1 plus how often we mispredict which is plus how often we mispredict which is plus how often we mispredict which is 12% of all instructions times the 12% of all instructions times the 12% of all instructions times the penalty of a misprediction which is two penalty of a misprediction which is two penalty of a misprediction which is two cycles which gives us a total CPI of cycles which gives us a total CPI of cycles which gives us a total CPI of 1.24. What happens when we have a better 1.24. What happens when we have a better 1.24. What happens when we have a better predictor? Well, we now get 1 + 0.01 01 predictor? Well, we now get 1 + 0.01 01 predictor? Well, we now get 1 + 0.01 01 only 1% of the time we mispredict and we only 1% of the time we mispredict and we only 1% of the time we mispredict and we get a two cycle penalty then so the CPI get a two cycle penalty then so the CPI get a two cycle penalty then so the CPI now is now is now is 102 and now let's look also at the speed 102 and now let's look also at the speed 102 and now let's look also at the speed up that we get from a better predictor up that we get from a better predictor up that we get from a better predictor if the frequency of the processor stays if the frequency of the processor stays if the frequency of the processor stays the same and it executes the same number the same and it executes the same number the same and it executes the same number of instructions then the speed up is of instructions then the speed up is of instructions then the speed up is simply the improvement in the CPI so the simply the improvement in the CPI so the simply the improvement in the CPI so the speed up would be the CPI of the not speed up would be the CPI of the not speed up would be the CPI of the not taken predict but divided by the CPI of taken predict but divided by the CPI of taken predict but divided by the CPI of the better predictor and we get a speed the better predictor and we get a speed the better predictor and we get a speed up of up of up of 1.22. This is actually a noticeable 1.22. This is actually a noticeable 1.22. This is actually a noticeable speed up. But things get more speed up. But things get more speed up. But things get more interesting when we have a deeper interesting when we have a deeper interesting when we have a deeper pipeline. In that case, we have 1 + pipeline. In that case, we have 1 + pipeline. In that case, we have 1 + still 12% accuracy for not taken. But still 12% accuracy for not taken. But still 12% accuracy for not taken. But this time, the penalty because we this time, the penalty because we this time, the penalty because we resolve branches in 11th stage will be resolve branches in 11th stage will be resolve branches in 11th stage will be 10 and we get an overall CPI of 2.2. 10 and we get an overall CPI of 2.2. 10 and we get an overall CPI of 2.2. two. This is much worse than the one we two. This is much worse than the one we two. This is much worse than the one we would be getting with no would be getting with no would be getting with no mispredictions. With our better mispredictions. With our better mispredictions. With our better predictor, we get 1 + 1% misprediction predictor, we get 1 + 1% misprediction predictor, we get 1 + 1% misprediction rate time 10 for a total CPI of rate time 10 for a total CPI of rate time 10 for a total CPI of 1.1 and that gives us a speed up of two. 1.1 and that gives us a speed up of two. 1.1 and that gives us a speed up of two. So as we can see now the better So as we can see now the better So as we can see now the better predictor is giving us twice the predictor is giving us twice the predictor is giving us twice the performance of the not taken predictor. performance of the not taken predictor. performance of the not taken predictor. And we have seen this already that the And we have seen this already that the And we have seen this already that the deeper the pipeline the more benefit we deeper the pipeline the more benefit we deeper the pipeline the more benefit we get from a better predictor. But what get from a better predictor. But what get from a better predictor. But what happens when we also do multiple happens when we also do multiple happens when we also do multiple instructions per cycle. So this instructions per cycle. So this instructions per cycle. So this processor can do four instructions in a processor can do four instructions in a processor can do four instructions in a cycle. So the ideal cycles per cycle. So the ideal cycles per cycle. So the ideal cycles per instruction is one quarter of a cycle. instruction is one quarter of a cycle. instruction is one quarter of a cycle. We add to that how often we have We add to that how often we have We add to that how often we have mispredictions times how many cycles do mispredictions times how many cycles do mispredictions times how many cycles do we waste for a misprediction. we still we waste for a misprediction. we still we waste for a misprediction. we still detect mispredictions in the 11th stage. detect mispredictions in the 11th stage. detect mispredictions in the 11th stage. So everything done in the 10 cycles So everything done in the 10 cycles So everything done in the 10 cycles after the branch was fetched is wasted. after the branch was fetched is wasted. after the branch was fetched is wasted. So we still have a 10 cycle penalty here So we still have a 10 cycle penalty here So we still have a 10 cycle penalty here and we get an overall CPI of and we get an overall CPI of and we get an overall CPI of 1.45. That's actually less than one 1.45. That's actually less than one 1.45. That's actually less than one instruction per cycle. It's better than instruction per cycle. It's better than instruction per cycle. It's better than a single instruction per cycle a single instruction per cycle a single instruction per cycle processor, but not that much better for processor, but not that much better for processor, but not that much better for something that should be four times as something that should be four times as something that should be four times as capable. Now, let's look at the better capable. Now, let's look at the better capable. Now, let's look at the better predictor, which should give us the predictor, which should give us the predictor, which should give us the ideal CPI plus 1% of the time 10 cycle ideal CPI plus 1% of the time 10 cycle ideal CPI plus 1% of the time 10 cycle penalty. And that results in a CPI of penalty. And that results in a CPI of penalty. And that results in a CPI of 0.35 or about three instructions per 0.35 or about three instructions per 0.35 or about three instructions per cycle even with the branches. If we cycle even with the branches. If we cycle even with the branches. If we compute the speed up, it's 4.14. compute the speed up, it's 4.14. compute the speed up, it's 4.14. So as you can see if we have a deeper So as you can see if we have a deeper So as you can see if we have a deeper pipeline or if we are able to execute pipeline or if we are able to execute pipeline or if we are able to execute more instructions per cycle then the more instructions per cycle then the more instructions per cycle then the better predictor is more important than better predictor is more important than better predictor is more important than in simpler processors.

## Predictor Impact Quiz： Part 1
Kind: captions Language: en so let's see if we understood the impact so let's see if we understood the impact so let's see if we understood the impact of branch pictors on performance we will of branch pictors on performance we will of branch pictors on performance we will consider the old penum 4 prescod consider the old penum 4 prescod consider the old penum 4 prescod processor which has an extremely deep processor which has an extremely deep processor which has an extremely deep pipeline it fetches then does 29 stages pipeline it fetches then does 29 stages pipeline it fetches then does 29 stages worth of work and only then it resolves worth of work and only then it resolves worth of work and only then it resolves branches it of course uses Branch branches it of course uses Branch branches it of course uses Branch friction and is capable of executing friction and is capable of executing friction and is capable of executing multiple instructions per cycle so that multiple instructions per cycle so that multiple instructions per cycle so that in a program where about 20% of all in a program where about 20% of all in a program where about 20% of all instructions are branches only 1% of instructions are branches only 1% of instructions are branches only 1% of branches might be mispredicted and let's branches might be mispredicted and let's branches might be mispredicted and let's say that that gives us a CPI on this say that that gives us a CPI on this say that that gives us a CPI on this program of program of program of 0.5 so the question for you is if we had 0.5 so the question for you is if we had 0.5 so the question for you is if we had a slightly worse predictor so that 2% of a slightly worse predictor so that 2% of a slightly worse predictor so that 2% of branches are mispredicted in this branches are mispredicted in this branches are mispredicted in this program how would that change the CPI program how would that change the CPI program how would that change the CPI what would the CPI be

## Predictor Impact Solution Quiz： Part 1
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our predictor impact quiz where we consider predictor impact quiz where we consider predictor impact quiz where we consider the intel pentium 4 price code processor the intel pentium 4 price code processor the intel pentium 4 price code processor that spends 30 cycles until it finally that spends 30 cycles until it finally that spends 30 cycles until it finally resolves branches uses branch prediction resolves branches uses branch prediction resolves branches uses branch prediction and multiple instructions per cycle so and multiple instructions per cycle so and multiple instructions per cycle so that in a program where 20 of that in a program where 20 of that in a program where 20 of instructions are branches instructions are branches instructions are branches and where one percent of branches are and where one percent of branches are and where one percent of branches are mispredicted it gets a cpi of 0.5 mispredicted it gets a cpi of 0.5 mispredicted it gets a cpi of 0.5 overall in this cpi overall in this cpi overall in this cpi we are getting we are getting we are getting some ideal cpi plus for branches that we some ideal cpi plus for branches that we some ideal cpi plus for branches that we mispredict 1 of the time mispredict 1 of the time mispredict 1 of the time we pay a we pay a we pay a 30 cycle penalty because we resolve 30 cycle penalty because we resolve 30 cycle penalty because we resolve branches really in the 31st cycle branches really in the 31st cycle branches really in the 31st cycle and that means that the ideal cpi and that means that the ideal cpi and that means that the ideal cpi is equal to the actual cpi we are is equal to the actual cpi we are is equal to the actual cpi we are getting minus getting minus getting minus the cpi penalty from branches the cpi penalty from branches the cpi penalty from branches and if we multiply this out we get 0.06 and if we multiply this out we get 0.06 and if we multiply this out we get 0.06 so x here the ideal cpi with a perfect so x here the ideal cpi with a perfect so x here the ideal cpi with a perfect branch vector branch vector branch vector would be would be would be 0.44 0.44 0.44 now when two percent of branches are now when two percent of branches are now when two percent of branches are mispredicted our cpi will be equal to mispredicted our cpi will be equal to mispredicted our cpi will be equal to x plus x plus x plus for branches for branches for branches we mispredict we mispredict we mispredict two percent of the time and we pay a 30 two percent of the time and we pay a 30 two percent of the time and we pay a 30 cycle penalty so we get 0.44 plus cycle penalty so we get 0.44 plus cycle penalty so we get 0.44 plus this which multiplies out to 0.12 this which multiplies out to 0.12 this which multiplies out to 0.12 for an overall cpi now of 0.56 for an overall cpi now of 0.56 for an overall cpi now of 0.56 so the key to solving this again was so the key to solving this again was so the key to solving this again was that we were given the actual cpi we that we were given the actual cpi we that we were given the actual cpi we needed to compute what the ideal cpi is needed to compute what the ideal cpi is needed to compute what the ideal cpi is after we remove the branch penalty after we remove the branch penalty after we remove the branch penalty and then we compute the new branch and then we compute the new branch and then we compute the new branch penalty and add it to this ideal cpi to penalty and add it to this ideal cpi to penalty and add it to this ideal cpi to get this answer

## Why We Need Better Predicition Part 2： Part 1
Kind: captions Language: en we said that we will explain why we need we said that we will explain why we need we said that we will explain why we need a better predictor in two ways this was a better predictor in two ways this was a better predictor in two ways this was one way the cpi gets a lot better the one way the cpi gets a lot better the one way the cpi gets a lot better the other way other way other way will be to see will be to see will be to see how much waste there is how much waste there is how much waste there is from amy's prediction so in a five-stage from amy's prediction so in a five-stage from amy's prediction so in a five-stage pipeline what's happening really we have pipeline what's happening really we have pipeline what's happening really we have three stages three stages three stages that are relevant the one where we that are relevant the one where we that are relevant the one where we resolve the branches and then we have to resolve the branches and then we have to resolve the branches and then we have to destroy two instructions that we have destroy two instructions that we have destroy two instructions that we have fetched if this branch has been fetched if this branch has been fetched if this branch has been mispredicted so a misprediction costs us mispredicted so a misprediction costs us mispredicted so a misprediction costs us two instructions here two instructions here two instructions here in a deeper pipeline what happens is we in a deeper pipeline what happens is we in a deeper pipeline what happens is we have our branch and for 10 cycles we have our branch and for 10 cycles we have our branch and for 10 cycles we have been fetching things so a have been fetching things so a have been fetching things so a misprediction now costs us 10 misprediction now costs us 10 misprediction now costs us 10 instructions instructions instructions worth of work and that means that worth of work and that means that worth of work and that means that avoiding mispredictions becomes more avoiding mispredictions becomes more avoiding mispredictions becomes more important and finally when we have a important and finally when we have a important and finally when we have a four instruction per cycle processor four instruction per cycle processor four instruction per cycle processor that resolves branches in the 11th stage that resolves branches in the 11th stage that resolves branches in the 11th stage after we have fetched this branch we after we have fetched this branch we after we have fetched this branch we have fetched a lot of instructions after have fetched a lot of instructions after have fetched a lot of instructions after we fetched the branch we fetched four we fetched the branch we fetched four we fetched the branch we fetched four instructions in the next cycle and four instructions in the next cycle and four instructions in the next cycle and four instructions in the next cycle so now instructions in the next cycle so now instructions in the next cycle so now when we mispredict the branch we waste when we mispredict the branch we waste when we mispredict the branch we waste 40 instructions 40 instructions 40 instructions that we could have done if the that we could have done if the that we could have done if the prediction was correct so the cost of a prediction was correct so the cost of a prediction was correct so the cost of a misprediction is much higher in terms of misprediction is much higher in terms of misprediction is much higher in terms of instructions that we have just wasted instructions that we have just wasted instructions that we have just wasted and that means that again it's much more and that means that again it's much more and that means that again it's much more important to predict branches correctly

## Better Prediction How： Part 1
Kind: captions Language: en so now we have seen that we really want so now we have seen that we really want so now we have seen that we really want to have a better predictor but the to have a better predictor but the to have a better predictor but the question is how we have seen that the question is how we have seen that the question is how we have seen that the predict taken predictor computes the predict taken predictor computes the predict taken predictor computes the next pc next pc next pc as a function of the current pc by just as a function of the current pc by just as a function of the current pc by just incrementing it so the question is if incrementing it so the question is if incrementing it so the question is if all we know is the current pc is there all we know is the current pc is there all we know is the current pc is there any better function that we can use to any better function that we can use to any better function that we can use to form a better prediction of what's form a better prediction of what's form a better prediction of what's coming next and the answer is if all you coming next and the answer is if all you coming next and the answer is if all you know is the current pc know is the current pc know is the current pc then probably you cannot make much of a then probably you cannot make much of a then probably you cannot make much of a better prediction better prediction better prediction it would help us it would help us it would help us if we knew if we knew if we knew is the instruction a branch but we don't is the instruction a branch but we don't is the instruction a branch but we don't know that we are still fetching the know that we are still fetching the know that we are still fetching the instruction while we are making the instruction while we are making the instruction while we are making the prediction is this instruction gonna be prediction is this instruction gonna be prediction is this instruction gonna be taken taken taken as a branch again we have no clue about as a branch again we have no clue about as a branch again we have no clue about that that that what is the offset field what is the offset field what is the offset field of the instruction if this is a branch of the instruction if this is a branch of the instruction if this is a branch this is what will be added to the pc to this is what will be added to the pc to this is what will be added to the pc to form the target address if it's taken form the target address if it's taken form the target address if it's taken but we don't know that either because we but we don't know that either because we but we don't know that either because we haven't fetched instruction so really haven't fetched instruction so really haven't fetched instruction so really we don't know for sure anything we don't know for sure anything we don't know for sure anything that might be really useful in forming that might be really useful in forming that might be really useful in forming the good prediction so it seems like the good prediction so it seems like the good prediction so it seems like it's hopeless but it's not it's hopeless but it's not it's hopeless but it's not so what do we know so what do we know so what do we know if we don't know anything for certain if we don't know anything for certain if we don't know anything for certain what we do know what we do know what we do know is what the current pc is but we also is what the current pc is but we also is what the current pc is but we also know something about how that branch at know something about how that branch at know something about how that branch at this pc was behaving in the past and we this pc was behaving in the past and we this pc was behaving in the past and we can use that because it turns out that can use that because it turns out that can use that because it turns out that branches tend to behave the same way branches tend to behave the same way branches tend to behave the same way over and over again so really that is over and over again so really that is over and over again so really that is what we can use what we can use what we can use we don't know for sure what this branch we don't know for sure what this branch we don't know for sure what this branch is going to do is going to do is going to do but we can know what it did but we can know what it did but we can know what it did the few previous times when it was the few previous times when it was the few previous times when it was executed

## BTB Branch Target Buffer： Part 1
Kind: captions Language: en so the simplest predictor that uses so the simplest predictor that uses so the simplest predictor that uses history is called the branch Target history is called the branch Target history is called the branch Target buffer what it does is it takes the buffer what it does is it takes the buffer what it does is it takes the current PC of the branch and uses that current PC of the branch and uses that current PC of the branch and uses that to index into a table which we call the to index into a table which we call the to index into a table which we call the btb and from this table what we read out btb and from this table what we read out btb and from this table what we read out is our best guess for what the next PC is our best guess for what the next PC is our best guess for what the next PC will be but how do we know what to put will be but how do we know what to put will be but how do we know what to put in this table well we carry the PC of in this table well we carry the PC of in this table well we carry the PC of the branch and the predicted PC the branch and the predicted PC the branch and the predicted PC with it through the pipeline so this is with it through the pipeline so this is with it through the pipeline so this is what happens at fetch later on in the what happens at fetch later on in the what happens at fetch later on in the pipeline we have the correct PC new at pipeline we have the correct PC new at pipeline we have the correct PC new at that point we will compare the predicted that point we will compare the predicted that point we will compare the predicted one with the correct one and if they're one with the correct one and if they're one with the correct one and if they're not equal we will treat that as a not equal we will treat that as a not equal we will treat that as a misprediction and also we will use the misprediction and also we will use the misprediction and also we will use the Branch's own PC to index again into the Branch's own PC to index again into the Branch's own PC to index again into the btb and write this new pc that is btb and write this new pc that is btb and write this new pc that is correct into the table so that next time correct into the table so that next time correct into the table so that next time we see this Branch we get a correct we see this Branch we get a correct we see this Branch we get a correct prediction as assuming that it's jumping prediction as assuming that it's jumping prediction as assuming that it's jumping to the same to the same to the same location but there is a problem with our location but there is a problem with our location but there is a problem with our btb how big does it needs to be we want btb how big does it needs to be we want btb how big does it needs to be we want it to in one cycle given the current PC it to in one cycle given the current PC it to in one cycle given the current PC predict a PC so it needs to have a predict a PC so it needs to have a predict a PC so it needs to have a single cycle latency that means we want single cycle latency that means we want single cycle latency that means we want it to be really small however it needs it to be really small however it needs it to be really small however it needs to contain an entire instruction address to contain an entire instruction address to contain an entire instruction address let's say we're using 64-bit addressing let's say we're using 64-bit addressing let's say we're using 64-bit addressing that means each entry is 8 bytes and we that means each entry is 8 bytes and we that means each entry is 8 bytes and we need one entry for each possible PC from need one entry for each possible PC from need one entry for each possible PC from which we can fetch the program can be which we can fetch the program can be which we can fetch the program can be large note that an instruction can be 4 large note that an instruction can be 4 large note that an instruction can be 4 bytes yet we need eight bytes in the btb bytes yet we need eight bytes in the btb bytes yet we need eight bytes in the btb so really the btb needs to be as large so really the btb needs to be as large so really the btb needs to be as large as the program itself which is really as the program itself which is really as the program itself which is really large so we cannot have a dedicated large so we cannot have a dedicated large so we cannot have a dedicated entry in the btb for every possible PC entry in the btb for every possible PC entry in the btb for every possible PC address we need the btb to be much address we need the btb to be much address we need the btb to be much smaller than that how do we do that

## Realistic BTB： Part 1
Kind: captions Language: en so how do we make this BT be realistic so how do we make this BT be realistic so how do we make this BT be realistic it needs to be small yet it needs to it needs to be small yet it needs to it needs to be small yet it needs to work this way first of all we don't need work this way first of all we don't need work this way first of all we don't need to have an entry for every possible PC to have an entry for every possible PC to have an entry for every possible PC it's enough if we have enough entries it's enough if we have enough entries it's enough if we have enough entries for all the instructions that are likely for all the instructions that are likely for all the instructions that are likely to execute soon so for example if our to execute soon so for example if our to execute soon so for example if our program is executing a loop that has program is executing a loop that has program is executing a loop that has about a hundred instructions of code we about a hundred instructions of code we about a hundred instructions of code we really need only slightly more than a really need only slightly more than a really need only slightly more than a hundred entries after the first hundred entries after the first hundred entries after the first iteration of the loop the BTB will be iteration of the loop the BTB will be iteration of the loop the BTB will be populated with instructions in the loop populated with instructions in the loop populated with instructions in the loop at which point we will keep finding what at which point we will keep finding what at which point we will keep finding what we need in the BTB so now we have we need in the BTB so now we have we need in the BTB so now we have relatively few entries in the BTB let's relatively few entries in the BTB let's relatively few entries in the BTB let's say that we do our timing experiments say that we do our timing experiments say that we do our timing experiments and we find that only 1,024 entries can and we find that only 1,024 entries can and we find that only 1,024 entries can be accessed in one cycle the question is be accessed in one cycle the question is be accessed in one cycle the question is now there are many many possible pieces now there are many many possible pieces now there are many many possible pieces how do we map each PC to an entry in a how do we map each PC to an entry in a how do we map each PC to an entry in a way that avoids conflicts among way that avoids conflicts among way that avoids conflicts among different pcs that map to the same entry different pcs that map to the same entry different pcs that map to the same entry and note also that we need this mapping and note also that we need this mapping and note also that we need this mapping function to be really really simple function to be really really simple function to be really really simple because any delay in computing the because any delay in computing the because any delay in computing the mapping function means that we need an mapping function means that we need an mapping function means that we need an even smaller BTB so that the whole thing even smaller BTB so that the whole thing even smaller BTB so that the whole thing finishes in one cycle the way we do this finishes in one cycle the way we do this finishes in one cycle the way we do this is the program counter from which we are is the program counter from which we are is the program counter from which we are fetching is let's say a 64-bit value so fetching is let's say a 64-bit value so fetching is let's say a 64-bit value so it has bits starting from 0 to 63 we it has bits starting from 0 to 63 we it has bits starting from 0 to 63 we need 10 bits to index into our BTB so need 10 bits to index into our BTB so need 10 bits to index into our BTB so what we will do is we'll take the least what we will do is we'll take the least what we will do is we'll take the least significant 10 bits and use that as the significant 10 bits and use that as the significant 10 bits and use that as the index into our BTB this mapping function index into our BTB this mapping function index into our BTB this mapping function is really fast because all we do is we is really fast because all we do is we is really fast because all we do is we just take these 10 bits and we just feed just take these 10 bits and we just feed just take these 10 bits and we just feed that to the BTB as an index why are we that to the BTB as an index why are we that to the BTB as an index why are we using the least significant bits and not using the least significant bits and not using the least significant bits and not the most significant bits think about a the most significant bits think about a the most significant bits think about a typical program where an instruction has typical program where an instruction has typical program where an instruction has some sort of PC address and then the some sort of PC address and then the some sort of PC address and then the next instruction will have an address next instruction will have an address next instruction will have an address that is very close to it for example that is very close to it for example that is very close to it for example just increment by 4 and so on so if we just increment by 4 and so on so if we just increment by 4 and so on so if we were using the most significant bits of were using the most significant bits of were using the most significant bits of the PC all instructions that are in the the PC all instructions that are in the the PC all instructions that are in the same part of the program would map to same part of the program would map to same part of the program would map to the same entry the same entry the same entry because they have the same most because they have the same most because they have the same most significant bits thus if you have a loop significant bits thus if you have a loop significant bits thus if you have a loop of some sort here all of the of some sort here all of the of some sort here all of the instructions in the loop would map to instructions in the loop would map to instructions in the loop would map to the same entry so that as we execute the same entry so that as we execute the same entry so that as we execute this loop these instructions are kicking this loop these instructions are kicking this loop these instructions are kicking each other's stuff from the BTB each other's stuff from the BTB each other's stuff from the BTB which we don't want to have we want each which we don't want to have we want each which we don't want to have we want each of these to have an entry if we use the of these to have an entry if we use the of these to have an entry if we use the least significant bits what we get is least significant bits what we get is least significant bits what we get is that each instruction gets a different that each instruction gets a different that each instruction gets a different entry from nearby instructions thus entry from nearby instructions thus entry from nearby instructions thus ensuring that things like loops and ensuring that things like loops and ensuring that things like loops and functions and so on neatly map to the functions and so on neatly map to the functions and so on neatly map to the BTB

## BTB Quiz： Part 1
Kind: captions Language: en so now that we have seen how to access so now that we have seen how to access so now that we have seen how to access the btb let's do a quiz let's say we the btb let's do a quiz let's say we the btb let's do a quiz let's say we have a btb with 1,24 entries note that have a btb with 1,24 entries note that have a btb with 1,24 entries note that these entries would be numers 0 through these entries would be numers 0 through these entries would be numers 0 through 1223 let's say that this particular 1223 let's say that this particular 1223 let's say that this particular architecture uses fix size four byte architecture uses fix size four byte architecture uses fix size four byte instructions that have to be word instructions that have to be word instructions that have to be word aligned which means an instruction needs aligned which means an instruction needs aligned which means an instruction needs to begin at a divisible by four address to begin at a divisible by four address to begin at a divisible by four address so an instruction can be at address 0 4 so an instruction can be at address 0 4 so an instruction can be at address 0 4 8 Etc but it cannot be at addresses 8 Etc but it cannot be at addresses 8 Etc but it cannot be at addresses something like three or five or 17 and something like three or five or 17 and something like three or five or 17 and let's say that the program counter is a let's say that the program counter is a let's say that the program counter is a 32-bit register meaning that this 32-bit register meaning that this 32-bit register meaning that this particular processor uses 32-bit particular processor uses 32-bit particular processor uses 32-bit instruction addresses the question for instruction addresses the question for instruction addresses the question for you is which btb entry again they are you is which btb entry again they are you is which btb entry again they are numbered from 0 to 1023 should be used numbered from 0 to 1023 should be used numbered from 0 to 1023 should be used for PC whose address is hex 000000 a b0 for PC whose address is hex 000000 a b0 for PC whose address is hex 000000 a b0 c right your answer here again the c right your answer here again the c right your answer here again the answer should be between 0 and 1,23

## BTB Solution Quiz： Part 1
Kind: captions Language: en let's look at the solution for our BTB let's look at the solution for our BTB let's look at the solution for our BTB quiz the question was which b2b entries quiz the question was which b2b entries quiz the question was which b2b entries is used for a particular PC it might is used for a particular PC it might is used for a particular PC it might seem easy because we need 10 bits to seem easy because we need 10 bits to seem easy because we need 10 bits to index into this BT B so we will just index into this BT B so we will just index into this BT B so we will just take the least significant 10 bits of take the least significant 10 bits of take the least significant 10 bits of this PC and index into the BTB with that this PC and index into the BTB with that this PC and index into the BTB with that however note that instructions are fixed however note that instructions are fixed however note that instructions are fixed size 4 bytes and word aligned which size 4 bytes and word aligned which size 4 bytes and word aligned which means that not all addresses are means that not all addresses are means that not all addresses are possible for the program counter if you possible for the program counter if you possible for the program counter if you now look at what would happen if we use now look at what would happen if we use now look at what would happen if we use the least significant 10 bits to index the least significant 10 bits to index the least significant 10 bits to index into the BTB we would see that the entry into the BTB we would see that the entry into the BTB we would see that the entry number 0 would be used an instruction number 0 would be used an instruction number 0 would be used an instruction can have the least significant n bits be can have the least significant n bits be can have the least significant n bits be all 0 entry number 1 however requires all 0 entry number 1 however requires all 0 entry number 1 however requires the least significant 10 bits to be 0 0 the least significant 10 bits to be 0 0 the least significant 10 bits to be 0 0 0 0 0 1 and that entry cannot be used 0 0 0 1 and that entry cannot be used 0 0 0 1 and that entry cannot be used because the instructions are word because the instructions are word because the instructions are word aligned as we said addresses such as all aligned as we said addresses such as all aligned as we said addresses such as all zeros and then just 1 are not possible zeros and then just 1 are not possible zeros and then just 1 are not possible basically all addresses actually need to basically all addresses actually need to basically all addresses actually need to be not only even but also divisible by 4 be not only even but also divisible by 4 be not only even but also divisible by 4 so if you take the lowermost bits that so if you take the lowermost bits that so if you take the lowermost bits that means that the only possible instruction means that the only possible instruction means that the only possible instruction addresses are of the form something addresses are of the form something addresses are of the form something something something and then the least something something and then the least something something and then the least significant two bits are 0 that means significant two bits are 0 that means significant two bits are 0 that means that we are really using only every 4th that we are really using only every 4th that we are really using only every 4th entry in our BTB pretty much of the entry in our BTB pretty much of the entry in our BTB pretty much of the 1,024 entry BTB we are really using only 1,024 entry BTB we are really using only 1,024 entry BTB we are really using only 256 entries which is a huge waste so in 256 entries which is a huge waste so in 256 entries which is a huge waste so in this case what should happen is we this case what should happen is we this case what should happen is we should not use the least significant 10 should not use the least significant 10 should not use the least significant 10 bits and instead we use these 10 bits so bits and instead we use these 10 bits so bits and instead we use these 10 bits so that the least significant bits that can that the least significant bits that can that the least significant bits that can be different among instructions be different among instructions be different among instructions basically these bits are always 0 so basically these bits are always 0 so basically these bits are always 0 so they're just going to waste entries we they're just going to waste entries we they're just going to waste entries we need to take the least significant 10 need to take the least significant 10 need to take the least significant 10 bits that are still different among bits that are still different among bits that are still different among different instructions so in this case different instructions so in this case different instructions so in this case our instruction has bits C is 1 1 0 0 0 our instruction has bits C is 1 1 0 0 0 our instruction has bits C is 1 1 0 0 0 is 0 0 0 0 B is 1 0 1 1 we're gonna is 0 0 0 0 B is 1 0 1 1 we're gonna is 0 0 0 0 B is 1 0 1 1 we're gonna ignore these two bits and take the ignore these two bits and take the ignore these two bits and take the next one two three four five six seven next one two three four five six seven next one two three four five six seven eight nine ten bits so this is the index eight nine ten bits so this is the index eight nine ten bits so this is the index into our BTB and now let's convert that into our BTB and now let's convert that into our BTB and now let's convert that to hex this will be the first hex digit to hex this will be the first hex digit to hex this will be the first hex digit so it would be three this would be the so it would be three this would be the so it would be three this would be the next hex digit so it would be C and this next hex digit so it would be C and this next hex digit so it would be C and this would be two so we are talking about the would be two so we are talking about the would be two so we are talking about the entry number to c3

## Direction Predictor： Part 1
Kind: captions Language: en So, how are we going to do the direction So, how are we going to do the direction So, how are we going to do the direction prediction? We're going to have a table prediction? We're going to have a table prediction? We're going to have a table that we will call a branch history table that we will call a branch history table that we will call a branch history table or BHT. We will take the PC of the or BHT. We will take the PC of the or BHT. We will take the PC of the instruction, grab the least significant instruction, grab the least significant instruction, grab the least significant bits, and that's what we index into this bits, and that's what we index into this bits, and that's what we index into this table with. So, it's the same as when table with. So, it's the same as when table with. So, it's the same as when indexing into the BTB, but the entry indexing into the BTB, but the entry indexing into the BTB, but the entry here is much smaller. The simplest here is much smaller. The simplest here is much smaller. The simplest predictor will simply have a one bit predictor will simply have a one bit predictor will simply have a one bit here that tells us if this is a not a here that tells us if this is a not a here that tells us if this is a not a taken branch, meaning it's a non branch taken branch, meaning it's a non branch taken branch, meaning it's a non branch or a not taken branch or when it's one, or a not taken branch or when it's one, or a not taken branch or when it's one, it says that this is a taken branch. If it says that this is a taken branch. If it says that this is a taken branch. If we get a zero from here, we just we get a zero from here, we just we get a zero from here, we just increment the PC. If we get a one, we increment the PC. If we get a one, we increment the PC. If we get a one, we use the BTB to tell us where to go. Just use the BTB to tell us where to go. Just use the BTB to tell us where to go. Just like we were updating the BTB, we can like we were updating the BTB, we can like we were updating the BTB, we can update this once we resolve the branch. update this once we resolve the branch. update this once we resolve the branch. If we resolve to a taken branch, then we If we resolve to a taken branch, then we If we resolve to a taken branch, then we put a one here and we write the put a one here and we write the put a one here and we write the destination address into the BTB. If we destination address into the BTB. If we destination address into the BTB. If we get a zero, if we decide that the branch get a zero, if we decide that the branch get a zero, if we decide that the branch is not taken or that the instruction is is not taken or that the instruction is is not taken or that the instruction is not a branch, then we put a zero here not a branch, then we put a zero here not a branch, then we put a zero here and we don't update the BTB so that we and we don't update the BTB so that we and we don't update the BTB so that we can use the BTB only for target can use the BTB only for target can use the BTB only for target addresses that we really really need the addresses that we really really need the addresses that we really really need the BTB for because an entry here can be BTB for because an entry here can be BTB for because an entry here can be only a single bit. This table can be only a single bit. This table can be only a single bit. This table can be quite large. So we can have lots of quite large. So we can have lots of quite large. So we can have lots of instructions have entries here avoiding instructions have entries here avoiding instructions have entries here avoiding conflicts between instructions that conflicts between instructions that conflicts between instructions that commonly execute while still reserving commonly execute while still reserving commonly execute while still reserving the small BTB which has much fewer the small BTB which has much fewer the small BTB which has much fewer entries only for branches in that code.

## BTB and BHT Quiz： Part 1
Kind: captions Language: en so now that we have seen how the btb and so now that we have seen how the btb and so now that we have seen how the btb and the BHT work together let's do a btb and the BHT work together let's do a btb and the BHT work together let's do a btb and BHT quiz on that suppose we have the BHT quiz on that suppose we have the BHT quiz on that suppose we have the following program this is the address of following program this is the address of following program this is the address of the instruction this is the instruction the instruction this is the instruction the instruction this is the instruction itself and these are the labels for itself and these are the labels for itself and these are the labels for branches so what this program is doing branches so what this program is doing branches so what this program is doing is it's moving 100 into R2 it's moving is it's moving 100 into R2 it's moving is it's moving 100 into R2 it's moving zero into R1 and then as long as R1 and zero into R1 and then as long as R1 and zero into R1 and then as long as R1 and R2 are different it's going to continue R2 are different it's going to continue R2 are different it's going to continue here once R1 and R2 are the same it's here once R1 and R2 are the same it's here once R1 and R2 are the same it's going to jump to the done label and exit going to jump to the done label and exit going to jump to the done label and exit this Loop in the loop it's adding R1 to this Loop in the loop it's adding R1 to this Loop in the loop it's adding R1 to R3 to form R4 uses that to load a value R3 to form R4 uses that to load a value R3 to form R4 uses that to load a value from that address and then adds that R4 from that address and then adds that R4 from that address and then adds that R4 to R5 adds one to R1 and then to R5 adds one to R1 and then to R5 adds one to R1 and then unconditionally branches back to this unconditionally branches back to this unconditionally branches back to this Loop so this is the loop this is where Loop so this is the loop this is where Loop so this is the loop this is where we exit from the loop and this is the we exit from the loop and this is the we exit from the loop and this is the sort of initialization for the loop sort of initialization for the loop sort of initialization for the loop suppose that we have a branch history suppose that we have a branch history suppose that we have a branch history table that has only 16 entries and makes table that has only 16 entries and makes table that has only 16 entries and makes perfect predictions about branches perfect predictions about branches perfect predictions about branches suppose we also have a btb with four suppose we also have a btb with four suppose we also have a btb with four entries also makes perfect predictions entries also makes perfect predictions entries also makes perfect predictions and as you can see all instructions are and as you can see all instructions are and as you can see all instructions are four bytes in size and we know that so four bytes in size and we know that so four bytes in size and we know that so the question for you is how many times the question for you is how many times the question for you is how many times do we access the BHT for each do we access the BHT for each do we access the BHT for each instruction right here how many times we instruction right here how many times we instruction right here how many times we access the BHT access the BHT access the BHT with this address because we are with this address because we are with this address because we are fetching this fetching this fetching this instruction here how many times we instruction here how many times we instruction here how many times we access the BHD with this address because access the BHD with this address because access the BHD with this address because we're fetching this instruction and so we're fetching this instruction and so we're fetching this instruction and so on there are eight instructions here for on there are eight instructions here for on there are eight instructions here for each one of them we need to say how many each one of them we need to say how many each one of them we need to say how many times we access the BHD

## BTB and BHT Solution Quiz： Part 1
Kind: captions Language: en so let's do the solution to our BTB and so let's do the solution to our BTB and so let's do the solution to our BTB and BHT quiz we have a BHT with 16 entries BHT quiz we have a BHT with 16 entries BHT quiz we have a BHT with 16 entries which gives perfect predictions we have which gives perfect predictions we have which gives perfect predictions we have a bt b with 4 entries gives us perfect a bt b with 4 entries gives us perfect a bt b with 4 entries gives us perfect predictions the question was how many predictions the question was how many predictions the question was how many times do we access the BHT for each of times do we access the BHT for each of times do we access the BHT for each of these instructions we access the BHT in these instructions we access the BHT in these instructions we access the BHT in order to tell us whether this order to tell us whether this order to tell us whether this instruction is a taken branch we do that instruction is a taken branch we do that instruction is a taken branch we do that every time we fetch an instruction and every time we fetch an instruction and every time we fetch an instruction and because we have a perfect prediction for because we have a perfect prediction for because we have a perfect prediction for both Bhd and the BTB both Bhd and the BTB both Bhd and the BTB that means that we will never fail that means that we will never fail that means that we will never fail instruction that actually is not instruction that actually is not instruction that actually is not supposed to execute so we now just need supposed to execute so we now just need supposed to execute so we now just need to figure out how many times each of to figure out how many times each of to figure out how many times each of these instructions execute this these instructions execute this these instructions execute this instruction here initializes r2 to 100 instruction here initializes r2 to 100 instruction here initializes r2 to 100 and that instruction executes only one and that instruction executes only one and that instruction executes only one time once we are done executing it we time once we are done executing it we time once we are done executing it we will go into the loop loop here and then will go into the loop loop here and then will go into the loop loop here and then exit never coming back here same here exit never coming back here same here exit never coming back here same here this puts zero in our one that executed this puts zero in our one that executed this puts zero in our one that executed only once now we have a loop this loop only once now we have a loop this loop only once now we have a loop this loop will execute as long as r1 and r2 are will execute as long as r1 and r2 are will execute as long as r1 and r2 are different different different r1 initially has a zero value goes r1 initially has a zero value goes r1 initially has a zero value goes through here gets incremented and then through here gets incremented and then through here gets incremented and then we branch back now we execute this loop we branch back now we execute this loop we branch back now we execute this loop once more with r1 being equal to 1 and once more with r1 being equal to 1 and once more with r1 being equal to 1 and then 2 3 etc the last iteration we do then 2 3 etc the last iteration we do then 2 3 etc the last iteration we do will be with r1 equal 99 and finally our will be with r1 equal 99 and finally our will be with r1 equal 99 and finally our 1 will be incremented we jump back here 1 will be incremented we jump back here 1 will be incremented we jump back here find out that r1 and r2 are now the same find out that r1 and r2 are now the same find out that r1 and r2 are now the same because r1 is now 100 and exit the loop because r1 is now 100 and exit the loop because r1 is now 100 and exit the loop so there is a hundred iterations of this so there is a hundred iterations of this so there is a hundred iterations of this loop and each of these instructions thus loop and each of these instructions thus loop and each of these instructions thus executes 100 times this instruction here executes 100 times this instruction here executes 100 times this instruction here executes saundra times when we are executes saundra times when we are executes saundra times when we are staying in the loop and once we for the staying in the loop and once we for the staying in the loop and once we for the hundredth time jump back it executes hundredth time jump back it executes hundredth time jump back it executes once more in order to exit the loop so once more in order to exit the loop so once more in order to exit the loop so this instruction executes for 101 times this instruction executes for 101 times this instruction executes for 101 times and each time any of these instructions and each time any of these instructions and each time any of these instructions execute we are going to access the BHT execute we are going to access the BHT execute we are going to access the BHT to see whether that instruction is an to see whether that instruction is an to see whether that instruction is an taken branch or not

## BTB and BHT Quiz 2： Part 1
Kind: captions Language: en let's do another quiz with our btb and let's do another quiz with our btb and let's do another quiz with our btb and BHT same BHT 16 entries perfect same btb BHT same BHT 16 entries perfect same btb BHT same BHT 16 entries perfect same btb four entries perfect same program with four entries perfect same program with four entries perfect same program with the same addresses the question for you the same addresses the question for you the same addresses the question for you now is which BHD entry do we access for now is which BHD entry do we access for now is which BHD entry do we access for each instruction There are 16 entries each instruction There are 16 entries each instruction There are 16 entries numbered 0 through 15 the question is numbered 0 through 15 the question is numbered 0 through 15 the question is when we are fetching this instruction when we are fetching this instruction when we are fetching this instruction here what is the BHT entry that we're here what is the BHT entry that we're here what is the BHT entry that we're going to be using to predict it and then going to be using to predict it and then going to be using to predict it and then for the this instruction you need to for the this instruction you need to for the this instruction you need to write it here for this instruction here write it here for this instruction here write it here for this instruction here and so on again there are eight and so on again there are eight and so on again there are eight instructions so we need to have eight instructions so we need to have eight instructions so we need to have eight numbers here

## BTB and BHT Solution 2 Quiz： Part 1
Kind: captions Language: en let's look at the solution to our BTB let's look at the solution to our BTB let's look at the solution to our BTB and BHT quiz the question this time is and BHT quiz the question this time is and BHT quiz the question this time is which BHT entry do we access for each which BHT entry do we access for each which BHT entry do we access for each instruction there are 16 entries in the instruction there are 16 entries in the instruction there are 16 entries in the phd and we said that we access the BHT phd and we said that we access the BHT phd and we said that we access the BHT using the lowermost bits of the using the lowermost bits of the using the lowermost bits of the instruction address that are not the instruction address that are not the instruction address that are not the same for all instructions in this case same for all instructions in this case same for all instructions in this case our instruction address has the our instruction address has the our instruction address has the following bits for the move instruction following bits for the move instruction following bits for the move instruction here we have C 0 0 0 because every here we have C 0 0 0 because every here we have C 0 0 0 because every instruction is 4 bytes in size the instruction is 4 bytes in size the instruction is 4 bytes in size the lowermost 2 bits are never changing they lowermost 2 bits are never changing they lowermost 2 bits are never changing they are always going to be 0 for own are always going to be 0 for own are always going to be 0 for own instructions and now because we have 16 instructions and now because we have 16 instructions and now because we have 16 Bhd entries we take the next 4 bits and Bhd entries we take the next 4 bits and Bhd entries we take the next 4 bits and this is the number of the BHT entry in this is the number of the BHT entry in this is the number of the BHT entry in this case we are accessing the BHT entry this case we are accessing the BHT entry this case we are accessing the BHT entry number 0 now let's look at this other number 0 now let's look at this other number 0 now let's look at this other move instruction here whose address is C move instruction here whose address is C move instruction here whose address is C 0 0 4 C 0 0 are not changing what is 0 0 4 C 0 0 are not changing what is 0 0 4 C 0 0 are not changing what is changing is 4 4 in binary 0 1 0 0 again changing is 4 4 in binary 0 1 0 0 again changing is 4 4 in binary 0 1 0 0 again we discard these bits because they are we discard these bits because they are we discard these bits because they are never changing among instructions and never changing among instructions and never changing among instructions and this is the number of the BHT entry so this is the number of the BHT entry so this is the number of the BHT entry so the entry that we access here is 1 now the entry that we access here is 1 now the entry that we access here is 1 now you can tell that for instruction 8 here you can tell that for instruction 8 here you can tell that for instruction 8 here this is going to be 1 0 so we're going this is going to be 1 0 so we're going this is going to be 1 0 so we're going to access the BHT entry number 2 3 4 5 6 to access the BHT entry number 2 3 4 5 6 to access the BHT entry number 2 3 4 5 6 &amp; 7 &amp; 7 &amp; 7 if we reached 15 we would then wrap if we reached 15 we would then wrap if we reached 15 we would then wrap around to 0 but we never reach it so around to 0 but we never reach it so around to 0 but we never reach it so this is really what we get


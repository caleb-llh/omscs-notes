# Playlist 4 Module 7 Raw Transcript

## Write Update vs Write Invalidate Quiz 2： Part 5
Kind: captions Language: en let's see another comparison of updates let's see another comparison of updates let's see another comparison of updates and invalidates this time let's look at and invalidates this time let's look at and invalidates this time let's look at two cores that execute the following two cores that execute the following two cores that execute the following program core zero will read and then program core zero will read and then program core zero will read and then write a and this is repeated 500 times write a and this is repeated 500 times write a and this is repeated 500 times and then core one will read and then and then core one will read and then and then core one will read and then write a and this is repeated 500 times write a and this is repeated 500 times write a and this is repeated 500 times and meanwhile nothing else happens in and meanwhile nothing else happens in and meanwhile nothing else happens in this course so nothing gets replaced the this course so nothing gets replaced the this course so nothing gets replaced the question for you is again how many bus question for you is again how many bus question for you is again how many bus uses do we get with the right update uses do we get with the right update uses do we get with the right update protocol and with the right invalidate protocol and with the right invalidate protocol and with the right invalidate protocol in this case

## Write Update vs Write Invalidate Quiz Solution 2： Part 5
Kind: captions Language: en let's look at the solution to our second let's look at the solution to our second let's look at the solution to our second write-update versus write-invalidate write-update versus write-invalidate write-update versus write-invalidate quiz this time we have a program where quiz this time we have a program where quiz this time we have a program where one core repeatedly reads and then one core repeatedly reads and then one core repeatedly reads and then writes a and then the other so how many writes a and then the other so how many writes a and then the other so how many bus uses do we get with the right update bus uses do we get with the right update bus uses do we get with the right update and with the right invalidate protocol and with the right invalidate protocol and with the right invalidate protocol this time the write-update protocol is this time the write-update protocol is this time the write-update protocol is going to have one bus access here to going to have one bus access here to going to have one bus access here to read because this is a Miss because read because this is a Miss because read because this is a Miss because there is no sharing the write doesn't there is no sharing the write doesn't there is no sharing the write doesn't need a write axis because we know that need a write axis because we know that need a write axis because we know that we privately have this thing in our we privately have this thing in our we privately have this thing in our cache so this is what happens the first cache so this is what happens the first cache so this is what happens the first time in the remaining 499 iterations of time in the remaining 499 iterations of time in the remaining 499 iterations of this nothing else happens we just hit in this nothing else happens we just hit in this nothing else happens we just hit in our cache once we get to core 1 however our cache once we get to core 1 however our cache once we get to core 1 however the read a is going to get the data from the read a is going to get the data from the read a is going to get the data from core 0 but now we have the shared bit core 0 but now we have the shared bit core 0 but now we have the shared bit set at 1 in both caches so the right set at 1 in both caches so the right set at 1 in both caches so the right here will need to do a broadcast the here will need to do a broadcast the here will need to do a broadcast the first time and then the next time and so first time and then the next time and so first time and then the next time and so on so overall we get one for the initial on so overall we get one for the initial on so overall we get one for the initial miss and then five hundred updates of miss and then five hundred updates of miss and then five hundred updates of cores zeros cache note that this is cores zeros cache note that this is cores zeros cache note that this is wasteful because core 0 is no longer wasteful because core 0 is no longer wasteful because core 0 is no longer really interested in this so overall we really interested in this so overall we really interested in this so overall we get 502 bus accesses in a write get 502 bus accesses in a write get 502 bus accesses in a write invalidate protocol we have the same invalidate protocol we have the same invalidate protocol we have the same thing here we have a mess and the data thing here we have a mess and the data thing here we have a mess and the data is not shared is not shared is not shared so the write and all of the reads and so the write and all of the reads and so the write and all of the reads and writes after that are just cache hits writes after that are just cache hits writes after that are just cache hits however here what happens is the read however here what happens is the read however here what happens is the read here will make the data shared and fetch here will make the data shared and fetch here will make the data shared and fetch the data from the other core so that's the data from the other core so that's the data from the other core so that's going to be a miss because the data going to be a miss because the data going to be a miss because the data shared the right will have to do an shared the right will have to do an shared the right will have to do an invalidation broadcast so we will have invalidation broadcast so we will have invalidation broadcast so we will have another bus access but that makes the another bus access but that makes the another bus access but that makes the data private to core 1 because this data private to core 1 because this data private to core 1 because this version is invalidated so for the version is invalidated so for the version is invalidated so for the remaining 499 iterations of this nothing remaining 499 iterations of this nothing remaining 499 iterations of this nothing else happens so overall we get only else happens so overall we get only else happens so overall we get only three bus accesses as you can see right three bus accesses as you can see right three bus accesses as you can see right invalidate is a lot more efficient if invalidate is a lot more efficient if invalidate is a lot more efficient if the the the it tends to be used frequently on one it tends to be used frequently on one it tends to be used frequently on one core and then the frequent use moves to core and then the frequent use moves to core and then the frequent use moves to another core

## Update VS Invalidate Coherence： Part 5
Kind: captions Language: en so let's compare update and invalidate so let's compare update and invalidate so let's compare update and invalidate coherence approaches we will compare coherence approaches we will compare coherence approaches we will compare them for different things that the them for different things that the them for different things that the applications often do here we will say applications often do here we will say applications often do here we will say what the update protocols are gonna do what the update protocols are gonna do what the update protocols are gonna do and here's what an invalidate protocol and here's what an invalidate protocol and here's what an invalidate protocol is going to do if the application has a is going to do if the application has a is going to do if the application has a burst of rights to one address and if burst of rights to one address and if burst of rights to one address and if that address is shared then each write that address is shared then each write that address is shared then each write will send an update in an update will send an update in an update will send an update in an update protocol this is bad because each update protocol this is bad because each update protocol this is bad because each update results in competing for the bus so it results in competing for the bus so it results in competing for the bus so it creates bus contention and also it creates bus contention and also it creates bus contention and also it consumes energy and makes write slower consumes energy and makes write slower consumes energy and makes write slower in an invalidate protocol only the first in an invalidate protocol only the first in an invalidate protocol only the first right will send out an invalidation and right will send out an invalidation and right will send out an invalidation and the remaining writes are just gonna be the remaining writes are just gonna be the remaining writes are just gonna be cache hits that don't result in any bus cache hits that don't result in any bus cache hits that don't result in any bus traffic that is good why would an traffic that is good why would an traffic that is good why would an application do abrasive writes to one application do abrasive writes to one application do abrasive writes to one address well because when we are address well because when we are address well because when we are computing something we may need to computing something we may need to computing something we may need to update it several times until we get to update it several times until we get to update it several times until we get to the final result another common behavior the final result another common behavior the final result another common behavior is when an application writes two is when an application writes two is when an application writes two different words in the same block for different words in the same block for different words in the same block for example when we are initializing the example when we are initializing the example when we are initializing the block we will write to every word in block we will write to every word in block we will write to every word in that block if that block is shared an that block if that block is shared an that block if that block is shared an update protocol will send an update for update protocol will send an update for update protocol will send an update for each word that we write that is bad each word that we write that is bad each word that we write that is bad because it means that one cache line because it means that one cache line because it means that one cache line worth of writes might result in worth of writes might result in worth of writes might result in something like 10 updates in an something like 10 updates in an something like 10 updates in an invalidation based protocol the first invalidation based protocol the first invalidation based protocol the first write that we have is gonna invalidate write that we have is gonna invalidate write that we have is gonna invalidate the other copies and then the remaining the other copies and then the remaining the other copies and then the remaining accesses to the block to the same or accesses to the block to the same or accesses to the block to the same or different words are gonna be just cache different words are gonna be just cache different words are gonna be just cache hits that don't result in bus traffic so hits that don't result in bus traffic so hits that don't result in bus traffic so this is again good for the invalidate this is again good for the invalidate this is again good for the invalidate bad for the update and the final bad for the update and the final bad for the update and the final scenario we will consider is the scenario we will consider is the scenario we will consider is the producer-consumer scenario where one producer-consumer scenario where one producer-consumer scenario where one core keeps writing data and another core core keeps writing data and another core core keeps writing data and another core is supposed to read it in this case for is supposed to read it in this case for is supposed to read it in this case for the update protocol the producer will the update protocol the producer will the update protocol the producer will send updates every time it modifies the send updates every time it modifies the send updates every time it modifies the data and the consumer then finds the data and the consumer then finds the data and the consumer then finds the data in its cache for example if there data in its cache for example if there data in its cache for example if there is a buffer the first pass through the is a buffer the first pass through the is a buffer the first pass through the buffer will put it in the cache and then buffer will put it in the cache and then buffer will put it in the cache and then reading the buffer keeps it in the cache reading the buffer keeps it in the cache reading the buffer keeps it in the cache and here writes are gonna put new data and here writes are gonna put new data and here writes are gonna put new data in the buffer so that the consumer just in the buffer so that the consumer just in the buffer so that the consumer just keeps getting it from its own keeps getting it from its own keeps getting it from its own - this is not great the producer sends - this is not great the producer sends - this is not great the producer sends updates but it's kind of the best we can updates but it's kind of the best we can updates but it's kind of the best we can do because the data really has to get do because the data really has to get do because the data really has to get from one place to the other so this is from one place to the other so this is from one place to the other so this is about as good as this scenario gets in about as good as this scenario gets in about as good as this scenario gets in an invalidation based protocol however an invalidation based protocol however an invalidation based protocol however the producer will do a write that the producer will do a write that the producer will do a write that invalidates the readers copy so when the invalidates the readers copy so when the invalidates the readers copy so when the reader the consumer wants to read it's reader the consumer wants to read it's reader the consumer wants to read it's gonna be a cache miss and it has to get gonna be a cache miss and it has to get gonna be a cache miss and it has to get the data from the writer and then a new the data from the writer and then a new the data from the writer and then a new write will happen and invalidate the write will happen and invalidate the write will happen and invalidate the copy and so on so this is about as bad copy and so on so this is about as bad copy and so on so this is about as bad as it gets for this scenario because not as it gets for this scenario because not as it gets for this scenario because not only do we create bus traffic when we only do we create bus traffic when we only do we create bus traffic when we invalidate we then have to have a cache invalidate we then have to have a cache invalidate we then have to have a cache miss and have another bus operation to miss and have another bus operation to miss and have another bus operation to get the data so as we can see from this get the data so as we can see from this get the data so as we can see from this table update and invalidate based table update and invalidate based table update and invalidate based protocols have different advantages so protocols have different advantages so protocols have different advantages so which one do we choose in practice well which one do we choose in practice well which one do we choose in practice well it turns out that all modern processors it turns out that all modern processors it turns out that all modern processors use the invalidate type protocol but not use the invalidate type protocol but not use the invalidate type protocol but not exactly for these reasons as we can see exactly for these reasons as we can see exactly for these reasons as we can see invalidate based protocols have some invalidate based protocols have some invalidate based protocols have some strengths and some weaknesses overall strengths and some weaknesses overall strengths and some weaknesses overall they tend to be slightly stronger than they tend to be slightly stronger than they tend to be slightly stronger than update on average but that's not really update on average but that's not really update on average but that's not really the knockout punch that makes them win the knockout punch that makes them win the knockout punch that makes them win handily what really makes them win with handily what really makes them win with handily what really makes them win with no contest is when a thread moves to no contest is when a thread moves to no contest is when a thread moves to another core so a thread was using one another core so a thread was using one another core so a thread was using one core and then simply the operating core and then simply the operating core and then simply the operating system decides to move it to another system decides to move it to another system decides to move it to another core so what happens in the update core so what happens in the update core so what happens in the update protocol is that it now will keep protocol is that it now will keep protocol is that it now will keep updating the old course cache note that updating the old course cache note that updating the old course cache note that our working set was in one cache now it our working set was in one cache now it our working set was in one cache now it moved to another one but in reality the moved to another one but in reality the moved to another one but in reality the other cache still has blocks that other cache still has blocks that other cache still has blocks that correspond to our data so what happens correspond to our data so what happens correspond to our data so what happens is all the rights that we make in a is all the rights that we make in a is all the rights that we make in a near-future near-future near-future are gonna be updating the other course are gonna be updating the other course are gonna be updating the other course cache until that cache replaces those cache until that cache replaces those cache until that cache replaces those blocks by running another core for blocks by running another core for blocks by running another core for example this is pretty horrible because example this is pretty horrible because example this is pretty horrible because a thread movement ideally what would a thread movement ideally what would a thread movement ideally what would happen is we would just move the data happen is we would just move the data happen is we would just move the data over and continue working privately on over and continue working privately on over and continue working privately on on our private data so let's see how on our private data so let's see how on our private data so let's see how this horrible scenario for updates works this horrible scenario for updates works this horrible scenario for updates works when we have invalidates in that case when we have invalidates in that case when we have invalidates in that case the first write to each of the the first write to each of the the first write to each of the blocks will have to invalidate the old blocks will have to invalidate the old blocks will have to invalidate the old copy but after that initial period there copy but after that initial period there copy but after that initial period there will be no traffic anymore will be no traffic anymore will be no traffic anymore so this is about as good as it gets so this is about as good as it gets so this is about as good as it gets without actually taking the content of without actually taking the content of without actually taking the content of the old cache and copying it explicitly the old cache and copying it explicitly the old cache and copying it explicitly to the new cache and again this is to the new cache and again this is to the new cache and again this is pretty common operating system behavior pretty common operating system behavior pretty common operating system behavior so we really don't want it to perform so we really don't want it to perform so we really don't want it to perform horribly and because invalidate based horribly and because invalidate based horribly and because invalidate based protocols are commonly used we will now protocols are commonly used we will now protocols are commonly used we will now look at invalidation based protocols for look at invalidation based protocols for look at invalidation based protocols for the rest of this lesson

## MSI Coherence： Part 5
Kind: captions Language: en what we will look at now is one of the what we will look at now is one of the what we will look at now is one of the simpler coherence protocols called MSI simpler coherence protocols called MSI simpler coherence protocols called MSI in MSI a block can be in one of three in MSI a block can be in one of three in MSI a block can be in one of three states in a cache I stands for the states in a cache I stands for the states in a cache I stands for the invalid State a block is in an invalid invalid State a block is in an invalid invalid State a block is in an invalid State either when it is present in the State either when it is present in the State either when it is present in the cache but its valid bit would not be set cache but its valid bit would not be set cache but its valid bit would not be set or when it's simply not present in the or when it's simply not present in the or when it's simply not present in the cach so a block that is not present in cach so a block that is not present in cach so a block that is not present in the cach is treated like it is present the cach is treated like it is present the cach is treated like it is present but in an invalid State s stands for the but in an invalid State s stands for the but in an invalid State s stands for the shared State a block that is in the shared State a block that is in the shared State a block that is in the shared state in a cache can be read shared state in a cache can be read shared state in a cache can be read without any further Ado but if we write without any further Ado but if we write without any further Ado but if we write it we have to do something a local read it we have to do something a local read it we have to do something a local read to a block that is in the shared State to a block that is in the shared State to a block that is in the shared State keeps the Block in the shared State and keeps the Block in the shared State and keeps the Block in the shared State and doesn't do anything else and finally m doesn't do anything else and finally m doesn't do anything else and finally m stands for the modified state in this stands for the modified state in this stands for the modified state in this state we can read the block just like in state we can read the block just like in state we can read the block just like in the shared State without sending the shared State without sending the shared State without sending anything out and we can also write to anything out and we can also write to anything out and we can also write to this block by the local core without this block by the local core without this block by the local core without sending anything out now because this is sending anything out now because this is sending anything out now because this is an invalidation based protocol what an invalidation based protocol what an invalidation based protocol what happens is in the modify State we are happens is in the modify State we are happens is in the modify State we are sure that there are no other cached sure that there are no other cached sure that there are no other cached copies which is why we are allowed to copies which is why we are allowed to copies which is why we are allowed to locally write we don't have to tell locally write we don't have to tell locally write we don't have to tell anybody that we are writing because we anybody that we are writing because we anybody that we are writing because we know there is nobody so this is sort of know there is nobody so this is sort of know there is nobody so this is sort of equivalent to having valid equals 1 and equivalent to having valid equals 1 and equivalent to having valid equals 1 and dirty equals 1 dirty equals 1 dirty equals 1 in a normal cache we just call this in a normal cache we just call this in a normal cache we just call this state modified because it's easier to state modified because it's easier to state modified because it's easier to understand what's going on this way then understand what's going on this way then understand what's going on this way then if we have to look at individual bits if we have to look at individual bits if we have to look at individual bits and we will see that with more and we will see that with more and we will see that with more complicated coherence protocols we will complicated coherence protocols we will complicated coherence protocols we will add maybe a state or two but we would add maybe a state or two but we would add maybe a state or two but we would need to add several bits in order to need to add several bits in order to need to add several bits in order to track what's going on so now in the track what's going on so now in the track what's going on so now in the invalid State we have what's equivalent invalid State we have what's equivalent invalid State we have what's equivalent to valid bit being zero pretty much we to valid bit being zero pretty much we to valid bit being zero pretty much we behave like we don't have the block behave like we don't have the block behave like we don't have the block either because we really don't have it either because we really don't have it either because we really don't have it or because we have it or because we have it or because we have it but we are not supposed to read it or but we are not supposed to read it or but we are not supposed to read it or write it so when we have a write and the write it so when we have a write and the write it so when we have a write and the block is in the invalid State we move block is in the invalid State we move block is in the invalid State we move the block to the modified state so when the block to the modified state so when the block to the modified state so when we have a local write meaning the local we have a local write meaning the local we have a local write meaning the local processor wants to write and the block processor wants to write and the block processor wants to write and the block is in the invalid State we move the is in the invalid State we move the is in the invalid State we move the block to the modified state but we block to the modified state but we block to the modified state but we cannot just do that to do that we have cannot just do that to do that we have cannot just do that to do that we have to put the right request on the bus so to put the right request on the bus so to put the right request on the bus so pretty much this is a right Miss we have pretty much this is a right Miss we have pretty much this is a right Miss we have to tell others that it's a right Miss we to tell others that it's a right Miss we to tell others that it's a right Miss we get the data and now we can put it in get the data and now we can put it in get the data and now we can put it in the modified State why well because our the modified State why well because our the modified State why well because our right request on the bus pretty much right request on the bus pretty much right request on the bus pretty much made sure that nobody else has a copy made sure that nobody else has a copy made sure that nobody else has a copy anymore cuz the right request that we anymore cuz the right request that we anymore cuz the right request that we made invalidated everybody else if we made invalidated everybody else if we made invalidated everybody else if we have a block in the modified State and have a block in the modified State and have a block in the modified State and we observe on the bus that another cach we observe on the bus that another cach we observe on the bus that another cach is placing a right on the bus then we is placing a right on the bus then we is placing a right on the bus then we will go to the invalid state so we do will go to the invalid state so we do will go to the invalid state so we do this when we Snoop the right on the bus this when we Snoop the right on the bus this when we Snoop the right on the bus and we cannot just do this and be done and we cannot just do this and be done and we cannot just do this and be done when we Snoop a right on the bus because when we Snoop a right on the bus because when we Snoop a right on the bus because we are in the modified state where we we are in the modified state where we we are in the modified state where we have the dirty copy meaning really the have the dirty copy meaning really the have the dirty copy meaning really the only fresh copy of the data in the only fresh copy of the data in the only fresh copy of the data in the system is in our cache it's not even in system is in our cache it's not even in system is in our cache it's not even in memory cuz it's dirty here we have to memory cuz it's dirty here we have to memory cuz it's dirty here we have to write the block back as we're write the block back as we're write the block back as we're transitioning to invalid and we have to transitioning to invalid and we have to transitioning to invalid and we have to delay the data that would be sent to the delay the data that would be sent to the delay the data that would be sent to the right request until we are done with the right request until we are done with the right request until we are done with the right back this can be done in two ways right back this can be done in two ways right back this can be done in two ways one is to actually cancel that request one is to actually cancel that request one is to actually cancel that request write back move to invalid and then that write back move to invalid and then that write back move to invalid and then that request gets repeated and now gets the request gets repeated and now gets the request gets repeated and now gets the data from memory another one is when we data from memory another one is when we data from memory another one is when we see a write on the bus if it's see a write on the bus if it's see a write on the bus if it's requesting data then what we do is we requesting data then what we do is we requesting data then what we do is we suppress the memory response and feed suppress the memory response and feed suppress the memory response and feed the data to that processor the simpler the data to that processor the simpler the data to that processor the simpler way of doing things is just to delay way of doing things is just to delay way of doing things is just to delay that request right back and then let it that request right back and then let it that request right back and then let it proceed but either way we have to make proceed but either way we have to make proceed but either way we have to make sure that before we move from modifi to sure that before we move from modifi to sure that before we move from modifi to invalid we write back the data to memory invalid we write back the data to memory invalid we write back the data to memory and also make sure that the right and also make sure that the right and also make sure that the right request if it is a right Miss and needs request if it is a right Miss and needs request if it is a right Miss and needs the data gets our new data not the data the data gets our new data not the data the data gets our new data not the data from memory now if we Snoop a read on from memory now if we Snoop a read on from memory now if we Snoop a read on the bus and we are in the modified State the bus and we are in the modified State the bus and we are in the modified State then we move to the shared state so we then we move to the shared state so we then we move to the shared state so we have snooped a read on the bus we will have snooped a read on the bus we will have snooped a read on the bus we will now not be the only one who has a copy now not be the only one who has a copy now not be the only one who has a copy of the data that means we will not be of the data that means we will not be of the data that means we will not be able to write without telling others so able to write without telling others so able to write without telling others so we will move to the shared state we will move to the shared state we will move to the shared state just like here we have to also make sure just like here we have to also make sure just like here we have to also make sure that the block is written back the that the block is written back the that the block is written back the shared state is equivalent to having V shared state is equivalent to having V shared state is equivalent to having V equals 1 and D equal Z so we will not be equals 1 and D equal Z so we will not be equals 1 and D equal Z so we will not be doing a write back from our shared State doing a write back from our shared State doing a write back from our shared State cuz it only allows reads anyway so as we cuz it only allows reads anyway so as we cuz it only allows reads anyway so as we are moving from the dirty to a clean are moving from the dirty to a clean are moving from the dirty to a clean State modified to Shared we're going to State modified to Shared we're going to State modified to Shared we're going to write back the data the reading write back the data the reading write back the data the reading processor wants a copy of this block processor wants a copy of this block processor wants a copy of this block again we have to either the delay that again we have to either the delay that again we have to either the delay that request do a right back change to the request do a right back change to the request do a right back change to the shared State and then let that processor shared State and then let that processor shared State and then let that processor proceed with a read or we have to proceed with a read or we have to proceed with a read or we have to suppress the memory response that would suppress the memory response that would suppress the memory response that would happen as a result of this read and happen as a result of this read and happen as a result of this read and provide our data instead as we are provide our data instead as we are provide our data instead as we are transitioning to the shared state if the transitioning to the shared state if the transitioning to the shared state if the processor reads a block and the block is processor reads a block and the block is processor reads a block and the block is in the invalid State then the block will in the invalid State then the block will in the invalid State then the block will be moved to a shared state so in be moved to a shared state so in be moved to a shared state so in response to a local read we put a read response to a local read we put a read response to a local read we put a read request on the bus and then we get the request on the bus and then we get the request on the bus and then we get the data and then we are in the shared state data and then we are in the shared state data and then we are in the shared state for that data note that the data might for that data note that the data might for that data note that the data might come from memory or it may come from come from memory or it may come from come from memory or it may come from another cache because it had the data in another cache because it had the data in another cache because it had the data in the modified State and we needed to get the modified State and we needed to get the modified State and we needed to get that data but as far as this local cache that data but as far as this local cache that data but as far as this local cache is concerned it puts a read request on is concerned it puts a read request on is concerned it puts a read request on the bus it gets the data and then it the bus it gets the data and then it the bus it gets the data and then it puts that block in the shared state in puts that block in the shared state in puts that block in the shared state in the local cache then we can do many the local cache then we can do many the local cache then we can do many reads without telling anybody if we have reads without telling anybody if we have reads without telling anybody if we have a block in the shared state and we a block in the shared state and we a block in the shared state and we observe a snoop right on the bus then we observe a snoop right on the bus then we observe a snoop right on the bus then we move the block to the invalid State we move the block to the invalid State we move the block to the invalid State we don't have to do anything because the don't have to do anything because the don't have to do anything because the block is clean as far as we are block is clean as far as we are block is clean as far as we are concerned we don't have to write it back concerned we don't have to write it back concerned we don't have to write it back we simply have to do the equivalent of we simply have to do the equivalent of we simply have to do the equivalent of resetting the valid bit and move to the resetting the valid bit and move to the resetting the valid bit and move to the invalid state so there is nothing really invalid state so there is nothing really invalid state so there is nothing really that we have to do except change the that we have to do except change the that we have to do except change the state so that next time we try to read state so that next time we try to read state so that next time we try to read it we can no longer read it here we have it we can no longer read it here we have it we can no longer read it here we have to put a read request on the bus again to put a read request on the bus again to put a read request on the bus again if we Snoop a read on the bus in the if we Snoop a read on the bus in the if we Snoop a read on the bus in the shared state we don't need to do shared state we don't need to do shared state we don't need to do anything why well because our shared anything why well because our shared anything why well because our shared state is okay with there being other state is okay with there being other state is okay with there being other sharers that are only going to read the sharers that are only going to read the sharers that are only going to read the data and finally although local reads data and finally although local reads data and finally although local reads are okay in the shared state if we have are okay in the shared state if we have are okay in the shared state if we have a local right that is not allowed in the a local right that is not allowed in the a local right that is not allowed in the shared state so a local right that finds shared state so a local right that finds shared state so a local right that finds the Block in the shared state will make the Block in the shared state will make the Block in the shared state will make us move the block to a modified state us move the block to a modified state us move the block to a modified state but we cannot do that silently we cannot but we cannot do that silently we cannot but we cannot do that silently we cannot just move the block and write to it what just move the block and write to it what just move the block and write to it what we have to do is put an invalidation on we have to do is put an invalidation on we have to do is put an invalidation on the bus know that things are slightly the bus know that things are slightly the bus know that things are slightly different when we move from invalid different when we move from invalid different when we move from invalid State on a local right to modified and State on a local right to modified and State on a local right to modified and when we moved from shared state to a when we moved from shared state to a when we moved from shared state to a modified State on a local right here we modified State on a local right here we modified State on a local right here we put a right request that asks for a put a right request that asks for a put a right request that asks for a block so that we can write it here we block so that we can write it here we block so that we can write it here we already have the block this is the most already have the block this is the most already have the block this is the most upto-date copy because if there was upto-date copy because if there was upto-date copy because if there was another right after that we would not another right after that we would not another right after that we would not have it in the shared state so now we have it in the shared state so now we have it in the shared state so now we want to write to it we already have the want to write to it we already have the want to write to it we already have the data we just need to put an invalidation data we just need to put an invalidation data we just need to put an invalidation request on the bus so this invalidation request on the bus so this invalidation request on the bus so this invalidation request does not actually request the request does not actually request the request does not actually request the data it just puts a request on the bus data it just puts a request on the bus data it just puts a request on the bus for everybody to invalidate their copies for everybody to invalidate their copies for everybody to invalidate their copies but it's not asking for the data so but it's not asking for the data so but it's not asking for the data so pretty much as soon as we put risk pretty much as soon as we put risk pretty much as soon as we put risk request on the bus we can write because request on the bus we can write because request on the bus we can write because by putting an invalidation on the bus we by putting an invalidation on the bus we by putting an invalidation on the bus we have made sure that now everybody is have made sure that now everybody is have made sure that now everybody is invalidated at which point we are in the invalidated at which point we are in the invalidated at which point we are in the modified State now of course in the modified State now of course in the modified State now of course in the invalid State whatever we Snoop for this invalid State whatever we Snoop for this invalid State whatever we Snoop for this block on the bus doesn't matter we're block on the bus doesn't matter we're block on the bus doesn't matter we're going to stay in the invalid State also going to stay in the invalid State also going to stay in the invalid State also in the shared state it doesn't matter in the shared state it doesn't matter in the shared state it doesn't matter whether we Snoop somebody's request to whether we Snoop somebody's request to whether we Snoop somebody's request to write or an invalidation that they put write or an invalidation that they put write or an invalidation that they put on the bus we still move to the invalid on the bus we still move to the invalid on the bus we still move to the invalid State without doing much and finally State without doing much and finally State without doing much and finally note that if we are in a modified State note that if we are in a modified State note that if we are in a modified State we will observe a right if somebody we will observe a right if somebody we will observe a right if somebody wants to write and not an invalidation wants to write and not an invalidation wants to write and not an invalidation because if we are in the modified state because if we are in the modified state because if we are in the modified state that means that everybody else is in the that means that everybody else is in the that means that everybody else is in the invalid state for this block so if they invalid state for this block so if they invalid state for this block so if they want to write they'll have to put a want to write they'll have to put a want to write they'll have to put a right request on the bus they cannot be right request on the bus they cannot be right request on the bus they cannot be in the shared State and put just an in the shared State and put just an in the shared State and put just an invalidation out

## Cache To Cache Transfers： Part 5
Kind: captions Language: en so let's look more closely at what is so let's look more closely at what is so let's look more closely at what is called cash to cash transfers which is called cash to cash transfers which is called cash to cash transfers which is what happens when Corwin has some Block what happens when Corwin has some Block what happens when Corwin has some Block B in a modified state and another core B in a modified state and another core B in a modified state and another core puts a read request on the bus so that puts a read request on the bus so that puts a read request on the bus so that it wants to basically bring the Block in it wants to basically bring the Block in it wants to basically bring the Block in its own cash at this point c1 has to its own cash at this point c1 has to its own cash at this point c1 has to react because c1 is the one that has to react because c1 is the one that has to react because c1 is the one that has to provide the data if we have the Block in provide the data if we have the Block in provide the data if we have the Block in the M state that means that the only the M state that means that the only the M state that means that the only up-to-date copy in the system is our up-to-date copy in the system is our up-to-date copy in the system is our copy even the memory doesn't have an copy even the memory doesn't have an copy even the memory doesn't have an up-to-date copy so if the data is going up-to-date copy so if the data is going up-to-date copy so if the data is going to be sent to see to it had better be to be sent to see to it had better be to be sent to see to it had better be our data but how and we have briefly our data but how and we have briefly our data but how and we have briefly touched on this when we were discussing touched on this when we were discussing touched on this when we were discussing the MSI protocol there are really two the MSI protocol there are really two the MSI protocol there are really two solutions the first solution is aboard solutions the first solution is aboard solutions the first solution is aboard and retry the idea here is that c1 and retry the idea here is that c1 and retry the idea here is that c1 somehow cancels c2 request and for that somehow cancels c2 request and for that somehow cancels c2 request and for that we need some sort of an abort bus signal we need some sort of an abort bus signal we need some sort of an abort bus signal that when a request is placed on the bus that when a request is placed on the bus that when a request is placed on the bus another core can assert the abort signal another core can assert the abort signal another core can assert the abort signal that tells the requesting core to kind that tells the requesting core to kind that tells the requesting core to kind of back off and try again later of back off and try again later of back off and try again later now that c2 request has been aborted c1 now that c2 request has been aborted c1 now that c2 request has been aborted c1 can do a normal right back to memory at can do a normal right back to memory at can do a normal right back to memory at this point the memory has the up-to-date this point the memory has the up-to-date this point the memory has the up-to-date data and when c2 retries its read data and when c2 retries its read data and when c2 retries its read request it will get the data from memory request it will get the data from memory request it will get the data from memory the problem with this approach is that the problem with this approach is that the problem with this approach is that really from the time c2 makes the really from the time c2 makes the really from the time c2 makes the request if this data was normally coming request if this data was normally coming request if this data was normally coming from memory we would have a memory from memory we would have a memory from memory we would have a memory latency so if this is just a read miss latency so if this is just a read miss latency so if this is just a read miss we will have a memory latency but if it we will have a memory latency but if it we will have a memory latency but if it is a read miss where another core has is a read miss where another core has is a read miss where another core has the data we really have to have two the data we really have to have two the data we really have to have two memory latencies before c2 can get the memory latencies before c2 can get the memory latencies before c2 can get the data one for the write back to memory to data one for the write back to memory to data one for the write back to memory to happen on c1 and then for us to actually happen on c1 and then for us to actually happen on c1 and then for us to actually get the data from memory so the read get the data from memory so the read get the data from memory so the read miss from c1 has a latency that is twice miss from c1 has a latency that is twice miss from c1 has a latency that is twice the memory latency plus some so this is the memory latency plus some so this is the memory latency plus some so this is why this approach is not ideal the why this approach is not ideal the why this approach is not ideal the second solution is called intervention second solution is called intervention second solution is called intervention in this case C one sees the read request in this case C one sees the read request in this case C one sees the read request is being made is being made is being made and it tells the memory somehow that c1 and it tells the memory somehow that c1 and it tells the memory somehow that c1 will supply the data instead of the will supply the data instead of the will supply the data instead of the memory so normally the memory would memory so normally the memory would memory so normally the memory would respond to a read miss request now c1 respond to a read miss request now c1 respond to a read miss request now c1 kind of tells memory that I will do that kind of tells memory that I will do that kind of tells memory that I will do that and for that we need an intervention bus and for that we need an intervention bus and for that we need an intervention bus signal through which c1 by asserting the signal through which c1 by asserting the signal through which c1 by asserting the intervention signal signals to the intervention signal signals to the intervention signal signals to the memory not to respond so the memory now memory not to respond so the memory now memory not to respond so the memory now doesn't respond with the data but c1 doesn't respond with the data but c1 doesn't respond with the data but c1 does and now the memory must pick up the does and now the memory must pick up the does and now the memory must pick up the data that c1 was responding with in data that c1 was responding with in data that c1 was responding with in order to put it in the appropriate order to put it in the appropriate order to put it in the appropriate memory block the reason the memory has memory block the reason the memory has memory block the reason the memory has to pick up the data is that once c1 to pick up the data is that once c1 to pick up the data is that once c1 responds with the data both c1 and c2 responds with the data both c1 and c2 responds with the data both c1 and c2 will have the Block in the shared state will have the Block in the shared state will have the Block in the shared state so both of them will think that the so both of them will think that the so both of them will think that the block is not dirty and that means that block is not dirty and that means that block is not dirty and that means that if the memory doesn't get the data now if the memory doesn't get the data now if the memory doesn't get the data now it will never get the fresh data so by it will never get the fresh data so by it will never get the fresh data so by picking up the data the memory ensures picking up the data the memory ensures picking up the data the memory ensures that it gets the data that will not be that it gets the data that will not be that it gets the data that will not be written back anymore so the disadvantage written back anymore so the disadvantage written back anymore so the disadvantage of this approach is that it needs more of this approach is that it needs more of this approach is that it needs more complex Hardware now a cache needs to be complex Hardware now a cache needs to be complex Hardware now a cache needs to be able to kind of insert its data into able to kind of insert its data into able to kind of insert its data into what would normally be the memory what would normally be the memory what would normally be the memory sending data and also the memory has to sending data and also the memory has to sending data and also the memory has to have enough intelligence to pick up the have enough intelligence to pick up the have enough intelligence to pick up the data when a core is responding to an data when a core is responding to an data when a core is responding to an intervention request instead of just intervention request instead of just intervention request instead of just picking up the data when there is a picking up the data when there is a picking up the data when there is a write back request in modern processors write back request in modern processors write back request in modern processors we nowadays mostly use a variant of an we nowadays mostly use a variant of an we nowadays mostly use a variant of an intervention approach and the reason is intervention approach and the reason is intervention approach and the reason is that yes the hardware is slightly more that yes the hardware is slightly more that yes the hardware is slightly more complex but fancier snooping protocols complex but fancier snooping protocols complex but fancier snooping protocols actually eliminate the problem of memory actually eliminate the problem of memory actually eliminate the problem of memory picking up the data and the complexity picking up the data and the complexity picking up the data and the complexity to respond with the data is not too to respond with the data is not too to respond with the data is not too large in contrast here the performance large in contrast here the performance large in contrast here the performance suffers so pretty much between having a suffers so pretty much between having a suffers so pretty much between having a fancier protocol and having enough fancier protocol and having enough fancier protocol and having enough transistors from Moore's Law to actually transistors from Moore's Law to actually transistors from Moore's Law to actually add this capability to caches we pretty add this capability to caches we pretty add this capability to caches we pretty much can do this and not have to suffer much can do this and not have to suffer much can do this and not have to suffer in performance like this

## MSI Quiz： Part 5
Kind: captions Language: en so let's see if we can do some MSI so let's see if we can do some MSI so let's see if we can do some MSI protocol transitions let's say we have protocol transitions let's say we have protocol transitions let's say we have two cores with private caches and that two cores with private caches and that two cores with private caches and that the caches are using MSI to maintain the caches are using MSI to maintain the caches are using MSI to maintain coherence initially block X is in memory coherence initially block X is in memory coherence initially block X is in memory and no cache has it so here are the and no cache has it so here are the and no cache has it so here are the actions of the caches in C1 and c2's actions of the caches in C1 and c2's actions of the caches in C1 and c2's cache core One issues a redex mean while cache core One issues a redex mean while cache core One issues a redex mean while core2 doesn't do anything what is the core2 doesn't do anything what is the core2 doesn't do anything what is the state of block X in C1 cache after this state of block X in C1 cache after this state of block X in C1 cache after this read is completed and what is the state read is completed and what is the state read is completed and what is the state of the block X in c2's Cache after this of the block X in c2's Cache after this of the block X in c2's Cache after this read on C1 is completed so put your Ms read on C1 is completed so put your Ms read on C1 is completed so put your Ms or I State here and another one here now or I State here and another one here now or I State here and another one here now let's say that the next axis is that C2 let's say that the next axis is that C2 let's say that the next axis is that C2 redex after this is completed what is redex after this is completed what is redex after this is completed what is the new state of X in c1s and c2's cache the new state of X in c1s and c2's cache the new state of X in c1s and c2's cache and finally C1 does another axis right x and finally C1 does another axis right x and finally C1 does another axis right x what is now the state in C1 cache after what is now the state in C1 cache after what is now the state in C1 cache after this right is complete and what's the this right is complete and what's the this right is complete and what's the state in c2's Cache after this right on state in c2's Cache after this right on state in c2's Cache after this right on C1 is complete

## MSI Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our MSI let's look at the solution to our MSI let's look at the solution to our MSI access quiz we have two cores with access quiz we have two cores with access quiz we have two cores with private caches and MSI coherence private caches and MSI coherence private caches and MSI coherence initially block X is in memory which initially block X is in memory which initially block X is in memory which means that it's really invalid in both means that it's really invalid in both means that it's really invalid in both of the caches after we read X in C once of the caches after we read X in C once of the caches after we read X in C once cache see one's cache will transition cache see one's cache will transition cache see one's cache will transition this block from I to s the shared state this block from I to s the shared state this block from I to s the shared state because this was a read we move to the because this was a read we move to the because this was a read we move to the shared state even though it is not shared state even though it is not shared state even though it is not really shared this is our way of saying really shared this is our way of saying really shared this is our way of saying basically that the block is clean and basically that the block is clean and basically that the block is clean and that we only want to read it in c2s that we only want to read it in c2s that we only want to read it in c2s cache it stays invalid when c2 then does cache it stays invalid when c2 then does cache it stays invalid when c2 then does a read it also gets the Block in the a read it also gets the Block in the a read it also gets the Block in the shared state and if we snoop a read shared state and if we snoop a read shared state and if we snoop a read request in the shared State we stay in request in the shared State we stay in request in the shared State we stay in the shared state so finally when c1 the shared state so finally when c1 the shared state so finally when c1 writes to X because it has the Block in writes to X because it has the Block in writes to X because it has the Block in the shared state it needs to send an the shared state it needs to send an the shared state it needs to send an invalidation out that results in c2 invalidation out that results in c2 invalidation out that results in c2 moving the block to the invalid state it moving the block to the invalid state it moving the block to the invalid state it can no longer be read until we get the can no longer be read until we get the can no longer be read until we get the new copy from C 1 and C 1 after writing new copy from C 1 and C 1 after writing new copy from C 1 and C 1 after writing has the Block in the modified State

## MSI Quiz 2： Part 5
Kind: captions Language: en let's now do another quiz on MSI same let's now do another quiz on MSI same let's now do another quiz on MSI same thing to course private caches MSI thing to course private caches MSI thing to course private caches MSI coherence block X initially in memory coherence block X initially in memory coherence block X initially in memory but now c1 is reading and then c2 is but now c1 is reading and then c2 is but now c1 is reading and then c2 is writing and then c1 writes so what is writing and then c1 writes so what is writing and then c1 writes so what is the state of C ones and c2s cache after the state of C ones and c2s cache after the state of C ones and c2s cache after each of these things occurs

## MSI Quiz 2 Solution： Part 5
Kind: captions Language: en let's look at the solution to our MSI let's look at the solution to our MSI let's look at the solution to our MSI quiz Redux results in the same behavior quiz Redux results in the same behavior quiz Redux results in the same behavior as in the previous quiz both of them as in the previous quiz both of them as in the previous quiz both of them have the state as I and then when readex have the state as I and then when readex have the state as I and then when readex occurs in C ones cache it sends out the occurs in C ones cache it sends out the occurs in C ones cache it sends out the read request gets the block from memory read request gets the block from memory read request gets the block from memory and now it has the Block in the shared and now it has the Block in the shared and now it has the Block in the shared state c2 still has the Block in the state c2 still has the Block in the state c2 still has the Block in the invalid state invalid state invalid state now when c2 writes to X it sends out a now when c2 writes to X it sends out a now when c2 writes to X it sends out a write miss request it will get the data write miss request it will get the data write miss request it will get the data and then write to its cache so it gets and then write to its cache so it gets and then write to its cache so it gets the block now in the modified state c1 the block now in the modified state c1 the block now in the modified state c1 meanwhile transitions from the shared meanwhile transitions from the shared meanwhile transitions from the shared state to the invalid state because it state to the invalid state because it state to the invalid state because it observes the write requests sent by c2 observes the write requests sent by c2 observes the write requests sent by c2 and then when C one wants to write it and then when C one wants to write it and then when C one wants to write it does pretty much the same thing as c2 does pretty much the same thing as c2 does pretty much the same thing as c2 just did it puts a write miss request on just did it puts a write miss request on just did it puts a write miss request on the bus gets the block rights to it puts the bus gets the block rights to it puts the bus gets the block rights to it puts it in the modified State it in the modified State it in the modified State meanwhile c2 has to intervene and meanwhile c2 has to intervene and meanwhile c2 has to intervene and provide the data that c1 gets and then provide the data that c1 gets and then provide the data that c1 gets and then because we snoop the right we need to because we snoop the right we need to because we snoop the right we need to transition to an invalid state note that transition to an invalid state note that transition to an invalid state note that if a block is in M state in any cache if a block is in M state in any cache if a block is in M state in any cache all the other caches have to have that all the other caches have to have that all the other caches have to have that block in the invalid state if a cache block in the invalid state if a cache block in the invalid state if a cache has a block in a shared state all the has a block in a shared state all the has a block in a shared state all the other caches need to have the Block in other caches need to have the Block in other caches need to have the Block in either the shared or the invalid caches either the shared or the invalid caches either the shared or the invalid caches so pretty much we can have one has M or so pretty much we can have one has M or so pretty much we can have one has M or others have I or we have several can others have I or we have several can others have I or we have several can have s and the rest have I we cannot have s and the rest have I we cannot have s and the rest have I we cannot have m and s in the system at the same have m and s in the system at the same have m and s in the system at the same time for the same block and definitely time for the same block and definitely time for the same block and definitely we cannot have mmm

## Avoiding Memory Writes On Cache to Cache Transfers： Part 5
Kind: captions Language: en so now we have seen that in MSI even so now we have seen that in MSI even so now we have seen that in MSI even when we have an intervention we need to when we have an intervention we need to when we have an intervention we need to write to memory every time we have a write to memory every time we have a write to memory every time we have a cache to cach transfer so for example C1 cache to cach transfer so for example C1 cache to cach transfer so for example C1 has the Block in the M State cuz it just has the Block in the M State cuz it just has the Block in the M State cuz it just wrote it C2 wants to read the data so it wrote it C2 wants to read the data so it wrote it C2 wants to read the data so it puts a read request on the bus C1 puts a read request on the bus C1 puts a read request on the bus C1 responds with the data so now C2 has the responds with the data so now C2 has the responds with the data so now C2 has the data but C1 can no longer just write to data but C1 can no longer just write to data but C1 can no longer just write to this block so C1 gets the block in the this block so C1 gets the block in the this block so C1 gets the block in the shared State C2 gets the Block in the shared State C2 gets the Block in the shared State C2 gets the Block in the shared State and that's why the memory shared State and that's why the memory shared State and that's why the memory had to grab the data when C1 was had to grab the data when C1 was had to grab the data when C1 was responding now what can happen is C2 responding now what can happen is C2 responding now what can happen is C2 writes so it sends an invalidation cuz writes so it sends an invalidation cuz writes so it sends an invalidation cuz it already has the data now C1 gets it already has the data now C1 gets it already has the data now C1 gets invalid State C2 gets the modified State invalid State C2 gets the modified State invalid State C2 gets the modified State and then let's say this read write and then let's say this read write and then let's say this read write Behavior moves to another core like C1 Behavior moves to another core like C1 Behavior moves to another core like C1 so pretty much if one core reads and so pretty much if one core reads and so pretty much if one core reads and writes then another one reads and writes writes then another one reads and writes writes then another one reads and writes and so on we can have a repetition of and so on we can have a repetition of and so on we can have a repetition of this so pretty much the data is kind of this so pretty much the data is kind of this so pretty much the data is kind of moving around the caches but the problem moving around the caches but the problem moving around the caches but the problem is every time it moves between caches is every time it moves between caches is every time it moves between caches the memory also gets written and the the memory also gets written and the the memory also gets written and the memory doesn't have as much bandwidth as memory doesn't have as much bandwidth as memory doesn't have as much bandwidth as the caches do so the problem really is the caches do so the problem really is the caches do so the problem really is that here we needed to do a memory right that here we needed to do a memory right that here we needed to do a memory right another problem occurs because when C2 another problem occurs because when C2 another problem occurs because when C2 has written for example and has the data has written for example and has the data has written for example and has the data in the modified State and then C2 in the modified State and then C2 in the modified State and then C2 responds with data and that causes a responds with data and that causes a responds with data and that causes a memory right we again get C1 has the memory right we again get C1 has the memory right we again get C1 has the Block in shared State C2 has the Block Block in shared State C2 has the Block Block in shared State C2 has the Block in shared State now if C3 reads because in shared State now if C3 reads because in shared State now if C3 reads because both of these have the Block in the both of these have the Block in the both of these have the Block in the shared State the memory provides the shared State the memory provides the shared State the memory provides the data so we have memory reads here even data so we have memory reads here even data so we have memory reads here even though one of these could have responded though one of these could have responded though one of these could have responded with the data if we only knew which of with the data if we only knew which of with the data if we only knew which of the two should respond definitely both the two should respond definitely both the two should respond definitely both of them should not respond so if we had of them should not respond so if we had of them should not respond so if we had the way to tell which one of them should the way to tell which one of them should the way to tell which one of them should respond we could avoid this memory read respond we could avoid this memory read respond we could avoid this memory read by the one that is responsible for by the one that is responsible for by the one that is responsible for responding providing the data as long as responding providing the data as long as responding providing the data as long as there are new requesters this can happen there are new requesters this can happen there are new requesters this can happen a lot of times so if another core reads a lot of times so if another core reads a lot of times so if another core reads same thing we get the memory read same thing we get the memory read same thing we get the memory read although now there are three cores with although now there are three cores with although now there are three cores with the data in the shared State and so on the data in the shared State and so on the data in the shared State and so on we want to avoid the update to memory as we want to avoid the update to memory as we want to avoid the update to memory as long as there is a cache that still long as there is a cache that still long as there is a cache that still holds the most recent value of the block holds the most recent value of the block holds the most recent value of the block and we want to avoid memory reads if and we want to avoid memory reads if and we want to avoid memory reads if there is a cache that can provide this there is a cache that can provide this there is a cache that can provide this block because again memory bandwidth is block because again memory bandwidth is block because again memory bandwidth is a lot lower than cash bandwidth and also a lot lower than cash bandwidth and also a lot lower than cash bandwidth and also memory reads and wrs are a lot more memory reads and wrs are a lot more memory reads and wrs are a lot more expensive in terms of power and latency expensive in terms of power and latency expensive in terms of power and latency than cach reads and wrs but to do this than cach reads and wrs but to do this than cach reads and wrs but to do this we need to make a non-modified version we need to make a non-modified version we need to make a non-modified version of the Block in one of the caches of the Block in one of the caches of the Block in one of the caches responsible for giving data to other responsible for giving data to other responsible for giving data to other caches when they want it so that the caches when they want it so that the caches when they want it so that the memory doesn't need to respond and memory doesn't need to respond and memory doesn't need to respond and eventually writing the block to memory eventually writing the block to memory eventually writing the block to memory so that the memory doesn't need to be so that the memory doesn't need to be so that the memory doesn't need to be updated every time we do something like updated every time we do something like updated every time we do something like this so what we really need to know is this so what we really need to know is this so what we really need to know is which of the shared copies is the one which of the shared copies is the one which of the shared copies is the one responsible and we will know this by responsible and we will know this by responsible and we will know this by introducing another state called o which introducing another state called o which introducing another state called o which stands for owned so there will be one stands for owned so there will be one stands for owned so there will be one owner of the block if others are sharing owner of the block if others are sharing owner of the block if others are sharing it the O state will behave just like the it the O state will behave just like the it the O state will behave just like the shared State except that whenever there shared State except that whenever there shared State except that whenever there is a request for the data in the block is a request for the data in the block is a request for the data in the block the owner is the one of the sharers that the owner is the one of the sharers that the owner is the one of the sharers that gets to respond and also if the owner gets to respond and also if the owner gets to respond and also if the owner replaces the block from the cache it replaces the block from the cache it replaces the block from the cache it gets to write back to memory so that we gets to write back to memory so that we gets to write back to memory so that we ensure that memory gets updated

## MOSI Coherence： Part 5
Kind: captions Language: en so now we have what is called a mozi so now we have what is called a mozi so now we have what is called a mozi coherence protocol M s and I are the coherence protocol M s and I are the coherence protocol M s and I are the same as before but now there is a new same as before but now there is a new same as before but now there is a new old state so the Moche protocol is like old state so the Moche protocol is like old state so the Moche protocol is like MSI except when we are in the m state MSI except when we are in the m state MSI except when we are in the m state and we snoopeh read from another core we and we snoopeh read from another core we and we snoopeh read from another core we provide the data as before but we now provide the data as before but we now provide the data as before but we now move into the off state not the s state move into the off state not the s state move into the off state not the s state when we are providing the data the when we are providing the data the when we are providing the data the memory now doesn't get accessed in MSI memory now doesn't get accessed in MSI memory now doesn't get accessed in MSI the memory would now need to be written the memory would now need to be written the memory would now need to be written with the data here we simply provide the with the data here we simply provide the with the data here we simply provide the data to the requester move to the O data to the requester move to the O data to the requester move to the O state without updating the memory while state without updating the memory while state without updating the memory while the block is in the O state in a cache the block is in the O state in a cache the block is in the O state in a cache it behaves similarly to the S state it behaves similarly to the S state it behaves similarly to the S state except that if we snoop a read we except that if we snoop a read we except that if we snoop a read we continue to provide the data just like continue to provide the data just like continue to provide the data just like we did in the M State and that we need we did in the M State and that we need we did in the M State and that we need to write the data back to memory if the to write the data back to memory if the to write the data back to memory if the block in the O state is replaced from block in the O state is replaced from block in the O state is replaced from that cache and that's because all of the that cache and that's because all of the that cache and that's because all of the other caches will have the Block in other caches will have the Block in other caches will have the Block in either invalid or shared state so they either invalid or shared state so they either invalid or shared state so they don't know that the block is dirty and don't know that the block is dirty and don't know that the block is dirty and needs to be replaced so the M state needs to be replaced so the M state needs to be replaced so the M state indicated both that we have exclusive indicated both that we have exclusive indicated both that we have exclusive readwrite access to the block nobody readwrite access to the block nobody readwrite access to the block nobody else has it and that the block is dirty else has it and that the block is dirty else has it and that the block is dirty as indicated that we have the shared as indicated that we have the shared as indicated that we have the shared read access to the block others may also read access to the block others may also read access to the block others may also have it in shared state and that the have it in shared state and that the have it in shared state and that the block is clean the memory has an block is clean the memory has an block is clean the memory has an up-to-date copy now all combines the up-to-date copy now all combines the up-to-date copy now all combines the properties of M and s States in that it properties of M and s States in that it properties of M and s States in that it is a shared read access state but the is a shared read access state but the is a shared read access state but the block is dirty and we are responsible block is dirty and we are responsible block is dirty and we are responsible for putting the data in memory and also for putting the data in memory and also for putting the data in memory and also because there is only one cache with the because there is only one cache with the because there is only one cache with the Block in the O state that cache knows Block in the O state that cache knows Block in the O state that cache knows that it can respond to all the read that it can respond to all the read that it can respond to all the read accesses to the data so that the memory accesses to the data so that the memory accesses to the data so that the memory doesn't

## M(O)SI Inefficiency： Part 5
Kind: captions Language: en so we have used the old state to avoid so we have used the old state to avoid so we have used the old state to avoid inefficiency of the MSI protocol that inefficiency of the MSI protocol that inefficiency of the MSI protocol that had to do with the memory getting had to do with the memory getting had to do with the memory getting unnecessary accesses that could be unnecessary accesses that could be unnecessary accesses that could be satisfied by caches that already have satisfied by caches that already have satisfied by caches that already have the data there is another inefficiency the data there is another inefficiency the data there is another inefficiency that both MSI and MOSI have and that that both MSI and MOSI have and that that both MSI and MOSI have and that inefficiencies related to thread private inefficiencies related to thread private inefficiencies related to thread private data or data that is only ever accessed data or data that is only ever accessed data or data that is only ever accessed by a single thread and thread private by a single thread and thread private by a single thread and thread private data is for example all data in a data is for example all data in a data is for example all data in a single-threaded program will be thread single-threaded program will be thread single-threaded program will be thread private so if we have a four core private so if we have a four core private so if we have a four core processor around for single threaded processor around for single threaded processor around for single threaded programs they never share any data all programs they never share any data all programs they never share any data all of the data in these programs is like of the data in these programs is like of the data in these programs is like this even when we have a parallel this even when we have a parallel this even when we have a parallel program we still have data such as for program we still have data such as for program we still have data such as for example the stacks of the individual example the stacks of the individual example the stacks of the individual threads that are still gonna be accessed threads that are still gonna be accessed threads that are still gonna be accessed only by a single thread and then only by a single thread and then only by a single thread and then efficiency occurs when we read the data efficiency occurs when we read the data efficiency occurs when we read the data in a thread and then rewrite it and this in a thread and then rewrite it and this in a thread and then rewrite it and this is really the only thread accessing this is really the only thread accessing this is really the only thread accessing this data with both MSI and mosy what's going data with both MSI and mosy what's going data with both MSI and mosy what's going to happen is the data was invalid before to happen is the data was invalid before to happen is the data was invalid before we had it we read it we have a mess now we had it we read it we have a mess now we had it we read it we have a mess now we get the data in the shared State we get the data in the shared State we get the data in the shared State when we want to write it because it's a when we want to write it because it's a when we want to write it because it's a shared state we need to send out an shared state we need to send out an shared state we need to send out an invalidation and then we have the Block invalidation and then we have the Block invalidation and then we have the Block in the modified state and for every in the modified state and for every in the modified state and for every single block of data we have to do this single block of data we have to do this single block of data we have to do this in contrast if we had a uniprocessor the in contrast if we had a uniprocessor the in contrast if we had a uniprocessor the block would have a valid bit of zero we block would have a valid bit of zero we block would have a valid bit of zero we read it we have a mess now the valid bit read it we have a mess now the valid bit read it we have a mess now the valid bit becomes one and then we have a hit when becomes one and then we have a hit when becomes one and then we have a hit when we write it and then we just change the we write it and then we just change the we write it and then we just change the dirty bit to one so somehow yeah dirty bit to one so somehow yeah dirty bit to one so somehow yeah uniprocessor gets to do a Miss here and uniprocessor gets to do a Miss here and uniprocessor gets to do a Miss here and a hit here whereas to do msi or mossy we a hit here whereas to do msi or mossy we a hit here whereas to do msi or mossy we have to do a miss followed by another have to do a miss followed by another have to do a miss followed by another bus request granted it doesn't care it bus request granted it doesn't care it bus request granted it doesn't care it data but still we have to wait for the data but still we have to wait for the data but still we have to wait for the bus send something outside the chip and bus send something outside the chip and bus send something outside the chip and so on so this is still a lot slower than so on so this is still a lot slower than so on so this is still a lot slower than just a cache hit and it's ok to pay the just a cache hit and it's ok to pay the just a cache hit and it's ok to pay the penalty for having coherence if we're penalty for having coherence if we're penalty for having coherence if we're actually sharing data because a actually sharing data because a actually sharing data because a uniprocessor would not provide coherent uniprocessor would not provide coherent uniprocessor would not provide coherent behavior but here we actually behavior but here we actually behavior but here we actually no sharing whatsoever so what we want to no sharing whatsoever so what we want to no sharing whatsoever so what we want to do is avoid this invalidation and to do do is avoid this invalidation and to do do is avoid this invalidation and to do that we introduce a new state called that we introduce a new state called that we introduce a new state called exclusive

## The E State： Part 5
Kind: captions Language: en so what does the e state do the M State so what does the e state do the M State so what does the e state do the M State gives us exclusive access so we can both gives us exclusive access so we can both gives us exclusive access so we can both read and write and makes us the owner of read and write and makes us the owner of read and write and makes us the owner of a dirty block so that we have to respond a dirty block so that we have to respond a dirty block so that we have to respond with the data and also update memory the with the data and also update memory the with the data and also update memory the S state gives us shared access so that S state gives us shared access so that S state gives us shared access so that we can only read and we are not we can only read and we are not we can only read and we are not responsible for giving others our data responsible for giving others our data responsible for giving others our data and also we are not responsible for and also we are not responsible for and also we are not responsible for updating memory we have seen that the updating memory we have seen that the updating memory we have seen that the old state also gives us shared access so old state also gives us shared access so old state also gives us shared access so we can do reads we can do reads we can do reads but makes us responsible for updating but makes us responsible for updating but makes us responsible for updating memory and giving data to others so that memory and giving data to others so that memory and giving data to others so that we avoid memory rights that occur when we avoid memory rights that occur when we avoid memory rights that occur when we don't have this state what it does is we don't have this state what it does is we don't have this state what it does is it gives us exclusive so that we can it gives us exclusive so that we can it gives us exclusive so that we can read and write read and write read and write although the block is still clean the although the block is still clean the although the block is still clean the memory still doesn't need to be updated memory still doesn't need to be updated memory still doesn't need to be updated so when acordes read a followed by write so when acordes read a followed by write so when acordes read a followed by write a in the MSI protocol we move from a in the MSI protocol we move from a in the MSI protocol we move from invalid to shared and suffer a mess and invalid to shared and suffer a mess and invalid to shared and suffer a mess and then we move from shared to modified and then we move from shared to modified and then we move from shared to modified and need to send out an invalidation so we need to send out an invalidation so we need to send out an invalidation so we had to bus accesses if we have the M o had to bus accesses if we have the M o had to bus accesses if we have the M o si we have the same sequence because si we have the same sequence because si we have the same sequence because really the old state is not helping us really the old state is not helping us really the old state is not helping us here it's helping us save memory axises here it's helping us save memory axises here it's helping us save memory axises after we had a write and other start after we had a write and other start after we had a write and other start reading if we have the m es i with no o reading if we have the m es i with no o reading if we have the m es i with no o state or a protocol that combines all of state or a protocol that combines all of state or a protocol that combines all of the states when we do a read we now the states when we do a read we now the states when we do a read we now detect that although we have read the detect that although we have read the detect that although we have read the block we are the only one having it so block we are the only one having it so block we are the only one having it so instead of going to the shared state we instead of going to the shared state we instead of going to the shared state we go to the e state and we suffer a miss go to the e state and we suffer a miss go to the e state and we suffer a miss but the idea now is that when our right but the idea now is that when our right but the idea now is that when our right to a occurs we know we have exclusive to a occurs we know we have exclusive to a occurs we know we have exclusive access so we can just write without access so we can just write without access so we can just write without telling anybody although we have to telling anybody although we have to telling anybody although we have to transition to the end state now because transition to the end state now because transition to the end state now because now the block is also dirty so now we now the block is also dirty so now we now the block is also dirty so now we get the same behavior that we would get get the same behavior that we would get get the same behavior that we would get if we did the sequence of accesses on a if we did the sequence of accesses on a if we did the sequence of accesses on a cash-in a uniprocessor so this is not a cash-in a uniprocessor so this is not a cash-in a uniprocessor so this is not a shared block here and we want to get the shared block here and we want to get the shared block here and we want to get the same number of missed same number of missed same number of missed and accesses as we do in a uniprocessor and accesses as we do in a uniprocessor and accesses as we do in a uniprocessor that doesn't share data so here we will that doesn't share data so here we will that doesn't share data so here we will have a Miss followed by a cache hit we have a Miss followed by a cache hit we have a Miss followed by a cache hit we want to have a cache hit here and the want to have a cache hit here and the want to have a cache hit here and the east state allows us to achieve that

## MOESI Quiz： Part 5
Kind: captions Language: en so now let's try our hand at the mo SC so now let's try our hand at the mo SC so now let's try our hand at the mo SC protocol suppose we have three cores protocol suppose we have three cores protocol suppose we have three cores maintain moiseev coherence and the maintain moiseev coherence and the maintain moiseev coherence and the variable X starts out by only being in variable X starts out by only being in variable X starts out by only being in memory so it's not in any of the caches memory so it's not in any of the caches memory so it's not in any of the caches so we have our three course C 0 C 1 and so we have our three course C 0 C 1 and so we have our three course C 0 C 1 and C 2 and the first axis that happens is C 2 and the first axis that happens is C 2 and the first axis that happens is read X on core 0 what is the new state read X on core 0 what is the new state read X on core 0 what is the new state of this block in the cache that belongs of this block in the cache that belongs of this block in the cache that belongs to this core next c1 does read X what is to this core next c1 does read X what is to this core next c1 does read X what is the new state of this block on this the new state of this block on this the new state of this block on this score and what is the state of this score and what is the state of this score and what is the state of this block on C 0 after c1 is done with this block on C 0 after c1 is done with this block on C 0 after c1 is done with this read now c2 does read X what is its new read now c2 does read X what is its new read now c2 does read X what is its new state for this block and what is the new state for this block and what is the new state for this block and what is the new state of this block in c1s cache or does state of this block in c1s cache or does state of this block in c1s cache or does it just stay the same and finally see it just stay the same and finally see it just stay the same and finally see one does a write to X what is the new one does a write to X what is the new one does a write to X what is the new state of the Block in see once cash-in state of the Block in see once cash-in state of the Block in see once cash-in c2s cache and in the cache of course is c2s cache and in the cache of course is c2s cache and in the cache of course is

## MOESI Solution Quiz： Part 5
Kind: captions Language: en in a mercy protocol the state can be m in a mercy protocol the state can be m in a mercy protocol the state can be m OE s or I initially all of the course OE s or I initially all of the course OE s or I initially all of the course have the Block in the I state in their have the Block in the I state in their have the Block in the I state in their caches when c0 reads it detects that caches when c0 reads it detects that caches when c0 reads it detects that it's the only one who actually has a it's the only one who actually has a it's the only one who actually has a copy of the block and because he's doing copy of the block and because he's doing copy of the block and because he's doing a read it doesn't go into the modified a read it doesn't go into the modified a read it doesn't go into the modified State it goes into the exclusive east State it goes into the exclusive east State it goes into the exclusive east state the others keep the Block in the I state the others keep the Block in the I state the others keep the Block in the I state when C one reads it detected it's state when C one reads it detected it's state when C one reads it detected it's not the only one who has the block so it not the only one who has the block so it not the only one who has the block so it normally goes to s state furthermore c0 normally goes to s state furthermore c0 normally goes to s state furthermore c0 now snoops another reader so it's no now snoops another reader so it's no now snoops another reader so it's no longer exclusive and it moves to the longer exclusive and it moves to the longer exclusive and it moves to the shared State - C - still has the invalid shared State - C - still has the invalid shared State - C - still has the invalid state for its block when c2 reads it state for its block when c2 reads it state for its block when c2 reads it also detects that there are two other also detects that there are two other also detects that there are two other sharers so it gets the Block in the sharers so it gets the Block in the sharers so it gets the Block in the shared state c1 nothing happens to its shared state c1 nothing happens to its shared state c1 nothing happens to its shared state when we snow parade and we shared state when we snow parade and we shared state when we snow parade and we are in the shared state we just stay are in the shared state we just stay are in the shared state we just stay there because others reading is there because others reading is there because others reading is compatible with the shared state c0 also compatible with the shared state c0 also compatible with the shared state c0 also actually is in the shared state although actually is in the shared state although actually is in the shared state although you were not asked to do this and now you were not asked to do this and now you were not asked to do this and now finally when c1 writes because it was in finally when c1 writes because it was in finally when c1 writes because it was in the shared state it will put an the shared state it will put an the shared state it will put an invalidation request on the bus and once invalidation request on the bus and once invalidation request on the bus and once it does that it can move to the modified it does that it can move to the modified it does that it can move to the modified State State State others see that invalidation request and others see that invalidation request and others see that invalidation request and go to the invalid state pretty much go to the invalid state pretty much go to the invalid state pretty much after a write we are always in the after a write we are always in the after a write we are always in the modified state and all others are always modified state and all others are always modified state and all others are always in the eye state at that point


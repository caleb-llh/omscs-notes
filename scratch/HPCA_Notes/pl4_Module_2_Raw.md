# Playlist 4 Module 2 Raw Transcript

## N Module quiz Quiz： Part 5
Kind: captions Language: en let's check our understanding of and let's check our understanding of and let's check our understanding of and module redundancy suppose we have a module redundancy suppose we have a module redundancy suppose we have a computer and we want to tolerate faults computer and we want to tolerate faults computer and we want to tolerate faults that can occur there so we go to a store that can occur there so we go to a store that can occur there so we go to a store and buy two more computers just like the and buy two more computers just like the and buy two more computers just like the first one and put all of them on the first one and put all of them on the first one and put all of them on the same desk and our approach to redundancy same desk and our approach to redundancy same desk and our approach to redundancy is that we run every computation on all is that we run every computation on all is that we run every computation on all three computers we compare their outputs three computers we compare their outputs three computers we compare their outputs and we take that output on which and we take that output on which and we take that output on which two or all three agree and with this two or all three agree and with this two or all three agree and with this approach we think we can tolerate any approach we think we can tolerate any approach we think we can tolerate any single event of the following kinds single event of the following kinds single event of the following kinds check each of those that can be check each of those that can be check each of those that can be tolerated can we tolerate any single tolerated can we tolerate any single tolerated can we tolerate any single alpha particle strike in a processor alpha particle strike in a processor alpha particle strike in a processor can we tolerate any single building can we tolerate any single building can we tolerate any single building collapse collapse collapse can we tolerate any single earthquake can we tolerate any single earthquake can we tolerate any single earthquake and can we tolerate any single mistake and can we tolerate any single mistake and can we tolerate any single mistake in processor design

## N module quiz Quiz Solution： Part 5
Kind: captions Language: en let's look at the solution to our n let's look at the solution to our n let's look at the solution to our n module redundancy quiz where we had module redundancy quiz where we had module redundancy quiz where we had three computers that are totally three computers that are totally three computers that are totally identical on the same desk and we use identical on the same desk and we use identical on the same desk and we use them as a three modular redundant system them as a three modular redundant system them as a three modular redundant system and the question is what type of event and the question is what type of event and the question is what type of event can we always overcome a single alpha can we always overcome a single alpha can we always overcome a single alpha particle strike is going to affect only particle strike is going to affect only particle strike is going to affect only a single processor and that means that a single processor and that means that a single processor and that means that the two computers that are not affected the two computers that are not affected the two computers that are not affected by this will still agree by this will still agree by this will still agree a building collapse a building collapse a building collapse will either be another building and not will either be another building and not will either be another building and not affect anything but if it is the affect anything but if it is the affect anything but if it is the building in which these systems are then building in which these systems are then building in which these systems are then the building collapse is probably going the building collapse is probably going the building collapse is probably going to destroy all three computers so we to destroy all three computers so we to destroy all three computers so we cannot tolerate this type of event cannot tolerate this type of event cannot tolerate this type of event earthquake earthquake earthquake is going to do a similar thing and a is going to do a similar thing and a is going to do a similar thing and a mistake in processor design because the mistake in processor design because the mistake in processor design because the three are identical three are identical three are identical if there is a mistake in processor if there is a mistake in processor if there is a mistake in processor design it's going to affect all three design it's going to affect all three design it's going to affect all three processors at the same time so again we processors at the same time so again we processors at the same time so again we cannot tolerate this cannot tolerate this cannot tolerate this so as you can see so as you can see so as you can see replicating the hardware with identical replicating the hardware with identical replicating the hardware with identical hardware and in an identical location hardware and in an identical location hardware and in an identical location only really ensures that we can survive only really ensures that we can survive only really ensures that we can survive something like a particle strike if we something like a particle strike if we something like a particle strike if we wanted to tolerate earthquakes and wanted to tolerate earthquakes and wanted to tolerate earthquakes and building collapses we would need to building collapses we would need to building collapses we would need to geographically distribute these three geographically distribute these three geographically distribute these three computers if we want to tolerate a computers if we want to tolerate a computers if we want to tolerate a mistake in processor design we would mistake in processor design we would mistake in processor design we would need to use three computers with need to use three computers with need to use three computers with different processors different processors different processors so the n module redundancy so the n module redundancy so the n module redundancy doesn't really guard against these types doesn't really guard against these types doesn't really guard against these types of faults of faults of faults unless the modules are in different unless the modules are in different unless the modules are in different places places places and of different designs

## Fault Tolerance for Memory and Storage： Part 5
Kind: captions Language: en so we have seen some relatively general so we have seen some relatively general so we have seen some relatively general fault tolerance techniques now let's fault tolerance techniques now let's fault tolerance techniques now let's look at the fault tolerance techniques look at the fault tolerance techniques look at the fault tolerance techniques for memory and storage we could use dual for memory and storage we could use dual for memory and storage we could use dual module redundancy or triple modular module redundancy or triple modular module redundancy or triple modular redundancy for memory and storage but redundancy for memory and storage but redundancy for memory and storage but they are considered to be overkill for they are considered to be overkill for they are considered to be overkill for these devices we can get better these devices we can get better these devices we can get better techniques that protect us from similar techniques that protect us from similar techniques that protect us from similar problems so dual module redundancy and problems so dual module redundancy and problems so dual module redundancy and triple modular redundancy are typically triple modular redundancy are typically triple modular redundancy are typically used for the hardware that does the used for the hardware that does the used for the hardware that does the computation which cannot be protected by computation which cannot be protected by computation which cannot be protected by some of the less expensive techniques some of the less expensive techniques some of the less expensive techniques that memory and storage can benefit from that memory and storage can benefit from that memory and storage can benefit from and these techniques for memory and and these techniques for memory and and these techniques for memory and storage storage storage are called error detection or error are called error detection or error are called error detection or error correction codes correction codes correction codes so the idea is that we can store bits so the idea is that we can store bits so the idea is that we can store bits with some extra information that allows with some extra information that allows with some extra information that allows us to detect us to detect us to detect and or correct one or more bits of error and or correct one or more bits of error and or correct one or more bits of error depending on the code so we don't have depending on the code so we don't have depending on the code so we don't have to store everything twice or everything to store everything twice or everything to store everything twice or everything three times in order to achieve three times in order to achieve three times in order to achieve detection or correction of a single detection or correction of a single detection or correction of a single error so the simplest technique that is error so the simplest technique that is error so the simplest technique that is often used for memory storage is parity often used for memory storage is parity often used for memory storage is parity with parity we add one extra bit to the with parity we add one extra bit to the with parity we add one extra bit to the data bits and that extra bit it can be data bits and that extra bit it can be data bits and that extra bit it can be computed simply as an xor of all the computed simply as an xor of all the computed simply as an xor of all the data bits data bits data bits when we do have parity if a fault flips when we do have parity if a fault flips when we do have parity if a fault flips any one of the bits including the parity any one of the bits including the parity any one of the bits including the parity bit the parity bit will no longer match bit the parity bit will no longer match bit the parity bit will no longer match the data by flipping one of the bits we the data by flipping one of the bits we the data by flipping one of the bits we are changing the result of this xor are changing the result of this xor are changing the result of this xor to be the opposite of what it previously to be the opposite of what it previously to be the opposite of what it previously was so when we read the data and the was so when we read the data and the was so when we read the data and the parity and we see that now the parity parity and we see that now the parity parity and we see that now the parity does not match data we assume that there does not match data we assume that there does not match data we assume that there has been a bit flip and that's how we has been a bit flip and that's how we has been a bit flip and that's how we detect a single bit error the next level detect a single bit error the next level detect a single bit error the next level of error detection and correction codes of error detection and correction codes of error detection and correction codes is the so-called ecc which stands for is the so-called ecc which stands for is the so-called ecc which stands for error correction code and a typical error correction code and a typical error correction code and a typical example of this would be a type of code example of this would be a type of code example of this would be a type of code called sec called sec called sec dead codes or dead codes or dead codes or single error correction and double error single error correction and double error single error correction and double error detection code detection code detection code so this code can detect any one bit flip so this code can detect any one bit flip so this code can detect any one bit flip and actually fix it or if there are two and actually fix it or if there are two and actually fix it or if there are two bit flips we will find out that that bit flips we will find out that that bit flips we will find out that that happened but we can no longer fix it happened but we can no longer fix it happened but we can no longer fix it using this code an example of where this using this code an example of where this using this code an example of where this type of a code is used is in data type of a code is used is in data type of a code is used is in data modules that have ecc this is how they modules that have ecc this is how they modules that have ecc this is how they are protected so pretty much if there is are protected so pretty much if there is are protected so pretty much if there is a single bit flip in such a module you a single bit flip in such a module you a single bit flip in such a module you don't even notice everything functions don't even notice everything functions don't even notice everything functions well if there are two errors well if there are two errors well if there are two errors in the data that is protected by the in the data that is protected by the in the data that is protected by the same code then we will get notified that same code then we will get notified that same code then we will get notified that there's been an error but we cannot there's been an error but we cannot there's been an error but we cannot actually continue operating normally actually continue operating normally actually continue operating normally because actually we cannot correct the because actually we cannot correct the because actually we cannot correct the error near state anymore hard drives use error near state anymore hard drives use error near state anymore hard drives use even fancier codes such as the read even fancier codes such as the read even fancier codes such as the read solomon codes that i'm not going to go solomon codes that i'm not going to go solomon codes that i'm not going to go into here and these codes can detect and into here and these codes can detect and into here and these codes can detect and correct multiple bit errors and they're correct multiple bit errors and they're correct multiple bit errors and they're especially powerful when we have a especially powerful when we have a especially powerful when we have a streak of flip bits which happens for streak of flip bits which happens for streak of flip bits which happens for example when the head oscillates a example when the head oscillates a example when the head oscillates a little bit and while the disc is little bit and while the disc is little bit and while the disc is spinning it for a while just is too high spinning it for a while just is too high spinning it for a while just is too high above the platter and misses some of the above the platter and misses some of the above the platter and misses some of the bits bits bits and then for hard drives in addition to and then for hard drives in addition to and then for hard drives in addition to these error detection and correction these error detection and correction these error detection and correction codes there is a family of techniques codes there is a family of techniques codes there is a family of techniques called raid called raid called raid that we will look at next

## RAID 0： Part 5
Kind: captions Language: en so r 80 uses a technique called striping so r 80 uses a technique called striping so r 80 uses a technique called striping to improve performance so if we have one to improve performance so if we have one to improve performance so if we have one disc it will have a track 0 track one disc it will have a track 0 track one disc it will have a track 0 track one track 2 etc and the problem is that track 2 etc and the problem is that track 2 etc and the problem is that while the head is positioned to retrack while the head is positioned to retrack while the head is positioned to retrack 0 it cannot be reading track 1 and also 0 it cannot be reading track 1 and also 0 it cannot be reading track 1 and also it's very far away from all of these it's very far away from all of these it's very far away from all of these other tracks so what happens is really other tracks so what happens is really other tracks so what happens is really we have to serialize accesses to all of we have to serialize accesses to all of we have to serialize accesses to all of the tracks if we need to retracts 0 17 the tracks if we need to retracts 0 17 the tracks if we need to retracts 0 17 and 27 we will have to decide in what and 27 we will have to decide in what and 27 we will have to decide in what order we access them but for each of order we access them but for each of order we access them but for each of these accesses we move the CAD we read these accesses we move the CAD we read these accesses we move the CAD we read the track we move on to something else the track we move on to something else the track we move on to something else so we cannot get more performance than so we cannot get more performance than so we cannot get more performance than just reading one track at a time at the just reading one track at a time at the just reading one track at a time at the current rotational speed of the disc current rotational speed of the disc current rotational speed of the disc what raid 0 does is it takes two disks what raid 0 does is it takes two disks what raid 0 does is it takes two disks and makes them look like this one disk and makes them look like this one disk and makes them look like this one disk by putting the original tracks 0 here by putting the original tracks 0 here by putting the original tracks 0 here track 1 here track to here track 3 here track 1 here track to here track 3 here track 1 here track to here track 3 here and so on so the idea is that now each and so on so the idea is that now each and so on so the idea is that now each of these tracks is called a stripe now of these tracks is called a stripe now of these tracks is called a stripe now and we're gonna put stripes 0 really and we're gonna put stripes 0 really and we're gonna put stripes 0 really here stripe one here and so on so track here stripe one here and so on so track here stripe one here and so on so track 0 of the first disc gets track 0 from 0 of the first disc gets track 0 from 0 of the first disc gets track 0 from the overall large disk track 0 here the overall large disk track 0 here the overall large disk track 0 here however gets the stripe 0 so instead of however gets the stripe 0 so instead of however gets the stripe 0 so instead of calling these tracks we call them calling these tracks we call them calling these tracks we call them stripes to avoid confusion between stripes to avoid confusion between stripes to avoid confusion between what's a track on a physical disk and what's a track on a physical disk and what's a track on a physical disk and these things that really are tracks that these things that really are tracks that these things that really are tracks that would belong to a fake disk that we would belong to a fake disk that we would belong to a fake disk that we don't really have because really these don't really have because really these don't really have because really these two disks are pretending to be this one two disks are pretending to be this one two disks are pretending to be this one disk as far as performance is concerned disk as far as performance is concerned disk as far as performance is concerned we get twice the data throughput if we we get twice the data throughput if we we get twice the data throughput if we have two disks why well because well the have two disks why well because well the have two disks why well because well the first disc is reading some data the first disc is reading some data the first disc is reading some data the second could be reading data to and second could be reading data to and second could be reading data to and because controllers and buses today are because controllers and buses today are because controllers and buses today are faster than what the disk can get to faster than what the disk can get to faster than what the disk can get to effectively we can transfer twice the effectively we can transfer twice the effectively we can transfer twice the amount of data assuming that those discs amount of data assuming that those discs amount of data assuming that those discs are actually transferring the data you are actually transferring the data you are actually transferring the data you can be unlucky and all the data you want can be unlucky and all the data you want can be unlucky and all the data you want is on the stripes that are placed on is on the stripes that are placed on is on the stripes that are placed on this desk in which case you're going to this desk in which case you're going to this desk in which case you're going to get only the data throughput of a single get only the data throughput of a single get only the data throughput of a single disk but on average we tend to get disk but on average we tend to get disk but on average we tend to get almost almost almost twice the throughput of a single disc by twice the throughput of a single disc by twice the throughput of a single disc by splitting the data across two disks like splitting the data across two disks like splitting the data across two disks like this as far as latency is concerned this as far as latency is concerned this as far as latency is concerned because we have more throughput we get because we have more throughput we get because we have more throughput we get less queuing delay normally we will be less queuing delay normally we will be less queuing delay normally we will be able to handle requests one at a time able to handle requests one at a time able to handle requests one at a time here now we can actually in parallel do here now we can actually in parallel do here now we can actually in parallel do two requests so in the queue we get two requests so in the queue we get two requests so in the queue we get faster to the request by basically faster to the request by basically faster to the request by basically handling previous requests faster but handling previous requests faster but handling previous requests faster but what about reliability unfortunately it what about reliability unfortunately it what about reliability unfortunately it gets worse than one disk

## RAID 0 Reliability： Part 5
Kind: captions Language: en to see what raid 0 reliability looks to see what raid 0 reliability looks to see what raid 0 reliability looks like let's assume like let's assume like let's assume that f is a failure rate for a single that f is a failure rate for a single that f is a failure rate for a single disk disk disk a failure rate is really how many a failure rate is really how many a failure rate is really how many failures do we expect for a single failures do we expect for a single failures do we expect for a single working disk to have in a second working disk to have in a second working disk to have in a second obviously this number will be extremely obviously this number will be extremely obviously this number will be extremely small small small usually we can assume that the failure usually we can assume that the failure usually we can assume that the failure rate is constant over time that is every rate is constant over time that is every rate is constant over time that is every second if the disk is working at the second if the disk is working at the second if the disk is working at the beginning of the second it has the same beginning of the second it has the same beginning of the second it has the same failure rate during that second failure rate during that second failure rate during that second for a single disk then for a single disk then for a single disk then the mttf the mean time to failure will the mttf the mean time to failure will the mttf the mean time to failure will be be be one over f one over f one over f so for example if the disk has one so for example if the disk has one so for example if the disk has one millionth of a failure per second then millionth of a failure per second then millionth of a failure per second then the mttf will be the mttf will be the mttf will be 1 million seconds 1 million seconds 1 million seconds for disks the mttf is also sometimes for disks the mttf is also sometimes for disks the mttf is also sometimes called called called mean time to data loss or how long until mean time to data loss or how long until mean time to data loss or how long until we lose some data on this disk we lose some data on this disk we lose some data on this disk and for a single disk the mean time to and for a single disk the mean time to and for a single disk the mean time to data loss is the same as the mttf of one data loss is the same as the mttf of one data loss is the same as the mttf of one disk disk disk now let's look at what happens when we now let's look at what happens when we now let's look at what happens when we have have have and such disks in a raid 0 configuration and such disks in a raid 0 configuration and such disks in a raid 0 configuration it turns out that the failure rate when it turns out that the failure rate when it turns out that the failure rate when we have n working disks we have n working disks we have n working disks is equal to n times is equal to n times is equal to n times the failure rate the failure rate the failure rate of one disk of one disk of one disk this is because for example if we can this is because for example if we can this is because for example if we can expect one millionth of a failure per expect one millionth of a failure per expect one millionth of a failure per working disk per second working disk per second working disk per second then we can expect one failure per then we can expect one failure per then we can expect one failure per million working disks per second so if million working disks per second so if million working disks per second so if we start the second with a million we start the second with a million we start the second with a million working disks working disks working disks chances are that one of them will fail chances are that one of them will fail chances are that one of them will fail during the second if we start the second during the second if we start the second during the second if we start the second with half a million working disks with half a million working disks with half a million working disks on average half of a single disk will on average half of a single disk will on average half of a single disk will fail per second and so on fail per second and so on fail per second and so on it also turns out that the mean time to it also turns out that the mean time to it also turns out that the mean time to the first failure among n working disks the first failure among n working disks the first failure among n working disks has the same distribution as the first has the same distribution as the first has the same distribution as the first failure for the single disk failure for the single disk failure for the single disk so we get that mttf so we get that mttf so we get that mttf for n disks for n disks for n disks in a raid zero configuration where mttf in a raid zero configuration where mttf in a raid zero configuration where mttf is really the mean time to data loss in is really the mean time to data loss in is really the mean time to data loss in this case this case this case is equal to is equal to is equal to the mttf of one disk the mttf of one disk the mttf of one disk divided by n divided by n divided by n so if you have two disks the mttf of the so if you have two disks the mttf of the so if you have two disks the mttf of the raid 0 array with two disks will be half raid 0 array with two disks will be half raid 0 array with two disks will be half of the fttf of a single disk of the fttf of a single disk of the fttf of a single disk a single disk typically will have the a single disk typically will have the a single disk typically will have the mttf on the order of 10-ish years to mttf on the order of 10-ish years to mttf on the order of 10-ish years to maybe 100 years maybe 100 years maybe 100 years so if we have too many disks in a raid so if we have too many disks in a raid so if we have too many disks in a raid zero configuration we can reduce the zero configuration we can reduce the zero configuration we can reduce the mttf so the average disk will not fail mttf so the average disk will not fail mttf so the average disk will not fail in the five or six years that we in the five or six years that we in the five or six years that we actually end up using it actually end up using it actually end up using it but if we have two or three or four such but if we have two or three or four such but if we have two or three or four such disks then we can expect the array to disks then we can expect the array to disks then we can expect the array to fail at least once until we actually get fail at least once until we actually get fail at least once until we actually get rid of it

## RAID 0 Quiz： Part 5
Kind: captions Language: en let us now test our understanding of let us now test our understanding of let us now test our understanding of raid0 suppose that we are building a raid0 suppose that we are building a raid0 suppose that we are building a raid 0 array with 4 disks suppose that raid 0 array with 4 disks suppose that raid 0 array with 4 disks suppose that for one such disk we get 200 gigabytes for one such disk we get 200 gigabytes for one such disk we get 200 gigabytes capacity 10 megabytes of throughput and capacity 10 megabytes of throughput and capacity 10 megabytes of throughput and an MTTF of a hundred thousand hours so a an MTTF of a hundred thousand hours so a an MTTF of a hundred thousand hours so a raid 0 array can store how many raid 0 array can store how many raid 0 array can store how many gigabytes has a throughput of how many gigabytes has a throughput of how many gigabytes has a throughput of how many megabytes per second and an MTTF of how megabytes per second and an MTTF of how megabytes per second and an MTTF of how many hours

## RAID 0 Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to a rate let's look at the solution to a rate let's look at the solution to a rate zero quiz where we have an array of four zero quiz where we have an array of four zero quiz where we have an array of four disks disks disks and for one disk we know the capacity and for one disk we know the capacity and for one disk we know the capacity throughput and mttf throughput and mttf throughput and mttf in raid 0 we're striping the data across in raid 0 we're striping the data across in raid 0 we're striping the data across all the disks with no redundancy so we all the disks with no redundancy so we all the disks with no redundancy so we can use the capacity of all four disks can use the capacity of all four disks can use the capacity of all four disks and thus get and thus get and thus get 800 gigabytes in capacity 800 gigabytes in capacity 800 gigabytes in capacity if we spread the read and write load if we spread the read and write load if we spread the read and write load evenly across the disks then one disk evenly across the disks then one disk evenly across the disks then one disk being able to support 10 megabytes per being able to support 10 megabytes per being able to support 10 megabytes per second and four disks being used in second and four disks being used in second and four disks being used in parallel like this parallel like this parallel like this gives us a throughput of 40 megabytes gives us a throughput of 40 megabytes gives us a throughput of 40 megabytes per second and finally per second and finally per second and finally if the mttf for one disk is a hundred if the mttf for one disk is a hundred if the mttf for one disk is a hundred thousand hours thousand hours thousand hours the expected mttf for the whole array is the expected mttf for the whole array is the expected mttf for the whole array is going to be a quarter of this if they going to be a quarter of this if they going to be a quarter of this if they are in raid 0 are in raid 0 are in raid 0 so we will get 25 so we will get 25 so we will get 25 000 hours 000 hours 000 hours this mttf ends up being almost 12 years this mttf ends up being almost 12 years this mttf ends up being almost 12 years this one is almost three years this one is almost three years this one is almost three years if we plan to replace our array after if we plan to replace our array after if we plan to replace our array after something like five or six years something like five or six years something like five or six years know that it can still fail within the know that it can still fail within the know that it can still fail within the five to six years five to six years five to six years but on average it's going to last but on average it's going to last but on average it's going to last about 12 years however here our average about 12 years however here our average about 12 years however here our average array is going to fail during the five array is going to fail during the five array is going to fail during the five to six years to six years to six years so obviously with such a low mttf we so obviously with such a low mttf we so obviously with such a low mttf we should not have used four disks in raid should not have used four disks in raid should not have used four disks in raid 0 like this

## RAID 1 Mirroring： Part 5
Kind: captions Language: en and Don Extra, etc. and the driver and d2multires And this is on Litvinovich Hungarian V.V.

## Raid 1 Reliability： Part 5
Kind: captions Language: en but what about the reliability and the but what about the reliability and the but what about the reliability and the MTTF MTTF MTTF of red one same as before we will assume of red one same as before we will assume of red one same as before we will assume that some F is the failure rate for a that some F is the failure rate for a that some F is the failure rate for a single disk in failures per disk per single disk in failures per disk per single disk in failures per disk per second and for a single disk remember second and for a single disk remember second and for a single disk remember that the MTTF is simply 1 over F where F that the MTTF is simply 1 over F where F that the MTTF is simply 1 over F where F is that failure rate and then the mean is that failure rate and then the mean is that failure rate and then the mean time to data loss which is the MTTF time to data loss which is the MTTF time to data loss which is the MTTF really of the whole disk array in this really of the whole disk array in this really of the whole disk array in this case just one disk would be simply the case just one disk would be simply the case just one disk would be simply the MTTF of that one disk well let's say now MTTF of that one disk well let's say now MTTF of that one disk well let's say now that we have two disks in raid 1 and that we have two disks in raid 1 and that we have two disks in raid 1 and remember that the failure rate when we remember that the failure rate when we remember that the failure rate when we have more than one disk is n times the have more than one disk is n times the have more than one disk is n times the failure rate of one disk as long as all failure rate of one disk as long as all failure rate of one disk as long as all of the disks are working so what we have of the disks are working so what we have of the disks are working so what we have really is that both of our disks in raid really is that both of our disks in raid really is that both of our disks in raid 1 are ok until the time MTTF of one disk 1 are ok until the time MTTF of one disk 1 are ok until the time MTTF of one disk over 2 this is the expected time when over 2 this is the expected time when over 2 this is the expected time when one of the disks will fail now unlike one of the disks will fail now unlike one of the disks will fail now unlike raid 0 where this is the point where it raid 0 where this is the point where it raid 0 where this is the point where it would lose data now we have mirroring so would lose data now we have mirroring so would lose data now we have mirroring so both of the disks need to fail in order both of the disks need to fail in order both of the disks need to fail in order for the whole thing to lose data one for the whole thing to lose data one for the whole thing to lose data one failure is not data loss yet so we don't failure is not data loss yet so we don't failure is not data loss yet so we don't have the failure of the array yet but at have the failure of the array yet but at have the failure of the array yet but at this time we are down to one disk that this time we are down to one disk that this time we are down to one disk that one disk is a working disk and has this one disk is a working disk and has this one disk is a working disk and has this failure rate so that one remaining this failure rate so that one remaining this failure rate so that one remaining this will live on for another MTTF of a will live on for another MTTF of a will live on for another MTTF of a single disk this is very important to single disk this is very important to single disk this is very important to understand if we have a working disk at understand if we have a working disk at understand if we have a working disk at some point in time the MTTF for that some point in time the MTTF for that some point in time the MTTF for that disk is going to be the overall MTTF for disk is going to be the overall MTTF for disk is going to be the overall MTTF for one disk this MTTF is not affected by one disk this MTTF is not affected by one disk this MTTF is not affected by the fact that this disk has already been the fact that this disk has already been the fact that this disk has already been working for all this time why well working for all this time why well working for all this time why well because the failure rate is constant because the failure rate is constant because the failure rate is constant it's not affected by time so if we have it's not affected by time so if we have it's not affected by time so if we have a working disk its failure rate is a working disk its failure rate is a working disk its failure rate is always the same the only thing that always the same the only thing that always the same the only thing that changes the failure rate is if the disk changes the failure rate is if the disk changes the failure rate is if the disk fails so if we had two disks and both fails so if we had two disks and both fails so if we had two disks and both work this is the expected time of them work this is the expected time of them work this is the expected time of them surviving and then if we're down to one surviving and then if we're down to one surviving and then if we're down to one this that works at the time when we have this that works at the time when we have this that works at the time when we have a single working disk this is the MTTF a single working disk this is the MTTF a single working disk this is the MTTF that we can expect so overall the mean that we can expect so overall the mean that we can expect so overall the mean time to data loss for raid one with two time to data loss for raid one with two time to data loss for raid one with two disks is going to be the MTTF of a disks is going to be the MTTF of a disks is going to be the MTTF of a single disk over two plus the MTTF of a single disk over two plus the MTTF of a single disk over two plus the MTTF of a single disk and now we bought two disks single disk and now we bought two disks single disk and now we bought two disks and use them in an array that has the and use them in an array that has the and use them in an array that has the capacity of only a single disk so capacity of only a single disk so capacity of only a single disk so basically paid for the extra disk just basically paid for the extra disk just basically paid for the extra disk just to get reliability and what we get is to get reliability and what we get is to get reliability and what we get is just one half more of the MTTF that we just one half more of the MTTF that we just one half more of the MTTF that we would normally get with only a single would normally get with only a single would normally get with only a single disk so what's the point the point is disk so what's the point the point is disk so what's the point the point is that this calculation assumes that no that this calculation assumes that no that this calculation assumes that no disk is replaced at the time when it disk is replaced at the time when it disk is replaced at the time when it fails so we had a failure and we just fails so we had a failure and we just fails so we had a failure and we just let that fail disk be there and we were let that fail disk be there and we were let that fail disk be there and we were down to one disk and didn't fix it and down to one disk and didn't fix it and down to one disk and didn't fix it and then we just were down to the MTTF of then we just were down to the MTTF of then we just were down to the MTTF of one disk for a while but remember that one disk for a while but remember that one disk for a while but remember that when we want reliability we definitely when we want reliability we definitely when we want reliability we definitely will replace the failed disks so when we will replace the failed disks so when we will replace the failed disks so when we have the first failure we will as soon have the first failure we will as soon have the first failure we will as soon as possible replace that fail disk copy as possible replace that fail disk copy as possible replace that fail disk copy the data from the working disk to it so the data from the working disk to it so the data from the working disk to it so now we are back to raid 1 working now we are back to raid 1 working now we are back to raid 1 working properly properly properly so what's the MTTF for that

## Raid 1 Reliability if Failed Disks Replaced： Part 5
Kind: captions Language: en so what's raid 1 reliability if we do so what's raid 1 reliability if we do so what's raid 1 reliability if we do replace failed disks as soon as possible replace failed disks as soon as possible replace failed disks as soon as possible we have the situation that both disks we have the situation that both disks we have the situation that both disks are ok are ok are ok for this amount of time then a disk for this amount of time then a disk for this amount of time then a disk fails and then we have only one ok disk fails and then we have only one ok disk fails and then we have only one ok disk for the mean time to repair for one disk for the mean time to repair for one disk for the mean time to repair for one disk so this is how long does it take us to so this is how long does it take us to so this is how long does it take us to replace the disk replace the disk replace the disk after that after that after that both disks are okay again and then we both disks are okay again and then we both disks are okay again and then we again have this long until the next again have this long until the next again have this long until the next failure so what's the mean time to data failure so what's the mean time to data failure so what's the mean time to data loss for raid 1 with two disks loss for raid 1 with two disks loss for raid 1 with two disks the question really is the question really is the question really is how many times can we have this until we how many times can we have this until we how many times can we have this until we encounter the period when a single disk encounter the period when a single disk encounter the period when a single disk has failed and during the time it takes has failed and during the time it takes has failed and during the time it takes to replace it to replace it to replace it another disk fails another disk fails another disk fails well it's equal to the length of this well it's equal to the length of this well it's equal to the length of this interval interval interval this is how long we survive with both this is how long we survive with both this is how long we survive with both disks working times how many times can disks working times how many times can disks working times how many times can we get this until we encounter a repair we get this until we encounter a repair we get this until we encounter a repair period where the second disk fails period where the second disk fails period where the second disk fails during it during it during it so the question is really what's the so the question is really what's the so the question is really what's the probability of the second disk failing probability of the second disk failing probability of the second disk failing during the time to repair one disk during the time to repair one disk during the time to repair one disk now normally to calculate this now normally to calculate this now normally to calculate this probability we would have to compute the probability we would have to compute the probability we would have to compute the distribution for distribution for distribution for this function and then see what's the this function and then see what's the this function and then see what's the probability for this distribution being probability for this distribution being probability for this distribution being under empty tr1 and so on but under empty tr1 and so on but under empty tr1 and so on but when when when the time to repair is a lot smaller than the time to repair is a lot smaller than the time to repair is a lot smaller than the time to failure of a single disk the time to failure of a single disk the time to failure of a single disk then we can approximate this probability then we can approximate this probability then we can approximate this probability of the second disk failing during the of the second disk failing during the of the second disk failing during the mttr for the first disk mttr for the first disk mttr for the first disk simply as simply as simply as the mttr divided by the mttf so for the mttr divided by the mttf so for the mttr divided by the mttf so for example if the example if the example if the mttr is let's say a single day and mttf mttr is let's say a single day and mttf mttr is let's say a single day and mttf is a hundred days is a hundred days is a hundred days then the probability of the second then the probability of the second then the probability of the second failure during the repair period is failure during the repair period is failure during the repair period is simply the length of the repair period simply the length of the repair period simply the length of the repair period divided by the period divided by the period divided by the period that we expected this to survive so for that we expected this to survive so for that we expected this to survive so for example if the time to repair is for example if the time to repair is for example if the time to repair is for example one day example one day example one day and the time to failure is let's say a and the time to failure is let's say a and the time to failure is let's say a hundred days then we can expect that hundred days then we can expect that hundred days then we can expect that there is a one in a hundred chance of a there is a one in a hundred chance of a there is a one in a hundred chance of a disk failing disk failing disk failing during the one day during the one day during the one day so now how many times so now how many times so now how many times can we repair the disk before we can we repair the disk before we can we repair the disk before we encounter one of these well encounter one of these well encounter one of these well the number of times we can try this be the number of times we can try this be the number of times we can try this be until we encounter a failure is going to until we encounter a failure is going to until we encounter a failure is going to be 1 over this probability be 1 over this probability be 1 over this probability so what we finally get is this so what we finally get is this so what we finally get is this the mean time to data loss is mttf of a the mean time to data loss is mttf of a the mean time to data loss is mttf of a single disk squared divided by two times single disk squared divided by two times single disk squared divided by two times the mean time to repair for one disk how the mean time to repair for one disk how the mean time to repair for one disk how long does it take us to replace the disk long does it take us to replace the disk long does it take us to replace the disk and copy the data on it so that now we and copy the data on it so that now we and copy the data on it so that now we have a fully functional raid1 array and have a fully functional raid1 array and have a fully functional raid1 array and as a reminder this is how long as a reminder this is how long as a reminder this is how long do we get a working array with both do we get a working array with both do we get a working array with both disks functional and this is how many disks functional and this is how many disks functional and this is how many times do we do a repair until we can times do we do a repair until we can times do we do a repair until we can expect that the second disk expect that the second disk expect that the second disk that is covering us during the repair is that is covering us during the repair is that is covering us during the repair is going to fail on us going to fail on us going to fail on us note that because the mttf note that because the mttf note that because the mttf is much larger than the mttr is much larger than the mttr is much larger than the mttr this is measured in years usually this this is measured in years usually this this is measured in years usually this hopefully is hours or days it's just how hopefully is hours or days it's just how hopefully is hours or days it's just how long does it take us to replace a disk long does it take us to replace a disk long does it take us to replace a disk this factor here is very very large and this factor here is very very large and this factor here is very very large and now we get many many times now we get many many times now we get many many times the mttf of a single disk over two this the mttf of a single disk over two this the mttf of a single disk over two this factor is way larger than two which factor is way larger than two which factor is way larger than two which means that the time to data loss with means that the time to data loss with means that the time to data loss with the raid one array with only two disks the raid one array with only two disks the raid one array with only two disks is way higher than the time to data loss is way higher than the time to data loss is way higher than the time to data loss for a single disk for a single disk for a single disk which means that raid 1 is dramatically which means that raid 1 is dramatically which means that raid 1 is dramatically improving reliability

## RAID 1 Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out a raid so let's see if we can figure out a raid so let's see if we can figure out a raid 1 array suppose we have a raid 1 array 1 array suppose we have a raid 1 array 1 array suppose we have a raid 1 array with two disks and for a single disk we with two disks and for a single disk we with two disks and for a single disk we have a 200 gigabyte capacity 10 have a 200 gigabyte capacity 10 have a 200 gigabyte capacity 10 megabytes per second throughput and the megabytes per second throughput and the megabytes per second throughput and the MTTF of a hundred thousand hours in that MTTF of a hundred thousand hours in that MTTF of a hundred thousand hours in that case our two disk raid 1 array has a case our two disk raid 1 array has a case our two disk raid 1 array has a data capacity of how many gigabytes a data capacity of how many gigabytes a data capacity of how many gigabytes a throughput of how many megabytes per throughput of how many megabytes per throughput of how many megabytes per second second second assuming that we do equal numbers of assuming that we do equal numbers of assuming that we do equal numbers of reads and writes reads and writes reads and writes and finally our MTTF is equal to how and finally our MTTF is equal to how and finally our MTTF is equal to how many hours and for this type of array we many hours and for this type of array we many hours and for this type of array we also need to know whether we replace also need to know whether we replace also need to know whether we replace failed disks or not failed disks or not failed disks or not so here assume that we do replace the so here assume that we do replace the so here assume that we do replace the failed disk and at the mean time to failed disk and at the mean time to failed disk and at the mean time to repair meaning the mean time it takes us repair meaning the mean time it takes us repair meaning the mean time it takes us to replace the failed disk is 24 hours

## RAID 1 Solution Quiz： Part 5
Kind: captions Language: en so let's look at the solution to a RAID so let's look at the solution to a RAID so let's look at the solution to a RAID one quiz we have a raid 1 array with two one quiz we have a raid 1 array with two one quiz we have a raid 1 array with two disks a single disk in this array is a disks a single disk in this array is a disks a single disk in this array is a 200 gigabyte disk with a throughput of 200 gigabyte disk with a throughput of 200 gigabyte disk with a throughput of 10 megabytes per second and an entity f 10 megabytes per second and an entity f 10 megabytes per second and an entity f of a hundred thousand hours and when a of a hundred thousand hours and when a of a hundred thousand hours and when a disk fails we replace it and it takes us disk fails we replace it and it takes us disk fails we replace it and it takes us 24 hours to do that so in a two disc 24 hours to do that so in a two disc 24 hours to do that so in a two disc RAID one the data capacity is the same RAID one the data capacity is the same RAID one the data capacity is the same as for one disk that's because both as for one disk that's because both as for one disk that's because both disks are storing exactly the same data disks are storing exactly the same data disks are storing exactly the same data which allows us to survive the failure which allows us to survive the failure which allows us to survive the failure of one disk the throughput when we are of one disk the throughput when we are of one disk the throughput when we are reading is twice the throughput of a reading is twice the throughput of a reading is twice the throughput of a single disk because we can issue reads single disk because we can issue reads single disk because we can issue reads to both disks at the same time but when to both disks at the same time but when to both disks at the same time but when we are writing we are writing to both we are writing we are writing to both we are writing we are writing to both disks at once so the throughput for disks at once so the throughput for disks at once so the throughput for writing is only 10 megabytes per second writing is only 10 megabytes per second writing is only 10 megabytes per second if half of the requests are reads and if half of the requests are reads and if half of the requests are reads and the other half of the requests are the other half of the requests are the other half of the requests are writes then in any given second we will writes then in any given second we will writes then in any given second we will be spending one third of a second on the be spending one third of a second on the be spending one third of a second on the reads and we will be spending two-thirds reads and we will be spending two-thirds reads and we will be spending two-thirds of the second on the writes that way we of the second on the writes that way we of the second on the writes that way we get to do the same number of reads and get to do the same number of reads and get to do the same number of reads and writes per second it's just that the writes per second it's just that the writes per second it's just that the reads are twice as fast so they take reads are twice as fast so they take reads are twice as fast so they take half the time of the writes in that same half the time of the writes in that same half the time of the writes in that same second in the one third of a second that second in the one third of a second that second in the one third of a second that we've been doing reads we were getting a we've been doing reads we were getting a we've been doing reads we were getting a 20 megabyte per second throughput 20 megabyte per second throughput 20 megabyte per second throughput because we were using both disks in the because we were using both disks in the because we were using both disks in the two-thirds of the second when we were two-thirds of the second when we were two-thirds of the second when we were doing writes we were getting only 10 doing writes we were getting only 10 doing writes we were getting only 10 megabytes per second so overall during megabytes per second so overall during megabytes per second so overall during this one second this one second this one second using the raid 1 array we ended up using the raid 1 array we ended up using the raid 1 array we ended up getting a true put of 40 over 3 getting a true put of 40 over 3 getting a true put of 40 over 3 megabytes which gives us a throughput of megabytes which gives us a throughput of megabytes which gives us a throughput of 13 and a third megabytes per second 13 and a third megabytes per second 13 and a third megabytes per second because in one second we managed to because in one second we managed to because in one second we managed to access 40 over 3 megabytes so the answer access 40 over 3 megabytes so the answer access 40 over 3 megabytes so the answer here is 13.33 note that the throughput here is 13.33 note that the throughput here is 13.33 note that the throughput is not 15 megabytes per second which is is not 15 megabytes per second which is is not 15 megabytes per second which is what we would get if we simply average what we would get if we simply average what we would get if we simply average the read throughput and the right true the read throughput and the right true the read throughput and the right true put we would get in this array because put we would get in this array because put we would get in this array because if we did this we are assuming that for if we did this we are assuming that for if we did this we are assuming that for half a second we are reading and then half a second we are reading and then half a second we are reading and then for half a second we are for half a second we are for half a second we are and thus we get 15 megabytes read and and thus we get 15 megabytes read and and thus we get 15 megabytes read and written in a second but if for half a written in a second but if for half a written in a second but if for half a second we read and for half a second we second we read and for half a second we second we read and for half a second we write we end up reading twice the amount write we end up reading twice the amount write we end up reading twice the amount of data that we would for writes so if of data that we would for writes so if of data that we would for writes so if the workload is 50/50 in terms of number the workload is 50/50 in terms of number the workload is 50/50 in terms of number of accesses then it's not also 50/50 in of accesses then it's not also 50/50 in of accesses then it's not also 50/50 in terms of time spent on reads and time terms of time spent on reads and time terms of time spent on reads and time spent on writes reads are faster thus we spent on writes reads are faster thus we spent on writes reads are faster thus we spend less time on them which is why spend less time on them which is why spend less time on them which is why again the throughput is not 15 megabytes again the throughput is not 15 megabytes again the throughput is not 15 megabytes per second and finally let us figure out per second and finally let us figure out per second and finally let us figure out the MTTF for the entire raid 1 array the the MTTF for the entire raid 1 array the the MTTF for the entire raid 1 array the MTTF of a whole raid 1 array with two MTTF of a whole raid 1 array with two MTTF of a whole raid 1 array with two disks we said is equal to the MTTF of a disks we said is equal to the MTTF of a disks we said is equal to the MTTF of a single disk divided by 2 so this is how single disk divided by 2 so this is how single disk divided by 2 so this is how long we have to operate with two disks long we have to operate with two disks long we have to operate with two disks until one of the disks fails and now until one of the disks fails and now until one of the disks fails and now each time we have a one disk failure we each time we have a one disk failure we each time we have a one disk failure we need to repair it and we said that the need to repair it and we said that the need to repair it and we said that the number of times we can try a repair number of times we can try a repair number of times we can try a repair until we finally experience a failure of until we finally experience a failure of until we finally experience a failure of the second disk during the repair of the the second disk during the repair of the the second disk during the repair of the first disk is empty the F of one disk first disk is empty the F of one disk first disk is empty the F of one disk divided by the MTTR of the disk so this divided by the MTTR of the disk so this divided by the MTTR of the disk so this first factor would be 50,000 hours the first factor would be 50,000 hours the first factor would be 50,000 hours the second factor is the ratio of the time second factor is the ratio of the time second factor is the ratio of the time to failure and the time to repair and to failure and the time to repair and to failure and the time to repair and when we divide these two we get four when we divide these two we get four when we divide these two we get four thousand one hundred sixty six point six thousand one hundred sixty six point six thousand one hundred sixty six point six six six etc so this is how many times six six etc so this is how many times six six etc so this is how many times can we try a repair while a single disk can we try a repair while a single disk can we try a repair while a single disk is working until we finally experience a is working until we finally experience a is working until we finally experience a repair in which the working disk also repair in which the working disk also repair in which the working disk also fails and when we multiply this out we fails and when we multiply this out we fails and when we multiply this out we get 208 million three hundred 33333 get 208 million three hundred 33333 get 208 million three hundred 33333 hours so this is a huge amount of time hours so this is a huge amount of time hours so this is a huge amount of time compared to the original disks mdtf compared to the original disks mdtf compared to the original disks mdtf as you can see rate one dramatically as you can see rate one dramatically as you can see rate one dramatically improves reliability this time is about improves reliability this time is about improves reliability this time is about 11 years this time is about 24,000 years 11 years this time is about 24,000 years 11 years this time is about 24,000 years so as you can see the reliability has so as you can see the reliability has so as you can see the reliability has improved from being able to expect data improved from being able to expect data improved from being able to expect data loss every about ten years to something loss every about ten years to something loss every about ten years to something that goes into tens of thousands of that goes into tens of thousands of that goes into tens of thousands of years

## Raid 1 Reliability： Part 5
Kind: captions Language: en but what about the reliability and the but what about the reliability and the but what about the reliability and the MTTF MTTF MTTF of red one same as before we will assume of red one same as before we will assume of red one same as before we will assume that some F is the failure rate for a that some F is the failure rate for a that some F is the failure rate for a single disk in failures per disk per single disk in failures per disk per single disk in failures per disk per second and for a single disk remember second and for a single disk remember second and for a single disk remember that the MTTF is simply 1 over F where F that the MTTF is simply 1 over F where F that the MTTF is simply 1 over F where F is that failure rate and then the mean is that failure rate and then the mean is that failure rate and then the mean time to data loss which is the MTTF time to data loss which is the MTTF time to data loss which is the MTTF really of the whole disk array in this really of the whole disk array in this really of the whole disk array in this case just one disk would be simply the case just one disk would be simply the case just one disk would be simply the MTTF of that one disk well let's say now MTTF of that one disk well let's say now MTTF of that one disk well let's say now that we have two disks in raid 1 and that we have two disks in raid 1 and that we have two disks in raid 1 and remember that the failure rate when we remember that the failure rate when we remember that the failure rate when we have more than one disk is n times the have more than one disk is n times the have more than one disk is n times the failure rate of one disk as long as all failure rate of one disk as long as all failure rate of one disk as long as all of the disks are working so what we have of the disks are working so what we have of the disks are working so what we have really is that both of our disks in raid really is that both of our disks in raid really is that both of our disks in raid 1 are ok until the time MTTF of one disk 1 are ok until the time MTTF of one disk 1 are ok until the time MTTF of one disk over 2 this is the expected time when over 2 this is the expected time when over 2 this is the expected time when one of the disks will fail now unlike one of the disks will fail now unlike one of the disks will fail now unlike raid 0 where this is the point where it raid 0 where this is the point where it raid 0 where this is the point where it would lose data now we have mirroring so would lose data now we have mirroring so would lose data now we have mirroring so both of the disks need to fail in order both of the disks need to fail in order both of the disks need to fail in order for the whole thing to lose data one for the whole thing to lose data one for the whole thing to lose data one failure is not data loss yet so we don't failure is not data loss yet so we don't failure is not data loss yet so we don't have the failure of the array yet but at have the failure of the array yet but at have the failure of the array yet but at this time we are down to one disk that this time we are down to one disk that this time we are down to one disk that one disk is a working disk and has this one disk is a working disk and has this one disk is a working disk and has this failure rate so that one remaining this failure rate so that one remaining this failure rate so that one remaining this will live on for another MTTF of a will live on for another MTTF of a will live on for another MTTF of a single disk this is very important to single disk this is very important to single disk this is very important to understand if we have a working disk at understand if we have a working disk at understand if we have a working disk at some point in time the MTTF for that some point in time the MTTF for that some point in time the MTTF for that disk is going to be the overall MTTF for disk is going to be the overall MTTF for disk is going to be the overall MTTF for one disk this MTTF is not affected by one disk this MTTF is not affected by one disk this MTTF is not affected by the fact that this disk has already been the fact that this disk has already been the fact that this disk has already been working for all this time why well working for all this time why well working for all this time why well because the failure rate is constant because the failure rate is constant because the failure rate is constant it's not affected by time so if we have it's not affected by time so if we have it's not affected by time so if we have a working disk its failure rate is a working disk its failure rate is a working disk its failure rate is always the same the only thing that always the same the only thing that always the same the only thing that changes the failure rate is if the disk changes the failure rate is if the disk changes the failure rate is if the disk fails so if we had two disks and both fails so if we had two disks and both fails so if we had two disks and both work this is the expected time of them work this is the expected time of them work this is the expected time of them surviving and then if we're down to one surviving and then if we're down to one surviving and then if we're down to one this that works at the time when we have this that works at the time when we have this that works at the time when we have a single working disk this is the MTTF a single working disk this is the MTTF a single working disk this is the MTTF that we can expect so overall the mean that we can expect so overall the mean that we can expect so overall the mean time to data loss for raid one with two time to data loss for raid one with two time to data loss for raid one with two disks is going to be the MTTF of a disks is going to be the MTTF of a disks is going to be the MTTF of a single disk over two plus the MTTF of a single disk over two plus the MTTF of a single disk over two plus the MTTF of a single disk and now we bought two disks single disk and now we bought two disks single disk and now we bought two disks and use them in an array that has the and use them in an array that has the and use them in an array that has the capacity of only a single disk so capacity of only a single disk so capacity of only a single disk so basically paid for the extra disk just basically paid for the extra disk just basically paid for the extra disk just to get reliability and what we get is to get reliability and what we get is to get reliability and what we get is just one half more of the MTTF that we just one half more of the MTTF that we just one half more of the MTTF that we would normally get with only a single would normally get with only a single would normally get with only a single disk so what's the point the point is disk so what's the point the point is disk so what's the point the point is that this calculation assumes that no that this calculation assumes that no that this calculation assumes that no disk is replaced at the time when it disk is replaced at the time when it disk is replaced at the time when it fails so we had a failure and we just fails so we had a failure and we just fails so we had a failure and we just let that fail disk be there and we were let that fail disk be there and we were let that fail disk be there and we were down to one disk and didn't fix it and down to one disk and didn't fix it and down to one disk and didn't fix it and then we just were down to the MTTF of then we just were down to the MTTF of then we just were down to the MTTF of one disk for a while but remember that one disk for a while but remember that one disk for a while but remember that when we want reliability we definitely when we want reliability we definitely when we want reliability we definitely will replace the failed disks so when we will replace the failed disks so when we will replace the failed disks so when we have the first failure we will as soon have the first failure we will as soon have the first failure we will as soon as possible replace that fail disk copy as possible replace that fail disk copy as possible replace that fail disk copy the data from the working disk to it so the data from the working disk to it so the data from the working disk to it so now we are back to raid 1 working now we are back to raid 1 working now we are back to raid 1 working properly properly properly so what's the MTTF for that

## Raid 1 Reliability if Failed Disks Replaced： Part 5
Kind: captions Language: en so what's raid 1 reliability if we do so what's raid 1 reliability if we do so what's raid 1 reliability if we do replace failed disks as soon as possible replace failed disks as soon as possible replace failed disks as soon as possible we have the situation that both disks we have the situation that both disks we have the situation that both disks are ok are ok are ok for this amount of time then a disk for this amount of time then a disk for this amount of time then a disk fails and then we have only one ok disk fails and then we have only one ok disk fails and then we have only one ok disk for the mean time to repair for one disk for the mean time to repair for one disk for the mean time to repair for one disk so this is how long does it take us to so this is how long does it take us to so this is how long does it take us to replace the disk replace the disk replace the disk after that after that after that both disks are okay again and then we both disks are okay again and then we both disks are okay again and then we again have this long until the next again have this long until the next again have this long until the next failure so what's the mean time to data failure so what's the mean time to data failure so what's the mean time to data loss for raid 1 with two disks loss for raid 1 with two disks loss for raid 1 with two disks the question really is the question really is the question really is how many times can we have this until we how many times can we have this until we how many times can we have this until we encounter the period when a single disk encounter the period when a single disk encounter the period when a single disk has failed and during the time it takes has failed and during the time it takes has failed and during the time it takes to replace it to replace it to replace it another disk fails another disk fails another disk fails well it's equal to the length of this well it's equal to the length of this well it's equal to the length of this interval interval interval this is how long we survive with both this is how long we survive with both this is how long we survive with both disks working times how many times can disks working times how many times can disks working times how many times can we get this until we encounter a repair we get this until we encounter a repair we get this until we encounter a repair period where the second disk fails period where the second disk fails period where the second disk fails during it during it during it so the question is really what's the so the question is really what's the so the question is really what's the probability of the second disk failing probability of the second disk failing probability of the second disk failing during the time to repair one disk during the time to repair one disk during the time to repair one disk now normally to calculate this now normally to calculate this now normally to calculate this probability we would have to compute the probability we would have to compute the probability we would have to compute the distribution for distribution for distribution for this function and then see what's the this function and then see what's the this function and then see what's the probability for this distribution being probability for this distribution being probability for this distribution being under empty tr1 and so on but under empty tr1 and so on but under empty tr1 and so on but when when when the time to repair is a lot smaller than the time to repair is a lot smaller than the time to repair is a lot smaller than the time to failure of a single disk the time to failure of a single disk the time to failure of a single disk then we can approximate this probability then we can approximate this probability then we can approximate this probability of the second disk failing during the of the second disk failing during the of the second disk failing during the mttr for the first disk mttr for the first disk mttr for the first disk simply as simply as simply as the mttr divided by the mttf so for the mttr divided by the mttf so for the mttr divided by the mttf so for example if the example if the example if the mttr is let's say a single day and mttf mttr is let's say a single day and mttf mttr is let's say a single day and mttf is a hundred days is a hundred days is a hundred days then the probability of the second then the probability of the second then the probability of the second failure during the repair period is failure during the repair period is failure during the repair period is simply the length of the repair period simply the length of the repair period simply the length of the repair period divided by the period divided by the period divided by the period that we expected this to survive so for that we expected this to survive so for that we expected this to survive so for example if the time to repair is for example if the time to repair is for example if the time to repair is for example one day example one day example one day and the time to failure is let's say a and the time to failure is let's say a and the time to failure is let's say a hundred days then we can expect that hundred days then we can expect that hundred days then we can expect that there is a one in a hundred chance of a there is a one in a hundred chance of a there is a one in a hundred chance of a disk failing disk failing disk failing during the one day during the one day during the one day so now how many times so now how many times so now how many times can we repair the disk before we can we repair the disk before we can we repair the disk before we encounter one of these well encounter one of these well encounter one of these well the number of times we can try this be the number of times we can try this be the number of times we can try this be until we encounter a failure is going to until we encounter a failure is going to until we encounter a failure is going to be 1 over this probability be 1 over this probability be 1 over this probability so what we finally get is this so what we finally get is this so what we finally get is this the mean time to data loss is mttf of a the mean time to data loss is mttf of a the mean time to data loss is mttf of a single disk squared divided by two times single disk squared divided by two times single disk squared divided by two times the mean time to repair for one disk how the mean time to repair for one disk how the mean time to repair for one disk how long does it take us to replace the disk long does it take us to replace the disk long does it take us to replace the disk and copy the data on it so that now we and copy the data on it so that now we and copy the data on it so that now we have a fully functional raid1 array and have a fully functional raid1 array and have a fully functional raid1 array and as a reminder this is how long as a reminder this is how long as a reminder this is how long do we get a working array with both do we get a working array with both do we get a working array with both disks functional and this is how many disks functional and this is how many disks functional and this is how many times do we do a repair until we can times do we do a repair until we can times do we do a repair until we can expect that the second disk expect that the second disk expect that the second disk that is covering us during the repair is that is covering us during the repair is that is covering us during the repair is going to fail on us going to fail on us going to fail on us note that because the mttf note that because the mttf note that because the mttf is much larger than the mttr is much larger than the mttr is much larger than the mttr this is measured in years usually this this is measured in years usually this this is measured in years usually this hopefully is hours or days it's just how hopefully is hours or days it's just how hopefully is hours or days it's just how long does it take us to replace a disk long does it take us to replace a disk long does it take us to replace a disk this factor here is very very large and this factor here is very very large and this factor here is very very large and now we get many many times now we get many many times now we get many many times the mttf of a single disk over two this the mttf of a single disk over two this the mttf of a single disk over two this factor is way larger than two which factor is way larger than two which factor is way larger than two which means that the time to data loss with means that the time to data loss with means that the time to data loss with the raid one array with only two disks the raid one array with only two disks the raid one array with only two disks is way higher than the time to data loss is way higher than the time to data loss is way higher than the time to data loss for a single disk for a single disk for a single disk which means that raid 1 is dramatically which means that raid 1 is dramatically which means that raid 1 is dramatically improving reliability

## RAID 1 Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out a raid so let's see if we can figure out a raid so let's see if we can figure out a raid 1 array suppose we have a raid 1 array 1 array suppose we have a raid 1 array 1 array suppose we have a raid 1 array with two disks and for a single disk we with two disks and for a single disk we with two disks and for a single disk we have a 200 gigabyte capacity 10 have a 200 gigabyte capacity 10 have a 200 gigabyte capacity 10 megabytes per second throughput and the megabytes per second throughput and the megabytes per second throughput and the MTTF of a hundred thousand hours in that MTTF of a hundred thousand hours in that MTTF of a hundred thousand hours in that case our two disk raid 1 array has a case our two disk raid 1 array has a case our two disk raid 1 array has a data capacity of how many gigabytes a data capacity of how many gigabytes a data capacity of how many gigabytes a throughput of how many megabytes per throughput of how many megabytes per throughput of how many megabytes per second second second assuming that we do equal numbers of assuming that we do equal numbers of assuming that we do equal numbers of reads and writes reads and writes reads and writes and finally our MTTF is equal to how and finally our MTTF is equal to how and finally our MTTF is equal to how many hours and for this type of array we many hours and for this type of array we many hours and for this type of array we also need to know whether we replace also need to know whether we replace also need to know whether we replace failed disks or not failed disks or not failed disks or not so here assume that we do replace the so here assume that we do replace the so here assume that we do replace the failed disk and at the mean time to failed disk and at the mean time to failed disk and at the mean time to repair meaning the mean time it takes us repair meaning the mean time it takes us repair meaning the mean time it takes us to replace the failed disk is 24 hours


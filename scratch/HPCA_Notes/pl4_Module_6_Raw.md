# Playlist 4 Module 6 Raw Transcript

## Cache Incoherence Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our cache let's look at the solution to our cache let's look at the solution to our cache incoherence quiz we had three caches and incoherence quiz we had three caches and incoherence quiz we had three caches and three cores a location that starts at three cores a location that starts at three cores a location that starts at zero and then each core reads increments zero and then each core reads increments zero and then each core reads increments and then writes to that memory location and then writes to that memory location and then writes to that memory location so the question is what's the final so the question is what's the final so the question is what's the final value we write here the correct behavior value we write here the correct behavior value we write here the correct behavior would be four cores zero to do this and would be four cores zero to do this and would be four cores zero to do this and then it replaces this from its cache so then it replaces this from its cache so then it replaces this from its cache so that core 1 reads a 1 here increments that core 1 reads a 1 here increments that core 1 reads a 1 here increments that writes a 2 and then core 2 reads that writes a 2 and then core 2 reads that writes a 2 and then core 2 reads that increments and writes a 3 so this 3 that increments and writes a 3 so this 3 that increments and writes a 3 so this 3 here is a possible answer if core 0 here here is a possible answer if core 0 here here is a possible answer if core 0 here happens to replace this block that happens to replace this block that happens to replace this block that contains a from its cash before core 1 contains a from its cash before core 1 contains a from its cash before core 1 tries to read it and then core 1 tries to read it and then core 1 tries to read it and then core 1 replaces before core to reads it and so replaces before core to reads it and so replaces before core to reads it and so on in that case we get actually a 3 here on in that case we get actually a 3 here on in that case we get actually a 3 here which is the correct answer so let's see which is the correct answer so let's see which is the correct answer so let's see now if we can get to well if core 0 does now if we can get to well if core 0 does now if we can get to well if core 0 does a replacement so the memory location a replacement so the memory location a replacement so the memory location after this actually contains a 1 then after this actually contains a 1 then after this actually contains a 1 then core 1 does this but doesn't replace the core 1 does this but doesn't replace the core 1 does this but doesn't replace the block from the cache then core to will block from the cache then core to will block from the cache then core to will still see a 1 it will not see what core still see a 1 it will not see what core still see a 1 it will not see what core 1 produced because core one keeps it in 1 produced because core one keeps it in 1 produced because core one keeps it in its own cache so core to here reads 1 its own cache so core to here reads 1 its own cache so core to here reads 1 increments it and then core 2 ends up increments it and then core 2 ends up increments it and then core 2 ends up writing a 2 so this is a possible answer writing a 2 so this is a possible answer writing a 2 so this is a possible answer and then we can get a 1 here by and then we can get a 1 here by and then we can get a 1 here by basically core 0 delaying the basically core 0 delaying the basically core 0 delaying the replacement and then core 1 can fetch a replacement and then core 1 can fetch a replacement and then core 1 can fetch a 0 as well put a 1 there but not replace 0 as well put a 1 there but not replace 0 as well put a 1 there but not replace until later and that means that when until later and that means that when until later and that means that when core 2 reads this it gets a 0 and then core 2 reads this it gets a 0 and then core 2 reads this it gets a 0 and then puts a 1 here so this is also possible puts a 1 here so this is also possible puts a 1 here so this is also possible it is not possible for core 2 to write a it is not possible for core 2 to write a it is not possible for core 2 to write a 0 because whatever it reads it will 0 because whatever it reads it will 0 because whatever it reads it will increment and write increment and write increment and write so for Core 2 to produce a 0 here it so for Core 2 to produce a 0 here it so for Core 2 to produce a 0 here it would need to read minus 1 note that would need to read minus 1 note that would need to read minus 1 note that each of these cores is still behaving as each of these cores is still behaving as each of these cores is still behaving as a correct uniprocessor so 0 is not a correct uniprocessor so 0 is not a correct uniprocessor so 0 is not possible possible possible similarly for is not possible because similarly for is not possible because similarly for is not possible because for 4 to be produced here we need to for 4 to be produced here we need to for 4 to be produced here we need to read a tree and there is just no way read a tree and there is just no way read a tree and there is just no way that to increase that to increase that to increase from zero can produce a three here and from zero can produce a three here and from zero can produce a three here and similarly more than four is not possible similarly more than four is not possible similarly more than four is not possible so the correct answers here are 1 2 &amp; 3 so the correct answers here are 1 2 &amp; 3 so the correct answers here are 1 2 &amp; 3 are possible at this point

## Coherence Definition： Part 5
Kind: captions Language: en so let's now Define what does it mean so let's now Define what does it mean so let's now Define what does it mean for caches to be coherent intuitively for caches to be coherent intuitively for caches to be coherent intuitively they're coherent when the whole thing they're coherent when the whole thing they're coherent when the whole thing behaves like there was no caches but behaves like there was no caches but behaves like there was no caches but let's define more precisely what exactly let's define more precisely what exactly let's define more precisely what exactly does it mean there are three does it mean there are three does it mean there are three requirements for coherence the first one requirements for coherence the first one requirements for coherence the first one says that the read let's call that read says that the read let's call that read says that the read let's call that read R from some address X on some core C1 R from some address X on some core C1 R from some address X on some core C1 will return the value written by the will return the value written by the will return the value written by the most recent WR let's call that right W most recent WR let's call that right W most recent WR let's call that right W to the same location and that right was to the same location and that right was to the same location and that right was on the same core C1 if no other core has on the same core C1 if no other core has on the same core C1 if no other core has written to that location between the written to that location between the written to that location between the axises W and R so this first part of the axises W and R so this first part of the axises W and R so this first part of the coherence definition really says that if coherence definition really says that if coherence definition really says that if one core is operating on a location one core is operating on a location one core is operating on a location alone then its reads should be getting alone then its reads should be getting alone then its reads should be getting the most recent rides from the same core the most recent rides from the same core the most recent rides from the same core this basically says that a coherent this basically says that a coherent this basically says that a coherent Behavior includes correct unit processor Behavior includes correct unit processor Behavior includes correct unit processor Behavior as long as the location is Behavior as long as the location is Behavior as long as the location is accessed by only one core the second accessed by only one core the second accessed by only one core the second part of the definition says that if core part of the definition says that if core part of the definition says that if core C1 wres to allocation X and C2 reads X C1 wres to allocation X and C2 reads X C1 wres to allocation X and C2 reads X after a sufficient time and if there are after a sufficient time and if there are after a sufficient time and if there are no other rights that happen in between no other rights that happen in between no other rights that happen in between c1s right and c2's read then c2's read c1s right and c2's read then c2's read c1s right and c2's read then c2's read must return the value C1 wrote so pretty must return the value C1 wrote so pretty must return the value C1 wrote so pretty much if there is a right and the read much if there is a right and the read much if there is a right and the read happens sufficiently after that right happens sufficiently after that right happens sufficiently after that right then the read has to read what the right then the read has to read what the right then the read has to read what the right wrote except if there was another right wrote except if there was another right wrote except if there was another right in between in which case the read is in between in which case the read is in between in which case the read is allowed to return that value instead of allowed to return that value instead of allowed to return that value instead of this one so this part of the definition this one so this part of the definition this one so this part of the definition really prevents the case when for really prevents the case when for really prevents the case when for example C1 writes something to its own example C1 writes something to its own example C1 writes something to its own cache C2 reads forever from its cache a cache C2 reads forever from its cache a cache C2 reads forever from its cache a stale value to be stale value to be stale value to be coherent if C1 wres then after some coherent if C1 wres then after some coherent if C1 wres then after some sufficient time C2 must start reading sufficient time C2 must start reading sufficient time C2 must start reading the new value even if maybe a normal the new value even if maybe a normal the new value even if maybe a normal cache with no coherence support would cache with no coherence support would cache with no coherence support would just keep reading the stale value just keep reading the stale value just keep reading the stale value because it never gets replaced so we because it never gets replaced so we because it never gets replaced so we have to do something in order to make have to do something in order to make have to do something in order to make this part of the definition work for this part of the definition work for this part of the definition work for private caches that are caching data in private caches that are caching data in private caches that are caching data in a coherent system so we must do a coherent system so we must do a coherent system so we must do something to make these caches behave something to make these caches behave something to make these caches behave this way and finally the third part of this way and finally the third part of this way and finally the third part of the the definition says that if there is the the definition says that if there is the the definition says that if there is more than one right to the same location more than one right to the same location more than one right to the same location then these rights must be serialized then these rights must be serialized then these rights must be serialized that is any two rights to the same that is any two rights to the same that is any two rights to the same location must be seen to occur in the location must be seen to occur in the location must be seen to occur in the same order on all the cores what this same order on all the cores what this same order on all the cores what this part of the definition really says part of the definition really says part of the definition really says different cores should not be able to different cores should not be able to different cores should not be able to disagree about which right occurred disagree about which right occurred disagree about which right occurred first and which occurred second note first and which occurred second note first and which occurred second note that the second part of the definition that the second part of the definition that the second part of the definition doesn't really require this to happen doesn't really require this to happen doesn't really require this to happen let's say there was one right on C1 and let's say there was one right on C1 and let's say there was one right on C1 and another right on C3 when C2 reads it's another right on C3 when C2 reads it's another right on C3 when C2 reads it's allowed to return after a sufficient allowed to return after a sufficient allowed to return after a sufficient time either c1s or c3's value because time either c1s or c3's value because time either c1s or c3's value because it's allowed to interpret this order as it's allowed to interpret this order as it's allowed to interpret this order as either C1 and then C3 wrote or the other either C1 and then C3 wrote or the other either C1 and then C3 wrote or the other way around what this part of the way around what this part of the way around what this part of the definition says is that whatever C2 sees definition says is that whatever C2 sees definition says is that whatever C2 sees if C2 for example CES that the final if C2 for example CES that the final if C2 for example CES that the final value is that from C1 and not from C3 value is that from C1 and not from C3 value is that from C1 and not from C3 then all the other cores when they read then all the other cores when they read then all the other cores when they read that location after C2 need to also see that location after C2 need to also see that location after C2 need to also see that order so it cannot happen that C2 that order so it cannot happen that C2 that order so it cannot happen that C2 thinks C1 wrote last some other core thinks C1 wrote last some other core thinks C1 wrote last some other core sometime later reads and still sees c3's sometime later reads and still sees c3's sometime later reads and still sees c3's value so what this is really saying is value so what this is really saying is value so what this is really saying is that everybody including the writing that everybody including the writing that everybody including the writing course will see the value that is course will see the value that is course will see the value that is consistent with their being some consistent with their being some consistent with their being some particular order to the rights it particular order to the rights it particular order to the rights it doesn't require any particular order for doesn't require any particular order for doesn't require any particular order for example the rights can happen almost example the rights can happen almost example the rights can happen almost simultaneously in which case we are free simultaneously in which case we are free simultaneously in which case we are free to choose which happen first and which to choose which happen first and which to choose which happen first and which happen second but we cannot allow some happen second but we cannot allow some happen second but we cannot allow some cores to think that one right was first cores to think that one right was first cores to think that one right was first and other cores think that another right and other cores think that another right and other cores think that another right was first

## Coherence Definition Quiz： Part 5
Kind: captions Language: en so let's see if we understood the so let's see if we understood the so let's see if we understood the coherence definition coherence definition coherence definition let's say that we have two cores in a let's say that we have two cores in a let's say that we have two cores in a system that is supposed to be coherent system that is supposed to be coherent system that is supposed to be coherent let's say that core one executes this let's say that core one executes this let's say that core one executes this program program program it sets a to one it sets a to one it sets a to one then waits for a to change from 1 so then waits for a to change from 1 so then waits for a to change from 1 so it's going to loop here as long as a is it's going to loop here as long as a is it's going to loop here as long as a is equal to 1 sets a again to 1 and then equal to 1 sets a again to 1 and then equal to 1 sets a again to 1 and then prints done 1. at the same time when prints done 1. at the same time when prints done 1. at the same time when core 1 is started on this core 1 is started on this core 1 is started on this core 2 starts on another program that core 2 starts on another program that core 2 starts on another program that writes 0 to the same location a writes 0 to the same location a writes 0 to the same location a in shared memory waits until a changes in shared memory waits until a changes in shared memory waits until a changes from 0 from 0 from 0 sets a to 0 again sets a to 0 again sets a to 0 again then prints down 2 and remember that then prints down 2 and remember that then prints down 2 and remember that core one and core two core one and core two core one and core two need not necessarily execute these two need not necessarily execute these two need not necessarily execute these two programs in lock steps so it's not programs in lock steps so it's not programs in lock steps so it's not necessary that core one executes this necessary that core one executes this necessary that core one executes this while core two does this core one does while core two does this core one does while core two does this core one does this while core to this and so on it this while core to this and so on it this while core to this and so on it could be for example that core runs could be for example that core runs could be for example that core runs executes several of these while core 2 executes several of these while core 2 executes several of these while core 2 is still is still is still not even beginning not even beginning not even beginning so you can insert arbitrary delays in so you can insert arbitrary delays in so you can insert arbitrary delays in between any of the statements here and between any of the statements here and between any of the statements here and here the question for you is which of here the question for you is which of here the question for you is which of the following printouts are possible the following printouts are possible the following printouts are possible first core one prints done one then core first core one prints done one then core first core one prints done one then core two prints down two two prints down two two prints down two the next possibility first core two the next possibility first core two the next possibility first core two prints down two then core one prints prints down two then core one prints prints down two then core one prints done one the next option core one prints done one the next option core one prints done one the next option core one prints down one down one down one but core two but core two but core two gets stuck and never gets to print done gets stuck and never gets to print done gets stuck and never gets to print done two two two the next option the next option the next option is score one gets stuck and never gets is score one gets stuck and never gets is score one gets stuck and never gets to print down one but core two to print down one but core two to print down one but core two successfully prints done too successfully prints done too successfully prints done too and the final option and the final option and the final option is that nothing is printed because both is that nothing is printed because both is that nothing is printed because both of the cores get stuck so neither of of the cores get stuck so neither of of the cores get stuck so neither of them prints anything ever so which of them prints anything ever so which of them prints anything ever so which of these five these five these five options options options are actually possible are actually possible are actually possible when these two programs when these two programs when these two programs run in a coherent system

## Coherence Definition Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our coherence definition quiz where we had coherence definition quiz where we had coherence definition quiz where we had two cores running in a coherent system two cores running in a coherent system two cores running in a coherent system executing some code and the question was executing some code and the question was executing some code and the question was basically which of these five basically which of these five basically which of these five possibilities for what gets printed are possibilities for what gets printed are possibilities for what gets printed are possible let's see what can happen in possible let's see what can happen in possible let's see what can happen in these two programs if core one gets to these two programs if core one gets to these two programs if core one gets to go first with a equals one it's then go first with a equals one it's then go first with a equals one it's then going to spin here for as long as X going to spin here for as long as X going to spin here for as long as X equal one so it gets stuck until Core 2 equal one so it gets stuck until Core 2 equal one so it gets stuck until Core 2 does something eventually however core 2 does something eventually however core 2 does something eventually however core 2 we'll do a equals 0 in which case core 1 we'll do a equals 0 in which case core 1 we'll do a equals 0 in which case core 1 gets to continue and now core 1 we'll gets to continue and now core 1 we'll gets to continue and now core 1 we'll set a to 1 korto will wait here until a set a to 1 korto will wait here until a set a to 1 korto will wait here until a is set to 1 because as long as it's set is set to 1 because as long as it's set is set to 1 because as long as it's set to 0 the way it set it is going to get to 0 the way it set it is going to get to 0 the way it set it is going to get stuck here stuck here stuck here so what then happens is core 1 when it so what then happens is core 1 when it so what then happens is core 1 when it sets this release is called - now core 2 sets this release is called - now core 2 sets this release is called - now core 2 can do this and this first while core 1 can do this and this first while core 1 can do this and this first while core 1 is still being delayed here or it can be is still being delayed here or it can be is still being delayed here or it can be that core 1 gets here first and then that core 1 gets here first and then that core 1 gets here first and then core 2 gets here so definitely these two core 2 gets here so definitely these two core 2 gets here so definitely these two outcomes are possible outcomes are possible outcomes are possible they basically depend on after core 1 they basically depend on after core 1 they basically depend on after core 1 waits for Core 2 to release it and then waits for Core 2 to release it and then waits for Core 2 to release it and then it itself releases core to who gets to it itself releases core to who gets to it itself releases core to who gets to print first now let's see if it's print first now let's see if it's print first now let's see if it's possible for core 1 to never get to this possible for core 1 to never get to this possible for core 1 to never get to this point for that to happen core one needs point for that to happen core one needs point for that to happen core one needs to get stuck here in this while loop to get stuck here in this while loop to get stuck here in this while loop which means that a needs to stay 1 which means that a needs to stay 1 which means that a needs to stay 1 forever that can only occur if core 2 is forever that can only occur if core 2 is forever that can only occur if core 2 is done with its program and the resulting done with its program and the resulting done with its program and the resulting value of a is still one or if core to value of a is still one or if core to value of a is still one or if core to also get stuck here and the resulting also get stuck here and the resulting also get stuck here and the resulting value of a is still one so core 1 is value of a is still one so core 1 is value of a is still one so core 1 is stuck here and a is 1 if that happens stuck here and a is 1 if that happens stuck here and a is 1 if that happens before core 2 gets to a equals 0 then before core 2 gets to a equals 0 then before core 2 gets to a equals 0 then core 2 is gonna release core 1 so a core 2 is gonna release core 1 so a core 2 is gonna release core 1 so a equals 0 had to happen before this if a equals 0 had to happen before this if a equals 0 had to happen before this if a equals 0 happened before all of this equals 0 happened before all of this equals 0 happened before all of this then what happens is a equals 0 equals 1 then what happens is a equals 0 equals 1 then what happens is a equals 0 equals 1 now we're stuck waiting for 1 but core 2 now we're stuck waiting for 1 but core 2 now we're stuck waiting for 1 but core 2 will continue and it has will continue and it has will continue and it has to see the one because this is a to see the one because this is a to see the one because this is a coherent system eventually because coherent system eventually because coherent system eventually because equals one happened after equals zero equals one happened after equals zero equals one happened after equals zero Core 2 has to see the 1 and proceed once Core 2 has to see the 1 and proceed once Core 2 has to see the 1 and proceed once it proceeds is going to set a 2-0 core it proceeds is going to set a 2-0 core it proceeds is going to set a 2-0 core one has to see that because of the one has to see that because of the one has to see that because of the second part of the definition and second part of the definition and second part of the definition and proceed so it's not possible to get core proceed so it's not possible to get core proceed so it's not possible to get core 1 stuck here by just moving the a equals 1 stuck here by just moving the a equals 1 stuck here by just moving the a equals 0 to the beginning now let's see if it's 0 to the beginning now let's see if it's 0 to the beginning now let's see if it's possible for Core 2 to finish the whole possible for Core 2 to finish the whole possible for Core 2 to finish the whole thing before this happens in which case thing before this happens in which case thing before this happens in which case core 1 will get stuck here but that's core 1 will get stuck here but that's core 1 will get stuck here but that's not possible because core 2 now needs to not possible because core 2 now needs to not possible because core 2 now needs to execute this whole program here before execute this whole program here before execute this whole program here before core 1 even begins that's not possible core 1 even begins that's not possible core 1 even begins that's not possible because core 2 sets a to 0 and then because core 2 sets a to 0 and then because core 2 sets a to 0 and then waits for it to become nonzero so core waits for it to become nonzero so core waits for it to become nonzero so core to cannot finish the whole thing until to cannot finish the whole thing until to cannot finish the whole thing until core 1 does at least this at which point core 1 does at least this at which point core 1 does at least this at which point we are back to the first case of pretty we are back to the first case of pretty we are back to the first case of pretty much core 1 will eventually see that the much core 1 will eventually see that the much core 1 will eventually see that the a is 0 because core to set it that way a is 0 because core to set it that way a is 0 because core to set it that way after a became 1 after a became 1 after a became 1 it's also important to notice that it's it's also important to notice that it's it's also important to notice that it's not possible for for example equals 1 not possible for for example equals 1 not possible for for example equals 1 and a equals 2 to happen one after the and a equals 2 to happen one after the and a equals 2 to happen one after the other and then core one thinks that this other and then core one thinks that this other and then core one thinks that this was the last right so it gets stuck here was the last right so it gets stuck here was the last right so it gets stuck here while core 2 thinks this was the last while core 2 thinks this was the last while core 2 thinks this was the last right and then gets stuck here because right and then gets stuck here because right and then gets stuck here because the third part of the coherence the third part of the coherence the third part of the coherence definition says that both of them have definition says that both of them have definition says that both of them have to think that the same axis was the last to think that the same axis was the last to think that the same axis was the last one so if they agree that this was the one so if they agree that this was the one so if they agree that this was the last one then this core gets released last one then this core gets released last one then this core gets released and then ends up releasing the first one and then ends up releasing the first one and then ends up releasing the first one if they think this was the last access if they think this was the last access if they think this was the last access then this core gets to be released and then this core gets to be released and then this core gets to be released and releases the second one so either way releases the second one so either way releases the second one so either way it's not possible to get any of the 3 it's not possible to get any of the 3 it's not possible to get any of the 3 printouts in an incoherent system it can printouts in an incoherent system it can printouts in an incoherent system it can easily happen that you get these or any easily happen that you get these or any easily happen that you get these or any of these other three so for example of these other three so for example of these other three so for example nothing can be printed then by simply nothing can be printed then by simply nothing can be printed then by simply storing one to the cache and then never storing one to the cache and then never storing one to the cache and then never really checking anything else just really checking anything else just really checking anything else just looping on the value in the cache here looping on the value in the cache here looping on the value in the cache here meanwhile this one stores another value meanwhile this one stores another value meanwhile this one stores another value in its own cache then just infinitely in its own cache then just infinitely in its own cache then just infinitely space space space on it and then you can get the other two on it and then you can get the other two on it and then you can get the other two possibilities here by simply one of them possibilities here by simply one of them possibilities here by simply one of them not being entirely incoherent so as you not being entirely incoherent so as you not being entirely incoherent so as you can see a coherent system prevents some can see a coherent system prevents some can see a coherent system prevents some of the behaviors that are possible of the behaviors that are possible of the behaviors that are possible in an incoherent system but all the in an incoherent system but all the in an incoherent system but all the behaviors that are possible in the behaviors that are possible in the behaviors that are possible in the coherent system are also possible in an coherent system are also possible in an coherent system are also possible in an incoherent one so coherence is allowed incoherent one so coherence is allowed incoherent one so coherence is allowed in an incoherent system is just that in an incoherent system is just that in an incoherent system is just that incoherent systems don't guarantee that incoherent systems don't guarantee that incoherent systems don't guarantee that all their behaviors are going to be all their behaviors are going to be all their behaviors are going to be coherent

## How to Get Coherence： Part 5
Kind: captions Language: en okay so we have seen that we want okay so we have seen that we want okay so we have seen that we want coherence and now the question is how do coherence and now the question is how do coherence and now the question is how do we get it so how do we make caches we get it so how do we make caches we get it so how do we make caches become coherent the first option is become coherent the first option is become coherent the first option is don't do caching but this is really bad don't do caching but this is really bad don't do caching but this is really bad for performance so we will not seriously for performance so we will not seriously for performance so we will not seriously consider this but if there are no caches consider this but if there are no caches consider this but if there are no caches then all of the axises go to memory and then all of the axises go to memory and then all of the axises go to memory and then rights to that memory will be seen then rights to that memory will be seen then rights to that memory will be seen by the reads to that memory and also the by the reads to that memory and also the by the reads to that memory and also the order in which the memory receives the order in which the memory receives the order in which the memory receives the rights is the order in which the rights rights is the order in which the rights rights is the order in which the rights will be seen by all the court so the will be seen by all the court so the will be seen by all the court so the system will be coherent the second system will be coherent the second system will be coherent the second option is if all cores share the same option is if all cores share the same option is if all cores share the same level one cache then the same argument level one cache then the same argument level one cache then the same argument for coherence supplies the order of for coherence supplies the order of for coherence supplies the order of Rights is the order seen by this level Rights is the order seen by this level Rights is the order seen by this level one one one cache and all of the accesses are going cache and all of the accesses are going cache and all of the accesses are going to go to the same cache where they see to go to the same cache where they see to go to the same cache where they see the same version of the block so the the same version of the block so the the same version of the block so the system will be coherent but will still system will be coherent but will still system will be coherent but will still have really bad performance better than have really bad performance better than have really bad performance better than this but not by much now we can try to this but not by much now we can try to this but not by much now we can try to do things like having private caches but do things like having private caches but do things like having private caches but make them right through in that case the make them right through in that case the make them right through in that case the memory is actually seeing all the rights memory is actually seeing all the rights memory is actually seeing all the rights but the problem is that these caches are but the problem is that these caches are but the problem is that these caches are still allowed to cache data that has still allowed to cache data that has still allowed to cache data that has only been read forever so once we read a only been read forever so once we read a only been read forever so once we read a value it doesn't matter that all the value it doesn't matter that all the value it doesn't matter that all the other cores are writing their values other cores are writing their values other cores are writing their values through to memory because the cache that through to memory because the cache that through to memory because the cache that only reads will potentially see forever only reads will potentially see forever only reads will potentially see forever the same stale value even after many the same stale value even after many the same stale value even after many many rights have occurred and for a long many rights have occurred and for a long many rights have occurred and for a long time so this is really not a way to get time so this is really not a way to get time so this is really not a way to get coherence even with write through caches coherence even with write through caches coherence even with write through caches private caches are not necessarily private caches are not necessarily private caches are not necessarily coherent what we must do really is to coherent what we must do really is to coherent what we must do really is to force reads made to one cash to see the force reads made to one cash to see the force reads made to one cash to see the rights made in another so we cannot rights made in another so we cannot rights made in another so we cannot allow cashes to Simply provide the same allow cashes to Simply provide the same allow cashes to Simply provide the same value over and over once it's a cash hit value over and over once it's a cash hit value over and over once it's a cash hit somehow once a right occurs in one cash somehow once a right occurs in one cash somehow once a right occurs in one cash another cash needs to eventually start another cash needs to eventually start another cash needs to eventually start returning that value to its local returning that value to its local returning that value to its local processor so how do we do that one way processor so how do we do that one way processor so how do we do that one way is for every right to be sent to all the is for every right to be sent to all the is for every right to be sent to all the other caches that might potentially have other caches that might potentially have other caches that might potentially have this block and they need to update their this block and they need to update their this block and they need to update their value so pretty much I write to my cach value so pretty much I write to my cach value so pretty much I write to my cach it's kind of like a write through cach it's kind of like a write through cach it's kind of like a write through cach in that it continues past the cache but in that it continues past the cache but in that it continues past the cache but now what really happens to it is not now what really happens to it is not now what really happens to it is not only does it get written back to memory only does it get written back to memory only does it get written back to memory it also get sent and seen by all the it also get sent and seen by all the it also get sent and seen by all the other caches which now take that new other caches which now take that new other caches which now take that new value if that that happens then when I value if that that happens then when I value if that that happens then when I write other caches now have my new value write other caches now have my new value write other caches now have my new value and then reads on those scores will and then reads on those scores will and then reads on those scores will actually provide the same value this is actually provide the same value this is actually provide the same value this is called Write update coherence because called Write update coherence because called Write update coherence because wres update all of the other cach copies wres update all of the other cach copies wres update all of the other cach copies the other approach for maintaining the other approach for maintaining the other approach for maintaining coherence is for rights to coherence is for rights to coherence is for rights to prevent other copies of the block from prevent other copies of the block from prevent other copies of the block from having hits so after a write to one having hits so after a write to one having hits so after a write to one cache all of the caches that had a copy cache all of the caches that had a copy cache all of the caches that had a copy of this block need need to behave like of this block need need to behave like of this block need need to behave like they're having a miss or they need to they're having a miss or they need to they're having a miss or they need to behave like the block is not valid in behave like the block is not valid in behave like the block is not valid in that cache so this is called Write that cache so this is called Write that cache so this is called Write invalidate a write will invalidate all invalidate a write will invalidate all invalidate a write will invalidate all the other copies note that these are the other copies note that these are the other copies note that these are just the two general approaches we will just the two general approaches we will just the two general approaches we will be looking at several right invalidate be looking at several right invalidate be looking at several right invalidate coherence protocols and also we will be coherence protocols and also we will be coherence protocols and also we will be considering a right update protocol so considering a right update protocol so considering a right update protocol so this ensures really that reads see the this ensures really that reads see the this ensures really that reads see the values produced by wres values produced by wres values produced by wres so the coherence property two is so the coherence property two is so the coherence property two is maintained by either this or this so we maintained by either this or this so we maintained by either this or this so we need to pick one of these two approaches need to pick one of these two approaches need to pick one of these two approaches to maintain coherence property two for to maintain coherence property two for to maintain coherence property two for coherence property 3 we need to decide coherence property 3 we need to decide coherence property 3 we need to decide what's the order of Rights and all the what's the order of Rights and all the what's the order of Rights and all the cores need to see the same order one way cores need to see the same order one way cores need to see the same order one way to enforce the same order of Rights to enforce the same order of Rights to enforce the same order of Rights being seen by all the cores is that all being seen by all the cores is that all being seen by all the cores is that all rights are broadcast on the shared bus rights are broadcast on the shared bus rights are broadcast on the shared bus and then the order in which they appear and then the order in which they appear and then the order in which they appear in this shared bus is the order that in this shared bus is the order that in this shared bus is the order that should be seen by everybody coherence should be seen by everybody coherence should be seen by everybody coherence done in this way is called snooping done in this way is called snooping done in this way is called snooping because when rights are broadcast on the because when rights are broadcast on the because when rights are broadcast on the bus everybody snoops those rights in bus everybody snoops those rights in bus everybody snoops those rights in order to tell what order they occurred order to tell what order they occurred order to tell what order they occurred on and also to update or invalidate on and also to update or invalidate on and also to update or invalidate their values in the caches of course the their values in the caches of course the their values in the caches of course the shared bus can now become a bottleneck shared bus can now become a bottleneck shared bus can now become a bottleneck the other approach is that each block is the other approach is that each block is the other approach is that each block is assigned an ordering point and different assigned an ordering point and different assigned an ordering point and different ordering points can be used for ordering points can be used for ordering points can be used for different blocks so that for each block different blocks so that for each block different blocks so that for each block all axess Es are ordered by the same all axess Es are ordered by the same all axess Es are ordered by the same entity but different blocks use entity but different blocks use entity but different blocks use different entities so they don't have different entities so they don't have different entities so they don't have contention this ordering point is called contention this ordering point is called contention this ordering point is called directory when doing coherence so this directory when doing coherence so this directory when doing coherence so this family of approaches is called a family of approaches is called a family of approaches is called a directory based coherence and this directory based coherence and this directory based coherence and this directory both serves as a ordering directory both serves as a ordering directory both serves as a ordering point for a block and also figures out point for a block and also figures out point for a block and also figures out who to update or invalidate when who to update or invalidate when who to update or invalidate when multiple caches have a version of the multiple caches have a version of the multiple caches have a version of the same block and a right occurs so among same block and a right occurs so among same block and a right occurs so among these two approaches we can also pick these two approaches we can also pick these two approaches we can also pick one and indeed all four possibilities one and indeed all four possibilities one and indeed all four possibilities this and this this and this this and this and this this and this this and this and this this and this this and this and this and that are all possible this and this and that are all possible this and this and that are all possible so you can have WR update with snooping so you can have WR update with snooping so you can have WR update with snooping WR update with directory WR inv validate WR update with directory WR inv validate WR update with directory WR inv validate with snooping right inv validate with with snooping right inv validate with with snooping right inv validate with directory

## Write Update Snooping Coherence： Part 5
Kind: captions Language: en So let's look at the right update So let's look at the right update So let's look at the right update snooping coherence first. Let's look at snooping coherence first. Let's look at snooping coherence first. Let's look at two caches only each of which has two two caches only each of which has two two caches only each of which has two blocks with a single valid bit per block blocks with a single valid bit per block blocks with a single valid bit per block and a tag and then the data and both of and a tag and then the data and both of and a tag and then the data and both of these caches are connected to the same these caches are connected to the same these caches are connected to the same bus to which we also connect the memory. bus to which we also connect the memory. bus to which we also connect the memory. So now let's say that the caches are So now let's say that the caches are So now let's say that the caches are initially empty. So the valid bits are initially empty. So the valid bits are initially empty. So the valid bits are all zero. And this processor reads all zero. And this processor reads all zero. And this processor reads something in block A. We have a cache something in block A. We have a cache something in block A. We have a cache miss here. We send a request to memory. miss here. We send a request to memory. miss here. We send a request to memory. This cache is always monitoring what's This cache is always monitoring what's This cache is always monitoring what's going on on the bus, but it's only going on on the bus, but it's only going on on the bus, but it's only interested in writes. So this read is interested in writes. So this read is interested in writes. So this read is really going to be seen and ignored by really going to be seen and ignored by really going to be seen and ignored by this cache. The data for A comes back this cache. The data for A comes back this cache. The data for A comes back from memory. And now we put this block from memory. And now we put this block from memory. And now we put this block in the cache. The block becomes valid. in the cache. The block becomes valid. in the cache. The block becomes valid. And the tag, let's say, just indicates And the tag, let's say, just indicates And the tag, let's say, just indicates that this is the block A. Now let's say that this is the block A. Now let's say that this is the block A. Now let's say that this processor writes to A. So now that this processor writes to A. So now that this processor writes to A. So now we have a situation where this processor we have a situation where this processor we have a situation where this processor even if it does a right through access even if it does a right through access even if it does a right through access directly to memory a read on this directly to memory a read on this directly to memory a read on this processor would still read the stale processor would still read the stale processor would still read the stale value of a. Let's say that the value we value of a. Let's say that the value we value of a. Let's say that the value we read here was zero. Now let's say that read here was zero. Now let's say that read here was zero. Now let's say that this processor writes one to a. this this processor writes one to a. this this processor writes one to a. this right even if we made it go all the way right even if we made it go all the way right even if we made it go all the way to memory would not result in coherent to memory would not result in coherent to memory would not result in coherent behavior for this cache because a behavior for this cache because a behavior for this cache because a subsequent read here would still hit in subsequent read here would still hit in subsequent read here would still hit in the cache and get a zero. So this is the cache and get a zero. So this is the cache and get a zero. So this is when write update and snooping come into when write update and snooping come into when write update and snooping come into the picture. We have a cache miss but the picture. We have a cache miss but the picture. We have a cache miss but because it's a write we send a request because it's a write we send a request because it's a write we send a request for the block along with an indication for the block along with an indication for the block along with an indication that it's a right and what is the value that it's a right and what is the value that it's a right and what is the value and what's the address. So now what and what's the address. So now what and what's the address. So now what happens is this request goes to memory happens is this request goes to memory happens is this request goes to memory and the memory writes one to A. But more and the memory writes one to A. But more and the memory writes one to A. But more importantly this cache is monitoring the importantly this cache is monitoring the importantly this cache is monitoring the bus and sees that block A is being bus and sees that block A is being bus and sees that block A is being updated. It checks its internal state updated. It checks its internal state updated. It checks its internal state and sees that it has block A and then it and sees that it has block A and then it and sees that it has block A and then it takes the value that is being written. takes the value that is being written. takes the value that is being written. So this part is the snooping part where So this part is the snooping part where So this part is the snooping part where we see what's going on on the bus. So we we see what's going on on the bus. So we we see what's going on on the bus. So we cannot miss any of the rides happening cannot miss any of the rides happening cannot miss any of the rides happening on another core because we're monitoring on another core because we're monitoring on another core because we're monitoring the bus and all rightes have to go the bus and all rightes have to go the bus and all rightes have to go through the bus. That's the snooping through the bus. That's the snooping through the bus. That's the snooping part. The right update part is that when part. The right update part is that when part. The right update part is that when we see a right, we update our copy of we see a right, we update our copy of we see a right, we update our copy of the data with the value that is being the data with the value that is being the data with the value that is being written. So this cache now gets a one written. So this cache now gets a one written. So this cache now gets a one here. If we read a after this, we are here. If we read a after this, we are here. If we read a after this, we are going to get this one. So this ensures going to get this one. So this ensures going to get this one. So this ensures correct behavior. If there are multiple correct behavior. If there are multiple correct behavior. If there are multiple cores, they cannot disagree about which cores, they cannot disagree about which cores, they cannot disagree about which right happened first, which one happened right happened first, which one happened right happened first, which one happened last because all of the rights are sent last because all of the rights are sent last because all of the rights are sent through the bus and because it's a through the bus and because it's a through the bus and because it's a single broadcast bus, it happens one at single broadcast bus, it happens one at single broadcast bus, it happens one at a time. So all of them see rights in a time. So all of them see rights in a time. So all of them see rights in exactly the same order in the order in exactly the same order in the order in exactly the same order in the order in which writes got to the bus. Now let's which writes got to the bus. Now let's which writes got to the bus. Now let's see what happens if both of these see what happens if both of these see what happens if both of these processor try to write to a at about the processor try to write to a at about the processor try to write to a at about the same time. So this is a situation where same time. So this is a situation where same time. So this is a situation where they might disagree about whose right they might disagree about whose right they might disagree about whose right happened first. Let's say that this one happened first. Let's say that this one happened first. Let's say that this one is trying to write two and this one is is trying to write two and this one is is trying to write two and this one is trying to write three. Well, there is a trying to write three. Well, there is a trying to write three. Well, there is a single bus. So there is a single set of single bus. So there is a single set of single bus. So there is a single set of wires that can carry the value and the wires that can carry the value and the wires that can carry the value and the address and so on. So processors when address and so on. So processors when address and so on. So processors when they want to send something on the bus they want to send something on the bus they want to send something on the bus first have to arbitrate for it. And this first have to arbitrate for it. And this first have to arbitrate for it. And this arbitration for the bus basically arbitration for the bus basically arbitration for the bus basically decides who gets the bus next. If this decides who gets the bus next. If this decides who gets the bus next. If this score won then it sends its right first. score won then it sends its right first. score won then it sends its right first. Now both of the caches will have two. Now both of the caches will have two. Now both of the caches will have two. This one because this is where the right This one because this is where the right This one because this is where the right happened. This one because this is what happened. This one because this is what happened. This one because this is what we pick up from snooping. And only then we pick up from snooping. And only then we pick up from snooping. And only then after this is complete this right here after this is complete this right here after this is complete this right here gets to occur. So now we go to three gets to occur. So now we go to three gets to occur. So now we go to three broadcast that here and it gets here. So broadcast that here and it gets here. So broadcast that here and it gets here. So as you can see both of them agree which as you can see both of them agree which as you can see both of them agree which right was first and which was second. right was first and which was second. right was first and which was second. Obviously the one that wrote three was Obviously the one that wrote three was Obviously the one that wrote three was the second one because that's the last the second one because that's the last the second one because that's the last value we kept. So the right update is value we kept. So the right update is value we kept. So the right update is ensuring that rights are seen by reads ensuring that rights are seen by reads ensuring that rights are seen by reads on all cores and the snooping of the on all cores and the snooping of the on all cores and the snooping of the common bus and the serialization on that common bus and the serialization on that common bus and the serialization on that bus basically going one at a time to the bus basically going one at a time to the bus basically going one at a time to the bus is guaranteeing that everybody sees bus is guaranteeing that everybody sees bus is guaranteeing that everybody sees the same order of rights.

## Write Update Coherence Quiz (new)： Part 5
Kind: captions Language: en let's take a moment to check if we let's take a moment to check if we let's take a moment to check if we understand how write-update coherence understand how write-update coherence understand how write-update coherence works works works suppose that the core has a single block suppose that the core has a single block suppose that the core has a single block cache with a valid bit a tag and the cache with a valid bit a tag and the cache with a valid bit a tag and the data and suppose that we have for such data and suppose that we have for such data and suppose that we have for such cores numbered 0 1 2 &amp; 3 cores numbered 0 1 2 &amp; 3 cores numbered 0 1 2 &amp; 3 suppose that core 0 reads from 700 first suppose that core 0 reads from 700 first suppose that core 0 reads from 700 first so now it gets the valid bit to be 1 the so now it gets the valid bit to be 1 the so now it gets the valid bit to be 1 the tag is 700 and let's say that the data tag is 700 and let's say that the data tag is 700 and let's say that the data trees is 6 trees is 6 trees is 6 after this core 1 reads the same after this core 1 reads the same after this core 1 reads the same location then Core 2 writes a value of location then Core 2 writes a value of location then Core 2 writes a value of 17 into this location and finally core 17 into this location and finally core 17 into this location and finally core tree reads from that location after all tree reads from that location after all tree reads from that location after all this in core 0 what is the valid bit in this in core 0 what is the valid bit in this in core 0 what is the valid bit in the cache the tag is still 700 and what the cache the tag is still 700 and what the cache the tag is still 700 and what is the data that we get and the same is the data that we get and the same is the data that we get and the same four cores one two and three four cores one two and three four cores one two and three what is the valid bit in each of these what is the valid bit in each of these what is the valid bit in each of these caches and what's the data value placed caches and what's the data value placed caches and what's the data value placed here you can use this as an example here you can use this as an example here you can use this as an example where the valid bit is either 1 or 0 and where the valid bit is either 1 or 0 and where the valid bit is either 1 or 0 and the data value in this case is 6 the data value in this case is 6 the data value in this case is 6 after this read so what's going on in after this read so what's going on in after this read so what's going on in these 4 caches after we do this sequence these 4 caches after we do this sequence these 4 caches after we do this sequence we complete this then this then this we complete this then this then this we complete this then this then this then this

## Write Update Coherence Quiz Solution (new)： Part 5
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our write-update coherence quiz where what write-update coherence quiz where what write-update coherence quiz where what we had is for access is like this we had is for access is like this we had is for access is like this happening in a write-update happening in a write-update happening in a write-update coherent set of caches after the first coherent set of caches after the first coherent set of caches after the first read completes here we have data six read completes here we have data six read completes here we have data six tags 700 hex and the valid bit is 1 so tags 700 hex and the valid bit is 1 so tags 700 hex and the valid bit is 1 so when this read completes we have a valid when this read completes we have a valid when this read completes we have a valid bit 1 here the data is 6 and the tag is bit 1 here the data is 6 and the tag is bit 1 here the data is 6 and the tag is 700 at that time in these other caches 700 at that time in these other caches 700 at that time in these other caches the valid bit is simply 0 because they the valid bit is simply 0 because they the valid bit is simply 0 because they have not accessed anything yet have not accessed anything yet have not accessed anything yet when core 1 reads it sets the valid bit when core 1 reads it sets the valid bit when core 1 reads it sets the valid bit and fetch is the same data when quarto and fetch is the same data when quarto and fetch is the same data when quarto writes it broadcasts the value in the writes it broadcasts the value in the writes it broadcasts the value in the address so these caches that already address so these caches that already address so these caches that already have this block will update what they have this block will update what they have this block will update what they have now 217 and now of course on core have now 217 and now of course on core have now 217 and now of course on core to this block starts being valid and has to this block starts being valid and has to this block starts being valid and has a value of 17 and finally when core tree a value of 17 and finally when core tree a value of 17 and finally when core tree reads it also gets the value of 17 so reads it also gets the value of 17 so reads it also gets the value of 17 so this is the final state of the caches this is the final state of the caches this is the final state of the caches all caches have the block and all caches all caches have the block and all caches all caches have the block and all caches have the same value of the block which have the same value of the block which have the same value of the block which means that really the caches are means that really the caches are means that really the caches are coherent for this block

## Write Update Optimization： Part 5
Kind: captions Language: en now we will look at two possible now we will look at two possible now we will look at two possible optimizations of a write-update protocol optimizations of a write-update protocol optimizations of a write-update protocol the first optimization will look at is the first optimization will look at is the first optimization will look at is about avoiding memory rights remember about avoiding memory rights remember about avoiding memory rights remember that in a write-update protocol every that in a write-update protocol every that in a write-update protocol every write in any of the processors needs to write in any of the processors needs to write in any of the processors needs to be broadcast on the bus and needs to be broadcast on the bus and needs to be broadcast on the bus and needs to update the memory because memory is big update the memory because memory is big update the memory because memory is big and slow it cannot keep up with all the and slow it cannot keep up with all the and slow it cannot keep up with all the rights and that means that the memory rights and that means that the memory rights and that means that the memory throughput becomes a bottleneck for this throughput becomes a bottleneck for this throughput becomes a bottleneck for this system pretty much it's not about how system pretty much it's not about how system pretty much it's not about how much traffic can the bus take it's about much traffic can the bus take it's about much traffic can the bus take it's about how much traffic and the memory take so how much traffic and the memory take so how much traffic and the memory take so we want to avoid unnecessary memory we want to avoid unnecessary memory we want to avoid unnecessary memory writes these caches previously behaved writes these caches previously behaved writes these caches previously behaved as right through caches every write was as right through caches every write was as right through caches every write was going through the cache to memory and we going through the cache to memory and we going through the cache to memory and we have seen already that right through is have seen already that right through is have seen already that right through is bad for memory traffic even with a bad for memory traffic even with a bad for memory traffic even with a single core now we're adding more than single core now we're adding more than single core now we're adding more than one core so it's going to be even a one core so it's going to be even a one core so it's going to be even a bigger problem so the solution to bigger problem so the solution to bigger problem so the solution to reducing memory traffic here will be the reducing memory traffic here will be the reducing memory traffic here will be the same we will add a dirty bit to each same we will add a dirty bit to each same we will add a dirty bit to each block in each of the caches and this block in each of the caches and this block in each of the caches and this dirty bit will allow us to delay a dirty bit will allow us to delay a dirty bit will allow us to delay a memory right until we actually replace a memory right until we actually replace a memory right until we actually replace a dirty block from our cache so now let us dirty block from our cache so now let us dirty block from our cache so now let us see how this work and how it avoids see how this work and how it avoids see how this work and how it avoids memory writes suppose that this cache memory writes suppose that this cache memory writes suppose that this cache reads a it gets this value from memory reads a it gets this value from memory reads a it gets this value from memory let's say it's 6 the tag will be a it's let's say it's 6 the tag will be a it's let's say it's 6 the tag will be a it's valid and it's not dirty valid and it's not dirty valid and it's not dirty now this cache writes a at this point it now this cache writes a at this point it now this cache writes a at this point it will broadcast the write with the will broadcast the write with the will broadcast the write with the address of a and the value that it address of a and the value that it address of a and the value that it writes let's say 17 so this cache here writes let's say 17 so this cache here writes let's say 17 so this cache here would update the value now without a would update the value now without a would update the value now without a dirty bit the memory would also be dirty bit the memory would also be dirty bit the memory would also be updated with a equals 17 we want to updated with a equals 17 we want to updated with a equals 17 we want to avoid that why well because there could avoid that why well because there could avoid that why well because there could be a lot of writes here until we finally be a lot of writes here until we finally be a lot of writes here until we finally replace it in a write back cache so what replace it in a write back cache so what replace it in a write back cache so what we will do is we will write 17 tag it we will do is we will write 17 tag it we will do is we will write 17 tag it this way and mark this as dirty dirty this way and mark this as dirty dirty this way and mark this as dirty dirty means two things here one is that the means two things here one is that the means two things here one is that the memory is not up-to-date and we need to memory is not up-to-date and we need to memory is not up-to-date and we need to update it this is exactly the same update it this is exactly the same update it this is exactly the same as in a normal unit processor write-back as in a normal unit processor write-back as in a normal unit processor write-back cache if we replace something dirty we cache if we replace something dirty we cache if we replace something dirty we have to send a write back to memory in a have to send a write back to memory in a have to send a write back to memory in a write-update protocol dirty has another write-update protocol dirty has another write-update protocol dirty has another meaning that we will now illustrate meaning that we will now illustrate meaning that we will now illustrate suppose that this cache replaces this suppose that this cache replaces this suppose that this cache replaces this block and then wants to read it again so block and then wants to read it again so block and then wants to read it again so it issues a request to read on the bus it issues a request to read on the bus it issues a request to read on the bus and now normally the memory would and now normally the memory would and now normally the memory would respond with the value but the memory respond with the value but the memory respond with the value but the memory still has the old value of equals 6 this still has the old value of equals 6 this still has the old value of equals 6 this cache here has the only up-to-date copy cache here has the only up-to-date copy cache here has the only up-to-date copy that's what the dirty bit also stands that's what the dirty bit also stands that's what the dirty bit also stands for so now this cache needs to stop read for so now this cache needs to stop read for so now this cache needs to stop read as well notice that we are reading the as well notice that we are reading the as well notice that we are reading the same block that it has in a dirty state same block that it has in a dirty state same block that it has in a dirty state so instead of the memory being allowed so instead of the memory being allowed so instead of the memory being allowed to respond we respond first the cache is to respond we respond first the cache is to respond we respond first the cache is faster than the memory so it will faster than the memory so it will faster than the memory so it will respond quicker and say that a is 17 so respond quicker and say that a is 17 so respond quicker and say that a is 17 so now here we get a becomes 17 in this now here we get a becomes 17 in this now here we get a becomes 17 in this cache and it's not dirty now after this cache and it's not dirty now after this cache and it's not dirty now after this let's say that we write a new value here let's say that we write a new value here let's say that we write a new value here of let's say 20 we put the new value of let's say 20 we put the new value of let's say 20 we put the new value here we broadcast the new value on the here we broadcast the new value on the here we broadcast the new value on the bus the snooping here gets that 20 but bus the snooping here gets that 20 but bus the snooping here gets that 20 but the memory again doesn't get updated if the memory again doesn't get updated if the memory again doesn't get updated if this block however gets replaced from this block however gets replaced from this block however gets replaced from this cache then an update is sent to this cache then an update is sent to this cache then an update is sent to memory another interesting situation memory another interesting situation memory another interesting situation here is what happens if we have the here is what happens if we have the here is what happens if we have the block dirty in this cache and now this block dirty in this cache and now this block dirty in this cache and now this cache writes let's say 32 a in that case cache writes let's say 32 a in that case cache writes let's say 32 a in that case this cache broadcasts the right this this cache broadcasts the right this this cache broadcasts the right this cache snoops the right puts 30 in its cache snoops the right puts 30 in its cache snoops the right puts 30 in its cache now this cache is no longer cache now this cache is no longer cache now this cache is no longer responsible for writing back to memory responsible for writing back to memory responsible for writing back to memory because the new writer takes that because the new writer takes that because the new writer takes that responsibility and the memory still responsibility and the memory still responsibility and the memory still doesn't get updated so as you can see we doesn't get updated so as you can see we doesn't get updated so as you can see we can now do many writes without updating can now do many writes without updating can now do many writes without updating the memory we can even move the the memory we can even move the the memory we can even move the responsibility for updating memory to responsibility for updating memory to responsibility for updating memory to another cache and keep writing there another cache and keep writing there another cache and keep writing there without the updates to memory only when without the updates to memory only when without the updates to memory only when the last writer replaces the block that the last writer replaces the block that the last writer replaces the block that is still dirty will we is still dirty will we is still dirty will we a single right to memory so the benefits a single right to memory so the benefits a single right to memory so the benefits of having the dirty bit are that we can of having the dirty bit are that we can of having the dirty bit are that we can now write to the memory only when the now write to the memory only when the now write to the memory only when the block is replaced so we can do many block is replaced so we can do many block is replaced so we can do many writes without updating the memory does writes without updating the memory does writes without updating the memory does saving a lot of the writes to memory and saving a lot of the writes to memory and saving a lot of the writes to memory and also we avoid memory reads because now also we avoid memory reads because now also we avoid memory reads because now we allow reads from memory only when we allow reads from memory only when we allow reads from memory only when nobody has the block in a dirty state if nobody has the block in a dirty state if nobody has the block in a dirty state if the block is dirty for example here then the block is dirty for example here then the block is dirty for example here then whoever asks for this block this is the whoever asks for this block this is the whoever asks for this block this is the cache that will provide the value and cache that will provide the value and cache that will provide the value and the memory doesn't need to do that so we the memory doesn't need to do that so we the memory doesn't need to do that so we are also saving reads from memory this are also saving reads from memory this are also saving reads from memory this way

## Write Update Optimization 2 Bus Writes： Part 5
Kind: captions Language: en and now let's look at the second and now let's look at the second and now let's look at the second possible right update optimization which possible right update optimization which possible right update optimization which is about reducing the number of bus is about reducing the number of bus is about reducing the number of bus rights so after we added our dirty bit rights so after we added our dirty bit rights so after we added our dirty bit what we got is that the memory now gets what we got is that the memory now gets what we got is that the memory now gets relatively few rights only those rights relatively few rights only those rights relatively few rights only those rights that are coming from replacements and that are coming from replacements and that are coming from replacements and also the memory no longer needs to also the memory no longer needs to also the memory no longer needs to respond to all the reads but the bus respond to all the reads but the bus respond to all the reads but the bus still gets all of the traffic for all of still gets all of the traffic for all of still gets all of the traffic for all of the rights every single right will need the rights every single right will need the rights every single right will need to go on the bus and broadcast the value to go on the bus and broadcast the value to go on the bus and broadcast the value so now the bus which sees all of the so now the bus which sees all of the so now the bus which sees all of the rights will become the bottleneck in rights will become the bottleneck in rights will become the bottleneck in this system this system this system because every single write from every because every single write from every because every single write from every single core ends up going to the bus and single core ends up going to the bus and single core ends up going to the bus and the bus can only take so much the bus the bus can only take so much the bus the bus can only take so much the bus broadcasts for rights that are needed to broadcasts for rights that are needed to broadcasts for rights that are needed to update other copies of the data need to update other copies of the data need to update other copies of the data need to happen because this is a write-update happen because this is a write-update happen because this is a write-update protocol it needs to update other copies protocol it needs to update other copies protocol it needs to update other copies of the data but the optimization we can of the data but the optimization we can of the data but the optimization we can do is about the rights that do not need do is about the rights that do not need do is about the rights that do not need to update anybody so we will add another to update anybody so we will add another to update anybody so we will add another bit called the shared bit to each block bit called the shared bit to each block bit called the shared bit to each block in each cache and that bit will tell us in each cache and that bit will tell us in each cache and that bit will tell us whether the block is shared with others whether the block is shared with others whether the block is shared with others or not let's see how things would work or not let's see how things would work or not let's see how things would work suppose we read a here we get this valid suppose we read a here we get this valid suppose we read a here we get this valid not dirty but for the shared bit we need not dirty but for the shared bit we need not dirty but for the shared bit we need to know whether others also have this to know whether others also have this to know whether others also have this block or not for this purpose block or not for this purpose block or not for this purpose we add a line to the bus when a reed we add a line to the bus when a reed we add a line to the bus when a reed goes to memory others snoop this read goes to memory others snoop this read goes to memory others snoop this read and if they have that same block in the and if they have that same block in the and if they have that same block in the cache then they pull the shared line to cache then they pull the shared line to cache then they pull the shared line to one if nobody pulls the shared line to one if nobody pulls the shared line to one if nobody pulls the shared line to one then it will remain at zero so for one then it will remain at zero so for one then it will remain at zero so for this particular block because this is this particular block because this is this particular block because this is the first read nobody pulls the shared the first read nobody pulls the shared the first read nobody pulls the shared line to one so the block we know is not line to one so the block we know is not line to one so the block we know is not shared this is the tag and let's say shared this is the tag and let's say shared this is the tag and let's say we're at six now let's see what happens we're at six now let's see what happens we're at six now let's see what happens when this processor writes to a and when this processor writes to a and when this processor writes to a and let's say it puts a value of seventeen let's say it puts a value of seventeen let's say it puts a value of seventeen there it's a Miss we broadcast both the there it's a Miss we broadcast both the there it's a Miss we broadcast both the right and the value the first time and right and the value the first time and right and the value the first time and we see now that somebody is writing we see now that somebody is writing we see now that somebody is writing and this cash here snoops that right and and this cash here snoops that right and and this cash here snoops that right and those two things one it now knows that those two things one it now knows that those two things one it now knows that the block is shared because somebody is the block is shared because somebody is the block is shared because somebody is accessing it now second it updates the accessing it now second it updates the accessing it now second it updates the value third it pulls this line to one so value third it pulls this line to one so value third it pulls this line to one so that this cash knows the block is shared that this cash knows the block is shared that this cash knows the block is shared and now it has the Block in the dirty and now it has the Block in the dirty and now it has the Block in the dirty State with the value of 17 so so far State with the value of 17 so so far State with the value of 17 so so far this has behaved exactly as it should this has behaved exactly as it should this has behaved exactly as it should without the shared line and without the without the shared line and without the without the shared line and without the shared bit so what's the benefit of the shared bit so what's the benefit of the shared bit so what's the benefit of the shared bits well the benefit is that now shared bits well the benefit is that now shared bits well the benefit is that now this cash if it wants to write again it this cash if it wants to write again it this cash if it wants to write again it sees that the shared bit is one which sees that the shared bit is one which sees that the shared bit is one which means that it does need to broadcast means that it does need to broadcast means that it does need to broadcast same as before so the broadcast will same as before so the broadcast will same as before so the broadcast will happen exactly as before if there is happen exactly as before if there is happen exactly as before if there is another sharer because we need to update another sharer because we need to update another sharer because we need to update that sharer to the new value for the that sharer to the new value for the that sharer to the new value for the data the benefit of the shared it is in data the benefit of the shared it is in data the benefit of the shared it is in that if this block reads let's say B and that if this block reads let's say B and that if this block reads let's say B and gets a value of 2 here and it's valid gets a value of 2 here and it's valid gets a value of 2 here and it's valid and it's not dirty and it is that so far and it's not dirty and it is that so far and it's not dirty and it is that so far nobody has B so sharing is 0 here and nobody has B so sharing is 0 here and nobody has B so sharing is 0 here and then wants to write 5 to be it checks then wants to write 5 to be it checks then wants to write 5 to be it checks the shared bit here and because we know the shared bit here and because we know the shared bit here and because we know that nobody else has this block we can that nobody else has this block we can that nobody else has this block we can write the value here make the thing write the value here make the thing write the value here make the thing dirty because the memory is no longer dirty because the memory is no longer dirty because the memory is no longer up-to-date but because the shared bit up-to-date but because the shared bit up-to-date but because the shared bit stays at 0 meaning we are the only one stays at 0 meaning we are the only one stays at 0 meaning we are the only one who has it who has it who has it we don't do a bus right here so as long we don't do a bus right here so as long we don't do a bus right here so as long as some block is being accessed only by as some block is being accessed only by as some block is being accessed only by one core it's no longer gonna broadcast one core it's no longer gonna broadcast one core it's no longer gonna broadcast all of the rights meanwhile this core all of the rights meanwhile this core all of the rights meanwhile this core could be having in a non shared State could be having in a non shared State could be having in a non shared State for example Block C and it can write for example Block C and it can write for example Block C and it can write anything in once there you can write 17 anything in once there you can write 17 anything in once there you can write 17 then 65 etc so B and C because they then 65 etc so B and C because they then 65 etc so B and C because they belong to only one core at this time belong to only one core at this time belong to only one core at this time will not do bus rights while a will do will not do bus rights while a will do will not do bus rights while a will do bus rights so we still get the right bus rights so we still get the right bus rights so we still get the right update behavior when there is sharing update behavior when there is sharing update behavior when there is sharing but now we avoid rights going to the bus but now we avoid rights going to the bus but now we avoid rights going to the bus when there is no sharing when there is no sharing when there is no sharing this can happen a lot for example B this can happen a lot for example B this can happen a lot for example B could be something that belongs to the could be something that belongs to the could be something that belongs to the stack of course zero seek would be stack of course zero seek would be stack of course zero seek would be something that is on the stack of cor something that is on the stack of cor something that is on the stack of cor one so naturally we will be just pushing one so naturally we will be just pushing one so naturally we will be just pushing and popping things to our own stack and and popping things to our own stack and and popping things to our own stack and never really needing to broadcast the never really needing to broadcast the never really needing to broadcast the updates somewhere else but for data that updates somewhere else but for data that updates somewhere else but for data that is actually shared we still get exactly is actually shared we still get exactly is actually shared we still get exactly the same right update behavior as before the same right update behavior as before the same right update behavior as before so this optimization will save us a lot so this optimization will save us a lot so this optimization will save us a lot of Rights on the bus it will save us all of Rights on the bus it will save us all of Rights on the bus it will save us all of the rights that are not necessary to of the rights that are not necessary to of the rights that are not necessary to maintain coherence among multiple copies maintain coherence among multiple copies maintain coherence among multiple copies of the same block in different caches

## Write Update Performance Quiz： Part 5
Kind: captions Language: en let's quickly check if we understood the let's quickly check if we understood the let's quickly check if we understood the right update optimizations suppose that right update optimizations suppose that right update optimizations suppose that we have a system with two cores that we have a system with two cores that we have a system with two cores that uses write-update coherence suppose that uses write-update coherence suppose that uses write-update coherence suppose that the two cores are executing a program the two cores are executing a program the two cores are executing a program where core 0 writes something to a where core 0 writes something to a where core 0 writes something to a memory address a then core 1 reads it memory address a then core 1 reads it memory address a then core 1 reads it and this continues so that the write and and this continues so that the write and and this continues so that the write and then read pattern repeats for a total of then read pattern repeats for a total of then read pattern repeats for a total of 1000 times so this is the first of 1,000 1000 times so this is the first of 1,000 1000 times so this is the first of 1,000 after they do this for 1,000 times this after they do this for 1,000 times this after they do this for 1,000 times this course go to do something else where course go to do something else where course go to do something else where core 0 replaces a and then core 1 also core 0 replaces a and then core 1 also core 0 replaces a and then core 1 also replaces a the question for you is how replaces a the question for you is how replaces a the question for you is how many bus uses do we see when the system many bus uses do we see when the system many bus uses do we see when the system uses no optimizations so there is no uses no optimizations so there is no uses no optimizations so there is no dirty bit and no shared it when the dirty bit and no shared it when the dirty bit and no shared it when the system uses only the dirty bit system uses only the dirty bit system uses only the dirty bit optimization and when the system uses optimization and when the system uses optimization and when the system uses both the dirty and the shared bit both the dirty and the shared bit both the dirty and the shared bit optimization so for each of these how optimization so for each of these how optimization so for each of these how many bus uses do we get during this many bus uses do we get during this many bus uses do we get during this whole time for block a and also how many whole time for block a and also how many whole time for block a and also how many memory writes do we get in each of these memory writes do we get in each of these memory writes do we get in each of these three cases

## Write Update Performance Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to a write let's look at the solution to a write let's look at the solution to a write update optimization quiz where we have update optimization quiz where we have update optimization quiz where we have two cores with write update coherence two cores with write update coherence two cores with write update coherence and and and we consider the case of no optimization we consider the case of no optimization we consider the case of no optimization dirty bit only optimization and both dirty bit only optimization and both dirty bit only optimization and both dirty bit and shared bit optimization dirty bit and shared bit optimization dirty bit and shared bit optimization and for this program how many bus uses and for this program how many bus uses and for this program how many bus uses and memory writes do we get so let's and memory writes do we get so let's and memory writes do we get so let's consider the case of no optimization consider the case of no optimization consider the case of no optimization first let us first consider the no first let us first consider the no first let us first consider the no optimization case optimization case optimization case this right here we'll use the bus this right here we'll use the bus this right here we'll use the bus and we'll write to memory this read here and we'll write to memory this read here and we'll write to memory this read here the first one the first one the first one will use the bus will use the bus will use the bus but will not update the memory because but will not update the memory because but will not update the memory because this is a read miss this is a read miss this is a read miss from then on each write on core 0 will from then on each write on core 0 will from then on each write on core 0 will use the bus to broadcast and update use the bus to broadcast and update use the bus to broadcast and update memory memory memory so we get 999 more so we get 999 more so we get 999 more accesses to each because of this accesses to each because of this accesses to each because of this whereas reads are no longer going to be whereas reads are no longer going to be whereas reads are no longer going to be misses misses misses and they find what they are looking for and they find what they are looking for and they find what they are looking for in the cache so they are no longer going in the cache so they are no longer going in the cache so they are no longer going to either bus or memory to either bus or memory to either bus or memory eventually these caches replace this but eventually these caches replace this but eventually these caches replace this but nothing happens on the replacement in nothing happens on the replacement in nothing happens on the replacement in this case because everything is already this case because everything is already this case because everything is already up to date so overall here we got 1001 up to date so overall here we got 1001 up to date so overall here we got 1001 bus use and 1000 memory writes now let bus use and 1000 memory writes now let bus use and 1000 memory writes now let us look at the dirty bit only us look at the dirty bit only us look at the dirty bit only optimization optimization optimization in this optimization in this optimization in this optimization the writes on core 0 will all be the writes on core 0 will all be the writes on core 0 will all be happening to core 0's cache and the only happening to core 0's cache and the only happening to core 0's cache and the only write to memory will be when the block write to memory will be when the block write to memory will be when the block is replaced so we get only one memory is replaced so we get only one memory is replaced so we get only one memory write how many bus users do we need well write how many bus users do we need well write how many bus users do we need well the first write and every subsequent the first write and every subsequent the first write and every subsequent write will be broadcast so we get 1000 write will be broadcast so we get 1000 write will be broadcast so we get 1000 bus accesses because of these rights bus accesses because of these rights bus accesses because of these rights we still have the one access because the we still have the one access because the we still have the one access because the first read is a miss after that that all first read is a miss after that that all first read is a miss after that that all hits and don't go to the bus hits and don't go to the bus hits and don't go to the bus and now we have an additional bus axis and now we have an additional bus axis and now we have an additional bus axis because the replacement of a here needs because the replacement of a here needs because the replacement of a here needs to send the data to memory for which we to send the data to memory for which we to send the data to memory for which we need a bus axis need a bus axis need a bus axis so overall we reduce dramatically the so overall we reduce dramatically the so overall we reduce dramatically the number of memory writes in exchange for number of memory writes in exchange for number of memory writes in exchange for one more one more one more bus use bus use bus use now let's see what happens with the now let's see what happens with the now let's see what happens with the dirty and shared optimization dirty and shared optimization dirty and shared optimization we're right here and at this point we we're right here and at this point we we're right here and at this point we are the only one who has this block so are the only one who has this block so are the only one who has this block so the shared bit the shared bit the shared bit is not set is not set is not set this would normally allow us to write to this would normally allow us to write to this would normally allow us to write to it without going to the bus anymore but it without going to the bus anymore but it without going to the bus anymore but because the very next axis is read of a because the very next axis is read of a because the very next axis is read of a on core 1 on core 1 on core 1 the sharing starts the sharing starts the sharing starts and that means that really and that means that really and that means that really we have exactly the same number of bus we have exactly the same number of bus we have exactly the same number of bus uses and memory writes uses and memory writes uses and memory writes remember that the sharing optimization remember that the sharing optimization remember that the sharing optimization only works when we are accessing only works when we are accessing only works when we are accessing a block only on one core this block is a block only on one core this block is a block only on one core this block is actually being actually being actually being read on one core written on another so read on one core written on another so read on one core written on another so all the writes on this score need to be all the writes on this score need to be all the writes on this score need to be propagated to the bus so that this propagated to the bus so that this propagated to the bus so that this course cache can be updated so really course cache can be updated so really course cache can be updated so really here the shared bit optimization doesn't here the shared bit optimization doesn't here the shared bit optimization doesn't result in any savings

## Write Invalidate Snooping Coherence： Part 5
Kind: captions Language: en We said that there are two types of We said that there are two types of We said that there are two types of snooping coherence approaches. One was snooping coherence approaches. One was snooping coherence approaches. One was write update. The other is write write update. The other is write write update. The other is write invalidate. So going back to our invalidate. So going back to our invalidate. So going back to our optimize write update setup where we had optimize write update setup where we had optimize write update setup where we had the valid and the tag bits also the the valid and the tag bits also the the valid and the tag bits also the dirty bit for write back caches and a dirty bit for write back caches and a dirty bit for write back caches and a shared bit that tells us whether we are shared bit that tells us whether we are shared bit that tells us whether we are alone in this. A write invalidate alone in this. A write invalidate alone in this. A write invalidate protocol will broadcast rights on the protocol will broadcast rights on the protocol will broadcast rights on the bus so that they can be seen by bus so that they can be seen by bus so that they can be seen by everybody. But instead of updating their everybody. But instead of updating their everybody. But instead of updating their copies, they will invalidate them. So copies, they will invalidate them. So copies, they will invalidate them. So let's see how this works. Let's say this let's see how this works. Let's say this let's see how this works. Let's say this score reads score reads score reads a. We send this get the value from a. We send this get the value from a. We send this get the value from memory. Now we have it as valid, not memory. Now we have it as valid, not memory. Now we have it as valid, not dirty. The tag is a the value is let's dirty. The tag is a the value is let's dirty. The tag is a the value is let's say zero and it's not shared. Let's say say zero and it's not shared. Let's say say zero and it's not shared. Let's say now we have the right to a and write one now we have the right to a and write one now we have the right to a and write one to it. We have a cache miss. We to it. We have a cache miss. We to it. We have a cache miss. We broadcast the right. We now don't need broadcast the right. We now don't need broadcast the right. We now don't need to broadcast the value with it because to broadcast the value with it because to broadcast the value with it because the other caches don't need this value. the other caches don't need this value. the other caches don't need this value. What's going to happen is as we send What's going to happen is as we send What's going to happen is as we send this to the bus, it's going to be this to the bus, it's going to be this to the bus, it's going to be snooped by this cache and it sees that snooped by this cache and it sees that snooped by this cache and it sees that somebody is trying to write to the block somebody is trying to write to the block somebody is trying to write to the block that I have in my cache. In a right that I have in my cache. In a right that I have in my cache. In a right update protocol, we would now need the update protocol, we would now need the update protocol, we would now need the value to put it here. In a right value to put it here. In a right value to put it here. In a right invalidate protocol, all we do is change invalidate protocol, all we do is change invalidate protocol, all we do is change the valid bit to zero. So now we will the valid bit to zero. So now we will the valid bit to zero. So now we will miss in the cache next time we read this miss in the cache next time we read this miss in the cache next time we read this particular cache now gets the valid it's particular cache now gets the valid it's particular cache now gets the valid it's not shared every time we do a write and not shared every time we do a write and not shared every time we do a write and broadcast it we know it's not shared broadcast it we know it's not shared broadcast it we know it's not shared because all the other copies have been because all the other copies have been because all the other copies have been destroyed so a write makes the block destroyed so a write makes the block destroyed so a write makes the block non-shared in the writing processor it non-shared in the writing processor it non-shared in the writing processor it is dirty and now it has a value of one is dirty and now it has a value of one is dirty and now it has a value of one so now a read of a here in a right so now a read of a here in a right so now a read of a here in a right update protocol would be a hit and just update protocol would be a hit and just update protocol would be a hit and just read here but in a right invalid date read here but in a right invalid date read here but in a right invalid date protocol, the read will still get the protocol, the read will still get the protocol, the read will still get the new value. It's not going to be new value. It's not going to be new value. It's not going to be incoherent because it doesn't get the incoherent because it doesn't get the incoherent because it doesn't get the value of zero. It will now be a miss and value of zero. It will now be a miss and value of zero. It will now be a miss and be sent to the bus. Now, the memory has be sent to the bus. Now, the memory has be sent to the bus. Now, the memory has not been updated yet, but the processor not been updated yet, but the processor not been updated yet, but the processor who has the dirty data and matches the who has the dirty data and matches the who has the dirty data and matches the address of the block will see that there address of the block will see that there address of the block will see that there is a read request on the bus and respond is a read request on the bus and respond is a read request on the bus and respond with the data. So now the block becomes with the data. So now the block becomes with the data. So now the block becomes valid. Somebody gave us the data. So valid. Somebody gave us the data. So valid. Somebody gave us the data. So definitely now it's shared. we get the definitely now it's shared. we get the definitely now it's shared. we get the data and the cache where we wrote now data and the cache where we wrote now data and the cache where we wrote now also knows it's shared because we gave also knows it's shared because we gave also knows it's shared because we gave it to somebody. So clearly we are not it to somebody. So clearly we are not it to somebody. So clearly we are not the only one who has it anymore. So now the only one who has it anymore. So now the only one who has it anymore. So now note that just like in a right update note that just like in a right update note that just like in a right update protocol if we have a lot of reads now protocol if we have a lot of reads now protocol if we have a lot of reads now they can have cash hits another write they can have cash hits another write they can have cash hits another write will invalidate this. So there will be a will invalidate this. So there will be a will invalidate this. So there will be a cache miss also. So the disadvantage of cache miss also. So the disadvantage of cache miss also. So the disadvantage of write invalidate protocols is that there write invalidate protocols is that there write invalidate protocols is that there is a miss on all the readers when is a miss on all the readers when is a miss on all the readers when somebody writes. However, there is an somebody writes. However, there is an somebody writes. However, there is an advantage to it as well in that if you advantage to it as well in that if you advantage to it as well in that if you need to update the same block twice. need to update the same block twice. need to update the same block twice. Let's say you write two to a, we Let's say you write two to a, we Let's say you write two to a, we invalidate this copy by broadcasting. invalidate this copy by broadcasting. invalidate this copy by broadcasting. Now the block is no longer shared. We Now the block is no longer shared. We Now the block is no longer shared. We put two there. So we had a broadcast for put two there. So we had a broadcast for put two there. So we had a broadcast for this first write. If we now do some more this first write. If we now do some more this first write. If we now do some more reads and writes, they can all be done reads and writes, they can all be done reads and writes, they can all be done locally. We don't have to broadcast locally. We don't have to broadcast locally. We don't have to broadcast them. So basically by invalidating this them. So basically by invalidating this them. So basically by invalidating this copy we made ourselves be the only one copy we made ourselves be the only one copy we made ourselves be the only one who has the block and now we don't need who has the block and now we don't need who has the block and now we don't need to broadcast all the rights. So for to broadcast all the rights. So for to broadcast all the rights. So for anything where one core works on it for anything where one core works on it for anything where one core works on it for a while then another one works on it for a while then another one works on it for a while then another one works on it for a while. If the work involves writing a while. If the work involves writing a while. If the work involves writing only the first write really has to be only the first write really has to be only the first write really has to be broadcast after that it's a local access broadcast after that it's a local access broadcast after that it's a local access for both reads and writes. In contrast for both reads and writes. In contrast for both reads and writes. In contrast with write update we would have to keep with write update we would have to keep with write update we would have to keep updating the other cache until it updating the other cache until it updating the other cache until it replaces the block. Just like with write replaces the block. Just like with write replaces the block. Just like with write update, write invalidate can benefit update, write invalidate can benefit update, write invalidate can benefit from the same type of optimization when from the same type of optimization when from the same type of optimization when we are writing two different blocks and we are writing two different blocks and we are writing two different blocks and accessing it later. So in that case B is accessing it later. So in that case B is accessing it later. So in that case B is fetched here is not shared becomes dirty fetched here is not shared becomes dirty fetched here is not shared becomes dirty and C is fetched here is not shared and C is fetched here is not shared and C is fetched here is not shared becomes dirty and gets a value of one. becomes dirty and gets a value of one. becomes dirty and gets a value of one. And now we can have cache hits on both And now we can have cache hits on both And now we can have cache hits on both reads and writes without going to the reads and writes without going to the reads and writes without going to the bus for B here. And we can access C here bus for B here. And we can access C here bus for B here. And we can access C here with no going to the bus. So for blocks with no going to the bus. So for blocks with no going to the bus. So for blocks that are only accessed on one core, we that are only accessed on one core, we that are only accessed on one core, we have the same behavior for both write have the same behavior for both write have the same behavior for both write update and write update and write update and write invalidate. But for blocks that are invalidate. But for blocks that are invalidate. But for blocks that are shared, we get different behavior. Write shared, we get different behavior. Write shared, we get different behavior. Write update tends to result in more hits but update tends to result in more hits but update tends to result in more hits but does more broadcasts if we repeatedly does more broadcasts if we repeatedly does more broadcasts if we repeatedly update. Write invalidate generates a update. Write invalidate generates a update. Write invalidate generates a miss on all the readers after a right miss on all the readers after a right miss on all the readers after a right occurred but it allows us to locally occurred but it allows us to locally occurred but it allows us to locally write after the first write. Note that write after the first write. Note that write after the first write. Note that with write update we ensured for a that with write update we ensured for a that with write update we ensured for a that the second property of coherence is the second property of coherence is the second property of coherence is maintained by updating all the copies. maintained by updating all the copies. maintained by updating all the copies. So when a right occurs all copies are up So when a right occurs all copies are up So when a right occurs all copies are up to date and thus they will return the to date and thus they will return the to date and thus they will return the new value of the right. With write new value of the right. With write new value of the right. With write invalidate we ensure the same property invalidate we ensure the same property invalidate we ensure the same property by the right invalidating all the other by the right invalidating all the other by the right invalidating all the other copies. So that now they have no choice copies. So that now they have no choice copies. So that now they have no choice but to get the new version of the data. but to get the new version of the data. but to get the new version of the data. So either way reads will get the new So either way reads will get the new So either way reads will get the new version of the data but that is achieved version of the data but that is achieved version of the data but that is achieved in different ways. The third property of in different ways. The third property of in different ways. The third property of coherence is still ensured by snooping coherence is still ensured by snooping coherence is still ensured by snooping in a right invalidate protocol with in a right invalidate protocol with in a right invalidate protocol with snooping. So basically snooping on the snooping. So basically snooping on the snooping. So basically snooping on the bus still ensures that any rights that bus still ensures that any rights that bus still ensures that any rights that need to go to the bus because there is need to go to the bus because there is need to go to the bus because there is sharing will be ordered by the order to sharing will be ordered by the order to sharing will be ordered by the order to which they go to the bus. So for example which they go to the bus. So for example which they go to the bus. So for example a right here will invalidate everything. a right here will invalidate everything. a right here will invalidate everything. Now a right here has to put a request on Now a right here has to put a request on Now a right here has to put a request on the bus because it's a miss and thus the bus because it's a miss and thus the bus because it's a miss and thus obtain the copy from here and now obtain the copy from here and now obtain the copy from here and now everybody knows what the order was. In everybody knows what the order was. In everybody knows what the order was. In fact, at any given time, only the last fact, at any given time, only the last fact, at any given time, only the last version really survives in caches version really survives in caches version really survives in caches because a right invalidates all the because a right invalidates all the because a right invalidates all the other copies. So, it's pretty clear other copies. So, it's pretty clear other copies. So, it's pretty clear whose version was last because all the whose version was last because all the whose version was last because all the readers need to get the copy that was readers need to get the copy that was readers need to get the copy that was written last anyway.

## Write Update vs Write Invalidate Quiz： Part 5
Kind: captions Language: en So now let's see how write update and So now let's see how write update and So now let's see how write update and write invalidate differ from each other. write invalidate differ from each other. write invalidate differ from each other. Suppose that we have two cores and a Suppose that we have two cores and a Suppose that we have two cores and a program where core zero writes to a and program where core zero writes to a and program where core zero writes to a and then core one reads from A and this then core one reads from A and this then core one reads from A and this repeats a thousand times. And you can repeats a thousand times. And you can repeats a thousand times. And you can assume that the caches are empty before assume that the caches are empty before assume that the caches are empty before all of this and that we don't care what all of this and that we don't care what all of this and that we don't care what happens after all of this. The question happens after all of this. The question happens after all of this. The question for you is how many bus uses do we get for you is how many bus uses do we get for you is how many bus uses do we get with a right update protocol that uses with a right update protocol that uses with a right update protocol that uses both the shared and the dirty bit both the shared and the dirty bit both the shared and the dirty bit optimization? Put your answer here. And optimization? Put your answer here. And optimization? Put your answer here. And then with a right invalidate protocol then with a right invalidate protocol then with a right invalidate protocol with the same shared and dirty bits, put with the same shared and dirty bits, put with the same shared and dirty bits, put your answer here.

## Write Update vs Write Invalidate Quiz Solution： Part 5
Kind: captions Language: en let's see the solution to a write-update let's see the solution to a write-update let's see the solution to a write-update versus write-invalidate quiz we have two versus write-invalidate quiz we have two versus write-invalidate quiz we have two cores that run a program where basically cores that run a program where basically cores that run a program where basically course zero writes and then core 1 reads course zero writes and then core 1 reads course zero writes and then core 1 reads and that happens a thousand times how and that happens a thousand times how and that happens a thousand times how many bus uses do we get with the many bus uses do we get with the many bus uses do we get with the write-update protocol and then with the write-update protocol and then with the write-update protocol and then with the write invalidate protocol we have write invalidate protocol we have write invalidate protocol we have already seen a similar example for write already seen a similar example for write already seen a similar example for write update what's going to happen is with update what's going to happen is with update what's going to happen is with the shared and dirty bits we do not the shared and dirty bits we do not the shared and dirty bits we do not really save much because there is actual really save much because there is actual really save much because there is actual sharing going on here so we are going to sharing going on here so we are going to sharing going on here so we are going to have a broadcast on the bus for each of have a broadcast on the bus for each of have a broadcast on the bus for each of these rights so we get a thousand bus these rights so we get a thousand bus these rights so we get a thousand bus accesses from these rights and the very accesses from these rights and the very accesses from these rights and the very first read is going to be a Miss so we first read is going to be a Miss so we first read is going to be a Miss so we get the bus access there so overall we get the bus access there so overall we get the bus access there so overall we get one thousand one bus uses in a write get one thousand one bus uses in a write get one thousand one bus uses in a write invalidate protocol let's see what invalidate protocol let's see what invalidate protocol let's see what happens core 0 writes this is a Miss we happens core 0 writes this is a Miss we happens core 0 writes this is a Miss we send something to the bus and now we get send something to the bus and now we get send something to the bus and now we get the only copy of this data core 1 reads the only copy of this data core 1 reads the only copy of this data core 1 reads has a miss core 0 has to supply the data has a miss core 0 has to supply the data has a miss core 0 has to supply the data so so far we had 1 and 1 and then what so so far we had 1 and 1 and then what so so far we had 1 and 1 and then what happens is core 0 writes we know that happens is core 0 writes we know that happens is core 0 writes we know that there is a sharer now because this core there is a sharer now because this core there is a sharer now because this core has a block so we need to send out an has a block so we need to send out an has a block so we need to send out an invalidation that means that when core 1 invalidation that means that when core 1 invalidation that means that when core 1 reads next it's going to have a Miss and reads next it's going to have a Miss and reads next it's going to have a Miss and use the bus so this will repeat a use the bus so this will repeat a use the bus so this will repeat a thousand times and we get a total of thousand times and we get a total of thousand times and we get a total of 2,000 bus uses so as you can see right 2,000 bus uses so as you can see right 2,000 bus uses so as you can see right update is a lot more efficient than update is a lot more efficient than update is a lot more efficient than write invalidate write invalidate write invalidate if we actually have a pattern where one if we actually have a pattern where one if we actually have a pattern where one core produces the data continuously and core produces the data continuously and core produces the data continuously and the other one keeps reading the data if the other one keeps reading the data if the other one keeps reading the data if we have a pattern in which one core is we have a pattern in which one core is we have a pattern in which one core is the producer of all the data and the the producer of all the data and the the producer of all the data and the other core is the consumer because the other core is the consumer because the other core is the consumer because the data flow from the producer to the data flow from the producer to the data flow from the producer to the consumer through the same memory consumer through the same memory consumer through the same memory location in a write-update protocol location in a write-update protocol location in a write-update protocol involves just the update by the right involves just the update by the right involves just the update by the right but the read can then find the data in but the read can then find the data in but the read can then find the data in the cache whereas in the invalidation the cache whereas in the invalidation the cache whereas in the invalidation protocol the write just sends out an protocol the write just sends out an protocol the write just sends out an invalidation and then the reader has to invalidation and then the reader has to invalidation and then the reader has to still suffer image to obtain the data


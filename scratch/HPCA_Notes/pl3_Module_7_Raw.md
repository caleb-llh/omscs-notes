# Playlist 3 Module 7 Raw Transcript

## DRAM Technology Quiz： Part 4
Kind: captions Language: en so let's see if we understood how DRM is so let's see if we understood how DRM is so let's see if we understood how DRM is built so what is the reason for not built so what is the reason for not built so what is the reason for not using a normal transistor and a real using a normal transistor and a real using a normal transistor and a real capacitor when making DRM is it because capacitor when making DRM is it because capacitor when making DRM is it because the trench cell is easier to make than a the trench cell is easier to make than a the trench cell is easier to make than a normal capacitor and transistor is it normal capacitor and transistor is it normal capacitor and transistor is it because the trench cell is more reliable because the trench cell is more reliable because the trench cell is more reliable or is it because the trench cell lets us or is it because the trench cell lets us or is it because the trench cell lets us make cheaper DRM chips

## DRAM Technology Solution Quiz： Part 4
Kind: captions Language: en let's look at the answer to a dram let's look at the answer to a dram let's look at the answer to a dram technology quiz which was asking us why technology quiz which was asking us why technology quiz which was asking us why not use a normal transistor and a not use a normal transistor and a not use a normal transistor and a capacitor when making dram is it because capacitor when making dram is it because capacitor when making dram is it because trench cell is easier to make this is trench cell is easier to make this is trench cell is easier to make this is not true the trench cell is not easier not true the trench cell is not easier not true the trench cell is not easier to make it requires us to bury something to make it requires us to bury something to make it requires us to bury something into the Silicon so this one is actually into the Silicon so this one is actually into the Silicon so this one is actually incorrect trench cells are more incorrect trench cells are more incorrect trench cells are more difficult to make than capacitors difficult to make than capacitors difficult to make than capacitors technologically trench cell is more technologically trench cell is more technologically trench cell is more reliable this is also not true trench reliable this is also not true trench reliable this is also not true trench cell lets us make cheaper dram chips it cell lets us make cheaper dram chips it cell lets us make cheaper dram chips it looks like trench cells are more looks like trench cells are more looks like trench cells are more difficult to make so the chips will be difficult to make so the chips will be difficult to make so the chips will be more more more expensive but remember that the cost of expensive but remember that the cost of expensive but remember that the cost of a chip grows quickly when the area is a chip grows quickly when the area is a chip grows quickly when the area is increased and decreases quickly when the increased and decreases quickly when the increased and decreases quickly when the area is decreased the trench cell area is decreased the trench cell area is decreased the trench cell occupies a lot less area on the chip occupies a lot less area on the chip occupies a lot less area on the chip than the transistor and the capacitor than the transistor and the capacitor than the transistor and the capacitor normally would so if we want to build a normally would so if we want to build a normally would so if we want to build a chip that has the same number of bit chip that has the same number of bit chip that has the same number of bit with the trench cell or this way the with the trench cell or this way the with the trench cell or this way the trench Cell results in much smaller and trench Cell results in much smaller and trench Cell results in much smaller and thus way way less expensive chips so thus way way less expensive chips so thus way way less expensive chips so this really is the correct answer

## Memory Chip Organization Part 1： Part 4
Kind: captions Language: en so we have seen what a single bit in so we have seen what a single bit in so we have seen what a single bit in memory looks like now let's see how the memory looks like now let's see how the memory looks like now let's see how the whole chip is organized we said already whole chip is organized we said already whole chip is organized we said already that we have these word lines that that we have these word lines that that we have these word lines that activate cells we have a number of word activate cells we have a number of word activate cells we have a number of word lines and the thing that decides which lines and the thing that decides which lines and the thing that decides which word line gets activated is called a row word line gets activated is called a row word line gets activated is called a row decoder what we give to the row decoder decoder what we give to the row decoder decoder what we give to the row decoder is some bits of the address that tell it is some bits of the address that tell it is some bits of the address that tell it which of the word lines to activate it which of the word lines to activate it which of the word lines to activate it can only activate one word line at a can only activate one word line at a can only activate one word line at a time so this is a real decoder you give time so this is a real decoder you give time so this is a real decoder you give it a number it activates a line that it a number it activates a line that it a number it activates a line that corresponds to that number in memory we corresponds to that number in memory we corresponds to that number in memory we call this row address there is also a call this row address there is also a call this row address there is also a bit line here and if you remember a bit line here and if you remember a bit line here and if you remember a memory cell exists at every intersection memory cell exists at every intersection memory cell exists at every intersection between this bit line and a word line so between this bit line and a word line so between this bit line and a word line so what a word line does is it connects what a word line does is it connects what a word line does is it connects this cell this cell this cell or this this cell this cell this cell or this this cell this cell this cell or this cell to the bit line cell to the bit line cell to the bit line so by supplying let's say two bits of so by supplying let's say two bits of so by supplying let's say two bits of the address we choose which of the four the address we choose which of the four the address we choose which of the four bits would be outputting on this one bit bits would be outputting on this one bit bits would be outputting on this one bit line of course there are more bit lines line of course there are more bit lines line of course there are more bit lines than just one and this for example is a than just one and this for example is a than just one and this for example is a 16bit memory it's a 4x4 bit memory four 16bit memory it's a 4x4 bit memory four 16bit memory it's a 4x4 bit memory four bits can output to the same bit line and bits can output to the same bit line and bits can output to the same bit line and there are four bits activated by each of there are four bits activated by each of there are four bits activated by each of the word lines so when we select the row the word lines so when we select the row the word lines so when we select the row four bits get output out of here now bit four bits get output out of here now bit four bits get output out of here now bit lines are very long and as we said the lines are very long and as we said the lines are very long and as we said the cell is either a relatively weak SRAM cell is either a relatively weak SRAM cell is either a relatively weak SRAM cell so it will slowly pull the bit line cell so it will slowly pull the bit line cell so it will slowly pull the bit line one way or the other or it is a dram one way or the other or it is a dram one way or the other or it is a dram cell that discharges are relatively cell that discharges are relatively cell that discharges are relatively small capacitor into this relatively small capacitor into this relatively small capacitor into this relatively long bit line if we discharge a small long bit line if we discharge a small long bit line if we discharge a small capacitor into a long bit line the capacitor into a long bit line the capacitor into a long bit line the voltage on the bit line will change but voltage on the bit line will change but voltage on the bit line will change but it will change relatively little it will it will change relatively little it will it will change relatively little it will not change all the way to the level that not change all the way to the level that not change all the way to the level that corresponds to a one or to a zero if we corresponds to a one or to a zero if we corresponds to a one or to a zero if we have a weak cell we don't want to wait have a weak cell we don't want to wait have a weak cell we don't want to wait for that cell to raise the whole bit for that cell to raise the whole bit for that cell to raise the whole bit line one way or the other this is why line one way or the other this is why line one way or the other this is why the bit lines are connected to a device the bit lines are connected to a device the bit lines are connected to a device called sense amplifier what it does is called sense amplifier what it does is called sense amplifier what it does is it senses the small changes on the bit it senses the small changes on the bit it senses the small changes on the bit line and amplifies them so it's really line and amplifies them so it's really line and amplifies them so it's really helping the cell raise or lower the helping the cell raise or lower the helping the cell raise or lower the voltage on the bit line and it has voltage on the bit line and it has voltage on the bit line and it has relatively powerful circuitry for each relatively powerful circuitry for each relatively powerful circuitry for each bit line so it's significantly bigger bit line so it's significantly bigger bit line so it's significantly bigger than a single row of cells but you only than a single row of cells but you only than a single row of cells but you only need one of these at the end of the bit need one of these at the end of the bit need one of these at the end of the bit line you don't need one of these at line you don't need one of these at line you don't need one of these at every cell so you have relatively small every cell so you have relatively small every cell so you have relatively small and weak cells and you have this beefy and weak cells and you have this beefy and weak cells and you have this beefy thing here that is helping them raise or thing here that is helping them raise or thing here that is helping them raise or lower the bit line the signals that are lower the bit line the signals that are lower the bit line the signals that are produced by the sense amplifier which produced by the sense amplifier which produced by the sense amplifier which are now correctly one or zero bits go to are now correctly one or zero bits go to are now correctly one or zero bits go to a storage element called row buffer the a storage element called row buffer the a storage element called row buffer the row buffer stores the correct values row buffer stores the correct values row buffer stores the correct values that we read from the whole row of cells that we read from the whole row of cells that we read from the whole row of cells so in this case the row buffer will so in this case the row buffer will so in this case the row buffer will contain four bits because that's how contain four bits because that's how contain four bits because that's how many bits are there in the row the row many bits are there in the row the row many bits are there in the row the row buffer feeds the data it latched to buffer feeds the data it latched to buffer feeds the data it latched to another decoder that is called the another decoder that is called the another decoder that is called the column decoder this decoder selects the column decoder this decoder selects the column decoder this decoder selects the correct bit among these four let's say correct bit among these four let's say correct bit among these four let's say using the column address which is using the column address which is using the column address which is another part of the data address and it another part of the data address and it another part of the data address and it outputs a single bit if we want to build outputs a single bit if we want to build outputs a single bit if we want to build something that has more than just one something that has more than just one something that has more than just one bit of data for each location we will bit of data for each location we will bit of data for each location we will replicate this so we will have let's say replicate this so we will have let's say replicate this so we will have let's say two of these give them the same row and two of these give them the same row and two of these give them the same row and column address and now they output a column address and now they output a column address and now they output a 2bit value

## Memory Chip Organization Part 2： Part 4
Kind: captions Language: en so we just described how to read a row so we just described how to read a row so we just described how to read a row into the sense amplifier and the row into the sense amplifier and the row into the sense amplifier and the row buffer and then get one bit out of that buffer and then get one bit out of that buffer and then get one bit out of that row so let's suppose that the bits we row so let's suppose that the bits we row so let's suppose that the bits we had here were one one1 and let's say had here were one one1 and let's say had here were one one1 and let's say this was theam so these bits drained this was theam so these bits drained this was theam so these bits drained into the bit line the sense amplifier into the bit line the sense amplifier into the bit line the sense amplifier amplifies them outputs here one 1 1 1 amplifies them outputs here one 1 1 1 amplifies them outputs here one 1 1 1 the row buffer now stores this value and the row buffer now stores this value and the row buffer now stores this value and starts outputting it here let's say that starts outputting it here let's say that starts outputting it here let's say that the column address finally selects this the column address finally selects this the column address finally selects this bit for to be output so we output a z bit for to be output so we output a z bit for to be output so we output a z remember that D reads are destructive remember that D reads are destructive remember that D reads are destructive once we do that we no longer have our once we do that we no longer have our once we do that we no longer have our bits here so what needs to be done is bits here so what needs to be done is bits here so what needs to be done is after the sense amplifier determines after the sense amplifier determines after the sense amplifier determines what the correct values of the bits are what the correct values of the bits are what the correct values of the bits are and the cells by now have been exhausted and the cells by now have been exhausted and the cells by now have been exhausted we reverse the direction and raise each we reverse the direction and raise each we reverse the direction and raise each of the bit lines to its proper value so of the bit lines to its proper value so of the bit lines to its proper value so now the sense amplifier is driving these now the sense amplifier is driving these now the sense amplifier is driving these correct values back into the cells and correct values back into the cells and correct values back into the cells and the cells get their values again so the cells get their values again so the cells get their values again so destructive read from theam means that destructive read from theam means that destructive read from theam means that really we have to read and then write really we have to read and then write really we have to read and then write each cell so we cannot just wait long each cell so we cannot just wait long each cell so we cannot just wait long enough to get the value we also have to enough to get the value we also have to enough to get the value we also have to wait long enough to put the value back wait long enough to put the value back wait long enough to put the value back in the cell this is one of the reasons in the cell this is one of the reasons in the cell this is one of the reasons why dram is slower than SRAM another why dram is slower than SRAM another why dram is slower than SRAM another reason of course is that the cell does reason of course is that the cell does reason of course is that the cell does not as strongly pull the bit line so the not as strongly pull the bit line so the not as strongly pull the bit line so the sense amplifier needs more time to sense amplifier needs more time to sense amplifier needs more time to figure out what it has after after this figure out what it has after after this figure out what it has after after this read and then write approach the cells read and then write approach the cells read and then write approach the cells have the correct values for one one1 and have the correct values for one one1 and have the correct values for one one1 and have been refreshed even if they have have been refreshed even if they have have been refreshed even if they have reduced to let's say 90% of what they reduced to let's say 90% of what they reduced to let's say 90% of what they should have once we read them out and should have once we read them out and should have once we read them out and put it back in we put back in the full put it back in we put back in the full put it back in we put back in the full value so now they can again leak for a value so now they can again leak for a value so now they can again leak for a while before we need to read and then while before we need to read and then while before we need to read and then write them again so refresh is really write them again so refresh is really write them again so refresh is really make sure that each of the rows is make sure that each of the rows is make sure that each of the rows is readed every once in a while if the time readed every once in a while if the time readed every once in a while if the time for the cell to lose the value enough so for the cell to lose the value enough so for the cell to lose the value enough so that we cannot recover it is some sort that we cannot recover it is some sort that we cannot recover it is some sort of time T then every row needs to be of time T then every row needs to be of time T then every row needs to be read and then written within the time t read and then written within the time t read and then written within the time t a destructive read and then a write will a destructive read and then a write will a destructive read and then a write will refesh the row but we cannot rely on the refesh the row but we cannot rely on the refesh the row but we cannot rely on the processor to access every Row in the processor to access every Row in the processor to access every Row in the memory so that we don't have to do memory so that we don't have to do memory so that we don't have to do anything else in fact when you have anything else in fact when you have anything else in fact when you have caches it often happens that some rows caches it often happens that some rows caches it often happens that some rows that are very popular and access by the that are very popular and access by the that are very popular and access by the processor all the time are actually the processor all the time are actually the processor all the time are actually the ones that don't get refreshed this way ones that don't get refreshed this way ones that don't get refreshed this way and the reason is that those become cach and the reason is that those become cach and the reason is that those become cach hits and the cach keeps keeping that hits and the cach keeps keeping that hits and the cach keeps keeping that data because the processor wants it so data because the processor wants it so data because the processor wants it so often so we end up actually not often so we end up actually not often so we end up actually not accessing the memory for things that the accessing the memory for things that the accessing the memory for things that the processor is accessing the most often so processor is accessing the most often so processor is accessing the most often so we cannot even rely on the program to go we cannot even rely on the program to go we cannot even rely on the program to go through the whole memory so what needs through the whole memory so what needs through the whole memory so what needs to happen for refresh is that we have to happen for refresh is that we have to happen for refresh is that we have this refresh row counter it starts at this refresh row counter it starts at this refresh row counter it starts at zero we use it to refresh this row then zero we use it to refresh this row then zero we use it to refresh this row then it moves to one we then use it to it moves to one we then use it to it moves to one we then use it to refresh this row and so on if a row refresh this row and so on if a row refresh this row and so on if a row needs to be refreshed within some needs to be refreshed within some needs to be refreshed within some refresh period let's say t and we have n refresh period let's say t and we have n refresh period let's say t and we have n rows then every T Over N seconds we have rows then every T Over N seconds we have rows then every T Over N seconds we have to refresh one row so that by the time to refresh one row so that by the time to refresh one row so that by the time we are done we are ready to refresh we are done we are ready to refresh we are done we are ready to refresh again a row which would only last for a again a row which would only last for a again a row which would only last for a period of T modern dams have a lot of period of T modern dams have a lot of period of T modern dams have a lot of rows and the refresh period is well rows and the refresh period is well rows and the refresh period is well under a second so we are actually seeing under a second so we are actually seeing under a second so we are actually seeing a lot of refreshes happen every second a lot of refreshes happen every second a lot of refreshes happen every second this is not something that happens this is not something that happens this is not something that happens occasionally this actually significantly occasionally this actually significantly occasionally this actually significantly interferes with when we can read and interferes with when we can read and interferes with when we can read and write our memory cuz while a refresh is write our memory cuz while a refresh is write our memory cuz while a refresh is going on we cannot do a useful read going on we cannot do a useful read going on we cannot do a useful read because the row decoder sense amps and because the row decoder sense amps and because the row decoder sense amps and the row buffer are used by the refresh the row buffer are used by the refresh the row buffer are used by the refresh operation which is reading a row that we operation which is reading a row that we operation which is reading a row that we didn't need and writing it back didn't need and writing it back didn't need and writing it back meanwhile we have to sit and wait with a meanwhile we have to sit and wait with a meanwhile we have to sit and wait with a real read until that gets done so that real read until that gets done so that real read until that gets done so that we can move on

## Memory Refresh Quiz： Part 4
Kind: captions Language: en so let's see if we understood the so let's see if we understood the so let's see if we understood the relationship between refreshes and reads relationship between refreshes and reads relationship between refreshes and reads suppose we have a memory array that has suppose we have a memory array that has suppose we have a memory array that has 496 rows and 248 columns and suppose 496 rows and 248 columns and suppose 496 rows and 248 columns and suppose that the refresh period for this array that the refresh period for this array that the refresh period for this array is 500 micros every cell needs to be is 500 micros every cell needs to be is 500 micros every cell needs to be refreshed within this time otherwise it refreshed within this time otherwise it refreshed within this time otherwise it will lose data now suppose that the will lose data now suppose that the will lose data now suppose that the timing for reads for this memory is as timing for reads for this memory is as timing for reads for this memory is as follows it takes us four NS to select a follows it takes us four NS to select a follows it takes us four NS to select a row row row once we activate the row 10 ns are once we activate the row 10 ns are once we activate the row 10 ns are needed for the sense amplifier to get needed for the sense amplifier to get needed for the sense amplifier to get the bit values from that the bit values from that the bit values from that row two NCS are needed to put that data row two NCS are needed to put that data row two NCS are needed to put that data in the row in the row in the row buffer and four ns are needed for the buffer and four ns are needed for the buffer and four ns are needed for the column decoder to actually figure out column decoder to actually figure out column decoder to actually figure out which bit from the row buffer we need which bit from the row buffer we need which bit from the row buffer we need while this is happening we are writing while this is happening we are writing while this is happening we are writing the data back from the sense amplifier the data back from the sense amplifier the data back from the sense amplifier to the memory row and it takes us 11 NS to the memory row and it takes us 11 NS to the memory row and it takes us 11 NS to do that so this part overlaps with to do that so this part overlaps with to do that so this part overlaps with these two so the question for you is how these two so the question for you is how these two so the question for you is how many data or non refresh reads per many data or non refresh reads per many data or non refresh reads per second can this memory support put your second can this memory support put your second can this memory support put your answer here

## Memory Refresh Quiz Solution： Part 4
Kind: captions Language: en let's look at the solution to our memory let's look at the solution to our memory let's look at the solution to our memory refresh quiz we have a memory with 4,096 refresh quiz we have a memory with 4,096 refresh quiz we have a memory with 4,096 rows and this many columns it needs to rows and this many columns it needs to rows and this many columns it needs to be refreshed every 500 microc and this be refreshed every 500 microc and this be refreshed every 500 microc and this is a read timing where these two steps is a read timing where these two steps is a read timing where these two steps and this one overlap so basically it and this one overlap so basically it and this one overlap so basically it takes us 4 + takes us 4 + takes us 4 + 10 plus either 11 or 2 + 4 Nan seconds 10 plus either 11 or 2 + 4 Nan seconds 10 plus either 11 or 2 + 4 Nan seconds before we complete one read and can before we complete one read and can before we complete one read and can proceed with the next one proceed with the next one proceed with the next one because 2 + 4 is 6 and 11 nond is longer because 2 + 4 is 6 and 11 nond is longer because 2 + 4 is 6 and 11 nond is longer that means that really the timing for a that means that really the timing for a that means that really the timing for a read will be to select the read will be to select the read will be to select the row so we need to select a row get the row so we need to select a row get the row so we need to select a row get the values from the row and then write the values from the row and then write the values from the row and then write the data back during this time the row needs data back during this time the row needs data back during this time the row needs to remain selected so the new read to remain selected so the new read to remain selected so the new read really cannot begin until 4 + 10 + 11 really cannot begin until 4 + 10 + 11 really cannot begin until 4 + 10 + 11 NCS into it so the read takes 25 NCS into it so the read takes 25 NCS into it so the read takes 25 NS and that means we could be doing 40 NS and that means we could be doing 40 NS and that means we could be doing 40 million reads per second if all we had million reads per second if all we had million reads per second if all we had to do is do reads and this is where it to do is do reads and this is where it to do is do reads and this is where it becomes tricky because during a second becomes tricky because during a second becomes tricky because during a second we need to do a number of refreshes the we need to do a number of refreshes the we need to do a number of refreshes the refresh period is 500 microc seconds so refresh period is 500 microc seconds so refresh period is 500 microc seconds so the number of refreshes we have to do is the number of refreshes we have to do is the number of refreshes we have to do is 2,000 times per second we need to do a 2,000 times per second we need to do a 2,000 times per second we need to do a refresh of each of the rows to do that refresh of each of the rows to do that refresh of each of the rows to do that the row needs to be selected we need to the row needs to be selected we need to the row needs to be selected we need to get the bit value and write it back so get the bit value and write it back so get the bit value and write it back so it takes the same time to do a refresh it takes the same time to do a refresh it takes the same time to do a refresh of the row as it takes to do a read of the row as it takes to do a read of the row as it takes to do a read meanwhile our memory cannot be doing the meanwhile our memory cannot be doing the meanwhile our memory cannot be doing the read so really we are spending 2,000 * read so really we are spending 2,000 * read so really we are spending 2,000 * 496 time 25 NS on refreshes but because 496 time 25 NS on refreshes but because 496 time 25 NS on refreshes but because they have the same timing as reads we they have the same timing as reads we they have the same timing as reads we can just compute how many refreshes we can just compute how many refreshes we can just compute how many refreshes we have per second and we end up with 8.1 have per second and we end up with 8.1 have per second and we end up with 8.1 192 million refreshes per second and 192 million refreshes per second and 192 million refreshes per second and that means that we cannot do all 40 that means that we cannot do all 40 that means that we cannot do all 40 million reads pretty much out of these million reads pretty much out of these million reads pretty much out of these really this was assuming that we don't really this was assuming that we don't really this was assuming that we don't do refreshes so because we have to do do refreshes so because we have to do do refreshes so because we have to do eight and change million refreshes we eight and change million refreshes we eight and change million refreshes we have that many less reads that we can do have that many less reads that we can do have that many less reads that we can do so the overall result here is the so the overall result here is the so the overall result here is the difference between the two that's how difference between the two that's how difference between the two that's how many reads do we have left after we have many reads do we have left after we have many reads do we have left after we have spent time on eight and change million spent time on eight and change million spent time on eight and change million refreshes so the final answer here is refreshes so the final answer here is refreshes so the final answer here is the difference between these which is 31 the difference between these which is 31 the difference between these which is 31 mil mil mil 88,000 reads per second so again without 88,000 reads per second so again without 88,000 reads per second so again without refreshes we would be able to do 40 refreshes we would be able to do 40 refreshes we would be able to do 40 million but because refreshes need to million but because refreshes need to million but because refreshes need to happen we only have this many reads left happen we only have this many reads left happen we only have this many reads left in a second

## Memory Chip Organization Part 3： Part 4
Kind: captions Language: en How do we do writes to this memory? How do we do writes to this memory? How do we do writes to this memory? Obviously, we need to change one of the Obviously, we need to change one of the Obviously, we need to change one of the cells. How do we do that? We use the row cells. How do we do that? We use the row cells. How do we do that? We use the row address to select the row of the cell. address to select the row of the cell. address to select the row of the cell. But we can only select a whole row of But we can only select a whole row of But we can only select a whole row of cells. We cannot just select an cells. We cannot just select an cells. We cannot just select an individual cell. So, if this is bit individual cell. So, if this is bit individual cell. So, if this is bit number seven in our 32-bit word, we number seven in our 32-bit word, we number seven in our 32-bit word, we select the address of the word and then select the address of the word and then select the address of the word and then we want to supply a bit here. Let's say we want to supply a bit here. Let's say we want to supply a bit here. Let's say A1 to be written here. So, how do we do A1 to be written here. So, how do we do A1 to be written here. So, how do we do that? because we can only select a whole that? because we can only select a whole that? because we can only select a whole row. Once we select the row, all of the row. Once we select the row, all of the row. Once we select the row, all of the cells get connected to the bit lines. We cells get connected to the bit lines. We cells get connected to the bit lines. We can maybe drive the bit line for this can maybe drive the bit line for this can maybe drive the bit line for this one to write the one. But that would one to write the one. But that would one to write the one. But that would lose us these bits. So the way the write lose us these bits. So the way the write lose us these bits. So the way the write needs to happen is we use the row needs to happen is we use the row needs to happen is we use the row address, select the row, read out these address, select the row, read out these address, select the row, read out these bits, the sense bits, the sense bits, the sense amplifies them, and we latch them into amplifies them, and we latch them into amplifies them, and we latch them into the row buffer. Now we write this bit to the row buffer. Now we write this bit to the row buffer. Now we write this bit to the row buffer at this position. And now the row buffer at this position. And now the row buffer at this position. And now we write the whole thing back into the we write the whole thing back into the we write the whole thing back into the cells. The cells lost their values when cells. The cells lost their values when cells. The cells lost their values when we did the read anyway. We would have to we did the read anyway. We would have to we did the read anyway. We would have to write the values back. Anyway, before we write the values back. Anyway, before we write the values back. Anyway, before we do that, we make all the writes that we do that, we make all the writes that we do that, we make all the writes that we want to that row and then send them want to that row and then send them want to that row and then send them back. So that when we are writing, we back. So that when we are writing, we back. So that when we are writing, we end up writing one one and this bit is end up writing one one and this bit is end up writing one one and this bit is changed to one. So a write in DM is also changed to one. So a write in DM is also changed to one. So a write in DM is also a read and then write operation.

## Fast Page Mode： Part 4
Kind: captions Language: en We have seen that DM accesses are slow We have seen that DM accesses are slow We have seen that DM accesses are slow because really we have to use the row because really we have to use the row because really we have to use the row decoder to select the row then get those decoder to select the row then get those decoder to select the row then get those bits onto the bit lines then sense bits onto the bit lines then sense bits onto the bit lines then sense what's on the bit lines and so on and what's on the bit lines and so on and what's on the bit lines and so on and only then we get the values in the row only then we get the values in the row only then we get the values in the row buffer from which we can read them and buffer from which we can read them and buffer from which we can read them and send them back to the processor. But not send them back to the processor. But not send them back to the processor. But not all of the DM accesses have to be that all of the DM accesses have to be that all of the DM accesses have to be that slow. So we will discuss a technique slow. So we will discuss a technique slow. So we will discuss a technique called fast page mode that makes some of called fast page mode that makes some of called fast page mode that makes some of the DM accesses a lot faster than the DM accesses a lot faster than the DM accesses a lot faster than others. Remember our organization of the others. Remember our organization of the others. Remember our organization of the DM. We use some part of the address to DM. We use some part of the address to DM. We use some part of the address to select a row. That row of bits gets select a row. That row of bits gets select a row. That row of bits gets output to the sense amplifier. We then output to the sense amplifier. We then output to the sense amplifier. We then latch those into the row buffer. And latch those into the row buffer. And latch those into the row buffer. And then from the row buffer we use the then from the row buffer we use the then from the row buffer we use the column address to select a bit to column address to select a bit to column address to select a bit to output. And then from the row buffer the output. And then from the row buffer the output. And then from the row buffer the column decoder using the column address column decoder using the column address column decoder using the column address selects the actual bit to output. Note selects the actual bit to output. Note selects the actual bit to output. Note that once we have done all of this the that once we have done all of this the that once we have done all of this the row buffer can retain the entire row. So row buffer can retain the entire row. So row buffer can retain the entire row. So if we want to read a bit from the same if we want to read a bit from the same if we want to read a bit from the same row we don't need to go through the row we don't need to go through the row we don't need to go through the whole row address sense amplifier whole row address sense amplifier whole row address sense amplifier row buffer mechanism again. What we can row buffer mechanism again. What we can row buffer mechanism again. What we can do is simply keep what we have in the do is simply keep what we have in the do is simply keep what we have in the row buffer and just change the column row buffer and just change the column row buffer and just change the column address. Then the bit simply gets read address. Then the bit simply gets read address. Then the bit simply gets read out of the row buffer without a new DM out of the row buffer without a new DM out of the row buffer without a new DM access. So this approach of finding what access. So this approach of finding what access. So this approach of finding what we need in the row buffer is called fast we need in the row buffer is called fast we need in the row buffer is called fast page mode. It's not called fast row mode page mode. It's not called fast row mode page mode. It's not called fast row mode just because some people prefer to call just because some people prefer to call just because some people prefer to call some people prefer to call a memory row some people prefer to call a memory row some people prefer to call a memory row a page. It usually has thousands of a page. It usually has thousands of a page. It usually has thousands of bits. So people call it a page. It has bits. So people call it a page. It has bits. So people call it a page. It has nothing to do with the pages that we use nothing to do with the pages that we use nothing to do with the pages that we use for virtual memory. It's just a name for for virtual memory. It's just a name for for virtual memory. It's just a name for something that has a lot of bits in this something that has a lot of bits in this something that has a lot of bits in this case. So fast page mode works this way. case. So fast page mode works this way. case. So fast page mode works this way. We do what is called opening a page. We do what is called opening a page. We do what is called opening a page. That's that amounts to providing a row That's that amounts to providing a row That's that amounts to providing a row address, selecting a row, send address, selecting a row, send address, selecting a row, send amplification of what the cells output amplification of what the cells output amplification of what the cells output and latching what we get into the row and latching what we get into the row and latching what we get into the row buffer. Now we can do a series of reads buffer. Now we can do a series of reads buffer. Now we can do a series of reads and writes to the page which really and writes to the page which really and writes to the page which really happens to the row buffer. We can read happens to the row buffer. We can read happens to the row buffer. We can read or we can modify bits in the row buffer. or we can modify bits in the row buffer. or we can modify bits in the row buffer. Finally, when we want to do something on Finally, when we want to do something on Finally, when we want to do something on another row in memory, we do what is another row in memory, we do what is another row in memory, we do what is called closing the page. We now use the called closing the page. We now use the called closing the page. We now use the sense amplifiers to write back to the sense amplifiers to write back to the sense amplifiers to write back to the row what we have in a row buffer. It row what we have in a row buffer. It row what we have in a row buffer. It could be different data than what we could be different data than what we could be different data than what we read because we have been modifying it read because we have been modifying it read because we have been modifying it also in between. And this way if we want also in between. And this way if we want also in between. And this way if we want to do a single read, we would have to to do a single read, we would have to to do a single read, we would have to open a page, do one read, then close the open a page, do one read, then close the open a page, do one read, then close the page to write back things to our memory page to write back things to our memory page to write back things to our memory cells because of the destructive read. cells because of the destructive read. cells because of the destructive read. And then finally, we have to do what is And then finally, we have to do what is And then finally, we have to do what is called closing the page, which amounts called closing the page, which amounts called closing the page, which amounts to writing the data from the row buffer to writing the data from the row buffer to writing the data from the row buffer back into the row from which we read it. back into the row from which we read it. back into the row from which we read it. So if we want to do a single memory So if we want to do a single memory So if we want to do a single memory read, we would open a page read, we would open a page read, we would open a page The data is now in the row buffer and no The data is now in the row buffer and no The data is now in the row buffer and no longer in the row. Do the read from the longer in the row. Do the read from the longer in the row. Do the read from the row buffer and then close the page which row buffer and then close the page which row buffer and then close the page which causes the data to be sent back and causes the data to be sent back and causes the data to be sent back and written back to the row. We can do a written back to the row. We can do a written back to the row. We can do a single write which amounts to opening a single write which amounts to opening a single write which amounts to opening a page. The data lines up in the row page. The data lines up in the row page. The data lines up in the row buffer. We do a single write. So we buffer. We do a single write. So we buffer. We do a single write. So we modify a bit in the row buffer and then modify a bit in the row buffer and then modify a bit in the row buffer and then we close the page which causes the row we close the page which causes the row we close the page which causes the row to get the new data. and some of the to get the new data. and some of the to get the new data. and some of the bits may not have changed which is why bits may not have changed which is why bits may not have changed which is why we had to read in the first place. But we had to read in the first place. But we had to read in the first place. But we can do a lot more than just one we can do a lot more than just one we can do a lot more than just one operation in between opening and closing operation in between opening and closing operation in between opening and closing a page. So usually it's advantageous to a page. So usually it's advantageous to a page. So usually it's advantageous to open a page, do all the reads and writes open a page, do all the reads and writes open a page, do all the reads and writes that we can to that page before we close that we can to that page before we close that we can to that page before we close the page and open another page.

## DRAM Access Scheduling Quiz： Part 4
Kind: captions Language: en so let's see if we can do some dram so let's see if we can do some dram so let's see if we can do some dram access scheduling to take advantage of access scheduling to take advantage of access scheduling to take advantage of the fast page access that we can have the fast page access that we can have the fast page access that we can have let's assume we have a data memory that let's assume we have a data memory that let's assume we have a data memory that has 32 one bit arrays just like the one has 32 one bit arrays just like the one has 32 one bit arrays just like the one we have seen before each array is one we have seen before each array is one we have seen before each array is one megabit and it's organized as megabit and it's organized as megabit and it's organized as two to the tenth rows two to the tenth rows two to the tenth rows times two to the tenth bits per row times two to the tenth bits per row times two to the tenth bits per row each array is a 16 megabit array each array is a 16 megabit array each array is a 16 megabit array organized as 2 to the 12th organized as 2 to the 12th organized as 2 to the 12th rows which is 4096 rows times 2 to the rows which is 4096 rows times 2 to the rows which is 4096 rows times 2 to the 12 bits per row 12 bits per row 12 bits per row and the 24 bit and the 24 bit and the 24 bit address address address that we feed to this memory where bits that we feed to this memory where bits that we feed to this memory where bits are numbered 0 through 23 is thus are numbered 0 through 23 is thus are numbered 0 through 23 is thus divided by the theorem into a 12 bit row divided by the theorem into a 12 bit row divided by the theorem into a 12 bit row address which is the upper bits of the address which is the upper bits of the address which is the upper bits of the address and the column address which is address and the column address which is address and the column address which is the lower 12 bits of the address of the lower 12 bits of the address of the lower 12 bits of the address of course the processor when it issues this course the processor when it issues this course the processor when it issues this 24-bit physical address wants to access 24-bit physical address wants to access 24-bit physical address wants to access a 32-bit memory location each chip is a 32-bit memory location each chip is a 32-bit memory location each chip is fed this address and the 32 chips fed this address and the 32 chips fed this address and the 32 chips together represent a 32-bit memory so together represent a 32-bit memory so together represent a 32-bit memory so one chip for example might hold bit one chip for example might hold bit one chip for example might hold bit number seven from all the words in the number seven from all the words in the number seven from all the words in the 16 mega word memory let's say we have 16 mega word memory let's say we have 16 mega word memory let's say we have cash misses currently pending for the cash misses currently pending for the cash misses currently pending for the following addresses let's assume that it following addresses let's assume that it following addresses let's assume that it takes 10 nanoseconds to open a page takes 10 nanoseconds to open a page takes 10 nanoseconds to open a page that it takes 2 nanoseconds to read that it takes 2 nanoseconds to read that it takes 2 nanoseconds to read something from the row buffer once the something from the row buffer once the something from the row buffer once the page is open and 5 nanoseconds to close page is open and 5 nanoseconds to close page is open and 5 nanoseconds to close a page a page a page these operations these operations these operations how long do they take how long do they take how long do they take if we do them in this particular order if we do them in this particular order if we do them in this particular order and how long do they take if we are and how long do they take if we are and how long do they take if we are allowed to reorder them allowed to reorder them allowed to reorder them so that the time gets reduced

## DRAM Access Scheduling Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our dram let's look at the solution to our dram let's look at the solution to our dram access scheduling quiz so we have 32 access scheduling quiz so we have 32 access scheduling quiz so we have 32 arrays each is 16 megabits and takes a arrays each is 16 megabits and takes a arrays each is 16 megabits and takes a 12 bit row address and a 12 bit column 12 bit row address and a 12 bit column 12 bit row address and a 12 bit column address address address 12 bits 12 bits 12 bits is 3 hex digits so the row address will is 3 hex digits so the row address will is 3 hex digits so the row address will be the upper three hex digits and the be the upper three hex digits and the be the upper three hex digits and the column address will be the lower column address will be the lower column address will be the lower hex digits each of these hex digits each of these hex digits each of these corresponds to a 32-bit memory location corresponds to a 32-bit memory location corresponds to a 32-bit memory location so what happens is each of these arrays so what happens is each of these arrays so what happens is each of these arrays sees the same address but it provides a sees the same address but it provides a sees the same address but it provides a different bit for that address so each different bit for that address so each different bit for that address so each address will have the same sequence of address will have the same sequence of address will have the same sequence of row and column addresses it's just that row and column addresses it's just that row and column addresses it's just that they will implement different bits of they will implement different bits of they will implement different bits of that address so what we have here is the that address so what we have here is the that address so what we have here is the first one of course needs to be at page first one of course needs to be at page first one of course needs to be at page open open open so we have 10 nanoseconds to open this so we have 10 nanoseconds to open this so we have 10 nanoseconds to open this first page first page first page we will then read we will then read we will then read in two nanoseconds in two nanoseconds in two nanoseconds then we will close the page then we will close the page then we will close the page in another five nanoseconds because the in another five nanoseconds because the in another five nanoseconds because the next one is on a different page next one is on a different page next one is on a different page the row address is different the row address is different the row address is different so we spent 17 nanoseconds here we will so we spent 17 nanoseconds here we will so we spent 17 nanoseconds here we will spend another 17 here spend another 17 here spend another 17 here we will then spend another 17 here we will then spend another 17 here we will then spend another 17 here because it's again a different row because it's again a different row because it's again a different row 17 here 17 here 17 here and here and here and here overall we and here and here and here overall we and here and here and here overall we have seven accesses each to a different have seven accesses each to a different have seven accesses each to a different page page page so each of them requires so each of them requires so each of them requires 17 nanoseconds 17 nanoseconds 17 nanoseconds for a total of for a total of for a total of 119 nanoseconds now let's try to 119 nanoseconds now let's try to 119 nanoseconds now let's try to reorganize these what we want to do when reorganize these what we want to do when reorganize these what we want to do when we reorganize these we reorganize these we reorganize these is once we open a page access everything is once we open a page access everything is once we open a page access everything on that page before we move on which on that page before we move on which on that page before we move on which means means means we will open this row we will open this row we will open this row and then we will access everything in and then we will access everything in and then we will access everything in that row which means this that row which means this that row which means this and this two and this two and this two now what happens is we spend ten now what happens is we spend ten now what happens is we spend ten nanoseconds opening that row nanoseconds opening that row nanoseconds opening that row three times two nanoseconds three times two nanoseconds three times two nanoseconds reading these three things reading these three things reading these three things and then we need to close the row in and then we need to close the row in and then we need to close the row in another 5 nanoseconds another 5 nanoseconds another 5 nanoseconds then what happens is we open the next then what happens is we open the next then what happens is we open the next row and we read everything from that row row and we read everything from that row row and we read everything from that row which includes this two which includes this two which includes this two so we end up opening one row reading two so we end up opening one row reading two so we end up opening one row reading two things from it and then closing it and things from it and then closing it and things from it and then closing it and now we spend now we spend now we spend 17 nanoseconds for each one of these 17 nanoseconds for each one of these 17 nanoseconds for each one of these in total in total in total we spent 74 nanoseconds we spent 74 nanoseconds we spent 74 nanoseconds doing these accesses so as you can see doing these accesses so as you can see doing these accesses so as you can see when caches have a number of pending when caches have a number of pending when caches have a number of pending misses and we need to send these misses misses and we need to send these misses misses and we need to send these misses to memory to memory to memory it pays to rearrange the order of the it pays to rearrange the order of the it pays to rearrange the order of the addresses we are accessing to benefit addresses we are accessing to benefit addresses we are accessing to benefit from the organization of the dram

## Connecting DRAM To The Processor： Part 4
Kind: captions Language: en now that we have seen what dram looks now that we have seen what dram looks now that we have seen what dram looks like let's briefly look at how to like let's briefly look at how to like let's briefly look at how to connect the dram to the processor so we connect the dram to the processor so we connect the dram to the processor so we have our processor it sends its requests have our processor it sends its requests have our processor it sends its requests to level one cach which sends its misses to level one cach which sends its misses to level one cach which sends its misses and write back requests to the larger and write back requests to the larger and write back requests to the larger level two cache which might send its level two cache which might send its level two cache which might send its misses and write back request to an even misses and write back request to an even misses and write back request to an even larger level three cache and let's say larger level three cache and let's say larger level three cache and let's say that these are all on our processor chip that these are all on our processor chip that these are all on our processor chip now what happens is the misses and the now what happens is the misses and the now what happens is the misses and the write back requests from the L3 cache write back requests from the L3 cache write back requests from the L3 cache would be made over an external would be made over an external would be made over an external connection so we need processor pins connection so we need processor pins connection so we need processor pins here and here and here and traditionally this data would go over traditionally this data would go over traditionally this data would go over what is called a front side bus now you what is called a front side bus now you what is called a front side bus now you want to design the processor chip so want to design the processor chip so want to design the processor chip so that you can connect it to many possible that you can connect it to many possible that you can connect it to many possible memories so you don't design the front memories so you don't design the front memories so you don't design the front side bus such that it supplies the row side bus such that it supplies the row side bus such that it supplies the row address and the column address and so on address and the column address and so on address and the column address and so on to the memory chips what you have to the memory chips what you have to the memory chips what you have instead is another chip that contains instead is another chip that contains instead is another chip that contains the memory controller and that memory the memory controller and that memory the memory controller and that memory controller will have what's called a controller will have what's called a controller will have what's called a memory Channel connecting it to a data memory Channel connecting it to a data memory Channel connecting it to a data module and over this channel it will module and over this channel it will module and over this channel it will issue things like open a row read issue things like open a row read issue things like open a row read something get the data write something something get the data write something something get the data write something Supply the data close a row open another Supply the data close a row open another Supply the data close a row open another one and so on and it will usually have one and so on and it will usually have one and so on and it will usually have more than one such memory channel so the more than one such memory channel so the more than one such memory channel so the memory latency as seen by the level memory latency as seen by the level memory latency as seen by the level three cache is not only the access time three cache is not only the access time three cache is not only the access time of the memory array here it includes of the memory array here it includes of the memory array here it includes sending the request over a front side sending the request over a front side sending the request over a front side bus having the memory controller figure bus having the memory controller figure bus having the memory controller figure it out sending the Page open to the it out sending the Page open to the it out sending the Page open to the appropriate Dam sending a request to appropriate Dam sending a request to appropriate Dam sending a request to read supplying the column addresses and read supplying the column addresses and read supplying the column addresses and everything getting the data over the everything getting the data over the everything getting the data over the memory Channel know that you have a memory Channel know that you have a memory Channel know that you have a level three Cash miss that means you level three Cash miss that means you level three Cash miss that means you want a whole Cash Line worth of data to want a whole Cash Line worth of data to want a whole Cash Line worth of data to read so it takes a while to transfer read so it takes a while to transfer read so it takes a while to transfer that data here then the memory that data here then the memory that data here then the memory controller reads it from the memory controller reads it from the memory controller reads it from the memory Channel which usually has one bus Channel which usually has one bus Channel which usually has one bus frequency and sends it at a different frequency and sends it at a different frequency and sends it at a different data rate over the front side bus to the data rate over the front side bus to the data rate over the front side bus to the processor chip which then puts the line processor chip which then puts the line processor chip which then puts the line together and puts it in the L cache so together and puts it in the L cache so together and puts it in the L cache so the latency includes all of this here in the latency includes all of this here in the latency includes all of this here in addition to just the memory axis and addition to just the memory axis and addition to just the memory axis and this can be a significant part of the this can be a significant part of the this can be a significant part of the overall memory overall memory overall memory latency so recent processor chips latency so recent processor chips latency so recent processor chips integrate the memory controller which integrate the memory controller which integrate the memory controller which means that the memory controller is put means that the memory controller is put means that the memory controller is put on the same chip as the processor and on the same chip as the processor and on the same chip as the processor and the caches now we no longer need the the caches now we no longer need the the caches now we no longer need the front side bus we can use lots of onchip front side bus we can use lots of onchip front side bus we can use lots of onchip wiring to communicate with the onchip wiring to communicate with the onchip wiring to communicate with the onchip memory controller so this can be plenty memory controller so this can be plenty memory controller so this can be plenty of bandwidth and very very close this of bandwidth and very very close this of bandwidth and very very close this whole thing is something like 2 by 2 whole thing is something like 2 by 2 whole thing is something like 2 by 2 cm and then we just send request cm and then we just send request cm and then we just send request directly through the memory channels to directly through the memory channels to directly through the memory channels to dram so now the procer chip directly dram so now the procer chip directly dram so now the procer chip directly knows how to talk to drams and open knows how to talk to drams and open knows how to talk to drams and open pages and so on which dramatically can pages and so on which dramatically can pages and so on which dramatically can reduce this part of the latency and reduce this part of the latency and reduce this part of the latency and because it's not a negligible part of because it's not a negligible part of because it's not a negligible part of the overall latency we get our data from the overall latency we get our data from the overall latency we get our data from dram a little bit faster actually 10 20 dram a little bit faster actually 10 20 dram a little bit faster actually 10 20 maybe 30% faster than before but the maybe 30% faster than before but the maybe 30% faster than before but the cost of that cost of that cost of that is that now we designed the processor is that now we designed the processor is that now we designed the processor chip to talk to a specific kind of Dr chip to talk to a specific kind of Dr chip to talk to a specific kind of Dr and because of that we need a relatively and because of that we need a relatively and because of that we need a relatively High degree of standardization of data High degree of standardization of data High degree of standardization of data modules so that for example when we go modules so that for example when we go modules so that for example when we go from 2 GB to 4 GB memory modules we from 2 GB to 4 GB memory modules we from 2 GB to 4 GB memory modules we don't have to redesign our whole chip so don't have to redesign our whole chip so don't have to redesign our whole chip so the protocols here got a lot more the protocols here got a lot more the protocols here got a lot more standardized and uniform and flexible standardized and uniform and flexible standardized and uniform and flexible than before but in exchange we get to than before but in exchange we get to than before but in exchange we get to trans data very quickly between the trans data very quickly between the trans data very quickly between the processor and the memory controller we processor and the memory controller we processor and the memory controller we can make the memory controller now very can make the memory controller now very can make the memory controller now very smart and then access memory in a more smart and then access memory in a more smart and then access memory in a more efficient way

## Memory Outro： Part 4
Kind: captions Language: en in this lesson to whom facade and the d in this lesson to whom facade and the d in this lesson to whom facade and the d friends bad windy window and the sra how friends bad windy window and the sra how friends bad windy window and the sra how they work and where i want to use each they work and where i want to use each they work and where i want to use each of them of them of them maxwell also in this ice-watch him even maxwell also in this ice-watch him even maxwell also in this ice-watch him even lower cost per bed bad arolsen max lower

## Storage Intro： Part 4
Kind: captions Language: en The national press and I'm going to this pastor The national press and I'm going to this pastor The national press and I'm going to this pastor remotely for free, hard drive and older, remotely for free, hard drive and older, remotely for free, hard drive and older, connect to the rest on the computer, connect to the rest on the computer, connect to the rest on the computer, let the defendant understand. Where her let the defendant understand. Where her let the defendant understand. Where her so much slower book, story so much more so much slower book, story so much more so much slower book, story so much more day and many more

## Storage： Part 4
Kind: captions Language: en so let's talk about the role of the so let's talk about the role of the so let's talk about the role of the storage in a computer system it keeps storage in a computer system it keeps storage in a computer system it keeps all the files the programs the data all all the files the programs the data all all the files the programs the data all the settings the operating system the settings the operating system the settings the operating system everything and also everything and also everything and also our virtual memory is implemented using our virtual memory is implemented using our virtual memory is implemented using storage storage storage because we cannot fit all the data that because we cannot fit all the data that because we cannot fit all the data that the application actually wants to access the application actually wants to access the application actually wants to access to physical memory to physical memory to physical memory some of those pages are actually on disk some of those pages are actually on disk some of those pages are actually on disk and when the program accesses them they and when the program accesses them they and when the program accesses them they are loaded into the physical memory are loaded into the physical memory are loaded into the physical memory but meanwhile they're sitting but meanwhile they're sitting but meanwhile they're sitting in our storage for example on our disk in our storage for example on our disk in our storage for example on our disk so storage is important not only to keep so storage is important not only to keep so storage is important not only to keep data but also to extend data but also to extend data but also to extend our virtual memory so that we think we our virtual memory so that we think we our virtual memory so that we think we have more memory than we actually have have more memory than we actually have have more memory than we actually have for both uses of storage we care about for both uses of storage we care about for both uses of storage we care about performance performance performance mainly mainly mainly throughput and latency throughput is how throughput and latency throughput is how throughput and latency throughput is how many bytes per second can we get out of many bytes per second can we get out of many bytes per second can we get out of our for example disk our for example disk our for example disk latency once we request latency once we request latency once we request a page of data when are we going to get a page of data when are we going to get a page of data when are we going to get it back it back it back and in storage this and in storage this and in storage this is improving but not as quickly as the is improving but not as quickly as the is improving but not as quickly as the processor speed is improving whereas the processor speed is improving whereas the processor speed is improving whereas the latency latency latency is improving is improving is improving but very slowly but very slowly but very slowly the reduction in storage latency is even the reduction in storage latency is even the reduction in storage latency is even slower than the reduction in dram slower than the reduction in dram slower than the reduction in dram latency latency latency so processors are getting faster and so processors are getting faster and so processors are getting faster and faster dram is getting faster but faster dram is getting faster but faster dram is getting faster but lagging behind processors and becoming lagging behind processors and becoming lagging behind processors and becoming slower and slower relative to the slower and slower relative to the slower and slower relative to the processors and that storage is falling processors and that storage is falling processors and that storage is falling further behind dram itself in addition further behind dram itself in addition further behind dram itself in addition to performance we are also really to performance we are also really to performance we are also really concerned concerned concerned with reliability if our processor fails with reliability if our processor fails with reliability if our processor fails our system temporarily doesn't work our system temporarily doesn't work our system temporarily doesn't work until we replace the processor but then until we replace the processor but then until we replace the processor but then we can boot up the system and it works we can boot up the system and it works we can boot up the system and it works again but if our disk fails again but if our disk fails again but if our disk fails we lose our programs data settings etc we lose our programs data settings etc we lose our programs data settings etc which is much much worse which is much much worse which is much much worse than just a processor failing so we are than just a processor failing so we are than just a processor failing so we are much more worried about the reliability much more worried about the reliability much more worried about the reliability of our storage than we are about most of of our storage than we are about most of of our storage than we are about most of the other elements of our computer the other elements of our computer the other elements of our computer system system system and finally the types of storage that we and finally the types of storage that we and finally the types of storage that we can use are very diverse we can have can use are very diverse we can have can use are very diverse we can have magnetic disks our traditional hard magnetic disks our traditional hard magnetic disks our traditional hard drives optical disks such as cds and drives optical disks such as cds and drives optical disks such as cds and dvds dvds dvds tape tape tape for backup for backup for backup flash drives that are much faster than flash drives that are much faster than flash drives that are much faster than most of the other ones and so on

## Magnetic Disks： Part 4
Kind: captions Language: en so let's talk about magnetic discs which so let's talk about magnetic discs which so let's talk about magnetic discs which today usually means a hard dis or a hard today usually means a hard dis or a hard today usually means a hard dis or a hard drive but the so-called floppy discs are drive but the so-called floppy discs are drive but the so-called floppy discs are also magnetic discs and work very much also magnetic discs and work very much also magnetic discs and work very much along the same lines it's just that along the same lines it's just that along the same lines it's just that today we mostly have hard drives and few today we mostly have hard drives and few today we mostly have hard drives and few people even remember floppy drives a people even remember floppy drives a people even remember floppy drives a magnetic disc has a spindle to which we magnetic disc has a spindle to which we magnetic disc has a spindle to which we can attach so-called platters all of the can attach so-called platters all of the can attach so-called platters all of the platters are attached to the same platters are attached to the same platters are attached to the same spindle and rotate at the same speed spindle and rotate at the same speed spindle and rotate at the same speed let's say in this direction there is a let's say in this direction there is a let's say in this direction there is a motor here that really drives the motor here that really drives the motor here that really drives the spindle to rotate and that causes all spindle to rotate and that causes all spindle to rotate and that causes all the platters to rotate at the same speed the platters to rotate at the same speed the platters to rotate at the same speed if we look at a single platter it has a if we look at a single platter it has a if we look at a single platter it has a surface covered with Magnetic material surface covered with Magnetic material surface covered with Magnetic material on one side and also on another so a dis on one side and also on another so a dis on one side and also on another so a dis drive that has three platters usually drive that has three platters usually drive that has three platters usually will have a total of six surfaces the will have a total of six surfaces the will have a total of six surfaces the data bits are on all the surfaces we data bits are on all the surfaces we data bits are on all the surfaces we access this data by having a magnetic access this data by having a magnetic access this data by having a magnetic head attached to an arm and another one head attached to an arm and another one head attached to an arm and another one for the lower surface and then all of for the lower surface and then all of for the lower surface and then all of these heads are attached to a head these heads are attached to a head these heads are attached to a head assembly which can move all the heads in assembly which can move all the heads in assembly which can move all the heads in unison so if this head is here this head unison so if this head is here this head unison so if this head is here this head will be in the same position on another will be in the same position on another will be in the same position on another platter and so on we don't individually platter and so on we don't individually platter and so on we don't individually move the heads we move them all at the move the heads we move them all at the move the heads we move them all at the same time so because the platter is same time so because the platter is same time so because the platter is rotating and the head is staying in rotating and the head is staying in rotating and the head is staying in place usually the head will be able to place usually the head will be able to place usually the head will be able to access this circle on the surface of the access this circle on the surface of the access this circle on the surface of the disc each head will be able to access disc each head will be able to access disc each head will be able to access the circle on its own surface and all of the circle on its own surface and all of the circle on its own surface and all of these are going to be at the same these are going to be at the same these are going to be at the same distance from the spindle this is called distance from the spindle this is called distance from the spindle this is called a track all of the tracks at the same a track all of the tracks at the same a track all of the tracks at the same distance from the spindle form what is distance from the spindle form what is distance from the spindle form what is called a cylinder the cylinder simply called a cylinder the cylinder simply called a cylinder the cylinder simply consists of one track from each consists of one track from each consists of one track from each surface where the tracks in the surface where the tracks in the surface where the tracks in the cylinders are those that can be accessed cylinders are those that can be accessed cylinders are those that can be accessed by different heads at the same time so by different heads at the same time so by different heads at the same time so for example here there are six heads for example here there are six heads for example here there are six heads when they stay in this position and all when they stay in this position and all when they stay in this position and all the plats rotate they're able to access the plats rotate they're able to access the plats rotate they're able to access the six tracks of that cylinder the way the six tracks of that cylinder the way the six tracks of that cylinder the way we access different tracks on the disc we access different tracks on the disc we access different tracks on the disc is by moving the head so that it comes is by moving the head so that it comes is by moving the head so that it comes closer or gets further from the spindle closer or gets further from the spindle closer or gets further from the spindle and that way we can access the entire and that way we can access the entire and that way we can access the entire area of each surface so the data area of each surface so the data area of each surface so the data naturally will be organized into bits naturally will be organized into bits naturally will be organized into bits that are positioned along the track and that are positioned along the track and that are positioned along the track and then another track will have different then another track will have different then another track will have different bits and so on if we look at the surface bits and so on if we look at the surface bits and so on if we look at the surface from above and this is now the spindle from above and this is now the spindle from above and this is now the spindle this would be one track and this would this would be one track and this would this would be one track and this would be another and finally on one track we be another and finally on one track we be another and finally on one track we don't store data continuously through don't store data continuously through don't store data continuously through the track because usually a lot of bits the track because usually a lot of bits the track because usually a lot of bits are on a single track instead the data are on a single track instead the data are on a single track instead the data along one track is divided into sectors along one track is divided into sectors along one track is divided into sectors and a sector will be the smallest unit and a sector will be the smallest unit and a sector will be the smallest unit that we can actually read now if the that we can actually read now if the that we can actually read now if the disc rotates in this direction and the disc rotates in this direction and the disc rotates in this direction and the head is currently here as the disc head is currently here as the disc head is currently here as the disc rotates different bits of the sector are rotates different bits of the sector are rotates different bits of the sector are going to be under the head so the sector going to be under the head so the sector going to be under the head so the sector will have some Preamble here that is a will have some Preamble here that is a will have some Preamble here that is a recognizable bit pattern that tells the recognizable bit pattern that tells the recognizable bit pattern that tells the head that this is the beginning of a head that this is the beginning of a head that this is the beginning of a sector then we have the appropriate sector then we have the appropriate sector then we have the appropriate number of the bits for the sector that number of the bits for the sector that number of the bits for the sector that actually hold the data followed usually actually hold the data followed usually actually hold the data followed usually by some check sum and other information by some check sum and other information by some check sum and other information that is needed to possibly correct that is needed to possibly correct that is needed to possibly correct errors in this sector and then another errors in this sector and then another errors in this sector and then another sector will just have the same things sector will just have the same things sector will just have the same things and so on so when the head assembly and so on so when the head assembly and so on so when the head assembly moves to a particular cylinder then the moves to a particular cylinder then the moves to a particular cylinder then the heads start listening for the beginning heads start listening for the beginning heads start listening for the beginning of the sector once they see the of the sector once they see the of the sector once they see the beginning of the sector and see which beginning of the sector and see which beginning of the sector and see which sector it is they know where they are in sector it is they know where they are in sector it is they know where they are in the whole track so the disc capacity can the whole track so the disc capacity can the whole track so the disc capacity can be computed as the number of platters be computed as the number of platters be computed as the number of platters time two this would really be the number time two this would really be the number time two this would really be the number of surfaces times how many tracks we of surfaces times how many tracks we of surfaces times how many tracks we have per surface or expressed in another have per surface or expressed in another have per surface or expressed in another way how many cylinders do we have on way how many cylinders do we have on way how many cylinders do we have on these discs times how many sectors do we these discs times how many sectors do we these discs times how many sectors do we have per track times how many bytes of have per track times how many bytes of have per track times how many bytes of data do we have in each sector usually data do we have in each sector usually data do we have in each sector usually we have a small number of platters such we have a small number of platters such we have a small number of platters such as 1 2 3 maybe four we have thousands of as 1 2 3 maybe four we have thousands of as 1 2 3 maybe four we have thousands of tracks per surface tens to hundreds tracks per surface tens to hundreds tracks per surface tens to hundreds sectors per track and something like a sectors per track and something like a sectors per track and something like a kilobyte or maybe half a kilobyte of kilobyte or maybe half a kilobyte of kilobyte or maybe half a kilobyte of bytes per sector so sectors are usually bytes per sector so sectors are usually bytes per sector so sectors are usually like kilobyte is in size we have about like kilobyte is in size we have about like kilobyte is in size we have about 100 let's say sectors per track maybe a 100 let's say sectors per track maybe a 100 let's say sectors per track maybe a little bit more and we have thousands of little bit more and we have thousands of little bit more and we have thousands of tracks per surface obviously the whole tracks per surface obviously the whole tracks per surface obviously the whole thing needs to be very thin so these thing needs to be very thin so these thing needs to be very thin so these platters are really close to each other platters are really close to each other platters are really close to each other it needs to be let's say 2 and 1/2 in it needs to be let's say 2 and 1/2 in it needs to be let's say 2 and 1/2 in wide total including the head assembly wide total including the head assembly wide total including the head assembly and so on so the platters are only a and so on so the platters are only a and so on so the platters are only a couple of inches in diameter and because couple of inches in diameter and because couple of inches in diameter and because we have so many tracks per we have so many tracks per we have so many tracks per surface that means that the tracks are surface that means that the tracks are surface that means that the tracks are spaced very very close to each other and spaced very very close to each other and spaced very very close to each other and the head assembly needs to be very very the head assembly needs to be very very the head assembly needs to be very very precise in how it positions heads


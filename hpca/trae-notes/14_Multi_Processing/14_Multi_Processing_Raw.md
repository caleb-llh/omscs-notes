# 14_Multi_Processing (Raw Transcripts)

# Playlist 4 Module 4 Raw Transcript

## Flynn's Taxonomy of Parallel Machines： Part 5
Kind: captions Language: en so there is a taxonomy called Flynn's so there is a taxonomy called Flynn's so there is a taxonomy called Flynn's taxonomy of parallel machines it taxonomy of parallel machines it taxonomy of parallel machines it distinguishes parallel machines distinguishes parallel machines distinguishes parallel machines according to how many instruction according to how many instruction according to how many instruction streams they have and how many data streams they have and how many data streams they have and how many data streams these instructions operate on streams these instructions operate on streams these instructions operate on the first type of machine is single the first type of machine is single the first type of machine is single instructions single data or S SD and instructions single data or S SD and instructions single data or S SD and logically this type of a machine logically this type of a machine logically this type of a machine executes one instruction stream and each executes one instruction stream and each executes one instruction stream and each instruction operates on one data stream instruction operates on one data stream instruction operates on one data stream this is really your normal uni processor this is really your normal uni processor this is really your normal uni processor so if we have for example a single core so if we have for example a single core so if we have for example a single core machine that's what it is the next type machine that's what it is the next type machine that's what it is the next type of machine is single instruction of machine is single instruction of machine is single instruction multiple data so it executes one multiple data so it executes one multiple data so it executes one instruction stream meaning there is one instruction stream meaning there is one instruction stream meaning there is one program with a single program counter we program with a single program counter we program with a single program counter we go through but there is more than one go through but there is more than one go through but there is more than one data stream they operate on the most data stream they operate on the most data stream they operate on the most common example of this used to be common example of this used to be common example of this used to be so-called Vector processors which so-called Vector processors which so-called Vector processors which execute a program but each instruction execute a program but each instruction execute a program but each instruction instead of operating on normal scalar instead of operating on normal scalar instead of operating on normal scalar values operates on vectors so for values operates on vectors so for values operates on vectors so for example you can execute an add example you can execute an add example you can execute an add instruction but it operates on two instruction but it operates on two instruction but it operates on two source and one destination Vector of source and one destination Vector of source and one destination Vector of things so it operates on many many data things so it operates on many many data things so it operates on many many data items at a time modern processors also items at a time modern processors also items at a time modern processors also have these multimedia extension have these multimedia extension have these multimedia extension instructions such as MMX or sse1 123 and instructions such as MMX or sse1 123 and instructions such as MMX or sse1 123 and they are also an example of limited simd they are also an example of limited simd they are also an example of limited simd where a single multimedia instruction where a single multimedia instruction where a single multimedia instruction will will operate on maybe four or eight will will operate on maybe four or eight will will operate on maybe four or eight data items then you can have a multiple data items then you can have a multiple data items then you can have a multiple instruction single data stream this is instruction single data stream this is instruction single data stream this is weird because we have multiple weird because we have multiple weird because we have multiple instruction streams so there are several instruction streams so there are several instruction streams so there are several different programs executing at the same different programs executing at the same different programs executing at the same time all operating on the same stream of time all operating on the same stream of time all operating on the same stream of data so it's not used much and the data so it's not used much and the data so it's not used much and the closest we get to this is something like closest we get to this is something like closest we get to this is something like a stream processor where you have the a stream processor where you have the a stream processor where you have the first processor operating on for example first processor operating on for example first processor operating on for example the raw data from the camera and then the raw data from the camera and then the raw data from the camera and then that data moves on to the next processor that data moves on to the next processor that data moves on to the next processor which is going to process it some which is going to process it some which is going to process it some further like find the edges or sharpen further like find the edges or sharpen further like find the edges or sharpen it or something and so on but even there it or something and so on but even there it or something and so on but even there it's not truly that we have a single it's not truly that we have a single it's not truly that we have a single data stream because the output of the data stream because the output of the data stream because the output of the first processor is not the same as the first processor is not the same as the first processor is not the same as the raw camera feed so this is just kind of raw camera feed so this is just kind of raw camera feed so this is just kind of the closest we get to it but really MISD the closest we get to it but really MISD the closest we get to it but really MISD is not really used much and then we have is not really used much and then we have is not really used much and then we have Mi IMD where there is more than one Mi IMD where there is more than one Mi IMD where there is more than one instruction stream more than one data instruction stream more than one data instruction stream more than one data stream and this would be your normal stream and this would be your normal stream and this would be your normal multiprocessor where each processor has multiprocessor where each processor has multiprocessor where each processor has its own program that it's running with its own program that it's running with its own program that it's running with its own program counter and so on and its own program counter and so on and its own program counter and so on and each of them operates on its own data so each of them operates on its own data so each of them operates on its own data so they don't have to do things in loog they don't have to do things in loog they don't have to do things in loog step to operate on the data and they step to operate on the data and they step to operate on the data and they don't do something like a stream don't do something like a stream don't do something like a stream processing so this type of processor processing so this type of processor processing so this type of processor really includes most of the processors really includes most of the processors really includes most of the processors you can buy today because most you can buy today because most you can buy today because most processors today are multicore which processors today are multicore which processors today are multicore which means really they have several processor means really they have several processor means really they have several processor cores each of which executes its own cores each of which executes its own cores each of which executes its own instruction stream and can access data instruction stream and can access data instruction stream and can access data independently so it has multiple data independently so it has multiple data independently so it has multiple data streams and this lesson is mostly about streams and this lesson is mostly about streams and this lesson is mostly about this type of machines

## Why Multiprocessors： Part 5
Kind: captions Language: en so why do we want a multiprocessor why so why do we want a multiprocessor why so why do we want a multiprocessor why not just use uniprocessors well first not just use uniprocessors well first not just use uniprocessors well first our uniprocessors our uniprocessors our uniprocessors would already be for wide when we got to would already be for wide when we got to would already be for wide when we got to about four to six wide is when we switch about four to six wide is when we switch about four to six wide is when we switch to using multi processors and that's to using multi processors and that's to using multi processors and that's because once you get to this point you because once you get to this point you because once you get to this point you get diminishing returns from making the get diminishing returns from making the get diminishing returns from making the uniprocessor even wider you can make it uniprocessor even wider you can make it uniprocessor even wider you can make it execute let's say six or eight execute let's say six or eight execute let's say six or eight instructions per cycle but you don't get instructions per cycle but you don't get instructions per cycle but you don't get as much from that extension as you did as much from that extension as you did as much from that extension as you did from for example making it from two to from for example making it from two to from for example making it from two to four wide and that's because we get four wide and that's because we get four wide and that's because we get diminishing returns if you remember your diminishing returns if you remember your diminishing returns if you remember your endures law there will be things that endures law there will be things that endures law there will be things that benefit from this such as programs that benefit from this such as programs that benefit from this such as programs that have a lot of parallelism between have a lot of parallelism between have a lot of parallelism between instructions and then there are things instructions and then there are things instructions and then there are things that don't benefit from this such as that don't benefit from this such as that don't benefit from this such as programs that have a lot of dependencies programs that have a lot of dependencies programs that have a lot of dependencies so they have to run things one at a time so they have to run things one at a time so they have to run things one at a time things that are four wide already are things that are four wide already are things that are four wide already are already making the parallel parts fast already making the parallel parts fast already making the parallel parts fast whereas the one at a time parts are not whereas the one at a time parts are not whereas the one at a time parts are not accelerated so by the time you get here accelerated so by the time you get here accelerated so by the time you get here you already have a lot of your program you already have a lot of your program you already have a lot of your program suffer from not enough parallelism to suffer from not enough parallelism to suffer from not enough parallelism to benefit from either four or even six benefit from either four or even six benefit from either four or even six wide another problem with unit wide another problem with unit wide another problem with unit processors if we make them even fancier processors if we make them even fancier processors if we make them even fancier is to make them faster we have to jack is to make them faster we have to jack is to make them faster we have to jack up the frequency to do that we have to up the frequency to do that we have to up the frequency to do that we have to raise the voltage from what it would be raise the voltage from what it would be raise the voltage from what it would be if we use the lower frequency and when if we use the lower frequency and when if we use the lower frequency and when we do both of these the power we do both of these the power we do both of these the power consumption grows up cubically so even consumption grows up cubically so even consumption grows up cubically so even if we increase the frequency by not too if we increase the frequency by not too if we increase the frequency by not too much we would end up burning our much we would end up burning our much we would end up burning our processor so we don't really want to processor so we don't really want to processor so we don't really want to make our unit processors be clocked make our unit processors be clocked make our unit processors be clocked twice as fast as before and we don't twice as fast as before and we don't twice as fast as before and we don't want to make them much wider than they want to make them much wider than they want to make them much wider than they already are already are already are but Moore's law continues we get twice but Moore's law continues we get twice but Moore's law continues we get twice the transistors every 18 months for the the transistors every 18 months for the the transistors every 18 months for the same cost as before and in the same area same cost as before and in the same area same cost as before and in the same area as before so we want to benefit from as before so we want to benefit from as before so we want to benefit from this and improve the overall performance this and improve the overall performance this and improve the overall performance but we no longer can do it this way so but we no longer can do it this way so but we no longer can do it this way so what do we do we just double the number what do we do we just double the number what do we do we just double the number of cores every 18 months that allows us of cores every 18 months that allows us of cores every 18 months that allows us to have the course that are not getting to have the course that are not getting to have the course that are not getting wider that are not getting up in wider that are not getting up in wider that are not getting up in frequency too much and yet the overall frequency too much and yet the overall frequency too much and yet the overall computational performance of the chip is computational performance of the chip is computational performance of the chip is improving with Moore's law improving with Moore's law improving with Moore's law so we can get approximately a doubling so we can get approximately a doubling so we can get approximately a doubling of overall performance every 18 months of overall performance every 18 months of overall performance every 18 months that is only if we assume that we can that is only if we assume that we can that is only if we assume that we can use all the cores if we only have one use all the cores if we only have one use all the cores if we only have one program with one thread then we are program with one thread then we are program with one thread then we are really using only one of the cores it really using only one of the cores it really using only one of the cores it doesn't matter if the number of cores doesn't matter if the number of cores doesn't matter if the number of cores doubles because we are still using one doubles because we are still using one doubles because we are still using one of those cores so we need programs that of those cores so we need programs that of those cores so we need programs that are parallel in order to exploit our are parallel in order to exploit our are parallel in order to exploit our many core processors that we are getting many core processors that we are getting many core processors that we are getting but if we do we continue the trend of but if we do we continue the trend of but if we do we continue the trend of scaling performance up exponentially to scaling performance up exponentially to scaling performance up exponentially to put this in another way we have multi put this in another way we have multi put this in another way we have multi processors today not because we think processors today not because we think processors today not because we think that we would rather have twice the that we would rather have twice the that we would rather have twice the cores than twice the unit processor cores than twice the unit processor cores than twice the unit processor performance it's always preferable to performance it's always preferable to performance it's always preferable to have a uniprocessor a single core that have a uniprocessor a single core that have a uniprocessor a single core that is twice as fast as before then having is twice as fast as before then having is twice as fast as before then having two cores that are just as fast as two cores that are just as fast as two cores that are just as fast as before but we ran out of things that we before but we ran out of things that we before but we ran out of things that we can do with uni processors so after a can do with uni processors so after a can do with uni processors so after a long string of years where we were doing long string of years where we were doing long string of years where we were doing this just getting better unit processors this just getting better unit processors this just getting better unit processors we had to switch to this if we want to we had to switch to this if we want to we had to switch to this if we want to get performance improvement

## Multicore Vs Single Core Quiz： Part 5
Kind: captions Language: en so let's compare what we can get from a so let's compare what we can get from a so let's compare what we can get from a multi core versus single core processor multi core versus single core processor multi core versus single core processor in a new technology suppose that in the in a new technology suppose that in the in a new technology suppose that in the previous technology generation we got a previous technology generation we got a previous technology generation we got a processor that has an area of 2 processor that has an area of 2 processor that has an area of 2 centimeters squared centimeters squared centimeters squared it was achieving 2.5 instructions per it was achieving 2.5 instructions per it was achieving 2.5 instructions per cycle and it consumed a hundred watts at cycle and it consumed a hundred watts at cycle and it consumed a hundred watts at two gigahertz frequency and this is the two gigahertz frequency and this is the two gigahertz frequency and this is the highest power that we can allow this highest power that we can allow this highest power that we can allow this processor to have and this hundred watts processor to have and this hundred watts processor to have and this hundred watts is what we can reasonably cool now let's is what we can reasonably cool now let's is what we can reasonably cool now let's say we get a new technology where the say we get a new technology where the say we get a new technology where the same exact processor would be half the same exact processor would be half the same exact processor would be half the area because our transistors have shrunk area because our transistors have shrunk area because our transistors have shrunk because it's the same design it's going because it's the same design it's going because it's the same design it's going to have the same IPC and because to have the same IPC and because to have the same IPC and because transistors have shrunk we also reduce transistors have shrunk we also reduce transistors have shrunk we also reduce the power of this processor when the power of this processor when the power of this processor when operating at the same two gigahertz operating at the same two gigahertz operating at the same two gigahertz frequency as before now of course frequency as before now of course frequency as before now of course because this processor is smaller in the because this processor is smaller in the because this processor is smaller in the new technology than in the old one if we new technology than in the old one if we new technology than in the old one if we want to do the best we can with two want to do the best we can with two want to do the best we can with two centimeter square we have two options centimeter square we have two options centimeter square we have two options our first option is a better single core our first option is a better single core our first option is a better single core processor this processor will be processor this processor will be processor this processor will be designed to occupy the entire area that designed to occupy the entire area that designed to occupy the entire area that we have which is 2 centimeters square we have which is 2 centimeters square we have which is 2 centimeters square it's a more complicated processor that it's a more complicated processor that it's a more complicated processor that allows us to achieve a IPC of 3.5 so it allows us to achieve a IPC of 3.5 so it allows us to achieve a IPC of 3.5 so it executes 3.5 instructions per cycle not executes 3.5 instructions per cycle not executes 3.5 instructions per cycle not 2.5 as the old processor but because 2.5 as the old processor but because 2.5 as the old processor but because it's a fancier processor and has more it's a fancier processor and has more it's a fancier processor and has more transistors it spends 75 watts when transistors it spends 75 watts when transistors it spends 75 watts when operating at 2 gigahertz so the question operating at 2 gigahertz so the question operating at 2 gigahertz so the question for you is first if we allow this for you is first if we allow this for you is first if we allow this processor to spend all hundred watts processor to spend all hundred watts processor to spend all hundred watts that we can call what is the frequency that we can call what is the frequency that we can call what is the frequency it can get in gigahertz and if we do it can get in gigahertz and if we do it can get in gigahertz and if we do that what is the speed up this better that what is the speed up this better that what is the speed up this better single core achieves versus what we can single core achieves versus what we can single core achieves versus what we can get in the old technology the second get in the old technology the second get in the old technology the second option we have in this new technology is option we have in this new technology is option we have in this new technology is to simply put two cores on this chip so to simply put two cores on this chip so to simply put two cores on this chip so it's going to be two cores like this it's going to be two cores like this it's going to be two cores like this this is going to get us 2 centimeters this is going to get us 2 centimeters this is going to get us 2 centimeters squared squared squared IPC is going to be 2.5 IPC is going to be 2.5 IPC is going to be 2.5 on each core and each core is going to on each core and each core is going to on each core and each core is going to consume 50 watts at two gigahertz so we consume 50 watts at two gigahertz so we consume 50 watts at two gigahertz so we get our entire budget spent by having get our entire budget spent by having get our entire budget spent by having this course operated two gigahertz so this course operated two gigahertz so this course operated two gigahertz so the question for you here is what's the the question for you here is what's the the question for you here is what's the speed-up of this two core approach speed-up of this two core approach speed-up of this two core approach versus the old technology assuming of versus the old technology assuming of versus the old technology assuming of course that the old work can simply be course that the old work can simply be course that the old work can simply be divided among the course so pretty much divided among the course so pretty much divided among the course so pretty much having two cores allows us just to split having two cores allows us just to split having two cores allows us just to split the same work that we had before onto the same work that we had before onto the same work that we had before onto two cores without adding any new two cores without adding any new two cores without adding any new instructions and without sacrificing the instructions and without sacrificing the instructions and without sacrificing the IPC

## Multicore Vs Single Core Solution Quiz： Part 5
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our multipore versus single core quiz this multipore versus single core quiz this multipore versus single core quiz this is what we had in the old technology in is what we had in the old technology in is what we had in the old technology in the new technology one of these scores the new technology one of these scores the new technology one of these scores is going to be half the size and half is going to be half the size and half is going to be half the size and half the power the power the power so we had the option of either replacing so we had the option of either replacing so we had the option of either replacing that with a better core that occupies that with a better core that occupies that with a better core that occupies the whole area gets a small IPC but the whole area gets a small IPC but the whole area gets a small IPC but consumes slightly more power and then consumes slightly more power and then consumes slightly more power and then the question is if we do consume the the question is if we do consume the the question is if we do consume the whole power budget of the chip which is whole power budget of the chip which is whole power budget of the chip which is a hundred watts how much can we increase a hundred watts how much can we increase a hundred watts how much can we increase this frequency and with that frequency this frequency and with that frequency this frequency and with that frequency what is the overall speed up versus what what is the overall speed up versus what what is the overall speed up versus what we had in the old technology we had in the old technology we had in the old technology the other option was just put the two the other option was just put the two the other option was just put the two cores that we can now fit on the chip cores that we can now fit on the chip cores that we can now fit on the chip each now gets us the same IPC as the each now gets us the same IPC as the each now gets us the same IPC as the single core did and together they single core did and together they single core did and together they consume the whole power budget of the consume the whole power budget of the consume the whole power budget of the chip at 2 gigahertz so the question is chip at 2 gigahertz so the question is chip at 2 gigahertz so the question is what's the speed up versus the old what's the speed up versus the old what's the speed up versus the old technology with the two core approach technology with the two core approach technology with the two core approach the first thing we need to figure out the first thing we need to figure out the first thing we need to figure out here is how much can we raise this here is how much can we raise this here is how much can we raise this frequency here so that we get to 100 frequency here so that we get to 100 frequency here so that we get to 100 Watts well we are allowed to increase Watts well we are allowed to increase Watts well we are allowed to increase the power from 75 watts to 100 Watts the power from 75 watts to 100 Watts the power from 75 watts to 100 Watts that's a factor of 1.33 remember that that's a factor of 1.33 remember that that's a factor of 1.33 remember that the dynamic power is proportional to the dynamic power is proportional to the dynamic power is proportional to voltage squared times the frequency voltage squared times the frequency voltage squared times the frequency itself and that we have to increase the itself and that we have to increase the itself and that we have to increase the voltage in proportion to frequency so if voltage in proportion to frequency so if voltage in proportion to frequency so if we are spending power to increase the we are spending power to increase the we are spending power to increase the frequency we have to proportionally get frequency we have to proportionally get frequency we have to proportionally get both the voltage and the frequency up both the voltage and the frequency up both the voltage and the frequency up and that means that we will be spending and that means that we will be spending and that means that we will be spending power in proportion to the cube of the power in proportion to the cube of the power in proportion to the cube of the frequency that means that the frequency frequency that means that the frequency frequency that means that the frequency that we can operate on is cubic root of that we can operate on is cubic root of that we can operate on is cubic root of 1 to 33 times the old frequency and this 1 to 33 times the old frequency and this 1 to 33 times the old frequency and this is equal to 1.1 so our new frequency is is equal to 1.1 so our new frequency is is equal to 1.1 so our new frequency is 1.1 times the old frequency the old 1.1 times the old frequency the old 1.1 times the old frequency the old frequency was 2 gigahertz that means we frequency was 2 gigahertz that means we frequency was 2 gigahertz that means we can achieve 2.2 gigahertz when we can achieve 2.2 gigahertz when we can achieve 2.2 gigahertz when we increase the power by a factor of 1.33 increase the power by a factor of 1.33 increase the power by a factor of 1.33 the speed up we achieve versus the old the speed up we achieve versus the old the speed up we achieve versus the old technology will be the speed up we technology will be the speed up we technology will be the speed up we achieve because of a shorter clock cycle achieve because of a shorter clock cycle achieve because of a shorter clock cycle time and that speed up is 1.1 times time and that speed up is 1.1 times time and that speed up is 1.1 times so we are 1.1 times faster now in terms so we are 1.1 times faster now in terms so we are 1.1 times faster now in terms of clock frequency times the speed up we of clock frequency times the speed up we of clock frequency times the speed up we achieve because our IPC is better and if achieve because our IPC is better and if achieve because our IPC is better and if we divide 3.5 and 2.5 we get a factor of we divide 3.5 and 2.5 we get a factor of we divide 3.5 and 2.5 we get a factor of 1.4 1.4 1.4 times times times the speed up we achieve because we have the speed up we achieve because we have the speed up we achieve because we have fewer instructions but because we are fewer instructions but because we are fewer instructions but because we are executing exactly the same code that executing exactly the same code that executing exactly the same code that speed up is one we don't speed up really speed up is one we don't speed up really speed up is one we don't speed up really so our overall speed up is just this and so our overall speed up is just this and so our overall speed up is just this and it ends up being 1.54 now let us look at it ends up being 1.54 now let us look at it ends up being 1.54 now let us look at the two core option we have the same IPC the two core option we have the same IPC the two core option we have the same IPC as before but we now have two cores that as before but we now have two cores that as before but we now have two cores that are doing that so we are executing twice are doing that so we are executing twice are doing that so we are executing twice as many instructions as before at the as many instructions as before at the as many instructions as before at the same frequency same frequency same frequency and at the same power so the speed up and at the same power so the speed up and at the same power so the speed up versus the old system here will simply versus the old system here will simply versus the old system here will simply be two we are operating at the same be two we are operating at the same be two we are operating at the same frequency splitting the same work into frequency splitting the same work into frequency splitting the same work into two pieces and each of the pieces is now two pieces and each of the pieces is now two pieces and each of the pieces is now done at the speed where we were doing done at the speed where we were doing done at the speed where we were doing only one of the pieces so overall we are only one of the pieces so overall we are only one of the pieces so overall we are getting a speed up of two as you can see getting a speed up of two as you can see getting a speed up of two as you can see two cores are achieving a better speed two cores are achieving a better speed two cores are achieving a better speed up relative to the old system than a up relative to the old system than a up relative to the old system than a fancier core which means that if we fancier core which means that if we fancier core which means that if we indeed do have programs that can use the indeed do have programs that can use the indeed do have programs that can use the two cores effectively then this is the two cores effectively then this is the two cores effectively then this is the better option better option better option than improving the core and consuming than improving the core and consuming than improving the core and consuming the chip area that way

## Multiprocessor Needs Parallel Programs!： Part 5
Kind: captions Language: en so we have already seen that a so we have already seen that a so we have already seen that a multi-processor multi-processor multi-processor can easily win in performance nowadays can easily win in performance nowadays can easily win in performance nowadays assuming that you have a program that assuming that you have a program that assuming that you have a program that can use multiple cores so there are some can use multiple cores so there are some can use multiple cores so there are some disadvantages to going to disadvantages to going to disadvantages to going to multiprocessors from single core multiprocessors from single core multiprocessors from single core processors processors processors one is that the so-called sequential or one is that the so-called sequential or one is that the so-called sequential or single threaded code is easier to single threaded code is easier to single threaded code is easier to develop for programmers and actually develop for programmers and actually develop for programmers and actually it's a lot easier to develop so there is it's a lot easier to develop so there is it's a lot easier to develop so there is significant cost involved significant cost involved significant cost involved in making an application multi-threaded in making an application multi-threaded in making an application multi-threaded where originally it was single threaded where originally it was single threaded where originally it was single threaded even when you do write even when you do write even when you do write a parallel application you will have a parallel application you will have a parallel application you will have some bugs and debugging of parallel code some bugs and debugging of parallel code some bugs and debugging of parallel code is much more difficult than sequential is much more difficult than sequential is much more difficult than sequential code code code and even if you have a program that and even if you have a program that and even if you have a program that works correctly works correctly works correctly and uses a lot of threads and uses a lot of threads and uses a lot of threads chances are chances are chances are that it will still be very hard to get that it will still be very hard to get that it will still be very hard to get what is called performance scaling what is called performance scaling what is called performance scaling and performance scaling is a property of and performance scaling is a property of and performance scaling is a property of the program that as we increase the the program that as we increase the the program that as we increase the number of cores number of cores number of cores if its overall performance if its overall performance if its overall performance keeps improving this would be ideal keeps improving this would be ideal keeps improving this would be ideal performance scaling you get more performance scaling you get more performance scaling you get more performance in proportion to the number performance in proportion to the number performance in proportion to the number of cores you're given of cores you're given of cores you're given unfortunately most programs when you unfortunately most programs when you unfortunately most programs when you write them do something like this they write them do something like this they write them do something like this they scale for a while and then stop scaling scale for a while and then stop scaling scale for a while and then stop scaling and eventually you stop getting more and eventually you stop getting more and eventually you stop getting more performance from the number of cores performance from the number of cores performance from the number of cores you can spend a lot of time improving you can spend a lot of time improving you can spend a lot of time improving this program and get something like this this program and get something like this this program and get something like this where it scales up to some larger number where it scales up to some larger number where it scales up to some larger number of cores reasonably well of cores reasonably well of cores reasonably well but each of these increments in scaling but each of these increments in scaling but each of these increments in scaling is going to result in a lot of work for is going to result in a lot of work for is going to result in a lot of work for you to achieve it and very very few you to achieve it and very very few you to achieve it and very very few programs achieve good scaling to a huge programs achieve good scaling to a huge programs achieve good scaling to a huge number of course number of course number of course and those that do usually took years to and those that do usually took years to and those that do usually took years to develop

## Centralized Shared Memory： Part 5
Kind: captions Language: en so now we will go through several types so now we will go through several types so now we will go through several types of of of multiprocessors the first type of multiprocessors the first type of multiprocessors the first type of multiprocessor uses what is called a multiprocessor uses what is called a multiprocessor uses what is called a centralized shared memory so all the centralized shared memory so all the centralized shared memory so all the cores let's say we have four of them cores let's say we have four of them cores let's say we have four of them might have their own caches but then might have their own caches but then might have their own caches but then they're all connected to the same bus so they're all connected to the same bus so they're all connected to the same bus so they can all access the same main memory they can all access the same main memory they can all access the same main memory and input output devices this scores can and input output devices this scores can and input output devices this scores can share data by simply reading and writing share data by simply reading and writing share data by simply reading and writing the main memory assuming of course that the main memory assuming of course that the main memory assuming of course that these reads and wrs go through the these reads and wrs go through the these reads and wrs go through the caches properly and we will see that we caches properly and we will see that we caches properly and we will see that we can achieve that so pretty much this can achieve that so pretty much this can achieve that so pretty much this score can send data to this score by score can send data to this score by score can send data to this score by writing to some memory location and then writing to some memory location and then writing to some memory location and then this score reads it from there similarly this score reads it from there similarly this score reads it from there similarly any of the cores can access any of the any of the cores can access any of the any of the cores can access any of the io devices here this type of a system is io devices here this type of a system is io devices here this type of a system is really what today's multicore processors really what today's multicore processors really what today's multicore processors tend to look like and is also called tend to look like and is also called tend to look like and is also called Yuma which stand stands for uniform Yuma which stand stands for uniform Yuma which stand stands for uniform memory access time but it's not umat memory access time but it's not umat memory access time but it's not umat it's just Yuma but what is uniform is it's just Yuma but what is uniform is it's just Yuma but what is uniform is really the time to access the memory why really the time to access the memory why really the time to access the memory why well because this main well because this main well because this main memory is at the same long distance from memory is at the same long distance from memory is at the same long distance from all of the cores yes it can be slightly all of the cores yes it can be slightly all of the cores yes it can be slightly closer to some of them but because closer to some of them but because closer to some of them but because they're all connected to it in the same they're all connected to it in the same they're all connected to it in the same way the memory really is about as bad to way the memory really is about as bad to way the memory really is about as bad to access as any one of them this type of access as any one of them this type of access as any one of them this type of multiprocessing is also called SM SMP multiprocessing is also called SM SMP multiprocessing is also called SM SMP which stands for symmetric which stands for symmetric which stands for symmetric multiprocessor it's called symmetric multiprocessor it's called symmetric multiprocessor it's called symmetric because any core and its cache look just because any core and its cache look just because any core and its cache look just like any other core and its cache in the like any other core and its cache in the like any other core and its cache in the system so if we had two cores we can system so if we had two cores we can system so if we had two cores we can just take one of them with its caches just take one of them with its caches just take one of them with its caches and replicate to make a four core system

## Multicore Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out what so let's see if we can figure out what so let's see if we can figure out what the problems are with Yuma the problems are with Yuma the problems are with Yuma aka SMP aka multi-core but suppose we aka SMP aka multi-core but suppose we aka SMP aka multi-core but suppose we want to have a lot of course and have want to have a lot of course and have want to have a lot of course and have them use decentralized shared memory in them use decentralized shared memory in them use decentralized shared memory in a human SMP fashion which of these our a human SMP fashion which of these our a human SMP fashion which of these our problems we'll have to face when we do problems we'll have to face when we do problems we'll have to face when we do this first memory needs to be very large this first memory needs to be very large this first memory needs to be very large and thus slow for this type of a machine and thus slow for this type of a machine and thus slow for this type of a machine our main memory will get too many our main memory will get too many our main memory will get too many accesses per second basically we'll need accesses per second basically we'll need accesses per second basically we'll need a huge memory throughput that a normal a huge memory throughput that a normal a huge memory throughput that a normal memory module will not be able to memory module will not be able to memory module will not be able to provide the next option is we'll need a provide the next option is we'll need a provide the next option is we'll need a lot of small memory modules in lot of small memory modules in lot of small memory modules in proportion to the number of cores next proportion to the number of cores next proportion to the number of cores next our caches will have very small block our caches will have very small block our caches will have very small block sizes and finally the pages for our sizes and finally the pages for our sizes and finally the pages for our virtual memory will become too small to virtual memory will become too small to virtual memory will become too small to be usable so select all of those that be usable so select all of those that be usable so select all of those that are problems for this type of are problems for this type of are problems for this type of architecture

## Multicore Solution Quiz： Part 5
Kind: captions Language: en so let's look at our answer to the uma so let's look at our answer to the uma so let's look at our answer to the uma smp quiz smp quiz smp quiz we want to have a lot of cores we want to have a lot of cores we want to have a lot of cores with this type of architecture so what with this type of architecture so what with this type of architecture so what are the problems that we'll have to face are the problems that we'll have to face are the problems that we'll have to face the memory will need to be large and the memory will need to be large and the memory will need to be large and slow and that's because slow and that's because slow and that's because having more cores having more cores having more cores usually means that we will be running usually means that we will be running usually means that we will be running programs that access more data programs that access more data programs that access more data and we need to keep that data somewhere and we need to keep that data somewhere and we need to keep that data somewhere so we will need to have a large memory so we will need to have a large memory so we will need to have a large memory and that makes the memory slow next that and that makes the memory slow next that and that makes the memory slow next that memory will get too many accesses per memory will get too many accesses per memory will get too many accesses per second because the cache misses second because the cache misses second because the cache misses from all of the cores will be going to from all of the cores will be going to from all of the cores will be going to memory if one of the core generates memory if one of the core generates memory if one of the core generates cache misses that the memory can survive cache misses that the memory can survive cache misses that the memory can survive doing doing doing two of them it might still survive but two of them it might still survive but two of them it might still survive but if we have a lot of cores if we have a lot of cores if we have a lot of cores our memory will probably not be able to our memory will probably not be able to our memory will probably not be able to keep up with all of those accesses keep up with all of those accesses keep up with all of those accesses we'll need a lot of small memory modules we'll need a lot of small memory modules we'll need a lot of small memory modules not necessarily the number of memory not necessarily the number of memory not necessarily the number of memory modules in a centralized modules in a centralized modules in a centralized memory memory memory doesn't matter what is really needed is doesn't matter what is really needed is doesn't matter what is really needed is to have enough memory for all the cores to have enough memory for all the cores to have enough memory for all the cores to get enough data in there to get enough data in there to get enough data in there and the memory that will keep up with and the memory that will keep up with and the memory that will keep up with the bandwidth demands of the course the bandwidth demands of the course the bandwidth demands of the course how many modules we have doesn't really how many modules we have doesn't really how many modules we have doesn't really matter matter matter caches will have very small block size caches will have very small block size caches will have very small block size this is not true either this is not true either this is not true either all of the caches in all of the course all of the caches in all of the course all of the caches in all of the course can have can have can have any block size they want the block size any block size they want the block size any block size they want the block size is not divided among the cores is not divided among the cores is not divided among the cores pages for virtual memory become too pages for virtual memory become too pages for virtual memory become too small same thing we can have any page small same thing we can have any page small same thing we can have any page size we want size we want size we want having more or fewer cores doesn't having more or fewer cores doesn't having more or fewer cores doesn't affect the page size in virtual memory affect the page size in virtual memory affect the page size in virtual memory so the only options you should have so the only options you should have so the only options you should have selected are the first tool

## Centralized Main Memory Problems： Part 5
Kind: captions Language: en so let's talk about problems or so let's talk about problems or so let's talk about problems or centralized main memory and why we may centralized main memory and why we may centralized main memory and why we may want to do something else as we have want to do something else as we have want to do something else as we have seen in the previous quiz memory size is seen in the previous quiz memory size is seen in the previous quiz memory size is a problem we need a large memory and a problem we need a large memory and a problem we need a large memory and because it's all one big memory that one because it's all one big memory that one because it's all one big memory that one memory will be slow and because of its memory will be slow and because of its memory will be slow and because of its large size relatively far away from all large size relatively far away from all large size relatively far away from all of the course we will also have the of the course we will also have the of the course we will also have the problem of memory bandwidth because problem of memory bandwidth because problem of memory bandwidth because message from all cores go to the one message from all cores go to the one message from all cores go to the one memory so the requests from all of the memory so the requests from all of the memory so the requests from all of the cores create contention for memory cores create contention for memory cores create contention for memory bandwidth because a big slow memory will bandwidth because a big slow memory will bandwidth because a big slow memory will have a limited bandwidth and what that have a limited bandwidth and what that have a limited bandwidth and what that does is it prevents really additional does is it prevents really additional does is it prevents really additional course from benefiting our performance course from benefiting our performance course from benefiting our performance because all of the accesses are really because all of the accesses are really because all of the accesses are really serializing in the queue waiting for serializing in the queue waiting for serializing in the queue waiting for memory so if we add more cores that just memory so if we add more cores that just memory so if we add more cores that just makes every course access is even slower makes every course access is even slower makes every course access is even slower because they have to wait for memory because they have to wait for memory because they have to wait for memory even longer so we don't really get a even longer so we don't really get a even longer so we don't really get a performance advantage anymore so performance advantage anymore so performance advantage anymore so centralized main memory because of these centralized main memory because of these centralized main memory because of these problems works well only for relatively problems works well only for relatively problems works well only for relatively small machines with something like 24 8 small machines with something like 24 8 small machines with something like 24 8 maybe 16 cores beyond this we saturate maybe 16 cores beyond this we saturate maybe 16 cores beyond this we saturate the memory bandwidth and also our memory the memory bandwidth and also our memory the memory bandwidth and also our memory needs to not be too big and too slow if needs to not be too big and too slow if needs to not be too big and too slow if we keep it centralized

## Distributed Shared Memory： Part 5
Kind: captions Language: en The next organization that we will be The next organization that we will be The next organization that we will be looking at is similar to distributed looking at is similar to distributed looking at is similar to distributed shared memory except that we are no shared memory except that we are no shared memory except that we are no longer sharing memory. So it's called longer sharing memory. So it's called longer sharing memory. So it's called distributed memory. No sharing of memory distributed memory. No sharing of memory distributed memory. No sharing of memory means that only one core can access a means that only one core can access a means that only one core can access a memory slice and the others cannot. So memory slice and the others cannot. So memory slice and the others cannot. So what we now have is cores that have what we now have is cores that have what we now have is cores that have their own caches and each of them has their own caches and each of them has their own caches and each of them has its own memory that can only be locally its own memory that can only be locally its own memory that can only be locally accessed here. So really each of the accessed here. So really each of the accessed here. So really each of the core has pretty much what looks like a core has pretty much what looks like a core has pretty much what looks like a complete single core computer system and complete single core computer system and complete single core computer system and a network interface card that connects a network interface card that connects a network interface card that connects it to a network. But now when a cork has it to a network. But now when a cork has it to a network. But now when a cork has a cache miss that cache miss goes a cache miss that cache miss goes a cache miss that cache miss goes directly to this memory and if the core directly to this memory and if the core directly to this memory and if the core wants to access something that is in wants to access something that is in wants to access something that is in another core's memory it cannot simply another core's memory it cannot simply another core's memory it cannot simply issue an access that misses in the cache issue an access that misses in the cache issue an access that misses in the cache and goes there. Now what it needs to do and goes there. Now what it needs to do and goes there. Now what it needs to do is actually create a network message is actually create a network message is actually create a network message using some sort of a send primitive in using some sort of a send primitive in using some sort of a send primitive in the operating system to actually send a the operating system to actually send a the operating system to actually send a request. The program here needs to request. The program here needs to request. The program here needs to receive that, see what is being receive that, see what is being receive that, see what is being requested, respond to it and so on. So requested, respond to it and so on. So requested, respond to it and so on. So now communication is explicit. It's no now communication is explicit. It's no now communication is explicit. It's no longer sufficient to simply put the data longer sufficient to simply put the data longer sufficient to simply put the data in memory and then another core just in memory and then another core just in memory and then another core just reads it from there. you have to reads it from there. you have to reads it from there. you have to actually send the data explicitly to actually send the data explicitly to actually send the data explicitly to another core if we want to communicate. another core if we want to communicate. another core if we want to communicate. So that means that you write programs So that means that you write programs So that means that you write programs differently from this symmetric shared differently from this symmetric shared differently from this symmetric shared memory and distributed shared memory memory and distributed shared memory memory and distributed shared memory pass data around using shared memory pass data around using shared memory pass data around using shared memory meaning reads and writes to memory are meaning reads and writes to memory are meaning reads and writes to memory are used to exchange data. Now we are using used to exchange data. Now we are using used to exchange data. Now we are using what is called message passing for what is called message passing for what is called message passing for communication. So now you're pretty much communication. So now you're pretty much communication. So now you're pretty much writing a program as if these were writing a program as if these were writing a program as if these were independent machines that communicate independent machines that communicate independent machines that communicate over a network. If you have a over a network. If you have a over a network. If you have a distributed memory supercomput for distributed memory supercomput for distributed memory supercomput for example, it's just that this network and example, it's just that this network and example, it's just that this network and these network cards are a lot faster these network cards are a lot faster these network cards are a lot faster than your normal Ethernet connection. than your normal Ethernet connection. than your normal Ethernet connection. This type of a system is also called a This type of a system is also called a This type of a system is also called a multicomput because really each of these multicomput because really each of these multicomput because really each of these is like a complete computer. It has the is like a complete computer. It has the is like a complete computer. It has the processor, the cache, the memory, some processor, the cache, the memory, some processor, the cache, the memory, some IO and the processor only directly IO and the processor only directly IO and the processor only directly accesses the local memory. So it looks accesses the local memory. So it looks accesses the local memory. So it looks like a complete uni processor system. like a complete uni processor system. like a complete uni processor system. These are also called cluster computers These are also called cluster computers These are also called cluster computers because really you put a bunch of normal because really you put a bunch of normal because really you put a bunch of normal computers together into a tightly computers together into a tightly computers together into a tightly network cluster and you get something network cluster and you get something network cluster and you get something like a distributed memory system. These like a distributed memory system. These like a distributed memory system. These types of types of types of computers tend to scale to a large computers tend to scale to a large computers tend to scale to a large number of processors. The reason is not number of processors. The reason is not number of processors. The reason is not that they're fundamentally better at that they're fundamentally better at that they're fundamentally better at communicating than shared memory communicating than shared memory communicating than shared memory systems. The reason is mainly that the systems. The reason is mainly that the systems. The reason is mainly that the programmer is forced to explicitly think programmer is forced to explicitly think programmer is forced to explicitly think about communication because you use a about communication because you use a about communication because you use a different type of primitive to different type of primitive to different type of primitive to communicate than you use to access your communicate than you use to access your communicate than you use to access your local memory. So the programmer is aware local memory. So the programmer is aware local memory. So the programmer is aware of communication going on and then of communication going on and then of communication going on and then naturally they will tend to minimize naturally they will tend to minimize naturally they will tend to minimize communication and if at all possible communication and if at all possible communication and if at all possible access the local memory. So this access the local memory. So this access the local memory. So this approach works better primarily because approach works better primarily because approach works better primarily because it forces the programmer to figure out it forces the programmer to figure out it forces the programmer to figure out things that otherwise it might not things that otherwise it might not things that otherwise it might not notice because it forces the programmer notice because it forces the programmer notice because it forces the programmer to figure out how to communicate to figure out how to communicate to figure out how to communicate efficiently. Whereas in shared memory, efficiently. Whereas in shared memory, efficiently. Whereas in shared memory, the programmer may not even realize that the programmer may not even realize that the programmer may not even realize that some of the luxes are not local and thus some of the luxes are not local and thus some of the luxes are not local and thus are slow and causing a lot of are slow and causing a lot of are slow and causing a lot of communication to happen.

## Numa Memory Allocation Quiz： Part 5
Kind: captions Language: en so let's see if we can figure out how we so let's see if we can figure out how we so let's see if we can figure out how we should allocate memory in an Ummah should allocate memory in an Ummah should allocate memory in an Ummah system so that it works well our system so that it works well our system so that it works well our operating system should put the stack operating system should put the stack operating system should put the stack pages for core n in the memory slice and pages for core n in the memory slice and pages for core n in the memory slice and put all data pages ever touched by core put all data pages ever touched by core put all data pages ever touched by core n in the memory slice N and put all data n in the memory slice N and put all data n in the memory slice N and put all data pages mostly accessed by core n in the pages mostly accessed by core n in the pages mostly accessed by core n in the memory slice and more than one of these memory slice and more than one of these memory slice and more than one of these could be correct could be correct could be correct or potentially none of them are correct or potentially none of them are correct or potentially none of them are correct you need to select all that are correct you need to select all that are correct you need to select all that are correct what should the operating system do

## Numa Memory Allocation Solution Quiz： Part 5
Kind: captions Language: en let's answer our numa memory location let's answer our numa memory location let's answer our numa memory location quiz in order to quiz in order to quiz in order to improve performance of a numa machine improve performance of a numa machine improve performance of a numa machine the operating system the operating system the operating system should really put all data pages that should really put all data pages that should really put all data pages that are mostly accessed by a single core are mostly accessed by a single core are mostly accessed by a single core in in in the memory slice local to that core that the memory slice local to that core that the memory slice local to that core that will ensure that most of the accesses by will ensure that most of the accesses by will ensure that most of the accesses by each of the cores are going to the local each of the cores are going to the local each of the cores are going to the local memory memory memory which both make sure that accesses will which both make sure that accesses will which both make sure that accesses will be quicker than they otherwise would be be quicker than they otherwise would be be quicker than they otherwise would be and also the network will not be used and also the network will not be used and also the network will not be used for the majority of accesses so this for the majority of accesses so this for the majority of accesses so this should be selected if we however put all should be selected if we however put all should be selected if we however put all data pages ever touched by core n in the data pages ever touched by core n in the data pages ever touched by core n in the memory slice n then we have a problem memory slice n then we have a problem memory slice n then we have a problem because because because for example one core might initialize for example one core might initialize for example one core might initialize all the data and then we put all of the all the data and then we put all of the all the data and then we put all of the data in its memory slice and at that data in its memory slice and at that data in its memory slice and at that point that memory slice becomes the point that memory slice becomes the point that memory slice becomes the bottom line because everybody will be bottom line because everybody will be bottom line because everybody will be accessing that slice and the data might accessing that slice and the data might accessing that slice and the data might not even fit there so this is not a not even fit there so this is not a not even fit there so this is not a correct answer correct answer correct answer and then the put the stack pages for and then the put the stack pages for and then the put the stack pages for core n in the memory slice n is a very core n in the memory slice n is a very core n in the memory slice n is a very extreme example of this where really it extreme example of this where really it extreme example of this where really it will just be the core n that is ever will just be the core n that is ever will just be the core n that is ever accessing this data so of course it accessing this data so of course it accessing this data so of course it should be in its memory slice should be in its memory slice should be in its memory slice so the stack is one of those things that so the stack is one of those things that so the stack is one of those things that the operating system can figure out very the operating system can figure out very the operating system can figure out very easily where to put easily where to put easily where to put some of the other pages are a little bit some of the other pages are a little bit some of the other pages are a little bit more difficult because more than one more difficult because more than one more difficult because more than one core are accessing it and then the core are accessing it and then the core are accessing it and then the operating system needs to choose a good operating system needs to choose a good operating system needs to choose a good location location location either because one core is accessing it either because one core is accessing it either because one core is accessing it more than others more than others more than others or to put it in a memory module that is or to put it in a memory module that is or to put it in a memory module that is kinda close to all the cores that are kinda close to all the cores that are kinda close to all the cores that are accessing it

## A Message Passing Program： Part 5
Kind: captions Language: en so to see what the programmer has to go so to see what the programmer has to go so to see what the programmer has to go through let's look at the message through let's look at the message through let's look at the message passing program passing program passing program this is a relatively simple program what this is a relatively simple program what this is a relatively simple program what it's trying to do is compute it's trying to do is compute it's trying to do is compute a sum a sum a sum of a relatively large array in parallel of a relatively large array in parallel of a relatively large array in parallel on four cores on four cores on four cores so this is the overall array size it has so this is the overall array size it has so this is the overall array size it has 1024 elements 1024 elements 1024 elements this is the number of processors we have this is the number of processors we have this is the number of processors we have in a message passing program in a message passing program in a message passing program each of the processors will get one each of the processors will get one each of the processors will get one quarter of the array they cannot access quarter of the array they cannot access quarter of the array they cannot access each other's memory directly so each one each other's memory directly so each one each other's memory directly so each one of them only allocates its part of the of them only allocates its part of the of them only allocates its part of the array in its memory each of them will array in its memory each of them will array in its memory each of them will have its own sum variable and then what have its own sum variable and then what have its own sum variable and then what happens is each of them iterates over happens is each of them iterates over happens is each of them iterates over its part of the array its part of the array its part of the array adding those array elements to the sum adding those array elements to the sum adding those array elements to the sum this of course assumes that the elements this of course assumes that the elements this of course assumes that the elements of the large array have somehow been of the large array have somehow been of the large array have somehow been distributed to all of the processors if distributed to all of the processors if distributed to all of the processors if they haven't been distributed already they haven't been distributed already they haven't been distributed already then then then we need to also write code that will we need to also write code that will we need to also write code that will actually send the elements of the array actually send the elements of the array actually send the elements of the array so that each of them gets a quarter of so that each of them gets a quarter of so that each of them gets a quarter of the array the array the array once we have computed the sum we need to once we have computed the sum we need to once we have computed the sum we need to compute the final sum compute the final sum compute the final sum and to do that we have to communicate so and to do that we have to communicate so and to do that we have to communicate so what we will do is we will have one of what we will do is we will have one of what we will do is we will have one of the processors the processors the processors act as the summing up agent and it will act as the summing up agent and it will act as the summing up agent and it will for every other processor for every other processor for every other processor receive receive receive the sum sent by that processor and add the sum sent by that processor and add the sum sent by that processor and add it to its sum and then print the final it to its sum and then print the final it to its sum and then print the final overall sum overall sum overall sum what do the rest of the processors do what do the rest of the processors do what do the rest of the processors do well what they do is they send well what they do is they send well what they do is they send to processor zero what they have to processor zero what they have to processor zero what they have collected so far and that's all they do collected so far and that's all they do collected so far and that's all they do so as you can see we need to explicitly so as you can see we need to explicitly so as you can see we need to explicitly send and then receive things in order send and then receive things in order send and then receive things in order for communication between processors to for communication between processors to for communication between processors to happen and also we have to explicitly happen and also we have to explicitly happen and also we have to explicitly control what gets into whose part of control what gets into whose part of control what gets into whose part of memory memory memory we need to explicitly also in the we need to explicitly also in the we need to explicitly also in the program program program figure out how to match the sends and figure out how to match the sends and figure out how to match the sends and receives because if we send something receives because if we send something receives because if we send something and it's not received it's going to be a and it's not received it's going to be a and it's not received it's going to be a problem and also if we wait to receive problem and also if we wait to receive problem and also if we wait to receive something and there is no cent for it something and there is no cent for it something and there is no cent for it it's going to be a problem we get stuck it's going to be a problem we get stuck it's going to be a problem we get stuck here here here so we need to know how many so we need to know how many so we need to know how many processes to wait for processes to wait for processes to wait for we need to send exactly what's expected we need to send exactly what's expected we need to send exactly what's expected at that time and so on at that time and so on at that time and so on but once we do all of this correctly but once we do all of this correctly but once we do all of this correctly it's very easy for the programmer to see it's very easy for the programmer to see it's very easy for the programmer to see which parts of this program are going to which parts of this program are going to which parts of this program are going to be expensive as far as communication is be expensive as far as communication is be expensive as far as communication is concerned because these are all local concerned because these are all local concerned because these are all local accesses so they're relatively fast and accesses so they're relatively fast and accesses so they're relatively fast and these are the only places where we these are the only places where we these are the only places where we actually communicate

## A Shared Memory Program： Part 5
Kind: captions Language: en now let's look at the shared memory now let's look at the shared memory now let's look at the shared memory version of the same program we still version of the same program we still version of the same program we still have the same size of the overall array have the same size of the overall array have the same size of the overall array and the same number of processor cores and the same number of processor cores and the same number of processor cores but this time they can all access the but this time they can all access the but this time they can all access the same memory so we can just put the whole same memory so we can just put the whole same memory so we can just put the whole array in memory we will have the total array in memory we will have the total array in memory we will have the total sum and each of them will be computing sum and each of them will be computing sum and each of them will be computing its own sum so because this arrays in its own sum so because this arrays in its own sum so because this arrays in the shared memory each processor will the shared memory each processor will the shared memory each processor will sum up its own part of the array sum up its own part of the array sum up its own part of the array the big difference occurs not only here the big difference occurs not only here the big difference occurs not only here where we are actually able to just where we are actually able to just where we are actually able to just access the part of the array we want access the part of the array we want access the part of the array we want but also in how we can compute the sum but also in how we can compute the sum but also in how we can compute the sum instead of having to explicitly send instead of having to explicitly send instead of having to explicitly send the elements of the array to one of the the elements of the array to one of the the elements of the array to one of the cores here each core can access the all cores here each core can access the all cores here each core can access the all sum variable to compute the final sum sum variable to compute the final sum sum variable to compute the final sum so what they do is they synchronize so so what they do is they synchronize so so what they do is they synchronize so they have a critical section around this they have a critical section around this they have a critical section around this and then each course simply takes the and then each course simply takes the and then each course simply takes the overall sum and adds its own sum into it overall sum and adds its own sum into it overall sum and adds its own sum into it and then freeze the log so that the next and then freeze the log so that the next and then freeze the log so that the next core can do it so the course can do this core can do it so the course can do this core can do it so the course can do this in any order they want we don't have to in any order they want we don't have to in any order they want we don't have to control that order or anything all that control that order or anything all that control that order or anything all that needs to happen is that all of them have needs to happen is that all of them have needs to happen is that all of them have added the sum together and now added the sum together and now added the sum together and now we still have to make sure that one of we still have to make sure that one of we still have to make sure that one of the cores prints out the sum but because the cores prints out the sum but because the cores prints out the sum but because this can be done in any order core 0 this can be done in any order core 0 this can be done in any order core 0 could be the one could be the one could be the one that does this first and then prints out that does this first and then prints out that does this first and then prints out only its part of the sum because the only its part of the sum because the only its part of the sum because the others have not updated this yet so we others have not updated this yet so we others have not updated this yet so we need to add code here to have what's need to add code here to have what's need to add code here to have what's called a barrier where basically we put called a barrier where basically we put called a barrier where basically we put a synchronization here that makes all of a synchronization here that makes all of a synchronization here that makes all of the cores wait for everybody to arrive the cores wait for everybody to arrive the cores wait for everybody to arrive before they proceed to ensure that core before they proceed to ensure that core before they proceed to ensure that core zero when it does this does it only when zero when it does this does it only when zero when it does this does it only when everybody else has arrived to this point everybody else has arrived to this point everybody else has arrived to this point and has incremented the sum already and has incremented the sum already and has incremented the sum already one of the nice properties of this is one of the nice properties of this is one of the nice properties of this is not only that it makes it more explicit not only that it makes it more explicit not only that it makes it more explicit who does what so that we don't have to who does what so that we don't have to who does what so that we don't have to think about this as being basically four think about this as being basically four think about this as being basically four different versions of code different versions of code different versions of code there is no need to distribute the array there is no need to distribute the array there is no need to distribute the array the whole array is all the time in the the whole array is all the time in the the whole array is all the time in the shared memory we just choose who's going shared memory we just choose who's going shared memory we just choose who's going to access what

## Message Passing Vs Shared Memory： Part 5
Kind: captions Language: en so let's summarize the differences so let's summarize the differences so let's summarize the differences between message-passing and shared between message-passing and shared between message-passing and shared memory regarding who does communication memory regarding who does communication memory regarding who does communication in message-passing the programmer is in message-passing the programmer is in message-passing the programmer is responsible for figuring out who sends responsible for figuring out who sends responsible for figuring out who sends and who receives the data in shared and who receives the data in shared and who receives the data in shared memory the communication is happening memory the communication is happening memory the communication is happening through shared variables in memory so through shared variables in memory so through shared variables in memory so all of the actual sending of the data all of the actual sending of the data all of the actual sending of the data back and forth is fully automatic the back and forth is fully automatic the back and forth is fully automatic the programmer doesn't need to do that they programmer doesn't need to do that they programmer doesn't need to do that they just store to memory and load from just store to memory and load from just store to memory and load from memory and it is the responsibility of memory and it is the responsibility of memory and it is the responsibility of the system to figure out when the data the system to figure out when the data the system to figure out when the data gets sent in to whom as far as data gets sent in to whom as far as data gets sent in to whom as far as data distribution is concerned that is which distribution is concerned that is which distribution is concerned that is which course get to have which data in their course get to have which data in their course get to have which data in their memory the program needs to manually do memory the program needs to manually do memory the program needs to manually do this data distribution by explicitly this data distribution by explicitly this data distribution by explicitly sending the data to the course that sending the data to the course that sending the data to the course that needed in shared memory the system is needed in shared memory the system is needed in shared memory the system is responsible for sending the data responsible for sending the data responsible for sending the data wherever it's needed so the programmer wherever it's needed so the programmer wherever it's needed so the programmer simply loads or stores to the data and simply loads or stores to the data and simply loads or stores to the data and it is job of the hardware and the it is job of the hardware and the it is job of the hardware and the software of the system to figure out software of the system to figure out software of the system to figure out which data should be local where at what which data should be local where at what which data should be local where at what time as far as how much hardware support time as far as how much hardware support time as far as how much hardware support we need for message passing this support we need for message passing this support we need for message passing this support is relatively simple we just need to is relatively simple we just need to is relatively simple we just need to basically have a network card in the basically have a network card in the basically have a network card in the network it can be a very fancy network network it can be a very fancy network network it can be a very fancy network but it's just a network in contrast for but it's just a network in contrast for but it's just a network in contrast for shared memory we need extensive hardware shared memory we need extensive hardware shared memory we need extensive hardware support that automatically figures out support that automatically figures out support that automatically figures out when to send data into home and where to when to send data into home and where to when to send data into home and where to cash in etc etc so we will be spending a cash in etc etc so we will be spending a cash in etc etc so we will be spending a large portion of the rest of this course large portion of the rest of this course large portion of the rest of this course on just figuring out what these hardware on just figuring out what these hardware on just figuring out what these hardware support needs to look like so as far as support needs to look like so as far as support needs to look like so as far as the programmer is concerned the the programmer is concerned the the programmer is concerned the programmer worries about two things programmer worries about two things programmer worries about two things correctness and performance of their correctness and performance of their correctness and performance of their program they want their program to be program they want their program to be program they want their program to be correct and do as well as possible on correct and do as well as possible on correct and do as well as possible on the available course in message passing the available course in message passing the available course in message passing getting correctness is difficult because getting correctness is difficult because getting correctness is difficult because you have to orchestrate everything you have to orchestrate everything you have to orchestrate everything perfectly and you can get all sorts of perfectly and you can get all sorts of perfectly and you can get all sorts of you know deadlock issues and wrong you know deadlock issues and wrong you know deadlock issues and wrong distribution etc etc in shared memory distribution etc etc in shared memory distribution etc etc in shared memory correctness is also not easy to get but correctness is also not easy to get but correctness is also not easy to get but it's less difficult than with message it's less difficult than with message it's less difficult than with message passing performance with message passing passing performance with message passing passing performance with message passing is difficult to get but once you get is difficult to get but once you get is difficult to get but once you get correctness performance is correctness performance is correctness performance is not far away in contrast shared-memory not far away in contrast shared-memory not far away in contrast shared-memory performance is very difficult to get and performance is very difficult to get and performance is very difficult to get and the difference between correctness and the difference between correctness and the difference between correctness and performance can be large so it might be performance can be large so it might be performance can be large so it might be relatively easy to get our program to be relatively easy to get our program to be relatively easy to get our program to be correct but its performance might not be correct but its performance might not be correct but its performance might not be anywhere near good and then it might anywhere near good and then it might anywhere near good and then it might take a lot more effort to get it to also take a lot more effort to get it to also take a lot more effort to get it to also perform well so it message-passing perform well so it message-passing perform well so it message-passing somehow by solving the correctness somehow by solving the correctness somehow by solving the correctness problem you are also solving a large problem you are also solving a large problem you are also solving a large part of the performance problem in part of the performance problem in part of the performance problem in shared memory it's easier to get things shared memory it's easier to get things shared memory it's easier to get things right but the effort to get it perform right but the effort to get it perform right but the effort to get it perform well might be even more than with well might be even more than with well might be even more than with message passing



---

# Playlist 4 Module 5 Raw Transcript

## Message Passing Vs Shared Memory Quiz： Part 5
Kind: captions Language: en so let's see if we understand the so let's see if we understand the so let's see if we understand the differences between message passing and differences between message passing and differences between message passing and shared memory if we have a program in shared memory if we have a program in shared memory if we have a program in which one core knows how to initialize which one core knows how to initialize which one core knows how to initialize an array and then all others need to an array and then all others need to an array and then all others need to read the entire array then the number of read the entire array then the number of read the entire array then the number of lines of code for the data distribution lines of code for the data distribution lines of code for the data distribution of the array once it has been of the array once it has been of the array once it has been initialized does message passing have initialized does message passing have initialized does message passing have fewer lines of code and share memory for fewer lines of code and share memory for fewer lines of code and share memory for this does it have about equal number of this does it have about equal number of this does it have about equal number of lines of code or do you think that it lines of code or do you think that it lines of code or do you think that it gets more lines of code than shared gets more lines of code than shared gets more lines of code than shared memory in which case write a larger than memory in which case write a larger than memory in which case write a larger than sign and then once we have achieved data sign and then once we have achieved data sign and then once we have achieved data distribution distribution distribution synchronization that is needed to make synchronization that is needed to make synchronization that is needed to make sure that the initialization is done sure that the initialization is done sure that the initialization is done before we can read the array adds how before we can read the array adds how before we can read the array adds how many lines of code in a message passing many lines of code in a message passing many lines of code in a message passing version of the code and in the shared version of the code and in the shared version of the code and in the shared memory version of the code you only need memory version of the code you only need memory version of the code you only need to say whether it's zero or larger than to say whether it's zero or larger than to say whether it's zero or larger than zero

## Message Passing Vs Shared Memory Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our message passing versus shared memory message passing versus shared memory message passing versus shared memory quiz if we have one core initializing an quiz if we have one core initializing an quiz if we have one core initializing an array and then all others reading the array and then all others reading the array and then all others reading the entire array data distribution the entire array data distribution the entire array data distribution the number of lines of code you need to do number of lines of code you need to do number of lines of code you need to do it in message passing it in message passing it in message passing is significant is significant is significant the number of lines of code we need for the number of lines of code we need for the number of lines of code we need for that in shared memory that in shared memory that in shared memory is really zero lines of code because is really zero lines of code because is really zero lines of code because once the array is initialized in memory once the array is initialized in memory once the array is initialized in memory others can simply read it they don't others can simply read it they don't others can simply read it they don't need to really distribute the data in need to really distribute the data in need to really distribute the data in any way in contrast in message passing any way in contrast in message passing any way in contrast in message passing the core that initializes the array now the core that initializes the array now the core that initializes the array now needs to send it explicitly to all the needs to send it explicitly to all the needs to send it explicitly to all the other cores and the other cores need to other cores and the other cores need to other cores and the other cores need to receive it explicitly so there is receive it explicitly so there is receive it explicitly so there is definitely definitely definitely more lines of code in message passing more lines of code in message passing more lines of code in message passing than in shared memory than in shared memory than in shared memory once you have the data distribution once you have the data distribution once you have the data distribution for shared memory you don't need to do for shared memory you don't need to do for shared memory you don't need to do anything for it for message passing you anything for it for message passing you anything for it for message passing you need to send and receive need to send and receive need to send and receive synchronization that makes sure that the synchronization that makes sure that the synchronization that makes sure that the array is initialized before we get to array is initialized before we get to array is initialized before we get to read it read it read it adds how many lines of code adds how many lines of code adds how many lines of code in message passing in message passing in message passing it adds zero lines of code because the it adds zero lines of code because the it adds zero lines of code because the sends and receives sends and receives sends and receives that we needed to do to distribute the that we needed to do to distribute the that we needed to do to distribute the data already ensure data already ensure data already ensure that until we receive the array we that until we receive the array we that until we receive the array we cannot read it so when we get to read it cannot read it so when we get to read it cannot read it so when we get to read it after the receive we know that it's been after the receive we know that it's been after the receive we know that it's been already initialized because the send already initialized because the send already initialized because the send happens after the initialization in happens after the initialization in happens after the initialization in shared memory however shared memory however shared memory however while the initialization is happening while the initialization is happening while the initialization is happening the other threads could just begin the other threads could just begin the other threads could just begin reading it so we need to reading it so we need to reading it so we need to add some explicit synchronization now to add some explicit synchronization now to add some explicit synchronization now to make sure that the readers wait while make sure that the readers wait while make sure that the readers wait while the initialization is happening so we the initialization is happening so we the initialization is happening so we here have more than zero lines of code here have more than zero lines of code here have more than zero lines of code in this case the message passing code is in this case the message passing code is in this case the message passing code is simpler as far as synchronization is simpler as far as synchronization is simpler as far as synchronization is concerned than the shared memory code concerned than the shared memory code concerned than the shared memory code this is typically going to be what this is typically going to be what this is typically going to be what happens between shared memory and happens between shared memory and happens between shared memory and message passing message passing needs to message passing message passing needs to message passing message passing needs to distribute data but in doing so it also distribute data but in doing so it also distribute data but in doing so it also synchronizes synchronizes synchronizes shared memory doesn't need to distribute shared memory doesn't need to distribute shared memory doesn't need to distribute data but if synchronization is needed data but if synchronization is needed data but if synchronization is needed after the distribution it needs to add after the distribution it needs to add after the distribution it needs to add explicit code to do that

## Shared Memory Hardware： Part 5
Kind: captions Language: en so what types of shared memory hardware so what types of shared memory hardware so what types of shared memory hardware do we have we can have multiple do we have we can have multiple do we have we can have multiple single cores single cores single cores that share the same physical address that share the same physical address that share the same physical address space so all of them can issue addresses space so all of them can issue addresses space so all of them can issue addresses to any memory address and the examples to any memory address and the examples to any memory address and the examples we have seen of this are the yuma and we have seen of this are the yuma and we have seen of this are the yuma and numa both of them actually are numa both of them actually are numa both of them actually are individual cores each of them individual cores each of them individual cores each of them being able to access the entire address being able to access the entire address being able to access the entire address space space space the other extreme is to only have one the other extreme is to only have one the other extreme is to only have one core core core and then do multi-threading and then do multi-threading and then do multi-threading basically run two threads by just time basically run two threads by just time basically run two threads by just time sharing the same core so because they're sharing the same core so because they're sharing the same core so because they're now running really on the same core they now running really on the same core they now running really on the same core they are naturally accessing the same are naturally accessing the same are naturally accessing the same physical memory so we get the shared physical memory so we get the shared physical memory so we get the shared memory behavior simply by virtue of memory behavior simply by virtue of memory behavior simply by virtue of actually not having more than one core actually not having more than one core actually not having more than one core and now the question is is there and now the question is is there and now the question is is there something in between them because this something in between them because this something in between them because this is not really capable of benefiting from is not really capable of benefiting from is not really capable of benefiting from having multi-threading we can have having multi-threading we can have having multi-threading we can have hardware multi-threading support in one hardware multi-threading support in one hardware multi-threading support in one core that actually tries to benefit from core that actually tries to benefit from core that actually tries to benefit from there being multiple threads there being multiple threads there being multiple threads this hardware multi-threading can be at this hardware multi-threading can be at this hardware multi-threading can be at a coarse grain a coarse grain a coarse grain where we change the thread we are where we change the thread we are where we change the thread we are working on every few cycles working on every few cycles working on every few cycles and we now need to have harder support and we now need to have harder support and we now need to have harder support that switches between threads that switches between threads that switches between threads this often because without hardware this often because without hardware this often because without hardware support support support we would be spending all of the time we would be spending all of the time we would be spending all of the time just saving and restoring registers of just saving and restoring registers of just saving and restoring registers of these threads so we actually need to now these threads so we actually need to now these threads so we actually need to now have every thread has its own set of have every thread has its own set of have every thread has its own set of hardware registers and so on so there is hardware registers and so on so there is hardware registers and so on so there is hardware support needed for this we can hardware support needed for this we can hardware support needed for this we can have fine grain hardware multi-threading have fine grain hardware multi-threading have fine grain hardware multi-threading where every cycle we are working on where every cycle we are working on where every cycle we are working on another thread another thread another thread this of course needs even more harder this of course needs even more harder this of course needs even more harder support because now we need to be able support because now we need to be able support because now we need to be able to very quickly change between these to very quickly change between these to very quickly change between these threads threads threads and we can also have and we can also have and we can also have simultaneous multi-threading or smt simultaneous multi-threading or smt simultaneous multi-threading or smt where in any given cycle we could be where in any given cycle we could be where in any given cycle we could be doing instructions that belong to these doing instructions that belong to these doing instructions that belong to these different threads this is called different threads this is called different threads this is called simultaneous multi-threading or smt simultaneous multi-threading or smt simultaneous multi-threading or smt and it's also called hyper threading by and it's also called hyper threading by and it's also called hyper threading by some manufacturers of processors so now some manufacturers of processors so now some manufacturers of processors so now we need hardware support for any one of we need hardware support for any one of we need hardware support for any one of these three and of course the hardware these three and of course the hardware these three and of course the hardware support becomes more extensive as we support becomes more extensive as we support becomes more extensive as we move from coarse grain to fine grain to move from coarse grain to fine grain to move from coarse grain to fine grain to simultaneous so the question is but what simultaneous so the question is but what simultaneous so the question is but what do we benefit by kind of sharing the do we benefit by kind of sharing the do we benefit by kind of sharing the same core same core same core by multiple threads don't we get the by multiple threads don't we get the by multiple threads don't we get the same thing as here

## Multi Threading Performance： Part 5
Kind: captions Language: en so let's discuss the performance of so let's discuss the performance of so let's discuss the performance of multi trading and how can it be better multi trading and how can it be better multi trading and how can it be better than simply running things one at a time than simply running things one at a time than simply running things one at a time so if we have a processor with no so if we have a processor with no so if we have a processor with no multi-threading support then this is how multi-threading support then this is how multi-threading support then this is how two threads might run these are Cycles two threads might run these are Cycles two threads might run these are Cycles going in this direction so each Notch going in this direction so each Notch going in this direction so each Notch here is a cycle and we have four here is a cycle and we have four here is a cycle and we have four instructions that can execute in this instructions that can execute in this instructions that can execute in this cycle so this is what might happen a cycle so this is what might happen a cycle so this is what might happen a green thread might be using this and green thread might be using this and green thread might be using this and this slot in this cycle and the other this slot in this cycle and the other this slot in this cycle and the other two issue slots are not used because the two issue slots are not used because the two issue slots are not used because the processor has no ready instructions to processor has no ready instructions to processor has no ready instructions to dis patch to execution in this cycle dis patch to execution in this cycle dis patch to execution in this cycle let's say there is a data dependence so let's say there is a data dependence so let's say there is a data dependence so next Cycle One instruction can wake up next Cycle One instruction can wake up next Cycle One instruction can wake up and then let's say that in the next and then let's say that in the next and then let's say that in the next cycle we can have three instructions go cycle we can have three instructions go cycle we can have three instructions go but then let's say that all of the but then let's say that all of the but then let's say that all of the instructions are now waiting for instructions are now waiting for instructions are now waiting for something to happen so let's say for a something to happen so let's say for a something to happen so let's say for a few Cycles we don't get much happening few Cycles we don't get much happening few Cycles we don't get much happening then when it happens we get to execute then when it happens we get to execute then when it happens we get to execute let's say all four let's say all four let's say all four and then maybe another cycle with two and then maybe another cycle with two and then maybe another cycle with two Etc so things can be like this basically Etc so things can be like this basically Etc so things can be like this basically much of the time we are running with a much of the time we are running with a much of the time we are running with a less than fully utilized processor less than fully utilized processor less than fully utilized processor pipeline let's say that at this point we pipeline let's say that at this point we pipeline let's say that at this point we get an interrupt and because there is get an interrupt and because there is get an interrupt and because there is another thread that is ready to run we another thread that is ready to run we another thread that is ready to run we now get many cycles of the operating now get many cycles of the operating now get many cycles of the operating system doing its thing so it's also system doing its thing so it's also system doing its thing so it's also going to utilize the pipeline in some going to utilize the pipeline in some going to utilize the pipeline in some way but it spends many cycles basically way but it spends many cycles basically way but it spends many cycles basically figuring out which thread to run next figuring out which thread to run next figuring out which thread to run next eventually decides that it's going to be eventually decides that it's going to be eventually decides that it's going to be the blue thread and begins executing the blue thread and begins executing the blue thread and begins executing instructions from that thread on the instructions from that thread on the instructions from that thread on the processor so this is where we return processor so this is where we return processor so this is where we return from the inup and start executing a new from the inup and start executing a new from the inup and start executing a new thread because let's say we have these thread because let's say we have these thread because let's say we have these two threads ready to go so the blue two threads ready to go so the blue two threads ready to go so the blue thread in a similar way will be using thread in a similar way will be using thread in a similar way will be using the processor partially let's say it has the processor partially let's say it has the processor partially let's say it has a level one miss takes a while to get a level one miss takes a while to get a level one miss takes a while to get the thing from level two meanwhile maybe the thing from level two meanwhile maybe the thing from level two meanwhile maybe it's able to do something for a while it's able to do something for a while it's able to do something for a while but eventually runs out of things to do but eventually runs out of things to do but eventually runs out of things to do and then for example starts doing and then for example starts doing and then for example starts doing something again and let's say this whole something again and let's say this whole something again and let's say this whole cycle is used so this is what might cycle is used so this is what might cycle is used so this is what might happen in a processor with no multi- happen in a processor with no multi- happen in a processor with no multi- threading support in that case really threading support in that case really threading support in that case really the overhead that you created by having the overhead that you created by having the overhead that you created by having to switch between threads is probably to switch between threads is probably to switch between threads is probably larger than the gains we are getting larger than the gains we are getting larger than the gains we are getting from running them we were better off from running them we were better off from running them we were better off just running a single thread that does just running a single thread that does just running a single thread that does the work of both of these threads so the work of both of these threads so the work of both of these threads so really a single procer with no multi- really a single procer with no multi- really a single procer with no multi- threading is capable of doing this with threading is capable of doing this with threading is capable of doing this with a good operating system not because it's a good operating system not because it's a good operating system not because it's better for performance to break work better for performance to break work better for performance to break work into multiple threads but because we into multiple threads but because we into multiple threads but because we want to be able to simultaneously run want to be able to simultaneously run want to be able to simultaneously run several applications and make progress several applications and make progress several applications and make progress in them so that for example if you're in them so that for example if you're in them so that for example if you're playing games and playing music at the playing games and playing music at the playing games and playing music at the same time we alternate between the game same time we alternate between the game same time we alternate between the game and the music player fast enough for you and the music player fast enough for you and the music player fast enough for you to not be able to tell that they're to not be able to tell that they're to not be able to tell that they're really executing one at a time on your really executing one at a time on your really executing one at a time on your One Core One Core One Core if we have a chip multiprocessor we get if we have a chip multiprocessor we get if we have a chip multiprocessor we get to do something like this now we have to do something like this now we have to do something like this now we have two cores Each of which pretty much gets two cores Each of which pretty much gets two cores Each of which pretty much gets to execute one thread so it's faster of to execute one thread so it's faster of to execute one thread so it's faster of course than this and we can benefit course than this and we can benefit course than this and we can benefit truly from being able to write multi- truly from being able to write multi- truly from being able to write multi- threaded programs but you have to have threaded programs but you have to have threaded programs but you have to have two cores for it so the cost is about two cores for it so the cost is about two cores for it so the cost is about twice the cost of a single core here so twice the cost of a single core here so twice the cost of a single core here so with fine grain multi- trading what we with fine grain multi- trading what we with fine grain multi- trading what we have is really One Core but with have is really One Core but with have is really One Core but with separate sets of registers for these separate sets of registers for these separate sets of registers for these threads and some scheduling logic with threads and some scheduling logic with threads and some scheduling logic with fine grain multi threading every cycle fine grain multi threading every cycle fine grain multi threading every cycle we can switch between threads so what we can switch between threads so what we can switch between threads so what happens is the green thread gets exactly happens is the green thread gets exactly happens is the green thread gets exactly the same behavior in the first cycle in the same behavior in the first cycle in the same behavior in the first cycle in the second cycle we do the blue thread the second cycle we do the blue thread the second cycle we do the blue thread so we get the behavior of its first so we get the behavior of its first so we get the behavior of its first cycle here then we do some of the work cycle here then we do some of the work cycle here then we do some of the work from the green thread again then from from the green thread again then from from the green thread again then from the blue thread third cycle of the green the blue thread third cycle of the green the blue thread third cycle of the green thread third cycle of the blue thread thread third cycle of the blue thread thread third cycle of the blue thread and now we would be running the green and now we would be running the green and now we would be running the green thread except that there is nothing to thread except that there is nothing to thread except that there is nothing to run there but what happens is the blue run there but what happens is the blue run there but what happens is the blue thread has something to do so we spend thread has something to do so we spend thread has something to do so we spend another cycle on the blue thread and another cycle on the blue thread and another cycle on the blue thread and then the green thread is still somewhere then the green thread is still somewhere then the green thread is still somewhere here there is nothing ready to run but here there is nothing ready to run but here there is nothing ready to run but these operations are going on so the these operations are going on so the these operations are going on so the blue thread gets one more cycle here at blue thread gets one more cycle here at blue thread gets one more cycle here at this point the blue thread has nothing this point the blue thread has nothing this point the blue thread has nothing to do but the green one now has to do but the green one now has to do but the green one now has something to do so we end up doing the something to do so we end up doing the something to do so we end up doing the work of the green thread and then for work of the green thread and then for work of the green thread and then for another cycle then let's say neither of another cycle then let's say neither of another cycle then let's say neither of the threads can do something but then the threads can do something but then the threads can do something but then the blue thread is able to do this the blue thread is able to do this the blue thread is able to do this because it's been three Cycles since because it's been three Cycles since because it's been three Cycles since here and then again and as you can see here and then again and as you can see here and then again and as you can see we get done faster than here because we we get done faster than here because we we get done faster than here because we are able to eliminate some of the Cycles are able to eliminate some of the Cycles are able to eliminate some of the Cycles where we were not able to do anything so where we were not able to do anything so where we were not able to do anything so fine grain multi trading benefits from fine grain multi trading benefits from fine grain multi trading benefits from the fact that when there is a long the fact that when there is a long the fact that when there is a long period of idleness in one thread for period of idleness in one thread for period of idleness in one thread for example due to a cache miss you might be example due to a cache miss you might be example due to a cache miss you might be able to run still instructions from the able to run still instructions from the able to run still instructions from the other thread and finally let's see what other thread and finally let's see what other thread and finally let's see what happens in smt in smt we are able to mix happens in smt in smt we are able to mix happens in smt in smt we are able to mix instructions from different threads in instructions from different threads in instructions from different threads in the same cycle so what we will get is the same cycle so what we will get is the same cycle so what we will get is the green one gets this the blue one the green one gets this the blue one the green one gets this the blue one also wants to use the bottom slot and also wants to use the bottom slot and also wants to use the bottom slot and let's say that there is a unit there for let's say that there is a unit there for let's say that there is a unit there for example the load unit that you have to example the load unit that you have to example the load unit that you have to use here but we can still run this in use here but we can still run this in use here but we can still run this in the same cycle next cycle the green the same cycle next cycle the green the same cycle next cycle the green thread does this the blue one wants to thread does this the blue one wants to thread does this the blue one wants to now do this this and this and we can now do this this and this and we can now do this this and this and we can let's say do these two but let's say let's say do these two but let's say let's say do these two but let's say there is a dependence from this to this there is a dependence from this to this there is a dependence from this to this so we can only do this one next cycle so we can only do this one next cycle so we can only do this one next cycle the green thread can do this and from the green thread can do this and from the green thread can do this and from the blue thread we can maybe do this the blue thread we can maybe do this the blue thread we can maybe do this instruction or maybe not so let's say we instruction or maybe not so let's say we instruction or maybe not so let's say we can't because maybe this one depends can't because maybe this one depends can't because maybe this one depends also on the one that we haven't done or also on the one that we haven't done or also on the one that we haven't done or the next one now however there is some the next one now however there is some the next one now however there is some period of idleness in the green thread period of idleness in the green thread period of idleness in the green thread and now the blue thread gets to make up and now the blue thread gets to make up and now the blue thread gets to make up for missing out on some things so it's for missing out on some things so it's for missing out on some things so it's able to maybe do this and this dependent able to maybe do this and this dependent able to maybe do this and this dependent instruction and at this point the green instruction and at this point the green instruction and at this point the green thread is ready to run something so we thread is ready to run something so we thread is ready to run something so we can choose whether to run the green or can choose whether to run the green or can choose whether to run the green or the blue one let's say that the green the blue one let's say that the green the blue one let's say that the green thread has a higher priority so we thread has a higher priority so we thread has a higher priority so we always try to do instructions from the always try to do instructions from the always try to do instructions from the green thread and only fill the gaps with green thread and only fill the gaps with green thread and only fill the gaps with the blue ones now we've done this next the blue ones now we've done this next the blue ones now we've done this next cycle we still do whatever we can from cycle we still do whatever we can from cycle we still do whatever we can from the green thread We Now can do this and the green thread We Now can do this and the green thread We Now can do this and now we only have the blue thread left to now we only have the blue thread left to now we only have the blue thread left to do if the green thread has more stuff to do if the green thread has more stuff to do if the green thread has more stuff to do we can do this so as you can see with do we can do this so as you can see with do we can do this so as you can see with smt we get to populate unused smt we get to populate unused smt we get to populate unused slots in what would otherwise be a full slots in what would otherwise be a full slots in what would otherwise be a full speed execution of the green thread so speed execution of the green thread so speed execution of the green thread so that the blue thread gets to make that the blue thread gets to make that the blue thread gets to make progress while the green thread really progress while the green thread really progress while the green thread really is not suffering from it and we can do is not suffering from it and we can do is not suffering from it and we can do that for more than just two threads so that for more than just two threads so that for more than just two threads so for example if if we had four threads we for example if if we had four threads we for example if if we had four threads we might be able to actually do another might be able to actually do another might be able to actually do another thread at a slightly slower pace and thread at a slightly slower pace and thread at a slightly slower pace and blue while we are also doing this so blue while we are also doing this so blue while we are also doing this so with smt what we doing we're really with smt what we doing we're really with smt what we doing we're really using underutilized issue slots in our using underutilized issue slots in our using underutilized issue slots in our outof order supercal processor the outof order supercal processor the outof order supercal processor the hardware cost to do this is very small hardware cost to do this is very small hardware cost to do this is very small our fetch stage needs to be slightly our fetch stage needs to be slightly our fetch stage needs to be slightly more complicated to allow us to fetch more complicated to allow us to fetch more complicated to allow us to fetch from one or the other PC and our from one or the other PC and our from one or the other PC and our register file needs to be slightly more register file needs to be slightly more register file needs to be slightly more complicated because really there are now complicated because really there are now complicated because really there are now two sets of architectural registers but two sets of architectural registers but two sets of architectural registers but other than that the whole comp other than that the whole comp other than that the whole comp complicated outof order engine stays the complicated outof order engine stays the complicated outof order engine stays the same so let's say this is going to cost same so let's say this is going to cost same so let's say this is going to cost something like 5% of the cost of a no something like 5% of the cost of a no something like 5% of the cost of a no multi threaded processor this here again multi threaded processor this here again multi threaded processor this here again after we do register renaming and after we do register renaming and after we do register renaming and everything else the processor scheduler everything else the processor scheduler everything else the processor scheduler stays just the same we just need to put stays just the same we just need to put stays just the same we just need to put instructions from multiple threads in instructions from multiple threads in instructions from multiple threads in the instruction window so that they the instruction window so that they the instruction window so that they could be scheduled from there and now could be scheduled from there and now could be scheduled from there and now also when we commit we have to figure also when we commit we have to figure also when we commit we have to figure out which instruction belongs to which out which instruction belongs to which out which instruction belongs to which commit and so on so the cost is slightly commit and so on so the cost is slightly commit and so on so the cost is slightly larger than fine grain but still not larger than fine grain but still not larger than fine grain but still not very large because again most of the very large because again most of the very large because again most of the complicated reservation stations and complicated reservation stations and complicated reservation stations and execution units and everything else execution units and everything else execution units and everything else stays pretty much unmodified so this is stays pretty much unmodified so this is stays pretty much unmodified so this is going to have performance that is maybe going to have performance that is maybe going to have performance that is maybe very close to a very close to a very close to a CMP two threads here two threads here CMP two threads here two threads here CMP two threads here two threads here maybe making almost as much progress but maybe making almost as much progress but maybe making almost as much progress but at a very small fraction of this cost

## SMT Vs Dual Core Quiz： Part 5
Kind: captions Language: en From the top of the Comper solids I have From the top of the Comper solids I have From the top of the Comper solids I have enamel trading in dual-core processors enamel trading in dual-core processors enamel trading in dual-core processors what's inside Capitu attractions Kyoto what's inside Capitu attractions Kyoto what's inside Capitu attractions Kyoto money is floating points most of the money is floating points most of the money is floating points most of the Stuff dose.de understands that the names Still Stuff dose.de understands that the names Still Stuff dose.de understands that the names Still nothing at Intel xeon Blue floating Point nothing at Intel xeon Blue floating Point nothing at Intel xeon Blue floating Point Operation is web WhatsApp Java foreach Operation is web WhatsApp Java foreach Operation is web WhatsApp Java foreach technical YouTube floating point and technical YouTube floating point and technical YouTube floating point and to enter This installation is Where do I to enter This installation is Where do I to enter This installation is Where do I get more performance for hardware dollar get more performance for hardware dollar get more performance for hardware dollar Whey Protein with Spa said the color ladies Whey Protein with Spa said the color ladies Whey Protein with Spa said the color ladies with Sweet Colors

## SMT Vs Dual Core Solution Quiz： Part 5
Kind: captions Language: en so let's answer our smt versus dual core so let's answer our smt versus dual core so let's answer our smt versus dual core quiz we have a floating point intensive quiz we have a floating point intensive quiz we have a floating point intensive thread thread thread it's it's it's going to use pretty much going to use pretty much going to use pretty much these two issue slots these two issue slots these two issue slots and very rarely use these and then we and very rarely use these and then we and very rarely use these and then we have an integer only thread that only have an integer only thread that only have an integer only thread that only gets to use these two so with the dual gets to use these two so with the dual gets to use these two so with the dual core processor core processor core processor one of the cores really gets to be one of the cores really gets to be one of the cores really gets to be two issue plus maybe a little bit two issue plus maybe a little bit two issue plus maybe a little bit another one gets to be just to issue another one gets to be just to issue another one gets to be just to issue because it never sees floating point because it never sees floating point because it never sees floating point instructions that it needs to have more instructions that it needs to have more instructions that it needs to have more than two issue than two issue than two issue in smt however in smt however in smt however we would be able we would be able we would be able to do to do to do two instructions two instructions two instructions from this thread and two instructions from this thread and two instructions from this thread and two instructions from this thread per cycle so we will from this thread per cycle so we will from this thread per cycle so we will getting very similar performance and as getting very similar performance and as getting very similar performance and as we have seen then this is the more we have seen then this is the more we have seen then this is the more effective approach because the cost that effective approach because the cost that effective approach because the cost that we are getting we are getting we are getting here here here versus here is significantly lower so we versus here is significantly lower so we versus here is significantly lower so we get similar performance for get similar performance for get similar performance for almost half the cost here now almost half the cost here now almost half the cost here now what would happen however if we didn't what would happen however if we didn't what would happen however if we didn't have a floating point intensive thread have a floating point intensive thread have a floating point intensive thread and instead got two of these and instead got two of these and instead got two of these in that case the two core is slightly in that case the two core is slightly in that case the two core is slightly more cost effective more cost effective more cost effective because it gets the two issue because it gets the two issue because it gets the two issue performance in each core performance in each core performance in each core for twice the cost of one core whereas for twice the cost of one core whereas for twice the cost of one core whereas this this this gets us the performance of a single core gets us the performance of a single core gets us the performance of a single core because both of these are sharing the because both of these are sharing the because both of these are sharing the two issue slots for a cost that is more two issue slots for a cost that is more two issue slots for a cost that is more than one core so in that case than one core so in that case than one core so in that case this would be the more cost effective this would be the more cost effective this would be the more cost effective option so as you can see it's not that option so as you can see it's not that option so as you can see it's not that smt always wins over multiple cores it's smt always wins over multiple cores it's smt always wins over multiple cores it's that it depends on exactly what types of that it depends on exactly what types of that it depends on exactly what types of programs we have and exactly what's the programs we have and exactly what's the programs we have and exactly what's the distribution of issue slots among units distribution of issue slots among units distribution of issue slots among units and how different applications are using and how different applications are using and how different applications are using it so the floating point intensive and it so the floating point intensive and it so the floating point intensive and integer only and this type of a split integer only and this type of a split integer only and this type of a split between issue slots is an extreme between issue slots is an extreme between issue slots is an extreme example that makes the smt look really example that makes the smt look really example that makes the smt look really good

## SMT Hardware Changes： Part 5
Kind: captions Language: en so we said that the SMT processor is not so we said that the SMT processor is not so we said that the SMT processor is not that much more expensive than a single that much more expensive than a single that much more expensive than a single threaded processor that has the same threaded processor that has the same threaded processor that has the same issue it and everything let's see what issue it and everything let's see what issue it and everything let's see what the hardware changes for SMT look like the hardware changes for SMT look like the hardware changes for SMT look like so that we can convince ourselves that so that we can convince ourselves that so that we can convince ourselves that it's indeed not that much more expensive it's indeed not that much more expensive it's indeed not that much more expensive in a normal processor we would have a in a normal processor we would have a in a normal processor we would have a program counter that we use to access program counter that we use to access program counter that we use to access the instruction cache to fetch our the instruction cache to fetch our the instruction cache to fetch our instructions because this is a instructions because this is a instructions because this is a superscalar processor we are fetching superscalar processor we are fetching superscalar processor we are fetching more than just one instruction with this more than just one instruction with this more than just one instruction with this program counter then we get into a program counter then we get into a program counter then we get into a decoder that is capable of decoding decoder that is capable of decoding decoder that is capable of decoding multiple instructions per cycle then a multiple instructions per cycle then a multiple instructions per cycle then a renamer that gets to read and update the renamer that gets to read and update the renamer that gets to read and update the rat for this thread and once the rat for this thread and once the rat for this thread and once the instruction is renamed it gets to the instruction is renamed it gets to the instruction is renamed it gets to the reservation stations and then from here reservation stations and then from here reservation stations and then from here we send the instructions for execution we send the instructions for execution we send the instructions for execution and in a Rob we need to track what's the and in a Rob we need to track what's the and in a Rob we need to track what's the next committed instruction from this next committed instruction from this next committed instruction from this thread so if we want to add the ability thread so if we want to add the ability thread so if we want to add the ability to simultaneously multi thread another to simultaneously multi thread another to simultaneously multi thread another thread here we definitely need a thread here we definitely need a thread here we definitely need a separate PC here and we need a logic separate PC here and we need a logic separate PC here and we need a logic that will fetch from these two pieces we that will fetch from these two pieces we that will fetch from these two pieces we could do this every cycle we fetch from could do this every cycle we fetch from could do this every cycle we fetch from both of the pieces half of the both of the pieces half of the both of the pieces half of the instructions or we can actually do one instructions or we can actually do one instructions or we can actually do one cycle fetch everything you can from this cycle fetch everything you can from this cycle fetch everything you can from this PC one cycle fetch everything you came PC one cycle fetch everything you came PC one cycle fetch everything you came from this PC so SMT usually refers to from this PC so SMT usually refers to from this PC so SMT usually refers to what's happening here but here we could what's happening here but here we could what's happening here but here we could actually be doing fine grained actually be doing fine grained actually be doing fine grained multi-threading as far as fetching is multi-threading as far as fetching is multi-threading as far as fetching is concerned most processors do SMT concerned most processors do SMT concerned most processors do SMT actually do it that way decoding is actually do it that way decoding is actually do it that way decoding is completely oblivious to where the completely oblivious to where the completely oblivious to where the instructions came from so we don't instructions came from so we don't instructions came from so we don't really need to change anything about really need to change anything about really need to change anything about their decoding logic the renamer does their decoding logic the renamer does their decoding logic the renamer does the same thing regardless of where the the same thing regardless of where the the same thing regardless of where the instruction came from except that the instruction came from except that the instruction came from except that the rat is accesses needs to be different so rat is accesses needs to be different so rat is accesses needs to be different so now we have two rats one for each thread now we have two rats one for each thread now we have two rats one for each thread that we need to access depending on what that we need to access depending on what that we need to access depending on what are we renaming after renaming are we renaming after renaming are we renaming after renaming instructions got separate physical instructions got separate physical instructions got separate physical registers for each thread registers for each thread registers for each thread so the reservation stations are simply so the reservation stations are simply so the reservation stations are simply shared by the two threads the execution shared by the two threads the execution shared by the two threads the execution units get to execute the operations with units get to execute the operations with units get to execute the operations with the values they are given so they have the values they are given so they have the values they are given so they have no clue where the instruction really no clue where the instruction really no clue where the instruction really came from and then when we get to the RO came from and then when we get to the RO came from and then when we get to the RO B we need to either have a separate B we need to either have a separate B we need to either have a separate therapy when its own commit point in therapy when its own commit point in therapy when its own commit point in which case each of the threads gets to which case each of the threads gets to which case each of the threads gets to commit its instruction in order what we commit its instruction in order what we commit its instruction in order what we actually put all of the instruction in actually put all of the instruction in actually put all of the instruction in some interleaved order in the arrow B in some interleaved order in the arrow B in some interleaved order in the arrow B in which case we will not be able to commit which case we will not be able to commit which case we will not be able to commit an instruction from one thread until is an instruction from one thread until is an instruction from one thread until is all the instructions from it and the all the instructions from it and the all the instructions from it and the other thread that preceded have been other thread that preceded have been other thread that preceded have been committed because the rob is a large and committed because the rob is a large and committed because the rob is a large and complex piece of hardware typically we complex piece of hardware typically we complex piece of hardware typically we don't do this and we actually suffer don't do this and we actually suffer don't do this and we actually suffer here by just committing the threads here by just committing the threads here by just committing the threads together but if we have sent the together but if we have sent the together but if we have sent the instructions in order to the reservation instructions in order to the reservation instructions in order to the reservation stations and there are ro B then the stations and there are ro B then the stations and there are ro B then the order in the Rob is still the correct order in the Rob is still the correct order in the Rob is still the correct order it's just at some times we could order it's just at some times we could order it's just at some times we could have committed a instruction from one have committed a instruction from one have committed a instruction from one thread but we have to wait for the other thread but we have to wait for the other thread but we have to wait for the other thread to also commit some instructions thread to also commit some instructions thread to also commit some instructions before that so it's slightly less before that so it's slightly less before that so it's slightly less optimal but still works fine the thing optimal but still works fine the thing optimal but still works fine the thing that happens at commit though is that we that happens at commit though is that we that happens at commit though is that we deposit the results into the deposit the results into the deposit the results into the architectural register file and also architectural register file and also architectural register file and also when we send instructions for execution when we send instructions for execution when we send instructions for execution we send it along with the data that we send it along with the data that we send it along with the data that comes from the IRF our threads do have comes from the IRF our threads do have comes from the IRF our threads do have different registers R 0 here is not the different registers R 0 here is not the different registers R 0 here is not the same as R 0 here so we need a separate same as R 0 here so we need a separate same as R 0 here so we need a separate IRF and a separate path to each of the IRF and a separate path to each of the IRF and a separate path to each of the RS here and also the separate read ports RS here and also the separate read ports RS here and also the separate read ports here so as you can see the complexity here so as you can see the complexity here so as you can see the complexity largely lies in having multiple pieces largely lies in having multiple pieces largely lies in having multiple pieces and being able to fetch from either of and being able to fetch from either of and being able to fetch from either of them and in the logic that immediately them and in the logic that immediately them and in the logic that immediately precedes the reservation stations but precedes the reservation stations but precedes the reservation stations but the really complicated part of the the really complicated part of the the really complicated part of the processor which is the reservation processor which is the reservation processor which is the reservation stations the broadcast of results and so stations the broadcast of results and so stations the broadcast of results and so on stays the same so this is why the on stays the same so this is why the on stays the same so this is why the cost is not twice the cost of the cost is not twice the cost of the cost is not twice the cost of the processor most of the cost of the processor most of the cost of the processor most of the cost of the processor is in the cache array here the processor is in the cache array here the processor is in the cache array here the complex Hardware here the large Rob complex Hardware here the large Rob complex Hardware here the large Rob where you can insert things and wait for where you can insert things and wait for where you can insert things and wait for them to finish and so on them to finish and so on them to finish and so on and commit and also the data cache and and commit and also the data cache and and commit and also the data cache and what we are really adding is another PC what we are really adding is another PC what we are really adding is another PC here same size caches before another rat here same size caches before another rat here same size caches before another rat another IRF but keep the complex part another IRF but keep the complex part another IRF but keep the complex part alone so that's why the cost is not very alone so that's why the cost is not very alone so that's why the cost is not very very high

## SMT, D$, TLB： Part 5
Kind: captions Language: en now let's look at the relationship now let's look at the relationship now let's look at the relationship between smt data cache in TLB if we have between smt data cache in TLB if we have between smt data cache in TLB if we have a virtual indexed virtually tagged cache a virtual indexed virtually tagged cache a virtual indexed virtually tagged cache then we simply send the virtual address then we simply send the virtual address then we simply send the virtual address into the cache and use the data that into the cache and use the data that into the cache and use the data that comes out of the cash so with virtual comes out of the cash so with virtual comes out of the cash so with virtual index virtually tagged cache is the index virtually tagged cache is the index virtually tagged cache is the problem is that our two threads might problem is that our two threads might problem is that our two threads might have different address spaces and now we have different address spaces and now we have different address spaces and now we are sending them simultaneously to the are sending them simultaneously to the are sending them simultaneously to the cash the cash is no clue which one of cash the cash is no clue which one of cash the cash is no clue which one of the two it's getting so unless we make the two it's getting so unless we make the two it's getting so unless we make something really complicated here to something really complicated here to something really complicated here to figure out which is which we will have figure out which is which we will have figure out which is which we will have some aliasing between the two threads some aliasing between the two threads some aliasing between the two threads note that normally with the givt cash note that normally with the givt cash note that normally with the givt cash when we switch between threads in a when we switch between threads in a when we switch between threads in a coarse grain we would flush the cache coarse grain we would flush the cache coarse grain we would flush the cache between that but now that the two between that but now that the two between that but now that the two threads can pretty much every cycle threads can pretty much every cycle threads can pretty much every cycle you're getting one or the other we you're getting one or the other we you're getting one or the other we cannot flush the cache in between those cannot flush the cache in between those cannot flush the cache in between those so with the virtual index virtually tag so with the virtual index virtually tag so with the virtual index virtually tag cache we are running a real risk of cache we are running a real risk of cache we are running a real risk of getting the wrong data because the same getting the wrong data because the same getting the wrong data because the same address in the two threads that are address in the two threads that are address in the two threads that are accessing the cash pretty much accessing the cash pretty much accessing the cash pretty much alternatingly in a very haphazard way alternatingly in a very haphazard way alternatingly in a very haphazard way might map to different physical might map to different physical might map to different physical addresses but the data cache doesn't addresses but the data cache doesn't addresses but the data cache doesn't know that so one thread saves data to know that so one thread saves data to know that so one thread saves data to each stack another one pops data from each stack another one pops data from each stack another one pops data from each stack and if they are at the same each stack and if they are at the same each stack and if they are at the same virtual address we get the data virtual address we get the data virtual address we get the data forwarded from one tree to the other forwarded from one tree to the other forwarded from one tree to the other which we didn't want if we use a virtual which we didn't want if we use a virtual which we didn't want if we use a virtual index physically tagged cache that index physically tagged cache that index physically tagged cache that avoids aliasing then we are fine we will avoids aliasing then we are fine we will avoids aliasing then we are fine we will find the set in the cash and the find the set in the cash and the find the set in the cash and the physical tags that come out will be physical tags that come out will be physical tags that come out will be compared with those from the TLB so compared with those from the TLB so compared with those from the TLB so assuming that the TLB does what it's assuming that the TLB does what it's assuming that the TLB does what it's supposed to do we will have the same supposed to do we will have the same supposed to do we will have the same virtual address from the two threads virtual address from the two threads virtual address from the two threads look up the same set but find different look up the same set but find different look up the same set but find different data based on the physical address in data based on the physical address in data based on the physical address in that red however that assumes that when that red however that assumes that when that red however that assumes that when we use the virtual address to access the we use the virtual address to access the we use the virtual address to access the TLB it doesn't look at just the page TLB it doesn't look at just the page TLB it doesn't look at just the page number in the virtual address because number in the virtual address because number in the virtual address because the two threads could have the same page the two threads could have the same page the two threads could have the same page number translate two different things so number translate two different things so number translate two different things so for virtual index physically tagged or for virtual index physically tagged or for virtual index physically tagged or physical index physically tagged caches physical index physically tagged caches physical index physically tagged caches the cash is just fine but the TLB must the cash is just fine but the TLB must the cash is just fine but the TLB must be thread aware that can be done by be thread aware that can be done by be thread aware that can be done by adding a single bit to each entry that adding a single bit to each entry that adding a single bit to each entry that tells us which tells us which tells us which of the two threads is this mapping for of the two threads is this mapping for of the two threads is this mapping for in that way indexing into the TLB with in that way indexing into the TLB with in that way indexing into the TLB with the virtual address we'll find a set in the virtual address we'll find a set in the virtual address we'll find a set in the TLB that has multiple entries and we the TLB that has multiple entries and we the TLB that has multiple entries and we will only be looking at entries that will only be looking at entries that will only be looking at entries that both match our virtual page number and both match our virtual page number and both match our virtual page number and also match at red ID so we will now send also match at red ID so we will now send also match at red ID so we will now send the page number and also the thread ID the page number and also the thread ID the page number and also the thread ID here and the TLB needs to match both here and the TLB needs to match both here and the TLB needs to match both before it treats this as a TLD hit and before it treats this as a TLD hit and before it treats this as a TLD hit and produces the physical address here

## SMT and Cache Performance： Part 5
Kind: captions Language: en in science, there are only smth Performance Performance and a n and with a and

## SMT and Cache Quiz： Part 5
Kind: captions Language: en so let's see if we understand the so let's see if we understand the so let's see if we understand the relationship between smt and cache relationship between smt and cache relationship between smt and cache behavior let's say we have a program behavior let's say we have a program behavior let's say we have a program named a who's working set is 10 named a who's working set is 10 named a who's working set is 10 kilobytes and we have programs being and kilobytes and we have programs being and kilobytes and we have programs being and see who's working sets are 4 kilobytes see who's working sets are 4 kilobytes see who's working sets are 4 kilobytes and one kilobytes respectively let's say and one kilobytes respectively let's say and one kilobytes respectively let's say our processor supports four threads our processor supports four threads our processor supports four threads simultaneously in the SMT fashion and simultaneously in the SMT fashion and simultaneously in the SMT fashion and has an eight kilobyte a level 1 cache in has an eight kilobyte a level 1 cache in has an eight kilobyte a level 1 cache in this case we should run a B and C this case we should run a B and C this case we should run a B and C together because all of them can run together because all of them can run together because all of them can run simultaneously a and B together we could simultaneously a and B together we could simultaneously a and B together we could run simultaneously but we choose to run run simultaneously but we choose to run run simultaneously but we choose to run it after we finish a and B or we do a it after we finish a and B or we do a it after we finish a and B or we do a and B for a wild and see for a while and and B for a wild and see for a while and and B for a wild and see for a while and so on so pretty much we don't run them so on so pretty much we don't run them so on so pretty much we don't run them simultaneously we run c separately from simultaneously we run c separately from simultaneously we run c separately from the other tool the next option is we run the other tool the next option is we run the other tool the next option is we run a without using the other three smt ways a without using the other three smt ways a without using the other three smt ways then we run B and C in two of the four then we run B and C in two of the four then we run B and C in two of the four smt ways but we don't run a together smt ways but we don't run a together smt ways but we don't run a together with the other tool the next option is with the other tool the next option is with the other tool the next option is run a and C together then be but don't run a and C together then be but don't run a and C together then be but don't do be simultaneously using do be simultaneously using do be simultaneously using multithreading when we are doing a and C multithreading when we are doing a and C multithreading when we are doing a and C or do them one at a time although we or do them one at a time although we or do them one at a time although we code around four at a time we choose to code around four at a time we choose to code around four at a time we choose to run one at a time

## SMT and Cache Solution Quiz： Part 5
Kind: captions Language: en let's look at the solution to our smt let's look at the solution to our smt let's look at the solution to our smt and casual ation ship quiz we have three and casual ation ship quiz we have three and casual ation ship quiz we have three programs program a has a working set programs program a has a working set programs program a has a working set that doesn't fit in the cache that means that doesn't fit in the cache that means that doesn't fit in the cache that means that program a will have a lot of cache that program a will have a lot of cache that program a will have a lot of cache misses no matter what we do program be misses no matter what we do program be misses no matter what we do program be alone would fit in the cache program see alone would fit in the cache program see alone would fit in the cache program see alone would fit in the cache programs B alone would fit in the cache programs B alone would fit in the cache programs B and C would fit in the cache but if we and C would fit in the cache but if we and C would fit in the cache but if we run all three together then the total run all three together then the total run all three together then the total working set size is 15 kilobytes which working set size is 15 kilobytes which working set size is 15 kilobytes which doesn't fit in the cache so all of them doesn't fit in the cache so all of them doesn't fit in the cache so all of them get poor cache behavior so really we get poor cache behavior so really we get poor cache behavior so really we should not be running all three together should not be running all three together should not be running all three together we should not be running a and B we should not be running a and B we should not be running a and B together because then C gets to run together because then C gets to run together because then C gets to run alone so we don't benefit from sm t and alone so we don't benefit from sm t and alone so we don't benefit from sm t and b runs with a sobe also has a lot of b runs with a sobe also has a lot of b runs with a sobe also has a lot of cache misses we should run a and then b cache misses we should run a and then b cache misses we should run a and then b and c that way a which has to get poor and c that way a which has to get poor and c that way a which has to get poor cache behavior gets poor cache behavior cache behavior gets poor cache behavior cache behavior gets poor cache behavior alone but B and C benefit from smt and alone but B and C benefit from smt and alone but B and C benefit from smt and fit in the cache so they still get good fit in the cache so they still get good fit in the cache so they still get good cache behavior so we should do this just cache behavior so we should do this just cache behavior so we should do this just like we shouldn't run be with a we like we shouldn't run be with a we like we shouldn't run be with a we shouldn't run serie de and then running shouldn't run serie de and then running shouldn't run serie de and then running a and then B and then c is not a and then B and then c is not a and then B and then c is not benefiting from smt although B and C benefiting from smt although B and C benefiting from smt although B and C would fit in the cache so that's why would fit in the cache so that's why would fit in the cache so that's why this is the only correct answer

## Multi Processing Outro： Part 5
Kind: captions Language: en in this lesson we learned about the two in this lesson we learned about the two in this lesson we learned about the two approaches to writing parallel programs approaches to writing parallel programs approaches to writing parallel programs message passing and shared memory message passing and shared memory message passing and shared memory in the next lesson we will learn how to in the next lesson we will learn how to in the next lesson we will learn how to correctly do caching when we have correctly do caching when we have correctly do caching when we have multiple cores that are supposed to multiple cores that are supposed to multiple cores that are supposed to share memory

## Coherence Intro： Part 5
Kind: captions Language: en in this lesson we will be discussing the in this lesson we will be discussing the in this lesson we will be discussing the basics of cache coherence coherence is basics of cache coherence coherence is basics of cache coherence coherence is needed to ensure that when one Co writes needed to ensure that when one Co writes needed to ensure that when one Co writes to its cache other cores get to see it to its cache other cores get to see it to its cache other cores get to see it when they read it out of their own when they read it out of their own when they read it out of their own caches

## Cache Coherence Problem： Part 5
Kind: captions Language: en let us first see what the problem is let us first see what the problem is let us first see what the problem is with cache coherence that is do we even with cache coherence that is do we even with cache coherence that is do we even need cache coherence our programmer is need cache coherence our programmer is need cache coherence our programmer is expecting to see shared memory behavior expecting to see shared memory behavior expecting to see shared memory behavior that means that when one core writes that means that when one core writes that means that when one core writes let's say 15 to a variable and then let's say 15 to a variable and then let's say 15 to a variable and then another core reads that variable it will another core reads that variable it will another core reads that variable it will read what the last right put there this read what the last right put there this read what the last right put there this is how cores communicate through shared is how cores communicate through shared is how cores communicate through shared memory one writes something to a shared memory one writes something to a shared memory one writes something to a shared memory address another one reads it and memory address another one reads it and memory address another one reads it and expects to see the data that was written expects to see the data that was written expects to see the data that was written there but in our Hardware each core has there but in our Hardware each core has there but in our Hardware each core has its own cache and we need cores to have its own cache and we need cores to have its own cache and we need cores to have their own caches at least in the level their own caches at least in the level their own caches at least in the level one because a single large level one one because a single large level one one because a single large level one cache would be too slow and would not cache would be too slow and would not cache would be too slow and would not have enough trut to keep up with the have enough trut to keep up with the have enough trut to keep up with the request from all of the cores so because request from all of the cores so because request from all of the cores so because we cannot do this we have private or per we cannot do this we have private or per we cannot do this we have private or per core L1 caches so each core has its own core L1 caches so each core has its own core L1 caches so each core has its own however once you have this private however once you have this private however once you have this private caches here is what can happen this is caches here is what can happen this is caches here is what can happen this is the core a with a level one cache for the core a with a level one cache for the core a with a level one cache for core a this is core B and it has its own core a this is core B and it has its own core a this is core B and it has its own level one cache so we said that let's level one cache so we said that let's level one cache so we said that let's say core a writes xal 15 suppose this is say core a writes xal 15 suppose this is say core a writes xal 15 suppose this is a cach Miss we use the address of x to a cach Miss we use the address of x to a cach Miss we use the address of x to index into our cache check the tag index into our cache check the tag index into our cache check the tag whether it corresponds to X and then if whether it corresponds to X and then if whether it corresponds to X and then if this is a cach Miss we go to our main this is a cach Miss we go to our main this is a cach Miss we go to our main memory and fetch the whole block into memory and fetch the whole block into memory and fetch the whole block into our cache let's say that in this block X our cache let's say that in this block X our cache let's say that in this block X is is zero once we fetch it into our is is zero once we fetch it into our is is zero once we fetch it into our cache we write our 15 here so now we cache we write our 15 here so now we cache we write our 15 here so now we have a 15 in our cach here now let's say have a 15 in our cach here now let's say have a 15 in our cach here now let's say that core B wants to read X so it wants that core B wants to read X so it wants that core B wants to read X so it wants to see what x is it uses the address of to see what x is it uses the address of to see what x is it uses the address of x to index into its own cache sees that x to index into its own cache sees that x to index into its own cache sees that it doesn't have what it's looking for it doesn't have what it's looking for it doesn't have what it's looking for there and reads the block from Main there and reads the block from Main there and reads the block from Main memory this block here has zero forx memory this block here has zero forx memory this block here has zero forx still because we didn't write back from still because we didn't write back from still because we didn't write back from a cach yet so we end up reading zero a cach yet so we end up reading zero a cach yet so we end up reading zero which is not correct and know that this which is not correct and know that this which is not correct and know that this doesn't occur only the first time when doesn't occur only the first time when doesn't occur only the first time when we have a cache Miss from now on a can we have a cache Miss from now on a can we have a cache Miss from now on a can write to its own L1 cache many many write to its own L1 cache many many write to its own L1 cache many many times B can read from its own cache many times B can read from its own cache many times B can read from its own cache many many times a will keep writing to its many times a will keep writing to its many times a will keep writing to its cache and not updating memory B will cache and not updating memory B will cache and not updating memory B will keep reading from its cache and not keep reading from its cache and not keep reading from its cache and not checking back from memory because once checking back from memory because once checking back from memory because once we bring it into the cash is going to be we bring it into the cash is going to be we bring it into the cash is going to be a hit B is never modifying it it's a hit B is never modifying it it's a hit B is never modifying it it's always going to read zero no matter how always going to read zero no matter how always going to read zero no matter how many times a writes 15 or something else many times a writes 15 or something else many times a writes 15 or something else here so obviously we are not getting the here so obviously we are not getting the here so obviously we are not getting the communication of data we want from A's communication of data we want from A's communication of data we want from A's rights to B's reads and thus our shared rights to B's reads and thus our shared rights to B's reads and thus our shared memory doesn't work when we get in a memory doesn't work when we get in a memory doesn't work when we get in a shared memory system Behavior like this shared memory system Behavior like this shared memory system Behavior like this we say that it is we say that it is we say that it is incoherent the same memory location as incoherent the same memory location as incoherent the same memory location as seen from different cores can have have seen from different cores can have have seen from different cores can have have different values which is not what different values which is not what different values which is not what should be happening in a shared memory should be happening in a shared memory should be happening in a shared memory in order to not get a incoherent system in order to not get a incoherent system in order to not get a incoherent system we need an active approach to Cache we need an active approach to Cache we need an active approach to Cache coherence that will make this whole coherence that will make this whole coherence that will make this whole thing behave as a single memory even thing behave as a single memory even thing behave as a single memory even though in reality there are multiple though in reality there are multiple though in reality there are multiple copies of the data floating around

## Cache Incoherence Quiz： Part 5
Kind: captions Language: en let us see if we understand how caches let us see if we understand how caches let us see if we understand how caches can become incoherent suppose that we can become incoherent suppose that we can become incoherent suppose that we have a memory location a that initially have a memory location a that initially have a memory location a that initially contains a zero value in this system we contains a zero value in this system we contains a zero value in this system we have three cores each core has its own have three cores each core has its own have three cores each core has its own right back right back right back l1 cache and these right back caches do l1 cache and these right back caches do l1 cache and these right back caches do not have any coherent support this means not have any coherent support this means not have any coherent support this means that these right back l1 cache is simply that these right back l1 cache is simply that these right back l1 cache is simply behaved as if each of these caches was behaved as if each of these caches was behaved as if each of these caches was just alone in a uniprocessor so they just alone in a uniprocessor so they just alone in a uniprocessor so they behaved correctly for a uniprocessor behaved correctly for a uniprocessor behaved correctly for a uniprocessor cache but they do not try to maintain cache but they do not try to maintain cache but they do not try to maintain coherence and this is what the three coherence and this is what the three coherence and this is what the three cores do called zero loads from a into a cores do called zero loads from a into a cores do called zero loads from a into a register increments the value in the register increments the value in the register increments the value in the register and then stores the register register and then stores the register register and then stores the register back into memory location a some time back into memory location a some time back into memory location a some time later later later core one does pretty much the same thing core one does pretty much the same thing core one does pretty much the same thing it loads a into a register increments it loads a into a register increments it loads a into a register increments and writes the register back to a and and writes the register back to a and and writes the register back to a and finally core 2 does the same thing note finally core 2 does the same thing note finally core 2 does the same thing note that these are happening one after the that these are happening one after the that these are happening one after the other other other so without caches core zero would be so without caches core zero would be so without caches core zero would be loading zero writing a one read one loading zero writing a one read one loading zero writing a one read one encore one put a two back in a and then encore one put a two back in a and then encore one put a two back in a and then here we read two and put a three but here we read two and put a three but here we read two and put a three but with the three l1 caches and no coherent with the three l1 caches and no coherent with the three l1 caches and no coherent support the question for you is the support the question for you is the support the question for you is the value that core a is writing to a here value that core a is writing to a here value that core a is writing to a here can be what can it be zero can it be one can be what can it be zero can it be one can be what can it be zero can it be one can it be - can it be three can it be can it be - can it be three can it be can it be - can it be three can it be for and can it be something larger than for and can it be something larger than for and can it be something larger than four put a check next to all of the four put a check next to all of the four put a check next to all of the answers that might be true for this answers that might be true for this answers that might be true for this right here



---


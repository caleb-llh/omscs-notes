# 17_Memory_Consistency (Raw Transcripts)

# Playlist 5 Module 1 Raw Transcript

## Consistency Intro： Part 6
Kind: captions Language: en in this lesson we will discuss memory in this lesson we will discuss memory in this lesson we will discuss memory consistency which determines how consistency which determines how consistency which determines how strictly should be enforced ordering strictly should be enforced ordering strictly should be enforced ordering among accesses to different memory among accesses to different memory among accesses to different memory locations this is needed to get what we locations this is needed to get what we locations this is needed to get what we expect from our synchronization and data expect from our synchronization and data expect from our synchronization and data accesses in a shared memory program

## Memory Consistency： Part 6
Kind: captions Language: en so let's see what is memory consistency so let's see what is memory consistency so let's see what is memory consistency and how it differs from coherence what and how it differs from coherence what and how it differs from coherence what coherence does is define the order of coherence does is define the order of coherence does is define the order of accesses observable by different threads accesses observable by different threads accesses observable by different threads if these accesses go to the same memory if these accesses go to the same memory if these accesses go to the same memory location or to the same address location or to the same address location or to the same address coherence is needed in order to share coherence is needed in order to share coherence is needed in order to share data without coherence at read is data without coherence at read is data without coherence at read is allowed to modify memory location while allowed to modify memory location while allowed to modify memory location while another thread is allowed to read another thread is allowed to read another thread is allowed to read forever the old value of that location forever the old value of that location forever the old value of that location in which case we cannot really write in which case we cannot really write in which case we cannot really write shared memory programs at work an shared memory programs at work an shared memory programs at work an important thing about coherence however important thing about coherence however important thing about coherence however is that it defines the order of accesses is that it defines the order of accesses is that it defines the order of accesses to the same address it does not say to the same address it does not say to the same address it does not say anything about accesses that go to anything about accesses that go to anything about accesses that go to different location and that is what different location and that is what different location and that is what memory consistency is about it defines memory consistency is about it defines memory consistency is about it defines the order of accesses to different the order of accesses to different the order of accesses to different addresses so the big question now before addresses so the big question now before addresses so the big question now before we get into consistencies does this even we get into consistencies does this even we get into consistencies does this even matter if we guarantee that when I write matter if we guarantee that when I write matter if we guarantee that when I write to something all the other cores get to to something all the other cores get to to something all the other cores get to see it and that happens for every see it and that happens for every see it and that happens for every address as in I right away somebody sees address as in I right away somebody sees address as in I right away somebody sees what I wrote I wrote to be somebody sees what I wrote I wrote to be somebody sees what I wrote I wrote to be somebody sees what they wrote what can change about what they wrote what can change about what they wrote what can change about the order of accesses to different the order of accesses to different the order of accesses to different addresses and how does that even matter addresses and how does that even matter addresses and how does that even matter if I need to maintain this

## Consistency Matters： Part 6
Kind: captions Language: en It turns out that consistency does It turns out that consistency does It turns out that consistency does matter and here's how. Let's say that in matter and here's how. Let's say that in matter and here's how. Let's say that in program order two cores do this. The program order two cores do this. The program order two cores do this. The first core is going to store a one into first core is going to store a one into first core is going to store a one into location D and then store a one into location D and then store a one into location D and then store a one into location F. Let's say that D and F are location F. Let's say that D and F are location F. Let's say that D and F are both zero initially. The second core both zero initially. The second core both zero initially. The second core loads F into R1 and then loads D into loads F into R1 and then loads D into loads F into R1 and then loads D into R2. And let's say that now we print the R2. And let's say that now we print the R2. And let's say that now we print the registers R1 and R2. Note that we can registers R1 and R2. Note that we can registers R1 and R2. Note that we can have an out of order processor that have an out of order processor that have an out of order processor that reorders loads and stores. reorders loads and stores. reorders loads and stores. So in execution order, core one might So in execution order, core one might So in execution order, core one might still be doing the stores in the same still be doing the stores in the same still be doing the stores in the same order, but core two decides to reorder order, but core two decides to reorder order, but core two decides to reorder these two loads. Note that in a these two loads. Note that in a these two loads. Note that in a uniprocessor reordering of loads, uniprocessor reordering of loads, uniprocessor reordering of loads, if this core is not writing to these if this core is not writing to these if this core is not writing to these locations, is perfectly okay. And in locations, is perfectly okay. And in locations, is perfectly okay. And in fact, even in a multiprocessor, most of fact, even in a multiprocessor, most of fact, even in a multiprocessor, most of the time it's okay. Now let's look at the time it's okay. Now let's look at the time it's okay. Now let's look at what's possible to get in R1 and R2 what's possible to get in R1 and R2 what's possible to get in R1 and R2 after this. In program order, these two after this. In program order, these two after this. In program order, these two loads on core two might execute before loads on core two might execute before loads on core two might execute before these two stores in core one. So we can these two stores in core one. So we can these two stores in core one. So we can get R1 and R2 are zero. Here, these two get R1 and R2 are zero. Here, these two get R1 and R2 are zero. Here, these two loads loads loads in core two, although they've been in core two, although they've been in core two, although they've been reordered, can still happen before core reordered, can still happen before core reordered, can still happen before core one gets to store. one gets to store. one gets to store. So we can get the same thing. These two So we can get the same thing. These two So we can get the same thing. These two loads can happen after the two stores. loads can happen after the two stores. loads can happen after the two stores. So we can get one in both registers So we can get one in both registers So we can get one in both registers here. In execution order, even if we here. In execution order, even if we here. In execution order, even if we reorder them, they can execute after the reorder them, they can execute after the reorder them, they can execute after the stores, so we can get one in both of stores, so we can get one in both of stores, so we can get one in both of them. In the program order, if these two them. In the program order, if these two them. In the program order, if these two happen like this, in between the two happen like this, in between the two happen like this, in between the two stores, we can get R1 when we load is stores, we can get R1 when we load is stores, we can get R1 when we load is still loading the zero here, but R2 still loading the zero here, but R2 still loading the zero here, but R2 when we load is already loading the one. when we load is already loading the one. when we load is already loading the one. The same thing can happen here. If these The same thing can happen here. If these The same thing can happen here. If these two loads are in between the stores, two loads are in between the stores, two loads are in between the stores, then R1 is going to be zero because it then R1 is going to be zero because it then R1 is going to be zero because it comes before this. comes before this. comes before this. And R2 is going to be one because it And R2 is going to be one because it And R2 is going to be one because it happens here. And now it gets happens here. And now it gets happens here. And now it gets interesting. We have seen that there are interesting. We have seen that there are interesting. We have seen that there are these three possibilities. So, the these three possibilities. So, the these three possibilities. So, the question is, but can we get R1 to be one question is, but can we get R1 to be one question is, but can we get R1 to be one while R2 is zero? And in the program while R2 is zero? And in the program while R2 is zero? And in the program order, if we consider that, it's not order, if we consider that, it's not order, if we consider that, it's not possible. R1 being one means that when possible. R1 being one means that when possible. R1 being one means that when we read F, we read one. we read F, we read one. we read F, we read one. That means that this load has to come That means that this load has to come That means that this load has to come after this store. If that happens, after this store. If that happens, after this store. If that happens, then this load has to be after this then this load has to be after this then this load has to be after this load, too. load, too. load, too. So, pretty much if we executed this So, pretty much if we executed this So, pretty much if we executed this here, here, here, then this has to be here. then this has to be here. then this has to be here. And that means that if we read one from And that means that if we read one from And that means that if we read one from F, we will read one from D, as well. So, F, we will read one from D, as well. So, F, we will read one from D, as well. So, the R1 = 1, R2 = 0 cannot happen in the R1 = 1, R2 = 0 cannot happen in the R1 = 1, R2 = 0 cannot happen in program order. But here, R1 = 1 means program order. But here, R1 = 1 means program order. But here, R1 = 1 means that this load needs to be here. And we that this load needs to be here. And we that this load needs to be here. And we can get a zero in R2 if this load comes can get a zero in R2 if this load comes can get a zero in R2 if this load comes here. So, if we put the stores in here. So, if we put the stores in here. So, if we put the stores in between the loads between the loads between the loads in the execution order like this, we in the execution order like this, we in the execution order like this, we will get a situation that was not will get a situation that was not will get a situation that was not possible in the original program order. possible in the original program order. possible in the original program order. So, the programmer might have expected So, the programmer might have expected So, the programmer might have expected that this code can never result in this, that this code can never result in this, that this code can never result in this, but in an out-of-order processor but in an out-of-order processor but in an out-of-order processor execution on each core, it can. And note execution on each core, it can. And note execution on each core, it can. And note that this doesn't violate coherence at that this doesn't violate coherence at that this doesn't violate coherence at all. In all cases, a load and a store all. In all cases, a load and a store all. In all cases, a load and a store obeyed coherence. But do we really care obeyed coherence. But do we really care obeyed coherence. But do we really care about that? Sure, for some obscure about that? Sure, for some obscure about that? Sure, for some obscure orderings, this can happen. Do we really orderings, this can happen. Do we really orderings, this can happen. Do we really care? When does it really matter that care? When does it really matter that care? When does it really matter that this doesn't happen and it does?

## Consistency Matters Quiz： Part 6
Kind: captions Language: en so let's see an example of when does it so let's see an example of when does it so let's see an example of when does it really matter let's look at a really matter let's look at a really matter let's look at a multi-processor that has coherent memory multi-processor that has coherent memory multi-processor that has coherent memory but out of order cores but out of order cores but out of order cores and specifically the core can predict a and specifically the core can predict a and specifically the core can predict a branch and later verify that it was branch and later verify that it was branch and later verify that it was taken or not taken correctly the store taken or not taken correctly the store taken or not taken correctly the store instructions in each core are done instructions in each core are done instructions in each core are done strictly in program order so it doesn't strictly in program order so it doesn't strictly in program order so it doesn't reorder any stores but the loads between reorder any stores but the loads between reorder any stores but the loads between stored instructions can be reordered on stored instructions can be reordered on stored instructions can be reordered on a core now let's say that we have core 1 a core now let's say that we have core 1 a core now let's say that we have core 1 and core 2 where core 1 executes the and core 2 where core 1 executes the and core 2 where core 1 executes the following code while a flag is 0 it following code while a flag is 0 it following code while a flag is 0 it waits waits waits and then it prints data while core 2 and then it prints data while core 2 and then it prints data while core 2 writes 10 to data writes 10 to data writes 10 to data then increments data by 5 then sets the then increments data by 5 then sets the then increments data by 5 then sets the flag and let's say that both flag and flag and let's say that both flag and flag and let's say that both flag and data are 0 initially before core 2 gets data are 0 initially before core 2 gets data are 0 initially before core 2 gets to change them so now core 1 when it to change them so now core 1 when it to change them so now core 1 when it prints the data what are the values that prints the data what are the values that prints the data what are the values that it can possibly print can it print 0 it can possibly print can it print 0 it can possibly print can it print 0 can it print 5 can it print 5 can it print 5 can it print 10 can it print 10 can it print 10 can it print 15 can it print 15 can it print 15 and can it print anything else

## Consistency Matters Quiz Solution： Part 6
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our consistency quiz where we have coherent consistency quiz where we have coherent consistency quiz where we have coherent memory memory memory but an out of order pair of cores where but an out of order pair of cores where but an out of order pair of cores where branches are predicted stores are still branches are predicted stores are still branches are predicted stores are still done strictly in program order but loads done strictly in program order but loads done strictly in program order but loads can be reordered and these two variables can be reordered and these two variables can be reordered and these two variables start at zero what the programmer is start at zero what the programmer is start at zero what the programmer is kind of expected to happen is that i put kind of expected to happen is that i put kind of expected to happen is that i put some values in some data then i set a some values in some data then i set a some values in some data then i set a flag and the other core waits for that flag and the other core waits for that flag and the other core waits for that flag to be set flag to be set flag to be set so that it can print the data that i so that it can print the data that i so that it can print the data that i produced so in program order the only produced so in program order the only produced so in program order the only thing that can really happen is that we thing that can really happen is that we thing that can really happen is that we print 15. in the execution order print 15. in the execution order print 15. in the execution order that can also happen that can also happen that can also happen if this score gets to this part before if this score gets to this part before if this score gets to this part before we even start checking we might compute we even start checking we might compute we even start checking we might compute 15 in the data set flag to 1 core 1 15 in the data set flag to 1 core 1 15 in the data set flag to 1 core 1 loops here loops here loops here sees that the flag is 1 exits prints 15. sees that the flag is 1 exits prints 15. sees that the flag is 1 exits prints 15. so clearly 15 is possible but the so clearly 15 is possible but the so clearly 15 is possible but the question is can we get anything else question is can we get anything else question is can we get anything else these are done strictly in order so if these are done strictly in order so if these are done strictly in order so if anything else does get printed it's not anything else does get printed it's not anything else does get printed it's not going to be because this score going to be because this score going to be because this score did the data right after the flag was did the data right after the flag was did the data right after the flag was set the flag actually gets it after the set the flag actually gets it after the set the flag actually gets it after the writes are complete writes are complete writes are complete but here's what can happen but here's what can happen but here's what can happen this loop here amounts to reading the this loop here amounts to reading the this loop here amounts to reading the flag testing if it's zero and then flag testing if it's zero and then flag testing if it's zero and then branching based on that branching based on that branching based on that this processor can easily predict this this processor can easily predict this this processor can easily predict this branch branch branch in a way that says in a way that says in a way that says we exit we exit we exit before it even gets to check what the before it even gets to check what the before it even gets to check what the flag was flag was flag was so at that point we will be here so at that point we will be here so at that point we will be here and we will fetch the data to be printed and we will fetch the data to be printed and we will fetch the data to be printed the data at that point the data at that point the data at that point might be zero so what we do is we might be zero so what we do is we might be zero so what we do is we predict it goes out and we fetch the predict it goes out and we fetch the predict it goes out and we fetch the data at this point so it's kind of step data at this point so it's kind of step data at this point so it's kind of step one predict and leave the loop step two one predict and leave the loop step two one predict and leave the loop step two read the data and it's zero at this read the data and it's zero at this read the data and it's zero at this point then let's say core two finishes point then let's say core two finishes point then let's say core two finishes this this this and at that point this out of order and at that point this out of order and at that point this out of order processor does this load remember these processor does this load remember these processor does this load remember these are loads they can be reordered if we do are loads they can be reordered if we do are loads they can be reordered if we do this this this we see that the flag at this point is we see that the flag at this point is we see that the flag at this point is one one one so our prediction was correct so our prediction was correct so our prediction was correct and at this point we can print the data and at this point we can print the data and at this point we can print the data that we found that we found that we found in this case we will print zero a in this case we will print zero a in this case we will print zero a similar outcome is possible for data similar outcome is possible for data similar outcome is possible for data equal stance simply by moving this here equal stance simply by moving this here equal stance simply by moving this here so we can also print 10 if access number so we can also print 10 if access number so we can also print 10 if access number which reads the date after the which reads the date after the which reads the date after the prediction of the branch prediction of the branch prediction of the branch happens here and finally we could get 15 happens here and finally we could get 15 happens here and finally we could get 15 in the data if we get it here but we in the data if we get it here but we in the data if we get it here but we already knew we could get 15. note that already knew we could get 15. note that already knew we could get 15. note that it's not possible to get 5 or anything it's not possible to get 5 or anything it's not possible to get 5 or anything else else else and the reason is that these writes on and the reason is that these writes on and the reason is that these writes on the same core will happen in program the same core will happen in program the same core will happen in program order it's incorrect for a core to order it's incorrect for a core to order it's incorrect for a core to somehow read 0 here because the only way somehow read 0 here because the only way somehow read 0 here because the only way we read data here we read data here we read data here is if is if is if it was already written here on the same it was already written here on the same it was already written here on the same core we have to obey the program order core we have to obey the program order core we have to obey the program order that was never in question that was never in question that was never in question so the data when it's read here has to so the data when it's read here has to so the data when it's read here has to be 10 we can only increment it by 5 to be 10 we can only increment it by 5 to be 10 we can only increment it by 5 to 15 we cannot read 0 here 15 we cannot read 0 here 15 we cannot read 0 here so that's why it's possible only to get so that's why it's possible only to get so that's why it's possible only to get 0 10 or 15. so basically the real 0 10 or 15. so basically the real 0 10 or 15. so basically the real problem is that somehow problem is that somehow problem is that somehow although we thought we are waiting for although we thought we are waiting for although we thought we are waiting for the flag to be set before we access the the flag to be set before we access the the flag to be set before we access the data in reality we managed to access the data in reality we managed to access the data in reality we managed to access the data at kind of any point before the data at kind of any point before the data at kind of any point before the flag was set flag was set flag was set and coherence again didn't prevent this and coherence again didn't prevent this and coherence again didn't prevent this what we need is consistency

## Why We Need Consistency： Part 6
Kind: captions Language: en so let's go back to our question of why so let's go back to our question of why so let's go back to our question of why we need consistency we have already seen we need consistency we have already seen we need consistency we have already seen an example in our quiz that something an example in our quiz that something an example in our quiz that something beyond coherence is needed for a beyond coherence is needed for a beyond coherence is needed for a data-ready flag synchronization to work data-ready flag synchronization to work data-ready flag synchronization to work another similar example is thread another similar example is thread another similar example is thread termination where a thread creates termination where a thread creates termination where a thread creates another thread now that created thread another thread now that created thread another thread now that created thread let's call it b let's call it b let's call it b does a lot of work and updates some data does a lot of work and updates some data does a lot of work and updates some data while thread a goes and waits until b while thread a goes and waits until b while thread a goes and waits until b exits and it really happens by doing a exits and it really happens by doing a exits and it really happens by doing a system call in thread a and then the system call in thread a and then the system call in thread a and then the system really will wait for b to mark system really will wait for b to mark system really will wait for b to mark that it's done and then when thread b that it's done and then when thread b that it's done and then when thread b really is done the operating system will really is done the operating system will really is done the operating system will mark that b is done and here we have a mark that b is done and here we have a mark that b is done and here we have a very similar situation inside the very similar situation inside the very similar situation inside the operating system that we have here which operating system that we have here which operating system that we have here which is the check for be exiting might be is the check for be exiting might be is the check for be exiting might be branch predicted and then confirmed branch predicted and then confirmed branch predicted and then confirmed while in fact thread b is not done when while in fact thread b is not done when while in fact thread b is not done when we branch predict this so what we need we branch predict this so what we need we branch predict this so what we need is some sort of additional set of is some sort of additional set of is some sort of additional set of ordering restrictions that are going to ordering restrictions that are going to ordering restrictions that are going to prevent the weird situations that we can prevent the weird situations that we can prevent the weird situations that we can get into without any consideration for get into without any consideration for get into without any consideration for consistency and again this goes beyond consistency and again this goes beyond consistency and again this goes beyond coherence alone we already saw that coherence alone we already saw that coherence alone we already saw that coherence doesn't really help us here

## Sequential Consistency： Part 6
Kind: captions Language: en so for programmers the most natural type so for programmers the most natural type so for programmers the most natural type of consistency is called sequential of consistency is called sequential of consistency is called sequential consistency and it says that the result consistency and it says that the result consistency and it says that the result of any execution should be as if of any execution should be as if of any execution should be as if accesses executed by each processor were accesses executed by each processor were accesses executed by each processor were executed in order and accesses among executed in order and accesses among executed in order and accesses among different processors were arbitrarily different processors were arbitrarily different processors were arbitrarily interleaved so we are allowed to let interleaved so we are allowed to let interleaved so we are allowed to let processors take turns in terms of processors take turns in terms of processors take turns in terms of accessing or let one access several accessing or let one access several accessing or let one access several times before another one does and so on times before another one does and so on times before another one does and so on so we can do any thing we want to inter so we can do any thing we want to inter so we can do any thing we want to inter leave the accesses from different leave the accesses from different leave the accesses from different processors but what comes from each of processors but what comes from each of processors but what comes from each of the processors needs to be exactly in the processors needs to be exactly in the processors needs to be exactly in program order so the simplest program order so the simplest program order so the simplest implementation of sequential consistency implementation of sequential consistency implementation of sequential consistency is one where we remove the should be as is one where we remove the should be as is one where we remove the should be as if so pretty much any execution proceeds if so pretty much any execution proceeds if so pretty much any execution proceeds by executing accesses from each by executing accesses from each by executing accesses from each processor in order so a core performs processor in order so a core performs processor in order so a core performs the next memory access only when all the next memory access only when all the next memory access only when all previous access previous access previous access are complete so in the code that waits are complete so in the code that waits are complete so in the code that waits for a flag to be set and then prints the for a flag to be set and then prints the for a flag to be set and then prints the data we cannot read the data until the data we cannot read the data until the data we cannot read the data until the read of the flag has completed remember read of the flag has completed remember read of the flag has completed remember that the problem in previous examples that the problem in previous examples that the problem in previous examples was that we Branch predict this and was that we Branch predict this and was that we Branch predict this and leave and read the data then read the leave and read the data then read the leave and read the data then read the flag and confirm our prediction now we flag and confirm our prediction now we flag and confirm our prediction now we can still predict but when we get to can still predict but when we get to can still predict but when we get to reading the data we have to delay that reading the data we have to delay that reading the data we have to delay that until the flag has been red at which until the flag has been red at which until the flag has been red at which point if we read one here by that time point if we read one here by that time point if we read one here by that time the rights to the flag and data are the rights to the flag and data are the rights to the flag and data are complete and we will read the correct complete and we will read the correct complete and we will read the correct data here or if we read zero here then data here or if we read zero here then data here or if we read zero here then we would correct the misprediction and we would correct the misprediction and we would correct the misprediction and go back to waiting here so either way go back to waiting here so either way go back to waiting here so either way you cannot print wrong data anymore you cannot print wrong data anymore you cannot print wrong data anymore under sequential consistency here so under sequential consistency here so under sequential consistency here so this works pretty well except that it's this works pretty well except that it's this works pretty well except that it's really really bad for performance

## Simple Implementation of SC Quiz： Part 6
Kind: captions Language: en so let's look at the drawbacks of the so let's look at the drawbacks of the so let's look at the drawbacks of the simple implementation of sequential simple implementation of sequential simple implementation of sequential consistency remember that that simple consistency remember that that simple consistency remember that that simple implementation basically issues the next implementation basically issues the next implementation basically issues the next AIS only when all prior accesses are AIS only when all prior accesses are AIS only when all prior accesses are complete so if we have outof order cores complete so if we have outof order cores complete so if we have outof order cores what is the memory level parallelism we what is the memory level parallelism we what is the memory level parallelism we get on each core by doing this put that get on each core by doing this put that get on each core by doing this put that number here

## Simple Implementation of SC Quiz Solution： Part 6
Kind: captions Language: en so let's look at the answer to our so let's look at the answer to our so let's look at the answer to our simple implementation quiz simple implementation quiz simple implementation quiz if we issue the next axis only when all if we issue the next axis only when all if we issue the next axis only when all prior accesses are complete prior accesses are complete prior accesses are complete what is the memory level parallelism we what is the memory level parallelism we what is the memory level parallelism we get and the answer is the memory level get and the answer is the memory level get and the answer is the memory level parallelism is exactly one parallelism is exactly one parallelism is exactly one remember that memory level parallelism remember that memory level parallelism remember that memory level parallelism is about is about is about having a load having a load having a load that misses that misses that misses and gets sent to memory and gets sent to memory and gets sent to memory and then and then and then our processor continues finding our processor continues finding our processor continues finding independent instructions here independent instructions here independent instructions here in the hope of finding another load that in the hope of finding another load that in the hope of finding another load that is a miss and sending it to memory while is a miss and sending it to memory while is a miss and sending it to memory while the first miss is still going the first miss is still going the first miss is still going what that does is that if we look at all what that does is that if we look at all what that does is that if we look at all the misses in our program the misses in our program the misses in our program we will do some execution we will do some execution we will do some execution and then wait for memory to respond and then wait for memory to respond and then wait for memory to respond and then do some execution and then wait and then do some execution and then wait and then do some execution and then wait for memory to respond for memory to respond for memory to respond then sometimes do very little execution then sometimes do very little execution then sometimes do very little execution and wait for memory to respond and wait for memory to respond and wait for memory to respond so basically our execution so basically our execution so basically our execution with memory level parallelism of one with memory level parallelism of one with memory level parallelism of one ends up being ends up being ends up being the execution without the misses the execution without the misses the execution without the misses plus plus plus the time to access memory which is very the time to access memory which is very the time to access memory which is very large times the number of misses we have large times the number of misses we have large times the number of misses we have with memory level parallelism with memory level parallelism with memory level parallelism we execute we execute we execute we send the request to memory now we'll we send the request to memory now we'll we send the request to memory now we'll wait for it wait for it wait for it but meanwhile we execute a little bit but meanwhile we execute a little bit but meanwhile we execute a little bit more and hopefully more and hopefully more and hopefully find that next miss in which case we can find that next miss in which case we can find that next miss in which case we can overlap misses and hopefully we will overlap misses and hopefully we will overlap misses and hopefully we will find more than just two find more than just two find more than just two so in this case so in this case so in this case we have that the cost of each miss we have that the cost of each miss we have that the cost of each miss in terms of memory access in terms of memory access in terms of memory access was not paid every time was not paid every time was not paid every time we paid really the cost of maybe we paid really the cost of maybe we paid really the cost of maybe something like one and a half misses something like one and a half misses something like one and a half misses total but we satisfied three misses here

## Better Implementation of SC： Part 6
Kind: captions Language: en so let's look at a better implementation so let's look at a better implementation so let's look at a better implementation of sequential consistency in which a of sequential consistency in which a of sequential consistency in which a core can reorder loads but this can lead core can reorder loads but this can lead core can reorder loads but this can lead to violations of sequential consistency to violations of sequential consistency to violations of sequential consistency so it has to also detect when sequential so it has to also detect when sequential so it has to also detect when sequential consistency may be violated and fixed consistency may be violated and fixed consistency may be violated and fixed things things things this is of course easy to say but how do this is of course easy to say but how do this is of course easy to say but how do we really do that so let's look at a we really do that so let's look at a we really do that so let's look at a program where there is a load to a and program where there is a load to a and program where there is a load to a and then sometime later but not too much then sometime later but not too much then sometime later but not too much later a load from B so we need to make later a load from B so we need to make later a load from B so we need to make this execute in order if doing otherwise this execute in order if doing otherwise this execute in order if doing otherwise will violate sequential consistency but will violate sequential consistency but will violate sequential consistency but we want to really execute them out of we want to really execute them out of we want to really execute them out of order if there is no potential for order if there is no potential for order if there is no potential for sequential consistency violation sequential consistency violation sequential consistency violation so let's look at what happens in the so let's look at what happens in the so let's look at what happens in the reorder buffer of a processor that tries reorder buffer of a processor that tries reorder buffer of a processor that tries to execute this the reorder buffer keeps to execute this the reorder buffer keeps to execute this the reorder buffer keeps instructions in program order so the instructions in program order so the instructions in program order so the first load will be first load will be first load will be known to come ahead of the second load known to come ahead of the second load known to come ahead of the second load now if we actually do this axis first now if we actually do this axis first now if we actually do this axis first and this second then there is no and this second then there is no and this second then there is no potential for violating sequential potential for violating sequential potential for violating sequential consistency because we are actually consistency because we are actually consistency because we are actually doing whatever the simple implementation doing whatever the simple implementation doing whatever the simple implementation would do anyway so the sequential would do anyway so the sequential would do anyway so the sequential consistency evaluation can occur when we consistency evaluation can occur when we consistency evaluation can occur when we first first first do load B do load B do load B in our real execution order in our real execution order in our real execution order and later for example because the and later for example because the and later for example because the computation for address of a has been computation for address of a has been computation for address of a has been delayed we do load of a now the delayed we do load of a now the delayed we do load of a now the important thing to note is that this is important thing to note is that this is important thing to note is that this is not necessarily a violation of not necessarily a violation of not necessarily a violation of sequential consistency if nobody writes sequential consistency if nobody writes sequential consistency if nobody writes either b or a during this time then the either b or a during this time then the either b or a during this time then the B here and the a here will be the same B here and the a here will be the same B here and the a here will be the same as if we reorder them back to their as if we reorder them back to their as if we reorder them back to their original program order so we are getting original program order so we are getting original program order so we are getting the same value of B no matter where we the same value of B no matter where we the same value of B no matter where we load it load it load it if nobody writes anything if nobody writes anything if nobody writes anything in fact in fact in fact for this to actually be a violation of for this to actually be a violation of for this to actually be a violation of sequential consistency like we can sequential consistency like we can sequential consistency like we can actually get a different value for B actually get a different value for B actually get a different value for B than we could in sequential consistency than we could in sequential consistency than we could in sequential consistency what has to happen is that there is at what has to happen is that there is at what has to happen is that there is at least one store to be least one store to be least one store to be between the time we actually read B between the time we actually read B between the time we actually read B and the time when we should have read B and the time when we should have read B and the time when we should have read B according to sequential consistency why according to sequential consistency why according to sequential consistency why well because if we don't have the store well because if we don't have the store well because if we don't have the store here here here then the value we write here is the same then the value we write here is the same then the value we write here is the same as the value that we would have read as the value that we would have read as the value that we would have read here where it would be okay to read B so here where it would be okay to read B so here where it would be okay to read B so now we can see how we can detect now we can see how we can detect now we can see how we can detect violations of sequential consistency violations of sequential consistency violations of sequential consistency when we read something ahead of program when we read something ahead of program when we read something ahead of program order we need to start monitoring the order we need to start monitoring the order we need to start monitoring the coherence traffic produced by other coherence traffic produced by other coherence traffic produced by other processors so if we see such a store processors so if we see such a store processors so if we see such a store then this load and anything that it fed then this load and anything that it fed then this load and anything that it fed the data to must be replayed note that the data to must be replayed note that the data to must be replayed note that this load is replayable because in the this load is replayable because in the this load is replayable because in the Rob it's here Rob it's here Rob it's here we are assuming that this load still we are assuming that this load still we are assuming that this load still hasn't happened which means that the hasn't happened which means that the hasn't happened which means that the commit pointer for the Rob is somewhere commit pointer for the Rob is somewhere commit pointer for the Rob is somewhere here or maybe at the load here but here or maybe at the load here but here or maybe at the load here but definitely the load from B has not been definitely the load from B has not been definitely the load from B has not been committed so we are capable of replaying committed so we are capable of replaying committed so we are capable of replaying it in the worst case it in the worst case it in the worst case we can just cancel this load and we can just cancel this load and we can just cancel this load and anything that follows and just refetch anything that follows and just refetch anything that follows and just refetch those instructions so we know how to those instructions so we know how to those instructions so we know how to undo the damage here but in order to undo the damage here but in order to undo the damage here but in order to detect the violation now we have to detect the violation now we have to detect the violation now we have to basically anything that we load out of basically anything that we load out of basically anything that we load out of order we need to monitor coherence order we need to monitor coherence order we need to monitor coherence traffic until that load becomes in order

## Relaxed Consistency： Part 6
Kind: captions Language: en an alternative approach to trying to an alternative approach to trying to an alternative approach to trying to create better create better create better solutions for sequential consistency solutions for sequential consistency solutions for sequential consistency which programmers naturally might expect which programmers naturally might expect which programmers naturally might expect is to actually relax the consistency is to actually relax the consistency is to actually relax the consistency that is we tell the programmers that that is we tell the programmers that that is we tell the programmers that they cannot really expect sequential they cannot really expect sequential they cannot really expect sequential consistency but we can still tell them consistency but we can still tell them consistency but we can still tell them that they can expect something that they can expect something that they can expect something and these relaxed consistency models and these relaxed consistency models and these relaxed consistency models that are not really sequential that are not really sequential that are not really sequential consistency but they're not just like consistency but they're not just like consistency but they're not just like anything goes anything goes anything goes usually differ in terms of what types of usually differ in terms of what types of usually differ in terms of what types of ordering do they enforce there are ordering do they enforce there are ordering do they enforce there are really four types of ordering really four types of ordering really four types of ordering there is ordering between rights to there is ordering between rights to there is ordering between rights to different locations different locations different locations there are orderings between rights to there are orderings between rights to there are orderings between rights to some locations and raids of other some locations and raids of other some locations and raids of other locations there is ordering between locations there is ordering between locations there is ordering between reads and subsequent writes in program reads and subsequent writes in program reads and subsequent writes in program order between reads and other reads in order between reads and other reads in order between reads and other reads in program order sequential consistency program order sequential consistency program order sequential consistency says that basically program execution says that basically program execution says that basically program execution must obey all four types of ordering at must obey all four types of ordering at must obey all four types of ordering at all times if we actually reorder all times if we actually reorder all times if we actually reorder something we have to fix any situation something we have to fix any situation something we have to fix any situation where that would result in a different where that would result in a different where that would result in a different outcome outcome outcome than than than program order execution so we are program order execution so we are program order execution so we are allowed to reorder things but we have to allowed to reorder things but we have to allowed to reorder things but we have to always hide that from anybody being able always hide that from anybody being able always hide that from anybody being able to see it in relaxed consistency models to see it in relaxed consistency models to see it in relaxed consistency models some of these types of ordering need not some of these types of ordering need not some of these types of ordering need not be obeyed at all times usually the first be obeyed at all times usually the first be obeyed at all times usually the first type of ordering that will not be obeyed type of ordering that will not be obeyed type of ordering that will not be obeyed is the read read so if we maintain is the read read so if we maintain is the read read so if we maintain the ordering between writes and between the ordering between writes and between the ordering between writes and between writes and reads but just allow reads to writes and reads but just allow reads to writes and reads but just allow reads to proceed out of order then we say that we proceed out of order then we say that we proceed out of order then we say that we have a less relaxed model than if we have a less relaxed model than if we have a less relaxed model than if we allow reordering of other types so if we allow reordering of other types so if we allow reordering of other types so if we told the programmers told the programmers told the programmers that you have to write programs that you have to write programs that you have to write programs without assuming without assuming without assuming that reads are done that reads are done that reads are done in correct order then the real question in correct order then the real question in correct order then the real question is but how do we then write correct is but how do we then write correct is but how do we then write correct programs to begin with so for example programs to begin with so for example programs to begin with so for example how do we do flag synchronization how do we do flag synchronization how do we do flag synchronization if we cannot expect that if we cannot expect that if we cannot expect that after waiting on a flag after waiting on a flag after waiting on a flag we will be able to read the new value of we will be able to read the new value of we will be able to read the new value of the data

## Relaxed Consistency： Part 6
Kind: captions Language: en so really when we do relaxed consistency so really when we do relaxed consistency so really when we do relaxed consistency we allow we allow we allow reordering reordering reordering of normal axises according to the four of normal axises according to the four of normal axises according to the four types of ordering so for example our types of ordering so for example our types of ordering so for example our reads can now be reordered but only if reads can now be reordered but only if reads can now be reordered but only if these are normal accesses but then we these are normal accesses but then we these are normal accesses but then we add special non-reorderable accesses add special non-reorderable accesses add special non-reorderable accesses and the programmer must use these and the programmer must use these and the programmer must use these non-reorderable accesses when ordering non-reorderable accesses when ordering non-reorderable accesses when ordering in the program matters in the program matters in the program matters an example of such non-reorderable an example of such non-reorderable an example of such non-reorderable accesses is the x86 msync instruction accesses is the x86 msync instruction accesses is the x86 msync instruction here here here in normal execution all accesses are in normal execution all accesses are in normal execution all accesses are reorderable reorderable reorderable we cannot rely on any specific in-order we cannot rely on any specific in-order we cannot rely on any specific in-order behavior as far as consistency is behavior as far as consistency is behavior as far as consistency is concerned but no reordering can happen concerned but no reordering can happen concerned but no reordering can happen across an msync instruction so we can across an msync instruction so we can across an msync instruction so we can have have have any accesses that we want reordered any accesses that we want reordered any accesses that we want reordered anywhere in here but in program order anywhere in here but in program order anywhere in here but in program order then comes the msync instruction the then comes the msync instruction the then comes the msync instruction the processor guarantees that all of these processor guarantees that all of these processor guarantees that all of these are going to be done before we do msync are going to be done before we do msync are going to be done before we do msync and that the msync will be done before and that the msync will be done before and that the msync will be done before we begin the next cloud of instructions we begin the next cloud of instructions we begin the next cloud of instructions that can again be ordered with each that can again be ordered with each that can again be ordered with each other so how does that help it helps other so how does that help it helps other so how does that help it helps because now we can write flag because now we can write flag because now we can write flag synchronization correctly the correct synchronization correctly the correct synchronization correctly the correct weight for a flag now includes an msync weight for a flag now includes an msync weight for a flag now includes an msync instruction it basically says that wait instruction it basically says that wait instruction it basically says that wait for the flag to become zero for the flag to become zero for the flag to become zero make that happen and then use the data make that happen and then use the data make that happen and then use the data now the processor will first make sure now the processor will first make sure now the processor will first make sure that the flag is red and only then that that the flag is red and only then that that the flag is red and only then that the data is read the data is read the data is read because we cannot reorder the reads to because we cannot reorder the reads to because we cannot reorder the reads to the flag and data across nm sync but it the flag and data across nm sync but it the flag and data across nm sync but it still allows all sorts of performance still allows all sorts of performance still allows all sorts of performance optimizations optimizations optimizations where we reorder instructions where we reorder instructions where we reorder instructions in situations where we are not trying to in situations where we are not trying to in situations where we are not trying to use memory for synchronization like this

## MSYNC Quiz： Part 6
Kind: captions Language: en so let's see if we can correctly use so let's see if we can correctly use so let's see if we can correctly use msync msync msync let's say that we have a processor with let's say that we have a processor with let's say that we have a processor with a very relaxed consistency model where a very relaxed consistency model where a very relaxed consistency model where all four reorderings are allowed all four reorderings are allowed all four reorderings are allowed including reordering writes to different including reordering writes to different including reordering writes to different locations on a core let's say that this locations on a core let's say that this locations on a core let's say that this is the code we are executing we do a is the code we are executing we do a is the code we are executing we do a load link of lock into r1 we then do a load link of lock into r1 we then do a load link of lock into r1 we then do a branch branch branch if we didn't get the lock back to if we didn't get the lock back to if we didn't get the lock back to checking checking checking we do a store conditional we do a store conditional we do a store conditional trying to now get the lock we loop back trying to now get the lock we loop back trying to now get the lock we loop back if we didn't get the lock if we didn't get the lock if we didn't get the lock then we load the variable increment it then we load the variable increment it then we load the variable increment it and store it and then we free the lock and store it and then we free the lock and store it and then we free the lock by writing zero to it by writing zero to it by writing zero to it so the question for you so the question for you so the question for you is where do we need msync if anywhere is where do we need msync if anywhere is where do we need msync if anywhere here here here do we need one do we need one do we need one here here here do we need one here do we need one here do we need one here do we need one here do we need one here do we need one here here here here how about here how about here how about here how about here how about here how about here how about here and how about how about here and how about how about here and how about at the end at the end at the end of everything

## MSYNC Quiz Solution： Part 6
Kind: captions Language: en so let's look at the solution of our so let's look at the solution of our so let's look at the solution of our msync quiz msync quiz msync quiz this processor allows all possible this processor allows all possible this processor allows all possible reorderings however reorderings however reorderings however it only allows reorderings of accesses it only allows reorderings of accesses it only allows reorderings of accesses to different locations to different locations to different locations because cache coherence and correct because cache coherence and correct because cache coherence and correct uniprocessor behavior ensures that uniprocessor behavior ensures that uniprocessor behavior ensures that accesses to the same variable accesses to the same variable accesses to the same variable are still done in correct order are still done in correct order are still done in correct order so we don't have to worry about so we don't have to worry about so we don't have to worry about the ll and sc and the store being the ll and sc and the store being the ll and sc and the store being reordered with respect to each other reordered with respect to each other reordered with respect to each other they're still going to behave they're still going to behave they're still going to behave exactly as if they were in program order exactly as if they were in program order exactly as if they were in program order however however however the variable and the lock are different the variable and the lock are different the variable and the lock are different memory locations so they can be memory locations so they can be memory locations so they can be arbitrarily reordered here arbitrarily reordered here arbitrarily reordered here so what can happen is that this load of so what can happen is that this load of so what can happen is that this load of a variable a variable a variable could be moved all the way up could be moved all the way up could be moved all the way up before we even try to acquire the lock before we even try to acquire the lock before we even try to acquire the lock so now we are loading potentially while so now we are loading potentially while so now we are loading potentially while somebody is still in their critical somebody is still in their critical somebody is still in their critical section so what we need is an msync here section so what we need is an msync here section so what we need is an msync here to ensure that we first get the lock to ensure that we first get the lock to ensure that we first get the lock and only then try to actually do our and only then try to actually do our and only then try to actually do our work in the critical section so that our work in the critical section so that our work in the critical section so that our work doesn't escape the critical section work doesn't escape the critical section work doesn't escape the critical section now another problem is that stores in now another problem is that stores in now another problem is that stores in this browser can be reordered so we can this browser can be reordered so we can this browser can be reordered so we can easily easily easily release the lock release the lock release the lock and then update the variable which again and then update the variable which again and then update the variable which again we need to prevent we need to prevent we need to prevent so we need to prevent that type of so we need to prevent that type of so we need to prevent that type of reordering we don't have to put an msync reordering we don't have to put an msync reordering we don't have to put an msync here because the load of the variable here because the load of the variable here because the load of the variable and the store will happen in order and the store will happen in order and the store will happen in order because they're to the same memory because they're to the same memory because they're to the same memory location and we don't have to put location and we don't have to put location and we don't have to put anything here or anywhere in between anything here or anywhere in between anything here or anywhere in between because because because the all of these accesses to the lock the all of these accesses to the lock the all of these accesses to the lock are going to be in program order anyway are going to be in program order anyway are going to be in program order anyway in general a lock function in general a lock function in general a lock function is considered to be an acquired type of is considered to be an acquired type of is considered to be an acquired type of synchronization and then we put an msync synchronization and then we put an msync synchronization and then we put an msync after it and unlock is considered to be after it and unlock is considered to be after it and unlock is considered to be a release type of synchronization and we a release type of synchronization and we a release type of synchronization and we put an m sync before it so the idea is put an m sync before it so the idea is put an m sync before it so the idea is that you first acquire access to that you first acquire access to that you first acquire access to something then um sync then you actually something then um sync then you actually something then um sync then you actually access it to make sure that you access access it to make sure that you access access it to make sure that you access it after you acquire it and then when it after you acquire it and then when it after you acquire it and then when you want to release you want to release you want to release you first do what you want then msync you first do what you want then msync you first do what you want then msync then release to make sure that the then release to make sure that the then release to make sure that the release actually happens after the work release actually happens after the work release actually happens after the work is done is done is done with flag synchronization we have seen with flag synchronization we have seen with flag synchronization we have seen that waiting for a flag is an acquire that waiting for a flag is an acquire that waiting for a flag is an acquire and that releasing the flag is really a and that releasing the flag is really a and that releasing the flag is really a release and then a barrier is a weird release and then a barrier is a weird release and then a barrier is a weird case where we have both an acquire and case where we have both an acquire and case where we have both an acquire and the release in the same operation the release in the same operation the release in the same operation so we need an m sync both before and so we need an m sync both before and so we need an m sync both before and after the barrier to ensure that the after the barrier to ensure that the after the barrier to ensure that the work gets done before we enter the work gets done before we enter the work gets done before we enter the barrier barrier barrier and that nobody leaves and that nobody leaves and that nobody leaves until we are done with the barrier

## Data Races and Consistency： Part 6
Kind: captions Language: en there is a concept of data rays that there is a concept of data rays that there is a concept of data rays that plays an important role in considering plays an important role in considering plays an important role in considering how we can relax consistency a data race how we can relax consistency a data race how we can relax consistency a data race occurs when there is a data dependence occurs when there is a data dependence occurs when there is a data dependence between axises that are on different between axises that are on different between axises that are on different cores and these axises are not ordered cores and these axises are not ordered cores and these axises are not ordered by synchronization so for a data race to by synchronization so for a data race to by synchronization so for a data race to occur we need to really have one occur we need to really have one occur we need to really have one processor reading another writing to the processor reading another writing to the processor reading another writing to the same variable or the other way around or same variable or the other way around or same variable or the other way around or both of them both of them both of them writing and the axises on these writing and the axises on these writing and the axises on these different cores are not ordered by different cores are not ordered by different cores are not ordered by synchronization so here's an example of synchronization so here's an example of synchronization so here's an example of a data race a data race here exists a data race a data race here exists a data race a data race here exists because really we can have this happen because really we can have this happen because really we can have this happen or in another execution this store might or in another execution this store might or in another execution this store might occur before the load and thus this can occur before the load and thus this can occur before the load and thus this can happen too so this is a data RM in happen too so this is a data RM in happen too so this is a data RM in contrast something like this where we do contrast something like this where we do contrast something like this where we do low a then a barrier and this thread low a then a barrier and this thread low a then a barrier and this thread participates in that barrier and then participates in that barrier and then participates in that barrier and then does store a does store a does store a there is a data dependence between there is a data dependence between there is a data dependence between axises but they're clearly ordered by axises but they're clearly ordered by axises but they're clearly ordered by synchronization this one will happen synchronization this one will happen synchronization this one will happen before this one because of the barrier before this one because of the barrier before this one because of the barrier synchronization so there is no data race synchronization so there is no data race synchronization so there is no data race here conceptually a data race allows any here conceptually a data race allows any here conceptually a data race allows any order of these operations and that might order of these operations and that might order of these operations and that might change the outcome of the program change the outcome of the program change the outcome of the program whereas whereas whereas here we know what's going to happen then here we know what's going to happen then here we know what's going to happen then there is a concept of a data Ras free there is a concept of a data Ras free there is a concept of a data Ras free program which which is a program that program which which is a program that program which which is a program that cannot create any data races in its cannot create any data races in its cannot create any data races in its execution pretty much that program is execution pretty much that program is execution pretty much that program is considered to be well synchronized so considered to be well synchronized so considered to be well synchronized so that any accesses to data are correctly that any accesses to data are correctly that any accesses to data are correctly ordered by synchronization barriers ordered by synchronization barriers ordered by synchronization barriers locks Flags Etc a key property of data locks Flags Etc a key property of data locks Flags Etc a key property of data Race Free programs is that they behave Race Free programs is that they behave Race Free programs is that they behave exactly the same as they would in exactly the same as they would in exactly the same as they would in sequential consistency even if we run sequential consistency even if we run sequential consistency even if we run them in any relaxed consistency model them in any relaxed consistency model them in any relaxed consistency model why is that so well because if your why is that so well because if your why is that so well because if your synchronization is implemented correctly synchronization is implemented correctly synchronization is implemented correctly then your synchronization is going to then your synchronization is going to then your synchronization is going to create the right orderings for things create the right orderings for things create the right orderings for things and then there is no and then there is no and then there is no opportunity for the reorderings that opportunity for the reorderings that opportunity for the reorderings that might violate sequential consistency so might violate sequential consistency so might violate sequential consistency so a data race free program is actually a data race free program is actually a data race free program is actually sequentially consistent even if we run sequentially consistent even if we run sequentially consistent even if we run it on a machine that doesn't support it on a machine that doesn't support it on a machine that doesn't support sequential consistency as long as sequential consistency as long as sequential consistency as long as synchronization is correctly implemented synchronization is correctly implemented synchronization is correctly implemented for that machine but keep in mind that for that machine but keep in mind that for that machine but keep in mind that while you're debugging your programs while you're debugging your programs while you're debugging your programs even if you intend them to be data Race even if you intend them to be data Race even if you intend them to be data Race Free they may not actually be data race Free they may not actually be data race Free they may not actually be data race free in which case free in which case free in which case theoretically anything can happen so theoretically anything can happen so theoretically anything can happen so pretty much while we are debugging a pretty much while we are debugging a pretty much while we are debugging a program to remove data races the program to remove data races the program to remove data races the behavior of that program on a weekly behavior of that program on a weekly behavior of that program on a weekly consistent system might be very weird consistent system might be very weird consistent system might be very weird and thus it makes debugging really and thus it makes debugging really and thus it makes debugging really really difficult so for most programs really difficult so for most programs really difficult so for most programs that are correct and that means data that are correct and that means data that are correct and that means data Race Free we can relax the consistency Race Free we can relax the consistency Race Free we can relax the consistency model but for debugging it really helps model but for debugging it really helps model but for debugging it really helps if we can support sequential if we can support sequential if we can support sequential consistency so some processors actually consistency so some processors actually consistency so some processors actually have support to flip between sequential have support to flip between sequential have support to flip between sequential consistency or some more relaxed consistency or some more relaxed consistency or some more relaxed consistency that gives us better consistency that gives us better consistency that gives us better performance so that when you're performance so that when you're performance so that when you're debugging you can use sequential debugging you can use sequential debugging you can use sequential consistency until you're reasonably sure consistency until you're reasonably sure consistency until you're reasonably sure that your program is working correctly that your program is working correctly that your program is working correctly and is data Ras free at which point we and is data Ras free at which point we and is data Ras free at which point we can run it on a weaker consistency model can run it on a weaker consistency model can run it on a weaker consistency model to benefit from the additional to benefit from the additional to benefit from the additional reordering that the processor is now reordering that the processor is now reordering that the processor is now allowed to do



---

# Playlist 5 Module 2 Raw Transcript

## Consistency Models： Part 6
Kind: captions Language: en So finally let's take a look at what So finally let's take a look at what So finally let's take a look at what kinds of consistency are there. We know kinds of consistency are there. We know kinds of consistency are there. We know that there is sequential consistency and that there is sequential consistency and that there is sequential consistency and then there is a family of relaxed then there is a family of relaxed then there is a family of relaxed consistency models which are allowed to consistency models which are allowed to consistency models which are allowed to violate one or more of those four violate one or more of those four violate one or more of those four ordering rules. And the models that have ordering rules. And the models that have ordering rules. And the models that have actually been proposed and sometimes actually been proposed and sometimes actually been proposed and sometimes used are called weak consistency, used are called weak consistency, used are called weak consistency, processor consistency, release processor consistency, release processor consistency, release consistency, lazy release consistency, consistency, lazy release consistency, consistency, lazy release consistency, scope consistency, etc. There are scope consistency, etc. There are scope consistency, etc. There are actually many many relax consistency actually many many relax consistency actually many many relax consistency models, too many to actually explain models, too many to actually explain models, too many to actually explain here. But the key to all of them is that here. But the key to all of them is that here. But the key to all of them is that although they allow arbitrary although they allow arbitrary although they allow arbitrary reorderings or maybe some reorderings reorderings or maybe some reorderings reorderings or maybe some reorderings among data operations, all of them among data operations, all of them among data operations, all of them support synchronization operations that support synchronization operations that support synchronization operations that allow you to ensure that the ordering allow you to ensure that the ordering allow you to ensure that the ordering that you really want in your program that you really want in your program that you really want in your program gets to happen. Some of them do it by gets to happen. Some of them do it by gets to happen. Some of them do it by doing something like the mync doing something like the mync doing something like the mync operations. Some of them do it by using operations. Some of them do it by using operations. Some of them do it by using more refined memory synchronization more refined memory synchronization more refined memory synchronization operations. Some of them do it by operations. Some of them do it by operations. Some of them do it by actually calling library functions that actually calling library functions that actually calling library functions that are ensuring such ordering and so

## Consistency Outro： Part 6
Kind: captions Language: en in this lesson we learned about memory in this lesson we learned about memory in this lesson we learned about memory consistency which defines what is consistency which defines what is consistency which defines what is allowed to happen when processor cores allowed to happen when processor cores allowed to happen when processor cores are reordering memory accesses are reordering memory accesses are reordering memory accesses we have seen that sequential consistency we have seen that sequential consistency we have seen that sequential consistency gives us the behavior we expect but that gives us the behavior we expect but that gives us the behavior we expect but that it can limit performance and we have it can limit performance and we have it can limit performance and we have seen that more relaxed memory seen that more relaxed memory seen that more relaxed memory consistency models can improve consistency models can improve consistency models can improve performance but make program behavior performance but make program behavior performance but make program behavior more difficult to reason about

## Many Cores Intro： Part 6
Kind: captions Language: en in this final lesson we will discuss in this final lesson we will discuss in this final lesson we will discuss what happens when we put a lot of cores what happens when we put a lot of cores what happens when we put a lot of cores on the same chip on the same chip on the same chip and we will apply and put together a lot and we will apply and put together a lot and we will apply and put together a lot of things we have already learned

## Many Core Challenges： Part 6
Kind: captions Language: en so in this lesson so in this lesson so in this lesson we will go through several challenges we will go through several challenges we will go through several challenges that a many core processor that is a that a many core processor that is a that a many core processor that is a processor with a very large number of processor with a very large number of processor with a very large number of cores will face cores will face cores will face the first challenge is that as we grow the first challenge is that as we grow the first challenge is that as we grow the number of cores the number of cores the number of cores the coherence traffic between this the coherence traffic between this the coherence traffic between this course goes up and that is because we course goes up and that is because we course goes up and that is because we have already seen that rights to shared have already seen that rights to shared have already seen that rights to shared memory locations result in invalidations memory locations result in invalidations memory locations result in invalidations and subsequent misses on those locations and subsequent misses on those locations and subsequent misses on those locations and both invalidations and the resulting and both invalidations and the resulting and both invalidations and the resulting misses misses misses go onto the shared bus and thus add to go onto the shared bus and thus add to go onto the shared bus and thus add to this coherent traffic this coherent traffic this coherent traffic as we increase the number of cores we as we increase the number of cores we as we increase the number of cores we get more writes per second and as a get more writes per second and as a get more writes per second and as a result result result the bus throughput that we need goes up the bus throughput that we need goes up the bus throughput that we need goes up until it eventually exceeds what we can until it eventually exceeds what we can until it eventually exceeds what we can do on a bus do on a bus do on a bus unfortunately the bus unfortunately the bus unfortunately the bus allows us to only do one request at a allows us to only do one request at a allows us to only do one request at a time in part time in part time in part that is needed because we rely on the that is needed because we rely on the that is needed because we rely on the bus to produce the easy ordering that we bus to produce the easy ordering that we bus to produce the easy ordering that we get between writes to maintain coherence get between writes to maintain coherence get between writes to maintain coherence but that means that really the bus but that means that really the bus but that means that really the bus eventually becomes the bottleneck eventually becomes the bottleneck eventually becomes the bottleneck so what we need so what we need so what we need is a scalable is a scalable is a scalable on-chip network that allows the traffic on-chip network that allows the traffic on-chip network that allows the traffic to grow as the number of course grows to grow as the number of course grows to grow as the number of course grows and then we also need to use directory and then we also need to use directory and then we also need to use directory coherence coherence coherence so that we don't rely so that we don't rely so that we don't rely on the bus on the bus on the bus to serialize everything for us

## Network On Chip： Part 6
Kind: captions Language: en so let's look at what a network on chip so let's look at what a network on chip so let's look at what a network on chip might look like let's say we have some might look like let's say we have some might look like let's say we have some sort of a square-ish tile that has a sort of a square-ish tile that has a sort of a square-ish tile that has a core and maybe a level one cache and core and maybe a level one cache and core and maybe a level one cache and such tiles normally will be connected to such tiles normally will be connected to such tiles normally will be connected to a shared bus now let's say that these a shared bus now let's say that these a shared bus now let's say that these four cores are using half of the four cores are using half of the four cores are using half of the available trut on the bus because their available trut on the bus because their available trut on the bus because their cash misses and coherence need to be cash misses and coherence need to be cash misses and coherence need to be taken care of through the bus now let's taken care of through the bus now let's taken care of through the bus now let's say we add another set of those cores say we add another set of those cores say we add another set of those cores pretty much the same thing just pretty much the same thing just pretty much the same thing just replicated now we have an even longer replicated now we have an even longer replicated now we have an even longer thus possibly a slower bus that all thus possibly a slower bus that all thus possibly a slower bus that all traffic from all these eight cores goes traffic from all these eight cores goes traffic from all these eight cores goes to so we got a slower bus probably with to so we got a slower bus probably with to so we got a slower bus probably with less throughput than the original small less throughput than the original small less throughput than the original small one had yet it has twice the demand for one had yet it has twice the demand for one had yet it has twice the demand for traffic so very quickly it will get traffic so very quickly it will get traffic so very quickly it will get saturated now let's consider a type of saturated now let's consider a type of saturated now let's consider a type of onchip network that is called a mesh so onchip network that is called a mesh so onchip network that is called a mesh so here what we have is these core tiles here what we have is these core tiles here what we have is these core tiles are individual are individual are individual connected like this for example now connected like this for example now connected like this for example now there is no more broadcast but this core there is no more broadcast but this core there is no more broadcast but this core can still send a message to any other can still send a message to any other can still send a message to any other core if we send a message to this this core if we send a message to this this core if we send a message to this this is the link that gets used meanwhile is the link that gets used meanwhile is the link that gets used meanwhile this core could be sending a message this core could be sending a message this core could be sending a message here if we need to send a message from here if we need to send a message from here if we need to send a message from here to here then these two links get here to here then these two links get here to here then these two links get used meanwhile we can get these two used meanwhile we can get these two used meanwhile we can get these two links used independently so as you can links used independently so as you can links used independently so as you can see the total available throughput in see the total available throughput in see the total available throughput in the entire network is several times the the entire network is several times the the entire network is several times the throughput of this individual link link throughput of this individual link link throughput of this individual link link and because it's a short link we can and because it's a short link we can and because it's a short link we can have a very large throughput now I'm have a very large throughput now I'm have a very large throughput now I'm going to take this 2x two mesh and copy going to take this 2x two mesh and copy going to take this 2x two mesh and copy it so that I get four of those and it so that I get four of those and it so that I get four of those and create a 4x4 mesh that now supports 16 create a 4x4 mesh that now supports 16 create a 4x4 mesh that now supports 16 cores twice as many as here note how cores twice as many as here note how cores twice as many as here note how many new links are there now if we have many new links are there now if we have many new links are there now if we have neighbor to neighbor communication we neighbor to neighbor communication we neighbor to neighbor communication we can have these two talking these two can have these two talking these two can have these two talking these two talking these two talking all over talking these two talking all over talking these two talking all over independent links so as I increase the independent links so as I increase the independent links so as I increase the number of cores I also increase the number of cores I also increase the number of cores I also increase the number of these links naturally so that number of these links naturally so that number of these links naturally so that the total trut available in this network the total trut available in this network the total trut available in this network grows with the number of cores so the grows with the number of cores so the grows with the number of cores so the number of cores grows the number of number of cores grows the number of number of cores grows the number of links grows and that allows the links grows and that allows the links grows and that allows the available through put to grow and that available through put to grow and that available through put to grow and that means that I can have many more cores means that I can have many more cores means that I can have many more cores than I could on the bus there are many than I could on the bus there are many than I could on the bus there are many types of this so-called point to point types of this so-called point to point types of this so-called point to point networks a mesh is one of those that is networks a mesh is one of those that is networks a mesh is one of those that is very good for chip building because none very good for chip building because none very good for chip building because none of these links intersect each other so of these links intersect each other so of these links intersect each other so because chips are basically made by because chips are basically made by because chips are basically made by printing things on Silicon it's printing things on Silicon it's printing things on Silicon it's naturally good to have something that is naturally good to have something that is naturally good to have something that is kind of flat like this and doesn't have kind of flat like this and doesn't have kind of flat like this and doesn't have links going across different links going across different links going across different nodes but we can build chips with some nodes but we can build chips with some nodes but we can build chips with some amount of links Crossing each other and amount of links Crossing each other and amount of links Crossing each other and thus we can have a mesh which is kind of thus we can have a mesh which is kind of thus we can have a mesh which is kind of like what we have seen we can have have like what we have seen we can have have like what we have seen we can have have a Taurus Network you build a Taurus by a Taurus Network you build a Taurus by a Taurus Network you build a Taurus by taking the mesh and connecting the end taking the mesh and connecting the end taking the mesh and connecting the end points to each other so the Taurus points to each other so the Taurus points to each other so the Taurus really takes the link and kind of wraps really takes the link and kind of wraps really takes the link and kind of wraps it around and then wraps it around and it around and then wraps it around and it around and then wraps it around and so on and then you do that in the other so on and then you do that in the other so on and then you do that in the other dimension tool you can think of these dimension tool you can think of these dimension tool you can think of these processors as basically things on a processors as basically things on a processors as basically things on a donut you would take this wrap it around donut you would take this wrap it around donut you would take this wrap it around you get a cylinder then you take the end you get a cylinder then you take the end you get a cylinder then you take the end points of the cylinder bend it and points of the cylinder bend it and points of the cylinder bend it and create a d so that's why it's called a create a d so that's why it's called a create a d so that's why it's called a Taurus but in reality what you really do Taurus but in reality what you really do Taurus but in reality what you really do is you do this have a long link that is you do this have a long link that is you do this have a long link that crosses here and a long link that crosses here and a long link that crosses here and a long link that crosses here and so on and then you also crosses here and so on and then you also crosses here and so on and then you also take this and create a long link that take this and create a long link that take this and create a long link that goes here so that's how you create a goes here so that's how you create a goes here so that's how you create a Taurus and then there are even fancier Taurus and then there are even fancier Taurus and then there are even fancier networks that are still reasonably good networks that are still reasonably good networks that are still reasonably good for on chip implementation such as for on chip implementation such as for on chip implementation such as flattened butterfly if you're interested flattened butterfly if you're interested flattened butterfly if you're interested in how this work a very good place to in how this work a very good place to in how this work a very good place to start would be an advanced class on on start would be an advanced class on on start would be an advanced class on on chip interconnects

## Mesh vs Bus Throughput Quiz： Part 6
Kind: captions Language: en let's see if we can compare the mesh and let's see if we can compare the mesh and let's see if we can compare the mesh and the bus throughput for a small four core the bus throughput for a small four core the bus throughput for a small four core system where the traffic is evenly system where the traffic is evenly system where the traffic is evenly distributed so that each core sends a distributed so that each core sends a distributed so that each core sends a quarter of all the messages in the quarter of all the messages in the quarter of all the messages in the system and the messages that each core system and the messages that each core system and the messages that each core sends are sent round robbing to all the sends are sent round robbing to all the sends are sent round robbing to all the other three cores so if we are core zero other three cores so if we are core zero other three cores so if we are core zero we send messages to one then two then we send messages to one then two then we send messages to one then two then three then one then two then three Etc three then one then two then three Etc three then one then two then three Etc if we're core two then we send to 0 1 3 if we're core two then we send to 0 1 3 if we're core two then we send to 0 1 3 0 1 3 Etc and you can assume that this 0 1 3 Etc and you can assume that this 0 1 3 Etc and you can assume that this is totally randomly distributed so that is totally randomly distributed so that is totally randomly distributed so that we don't necessarily get any we don't necessarily get any we don't necessarily get any synchronicity among these axises pretty synchronicity among these axises pretty synchronicity among these axises pretty much just like a third of the traffic much just like a third of the traffic much just like a third of the traffic sent by each core goes to each of the sent by each core goes to each of the sent by each core goes to each of the other core but you can assume that it other core but you can assume that it other core but you can assume that it happens willy-nilly each core sends 10 happens willy-nilly each core sends 10 happens willy-nilly each core sends 10 million messages per second the bus can million messages per second the bus can million messages per second the bus can support 20 million messages per second support 20 million messages per second support 20 million messages per second the mesh can support 20 million messages the mesh can support 20 million messages the mesh can support 20 million messages per second per link and obviously the per second per link and obviously the per second per link and obviously the four cores are going to saturate the bus four cores are going to saturate the bus four cores are going to saturate the bus bandwidth in which case they will just bandwidth in which case they will just bandwidth in which case they will just have to slow down proportionally so that have to slow down proportionally so that have to slow down proportionally so that they match the bandwidth of the bus the they match the bandwidth of the bus the they match the bandwidth of the bus the question for you is the speed up question for you is the speed up question for you is the speed up achieved by switching to the mesh from achieved by switching to the mesh from achieved by switching to the mesh from the bus is what

## Mesh vs Bus Thoughput Quiz Solution： Part 6
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our mesh versus bus throughput quiz where we mesh versus bus throughput quiz where we mesh versus bus throughput quiz where we had four cores had four cores had four cores with a totally random and even with a totally random and even with a totally random and even distribution of traffic among the cores distribution of traffic among the cores distribution of traffic among the cores we are given what each core is sending we are given what each core is sending we are given what each core is sending we are getting what the bus can do and we are getting what the bus can do and we are getting what the bus can do and what the link in a mesh can do and now what the link in a mesh can do and now what the link in a mesh can do and now we are asked how much faster is it we are asked how much faster is it we are asked how much faster is it on the bus versus on the mesh our bus on the bus versus on the mesh our bus on the bus versus on the mesh our bus cannot sustain the 40 million messages cannot sustain the 40 million messages cannot sustain the 40 million messages per second per second per second that the cores are trying to give it that the cores are trying to give it that the cores are trying to give it so the cores are really going to slow so the cores are really going to slow so the cores are really going to slow down to half of their speed so that the down to half of their speed so that the down to half of their speed so that the bus bandwidth demand matches what it can bus bandwidth demand matches what it can bus bandwidth demand matches what it can provide provide provide so on the bus we get 2x the execution so on the bus we get 2x the execution so on the bus we get 2x the execution time that each core is capable of time that each core is capable of time that each core is capable of providing on the mesh we can support 20 providing on the mesh we can support 20 providing on the mesh we can support 20 million messages per second per link million messages per second per link million messages per second per link so now the question is do our links get so now the question is do our links get so now the question is do our links get saturated so let's see what we get on a saturated so let's see what we get on a saturated so let's see what we get on a mesh like this in terms of traffic each mesh like this in terms of traffic each mesh like this in terms of traffic each quarter is generating 10 million quarter is generating 10 million quarter is generating 10 million messages 3.3 million are going to each messages 3.3 million are going to each messages 3.3 million are going to each other core other core other core so of the 10 million messages sent here so of the 10 million messages sent here so of the 10 million messages sent here one third are going to this score one one third are going to this score one one third are going to this score one third are going through this link and third are going through this link and third are going through this link and then one third are going here let's say then one third are going here let's say then one third are going here let's say that one sixth go this way one sixth go that one sixth go this way one sixth go that one sixth go this way one sixth go this way so this link will be used for this way so this link will be used for this way so this link will be used for one sixth of the messages sent by this one sixth of the messages sent by this one sixth of the messages sent by this score and this one for another one sixth score and this one for another one sixth score and this one for another one sixth and then we add 1 6 to each of these and then we add 1 6 to each of these and then we add 1 6 to each of these which brings us to one half of 10 which brings us to one half of 10 which brings us to one half of 10 million messages so now we are done with million messages so now we are done with million messages so now we are done with this score what happens from this score this score what happens from this score this score what happens from this score same thing this link gets one half of same thing this link gets one half of same thing this link gets one half of the messages one third are going here the messages one third are going here the messages one third are going here one 6 are going here one 6 are going here one 6 are going here so this link suffers one half of this so this link suffers one half of this so this link suffers one half of this traffic again now this link gets traffic again now this link gets traffic again now this link gets one half of the traffic from this core one half of the traffic from this core one half of the traffic from this core and these two links get one sixth each and these two links get one sixth each and these two links get one sixth each and now we're done with this score now and now we're done with this score now and now we're done with this score now what happens when this score is sending what happens when this score is sending what happens when this score is sending one half of the messages go this way one one half of the messages go this way one one half of the messages go this way one half going go this way half going go this way half going go this way one sixth of them go through this link one sixth of them go through this link one sixth of them go through this link and one six go through this link done and one six go through this link done and one six go through this link done with this score and finally this score with this score and finally this score with this score and finally this score sends messages half here have here sends messages half here have here sends messages half here have here and then one sixth here and then one sixth here and then one sixth here and one sixth here now we're done with and one sixth here now we're done with and one sixth here now we're done with this course so as you can see our links this course so as you can see our links this course so as you can see our links are actually evenly used are actually evenly used are actually evenly used each of them gets two times one-half each of them gets two times one-half each of them gets two times one-half plus two-sixths so we get plus two-sixths so we get plus two-sixths so we get one and one-third one and one-third one and one-third times times times the traffic generated by one core that the traffic generated by one core that the traffic generated by one core that was what the one-half was off the was what the one-half was off the was what the one-half was off the traffic from one core is 10 million traffic from one core is 10 million traffic from one core is 10 million messages a second so we're really messages a second so we're really messages a second so we're really getting 10 getting 10 getting 10 plus 3.3 so this is 13.3 million of plus 3.3 so this is 13.3 million of plus 3.3 so this is 13.3 million of messages per second which is available messages per second which is available messages per second which is available on our mesh so the mesh doesn't slow on our mesh so the mesh doesn't slow on our mesh so the mesh doesn't slow down the processors down the processors down the processors so really the speed up of using the mesh so really the speed up of using the mesh so really the speed up of using the mesh versus the bus versus the bus versus the bus is two because the bus requires the is two because the bus requires the is two because the bus requires the cores to slow down so that cores to slow down so that cores to slow down so that it can support the traffic that they're it can support the traffic that they're it can support the traffic that they're generating generating generating while the mesh allows the cores to while the mesh allows the cores to while the mesh allows the cores to proceed at full speed at 10 million proceed at full speed at 10 million proceed at full speed at 10 million messages per second and still has some messages per second and still has some messages per second and still has some traffic left over traffic left over traffic left over if we went to a larger network than four if we went to a larger network than four if we went to a larger network than four cores the bus would have to cores the bus would have to cores the bus would have to proportionally slow down while the mesh proportionally slow down while the mesh proportionally slow down while the mesh would get more links would get more links would get more links and it will be a while until any of the and it will be a while until any of the and it will be a while until any of the mesh links become saturated so clearly mesh links become saturated so clearly mesh links become saturated so clearly the mesh is far superior to the bus in the mesh is far superior to the bus in the mesh is far superior to the bus in terms of the traffic it can support when terms of the traffic it can support when terms of the traffic it can support when we go to more course

## Many Core Challenges 2： Part 6
Kind: captions Language: en so going back to our many core so going back to our many core so going back to our many core challenges we have seen that when the challenges we have seen that when the challenges we have seen that when the number of cores grows number of cores grows number of cores grows so does the coherence traffic on the so does the coherence traffic on the so does the coherence traffic on the chip chip chip and for that we needed a scalable and for that we needed a scalable and for that we needed a scalable on-chip network such as a mesh on-chip network such as a mesh on-chip network such as a mesh and the directory coherence which we and the directory coherence which we and the directory coherence which we have already seen in our cache coherence have already seen in our cache coherence have already seen in our cache coherence lesson lesson lesson another thing another thing another thing that having more cores that having more cores that having more cores will make more difficult is that as the will make more difficult is that as the will make more difficult is that as the number of cores goes up number of cores goes up number of cores goes up the off-chip traffic that we need goes the off-chip traffic that we need goes the off-chip traffic that we need goes up to have decent performance up to have decent performance up to have decent performance we will as we increase the number of we will as we increase the number of we will as we increase the number of cores increase the number of on-chip cores increase the number of on-chip cores increase the number of on-chip caches so if we have four cores we have caches so if we have four cores we have caches so if we have four cores we have four level one and possibly level 2 four level one and possibly level 2 four level one and possibly level 2 caches when we have 64 cores we'll have caches when we have 64 cores we'll have caches when we have 64 cores we'll have 64 level 1 and level 2 caches 64 level 1 and level 2 caches 64 level 1 and level 2 caches so each so each so each core core core doesn't really generate more misses but doesn't really generate more misses but doesn't really generate more misses but it generates the same number of misses it generates the same number of misses it generates the same number of misses regardless of how many cores we have so regardless of how many cores we have so regardless of how many cores we have so we have the same number of misses per we have the same number of misses per we have the same number of misses per core so as we have more cores the number core so as we have more cores the number core so as we have more cores the number of memory requests of memory requests of memory requests which is what we do when we have a miss which is what we do when we have a miss which is what we do when we have a miss would go up in proportion to the number would go up in proportion to the number would go up in proportion to the number of cores of cores of cores now note that the number of pins the now note that the number of pins the now note that the number of pins the number of those little wires sticking number of those little wires sticking number of those little wires sticking out of a chip out of a chip out of a chip does go up slowly does go up slowly does go up slowly but not in proportion or anywhere close but not in proportion or anywhere close but not in proportion or anywhere close to in proportion to the number of cores to in proportion to the number of cores to in proportion to the number of cores so if we double the number of cores so if we double the number of cores so if we double the number of cores possibly we get maybe 10 percent more possibly we get maybe 10 percent more possibly we get maybe 10 percent more pins but that's it pins but that's it pins but that's it because these pins have to physically be because these pins have to physically be because these pins have to physically be large enough to not break when we are large enough to not break when we are large enough to not break when we are moving the chip around moving the chip around moving the chip around and plugging it into a motherboard so and plugging it into a motherboard so and plugging it into a motherboard so what happens is we get some little what happens is we get some little what happens is we get some little improvement in off-chip throughput while improvement in off-chip throughput while improvement in off-chip throughput while our demand for it basically grows in our demand for it basically grows in our demand for it basically grows in proportion to the number of cores so our proportion to the number of cores so our proportion to the number of cores so our off-chip available throughput becomes a off-chip available throughput becomes a off-chip available throughput becomes a bottleneck so we need to reduce the bottleneck so we need to reduce the bottleneck so we need to reduce the number of memory requests that we number of memory requests that we number of memory requests that we generate per core and the way we do that generate per core and the way we do that generate per core and the way we do that is to make the last level cache which in is to make the last level cache which in is to make the last level cache which in most today's processors is the level most today's processors is the level most today's processors is the level three cache three cache three cache shared so that all cores go to that shared so that all cores go to that shared so that all cores go to that cache and its size is shared equally by cache and its size is shared equally by cache and its size is shared equally by all the cores and the size of that cache all the cores and the size of that cache all the cores and the size of that cache needs to go up in proportion to the needs to go up in proportion to the needs to go up in proportion to the number of cores as it goes up so as we number of cores as it goes up so as we number of cores as it goes up so as we double the number of cores we should at double the number of cores we should at double the number of cores we should at least double the size of the last level least double the size of the last level least double the size of the last level cache but there is a problem to having cache but there is a problem to having cache but there is a problem to having one huge llc like this one huge llc like this one huge llc like this such a large cache would be slow and if such a large cache would be slow and if such a large cache would be slow and if it's really one single cache it would it's really one single cache it would it's really one single cache it would have one entry point where we enter the have one entry point where we enter the have one entry point where we enter the address and expect the data out address and expect the data out address and expect the data out and that entry point needs to be and that entry point needs to be and that entry point needs to be somewhere on our chip that now possibly somewhere on our chip that now possibly somewhere on our chip that now possibly has a mesh or another advanced network has a mesh or another advanced network has a mesh or another advanced network and that means that that one point and that means that that one point and that means that that one point will be where we send all of the level will be where we send all of the level will be where we send all of the level two misses so it would become a two misses so it would become a two misses so it would become a bottleneck pretty much now we don't get bottleneck pretty much now we don't get bottleneck pretty much now we don't get to use all of the links equally this set to use all of the links equally this set to use all of the links equally this set of links that go to this entry point for of links that go to this entry point for of links that go to this entry point for the cache would get much more traffic the cache would get much more traffic the cache would get much more traffic than the other links if we double the than the other links if we double the than the other links if we double the number of cores then those links around number of cores then those links around number of cores then those links around the entry point get double the traffic the entry point get double the traffic the entry point get double the traffic so what we do so what we do so what we do is we don't have just one big last level is we don't have just one big last level is we don't have just one big last level cache we have what is called a cache we have what is called a cache we have what is called a distributed last level cache now let's distributed last level cache now let's distributed last level cache now let's see what that looks like

## Distributed LLC： Part 6
Kind: captions Language: en so a distributed last level cache is so a distributed last level cache is so a distributed last level cache is logically a single cache logically a single cache logically a single cache in that a data block will not be in that a data block will not be in that a data block will not be replicated replicated replicated like it would be in private caches where like it would be in private caches where like it would be in private caches where if two or three or four cores for if two or three or four cores for if two or three or four cores for example read a block each of their example read a block each of their example read a block each of their caches would have that block and thus caches would have that block and thus caches would have that block and thus really the total capacity of the caches really the total capacity of the caches really the total capacity of the caches is not how much memory we are covering is not how much memory we are covering is not how much memory we are covering because some memory appears many times because some memory appears many times because some memory appears many times possibly in these caches possibly in these caches possibly in these caches a last level cache that is distributed a last level cache that is distributed a last level cache that is distributed is logically still one cache is logically still one cache is logically still one cache meaning if its capacity is let's say meaning if its capacity is let's say meaning if its capacity is let's say four megabytes then we really cover four four megabytes then we really cover four four megabytes then we really cover four megabytes of memory without any megabytes of memory without any megabytes of memory without any replication replication replication but this distributed cache is sliced up but this distributed cache is sliced up but this distributed cache is sliced up so that each tile which contains a core so that each tile which contains a core so that each tile which contains a core and possibly local caches now also and possibly local caches now also and possibly local caches now also contains a part of this cache so now contains a part of this cache so now contains a part of this cache so now what we have is what we have is what we have is in our mesh in our mesh in our mesh we have a tile we have a tile we have a tile that contains a core a level one cache a that contains a core a level one cache a that contains a core a level one cache a level two cache and a slice of the l3 level two cache and a slice of the l3 level two cache and a slice of the l3 cache cache cache so if this slice here that we use per so if this slice here that we use per so if this slice here that we use per core is let's say one megabyte core is let's say one megabyte core is let's say one megabyte is really n times one megabytes where n is really n times one megabytes where n is really n times one megabytes where n is the number of cores so now as you can is the number of cores so now as you can is the number of cores so now as you can see see see if we use four cores we get four if we use four cores we get four if we use four cores we get four megabytes with 16 cores we have 16 megabytes with 16 cores we have 16 megabytes with 16 cores we have 16 megabytes so we get what we want megabytes so we get what we want megabytes so we get what we want the big cache the big cache the big cache that is there to prevent memory accesses that is there to prevent memory accesses that is there to prevent memory accesses is growing with the number of cores but is growing with the number of cores but is growing with the number of cores but it doesn't have a single entry point in it doesn't have a single entry point in it doesn't have a single entry point in fact each slice has its own entry point fact each slice has its own entry point fact each slice has its own entry point so if we want the data that is in this so if we want the data that is in this so if we want the data that is in this slice we have to route our message to slice we have to route our message to slice we have to route our message to that slice that slice that slice if we want the data that resides in if we want the data that resides in if we want the data that resides in another slice we send a message there another slice we send a message there another slice we send a message there if the data is spread among the slices if the data is spread among the slices if the data is spread among the slices so that so that so that all of them get equal demands for the all of them get equal demands for the all of them get equal demands for the data then the network traffic would be data then the network traffic would be data then the network traffic would be nicely distributed across our chip so nicely distributed across our chip so nicely distributed across our chip so that none of the links get over utilized that none of the links get over utilized that none of the links get over utilized but if we have a level two miss but if we have a level two miss but if we have a level two miss in a particular core how do we know in a particular core how do we know in a particular core how do we know which slice of the level three cache which slice of the level three cache which slice of the level three cache to ask for the data one simple solution to ask for the data one simple solution to ask for the data one simple solution is to spread the cash round robin among is to spread the cash round robin among is to spread the cash round robin among slices by cash index so if we have our slices by cash index so if we have our slices by cash index so if we have our level 3 cash with let's say 1024 sets level 3 cash with let's say 1024 sets level 3 cash with let's say 1024 sets slice 0 gets set 0 slice 1 gets set 1 slice 0 gets set 0 slice 1 gets set 1 slice 0 gets set 0 slice 1 gets set 1 etc and then let's say that we have 8 etc and then let's say that we have 8 etc and then let's say that we have 8 cores then slice 7 gets set 7. slice 0 cores then slice 7 gets set 7. slice 0 cores then slice 7 gets set 7. slice 0 gets set 8 set 9 is in slice 1 again and gets set 8 set 9 is in slice 1 again and gets set 8 set 9 is in slice 1 again and so on so now we can relatively easily so on so now we can relatively easily so on so now we can relatively easily after we break down the address to after we break down the address to after we break down the address to access the cache we know the set number access the cache we know the set number access the cache we know the set number and and and the least significant the least significant the least significant three bits in this case tell us which three bits in this case tell us which three bits in this case tell us which slice to ask if we have slice to ask if we have slice to ask if we have even more cores then the number of sets even more cores then the number of sets even more cores then the number of sets in the cache will grow and as a result in the cache will grow and as a result in the cache will grow and as a result the number of bits the number of bits the number of bits that we need to tell us which slice we that we need to tell us which slice we that we need to tell us which slice we have is growing and that means that the have is growing and that means that the have is growing and that means that the number of bits in the index are growing number of bits in the index are growing number of bits in the index are growing and we will need additional bits to tell and we will need additional bits to tell and we will need additional bits to tell us in which set to look note that if we us in which set to look note that if we us in which set to look note that if we sequentially access data then we will be sequentially access data then we will be sequentially access data then we will be really looking at you know one set and really looking at you know one set and really looking at you know one set and then the next set and then the next set then the next set and then the next set then the next set and then the next set and this nicely spreads the load among and this nicely spreads the load among and this nicely spreads the load among the slices of the level three cache the slices of the level three cache the slices of the level three cache because sequential accesses are because sequential accesses are because sequential accesses are basically going to be spread across the basically going to be spread across the basically going to be spread across the entire chip however the spreading of entire chip however the spreading of entire chip however the spreading of blocks round robin blocks round robin blocks round robin across the entire chip may not be good across the entire chip may not be good across the entire chip may not be good for locality for locality for locality because a core is just as likely to because a core is just as likely to because a core is just as likely to access something that is in the opposite access something that is in the opposite access something that is in the opposite corner of the chip as it is to access corner of the chip as it is to access corner of the chip as it is to access its own slice in which case there is no its own slice in which case there is no its own slice in which case there is no on-chip network traffic whatsoever on-chip network traffic whatsoever on-chip network traffic whatsoever so another way that can help with that so another way that can help with that so another way that can help with that is that we spread the data around but is that we spread the data around but is that we spread the data around but this time we spread it by page number so this time we spread it by page number so this time we spread it by page number so all of the blocks that belong to the all of the blocks that belong to the all of the blocks that belong to the same page would end up in the same slice same page would end up in the same slice same page would end up in the same slice of the level three that helps because of the level three that helps because of the level three that helps because now the operating system can map pages now the operating system can map pages now the operating system can map pages so that it makes the accesses to l3 more so that it makes the accesses to l3 more so that it makes the accesses to l3 more local for example here this score is local for example here this score is local for example here this score is running a thread that has a stack running a thread that has a stack running a thread that has a stack the pages that belong to that stack the pages that belong to that stack the pages that belong to that stack should be those that end up in this should be those that end up in this should be those that end up in this slice so that if we have a level two slice so that if we have a level two slice so that if we have a level two miss we are very likely to access our miss we are very likely to access our miss we are very likely to access our own local slice of the l3 cache of own local slice of the l3 cache of own local slice of the l3 cache of course course course some of the shared data will be us some of the shared data will be us some of the shared data will be us elsewhere but this dramatically improves elsewhere but this dramatically improves elsewhere but this dramatically improves the locality in terms of how far do we the locality in terms of how far do we the locality in terms of how far do we need to go to get to the correct slice need to go to get to the correct slice need to go to get to the correct slice of l3 of l3 of l3 any data that tends to be accessed by any data that tends to be accessed by any data that tends to be accessed by one core can be now mapped by the one core can be now mapped by the one core can be now mapped by the operating system so that it's usually operating system so that it's usually operating system so that it's usually found in the local slice of the last found in the local slice of the last found in the local slice of the last level cache

## Distributed LLC Quiz： Part 6
Kind: captions Language: en so let's see if we can figure out how so let's see if we can figure out how so let's see if we can figure out how the distributed last level cache works the distributed last level cache works the distributed last level cache works let's say we have a process chip with 16 let's say we have a process chip with 16 let's say we have a process chip with 16 cores organized is a four by four mesh cores organized is a four by four mesh cores organized is a four by four mesh like this the cores along with their like this the cores along with their like this the cores along with their level one and level two caches and a level one and level two caches and a level one and level two caches and a slice of l3 are connected like this so a slice of l3 are connected like this so a slice of l3 are connected like this so a single tile here in this mesh has a core single tile here in this mesh has a core single tile here in this mesh has a core a level one level two cache private to a level one level two cache private to a level one level two cache private to that core and a slice of the shared last that core and a slice of the shared last that core and a slice of the shared last level level three cash and that level level level three cash and that level level level three cash and that level three cash is eight megabytes in size three cash is eight megabytes in size three cash is eight megabytes in size has a 256 byte block size is 16 way set has a 256 byte block size is 16 way set has a 256 byte block size is 16 way set associative and is distributed among the associative and is distributed among the associative and is distributed among the slices round-robin by set number if core slices round-robin by set number if core slices round-robin by set number if core zero issues a load from address one two zero issues a load from address one two zero issues a load from address one two three four five six seven eight hex and three four five six seven eight hex and three four five six seven eight hex and if this load is a missing level one and if this load is a missing level one and if this load is a missing level one and in level two we send the level three in level two we send the level three in level two we send the level three requests to what tile

## Distributed LLC Quiz Solution： Part 6
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our distributed last level cache we have 16 distributed last level cache we have 16 distributed last level cache we have 16 cores in a four by four mesh it doesn't cores in a four by four mesh it doesn't cores in a four by four mesh it doesn't really matter what the organization of really matter what the organization of really matter what the organization of the network is we need to know that the network is we need to know that the network is we need to know that there are really 16 tiles there are really 16 tiles there are really 16 tiles numbered 0 through 15 numbered 0 through 15 numbered 0 through 15 and that we have an 8 megabyte cache and that we have an 8 megabyte cache and that we have an 8 megabyte cache with 256 byte block 16 way set with 256 byte block 16 way set with 256 byte block 16 way set associative distributed round robin by associative distributed round robin by associative distributed round robin by set so set 0 will be in slice 0 set 1 set so set 0 will be in slice 0 set 1 set so set 0 will be in slice 0 set 1 will be in slice 1 etc and then it just will be in slice 1 etc and then it just will be in slice 1 etc and then it just wraps around so let's look at wraps around so let's look at wraps around so let's look at what this address maps to what this address maps to what this address maps to the least significant the least significant the least significant 8 bits are telling us what the block 8 bits are telling us what the block 8 bits are telling us what the block offset is offset is offset is the next so many bits are going to be the next so many bits are going to be the next so many bits are going to be the index and the least significant bits the index and the least significant bits the index and the least significant bits of the index are going to tell us which of the index are going to tell us which of the index are going to tell us which tile to look at tile to look at tile to look at there are 16 tiles there are 16 tiles there are 16 tiles so the next digit is really the tile so the next digit is really the tile so the next digit is really the tile number number number and that means and that means and that means that we go to tile number six that we go to tile number six that we go to tile number six note that we don't really need to even note that we don't really need to even note that we don't really need to even compute what exactly is the index compute what exactly is the index compute what exactly is the index because the least significant bits in because the least significant bits in because the least significant bits in this case are telling us this case are telling us this case are telling us what we need to know

## Many Core Challenges 2： Part 6
Kind: captions Language: en now going back to our many core now going back to our many core now going back to our many core challenges we have seen that the number challenges we have seen that the number challenges we have seen that the number of cores as it grows makes the coherence of cores as it grows makes the coherence of cores as it grows makes the coherence traffic grow and we looked at solutions traffic grow and we looked at solutions traffic grow and we looked at solutions to deal with that we then looked at the to deal with that we then looked at the to deal with that we then looked at the problem of the off-chip traffic also problem of the off-chip traffic also problem of the off-chip traffic also growing with the number of course and we growing with the number of course and we growing with the number of course and we just saw that a large shared but just saw that a large shared but just saw that a large shared but distributed last level cache can help distributed last level cache can help distributed last level cache can help with this problem the next problem we with this problem the next problem we with this problem the next problem we face is that the coherence directory face is that the coherence directory face is that the coherence directory that we need is just way too large to that we need is just way too large to that we need is just way too large to fit on chip because we have a scalable fit on chip because we have a scalable fit on chip because we have a scalable onship network and no longer a bus we onship network and no longer a bus we onship network and no longer a bus we need directory coherence but a need directory coherence but a need directory coherence but a traditional directory has one entry for traditional directory has one entry for traditional directory has one entry for each possible memory block the memory each possible memory block the memory each possible memory block the memory can be many gigabytes in size that might can be many gigabytes in size that might can be many gigabytes in size that might require even billions of directory require even billions of directory require even billions of directory entries and that won't fit on ship so entries and that won't fit on ship so entries and that won't fit on ship so how do we deal with that problem

## On Chip Directory： Part 6
Kind: captions Language: en so we need a directory that will fit on so we need a directory that will fit on so we need a directory that will fit on chip otherwise to maintain coherence chip otherwise to maintain coherence chip otherwise to maintain coherence between our caches that are very very between our caches that are very very between our caches that are very very fast we would need to look up a fast we would need to look up a fast we would need to look up a directory in very very slow memory which directory in very very slow memory which directory in very very slow memory which kind of defeats the purpose of having kind of defeats the purpose of having kind of defeats the purpose of having these caches some of the questions about these caches some of the questions about these caches some of the questions about this directory are easy to answer like this directory are easy to answer like this directory are easy to answer like for example where is the home node where for example where is the home node where for example where is the home node where we go to find the directory information we go to find the directory information we go to find the directory information about the block and the answer to that about the block and the answer to that about the block and the answer to that is the same tile that has the last level is the same tile that has the last level is the same tile that has the last level cache slice where that block would be cache slice where that block would be cache slice where that block would be because if the directory entry for that because if the directory entry for that because if the directory entry for that block indicates that none of the private block indicates that none of the private block indicates that none of the private caches have it then we want to look up caches have it then we want to look up caches have it then we want to look up the last level cache to find that block the last level cache to find that block the last level cache to find that block so we might as well do that in the same so we might as well do that in the same so we might as well do that in the same node that has a directory otherwise node that has a directory otherwise node that has a directory otherwise there will be a lot of traffic between there will be a lot of traffic between there will be a lot of traffic between where the directory for a block is and where the directory for a block is and where the directory for a block is and where the last level cache for the block where the last level cache for the block where the last level cache for the block is so naturally they should be on the is so naturally they should be on the is so naturally they should be on the same node but that doesn't really answer same node but that doesn't really answer same node but that doesn't really answer the question of having a directory entry the question of having a directory entry the question of having a directory entry for every memory block we have seen that for every memory block we have seen that for every memory block we have seen that we only keep the data in the last level we only keep the data in the last level we only keep the data in the last level cache slides for those blocks that we cache slides for those blocks that we cache slides for those blocks that we actually have in our level 3 cache but a actually have in our level 3 cache but a actually have in our level 3 cache but a directory without any changes would need directory without any changes would need directory without any changes would need to be for every possible memory block to be for every possible memory block to be for every possible memory block that might ever come into that slice that might ever come into that slice that might ever come into that slice which is a big problem because there are which is a big problem because there are which is a big problem because there are a lot of memory blocks so instead of a lot of memory blocks so instead of a lot of memory blocks so instead of keeping a director entry for every keeping a director entry for every keeping a director entry for every memory block we need to realize that the memory block we need to realize that the memory block we need to realize that the entries that are not in our last level entries that are not in our last level entries that are not in our last level cache or in any caches on chip really cache or in any caches on chip really cache or in any caches on chip really are not shared by anybody so we don't are not shared by anybody so we don't are not shared by anybody so we don't really have to maintain that information really have to maintain that information really have to maintain that information so we will maintain what is called a so we will maintain what is called a so we will maintain what is called a partial directory we don't maintain a partial directory we don't maintain a partial directory we don't maintain a directory entry for every memory block directory entry for every memory block directory entry for every memory block instead our directory will have a instead our directory will have a instead our directory will have a limited number of entries and then we limited number of entries and then we limited number of entries and then we allocate and enter in this limited allocate and enter in this limited allocate and enter in this limited directory only for such blocks that have directory only for such blocks that have directory only for such blocks that have at least one presence bit at one they at least one presence bit at one they at least one presence bit at one they basically only for blocks that might be basically only for blocks that might be basically only for blocks that might be in at least one of the private caches so in at least one of the private caches so in at least one of the private caches so for a block for which we know that it is for a block for which we know that it is for a block for which we know that it is not in any L1 or L2 cache it's present not in any L1 or L2 cache it's present not in any L1 or L2 cache it's present only in the LLC or only in memory for only in the LLC or only in memory for only in the LLC or only in memory for that matter we don't need a directory that matter we don't need a directory that matter we don't need a directory entry because implicitly we know that entry because implicitly we know that entry because implicitly we know that such a directory entry would just be all such a directory entry would just be all such a directory entry would just be all zeros but let's say that we have decided zeros but let's say that we have decided zeros but let's say that we have decided on some limited number of entries we on some limited number of entries we on some limited number of entries we start allocating those entries when we start allocating those entries when we start allocating those entries when we put blocks in L2 and L1 caches and put blocks in L2 and L1 caches and put blocks in L2 and L1 caches and eventually we will run out of directory eventually we will run out of directory eventually we will run out of directory entries what do we do now

## On Chip Directory Quiz： Part 6
Kind: captions Language: en So now let's see if we can figure out So now let's see if we can figure out So now let's see if we can figure out how to do an onchip directory where we how to do an onchip directory where we how to do an onchip directory where we said that we're going to have a partial said that we're going to have a partial said that we're going to have a partial directory. The directory has a limited directory. The directory has a limited directory. The directory has a limited number of entries in each slice. We number of entries in each slice. We number of entries in each slice. We allocate an entry only for a block that allocate an entry only for a block that allocate an entry only for a block that might have some presence bit set at one. might have some presence bit set at one. might have some presence bit set at one. But as we request blocks from level one But as we request blocks from level one But as we request blocks from level one caches, we will allocate these entries. caches, we will allocate these entries. caches, we will allocate these entries. Eventually we will run out of them. So Eventually we will run out of them. So Eventually we will run out of them. So the question for you is what do we do the question for you is what do we do the question for you is what do we do now? We have run out of directory now? We have run out of directory now? We have run out of directory entries. How do we proceed?

## On Chip Directory Quiz Solution： Part 6
Kind: captions Language: en let's look at the answer to our onship let's look at the answer to our onship let's look at the answer to our onship directory quiz directory quiz directory quiz we said that we might run out of these we said that we might run out of these we said that we might run out of these partial directory entries and the partial directory entries and the partial directory entries and the question is what do we do now and the question is what do we do now and the question is what do we do now and the answer is the same thing we did when we answer is the same thing we did when we answer is the same thing we did when we run out of room in a cache and that is run out of room in a cache and that is run out of room in a cache and that is replace an existing entry possibly using replace an existing entry possibly using replace an existing entry possibly using a replacement policy such as lru the a replacement policy such as lru the a replacement policy such as lru the really interesting question is what do really interesting question is what do really interesting question is what do we need to do when we replace a we need to do when we replace a we need to do when we replace a directory entry



---


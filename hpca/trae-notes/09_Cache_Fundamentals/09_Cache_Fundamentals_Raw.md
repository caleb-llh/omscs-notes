# 09_Cache_Fundamentals (Raw Transcripts)

# Playlist 2 Module 8 Raw Transcript

## Locality Principle： Part 3
Kind: captions Language: en to understand caches we must first to understand caches we must first to understand caches we must first understand the concept of locality which understand the concept of locality which understand the concept of locality which is also called the locality principle it is also called the locality principle it is also called the locality principle it says that things that will happen soon says that things that will happen soon says that things that will happen soon are likely to be close to things that are likely to be close to things that are likely to be close to things that just happened this means that if we know just happened this means that if we know just happened this means that if we know something about past behavior we are something about past behavior we are something about past behavior we are likely to be able to guess what's going likely to be able to guess what's going likely to be able to guess what's going to happen soon we have already used this to happen soon we have already used this to happen soon we have already used this locality principle for example when we locality principle for example when we locality principle for example when we did branch friction now we will use it did branch friction now we will use it did branch friction now we will use it for caches

## Locality Quiz： Part 3
Kind: captions Language: en let's do a quiz to see if we understood let's do a quiz to see if we understood let's do a quiz to see if we understood locality which of these are not good locality which of these are not good locality which of these are not good examples of locality it trained three examples of locality it trained three examples of locality it trained three times already today so we can conclude times already today so we can conclude times already today so we can conclude that it is likely to rain again today we that it is likely to rain again today we that it is likely to rain again today we ate dinner at 600 every day last week so ate dinner at 600 every day last week so ate dinner at 600 every day last week so we conclude that probably we will eat we conclude that probably we will eat we conclude that probably we will eat dinner around 6 this week too and it was dinner around 6 this week too and it was dinner around 6 this week too and it was New Year's Eve yesterday so we conclude New Year's Eve yesterday so we conclude New Year's Eve yesterday so we conclude that probably it will be New Year's Eve that probably it will be New Year's Eve that probably it will be New Year's Eve today too again you you need to find today too again you you need to find today too again you you need to find which of these are not good examples of which of these are not good examples of which of these are not good examples of locality

## Locality Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our locality let's look at the answer to our locality let's look at the answer to our locality quiz we were asked which of these are quiz we were asked which of these are quiz we were asked which of these are not good examples of locality the not good examples of locality the not good examples of locality the statement that he trained three times statement that he trained three times statement that he trained three times today so it will likely rain today again today so it will likely rain today again today so it will likely rain today again is a decent example of locality usually is a decent example of locality usually is a decent example of locality usually if it rained often it's probably gonna if it rained often it's probably gonna if it rained often it's probably gonna just continue raining often for a while just continue raining often for a while just continue raining often for a while we ate dinner at 6:00 every day last we ate dinner at 6:00 every day last we ate dinner at 6:00 every day last week so probably we will eat dinner week so probably we will eat dinner week so probably we will eat dinner around 6:00 this week too this also around 6:00 this week too this also around 6:00 this week too this also tends to be true people tend to eat tends to be true people tend to eat tends to be true people tend to eat meals around the same time for a while meals around the same time for a while meals around the same time for a while and finally it was New Year's Eve and finally it was New Year's Eve and finally it was New Year's Eve yesterday so it will probably be New yesterday so it will probably be New yesterday so it will probably be New Year's Eve today too this one is not a Year's Eve today too this one is not a Year's Eve today too this one is not a good example of vocality why well good example of vocality why well good example of vocality why well because some things do not have locality because some things do not have locality because some things do not have locality for example it being New Year's Eve one for example it being New Year's Eve one for example it being New Year's Eve one day usually means that the next day it day usually means that the next day it day usually means that the next day it won't be New Year's Eve because New won't be New Year's Eve because New won't be New Year's Eve because New Year's Eve is one of those things that Year's Eve is one of those things that Year's Eve is one of those things that do not have much locality if it was do not have much locality if it was do not have much locality if it was yesterday then we know it's not gonna be yesterday then we know it's not gonna be yesterday then we know it's not gonna be today which is kind of opposite of what today which is kind of opposite of what today which is kind of opposite of what the locality principle is about as we the locality principle is about as we the locality principle is about as we can see some things do have the locality can see some things do have the locality can see some things do have the locality property and some things do not

## Memory References： Part 3
Kind: captions Language: en in computer architecture we are in computer architecture we are in computer architecture we are interested in the locality property of interested in the locality property of interested in the locality property of memory references if we know that the memory references if we know that the memory references if we know that the processor has accessed address X processor has accessed address X processor has accessed address X recently the locality principle says recently the locality principle says recently the locality principle says that it is likely to access the same that it is likely to access the same that it is likely to access the same address again in the near future and it address again in the near future and it address again in the near future and it is likely to access addresses close to is likely to access addresses close to is likely to access addresses close to x2 this is what we call temporal x2 this is what we call temporal x2 this is what we call temporal locality once we access an address we locality once we access an address we locality once we access an address we are likely to access the same address are likely to access the same address are likely to access the same address again this is what we call spatial again this is what we call spatial again this is what we call spatial locality if we access an address we are locality if we access an address we are locality if we access an address we are likely to access nearby addresses soon

## Temporal Locality Quiz： Part 3
Kind: captions Language: en let's do a quiz about temporal locality let's do a quiz about temporal locality let's do a quiz about temporal locality to see if we understood it consider this to see if we understood it consider this to see if we understood it consider this code fragment code fragment code fragment which is basically iterating through an which is basically iterating through an which is basically iterating through an array and adding every element of that array and adding every element of that array and adding every element of that array to the sum which of these memory array to the sum which of these memory array to the sum which of these memory locations locations locations has temporal locality in this code the has temporal locality in this code the has temporal locality in this code the variable j the variable sum and or the variable j the variable sum and or the variable j the variable sum and or the elements of the array are elements of the array are elements of the array are more than one more than one more than one might have temporal locality so check might have temporal locality so check might have temporal locality so check all of those that do

## Temporal Locality Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our temporal let's look at the answer to our temporal let's look at the answer to our temporal locality quiz in this code the question locality quiz in this code the question locality quiz in this code the question was which of these memory locations have was which of these memory locations have was which of these memory locations have temporal locality and the choices were J temporal locality and the choices were J temporal locality and the choices were J sum and the elements of our temporal sum and the elements of our temporal sum and the elements of our temporal locality says that once we access a locality says that once we access a locality says that once we access a memory location we are likely to access memory location we are likely to access memory location we are likely to access it again for the variable J this is true it again for the variable J this is true it again for the variable J this is true we are using it here here here in every we are using it here here here in every we are using it here here here in every iteration of the loop so the first time iteration of the loop so the first time iteration of the loop so the first time we access it if we conclude that it will we access it if we conclude that it will we access it if we conclude that it will be accessed again soon we would be right be accessed again soon we would be right be accessed again soon we would be right actually once we begin accessing it is actually once we begin accessing it is actually once we begin accessing it is going to be accessed a lot it's going to going to be accessed a lot it's going to going to be accessed a lot it's going to be accessed to initialize it to check if be accessed to initialize it to check if be accessed to initialize it to check if it's less than a thousand to access the it's less than a thousand to access the it's less than a thousand to access the array to increment it to check to access array to increment it to check to access array to increment it to check to access to increment many many times so this to increment many many times so this to increment many many times so this variable definitely does have temporal variable definitely does have temporal variable definitely does have temporal locality the variable sum same thing it locality the variable sum same thing it locality the variable sum same thing it is being initialized and then in every is being initialized and then in every is being initialized and then in every iteration of this loop it is going to be iteration of this loop it is going to be iteration of this loop it is going to be read so that we can add something to it read so that we can add something to it read so that we can add something to it and then written back to memory so this and then written back to memory so this and then written back to memory so this definitely also has temporal locality definitely also has temporal locality definitely also has temporal locality once we access it we can conclude that once we access it we can conclude that once we access it we can conclude that it will be accessed soon again for each it will be accessed soon again for each it will be accessed soon again for each element of the array are however and element of the array are however and element of the array are however and note that the array has many many memory note that the array has many many memory note that the array has many many memory locations we do not have temporal locations we do not have temporal locations we do not have temporal locality element are of zero once it's locality element are of zero once it's locality element are of zero once it's accessed in the loop that's the only accessed in the loop that's the only accessed in the loop that's the only time we access in this loop we will time we access in this loop we will time we access in this loop we will access our of one out of two etc but for access our of one out of two etc but for access our of one out of two etc but for each element it's accessed only once so each element it's accessed only once so each element it's accessed only once so if we conclude that when it's accessed if we conclude that when it's accessed if we conclude that when it's accessed it's going to be accessed again soon we it's going to be accessed again soon we it's going to be accessed again soon we would not be correct so this choice would not be correct so this choice would not be correct so this choice should not be selected this variable should not be selected this variable should not be selected this variable does not have temporal locality in this does not have temporal locality in this does not have temporal locality in this code

## Spatial Locality Quiz： Part 3
Kind: captions Language: en let's do another quiz on locality this let's do another quiz on locality this let's do another quiz on locality this time about spatial locality if we have time about spatial locality if we have time about spatial locality if we have the same code as in the previous quiz the same code as in the previous quiz the same code as in the previous quiz and the question now is which of these and the question now is which of these and the question now is which of these memory locations has spatial locality in memory locations has spatial locality in memory locations has spatial locality in this code check all of those that do

## Spatial Locality Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our spatial let's look at the answer to our spatial let's look at the answer to our spatial locality quiz the question is which locality quiz the question is which locality quiz the question is which memory locations have spatial locality memory locations have spatial locality memory locations have spatial locality remember that spatial locality says that remember that spatial locality says that remember that spatial locality says that once we access the memory location we once we access the memory location we once we access the memory location we are likely to access nearby memory are likely to access nearby memory are likely to access nearby memory locations soon for the variable J we locations soon for the variable J we locations soon for the variable J we keep pre accessing J but we don't know keep pre accessing J but we don't know keep pre accessing J but we don't know what's close to J so we cannot really what's close to J so we cannot really what's close to J so we cannot really say that J has spatial locality if we say that J has spatial locality if we say that J has spatial locality if we access J there is a lot of temporal access J there is a lot of temporal access J there is a lot of temporal locality but not necessarily any spatial locality but not necessarily any spatial locality but not necessarily any spatial locality same for some we are accessing locality same for some we are accessing locality same for some we are accessing some very often by itself but we are not some very often by itself but we are not some very often by itself but we are not really sure whether we are accessing really sure whether we are accessing really sure whether we are accessing anything that is close to some four anything that is close to some four anything that is close to some four elements of array R we have seen that elements of array R we have seen that elements of array R we have seen that there is no temporal locality each there is no temporal locality each there is no temporal locality each element is accessed only once in this element is accessed only once in this element is accessed only once in this loop however there is a lot of spatial loop however there is a lot of spatial loop however there is a lot of spatial locality once we access R of 0 in the locality once we access R of 0 in the locality once we access R of 0 in the next iteration we will be accessing R of next iteration we will be accessing R of next iteration we will be accessing R of 1 and then R of 2 and so on so it is 1 and then R of 2 and so on so it is 1 and then R of 2 and so on so it is true that once we access a memory true that once we access a memory true that once we access a memory location in this array we are likely to location in this array we are likely to location in this array we are likely to access nearby locations soon so this access nearby locations soon so this access nearby locations soon so this variable definitely has spatial locality variable definitely has spatial locality variable definitely has spatial locality it turns out that the way this program it turns out that the way this program it turns out that the way this program is compiled usually means that there is compiled usually means that there is compiled usually means that there will be some spatial locality between J will be some spatial locality between J will be some spatial locality between J and some J and some are likely to be and some J and some are likely to be and some J and some are likely to be allocated by the compiler on the stack allocated by the compiler on the stack allocated by the compiler on the stack near each other so when we access some near each other so when we access some near each other so when we access some and then access J that might be an and then access J that might be an and then access J that might be an example of spatial locality

## Locality and Data Accesses： Part 3
Kind: captions Language: en so now that we know what locality is so now that we know what locality is so now that we know what locality is let's see how do we use it to improve let's see how do we use it to improve let's see how do we use it to improve data accesses first we will look at an data accesses first we will look at an data accesses first we will look at an example that considers borrowing books example that considers borrowing books example that considers borrowing books from the library in this example the from the library in this example the from the library in this example the library represents a data repository library represents a data repository library represents a data repository that is large so a lot of data there but that is large so a lot of data there but that is large so a lot of data there but it is very slow to access we need to go it is very slow to access we need to go it is very slow to access we need to go there find the book in the building there find the book in the building there find the book in the building check it out bring it back home and so check it out bring it back home and so check it out bring it back home and so on when we are borrowing books from the on when we are borrowing books from the on when we are borrowing books from the library typically there is a lot of library typically there is a lot of library typically there is a lot of temporal locality to it for example if temporal locality to it for example if temporal locality to it for example if we are a student we might need to look we are a student we might need to look we are a student we might need to look up the definition of locality very often up the definition of locality very often up the definition of locality very often while we're studying and usually also while we're studying and usually also while we're studying and usually also there is a lot of spatial locality to there is a lot of spatial locality to there is a lot of spatial locality to using books for example if we look at using books for example if we look at using books for example if we look at some computer architecture definition some computer architecture definition some computer architecture definition ones we are likely to be looking up ones we are likely to be looking up ones we are likely to be looking up other computer architecture stuff too in other computer architecture stuff too in other computer architecture stuff too in the near future so continuing our the near future so continuing our the near future so continuing our library based locality in data access library based locality in data access library based locality in data access example we have seen that the library is example we have seen that the library is example we have seen that the library is large but very slow to access and it large but very slow to access and it large but very slow to access and it accesses to information in the library accesses to information in the library accesses to information in the library have temporal and spatial locality have temporal and spatial locality have temporal and spatial locality usually so when a student needs a piece usually so when a student needs a piece usually so when a student needs a piece of information from the library what of information from the library what of information from the library what should he do should he go to the library should he do should he go to the library should he do should he go to the library find information read it and then just find information read it and then just find information read it and then just go back home go back home go back home should he borrow the book take it home should he borrow the book take it home should he borrow the book take it home so that future accesses to the so that future accesses to the so that future accesses to the information in the book are much faster information in the book are much faster information in the book are much faster or should he take all the books and then or should he take all the books and then or should he take all the books and then build a library at home let's consider build a library at home let's consider build a library at home let's consider each of these three options if we go to each of these three options if we go to each of these three options if we go to the library every time we need something the library every time we need something the library every time we need something we're wasting a lot of time finding the we're wasting a lot of time finding the we're wasting a lot of time finding the books and then looking up only one piece books and then looking up only one piece books and then looking up only one piece of information and then returning the of information and then returning the of information and then returning the book because we know that there is both book because we know that there is both book because we know that there is both temporal and spatial locality which temporal and spatial locality which temporal and spatial locality which means we will be probably reading the means we will be probably reading the means we will be probably reading the same book and nearby books very often so same book and nearby books very often so same book and nearby books very often so the problem with this approach is that the problem with this approach is that the problem with this approach is that it does not benefit from locality and it does not benefit from locality and it does not benefit from locality and this is why typically people do not use this is why typically people do not use this is why typically people do not use it when they need to study a topic and it when they need to study a topic and it when they need to study a topic and use library resources if we go to the use library resources if we go to the use library resources if we go to the library and borrow the book we break it library and borrow the book we break it library and borrow the book we break it home and now we have the book on the home and now we have the book on the home and now we have the book on the topic that we are currently working on topic that we are currently working on topic that we are currently working on close to us so this is an approach that close to us so this is an approach that close to us so this is an approach that does exploit temporal and spatial does exploit temporal and spatial does exploit temporal and spatial locality and eliminates the problem of locality and eliminates the problem of locality and eliminates the problem of the the the library being large and slow because we library being large and slow because we library being large and slow because we have very few books bottled at home so have very few books bottled at home so have very few books bottled at home so we can quickly find what we need in them we can quickly find what we need in them we can quickly find what we need in them this is why most people will use this this is why most people will use this this is why most people will use this option the approach of taking all the option the approach of taking all the option the approach of taking all the books in building a library at home is books in building a library at home is books in building a library at home is very expensive and also provides very very expensive and also provides very very expensive and also provides very little benefit yes it saves us the trip little benefit yes it saves us the trip little benefit yes it saves us the trip to the library and back but it doesn't to the library and back but it doesn't to the library and back but it doesn't save us the time of searching among many save us the time of searching among many save us the time of searching among many books finding them on the shelves and so books finding them on the shelves and so books finding them on the shelves and so on so really we'd rather have relatively on so really we'd rather have relatively on so really we'd rather have relatively few books that we are currently few books that we are currently few books that we are currently interested in then have many books and interested in then have many books and interested in then have many books and have to look them up very slowly so just have to look them up very slowly so just have to look them up very slowly so just like people when faced with these like people when faced with these like people when faced with these choices about the library usually choose choices about the library usually choose choices about the library usually choose the second option because it's a good the second option because it's a good the second option because it's a good trade-off between being able to find the trade-off between being able to find the trade-off between being able to find the information quickly and not being information quickly and not being information quickly and not being overwhelmed with the quantity of overwhelmed with the quantity of overwhelmed with the quantity of information that we have brought in when information that we have brought in when information that we have brought in when we have a processor that needs to access we have a processor that needs to access we have a processor that needs to access main memory we will use the same main memory we will use the same main memory we will use the same approach instead of going to memory for approach instead of going to memory for approach instead of going to memory for every single memory location we will every single memory location we will every single memory location we will bring the content of memory locations bring the content of memory locations bring the content of memory locations that we are interested in closer to us that we are interested in closer to us that we are interested in closer to us but we will try not to bring too much but we will try not to bring too much but we will try not to bring too much close to us because that will result in close to us because that will result in close to us because that will result in a very slow structure close to us so we a very slow structure close to us so we a very slow structure close to us so we will again have slow accesses so what we will again have slow accesses so what we will again have slow accesses so what we want is something small that keeps only want is something small that keeps only want is something small that keeps only the information we are really interested the information we are really interested the information we are really interested in and that small repository of in and that small repository of in and that small repository of information is called a cache when we information is called a cache when we information is called a cache when we are talking about memory accesses

## Cache Quiz： Part 3
Kind: captions Language: en so now let's try to apply the concept so now let's try to apply the concept so now let's try to apply the concept that we have learned to an actual cache that we have learned to an actual cache that we have learned to an actual cache we know that the main memory in a we know that the main memory in a we know that the main memory in a computer is large and very slow to computer is large and very slow to computer is large and very slow to access compared to processor speed and access compared to processor speed and access compared to processor speed and also we know that there is lots of also we know that there is lots of also we know that there is lots of spatial and temporal locality present in spatial and temporal locality present in spatial and temporal locality present in our data accesses most programs exhibit our data accesses most programs exhibit our data accesses most programs exhibit a lot of spatial and temporal locality a lot of spatial and temporal locality a lot of spatial and temporal locality so for an access to a memory location so for an access to a memory location so for an access to a memory location what do you think we should do should we what do you think we should do should we what do you think we should do should we go to the main memory for every access go to the main memory for every access go to the main memory for every access should we have a small memory inside the should we have a small memory inside the should we have a small memory inside the processor core and then we bring stuff processor core and then we bring stuff processor core and then we bring stuff we access there so that next time we we access there so that next time we we access there so that next time we need it we can find it quickly or should need it we can find it quickly or should need it we can find it quickly or should we have a huge memory next to our we have a huge memory next to our we have a huge memory next to our processor chip because it won't fit on processor chip because it won't fit on processor chip because it won't fit on the processor chip and then as we read the processor chip and then as we read the processor chip and then as we read from the main memory we bring everything from the main memory we bring everything from the main memory we bring everything into this memory that is next to us

## Cache Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our cache let's look at the answer to our cache let's look at the answer to our cache quiz these three are very similar to the quiz these three are very similar to the quiz these three are very similar to the options we have when we want to get options we have when we want to get options we have when we want to get books from a library this is like going books from a library this is like going books from a library this is like going to the library every time we need to to the library every time we need to to the library every time we need to look up something and clearly we don't look up something and clearly we don't look up something and clearly we don't want to do that main memory is just like want to do that main memory is just like want to do that main memory is just like the library slow and very large so if we the library slow and very large so if we the library slow and very large so if we go to the main memory every time we're go to the main memory every time we're go to the main memory every time we're spending a lot of time just waiting for spending a lot of time just waiting for spending a lot of time just waiting for the main memory to respond with data the main memory to respond with data the main memory to respond with data having the small memory inside the having the small memory inside the having the small memory inside the processor core and bringing the stuff we processor core and bringing the stuff we processor core and bringing the stuff we access there is the approach of access there is the approach of access there is the approach of basically basically basically caching the memory caching the memory caching the memory locations and this is very similar to locations and this is very similar to locations and this is very similar to borrowing the book from the library so borrowing the book from the library so borrowing the book from the library so that we can use it for a while while that we can use it for a while while that we can use it for a while while we're interested in that topic and then we're interested in that topic and then we're interested in that topic and then return it and finally having a huge return it and finally having a huge return it and finally having a huge memory NEX door processor and then memory NEX door processor and then memory NEX door processor and then bringing everything in there is solving bringing everything in there is solving bringing everything in there is solving some of the problems of going to the some of the problems of going to the some of the problems of going to the main memory because this memory is main memory because this memory is main memory because this memory is hopefully somewhat closer to our hopefully somewhat closer to our hopefully somewhat closer to our processor but this memory is still going processor but this memory is still going processor but this memory is still going to be large and thus very slow so this to be large and thus very slow so this to be large and thus very slow so this is very similar to just building a is very similar to just building a is very similar to just building a library closer to home yes you are library closer to home yes you are library closer to home yes you are saving some time on each access but most saving some time on each access but most saving some time on each access but most of the time that you spend per access is of the time that you spend per access is of the time that you spend per access is still there simply because it takes a still there simply because it takes a still there simply because it takes a while to find data in such a large while to find data in such a large while to find data in such a large memory so caches are exploiting spatial memory so caches are exploiting spatial memory so caches are exploiting spatial and temporal locality and overcoming the and temporal locality and overcoming the and temporal locality and overcoming the problem of having a slow and large main problem of having a slow and large main problem of having a slow and large main memory just like borrowing books from memory just like borrowing books from memory just like borrowing books from the library is exploiting spatial and the library is exploiting spatial and the library is exploiting spatial and temporal locality and over coming the temporal locality and over coming the temporal locality and over coming the problem of accessing a huge Library problem of accessing a huge Library problem of accessing a huge Library slowly

## Cache Look Ups： Part 3
Kind: captions Language: en so now that we know that the cash is a so now that we know that the cash is a so now that we know that the cash is a small memory insider processor where we small memory insider processor where we small memory insider processor where we are trying to find the data before we go are trying to find the data before we go are trying to find the data before we go to the main memory let's take some time to the main memory let's take some time to the main memory let's take some time and recap what we need out of a cache we and recap what we need out of a cache we and recap what we need out of a cache we need it to be fast which means it has to need it to be fast which means it has to need it to be fast which means it has to be small because it's small not be small because it's small not be small because it's small not everything will fit there will be a lot everything will fit there will be a lot everything will fit there will be a lot of memory locations that we do not have of memory locations that we do not have of memory locations that we do not have room for in our cache so when a room for in our cache so when a room for in our cache so when a processor wants to access some memory we processor wants to access some memory we processor wants to access some memory we can have a cache hit which occurs when can have a cache hit which occurs when can have a cache hit which occurs when we find what we are looking for in the we find what we are looking for in the we find what we are looking for in the cache the memory location we are cache the memory location we are cache the memory location we are interested in was already brought from interested in was already brought from interested in was already brought from the memory to the cache before so now we the memory to the cache before so now we the memory to the cache before so now we are accessing it and we can just find it are accessing it and we can just find it are accessing it and we can just find it in the cache and the access is then very in the cache and the access is then very in the cache and the access is then very quick so this is what we want we want quick so this is what we want we want quick so this is what we want we want fast accesses for most of our memory fast accesses for most of our memory fast accesses for most of our memory accesses and that happens when we have a accesses and that happens when we have a accesses and that happens when we have a lot of cache hits but because the cache lot of cache hits but because the cache lot of cache hits but because the cache is very small and not everything fits in is very small and not everything fits in is very small and not everything fits in it we can also have a cache miss this it we can also have a cache miss this it we can also have a cache miss this happens when what we are looking for is happens when what we are looking for is happens when what we are looking for is not in the cache and in that case we not in the cache and in that case we not in the cache and in that case we have to access the slow memory when we have to access the slow memory when we have to access the slow memory when we have a cache miss we want to copy this have a cache miss we want to copy this have a cache miss we want to copy this location to the cache so that next time location to the cache so that next time location to the cache so that next time we access it and hopefully we will we access it and hopefully we will we access it and hopefully we will because of locality we will have a cache because of locality we will have a cache because of locality we will have a cache hit so it is necessary to have cache hit so it is necessary to have cache hit so it is necessary to have cache misses because that's how we really misses because that's how we really misses because that's how we really bring stuff into the cache in order to bring stuff into the cache in order to bring stuff into the cache in order to have hits the more locality we have the have hits the more locality we have the have hits the more locality we have the more cache hits we will have and the more cache hits we will have and the more cache hits we will have and the fewer cache misses because one miss will fewer cache misses because one miss will fewer cache misses because one miss will bring the data into the cache and then bring the data into the cache and then bring the data into the cache and then hopefully we will use it many many times hopefully we will use it many many times hopefully we will use it many many times when we have a cache miss that is slow when we have a cache miss that is slow when we have a cache miss that is slow that access is going to take some time that access is going to take some time that access is going to take some time but that should happen very rarely most but that should happen very rarely most but that should happen very rarely most of the time when we need something we of the time when we need something we of the time when we need something we find it in the cache or at least we hope find it in the cache or at least we hope find it in the cache or at least we hope to do that and we want to design our to do that and we want to design our to do that and we want to design our caches so that that's how it works

## Cache Performance： Part 3
Kind: captions Language: en So let's see what does it take to have a So let's see what does it take to have a So let's see what does it take to have a good cache or what are the properties of good cache or what are the properties of good cache or what are the properties of a good cache. We want our system to have a good cache. We want our system to have a good cache. We want our system to have a good average memory access time or AAT a good average memory access time or AAT a good average memory access time or AAT and this is the access time to memory as and this is the access time to memory as and this is the access time to memory as seen by the processor. The seen by the processor. The seen by the processor. The AAT is equal to the cach's hit time. How AAT is equal to the cach's hit time. How AAT is equal to the cach's hit time. How quickly can it return the data when we quickly can it return the data when we quickly can it return the data when we do have a cash hit plus how often do we do have a cash hit plus how often do we do have a cash hit plus how often do we have a miss times the miss penalty. We have a miss times the miss penalty. We have a miss times the miss penalty. We want the AAT to be very low. The lower want the AAT to be very low. The lower want the AAT to be very low. The lower the better. This means that we want the the better. This means that we want the the better. This means that we want the hit time to be low which means we want a hit time to be low which means we want a hit time to be low which means we want a small and fast cache because that will small and fast cache because that will small and fast cache because that will contribute to having this be low. We contribute to having this be low. We contribute to having this be low. We want a low miss rate, which means we want a low miss rate, which means we want a low miss rate, which means we want our cash to be large and/or smart. want our cash to be large and/or smart. want our cash to be large and/or smart. If it's a large cache, more stuff will If it's a large cache, more stuff will If it's a large cache, more stuff will fit in it. So, we will miss less often. fit in it. So, we will miss less often. fit in it. So, we will miss less often. Or if it's smart, it will know better Or if it's smart, it will know better Or if it's smart, it will know better what to keep inside. So, again, we will what to keep inside. So, again, we will what to keep inside. So, again, we will have fewer misses. The miss penalty for have fewer misses. The miss penalty for have fewer misses. The miss penalty for simple caches is simply the main memory simple caches is simply the main memory simple caches is simply the main memory access time. This is going to be very access time. This is going to be very access time. This is going to be very large, usually tens or even hundreds of large, usually tens or even hundreds of large, usually tens or even hundreds of processor cycles. So really when we're processor cycles. So really when we're processor cycles. So really when we're designing caches, we're trying to designing caches, we're trying to designing caches, we're trying to balance the hit time and the miss balance the hit time and the miss balance the hit time and the miss rate. Small and fast versus large and rate. Small and fast versus large and rate. Small and fast versus large and intelligent. And intelligent typically intelligent. And intelligent typically intelligent. And intelligent typically means slightly means slightly means slightly slower. So some caches can be extremely slower. So some caches can be extremely slower. So some caches can be extremely small and fast, have very good hit time, small and fast, have very good hit time, small and fast, have very good hit time, but sometimes that's at the expense of but sometimes that's at the expense of but sometimes that's at the expense of having a larger miss rate. Some caches having a larger miss rate. Some caches having a larger miss rate. Some caches will have a relatively large hit time will have a relatively large hit time will have a relatively large hit time but extremely low miss rates so that we but extremely low miss rates so that we but extremely low miss rates so that we can reduce the overall contribution of can reduce the overall contribution of can reduce the overall contribution of main memory accesses that contribute a main memory accesses that contribute a main memory accesses that contribute a lot each time we have them. Sometimes we lot each time we have them. Sometimes we lot each time we have them. Sometimes we also talk about miss time which is the also talk about miss time which is the also talk about miss time which is the overall time it takes to have a cache overall time it takes to have a cache overall time it takes to have a cache miss. A miss time is simply the hit time miss. A miss time is simply the hit time miss. A miss time is simply the hit time followed by the miss penalty because we followed by the miss penalty because we followed by the miss penalty because we try to find what we have in the cache try to find what we have in the cache try to find what we have in the cache but fail and then we still have to pay but fail and then we still have to pay but fail and then we still have to pay the miss penalty. We can also think the miss penalty. We can also think the miss penalty. We can also think about miss time is the memory access about miss time is the memory access about miss time is the memory access time when we are missing. So miss rate time when we are missing. So miss rate time when we are missing. So miss rate is one. So again it would be hit time is one. So again it would be hit time is one. So again it would be hit time plus the miss penalty. So another way to plus the miss penalty. So another way to plus the miss penalty. So another way to write the amat expression would be to write the amat expression would be to write the amat expression would be to say that it is 1 minus miss rate times say that it is 1 minus miss rate times say that it is 1 minus miss rate times the hit time. So this is how often do we the hit time. So this is how often do we the hit time. So this is how often do we have the hits times the time for the hit have the hits times the time for the hit have the hits times the time for the hit plus how often do we have the misses plus how often do we have the misses plus how often do we have the misses times the miss time. Usually we want to times the miss time. Usually we want to times the miss time. Usually we want to use this expression simply because the use this expression simply because the use this expression simply because the miss time typically includes checking miss time typically includes checking miss time typically includes checking whether what we have is in the cache whether what we have is in the cache whether what we have is in the cache followed by whatever we need to do on a followed by whatever we need to do on a followed by whatever we need to do on a miss. So somehow every time we will have miss. So somehow every time we will have miss. So somehow every time we will have the time it takes to hit but sometimes the time it takes to hit but sometimes the time it takes to hit but sometimes we will also pay the miss penalty.

## Hit Time Quiz： Part 3
Kind: captions Language: en now that we have seen the relationship now that we have seen the relationship now that we have seen the relationship between hit time missed time and cash between hit time missed time and cash between hit time missed time and cash performance let's do a quiz about them performance let's do a quiz about them performance let's do a quiz about them which of the following are true for a which of the following are true for a which of the following are true for a well-designed cash is it true that hit well-designed cash is it true that hit well-designed cash is it true that hit time is less than miss time that hit time is less than miss time that hit time is less than miss time that hit time is larger than the Miss penalty time is larger than the Miss penalty time is larger than the Miss penalty that the hit time should be equal to the that the hit time should be equal to the that the hit time should be equal to the Miss penalty or miss time is larger than Miss penalty or miss time is larger than Miss penalty or miss time is larger than miss penalty note that more than one miss penalty note that more than one miss penalty note that more than one possibly all of them might be true or possibly all of them might be true or possibly all of them might be true or maybe none of them are true so check all maybe none of them are true so check all maybe none of them are true so check all of those that are true for a of those that are true for a of those that are true for a well-designed cash

## Hit Time Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer for her heat let's look at the answer for her heat let's look at the answer for her heat time quiz in a well-designed cache hit time quiz in a well-designed cache hit time quiz in a well-designed cache hit time is significantly lower than Miss time is significantly lower than Miss time is significantly lower than Miss time miss time includes the hit time and time miss time includes the hit time and time miss time includes the hit time and the Miss penalty typically we want the the Miss penalty typically we want the the Miss penalty typically we want the hit time to be very small and that means hit time to be very small and that means hit time to be very small and that means that the Miss time should not be that the Miss time should not be that the Miss time should not be anywhere close to the hit time and if anywhere close to the hit time and if anywhere close to the hit time and if there is any miss penalty miss time will there is any miss penalty miss time will there is any miss penalty miss time will be larger than hit time anyway so this be larger than hit time anyway so this be larger than hit time anyway so this should be true for every cache even if should be true for every cache even if should be true for every cache even if you didn't design it very well hit time you didn't design it very well hit time you didn't design it very well hit time is larger than the Miss penalty we is larger than the Miss penalty we is larger than the Miss penalty we definitely don't want that the Miss definitely don't want that the Miss definitely don't want that the Miss penalty is the memory access time if our penalty is the memory access time if our penalty is the memory access time if our hit time is similar to that or larger hit time is similar to that or larger hit time is similar to that or larger than that then the cache is not gonna be than that then the cache is not gonna be than that then the cache is not gonna be useful at all so this should not be useful at all so this should not be useful at all so this should not be selected hit time equals to the Miss selected hit time equals to the Miss selected hit time equals to the Miss penalty no we want the hit time to be penalty no we want the hit time to be penalty no we want the hit time to be significantly less than the Miss penalty significantly less than the Miss penalty significantly less than the Miss penalty if it is equal to the Miss penalty we if it is equal to the Miss penalty we if it is equal to the Miss penalty we might just as well go to memory every might just as well go to memory every might just as well go to memory every time because that's what the Miss time because that's what the Miss time because that's what the Miss penalty is about so we should not select penalty is about so we should not select penalty is about so we should not select this and finally miss time larger than this and finally miss time larger than this and finally miss time larger than the Miss penalty Miss time is equal to the Miss penalty Miss time is equal to the Miss penalty Miss time is equal to the hit time plus the Miss penalty which the hit time plus the Miss penalty which the hit time plus the Miss penalty which means that the Miss time should always means that the Miss time should always means that the Miss time should always be larger than the Miss penalty anyway be larger than the Miss penalty anyway be larger than the Miss penalty anyway so this is true for every cache not only so this is true for every cache not only so this is true for every cache not only a well designed one as we can see for a a well designed one as we can see for a a well designed one as we can see for a cache to be beneficial it needs to have cache to be beneficial it needs to have cache to be beneficial it needs to have a low hit time in fact in a a low hit time in fact in a a low hit time in fact in a well-designed cache we can say that the well-designed cache we can say that the well-designed cache we can say that the hit time is much less than miss time hit time is much less than miss time hit time is much less than miss time while the Miss time and Miss penalty while the Miss time and Miss penalty while the Miss time and Miss penalty should be similar to each other because should be similar to each other because should be similar to each other because the hit time is so small and the the hit time is so small and the the hit time is so small and the difference between the two is the hit difference between the two is the hit difference between the two is the hit time



---

# Playlist 2 Module 9 Raw Transcript

## Miss Rate Quiz： Part 3
Kind: captions Language: en let's also do a quiz about the Miss rate let's also do a quiz about the Miss rate let's also do a quiz about the Miss rate keep in mind that the hit rate is equal keep in mind that the hit rate is equal keep in mind that the hit rate is equal to 1 minus the Miss rate the Miss rate to 1 minus the Miss rate the Miss rate to 1 minus the Miss rate the Miss rate is how often do we have a cache miss the is how often do we have a cache miss the is how often do we have a cache miss the hit rate is how often do we have a cache hit rate is how often do we have a cache hit rate is how often do we have a cache hit and the two add up to 1 because we hit and the two add up to 1 because we hit and the two add up to 1 because we either have one or the other so which of either have one or the other so which of either have one or the other so which of the following are true for a the following are true for a the following are true for a well-designed cache is it true that in a well-designed cache is it true that in a well-designed cache is it true that in a well-designed cache the hit rate should well-designed cache the hit rate should well-designed cache the hit rate should be larger than the Miss rate the hit be larger than the Miss rate the hit be larger than the Miss rate the hit rate should be less than the Miss rate rate should be less than the Miss rate rate should be less than the Miss rate that the hit rate and the Miss rate that the hit rate and the Miss rate that the hit rate and the Miss rate should be balanced as close to equal as should be balanced as close to equal as should be balanced as close to equal as possible that the hit rate should be possible that the hit rate should be possible that the hit rate should be almost 1 that the Miss rate should be almost 1 that the Miss rate should be almost 1 that the Miss rate should be almost 1 again more than one possibly or almost 1 again more than one possibly or almost 1 again more than one possibly or possibly none of them might be true so possibly none of them might be true so possibly none of them might be true so check all of those that are true for a check all of those that are true for a check all of those that are true for a well-designed cache

## Miss Rate Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer to our miss let's look at the answer to our miss let's look at the answer to our miss rate quiz in a well-designed cache we rate quiz in a well-designed cache we rate quiz in a well-designed cache we want the hit rate to be as high as want the hit rate to be as high as want the hit rate to be as high as possible and the Miss rate to be as low possible and the Miss rate to be as low possible and the Miss rate to be as low as possible because the hit rate as possible because the hit rate as possible because the hit rate determines how often do we have just a determines how often do we have just a determines how often do we have just a hit latency and the Miss rate is how hit latency and the Miss rate is how hit latency and the Miss rate is how often we have to pay the full memory often we have to pay the full memory often we have to pay the full memory latency so we definitely want the hit latency so we definitely want the hit latency so we definitely want the hit rate to be larger than the Miss rate we rate to be larger than the Miss rate we rate to be larger than the Miss rate we definitely do not want the hitter to be definitely do not want the hitter to be definitely do not want the hitter to be smaller than the Miss rate we don't want smaller than the Miss rate we don't want smaller than the Miss rate we don't want them to be equal to each other because them to be equal to each other because them to be equal to each other because that means that half of the time we're that means that half of the time we're that means that half of the time we're going to memory we want to hit much more going to memory we want to hit much more going to memory we want to hit much more than that than that than that we want the hit rate to be almost 1 we want the hit rate to be almost 1 we want the hit rate to be almost 1 because that means that almost always we because that means that almost always we because that means that almost always we are going to have a cache hit and we are going to have a cache hit and we are going to have a cache hit and we definitely do not want the Miss rate to definitely do not want the Miss rate to definitely do not want the Miss rate to be almost 1 because that means that we be almost 1 because that means that we be almost 1 because that means that we are almost never hitting in our cache

## Cache Size in Real Processors： Part 3
Kind: captions Language: en so we have seen that we want the cash to so we have seen that we want the cash to so we have seen that we want the cash to be small and fast but not too small be small and fast but not too small be small and fast but not too small because then it keeps very little stuff because then it keeps very little stuff because then it keeps very little stuff so that it has very few hits so what are so that it has very few hits so what are so that it has very few hits so what are the cache sizes that we have actually the cache sizes that we have actually the cache sizes that we have actually observed in real processors there is a observed in real processors there is a observed in real processors there is a complication there in that modern complication there in that modern complication there in that modern processors usually have several caches processors usually have several caches processors usually have several caches not just one so depending on which cash not just one so depending on which cash not just one so depending on which cash are we talking about it might be larger are we talking about it might be larger are we talking about it might be larger or smaller or smaller or smaller so let's look at caches that we call l1 so let's look at caches that we call l1 so let's look at caches that we call l1 or level 1 caches which are the caches or level 1 caches which are the caches or level 1 caches which are the caches that directly serve is the read and that directly serve is the read and that directly serve is the read and write requests from the processor so write requests from the processor so write requests from the processor so when the processor wants to read a write when the processor wants to read a write when the processor wants to read a write it first goes to the l1 cache to check it first goes to the l1 cache to check it first goes to the l1 cache to check if it has a hit if the l1 cache has a if it has a hit if the l1 cache has a if it has a hit if the l1 cache has a mess then things get complicated because mess then things get complicated because mess then things get complicated because before we go to the main memory we go to before we go to the main memory we go to before we go to the main memory we go to other caches and in several lessons we other caches and in several lessons we other caches and in several lessons we will actually see how that works but for will actually see how that works but for will actually see how that works but for now let you see how big are these level now let you see how big are these level now let you see how big are these level 1 caches in recent processors typical 1 caches in recent processors typical 1 caches in recent processors typical sizes of the l1 cache have been 16 to 64 sizes of the l1 cache have been 16 to 64 sizes of the l1 cache have been 16 to 64 kilobytes this is large enough to get kilobytes this is large enough to get kilobytes this is large enough to get about 90% hit rate meaning only 1/10 of about 90% hit rate meaning only 1/10 of about 90% hit rate meaning only 1/10 of all the accesses from the processor end all the accesses from the processor end all the accesses from the processor end up going beyond this cache yet these are up going beyond this cache yet these are up going beyond this cache yet these are still small enough to have the hit time still small enough to have the hit time still small enough to have the hit time that is equal to 1 2 3 processor cycles that is equal to 1 2 3 processor cycles that is equal to 1 2 3 processor cycles so we spend very few processor cycles so we spend very few processor cycles so we spend very few processor cycles waiting for the data to come back from waiting for the data to come back from waiting for the data to come back from this cache if it's a hit this cache if it's a hit this cache if it's a hit recall that memories have hundreds of recall that memories have hundreds of recall that memories have hundreds of cycles so this is very very fast cycles so this is very very fast cycles so this is very very fast compared to the memory

## Cache Organization： Part 3
Kind: captions Language: en now that we know what the purpose of the now that we know what the purpose of the now that we know what the purpose of the cache is and how big approximately it cache is and how big approximately it cache is and how big approximately it needs to be let's look at how it is needs to be let's look at how it is needs to be let's look at how it is organized internally the questions we organized internally the questions we organized internally the questions we need to ask ourselves is how do we need to ask ourselves is how do we need to ask ourselves is how do we determine if we have a hit or miss so we determine if we have a hit or miss so we determine if we have a hit or miss so we need to know what data do we have in the need to know what data do we have in the need to know what data do we have in the cache and then when the processor gives cache and then when the processor gives cache and then when the processor gives us an address we need to determine us an address we need to determine us an address we need to determine whether that data is or is not in the whether that data is or is not in the whether that data is or is not in the cache and we need to do that very cache and we need to do that very cache and we need to do that very quickly and the other important question quickly and the other important question quickly and the other important question is how do we determine what to kick out is how do we determine what to kick out is how do we determine what to kick out from the cache once we are out of room from the cache once we are out of room from the cache once we are out of room in it and we need to put more stuff in in it and we need to put more stuff in in it and we need to put more stuff in it because we are just having a cache it because we are just having a cache it because we are just having a cache miss so for the first question of how do miss so for the first question of how do miss so for the first question of how do we know whether we have a kid or a Miss we know whether we have a kid or a Miss we know whether we have a kid or a Miss we want something that is very fast that we want something that is very fast that we want something that is very fast that means it needs to be a table of some means it needs to be a table of some means it needs to be a table of some sort that we can quickly index with some sort that we can quickly index with some sort that we can quickly index with some bits of the address and it tells us what bits of the address and it tells us what bits of the address and it tells us what we have in the cache and if we do have we have in the cache and if we do have we have in the cache and if we do have what we are looking for then it gives us what we are looking for then it gives us what we are looking for then it gives us the data so conceptually the cache is a the data so conceptually the cache is a the data so conceptually the cache is a table which we index with some bits that table which we index with some bits that table which we index with some bits that we take from the address of the data so we take from the address of the data so we take from the address of the data so some stuff here needs to tell us whether some stuff here needs to tell us whether some stuff here needs to tell us whether we have hit and the rest of the stuff we have hit and the rest of the stuff we have hit and the rest of the stuff here here here is the data we want if we do have the is the data we want if we do have the is the data we want if we do have the hit how much data we have in each cache hit how much data we have in each cache hit how much data we have in each cache entry is called the block size or the entry is called the block size or the entry is called the block size or the line size because the entry in the cache line size because the entry in the cache line size because the entry in the cache is also called a line and they tell us is also called a line and they tell us is also called a line and they tell us how many bytes are in each of these how many bytes are in each of these how many bytes are in each of these entries if we have only one byte per entries if we have only one byte per entries if we have only one byte per entry then every address will map to a entry then every address will map to a entry then every address will map to a different entry if we have a block size different entry if we have a block size different entry if we have a block size of 1 that means the entry in the cache of 1 that means the entry in the cache of 1 that means the entry in the cache is only one byte every single byte is only one byte every single byte is only one byte every single byte address will have a different entry that address will have a different entry that address will have a different entry that creates several problems one usually the creates several problems one usually the creates several problems one usually the processor can issue accesses not only to processor can issue accesses not only to processor can issue accesses not only to a single byte but also for example load a single byte but also for example load a single byte but also for example load word and store word will access a four word and store word will access a four word and store word will access a four byte location so for a single access now byte location so for a single access now byte location so for a single access now we would need to look up four different we would need to look up four different we would need to look up four different lines in the cache that complicates and lines in the cache that complicates and lines in the cache that complicates and slows down the cache a lot so we slows down the cache a lot so we slows down the cache a lot so we definitely don't want a block size to be definitely don't want a block size to be definitely don't want a block size to be this small we want the block size to be this small we want the block size to be this small we want the block size to be at least as large as the largest single at least as large as the largest single at least as large as the largest single axis that we can do in the cache and axis that we can do in the cache and axis that we can do in the cache and hopefully slightly larger than that just hopefully slightly larger than that just hopefully slightly larger than that just so so so that when we do a load word or store that when we do a load word or store that when we do a load word or store word we find the data in the same cache word we find the data in the same cache word we find the data in the same cache block most of the time the next thing block most of the time the next thing block most of the time the next thing that we want to consider when deciding that we want to consider when deciding that we want to consider when deciding the block size is spatial locality if we the block size is spatial locality if we the block size is spatial locality if we Mason the cache on one axis we will Mason the cache on one axis we will Mason the cache on one axis we will bring in an entire block worth of memory bring in an entire block worth of memory bring in an entire block worth of memory stuff into the cache so if there is no stuff into the cache so if there is no stuff into the cache so if there is no spatial locality we want to bring in spatial locality we want to bring in spatial locality we want to bring in just what we are currently accessing but just what we are currently accessing but just what we are currently accessing but if there is any spatial locality we want if there is any spatial locality we want if there is any spatial locality we want to bring more than what we are just to bring more than what we are just to bring more than what we are just accessing so typically block sizes of 32 accessing so typically block sizes of 32 accessing so typically block sizes of 32 to 128 bytes work well both from the to 128 bytes work well both from the to 128 bytes work well both from the perspective of they're larger than a perspective of they're larger than a perspective of they're larger than a typical access and also they capture typical access and also they capture typical access and also they capture much of the spatial locality that exists much of the spatial locality that exists much of the spatial locality that exists in programs now let's look at block in programs now let's look at block in programs now let's look at block sizes of something like one kilobyte in sizes of something like one kilobyte in sizes of something like one kilobyte in that case what happens is we fetch a lot that case what happens is we fetch a lot that case what happens is we fetch a lot of data from memory every time we have a of data from memory every time we have a of data from memory every time we have a Miss and if there is not enough locality Miss and if there is not enough locality Miss and if there is not enough locality a lot of the data will not be used so we a lot of the data will not be used so we a lot of the data will not be used so we are occupying space in our cache are occupying space in our cache are occupying space in our cache remember that the cache is only 16 to 64 remember that the cache is only 16 to 64 remember that the cache is only 16 to 64 of these kilobytes so if we bring in a of these kilobytes so if we bring in a of these kilobytes so if we bring in a block like this we are consuming a block like this we are consuming a block like this we are consuming a significant part of the overall space in significant part of the overall space in significant part of the overall space in the cache and we are doing it probably the cache and we are doing it probably the cache and we are doing it probably with data that we won't use much so it with data that we won't use much so it with data that we won't use much so it turns out that for level 1 cache is at turns out that for level 1 cache is at turns out that for level 1 cache is at least we don't want the block size to be least we don't want the block size to be least we don't want the block size to be too large so block sizes of 32 to 128 too large so block sizes of 32 to 128 too large so block sizes of 32 to 128 kilobytes are a balance between getting kilobytes are a balance between getting kilobytes are a balance between getting use of spatial locality when we bring use of spatial locality when we bring use of spatial locality when we bring stuff in versus not being dependent too stuff in versus not being dependent too stuff in versus not being dependent too much on there being a lot of spatial much on there being a lot of spatial much on there being a lot of spatial locality because some programs don't locality because some programs don't locality because some programs don't have that much of it

## Block Size Quiz： Part 3
Kind: captions Language: en so let's do a quiz on block sizes now so let's do a quiz on block sizes now so let's do a quiz on block sizes now consider a cache that is 32 kilobytes in consider a cache that is 32 kilobytes in consider a cache that is 32 kilobytes in size and has a 64 byte block size so size and has a 64 byte block size so size and has a 64 byte block size so let's consider a program that accesses let's consider a program that accesses let's consider a program that accesses variables X 1 X 2 etc up to some X N and variables X 1 X 2 etc up to some X N and variables X 1 X 2 etc up to some X N and these are scalar variables not part of these are scalar variables not part of these are scalar variables not part of an array and let's assume that the an array and let's assume that the an array and let's assume that the program has lots of temporal locality program has lots of temporal locality program has lots of temporal locality when accessing these variables but no when accessing these variables but no when accessing these variables but no spatial locality the question for you is spatial locality the question for you is spatial locality the question for you is what is the largest n the number of what is the largest n the number of what is the largest n the number of these variables that still results in a these variables that still results in a these variables that still results in a high hit rate in this cache put the high hit rate in this cache put the high hit rate in this cache put the number here

## Block Size Quiz Solution： Part 3
Kind: captions Language: en let's look at the solution to our block let's look at the solution to our block let's look at the solution to our block size quiz we have a 32 kilobyte cache size quiz we have a 32 kilobyte cache size quiz we have a 32 kilobyte cache with 64 byte block size and the program with 64 byte block size and the program with 64 byte block size and the program that accesses n variables that have lots that accesses n variables that have lots that accesses n variables that have lots of temporal locality meaning each of of temporal locality meaning each of of temporal locality meaning each of these variables is accessed often in the these variables is accessed often in the these variables is accessed often in the program but no spatial locality meaning program but no spatial locality meaning program but no spatial locality meaning they are nowhere close to each other the they are nowhere close to each other the they are nowhere close to each other the question is what is the largest n the question is what is the largest n the question is what is the largest n the number of these variables that still number of these variables that still number of these variables that still results in a high hit rate now x1 when results in a high hit rate now x1 when results in a high hit rate now x1 when we bring it into the cache we will bring we bring it into the cache we will bring we bring it into the cache we will bring an entire block worth of stuff around it an entire block worth of stuff around it an entire block worth of stuff around it and because there is no spatial locality and because there is no spatial locality and because there is no spatial locality that means that none of the other that means that none of the other that means that none of the other variables will be in that block when we variables will be in that block when we variables will be in that block when we access x2 same thing we will bring 64 access x2 same thing we will bring 64 access x2 same thing we will bring 64 bytes worth of stuff that doesn't bytes worth of stuff that doesn't bytes worth of stuff that doesn't include any of the other variables so include any of the other variables so include any of the other variables so every time we access one of these every time we access one of these every time we access one of these variables we will need 64 bytes worth of variables we will need 64 bytes worth of variables we will need 64 bytes worth of space in the cache if we want to have a space in the cache if we want to have a space in the cache if we want to have a high hit rate because these variables high hit rate because these variables high hit rate because these variables have lots of temporal locality we want have lots of temporal locality we want have lots of temporal locality we want all of them to remain in the cache that all of them to remain in the cache that all of them to remain in the cache that is we want all of them to fit in the is we want all of them to fit in the is we want all of them to fit in the cache so the largest n that still cache so the largest n that still cache so the largest n that still results in the high hit rate is how many results in the high hit rate is how many results in the high hit rate is how many blocks can we fit in your cache really blocks can we fit in your cache really blocks can we fit in your cache really it's a 32 kilobyte cache there are 64 it's a 32 kilobyte cache there are 64 it's a 32 kilobyte cache there are 64 byte blocks how many blocks do we have byte blocks how many blocks do we have byte blocks how many blocks do we have so the answer is 32 kilobytes divided by so the answer is 32 kilobytes divided by so the answer is 32 kilobytes divided by 64 bytes which gives us 512 so the 64 bytes which gives us 512 so the 64 bytes which gives us 512 so the answer here is 512 if we have 512 or answer here is 512 if we have 512 or answer here is 512 if we have 512 or fewer variables then the fact that each fewer variables then the fact that each fewer variables then the fact that each one of them has its own cache block in one of them has its own cache block in one of them has its own cache block in the cache although of the variables the cache although of the variables the cache although of the variables probably only 4 bytes in size is not probably only 4 bytes in size is not probably only 4 bytes in size is not gonna hurt us in terms of hit rate but gonna hurt us in terms of hit rate but gonna hurt us in terms of hit rate but if we have more than 512 variables then if we have more than 512 variables then if we have more than 512 variables then even though there is plenty of space in even though there is plenty of space in even though there is plenty of space in the cache if it's 4 byte variables 512 the cache if it's 4 byte variables 512 the cache if it's 4 byte variables 512 variables is 2 kilobytes worth of actual variables is 2 kilobytes worth of actual variables is 2 kilobytes worth of actual stuff that we are using the block is stuff that we are using the block is stuff that we are using the block is much larger than that but because we are much larger than that but because we are much larger than that but because we are fetching an entire block when we have a fetching an entire block when we have a fetching an entire block when we have a Miss we will not have a good hit rate if Miss we will not have a good hit rate if Miss we will not have a good hit rate if you have you have you have more than 512 of these variables in more than 512 of these variables in more than 512 of these variables in order to be able to have more variables order to be able to have more variables order to be able to have more variables than this we really need them to be than this we really need them to be than this we really need them to be close to each other so that we can close to each other so that we can close to each other so that we can benefit from spatial locality meaning benefit from spatial locality meaning benefit from spatial locality meaning that a block we fetch for one variable that a block we fetch for one variable that a block we fetch for one variable needs to include some of the other needs to include some of the other needs to include some of the other variables

## Cache Block Start Addr： Part 3
Kind: captions Language: en so now that we know that when we have a so now that we know that when we have a so now that we know that when we have a cache miss we fetch an entire block cache miss we fetch an entire block cache miss we fetch an entire block worth of stuff from memory let's talk worth of stuff from memory let's talk worth of stuff from memory let's talk about where can these blocks begin in about where can these blocks begin in about where can these blocks begin in memory one option is a block can begin memory one option is a block can begin memory one option is a block can begin anywhere so let's say we have 64 byte anywhere so let's say we have 64 byte anywhere so let's say we have 64 byte blocks that means that a block for blocks that means that a block for blocks that means that a block for example might go from address 0 through example might go from address 0 through example might go from address 0 through 63 bytes or it can be from 1 through 64 63 bytes or it can be from 1 through 64 63 bytes or it can be from 1 through 64 or it can go from 2 to 65 and so on so or it can go from 2 to 65 and so on so or it can go from 2 to 65 and so on so pretty much if the starting address of pretty much if the starting address of pretty much if the starting address of the block can be anywhere then these are the block can be anywhere then these are the block can be anywhere then these are the possible blocks that we might fetch the possible blocks that we might fetch the possible blocks that we might fetch in our cache this complicates things a in our cache this complicates things a in our cache this complicates things a lot first our cache is a table we want lot first our cache is a table we want lot first our cache is a table we want to index into the table using some bits to index into the table using some bits to index into the table using some bits of the address that we are accessing but of the address that we are accessing but of the address that we are accessing but let's say that what we're accessing is let's say that what we're accessing is let's say that what we're accessing is the address 27 the problem is the block the address 27 the problem is the block the address 27 the problem is the block can actually be found wherever the can actually be found wherever the can actually be found wherever the beginning of address of the block maps beginning of address of the block maps beginning of address of the block maps so block 0 to 63 might map here in the so block 0 to 63 might map here in the so block 0 to 63 might map here in the cache block 1 through 64 might map cache block 1 through 64 might map cache block 1 through 64 might map somewhere else in the cache and so on so somewhere else in the cache and so on so somewhere else in the cache and so on so basically there are many possible places basically there are many possible places basically there are many possible places where the address 27 might be simply where the address 27 might be simply where the address 27 might be simply because each of these blocks might map because each of these blocks might map because each of these blocks might map somewhere else depending on what its somewhere else depending on what its somewhere else depending on what its beginning address is second there is beginning address is second there is beginning address is second there is another complication which is these another complication which is these another complication which is these blocks overlap so the block 0 to 63 blocks overlap so the block 0 to 63 blocks overlap so the block 0 to 63 contains stuff that overlaps with most contains stuff that overlaps with most contains stuff that overlaps with most of what 1 through 64 contains so this of what 1 through 64 contains so this of what 1 through 64 contains so this other than this is the same stuff now other than this is the same stuff now other than this is the same stuff now the problem is when we are reading it's the problem is when we are reading it's the problem is when we are reading it's easy we just need to read one of these easy we just need to read one of these easy we just need to read one of these copies for 27 but when we are writing we copies for 27 but when we are writing we copies for 27 but when we are writing we need to determine which of the copies need to determine which of the copies need to determine which of the copies exist and write to all of them so that exist and write to all of them so that exist and write to all of them so that reading any of them will still be ok so reading any of them will still be ok so reading any of them will still be ok so pretty much we don't want this to happen pretty much we don't want this to happen pretty much we don't want this to happen we don't want to have multiple places we don't want to have multiple places we don't want to have multiple places where the same byte address might be so where the same byte address might be so where the same byte address might be so in order to both reduce the complexity in order to both reduce the complexity in order to both reduce the complexity of accessing the cache and eliminate the of accessing the cache and eliminate the of accessing the cache and eliminate the problem of this repetition of stuff in problem of this repetition of stuff in problem of this repetition of stuff in the cache we will only have caches where the cache we will only have caches where the cache we will only have caches where the blocks start at block aligned the blocks start at block aligned the blocks start at block aligned addresses so for a 64 byte block we will addresses so for a 64 byte block we will addresses so for a 64 byte block we will have a block have a block have a block that contains data from 0 to 63 bytes that contains data from 0 to 63 bytes that contains data from 0 to 63 bytes the next block begins at byte 64 and the next block begins at byte 64 and the next block begins at byte 64 and goes to 127 and so on so pretty much any goes to 127 and so on so pretty much any goes to 127 and so on so pretty much any byte address can be found in only one of byte address can be found in only one of byte address can be found in only one of the possible blocks that also allows us the possible blocks that also allows us the possible blocks that also allows us to simply use some bits of the address to simply use some bits of the address to simply use some bits of the address that tell us which of these blocks are that tell us which of these blocks are that tell us which of these blocks are we talking about an index into the cache we talking about an index into the cache we talking about an index into the cache and find it there so there is only one and find it there so there is only one and find it there so there is only one place in the cache where each of these place in the cache where each of these place in the cache where each of these will fit and so on so for all reasonable will fit and so on so for all reasonable will fit and so on so for all reasonable caches we will really only consider caches we will really only consider caches we will really only consider aligned blocks meaning you cannot fetch aligned blocks meaning you cannot fetch aligned blocks meaning you cannot fetch any 64 bytes or 32 bytes or whatever any 64 bytes or 32 bytes or whatever any 64 bytes or 32 bytes or whatever block sizes from memory into the cache block sizes from memory into the cache block sizes from memory into the cache we need to fetch the 64 byte block that we need to fetch the 64 byte block that we need to fetch the 64 byte block that contains the data and is block aligned

## Blocks in Cache and Memory： Part 3
Kind: captions Language: en so now that we know that cache blocks so now that we know that cache blocks so now that we know that cache blocks need to be at aligned memory addresses need to be at aligned memory addresses need to be at aligned memory addresses let's look at what the memory and cache let's look at what the memory and cache let's look at what the memory and cache look like in terms of blocks our memory look like in terms of blocks our memory look like in terms of blocks our memory can be seen as just a huge array of can be seen as just a huge array of can be seen as just a huge array of memory locations let's say this is memory locations let's say this is memory locations let's say this is location number 0 and let's say that location number 0 and let's say that location number 0 and let's say that each location is 4 bytes wide so the each location is 4 bytes wide so the each location is 4 bytes wide so the next location is at address 4 and then 8 next location is at address 4 and then 8 next location is at address 4 and then 8 and so on let's say that our block size and so on let's say that our block size and so on let's say that our block size is 16 bytes that means that a possible is 16 bytes that means that a possible is 16 bytes that means that a possible block in memory is this this would be block in memory is this this would be block in memory is this this would be the first block this would be the second the first block this would be the second the first block this would be the second block this would be the third block and block this would be the third block and block this would be the third block and so on a cache then can be considered to so on a cache then can be considered to so on a cache then can be considered to be a number of slots where a block can be a number of slots where a block can be a number of slots where a block can fit in this case we have just a two fit in this case we have just a two fit in this case we have just a two block cache so the cache size is just block cache so the cache size is just block cache so the cache size is just equal to two block sizes so our memory equal to two block sizes so our memory equal to two block sizes so our memory has blocks of data and their cache has has blocks of data and their cache has has blocks of data and their cache has lines which are basically slots where a lines which are basically slots where a lines which are basically slots where a block can fit so we can say for example block can fit so we can say for example block can fit so we can say for example that block number zero can be fetched that block number zero can be fetched that block number zero can be fetched into the cache and placed in line number into the cache and placed in line number into the cache and placed in line number zero so when we want to make a precise zero so when we want to make a precise zero so when we want to make a precise distinction between the space in the distinction between the space in the distinction between the space in the cache where we can put a block and the cache where we can put a block and the cache where we can put a block and the actual memory content we put there the actual memory content we put there the actual memory content we put there the space in the cache is called a line and space in the cache is called a line and space in the cache is called a line and the stuff we put there is called a block the stuff we put there is called a block the stuff we put there is called a block the line size and the block size are the the line size and the block size are the the line size and the block size are the same so a line is effectively a slot same so a line is effectively a slot same so a line is effectively a slot where a block might fit

## Cache Line Sizes Quiz： Part 3
Kind: captions Language: en so now that we have seen what the cache so now that we have seen what the cache so now that we have seen what the cache line is and what its size does let's do line is and what its size does let's do line is and what its size does let's do a quiz about that so which of these are a quiz about that so which of these are a quiz about that so which of these are not good line sizes in a two kilobyte not good line sizes in a two kilobyte not good line sizes in a two kilobyte cache one byte 32 bytes 48 bytes 64 cache one byte 32 bytes 48 bytes 64 cache one byte 32 bytes 48 bytes 64 bytes and one kilobyte again we're bytes and one kilobyte again we're bytes and one kilobyte again we're looking for those that are not good line looking for those that are not good line looking for those that are not good line sizes in a cache of two kilobyte size

## Cache Line Sizes Quiz Solution： Part 3
Kind: captions Language: en let's look at the answer for our cache let's look at the answer for our cache let's look at the answer for our cache line size quiz we are asked to determine line size quiz we are asked to determine line size quiz we are asked to determine which of these are not good line sizes which of these are not good line sizes which of these are not good line sizes for a two kilobyte cache a one byte line for a two kilobyte cache a one byte line for a two kilobyte cache a one byte line is not a good one because it doesn't is not a good one because it doesn't is not a good one because it doesn't exploit spatial locality and because exploit spatial locality and because exploit spatial locality and because word size accesses we'll need to access word size accesses we'll need to access word size accesses we'll need to access multiple blocks or multiple lines in the multiple blocks or multiple lines in the multiple blocks or multiple lines in the cache so this is definitely not a good cache so this is definitely not a good cache so this is definitely not a good size 32 bytes is a good size it exploits size 32 bytes is a good size it exploits size 32 bytes is a good size it exploits spatial locality it's not too large and spatial locality it's not too large and spatial locality it's not too large and it's a power of two so we can easily it's a power of two so we can easily it's a power of two so we can easily locate blocks if I give you an address locate blocks if I give you an address locate blocks if I give you an address you can easily find what block does it you can easily find what block does it you can easily find what block does it belong to 48 bytes is a good size as far belong to 48 bytes is a good size as far belong to 48 bytes is a good size as far as it's not too large or too small but as it's not too large or too small but as it's not too large or too small but it's not a power of two so if we are it's not a power of two so if we are it's not a power of two so if we are given an address and we need to find out given an address and we need to find out given an address and we need to find out what is the block that contains that what is the block that contains that what is the block that contains that address we need to divide by 48 which is address we need to divide by 48 which is address we need to divide by 48 which is much more complicated than dividing by a much more complicated than dividing by a much more complicated than dividing by a power of two for example for a 32 byte power of two for example for a 32 byte power of two for example for a 32 byte size the lowest five bits of the address size the lowest five bits of the address size the lowest five bits of the address tell us where we are within the block tell us where we are within the block tell us where we are within the block this is kind of telling us where within this is kind of telling us where within this is kind of telling us where within the 32 bytes of the block we are and the the 32 bytes of the block we are and the the 32 bytes of the block we are and the upper bits of the address basically are upper bits of the address basically are upper bits of the address basically are the block number so we divide by 32 the block number so we divide by 32 the block number so we divide by 32 simply by discarding the lowermost simply by discarding the lowermost simply by discarding the lowermost 32 bytes in contrast dividing by 48 32 bytes in contrast dividing by 48 32 bytes in contrast dividing by 48 bytes would actually need to be a bytes would actually need to be a bytes would actually need to be a division by 48 so this is not a good division by 48 so this is not a good division by 48 so this is not a good line size 64 bytes is also a reasonable line size 64 bytes is also a reasonable line size 64 bytes is also a reasonable blocks as it's larger than 32 but still blocks as it's larger than 32 but still blocks as it's larger than 32 but still a lot of them fit in a two kilobyte a lot of them fit in a two kilobyte a lot of them fit in a two kilobyte cache one kilobyte is good because it's cache one kilobyte is good because it's cache one kilobyte is good because it's a power of two but only two of them fit a power of two but only two of them fit a power of two but only two of them fit in a two kilobyte cache so this is not a in a two kilobyte cache so this is not a in a two kilobyte cache so this is not a good line size because it is too large good line size because it is too large good line size because it is too large given that the cache is only two given that the cache is only two given that the cache is only two kilobytes in size what we want to have kilobytes in size what we want to have kilobytes in size what we want to have as a block size is something that is a as a block size is something that is a as a block size is something that is a power of two exploits spatial locality power of two exploits spatial locality power of two exploits spatial locality but still a significant number of them but still a significant number of them but still a significant number of them fit in our cache

## Block Offset and Number： Part 3
Kind: captions Language: en so let's talk about how we determine so let's talk about how we determine so let's talk about how we determine what the block offset is and what the what the block offset is and what the what the block offset is and what the block number is given an address let's block number is given an address let's block number is given an address let's say that the processor produces a 32-bit say that the processor produces a 32-bit say that the processor produces a 32-bit address with bits numbered from 0 address with bits numbered from 0 address with bits numbered from 0 through 31 so this is the address of the through 31 so this is the address of the through 31 so this is the address of the location the processor wants us to find location the processor wants us to find location the processor wants us to find in the cache the cache can be seen as an in the cache the cache can be seen as an in the cache the cache can be seen as an array of lines each block size in size array of lines each block size in size array of lines each block size in size in this example let's look at 16 as the in this example let's look at 16 as the in this example let's look at 16 as the block size some bits of this address block size some bits of this address block size some bits of this address tell us which block are we talking about tell us which block are we talking about tell us which block are we talking about and some bits are telling us we're in and some bits are telling us we're in and some bits are telling us we're in the block are we looking for our data the block are we looking for our data the block are we looking for our data with the block size of 16 we need to with the block size of 16 we need to with the block size of 16 we need to determine how many bits tell us where in determine how many bits tell us where in determine how many bits tell us where in the block we are the block has 16 bytes the block we are the block has 16 bytes the block we are the block has 16 bytes 2 to the 4th is 16 so we need 4 bits to 2 to the 4th is 16 so we need 4 bits to 2 to the 4th is 16 so we need 4 bits to tell us we're within a block we are that tell us we're within a block we are that tell us we're within a block we are that means that bits 0 through 3 are going to means that bits 0 through 3 are going to means that bits 0 through 3 are going to tell us once we find the block in the tell us once we find the block in the tell us once we find the block in the cache which part of the block to read cache which part of the block to read cache which part of the block to read the remaining bits bits four through 31 the remaining bits bits four through 31 the remaining bits bits four through 31 tell us which block are we trying to tell us which block are we trying to tell us which block are we trying to find so these bits that tell us where in find so these bits that tell us where in find so these bits that tell us where in the block are we going to find our data the block are we going to find our data the block are we going to find our data are called the block offset and the bits are called the block offset and the bits are called the block offset and the bits that tell us which block are we that tell us which block are we that tell us which block are we interested in are called the block interested in are called the block interested in are called the block number so what we do is we access the number so what we do is we access the number so what we do is we access the cache trying to find a block according cache trying to find a block according cache trying to find a block according to the block number if we find a block to the block number if we find a block to the block number if we find a block we use the block offset to get the right we use the block offset to get the right we use the block offset to get the right data from the block because there is data from the block because there is data from the block because there is more than one location in the block

## Block Number Quiz： Part 3
Kind: captions Language: en so let's see if we can put this to work so let's see if we can put this to work so let's see if we can put this to work in a quiz consider a cache with a 32 in a quiz consider a cache with a 32 in a quiz consider a cache with a 32 byte block size and the processor that byte block size and the processor that byte block size and the processor that creates 16-bit addresses if the address creates 16-bit addresses if the address creates 16-bit addresses if the address in binary is this there are sixteen bits in binary is this there are sixteen bits in binary is this there are sixteen bits here so if that is the address what is here so if that is the address what is here so if that is the address what is the block number that corresponds to the block number that corresponds to the block number that corresponds to this address and what is the block this address and what is the block this address and what is the block offset that corresponds to this address offset that corresponds to this address offset that corresponds to this address you can put binary numbers here because you can put binary numbers here because you can put binary numbers here because it's a lot easier that way

## Block Number Quiz Solution： Part 3
Kind: captions Language: en let's look at the solution to our block let's look at the solution to our block let's look at the solution to our block number quiz if we have a 32 byte block number quiz if we have a 32 byte block number quiz if we have a 32 byte block size and a 16-bit address like this the size and a 16-bit address like this the size and a 16-bit address like this the question is what is the block offset and question is what is the block offset and question is what is the block offset and the block number for a 32 byte block the the block number for a 32 byte block the the block number for a 32 byte block the block offset will determine where within block offset will determine where within block offset will determine where within the 32 bytes are we we need 5 bits to the 32 bytes are we we need 5 bits to the 32 bytes are we we need 5 bits to tell us that because 2 to the 5 is 32 so tell us that because 2 to the 5 is 32 so tell us that because 2 to the 5 is 32 so we need 5 bits to tell us which of the we need 5 bits to tell us which of the we need 5 bits to tell us which of the 32 bytes are we interested in accessing 32 bytes are we interested in accessing 32 bytes are we interested in accessing that means that the least significant 5 that means that the least significant 5 that means that the least significant 5 bits of this address are the block bits of this address are the block bits of this address are the block offset so the block offset is this the offset so the block offset is this the offset so the block offset is this the remaining bits of the address are the remaining bits of the address are the remaining bits of the address are the block number they are telling us which block number they are telling us which block number they are telling us which of the 32 byte blocks contains this of the 32 byte blocks contains this of the 32 byte blocks contains this address so this is our block number

## Cache Tags： Part 3
Kind: captions Language: en so now that we know how to find a block so now that we know how to find a block so now that we know how to find a block offset and the block number let's talk offset and the block number let's talk offset and the block number let's talk about the cache tags which is how we about the cache tags which is how we about the cache tags which is how we determine which block do we actually determine which block do we actually determine which block do we actually have in the cache so our cache can have in the cache so our cache can have in the cache so our cache can contain some number of lines in this contain some number of lines in this contain some number of lines in this case we have a four line cache we could case we have a four line cache we could case we have a four line cache we could have something like for example 64 bytes have something like for example 64 bytes have something like for example 64 bytes of data in each line let's say that any of data in each line let's say that any of data in each line let's say that any block from memory can be placed in any block from memory can be placed in any block from memory can be placed in any of these four lines in that case when we of these four lines in that case when we of these four lines in that case when we are given the address by the processor are given the address by the processor are given the address by the processor the least significant bits of the the least significant bits of the the least significant bits of the address will tell us where in the line address will tell us where in the line address will tell us where in the line we are after we find the line but each we are after we find the line but each we are after we find the line but each of these lines in the cache could of these lines in the cache could of these lines in the cache could contain any block number from the memory contain any block number from the memory contain any block number from the memory so the question is how do we find so the question is how do we find so the question is how do we find whether our access is actually a cache whether our access is actually a cache whether our access is actually a cache hit or not and the answer is in addition hit or not and the answer is in addition hit or not and the answer is in addition to the data of the blocks the cache also to the data of the blocks the cache also to the data of the blocks the cache also keeps a so called tag that tells it keeps a so called tag that tells it keeps a so called tag that tells it which block does it have here in this which block does it have here in this which block does it have here in this particular cache the tag will contain particular cache the tag will contain particular cache the tag will contain the block number that the cache has in the block number that the cache has in the block number that the cache has in each of these spots so we need four tags each of these spots so we need four tags each of these spots so we need four tags because there could be four blocks in because there could be four blocks in because there could be four blocks in this cache so given the address we take this cache so given the address we take this cache so given the address we take the block number part of the address and the block number part of the address and the block number part of the address and compare it to each of the tags we have compare it to each of the tags we have compare it to each of the tags we have in our cache if one of these says one in our cache if one of these says one in our cache if one of these says one that means that the tag matches the that means that the tag matches the that means that the tag matches the block number and that means that our block number and that means that our block number and that means that our data is in the corresponding line in the data is in the corresponding line in the data is in the corresponding line in the cache let's say that this is the cache let's say that this is the cache let's say that this is the situation we have so we have a tag match situation we have so we have a tag match situation we have so we have a tag match here that means that we have a hit and here that means that we have a hit and here that means that we have a hit and that our data is here in the cache now that our data is here in the cache now that our data is here in the cache now we can use the block offset to tell us we can use the block offset to tell us we can use the block offset to tell us where in that line our data is and we where in that line our data is and we where in that line our data is and we take the data and supply to the take the data and supply to the take the data and supply to the processor so in this particular cache we processor so in this particular cache we processor so in this particular cache we have the offset part of the address here have the offset part of the address here have the offset part of the address here and the block number is in this case and the block number is in this case and the block number is in this case called the tag part of the address when called the tag part of the address when called the tag part of the address when we have a cache miss we will bring the we have a cache miss we will bring the we have a cache miss we will bring the data here and we will put the block data here and we will put the block data here and we will put the block number in the corresponding tag so that number in the corresponding tag so that number in the corresponding tag so that we can find this block later we will see we can find this block later we will see we can find this block later we will see later that sometimes the tag part of the later that sometimes the tag part of the later that sometimes the tag part of the address address address is not exactly the same as the block is not exactly the same as the block is not exactly the same as the block number but in this cache it is

## Cache Tag Quiz： Part 3
Kind: captions Language: en let's check our knowledge of cach tags let's check our knowledge of cach tags let's check our knowledge of cach tags using a quiz which is always true about using a quiz which is always true about using a quiz which is always true about the cach tags it contains the entire the cach tags it contains the entire the cach tags it contains the entire address of the first bite of the block address of the first bite of the block address of the first bite of the block it contains at least one bit from the it contains at least one bit from the it contains at least one bit from the Block offset it contains at least one Block offset it contains at least one Block offset it contains at least one bit from the block number it contains bit from the block number it contains bit from the block number it contains some bits from the Block offset and some some bits from the Block offset and some some bits from the Block offset and some from the block number select all of from the block number select all of from the block number select all of those that the tag always has



---

# Playlist 2 Module 10 Raw Transcript

## Cache Tag Quiz Solution- Georgia Tech - HPCA： Part 3
Kind: captions Language: en let's look at the answer to our cache let's look at the answer to our cache let's look at the answer to our cache line quiz the cache tag always contains line quiz the cache tag always contains line quiz the cache tag always contains the entire address of the first byte we the entire address of the first byte we the entire address of the first byte we know this is not true the address that know this is not true the address that know this is not true the address that we supply to the cache on an Access we supply to the cache on an Access we supply to the cache on an Access contains the offset and the block number contains the offset and the block number contains the offset and the block number we have seen that the tag only needs to we have seen that the tag only needs to we have seen that the tag only needs to contain the block number part not the contain the block number part not the contain the block number part not the offset the block always begins at an offset the block always begins at an offset the block always begins at an aligned address so the first byte of the aligned address so the first byte of the aligned address so the first byte of the block actually always have zero here so block actually always have zero here so block actually always have zero here so there is no need to store those zeros in there is no need to store those zeros in there is no need to store those zeros in the cache as a tag the tag only needs to the cache as a tag the tag only needs to the cache as a tag the tag only needs to give us the bits that we need to give us the bits that we need to give us the bits that we need to identify the block the bits that we know identify the block the bits that we know identify the block the bits that we know are always zero are not necessary so are always zero are not necessary so are always zero are not necessary so this is definitely not true it contains this is definitely not true it contains this is definitely not true it contains at least one bit from the block offset at least one bit from the block offset at least one bit from the block offset again this is not true we need only the again this is not true we need only the again this is not true we need only the bits that tell us which block we have bits that tell us which block we have bits that tell us which block we have the block offset tells us where are we the block offset tells us where are we the block offset tells us where are we within a particular block so these bits within a particular block so these bits within a particular block so these bits are never necessary that also means that are never necessary that also means that are never necessary that also means that this is not true however the tag does this is not true however the tag does this is not true however the tag does contain at least one bit from the block contain at least one bit from the block contain at least one bit from the block number we have seen in our example that number we have seen in our example that number we have seen in our example that the entire block number can be a tag in the entire block number can be a tag in the entire block number can be a tag in that particular cache in general it that particular cache in general it that particular cache in general it might be fewer than all of the bits but might be fewer than all of the bits but might be fewer than all of the bits but there is at least one of them so this is there is at least one of them so this is there is at least one of them so this is the only correct answer

## Valid Bit： Part 3
Kind: captions Language: en another piece of information that the another piece of information that the another piece of information that the cache maintains about each of the blocks cache maintains about each of the blocks cache maintains about each of the blocks it has is the so-called valid bit if we it has is the so-called valid bit if we it has is the so-called valid bit if we look at the cache with four blocks of look at the cache with four blocks of look at the cache with four blocks of data and the tag that tells us which data and the tag that tells us which data and the tag that tells us which block we have in each of the lines the block we have in each of the lines the block we have in each of the lines the question is what happens when we turn on question is what happens when we turn on question is what happens when we turn on the processor at that time the cache the processor at that time the cache the processor at that time the cache contains no useful data but the data contains no useful data but the data contains no useful data but the data will have some bits here even if all of will have some bits here even if all of will have some bits here even if all of the bits are 0 we need to know that the bits are 0 we need to know that the bits are 0 we need to know that whatever address we produce it doesn't whatever address we produce it doesn't whatever address we produce it doesn't match any of these blocks here so that match any of these blocks here so that match any of these blocks here so that we need to access memory and bring we need to access memory and bring we need to access memory and bring actual data into the cache we do this by actual data into the cache we do this by actual data into the cache we do this by matching the tag but what does the tag matching the tag but what does the tag matching the tag but what does the tag initially have if the tag initially just initially have if the tag initially just initially have if the tag initially just has all zeros what if we are accessing has all zeros what if we are accessing has all zeros what if we are accessing address like this or it might happen address like this or it might happen address like this or it might happen that this address actually has a tag that this address actually has a tag that this address actually has a tag bits that correspond to 0 in that case bits that correspond to 0 in that case bits that correspond to 0 in that case the initial tag would match this and we the initial tag would match this and we the initial tag would match this and we would be accessing this garbage data in would be accessing this garbage data in would be accessing this garbage data in the cache which hasn't been brought from the cache which hasn't been brought from the cache which hasn't been brought from memory so we are really accessing memory so we are really accessing memory so we are really accessing something that is not equal to what we something that is not equal to what we something that is not equal to what we should be accessing note that any should be accessing note that any should be accessing note that any possible value for the tag might match possible value for the tag might match possible value for the tag might match the tag of an actual address so it is the tag of an actual address so it is the tag of an actual address so it is not that it is wrong to initialize to 0 not that it is wrong to initialize to 0 not that it is wrong to initialize to 0 it's that any particular initial value it's that any particular initial value it's that any particular initial value here would produce the same problem so here would produce the same problem so here would produce the same problem so what we do is we add an additional bit what we do is we add an additional bit what we do is we add an additional bit of state to the cache for each line that of state to the cache for each line that of state to the cache for each line that tells us whether the tag and the data tells us whether the tag and the data tells us whether the tag and the data are valid initially the valid bits need are valid initially the valid bits need are valid initially the valid bits need to be set to 0 which means that if our to be set to 0 which means that if our to be set to 0 which means that if our tag matches the address but the valid tag matches the address but the valid tag matches the address but the valid bit is 0 we treat that as not a hit so bit is 0 we treat that as not a hit so bit is 0 we treat that as not a hit so the hit condition is really the tag the hit condition is really the tag the hit condition is really the tag matches our block number and the valid matches our block number and the valid matches our block number and the valid bit is set to 1 by setting the valid bit is set to 1 by setting the valid bit is set to 1 by setting the valid bits to 0 initially we make sure that bits to 0 initially we make sure that bits to 0 initially we make sure that none of these can hit until real data is none of these can hit until real data is none of these can hit until real data is brought in and thus we also remove the brought in and thus we also remove the brought in and thus we also remove the problem of initializing the data and problem of initializing the data and problem of initializing the data and attack we can leave anything here so attack we can leave anything here so attack we can leave anything here so these don't need to be these don't need to be these don't need to be zero doubt as long as the valid bit is zero doubt as long as the valid bit is zero doubt as long as the valid bit is set to zero correctly so when we boot up set to zero correctly so when we boot up set to zero correctly so when we boot up the processor we set all the valid bits the processor we set all the valid bits the processor we set all the valid bits to zero and then when we bring stuff to zero and then when we bring stuff to zero and then when we bring stuff from memory put the data here set the from memory put the data here set the from memory put the data here set the tag to correspond to the blocks number tag to correspond to the blocks number tag to correspond to the blocks number and at that time we also set the valid and at that time we also set the valid and at that time we also set the valid bit to one

## Types of Caches： Part 3
Kind: captions Language: en there are several types of caches when there are several types of caches when there are several types of caches when it comes to which blocks can be placed it comes to which blocks can be placed it comes to which blocks can be placed where in the cache the cache examples we where in the cache the cache examples we where in the cache the cache examples we have seen so far are the fully have seen so far are the fully have seen so far are the fully associative caches in this type of a associative caches in this type of a associative caches in this type of a cache any block from memory can be cache any block from memory can be cache any block from memory can be placed into any line in the cache so if placed into any line in the cache so if placed into any line in the cache so if the cache has room for 16 lines then we the cache has room for 16 lines then we the cache has room for 16 lines then we need to search up to 16 tags in order to need to search up to 16 tags in order to need to search up to 16 tags in order to find whether the block we are looking find whether the block we are looking find whether the block we are looking for is actually in the cache the for is actually in the cache the for is actually in the cache the opposite of that is a direct mapped opposite of that is a direct mapped opposite of that is a direct mapped cache where for a given block there is cache where for a given block there is cache where for a given block there is exactly one place in the cache where exactly one place in the cache where exactly one place in the cache where that block can go that means that we that block can go that means that we that block can go that means that we only need to check one line in the cache only need to check one line in the cache only need to check one line in the cache if it contains that block because that's if it contains that block because that's if it contains that block because that's the only line where it can possibly be the only line where it can possibly be the only line where it can possibly be different blocks of course we'll still different blocks of course we'll still different blocks of course we'll still be able to map to different lines but a be able to map to different lines but a be able to map to different lines but a particular block can only go into one particular block can only go into one particular block can only go into one place in the cache and then of course place in the cache and then of course place in the cache and then of course there is the middle ground between them there is the middle ground between them there is the middle ground between them which is called a set associative cache which is called a set associative cache which is called a set associative cache where there are n lines where a block where there are n lines where a block where there are n lines where a block can be now n typically will be larger can be now n typically will be larger can be now n typically will be larger than one and smaller than the total than one and smaller than the total than one and smaller than the total number of lines in the cache so we can number of lines in the cache so we can number of lines in the cache so we can really view the direct mapped cache as a really view the direct mapped cache as a really view the direct mapped cache as a case of a set associative cache where n case of a set associative cache where n case of a set associative cache where n is one and the fully associative cache is one and the fully associative cache is one and the fully associative cache is an extreme case of a set associative is an extreme case of a set associative is an extreme case of a set associative cache where n is equal to the number of cache where n is equal to the number of cache where n is equal to the number of lines in the cache but what we normally lines in the cache but what we normally lines in the cache but what we normally call a set associative cache has a call a set associative cache has a call a set associative cache has a relatively small and compared to the relatively small and compared to the relatively small and compared to the number of blocks in that cache yet that number of blocks in that cache yet that number of blocks in that cache yet that and typically is 2 4 or 8 so it's larger and typically is 2 4 or 8 so it's larger and typically is 2 4 or 8 so it's larger than 1 now let's look at each of these than 1 now let's look at each of these than 1 now let's look at each of these types of caches

## Direct Mapped Cache- Georgia Tech - HPCA： Part 3
Kind: captions Language: en we have seen a glimpse of fully we have seen a glimpse of fully we have seen a glimpse of fully associative caches so now let's go from associative caches so now let's go from associative caches so now let's go from the other direction which is let's first the other direction which is let's first the other direction which is let's first look at direct mat caches if this is the look at direct mat caches if this is the look at direct mat caches if this is the memory and it contains blocks 0 1 2 3 4 memory and it contains blocks 0 1 2 3 4 memory and it contains blocks 0 1 2 3 4 5 6 Etc and these are the block numbers 5 6 Etc and these are the block numbers 5 6 Etc and these are the block numbers not the addresses so I'm assuming that not the addresses so I'm assuming that not the addresses so I'm assuming that this thing here is an entire block worth this thing here is an entire block worth this thing here is an entire block worth of memory which contains multiple of memory which contains multiple of memory which contains multiple locations and let's say we have a cache locations and let's say we have a cache locations and let's say we have a cache with room for four blocks we canot with room for four blocks we canot with room for four blocks we canot number the lines 0 1 2 and three and in number the lines 0 1 2 and three and in number the lines 0 1 2 and three and in a direct map cache block zero if it's in a direct map cache block zero if it's in a direct map cache block zero if it's in the cach at all needs to be in line the cach at all needs to be in line the cach at all needs to be in line number zero block one if it's in the number zero block one if it's in the number zero block one if it's in the cach at all will be in slot number one cach at all will be in slot number one cach at all will be in slot number one block number two if it is in the cache block number two if it is in the cache block number two if it is in the cache at all has to be in this place here at all has to be in this place here at all has to be in this place here block number three if it is in the cash block number three if it is in the cash block number three if it is in the cash at all has to be here and now block at all has to be here and now block at all has to be here and now block number four will again get to map to number four will again get to map to number four will again get to map to line number zero block number five maps line number zero block number five maps line number zero block number five maps to line number one block number six maps to line number one block number six maps to line number one block number six maps to line number two Etc so pretty much to line number two Etc so pretty much to line number two Etc so pretty much for any given block address there is for any given block address there is for any given block address there is only one place in the cache where that only one place in the cache where that only one place in the cache where that address can be if we look at a memory address can be if we look at a memory address can be if we look at a memory address the least significant bits are address the least significant bits are address the least significant bits are still the block offset they tell us still the block offset they tell us still the block offset they tell us where in the block are we assuming that where in the block are we assuming that where in the block are we assuming that we find the block the block number now we find the block the block number now we find the block the block number now contains some bits that tell us where in contains some bits that tell us where in contains some bits that tell us where in the cache this block can be the index the cache this block can be the index the cache this block can be the index bits need to be sufficient to tell us bits need to be sufficient to tell us bits need to be sufficient to tell us which of the lines in the cache are we which of the lines in the cache are we which of the lines in the cache are we talking about so if there are four lines talking about so if there are four lines talking about so if there are four lines in the cache then we need two index bits in the cache then we need two index bits in the cache then we need two index bits to tell us where a block can map and to tell us where a block can map and to tell us where a block can map and that is why in the block that is why in the block that is why in the block number it was 0 1 2 3 0 1 2 3 Etc it was number it was 0 1 2 3 0 1 2 3 Etc it was number it was 0 1 2 3 0 1 2 3 Etc it was determined by the lowermost bits of the determined by the lowermost bits of the determined by the lowermost bits of the block number and the remaining bits of block number and the remaining bits of block number and the remaining bits of the the address are now the tag now the the address are now the tag now the the address are now the tag now there is an interesting question of why there is an interesting question of why there is an interesting question of why the tag does not include the full block the tag does not include the full block the tag does not include the full block number so why doesn't the tag cover also number so why doesn't the tag cover also number so why doesn't the tag cover also the index bits and the answer to this is the index bits and the answer to this is the index bits and the answer to this is that the tag needs to tell us which of that the tag needs to tell us which of that the tag needs to tell us which of the blocks do we have in this place and the blocks do we have in this place and the blocks do we have in this place and the answer is that the tag needs to the answer is that the tag needs to the answer is that the tag needs to identify which block we have in the identify which block we have in the identify which block we have in the cache considering that the block with cache considering that the block with cache considering that the block with some index bits has to be in one place some index bits has to be in one place some index bits has to be in one place in the cache the tag for that place in in the cache the tag for that place in in the cache the tag for that place in the cache doesn't need to tell us what the cache doesn't need to tell us what the cache doesn't need to tell us what the index bits are by looking at this the index bits are by looking at this the index bits are by looking at this particular line we have already particular line we have already particular line we have already determined that these index bits have to determined that these index bits have to determined that these index bits have to be the ones that correspond to that line be the ones that correspond to that line be the ones that correspond to that line so all we have to do is tell us which of so all we have to do is tell us which of so all we have to do is tell us which of the lines that can possibly be here do the lines that can possibly be here do the lines that can possibly be here do we really have and for that we only need we really have and for that we only need we really have and for that we only need these bits because the index bits are these bits because the index bits are these bits because the index bits are pretty much predetermined once we are pretty much predetermined once we are pretty much predetermined once we are already looking here in other words if already looking here in other words if already looking here in other words if we stored the index bits in the tag all we stored the index bits in the tag all we stored the index bits in the tag all of the tags that we could possibly put of the tags that we could possibly put of the tags that we could possibly put here would have the same index bits here would have the same index bits here would have the same index bits which means we don't have to store them which means we don't have to store them which means we don't have to store them because we already know them

## Direct Mapped Cache Pros and Cons： Part 3
Kind: captions Language: en so let's look at the upsides and so let's look at the upsides and so let's look at the upsides and downsides of direct mapped cache --is downsides of direct mapped cache --is downsides of direct mapped cache --is when we access the direct mapped cache when we access the direct mapped cache when we access the direct mapped cache we need to look in only one place that we need to look in only one place that we need to look in only one place that makes this cache fast if we need to look makes this cache fast if we need to look makes this cache fast if we need to look in more than one place then we need to in more than one place then we need to in more than one place then we need to wait for all of those to be read out we wait for all of those to be read out we wait for all of those to be read out we don't need to figure out which one of don't need to figure out which one of don't need to figure out which one of them if any has it and so on with a them if any has it and so on with a them if any has it and so on with a direct mapped cache we just check in one direct mapped cache we just check in one direct mapped cache we just check in one place if it's there we have it if it's place if it's there we have it if it's place if it's there we have it if it's not there then it's a Miss so we can not there then it's a Miss so we can not there then it's a Miss so we can very quickly determine whether we have a very quickly determine whether we have a very quickly determine whether we have a hit or not so it's very good for hit hit or not so it's very good for hit hit or not so it's very good for hit time it is also cheaper than more time it is also cheaper than more time it is also cheaper than more complex caches because of it we only complex caches because of it we only complex caches because of it we only need to do one comparison on every need to do one comparison on every need to do one comparison on every access to the cache so we only need to access to the cache so we only need to access to the cache so we only need to have one tagged comparator one valid bit have one tagged comparator one valid bit have one tagged comparator one valid bit checker and so on and that also makes it checker and so on and that also makes it checker and so on and that also makes it energy-efficient we only need to do one energy-efficient we only need to do one energy-efficient we only need to do one tag comparison and one valid bit check tag comparison and one valid bit check tag comparison and one valid bit check per access which spends less energy than per access which spends less energy than per access which spends less energy than if we had to check more than one the if we had to check more than one the if we had to check more than one the downsides of this cache are related to downsides of this cache are related to downsides of this cache are related to the fact that the block must go in one the fact that the block must go in one the fact that the block must go in one place to see why this is a problem place to see why this is a problem place to see why this is a problem consider what happens if the processor consider what happens if the processor consider what happens if the processor is frequently accesses blocks a and B is frequently accesses blocks a and B is frequently accesses blocks a and B like this where both a and B map to the like this where both a and B map to the like this where both a and B map to the same place in the cache when we access a same place in the cache when we access a same place in the cache when we access a we will bring it to the cache when we we will bring it to the cache when we we will bring it to the cache when we access B we need to put it in the same access B we need to put it in the same access B we need to put it in the same place where a is currently so a needs to place where a is currently so a needs to place where a is currently so a needs to be kicked out there is no choice about be kicked out there is no choice about be kicked out there is no choice about what we kick out a has to go out when we what we kick out a has to go out when we what we kick out a has to go out when we access a again B needs to be kicked out access a again B needs to be kicked out access a again B needs to be kicked out and so on so the downside of this cache and so on so the downside of this cache and so on so the downside of this cache is that it can have these conflicts is that it can have these conflicts is that it can have these conflicts where particular blocks that we want to where particular blocks that we want to where particular blocks that we want to use fight over the one spot in the cache use fight over the one spot in the cache use fight over the one spot in the cache although the cache has plenty of other although the cache has plenty of other although the cache has plenty of other spots that are underutilized what spots that are underutilized what spots that are underutilized what conflicts do is they increase the miss conflicts do is they increase the miss conflicts do is they increase the miss rate so we can have a cache that has a rate so we can have a cache that has a rate so we can have a cache that has a fast hit time but suffers in the Miss fast hit time but suffers in the Miss fast hit time but suffers in the Miss rate in return

## Direct Mapped Cache Quiz： Part 3
Kind: captions Language: en let's check our knowledge of direct let's check our knowledge of direct let's check our knowledge of direct mapped cache is and how they handle mapped cache is and how they handle mapped cache is and how they handle conflicts let's consider a 16 kilobyte conflicts let's consider a 16 kilobyte conflicts let's consider a 16 kilobyte direct mapped cache with 256 byte blocks direct mapped cache with 256 byte blocks direct mapped cache with 256 byte blocks the question is which of these addresses the question is which of these addresses the question is which of these addresses conflict with one two three four five conflict with one two three four five conflict with one two three four five six seven hacks the first choice is 1 2 six seven hacks the first choice is 1 2 six seven hacks the first choice is 1 2 3 4 5 6 7 7 the next stretch is 1 1 3 3 3 4 5 6 7 7 the next stretch is 1 1 3 3 3 4 5 6 7 7 the next stretch is 1 1 3 3 5 5 7 7 the next choice is 1 1 1 1 5 6 7 5 5 7 7 the next choice is 1 1 1 1 5 6 7 5 5 7 7 the next choice is 1 1 1 1 5 6 7 8 and the last choice is 1 2 3 4 1 6 6 6 8 and the last choice is 1 2 3 4 1 6 6 6 8 and the last choice is 1 2 3 4 1 6 6 6 so which of these will conflict with so which of these will conflict with so which of these will conflict with this address in a 16 kilobyte direct this address in a 16 kilobyte direct this address in a 16 kilobyte direct mapped cache with 256 byte blocks

## Direct Mapped Cache Quiz Solution- Georgia Tech - HPCA： Part 3
Kind: captions Language: en the key to solving problems like this is the key to solving problems like this is the key to solving problems like this is to figure out how does the address break to figure out how does the address break to figure out how does the address break down into offset index and tag bits down into offset index and tag bits down into offset index and tag bits conflicts will occur where different conflicts will occur where different conflicts will occur where different blocks have the same index bits in this blocks have the same index bits in this blocks have the same index bits in this particular cache 256 byte block size particular cache 256 byte block size particular cache 256 byte block size means that there are 8 bits to the block means that there are 8 bits to the block means that there are 8 bits to the block offset in hex conveniently that means offset in hex conveniently that means offset in hex conveniently that means that the least significant two digits that the least significant two digits that the least significant two digits are the offset and we can safely ignore are the offset and we can safely ignore are the offset and we can safely ignore those because they are not part of the those because they are not part of the those because they are not part of the index now we need to figure out how many index now we need to figure out how many index now we need to figure out how many bits are in the index we have 16 bits are in the index we have 16 bits are in the index we have 16 kilobytes and 256 byte blocks the number kilobytes and 256 byte blocks the number kilobytes and 256 byte blocks the number of blocks in the cache is this divided of blocks in the cache is this divided of blocks in the cache is this divided by this and that gives us 64 blocks 64 by this and that gives us 64 blocks 64 by this and that gives us 64 blocks 64 is two to the sixth so we need the next is two to the sixth so we need the next is two to the sixth so we need the next six bits here to tell us what the index six bits here to tell us what the index six bits here to tell us what the index is the six bits will be contained in the is the six bits will be contained in the is the six bits will be contained in the next two hex digits so 456 hex we can next two hex digits so 456 hex we can next two hex digits so 456 hex we can write it out as binary this is 5 this is write it out as binary this is 5 this is write it out as binary this is 5 this is 6 and we are interested in these bits 6 and we are interested in these bits 6 and we are interested in these bits here so we are really looking for which here so we are really looking for which here so we are really looking for which blocks of these have the same bits here blocks of these have the same bits here blocks of these have the same bits here those that have 56 definitely will have those that have 56 definitely will have those that have 56 definitely will have the same index bits so we will the same index bits so we will the same index bits so we will definitely have a conflict here and here definitely have a conflict here and here definitely have a conflict here and here possibly but in order to actually have a possibly but in order to actually have a possibly but in order to actually have a conflict it needs to be a different conflict it needs to be a different conflict it needs to be a different block this address here does not block this address here does not block this address here does not conflict with this one here because they conflict with this one here because they conflict with this one here because they are actually in the same block they have are actually in the same block they have are actually in the same block they have the same block number so this is not a the same block number so this is not a the same block number so this is not a conflict they do map to the same place conflict they do map to the same place conflict they do map to the same place in the cache but they are actually the in the cache but they are actually the in the cache but they are actually the same block so it's not a conflict among same block so it's not a conflict among same block so it's not a conflict among blocks this however is a different block blocks this however is a different block blocks this however is a different block number that definitely maps to the same number that definitely maps to the same number that definitely maps to the same index so this is a conflict 5/5 looks index so this is a conflict 5/5 looks index so this is a conflict 5/5 looks like this these are the six bits they're like this these are the six bits they're like this these are the six bits they're not the same so this block will not not the same so this block will not not the same so this block will not conflict with the one here finally 1/6 conflict with the one here finally 1/6 conflict with the one here finally 1/6 looks like this these are the bits that looks like this these are the bits that looks like this these are the bits that we are interested in and they do indeed we are interested in and they do indeed we are interested in and they do indeed match the ones of this block so this match the ones of this block so this match the ones of this block so this goes to the same place in the cache as goes to the same place in the cache as goes to the same place in the cache as this and it's a different block this and it's a different block this and it's a different block this is one two three four one six and this is one two three four one six and this is one two three four one six and this is one two three four five six so this is one two three four five six so this is one two three four five six so this will also conflict with our block

## Direct Mapped Cache Quiz： Part 3
Kind: captions Language: en Let's do an example of direct map caches Let's do an example of direct map caches Let's do an example of direct map caches and how they're accessed here is a and how they're accessed here is a and how they're accessed here is a direct map cache with eight lines Each direct map cache with eight lines Each direct map cache with eight lines Each of which is 32 bytes in size the of which is 32 bytes in size the of which is 32 bytes in size the processor produces the following processor produces the following processor produces the following sequence of accesses one at a time 3 F1 sequence of accesses one at a time 3 F1 sequence of accesses one at a time 3 F1 F hex 3 F2 fhx 3 F2 e hex and 3 E1 fhx F hex 3 F2 fhx 3 F2 e hex and 3 E1 fhx F hex 3 F2 fhx 3 F2 e hex and 3 E1 fhx in this order the question for you is in this order the question for you is in this order the question for you is what is the content of the cache after what is the content of the cache after what is the content of the cache after these four accesses

## Direct Mapped Cache Quiz Solution 2 HPCA Part 3
Kind: captions Language: en let's look at the solution for our let's look at the solution for our let's look at the solution for our direct mapped cache access quiz we have direct mapped cache access quiz we have direct mapped cache access quiz we have these four accesses in this cache the these four accesses in this cache the these four accesses in this cache the cache block is 32 bytes and there are cache block is 32 bytes and there are cache block is 32 bytes and there are eight blocks in the cache so the address eight blocks in the cache so the address eight blocks in the cache so the address break down will be the offset bits will break down will be the offset bits will break down will be the offset bits will be 5 because we need 5 bits to tell us be 5 because we need 5 bits to tell us be 5 because we need 5 bits to tell us which of the 32 bytes we are talking which of the 32 bytes we are talking which of the 32 bytes we are talking about the index bits will be 3 because about the index bits will be 3 because about the index bits will be 3 because we need 3 bits to tell us which of the we need 3 bits to tell us which of the we need 3 bits to tell us which of the eight slots in the cache to use and the eight slots in the cache to use and the eight slots in the cache to use and the remaining bits are going to be the tag remaining bits are going to be the tag remaining bits are going to be the tag now let's consider which of these map now let's consider which of these map now let's consider which of these map we're in the cache we're in the cache we're in the cache as you can see the least significant 8 as you can see the least significant 8 as you can see the least significant 8 bits are going to determine where in the bits are going to determine where in the bits are going to determine where in the cache something goes so we can cache something goes so we can cache something goes so we can conveniently look at just the least conveniently look at just the least conveniently look at just the least significant two hex digits one F is 0 0 significant two hex digits one F is 0 0 significant two hex digits one F is 0 0 0 1 1 1 1 1 5 bits are the offset and 0 0 1 1 1 1 1 5 bits are the offset and 0 0 1 1 1 1 1 5 bits are the offset and 0 0 0 is the index so this goes here 3 F 2 0 0 is the index so this goes here 3 F 2 0 0 is the index so this goes here 3 F 2 F has 0 0 1 0 here and then 1 1 1 1 1 F has 0 0 1 0 here and then 1 1 1 1 1 F has 0 0 1 0 here and then 1 1 1 1 1 but it goes to index 1 3 F 2 e has to but it goes to index 1 3 F 2 e has to but it goes to index 1 3 F 2 e has to and then e it also maps here because it and then e it also maps here because it and then e it also maps here because it is actually the same block as this one is actually the same block as this one is actually the same block as this one they will both be in this slot here they will both be in this slot here they will both be in this slot here because really the entire block 3 F 2 because really the entire block 3 F 2 because really the entire block 3 F 2 something is there and finally 3 e 1 F something is there and finally 3 e 1 F something is there and finally 3 e 1 F is going to map to 0 0 1 F so the index is going to map to 0 0 1 F so the index is going to map to 0 0 1 F so the index is 0 and it's a different block from the is 0 and it's a different block from the is 0 and it's a different block from the one we had previously so this one will one we had previously so this one will one we had previously so this one will be replaced by this so the final content be replaced by this so the final content be replaced by this so the final content of the cache really is that the block 3 of the cache really is that the block 3 of the cache really is that the block 3 e 1 something is here and 3 F 2 e 1 something is here and 3 F 2 e 1 something is here and 3 F 2 something is here

## Set Associative Caches HPCA Part 3
Kind: captions Language: en Now let us look at set associative Now let us look at set associative Now let us look at set associative caches. We say that a cache is nway set caches. We say that a cache is nway set caches. We say that a cache is nway set associative when a particular block can associative when a particular block can associative when a particular block can be in one of n possible lines where it be in one of n possible lines where it be in one of n possible lines where it can go. So the overall cache now is can go. So the overall cache now is can go. So the overall cache now is divided into regions called sets. In divided into regions called sets. In divided into regions called sets. In this case we have four sets. A given this case we have four sets. A given this case we have four sets. A given block can go to one of its sets. So block can go to one of its sets. So block can go to one of its sets. So pretty much we use some bits of the pretty much we use some bits of the pretty much we use some bits of the block's address to tell us which set block's address to tell us which set block's address to tell us which set does it need to go in within a set. does it need to go in within a set. does it need to go in within a set. There could be a number of lines that There could be a number of lines that There could be a number of lines that can contain a block. In this case, we can contain a block. In this case, we can contain a block. In this case, we have a two-way set associative cache have a two-way set associative cache have a two-way set associative cache because there are two lines in a set. So because there are two lines in a set. So because there are two lines in a set. So a given block could be in either of the a given block could be in either of the a given block could be in either of the two lines. Note that different blocks two lines. Note that different blocks two lines. Note that different blocks might go to different sets. But a might go to different sets. But a might go to different sets. But a particular block will map to one set and particular block will map to one set and particular block will map to one set and then it has a choice between the two then it has a choice between the two then it has a choice between the two lines there. In this particular cache, lines there. In this particular cache, lines there. In this particular cache, we have eight lines, four sets, and two we have eight lines, four sets, and two we have eight lines, four sets, and two ways. Note that n is two here. So we say ways. Note that n is two here. So we say ways. Note that n is two here. So we say that the cache is two-way set that the cache is two-way set that the cache is two-way set associative not when it has two sets but associative not when it has two sets but associative not when it has two sets but when there is two blocks in each

## Offset, Index, Tag for Set Associative HPCA Part 3
Kind: captions Language: en so now let's look at how do we form the so now let's look at how do we form the so now let's look at how do we form the offset index and tag in the address for offset index and tag in the address for offset index and tag in the address for set associative caches this is a two-way set associative caches this is a two-way set associative caches this is a two-way set associative cache that has four sets set associative cache that has four sets set associative cache that has four sets when the processor produces an address when the processor produces an address when the processor produces an address the least significant bits that tell us the least significant bits that tell us the least significant bits that tell us where in the block we are are still where in the block we are are still where in the block we are are still determined by the block size the index determined by the block size the index determined by the block size the index bits are the ones that tell us which set bits are the ones that tell us which set bits are the ones that tell us which set are we talking about so the number of are we talking about so the number of are we talking about so the number of index bits is determined by how many index bits is determined by how many index bits is determined by how many sets we have in this case we have four sets we have in this case we have four sets we have in this case we have four sets so we need two index bits the sets so we need two index bits the sets so we need two index bits the remaining bits are the Tag like in a remaining bits are the Tag like in a remaining bits are the Tag like in a direct map cache once we have placed a direct map cache once we have placed a direct map cache once we have placed a line in for example set zero we know line in for example set zero we know line in for example set zero we know that everything that maps to set zero that everything that maps to set zero that everything that maps to set zero will have bits 0 0 here so we don't have will have bits 0 0 here so we don't have will have bits 0 0 here so we don't have to keep that in the tag in set zero we to keep that in the tag in set zero we to keep that in the tag in set zero we only need to make sure that what we now only need to make sure that what we now only need to make sure that what we now need matches what we have in the need matches what we have in the need matches what we have in the uppermost bits not in the index bits uppermost bits not in the index bits uppermost bits not in the index bits because the fact that we are looking at because the fact that we are looking at because the fact that we are looking at set zero means that we have already set zero means that we have already set zero means that we have already determined that these bits are 0 0 and determined that these bits are 0 0 and determined that these bits are 0 0 and everything that we could have placed everything that we could have placed everything that we could have placed here also has z0 bits in the index here also has z0 bits in the index here also has z0 bits in the index interestingly a direct map cache of the interestingly a direct map cache of the interestingly a direct map cache of the same size would have one more index bit same size would have one more index bit same size would have one more index bit so it would have slightly smaller tag

## 2 Way Set Associative Quiz HPCA Part 3
Kind: captions Language: en now let's do a quiz with the 2-way now let's do a quiz with the 2-way now let's do a quiz with the 2-way set-associative cache let's say we have set-associative cache let's say we have set-associative cache let's say we have a cache with 32 byte block size and 4 a cache with 32 byte block size and 4 a cache with 32 byte block size and 4 sets with 2 blocks each so that's why sets with 2 blocks each so that's why sets with 2 blocks each so that's why it's 2-way set-associative every set has it's 2-way set-associative every set has it's 2-way set-associative every set has two ways if the processor does these two ways if the processor does these two ways if the processor does these accesses in this order what is the final accesses in this order what is the final accesses in this order what is the final content of this cache

## 2 Way Set Associative Quiz Solution HPCA Part 3
Kind: captions Language: en Let's look at the solution to our Let's look at the solution to our Let's look at the solution to our two-way set associative cache quiz. As two-way set associative cache quiz. As two-way set associative cache quiz. As before, the answer to this type of before, the answer to this type of before, the answer to this type of question requires us to figure out which question requires us to figure out which question requires us to figure out which bits in the address determine what in bits in the address determine what in bits in the address determine what in the cache. So the address will be broken the cache. So the address will be broken the cache. So the address will be broken into offset in this case five bits, into offset in this case five bits, into offset in this case five bits, index, in this case two bits to tell us index, in this case two bits to tell us index, in this case two bits to tell us which set we have and the tag. Seven which set we have and the tag. Seven which set we have and the tag. Seven bits are used by the index and the bits are used by the index and the bits are used by the index and the offset which means the index will be offset which means the index will be offset which means the index will be determined only by the least significant determined only by the least significant determined only by the least significant two digits. So now we can just figure two digits. So now we can just figure two digits. So now we can just figure out that 0000 3 is like this. Five bits out that 0000 3 is like this. Five bits out that 0000 3 is like this. Five bits are the offset. The next two bits are are the offset. The next two bits are are the offset. The next two bits are the index. So this maps to set zero and the index. So this maps to set zero and the index. So this maps to set zero and we will put F3 we will put F3 we will put F3 there. Next one is F503. there. Next one is F503. there. Next one is F503. It maps to the same set but this set now It maps to the same set but this set now It maps to the same set but this set now has two places. So we can actually put has two places. So we can actually put has two places. So we can actually put F503 F503 F503 in the other line in that set. Next we in the other line in that set. Next we in the other line in that set. Next we have F563 have F563 have F563 like this. This maps to set number like this. This maps to set number like this. This maps to set number three. And finally EF63 three. And finally EF63 three. And finally EF63 is going to map to the same set and is going to map to the same set and is going to map to the same set and we'll put it here. So, as you can see, we'll put it here. So, as you can see, we'll put it here. So, as you can see, with a two-way set associative cache, we with a two-way set associative cache, we with a two-way set associative cache, we can put more than one block that maps to can put more than one block that maps to can put more than one block that maps to the same place in the cache and not have the same place in the cache and not have the same place in the cache and not have conflicts. This is why we want to have a conflicts. This is why we want to have a conflicts. This is why we want to have a two-way cache or more because it reduces two-way cache or more because it reduces two-way cache or more because it reduces the number of conflicts. However, it the number of conflicts. However, it the number of conflicts. However, it complicates the tag checks because now complicates the tag checks because now complicates the tag checks because now when we're looking for something, we when we're looking for something, we when we're looking for something, we have two places to search before we are have two places to search before we are have two places to search before we are sure whether it's there or not.

## Fully Associative Cache HPCA Part 3
Kind: captions Language: en and finally let's look at a fully and finally let's look at a fully and finally let's look at a fully associative cache if we have an eight associative cache if we have an eight associative cache if we have an eight entry cache a fully associative cache is entry cache a fully associative cache is entry cache a fully associative cache is one where any block can map to any of one where any block can map to any of one where any block can map to any of these lines in this case the address these lines in this case the address these lines in this case the address breaks down into the offset part the breaks down into the offset part the breaks down into the offset part the number of index bits is the number of number of index bits is the number of number of index bits is the number of bits we need to tell us where in the bits we need to tell us where in the bits we need to tell us where in the cash we can be but because we can be cash we can be but because we can be cash we can be but because we can be anywhere in the cash we don't need any anywhere in the cash we don't need any anywhere in the cash we don't need any bits to tell us that so there are no bits to tell us that so there are no bits to tell us that so there are no index bits when using a fully index bits when using a fully index bits when using a fully associative cache all of the remaining associative cache all of the remaining associative cache all of the remaining bits are simply the tag

## Direct Mapped and Fully Associative HPCA Part 3
Kind: captions Language: en as we already said before direct map and as we already said before direct map and as we already said before direct map and fully associative caches can be fully associative caches can be fully associative caches can be considered to be just special cases or considered to be just special cases or considered to be just special cases or set associative caches so for example a set associative caches so for example a set associative caches so for example a direct map cache is actually a one-way direct map cache is actually a one-way direct map cache is actually a one-way set associative cache and a fully set associative cache and a fully set associative cache and a fully associative cache is an N wasted associative cache is an N wasted associative cache is an N wasted associative cache where n is equal to associative cache where n is equal to associative cache where n is equal to the number of lines for all these caches the number of lines for all these caches the number of lines for all these caches the address that the processor supplies the address that the processor supplies the address that the processor supplies is broken into the offset index and tag is broken into the offset index and tag is broken into the offset index and tag components the number of offset bits is components the number of offset bits is components the number of offset bits is the number of bits that we need to tell the number of bits that we need to tell the number of bits that we need to tell us where in the block size we are so us where in the block size we are so us where in the block size we are so it's log to a block size the number of it's log to a block size the number of it's log to a block size the number of index bits is log 2 of the number of index bits is log 2 of the number of index bits is log 2 of the number of sets we have in a direct mapped cache sets we have in a direct mapped cache sets we have in a direct mapped cache that is the number of blocks we have in that is the number of blocks we have in that is the number of blocks we have in a fully associative cache that's 1 so a fully associative cache that's 1 so a fully associative cache that's 1 so log 2 of that is 0 and we have no index log 2 of that is 0 and we have no index log 2 of that is 0 and we have no index bits and the tag bits are the remaining bits and the tag bits are the remaining bits and the tag bits are the remaining bits when you're trying to figure out bits when you're trying to figure out bits when you're trying to figure out which bits are the index bits we which bits are the index bits we which bits are the index bits we actually need to know which bits are the actually need to know which bits are the actually need to know which bits are the offset bits too because knowing that we offset bits too because knowing that we offset bits too because knowing that we have for example 3 index bits doesn't have for example 3 index bits doesn't have for example 3 index bits doesn't really tell us whether they're here or really tell us whether they're here or really tell us whether they're here or here or here that depends on the offset here or here that depends on the offset here or here that depends on the offset bits so make sure that when you are bits so make sure that when you are bits so make sure that when you are determining these bits you do it in that determining these bits you do it in that determining these bits you do it in that order first figure out how many offset order first figure out how many offset order first figure out how many offset bits then the next so many bits are the bits then the next so many bits are the bits then the next so many bits are the index bits and then the rest is the tag



---

# Playlist 2 Module 11 Raw Transcript

## Cache Replacement HPCA Part 3
Kind: captions Language: en now that we know how caches find data now that we know how caches find data now that we know how caches find data when we are looking for it let's talk when we are looking for it let's talk when we are looking for it let's talk about what happens when we need to about what happens when we need to about what happens when we need to replace something from the cache to make replace something from the cache to make replace something from the cache to make room for new data because we have a miss room for new data because we have a miss room for new data because we have a miss so the situation when we need so the situation when we need so the situation when we need replacement is the set where the data replacement is the set where the data replacement is the set where the data needs to go is full we have a miss so we needs to go is full we have a miss so we needs to go is full we have a miss so we need somewhere to put the new block that need somewhere to put the new block that need somewhere to put the new block that we're bringing in and we need to put it we're bringing in and we need to put it we're bringing in and we need to put it in that set and the question for cach in that set and the question for cach in that set and the question for cach replacement is which of the blocks in replacement is which of the blocks in replacement is which of the blocks in the set do we kick out to make room for the set do we kick out to make room for the set do we kick out to make room for the new one possible replacement the new one possible replacement the new one possible replacement policies are random just pick a block policies are random just pick a block policies are random just pick a block randomly from those that are already in randomly from those that are already in randomly from those that are already in the set fifo which kicks out the block the set fifo which kicks out the block the set fifo which kicks out the block that has been there the longest so in that has been there the longest so in that has been there the longest so in fifo let's say we have two blocks we fifo let's say we have two blocks we fifo let's say we have two blocks we bring in a then we bring B when we want bring in a then we bring B when we want bring in a then we bring B when we want to bring in C we're going to kick out a to bring in C we're going to kick out a to bring in C we're going to kick out a because it's been there the longest then because it's been there the longest then because it's been there the longest then we're going to kick out B to bring we're going to kick out B to bring we're going to kick out B to bring and so on the next policy is lru or and so on the next policy is lru or and so on the next policy is lru or least recently used we want to kick out least recently used we want to kick out least recently used we want to kick out the block that has not been used the the block that has not been used the the block that has not been used the longest so we definitely don't want to longest so we definitely don't want to longest so we definitely don't want to kick out the block that is the most kick out the block that is the most kick out the block that is the most recently used and then the next most recently used and then the next most recently used and then the next most recently used and so on so pretty much recently used and so on so pretty much recently used and so on so pretty much we track which block has been used more we track which block has been used more we track which block has been used more recently and when we need to replace recently and when we need to replace recently and when we need to replace something we choose the block that we something we choose the block that we something we choose the block that we haven't used for the longest time it haven't used for the longest time it haven't used for the longest time it turns out that lru is a really good turns out that lru is a really good turns out that lru is a really good policy so there are several policies policy so there are several policies policy so there are several policies that are trying to approximate it that are trying to approximate it that are trying to approximate it because this is not easy to do one such because this is not easy to do one such because this is not easy to do one such policy for example is NMU or not most policy for example is NMU or not most policy for example is NMU or not most recently used which tracks just which recently used which tracks just which recently used which tracks just which block has been used the most recently block has been used the most recently block has been used the most recently and then picks randomly among the and then picks randomly among the and then picks randomly among the remaining blocks so pretty much don't remaining blocks so pretty much don't remaining blocks so pretty much don't pick the block that was just used

## Implementing LRU HPCA Part 3
Kind: captions Language: en so how do we implement the least so how do we implement the least so how do we implement the least recently used policy that works really recently used policy that works really recently used policy that works really well and it works really well because it well and it works really well because it well and it works really well because it exploits locality the most recently used exploits locality the most recently used exploits locality the most recently used thing is likely to be used very soon thing is likely to be used very soon thing is likely to be used very soon afterwards the thing that we haven't afterwards the thing that we haven't afterwards the thing that we haven't used for a while means that there is used for a while means that there is used for a while means that there is less reason for locality to kind of less reason for locality to kind of less reason for locality to kind of cause it to be accessed again let's say cause it to be accessed again let's say cause it to be accessed again let's say we have a four-way set assertive cache we have a four-way set assertive cache we have a four-way set assertive cache and let's just track what's happening in and let's just track what's happening in and let's just track what's happening in this one set so there is four blocks in this one set so there is four blocks in this one set so there is four blocks in it for each of these blocks we keep the it for each of these blocks we keep the it for each of these blocks we keep the tag and the valid bit and in order to tag and the valid bit and in order to tag and the valid bit and in order to track which block was accessed to when track which block was accessed to when track which block was accessed to when we're going to also have an lru counter we're going to also have an lru counter we're going to also have an lru counter for each block these counters take for each block these counters take for each block these counters take values that correspond to the size of values that correspond to the size of values that correspond to the size of the set in this case 0 through three the set in this case 0 through three the set in this case 0 through three because there are four things in the set because there are four things in the set because there are four things in the set so let's initialize them to 0 1 2 and so let's initialize them to 0 1 2 and so let's initialize them to 0 1 2 and three they have to all be different all three they have to all be different all three they have to all be different all the time and we will see how that is the time and we will see how that is the time and we will see how that is maintained so we start like this when we maintained so we start like this when we maintained so we start like this when we want to replace something want to replace something want to replace something we will replace the block whose counter we will replace the block whose counter we will replace the block whose counter is currently zero because that's going is currently zero because that's going is currently zero because that's going to be the least recently used block so to be the least recently used block so to be the least recently used block so let's say that we access some block a let's say that we access some block a let's say that we access some block a and we need to put it somewhere we will and we need to put it somewhere we will and we need to put it somewhere we will choose the place with a zero counter and choose the place with a zero counter and choose the place with a zero counter and put it there now that the block is put it there now that the block is put it there now that the block is placed there the processor accesses it placed there the processor accesses it placed there the processor accesses it and suddenly this becomes the most and suddenly this becomes the most and suddenly this becomes the most recently used block so its counter now recently used block so its counter now recently used block so its counter now needs to be three in order to make sure needs to be three in order to make sure needs to be three in order to make sure that all of the counter still have that all of the counter still have that all of the counter still have different values that means that all of different values that means that all of different values that means that all of the other counters now need to be the other counters now need to be the other counters now need to be decremented so the one that was one decremented so the one that was one decremented so the one that was one becomes zero two becomes one and three becomes zero two becomes one and three becomes zero two becomes one and three becomes two and now let's say that we becomes two and now let's say that we becomes two and now let's say that we access some Block B we put it here access some Block B we put it here access some Block B we put it here because the counter is zero now here because the counter is zero now here because the counter is zero now here when B is accessed after putting it here when B is accessed after putting it here when B is accessed after putting it here that counter becomes three this one that counter becomes three this one that counter becomes three this one becomes two this one becomes zero and becomes two this one becomes zero and becomes two this one becomes zero and this one becomes one we will now put this one becomes one we will now put this one becomes one we will now put some Block C here that becomes three some Block C here that becomes three some Block C here that becomes three three this becomes two this becomes one three this becomes two this becomes one three this becomes two this becomes one this becomes Zero D 3 2 1 0 now we're this becomes Zero D 3 2 1 0 now we're this becomes Zero D 3 2 1 0 now we're back to this situation so I'm going to back to this situation so I'm going to back to this situation so I'm going to clean up here and now if we access e clean up here and now if we access e clean up here and now if we access e we're going to find what was the least we're going to find what was the least we're going to find what was the least recently used block and a at this point recently used block and a at this point recently used block and a at this point is because it's been accessed the least is because it's been accessed the least is because it's been accessed the least recently so a will be replaced by E and recently so a will be replaced by E and recently so a will be replaced by E and the lru counter here becomes three which the lru counter here becomes three which the lru counter here becomes three which brings these one notch lower so this is brings these one notch lower so this is brings these one notch lower so this is the state we have now let's say that b the state we have now let's say that b the state we have now let's say that b which is the least recently used block which is the least recently used block which is the least recently used block now is re accessed what happens then is now is re accessed what happens then is now is re accessed what happens then is this counter needs to become three all this counter needs to become three all this counter needs to become three all of the other ones now need to be changed of the other ones now need to be changed of the other ones now need to be changed lower what happens if the most recently lower what happens if the most recently lower what happens if the most recently block let's say B is accessed again then block let's say B is accessed again then block let's say B is accessed again then this counter just stays at three because this counter just stays at three because this counter just stays at three because that indicates that this is already the that indicates that this is already the that indicates that this is already the most recently access block what happens most recently access block what happens most recently access block what happens if block D which is neither the most nor if block D which is neither the most nor if block D which is neither the most nor the least recent is accessed well this the least recent is accessed well this the least recent is accessed well this counter needs to become three and now we counter needs to become three and now we counter needs to become three and now we have an interesting situation we don't have an interesting situation we don't have an interesting situation we don't just lower all the other counters a just lower all the other counters a just lower all the other counters a notch down we take the ones that were notch down we take the ones that were notch down we take the ones that were above the original value of this counter above the original value of this counter above the original value of this counter so we take the counter that is above one so we take the counter that is above one so we take the counter that is above one and those are the ones that get a notch and those are the ones that get a notch and those are the ones that get a notch down but the ones that are below one down but the ones that are below one down but the ones that are below one stay the same that ensures that the stay the same that ensures that the stay the same that ensures that the counters still have different values counters still have different values counters still have different values let's do another one of those where let's do another one of those where let's do another one of those where let's say Block B that is almost the let's say Block B that is almost the let's say Block B that is almost the most recently used block is accessed in most recently used block is accessed in most recently used block is accessed in that case this counter needs to become that case this counter needs to become that case this counter needs to become three all counters that were above its three all counters that were above its three all counters that were above its original value of two need to be original value of two need to be original value of two need to be decremented so this becomes two but decremented so this becomes two but decremented so this becomes two but these two stay the same because they're these two stay the same because they're these two stay the same because they're below two so again we have this below two so again we have this below two so again we have this situation so now you know how to situation so now you know how to situation so now you know how to maintain the lru counters properly maintain the lru counters properly maintain the lru counters properly after every step you can after every step you can after every step you can verify that the counters have different verify that the counters have different verify that the counters have different values if they don't then you probably values if they don't then you probably values if they don't then you probably made a mistake somewhere so as you can made a mistake somewhere so as you can made a mistake somewhere so as you can see maintaining lru is relatively see maintaining lru is relatively see maintaining lru is relatively complicated for an nway set asso of complicated for an nway set asso of complicated for an nway set asso of cache we need n counters whose size is cache we need n counters whose size is cache we need n counters whose size is log two of n in this case the size of log two of n in this case the size of log two of n in this case the size of the counters is to bit because they need the counters is to bit because they need the counters is to bit because they need to have a number between 0 and three to to have a number between 0 and three to to have a number between 0 and three to tell us where are we in the order of tell us where are we in the order of tell us where are we in the order of axis in this set so it's a fourway set axis in this set so it's a fourway set axis in this set so it's a fourway set assive cash that means we need two bits assive cash that means we need two bits assive cash that means we need two bits per counter and four counters so for per counter and four counters so for per counter and four counters so for highly associative caches something like highly associative caches something like highly associative caches something like 32 way associative you would need to 32 way associative you would need to 32 way associative you would need to have five bit counters and 32 of them have five bit counters and 32 of them have five bit counters and 32 of them per set so this was the cost it's pretty per set so this was the cost it's pretty per set so this was the cost it's pretty high worse there is an energy problem we high worse there is an energy problem we high worse there is an energy problem we need to modify up to end counters on need to modify up to end counters on need to modify up to end counters on each access even cash hits that can each access even cash hits that can each access even cash hits that can happen very frequently so the lru happen very frequently so the lru happen very frequently so the lru approximations try to keep fewer approximations try to keep fewer approximations try to keep fewer counters and do fewer updates on cash counters and do fewer updates on cash counters and do fewer updates on cash hits

## LRU Quiz HPCA Part 3
Kind: captions Language: en now that we have seen how LRU works now that we have seen how LRU works now that we have seen how LRU works let's do a quiz about it let's say that let's do a quiz about it let's say that let's do a quiz about it let's say that this is a single set in a cache that is this is a single set in a cache that is this is a single set in a cache that is a 2-way set-associative so there are a 2-way set-associative so there are a 2-way set-associative so there are eight lines in the set let's say that eight lines in the set let's say that eight lines in the set let's say that the blocks we have our a b c d e f g and the blocks we have our a b c d e f g and the blocks we have our a b c d e f g and h and let's say that the LRU counters h and let's say that the LRU counters h and let's say that the LRU counters that correspond to these are like this that correspond to these are like this that correspond to these are like this where zero is the least recently used where zero is the least recently used where zero is the least recently used what is the new content of the cache what is the new content of the cache what is the new content of the cache after we perform accesses to a then B after we perform accesses to a then B after we perform accesses to a then B then a again then D then K you only need then a again then D then K you only need then a again then D then K you only need to say which blocks are in each line in to say which blocks are in each line in to say which blocks are in each line in the cache not what they're LRU counters the cache not what they're LRU counters the cache not what they're LRU counters are but you don't need to track the LRU are but you don't need to track the LRU are but you don't need to track the LRU countries in order to arrive at the countries in order to arrive at the countries in order to arrive at the correct answer

## LRU Quiz Solution HPCA Part 3
Kind: captions Language: en let's look at the solution to our LRU let's look at the solution to our LRU let's look at the solution to our LRU quiz we had this situation in the cache quiz we had this situation in the cache quiz we had this situation in the cache and these counters before these accesses and these counters before these accesses and these counters before these accesses and now we're performing accesses to a B and now we're performing accesses to a B and now we're performing accesses to a B a D and K a is the most recently used a D and K a is the most recently used a D and K a is the most recently used block weary access it none of the block weary access it none of the block weary access it none of the counters change we access B B's counter counters change we access B B's counter counters change we access B B's counter becomes 7 from 3 that means that all the becomes 7 from 3 that means that all the becomes 7 from 3 that means that all the counters above 3 will be decremented 2 counters above 3 will be decremented 2 counters above 3 will be decremented 2 stays the same one stays the same and stays the same one stays the same and stays the same one stays the same and zero stays the same so these are the new zero stays the same so these are the new zero stays the same so these are the new counters after B is accessed next we counters after B is accessed next we counters after B is accessed next we access a again this counter becomes 7 access a again this counter becomes 7 access a again this counter becomes 7 all of the counters above 6 are all of the counters above 6 are all of the counters above 6 are decremented so this is really the only decremented so this is really the only decremented so this is really the only change now D is accessed this counter change now D is accessed this counter change now D is accessed this counter becomes 7 those above 5 are decremented becomes 7 those above 5 are decremented becomes 7 those above 5 are decremented the remaining ones stay the same all of the remaining ones stay the same all of the remaining ones stay the same all of these have been hit so far so nothing these have been hit so far so nothing these have been hit so far so nothing has changed in the cache we still have has changed in the cache we still have has changed in the cache we still have exactly the same stuff there the only exactly the same stuff there the only exactly the same stuff there the only change will be when we access K which is change will be when we access K which is change will be when we access K which is a Miss so now we need to choose who goes a Miss so now we need to choose who goes a Miss so now we need to choose who goes out and the one that goes out is the one out and the one that goes out is the one out and the one that goes out is the one whose counter is zero so H is replaced whose counter is zero so H is replaced whose counter is zero so H is replaced by K the other blocks remain the same as by K the other blocks remain the same as by K the other blocks remain the same as they were and this is the final answer they were and this is the final answer they were and this is the final answer for this quiz

## Write Policy HPCA Part 3
Kind: captions Language: en the final aspect of how the caches work the final aspect of how the caches work the final aspect of how the caches work concerns the right policy there are concerns the right policy there are concerns the right policy there are really two parts of it first is the so really two parts of it first is the so really two parts of it first is the so called allocate policy which is about do called allocate policy which is about do called allocate policy which is about do we insert into the cash meaning allocate we insert into the cash meaning allocate we insert into the cash meaning allocate and entering the cash for blogs that we and entering the cash for blogs that we and entering the cash for blogs that we write so we have a write miss the write so we have a write miss the write so we have a write miss the question is does the block get brought question is does the block get brought question is does the block get brought to the cash or not there are two types to the cash or not there are two types to the cash or not there are two types of caches the first type of cash is of caches the first type of cash is of caches the first type of cash is according to this is a write allocate according to this is a write allocate according to this is a write allocate cash it brings the block that we write cash it brings the block that we write cash it brings the block that we write into the cash into the cash into the cash the second type does not so when we have the second type does not so when we have the second type does not so when we have a read miss the block is brought into a read miss the block is brought into a read miss the block is brought into the cash but if we have a write miss the the cash but if we have a write miss the the cash but if we have a write miss the block is not brought into the cash most block is not brought into the cash most block is not brought into the cash most cash is today our write allocate simply cash is today our write allocate simply cash is today our write allocate simply because there is some locality between because there is some locality between because there is some locality between reads and writes meaning if we write to reads and writes meaning if we write to reads and writes meaning if we write to something we are also likely to read it something we are also likely to read it something we are also likely to read it so write allocate helps us have read so write allocate helps us have read so write allocate helps us have read hits even if we had a write miss the hits even if we had a write miss the hits even if we had a write miss the other aspect of the right policy is when other aspect of the right policy is when other aspect of the right policy is when we have a write do we write just to the we have a write do we write just to the we have a write do we write just to the cash or also to the memory we can have a cash or also to the memory we can have a cash or also to the memory we can have a write through cache which updates the write through cache which updates the write through cache which updates the memory immediately so pretty much when a memory immediately so pretty much when a memory immediately so pretty much when a write happens we write to the cache and write happens we write to the cache and write happens we write to the cache and we just propagate the write also to we just propagate the write also to we just propagate the write also to memory that's why it's called right memory that's why it's called right memory that's why it's called right through because the right kind of goes through because the right kind of goes through because the right kind of goes through the cache or we can have a write through the cache or we can have a write through the cache or we can have a write back cache where we only write to the back cache where we only write to the back cache where we only write to the cache and we write to the memory only cache and we write to the memory only cache and we write to the memory only when the block is replaced from the when the block is replaced from the when the block is replaced from the cache we cannot just discard the block cache we cannot just discard the block cache we cannot just discard the block at this time because the memory does not at this time because the memory does not at this time because the memory does not have the most recent version of the have the most recent version of the have the most recent version of the block so the memory has to be updated block so the memory has to be updated block so the memory has to be updated once the block is no longer in the cache once the block is no longer in the cache once the block is no longer in the cache but as long as it's in the cache we will but as long as it's in the cache we will but as long as it's in the cache we will just keep writing to the cache and not just keep writing to the cache and not just keep writing to the cache and not keep memory up-to-date right through is keep memory up-to-date right through is keep memory up-to-date right through is a very unpopular cache for caches that a very unpopular cache for caches that a very unpopular cache for caches that send stuff to memory directly we usually send stuff to memory directly we usually send stuff to memory directly we usually want to have a write back cache because want to have a write back cache because want to have a write back cache because that way writes which have a lot of that way writes which have a lot of that way writes which have a lot of locality will update the cache possibly locality will update the cache possibly locality will update the cache possibly many times and only send the write to many times and only send the write to many times and only send the write to memory once once the block is replaced memory once once the block is replaced memory once once the block is replaced so we can have many rights and only one so we can have many rights and only one so we can have many rights and only one memory right for that memory right for that memory right for that that prevents the memory from being that prevents the memory from being that prevents the memory from being overwhelmed with the number of rights overwhelmed with the number of rights overwhelmed with the number of rights that we might have there is also some that we might have there is also some that we might have there is also some relationship between this choice and relationship between this choice and relationship between this choice and this if you have a write-back cache you this if you have a write-back cache you this if you have a write-back cache you want to also have a right allocate cache want to also have a right allocate cache want to also have a right allocate cache because you're saving rights to memory because you're saving rights to memory because you're saving rights to memory by writing only to the cache if you have by writing only to the cache if you have by writing only to the cache if you have a right miss you want to have future a right miss you want to have future a right miss you want to have future rights go to the cache to do right rights go to the cache to do right rights go to the cache to do right allocate

## Write Back Caches HPCA Part 3
Kind: captions Language: en now let's talk about what happens in a now let's talk about what happens in a now let's talk about what happens in a write-back cache right through is fairly write-back cache right through is fairly write-back cache right through is fairly simple we just send rights to memory and simple we just send rights to memory and simple we just send rights to memory and the cache works just as we described it the cache works just as we described it the cache works just as we described it before but for right back caches we can before but for right back caches we can before but for right back caches we can have a block that we did right since we have a block that we did right since we have a block that we did right since we lost brought it from the memory in that lost brought it from the memory in that lost brought it from the memory in that case when we replace the block we need case when we replace the block we need case when we replace the block we need to write it to memory but we can have a to write it to memory but we can have a to write it to memory but we can have a block that we didn't write since we lost block that we didn't write since we lost block that we didn't write since we lost brought it from memory that block has brought it from memory that block has brought it from memory that block has only been read since we brought it in in only been read since we brought it in in only been read since we brought it in in that case when we replace there is no that case when we replace there is no that case when we replace there is no need to write that block back to memory need to write that block back to memory need to write that block back to memory but how do we know which one of these we but how do we know which one of these we but how do we know which one of these we have one choice that we might make is have one choice that we might make is have one choice that we might make is that we don't want to know we'll just that we don't want to know we'll just that we don't want to know we'll just write every time that we replace the write every time that we replace the write every time that we replace the block unfortunately there is a lot of block unfortunately there is a lot of block unfortunately there is a lot of read-only stuff that we will now end up read-only stuff that we will now end up read-only stuff that we will now end up writing to memory over and over although writing to memory over and over although writing to memory over and over although it's unnecessary so instead we choose to it's unnecessary so instead we choose to it's unnecessary so instead we choose to add a dirty bit to every block in the add a dirty bit to every block in the add a dirty bit to every block in the cache the dirty bit says whether we have cache the dirty bit says whether we have cache the dirty bit says whether we have written the block since it was placed in written the block since it was placed in written the block since it was placed in the cache or not so if the dirty bit is the cache or not so if the dirty bit is the cache or not so if the dirty bit is zero it means that the block is clean or zero it means that the block is clean or zero it means that the block is clean or it was not written since it was last it was not written since it was last it was not written since it was last brought from memory or we can have the brought from memory or we can have the brought from memory or we can have the dirty bit big one in which case we say dirty bit big one in which case we say dirty bit big one in which case we say that the block is dirty or it was that the block is dirty or it was that the block is dirty or it was written since we last brought it from written since we last brought it from written since we last brought it from memory and that means that we need to memory and that means that we need to memory and that means that we need to write this block back to memory on write this block back to memory on write this block back to memory on replacement when this block is kicked replacement when this block is kicked replacement when this block is kicked out of the cache it needs to be written out of the cache it needs to be written out of the cache it needs to be written to memory if it's clean then we don't to memory if it's clean then we don't to memory if it's clean then we don't have to write it to memory

## Write Back Cache Example HPCA Part 3
Kind: captions Language: en so let's look at an example of how a so let's look at an example of how a so let's look at an example of how a right back cache would work say we have right back cache would work say we have right back cache would work say we have a small for entry cache that is direct a small for entry cache that is direct a small for entry cache that is direct map each line has a valid bit a tag and map each line has a valid bit a tag and map each line has a valid bit a tag and a dirty bit we don't need the lru a dirty bit we don't need the lru a dirty bit we don't need the lru counter because in a direct map cache we counter because in a direct map cache we counter because in a direct map cache we know exactly what to replace we don't know exactly what to replace we don't know exactly what to replace we don't need L counters to tell us that let's need L counters to tell us that let's need L counters to tell us that let's say that the processor is doing this say that the processor is doing this say that the processor is doing this sequence of accesses and a b and c map sequence of accesses and a b and c map sequence of accesses and a b and c map to different sets in the cache the first to different sets in the cache the first to different sets in the cache the first ACC is Right a so what we do is we have ACC is Right a so what we do is we have ACC is Right a so what we do is we have a miss all of our valid bits are zero so a miss all of our valid bits are zero so a miss all of our valid bits are zero so it doesn't matter what the rest of the it doesn't matter what the rest of the it doesn't matter what the rest of the cache has because none of the lines are cache has because none of the lines are cache has because none of the lines are valid when we have the first right to a valid when we have the first right to a valid when we have the first right to a we will bring a here we will change the we will bring a here we will change the we will bring a here we will change the valid bit to one and we would change the valid bit to one and we would change the valid bit to one and we would change the tag to correspond to a because this is a tag to correspond to a because this is a tag to correspond to a because this is a right the processor as soon as the data right the processor as soon as the data right the processor as soon as the data is brought in will write it and the is brought in will write it and the is brought in will write it and the Dirty Bit will be set to one next we Dirty Bit will be set to one next we Dirty Bit will be set to one next we have a read to a the processor checks have a read to a the processor checks have a read to a the processor checks the tag and the valid bit sees that the the tag and the valid bit sees that the the tag and the valid bit sees that the tag match is a and the valid bit is one tag match is a and the valid bit is one tag match is a and the valid bit is one so it can just use a the fact that the so it can just use a the fact that the so it can just use a the fact that the Dirty Bit is one doesn't change anything Dirty Bit is one doesn't change anything Dirty Bit is one doesn't change anything next let's say we have a read B we don't next let's say we have a read B we don't next let's say we have a read B we don't find anything that matches B in the find anything that matches B in the find anything that matches B in the cache or maybe if this does the valid cache or maybe if this does the valid cache or maybe if this does the valid bit is zero so it doesn't matter so we bit is zero so it doesn't matter so we bit is zero so it doesn't matter so we will have a miss and bring B into the will have a miss and bring B into the will have a miss and bring B into the cache change the tag to correspond to B cache change the tag to correspond to B cache change the tag to correspond to B change the valid bit to one to indicate change the valid bit to one to indicate change the valid bit to one to indicate that we do have something here now and that we do have something here now and that we do have something here now and because this is only a read the Dirty because this is only a read the Dirty because this is only a read the Dirty Bit will be zero next let's say we read Bit will be zero next let's say we read Bit will be zero next let's say we read C it's a Miss bring C in change the tag C it's a Miss bring C in change the tag C it's a Miss bring C in change the tag on the valid bit it's a read so the on the valid bit it's a read so the on the valid bit it's a read so the Dirty Bit starts out at zero next what Dirty Bit starts out at zero next what Dirty Bit starts out at zero next what we have is a right to see we check if we have is a right to see we check if we have is a right to see we check if it's a hit and it is we write only to it's a hit and it is we write only to it's a hit and it is we write only to the cache and change the Dirty Bit to the cache and change the Dirty Bit to the cache and change the Dirty Bit to one and now as you can see the Dirty Bit one and now as you can see the Dirty Bit one and now as you can see the Dirty Bit for every line indicates simply was the for every line indicates simply was the for every line indicates simply was the line ever written since it was last line ever written since it was last line ever written since it was last brought into the cache let's continue brought into the cache let's continue brought into the cache let's continue this by an access to e that is a read this by an access to e that is a read this by an access to e that is a read and let's say that e Maps where a is so and let's say that e Maps where a is so and let's say that e Maps where a is so now what needs to happen is we're going now what needs to happen is we're going now what needs to happen is we're going to check here to see whether the tag to check here to see whether the tag to check here to see whether the tag matches e it doesn't so we have a miss matches e it doesn't so we have a miss matches e it doesn't so we have a miss our replacement policy is simple for our replacement policy is simple for our replacement policy is simple for direct map caches we simply replace direct map caches we simply replace direct map caches we simply replace whatever is there so at this point we whatever is there so at this point we whatever is there so at this point we cannot just bring in E from memory first cannot just bring in E from memory first cannot just bring in E from memory first we have to take the data for a that we we have to take the data for a that we we have to take the data for a that we have written and not written to memory have written and not written to memory have written and not written to memory yet because the Dirty Bit is one this yet because the Dirty Bit is one this yet because the Dirty Bit is one this block gets sent to memory as a right block gets sent to memory as a right block gets sent to memory as a right after that happens we can bring e in set after that happens we can bring e in set after that happens we can bring e in set the tag and valid bit and the Dirty Bit the tag and valid bit and the Dirty Bit the tag and valid bit and the Dirty Bit to zero because it's a read and this to zero because it's a read and this to zero because it's a read and this continues on let's say that now we have continues on let's say that now we have continues on let's say that now we have a read to F which replaces B when we a read to F which replaces B when we a read to F which replaces B when we want to replace B we check the Dirty Bit want to replace B we check the Dirty Bit want to replace B we check the Dirty Bit it's zero so B doesn't get sent to it's zero so B doesn't get sent to it's zero so B doesn't get sent to memory it simply gets overwritten with memory it simply gets overwritten with memory it simply gets overwritten with the data from F we Fetch and because the data from F we Fetch and because the data from F we Fetch and because this is a read we get a zero here and so this is a read we get a zero here and so this is a read we get a zero here and so on

## Write Back Cache Quiz HPCA Part 3
Kind: captions Language: en now it's your turn let's do a quiz on now it's your turn let's do a quiz on now it's your turn let's do a quiz on right back caches suppose we have a right back caches suppose we have a right back caches suppose we have a direct map cache and this is one entry direct map cache and this is one entry direct map cache and this is one entry in that cache and all of the accesses we in that cache and all of the accesses we in that cache and all of the accesses we will do will map to that one entry the will do will map to that one entry the will do will map to that one entry the valid bit starts out at zero the dirty valid bit starts out at zero the dirty valid bit starts out at zero the dirty bit starts out at one and the tag starts bit starts out at one and the tag starts bit starts out at one and the tag starts out at a the processor does the out at a the processor does the out at a the processor does the following sequence of accesses it reads following sequence of accesses it reads following sequence of accesses it reads a reads be writes be read C reads the a reads be writes be read C reads the a reads be writes be read C reads the writes D and all of these ABC and D map writes D and all of these ABC and D map writes D and all of these ABC and D map to the same place in the cache the to the same place in the cache the to the same place in the cache the question for you is after these accesses question for you is after these accesses question for you is after these accesses what is the new state of the cache also what is the new state of the cache also what is the new state of the cache also you need to determine how many cache you need to determine how many cache you need to determine how many cache misses do we have in this sequence of misses do we have in this sequence of misses do we have in this sequence of accesses and how many write backs to accesses and how many write backs to accesses and how many write backs to memory will happen during this sequence

## Write Back Cache Quiz Solution HPCA Part 3
Kind: captions Language: en let's look at the solution to a let's look at the solution to a let's look at the solution to a write-back cache quiz when we have read write-back cache quiz when we have read write-back cache quiz when we have read a we're going to check the tag and the a we're going to check the tag and the a we're going to check the tag and the valid bit the tag matches a but the valid bit the tag matches a but the valid bit the tag matches a but the valid bit is zero so this is a Miss so valid bit is zero so this is a Miss so valid bit is zero so this is a Miss so now we had one miss we will make the now we had one miss we will make the now we had one miss we will make the valid bit be one the tag becomes a this valid bit be one the tag becomes a this valid bit be one the tag becomes a this is a read so the dirty bit is zero note is a read so the dirty bit is zero note is a read so the dirty bit is zero note that the fact that the dirty bit was one that the fact that the dirty bit was one that the fact that the dirty bit was one doesn't matter here because this was not doesn't matter here because this was not doesn't matter here because this was not valid so simply it doesn't matter what's valid so simply it doesn't matter what's valid so simply it doesn't matter what's here here here if valid bit is zero for that line next if valid bit is zero for that line next if valid bit is zero for that line next we read B we check the tag and the valid we read B we check the tag and the valid we read B we check the tag and the valid bit the tag is a mismatch so this is a bit the tag is a mismatch so this is a bit the tag is a mismatch so this is a Miss we have a Miss we need to kick out Miss we have a Miss we need to kick out Miss we have a Miss we need to kick out a four that we check the dirty bit it's a four that we check the dirty bit it's a four that we check the dirty bit it's zero so we don't have a right back we zero so we don't have a right back we zero so we don't have a right back we replace a with B here the valid bit is 1 replace a with B here the valid bit is 1 replace a with B here the valid bit is 1 the dirty bit is zero because this is a the dirty bit is zero because this is a the dirty bit is zero because this is a read next we have a right to be check read next we have a right to be check read next we have a right to be check the tag and the valid bit this is a hit the tag and the valid bit this is a hit the tag and the valid bit this is a hit so it's not a miss because this is a so it's not a miss because this is a so it's not a miss because this is a right we set the dirty bit next we read right we set the dirty bit next we read right we set the dirty bit next we read C check the tag mismatch this is a mess C check the tag mismatch this is a mess C check the tag mismatch this is a mess we need to replace B so we check is it we need to replace B so we check is it we need to replace B so we check is it dirty yes it is we need to write back B dirty yes it is we need to write back B dirty yes it is we need to write back B to memory then we can bring in C set its to memory then we can bring in C set its to memory then we can bring in C set its valid bit to 1 and because this is a valid bit to 1 and because this is a valid bit to 1 and because this is a read the dirty bit becomes zero read the dirty bit becomes zero read the dirty bit becomes zero next we read D it's a Miss we need to next we read D it's a Miss we need to next we read D it's a Miss we need to kick out C C is clean so we don't have a kick out C C is clean so we don't have a kick out C C is clean so we don't have a right back right back right back we just bring in the an override C in we just bring in the an override C in we just bring in the an override C in the cache the cache the cache it's a read so the dirty bit is zero and it's a read so the dirty bit is zero and it's a read so the dirty bit is zero and the valid bit is 1 because the tag is the valid bit is 1 because the tag is the valid bit is 1 because the tag is valid next we have a right to D it's a valid next we have a right to D it's a valid next we have a right to D it's a hit but the dirty bit changes to 1 so hit but the dirty bit changes to 1 so hit but the dirty bit changes to 1 so overall we had one right back and for overall we had one right back and for overall we had one right back and for misses and the state of the cache is misses and the state of the cache is misses and the state of the cache is that valid bit is 1 the dirty bit is 1 that valid bit is 1 the dirty bit is 1 that valid bit is 1 the dirty bit is 1 and the tag corresponds to D

## Cache Summary Part 1 HPCA Part 3
Kind: captions Language: en so now that we have seen multiple so now that we have seen multiple so now that we have seen multiple aspects of how caches work let's aspects of how caches work let's aspects of how caches work let's summarize them for a more realistic summarize them for a more realistic summarize them for a more realistic cache let's say we have a 4 kilobyte cache let's say we have a 4 kilobyte cache let's say we have a 4 kilobyte four-way set associative cache with a 64 four-way set associative cache with a 64 four-way set associative cache with a 64 byte line size with a right back and byte line size with a right back and byte line size with a right back and right allocate right policies the right allocate right policies the right allocate right policies the processor let's say creates 64-bit processor let's say creates 64-bit processor let's say creates 64-bit addresses to access the cash when addresses to access the cash when addresses to access the cash when accessing this cache the 64-bit address accessing this cache the 64-bit address accessing this cache the 64-bit address will be divided into offset bits index will be divided into offset bits index will be divided into offset bits index bits and tag bits first for offset bits bits and tag bits first for offset bits bits and tag bits first for offset bits it's a 64 byte line so we need six bits it's a 64 byte line so we need six bits it's a 64 byte line so we need six bits bits 0 through five there are six of bits 0 through five there are six of bits 0 through five there are six of them are going to tell us where in the them are going to tell us where in the them are going to tell us where in the cache block we are next we need to cache block we are next we need to cache block we are next we need to determine how many sets this cache has determine how many sets this cache has determine how many sets this cache has it's a 4 kilobyte cache that's 2 to the it's a 4 kilobyte cache that's 2 to the it's a 4 kilobyte cache that's 2 to the 12 bytes the number of blocks is that 12 bytes the number of blocks is that 12 bytes the number of blocks is that divided by 64 4 which is 2 to the 6 so divided by 64 4 which is 2 to the 6 so divided by 64 4 which is 2 to the 6 so we have 2 to the 6 blocks in the cache we have 2 to the 6 blocks in the cache we have 2 to the 6 blocks in the cache it's a four-way set asstive cache which it's a four-way set asstive cache which it's a four-way set asstive cache which means that every set has four blocks in means that every set has four blocks in means that every set has four blocks in it so we divide the number of blocks it so we divide the number of blocks it so we divide the number of blocks with the number of blocks in a set and with the number of blocks in a set and with the number of blocks in a set and we get 2 to the 4th or 16 sets now we we get 2 to the 4th or 16 sets now we we get 2 to the 4th or 16 sets now we know that we need four bits to tell us know that we need four bits to tell us know that we need four bits to tell us which set we are in so bits six 7 8 and which set we are in so bits six 7 8 and which set we are in so bits six 7 8 and nine four of them are the index the bits nine four of them are the index the bits nine four of them are the index the bits that remain bits 10 through that remain bits 10 through that remain bits 10 through 63 54 of them are the Tag now in our 63 54 of them are the Tag now in our 63 54 of them are the Tag now in our cache we will have for each block a cache we will have for each block a cache we will have for each block a valid bit it's a right back hash so we valid bit it's a right back hash so we valid bit it's a right back hash so we will also have a dirty bit then there will also have a dirty bit then there will also have a dirty bit then there will be a tag 54 bits it's a fourway set will be a tag 54 bits it's a fourway set will be a tag 54 bits it's a fourway set asstive cash so we need to pick a asstive cash so we need to pick a asstive cash so we need to pick a replacement policy let's say the replacement policy let's say the replacement policy let's say the replacement policy is lru for a four-way replacement policy is lru for a four-way replacement policy is lru for a four-way set associative cache each block needs set associative cache each block needs set associative cache each block needs to have a 2bit lru counter so here we to have a 2bit lru counter so here we to have a 2bit lru counter so here we have 1 1 54 2 and then we have 64 bytes have 1 1 54 2 and then we have 64 bytes have 1 1 54 2 and then we have 64 bytes worth of actual data as you can see we worth of actual data as you can see we worth of actual data as you can see we have 58 bytes in addition to the data so have 58 bytes in addition to the data so have 58 bytes in addition to the data so there is some overhead the cache size is there is some overhead the cache size is there is some overhead the cache size is usually expressed in terms of how much usually expressed in terms of how much usually expressed in terms of how much data it contains but the actual size of data it contains but the actual size of data it contains but the actual size of the cache array is larger because it the cache array is larger because it the cache array is larger because it needs to contain all these extra bits needs to contain all these extra bits needs to contain all these extra bits for each line of data

## Cache Summary Part 2 HPCA Part 3
Kind: captions Language: en now let's see how for the same cache we now let's see how for the same cache we now let's see how for the same cache we use this address to access the cache to use this address to access the cache to use this address to access the cache to make things easier to represent we will make things easier to represent we will make things easier to represent we will draw the lines in the same set draw the lines in the same set draw the lines in the same set horizontally so these are the lines that horizontally so these are the lines that horizontally so these are the lines that belong to set 0 then we will have belong to set 0 then we will have belong to set 0 then we will have additional lines and there will be lines additional lines and there will be lines additional lines and there will be lines that belong to set 15 there are 16 sets that belong to set 15 there are 16 sets that belong to set 15 there are 16 sets if you remember so the way we access if you remember so the way we access if you remember so the way we access this cache is we take the index bits and this cache is we take the index bits and this cache is we take the index bits and they tell us which set we have let's say they tell us which set we have let's say they tell us which set we have let's say we have set 0 we then read the tag and we have set 0 we then read the tag and we have set 0 we then read the tag and valid bits for all of the blocks in that valid bits for all of the blocks in that valid bits for all of the blocks in that set the tag bits that we read are set the tag bits that we read are set the tag bits that we read are compared to the tag the valid bit needs compared to the tag the valid bit needs compared to the tag the valid bit needs to be also 1 in order to have a hit and to be also 1 in order to have a hit and to be also 1 in order to have a hit and we do this for each of these blocks in we do this for each of these blocks in we do this for each of these blocks in the set simultaneously so the tag is the set simultaneously so the tag is the set simultaneously so the tag is distributed to all of them to compare as distributed to all of them to compare as distributed to all of them to compare as you can see there is 4 separate you can see there is 4 separate you can see there is 4 separate activities in order to determine for activities in order to determine for activities in order to determine for each of these whether it's a hit and each of these whether it's a hit and each of these whether it's a hit and then there is an or circuit that checks then there is an or circuit that checks then there is an or circuit that checks if any one of these is one if that or if any one of these is one if that or if any one of these is one if that or returns a one that means that we have a returns a one that means that we have a returns a one that means that we have a hit in that case what we do is we find hit in that case what we do is we find hit in that case what we do is we find which one had a hit and go back and read which one had a hit and go back and read which one had a hit and go back and read out the data for that block once the out the data for that block once the out the data for that block once the data is read out let's say this is our data is read out let's say this is our data is read out let's say this is our 64 bytes of data we use the offset to 64 bytes of data we use the offset to 64 bytes of data we use the offset to tell us where our actual data to return tell us where our actual data to return tell us where our actual data to return to the processor is if this was a right to the processor is if this was a right to the processor is if this was a right then this offset tells us where to write then this offset tells us where to write then this offset tells us where to write and if this is a right we will also set and if this is a right we will also set and if this is a right we will also set the dirty bit here note that we don't the dirty bit here note that we don't the dirty bit here note that we don't actually check the dirty bit if it's 0 actually check the dirty bit if it's 0 actually check the dirty bit if it's 0 and set it if it is 0 we just set it to and set it if it is 0 we just set it to and set it if it is 0 we just set it to 1 regardless of what it was it's faster 1 regardless of what it was it's faster 1 regardless of what it was it's faster to just set it than to check it and then to just set it than to check it and then to just set it than to check it and then set it so if we have a hit this is how set it so if we have a hit this is how set it so if we have a hit this is how we find the data what if we have a miss we find the data what if we have a miss we find the data what if we have a miss what if all of these have returned 0 in what if all of these have returned 0 in what if all of these have returned 0 in that case we will check the LRU counters that case we will check the LRU counters that case we will check the LRU counters in these to find the one that is 0 check in these to find the one that is 0 check in these to find the one that is 0 check the dirty bit of that block if it is 1 the dirty bit of that block if it is 1 the dirty bit of that block if it is 1 we will first take this data and write we will first take this data and write we will first take this data and write it to memory and it to memory and it to memory and then put the new block here if it is then put the new block here if it is then put the new block here if it is zero then we simply bring in the new zero then we simply bring in the new zero then we simply bring in the new block we update the tag and everything block we update the tag and everything block we update the tag and everything here we set the LRU counters accordingly here we set the LRU counters accordingly here we set the LRU counters accordingly and we then supply the data to the and we then supply the data to the and we then supply the data to the processor so as you can see all of the processor so as you can see all of the processor so as you can see all of the stuff that we have been talking about in stuff that we have been talking about in stuff that we have been talking about in the cache really happens kind of the cache really happens kind of the cache really happens kind of simultaneously on every axis all of this simultaneously on every axis all of this simultaneously on every axis all of this needs to happen if it's a hit up to here needs to happen if it's a hit up to here needs to happen if it's a hit up to here if it's a miss all the way to writing if it's a miss all the way to writing if it's a miss all the way to writing back to memory fetching another block back to memory fetching another block back to memory fetching another block from memory and updating the tagging from memory and updating the tagging from memory and updating the tagging valid bits

## Cache Summary Quiz 1 HPCA Part 3
Kind: captions Language: en now let's do a quiz on the overall cache now let's do a quiz on the overall cache now let's do a quiz on the overall cache behavior let's assume we have a cache behavior let's assume we have a cache behavior let's assume we have a cache that is only 256 bytes in size with a 32 that is only 256 bytes in size with a 32 that is only 256 bytes in size with a 32 byte line size byte line size byte line size two-way set associative right back right two-way set associative right back right two-way set associative right back right allocate let's say that the processor is allocate let's say that the processor is allocate let's say that the processor is generating 32 bit addresses and that the generating 32 bit addresses and that the generating 32 bit addresses and that the bits of the address are numbered 0 bits of the address are numbered 0 bits of the address are numbered 0 through 31. so the question for you through 31. so the question for you through 31. so the question for you is the tag bits are bit what through is the tag bits are bit what through is the tag bits are bit what through what the index bits in the address are what the index bits in the address are what the index bits in the address are what through what and the offset bits what through what and the offset bits what through what and the offset bits are which bit through which bits

## Cache Summary Quiz Solution 1 HPCA Part 3
Kind: captions Language: en let's discuss the solution to our cache let's discuss the solution to our cache let's discuss the solution to our cache summary quiz part one we have a 256 byte summary quiz part one we have a 256 byte summary quiz part one we have a 256 byte cache with 32 byte lines and two way set cache with 32 byte lines and two way set cache with 32 byte lines and two way set associative we have a 32-bit address we associative we have a 32-bit address we associative we have a 32-bit address we need to know which bits are tagged index need to know which bits are tagged index need to know which bits are tagged index and offset and offset and offset remember to always start from the offset remember to always start from the offset remember to always start from the offset bits bits bits 0 through something are the offset 0 through something are the offset 0 through something are the offset which bit well 32 bytes which bit well 32 bytes which bit well 32 bytes is 2 to the 5th bytes is 2 to the 5th bytes is 2 to the 5th bytes so we need 5 bits to tell us which byte so we need 5 bits to tell us which byte so we need 5 bits to tell us which byte we have in a line that means that we're we have in a line that means that we're we have in a line that means that we're talking about bits 0 talking about bits 0 talking about bits 0 through 4 this is 5 bits because we through 4 this is 5 bits because we through 4 this is 5 bits because we begin at 0. the index bits begin at 0. the index bits begin at 0. the index bits are the next bits in the order of are the next bits in the order of are the next bits in the order of significance so they will begin at bit significance so they will begin at bit significance so they will begin at bit five the question is how many do we need five the question is how many do we need five the question is how many do we need to determine that we need to determine to determine that we need to determine to determine that we need to determine how many sets do we have in this cache how many sets do we have in this cache how many sets do we have in this cache the cache has the cache has the cache has 256 bytes divided by 32 bytes this is 256 bytes divided by 32 bytes this is 256 bytes divided by 32 bytes this is the number of lines the number of lines the number of lines and the result is eight so we have eight and the result is eight so we have eight and the result is eight so we have eight lines lines lines it is a two-way set associative cache it is a two-way set associative cache it is a two-way set associative cache so we have eight over two sets this is so we have eight over two sets this is so we have eight over two sets this is four we need two bits to tell us which four we need two bits to tell us which four we need two bits to tell us which set we have so it's going to be bits 5 set we have so it's going to be bits 5 set we have so it's going to be bits 5 and 6 and 6 and 6 and that means that the tag bits are and that means that the tag bits are and that means that the tag bits are starting from 7 all the way to 31.

## Cache Summary Quiz 2 HPCA Part 3
Kind: captions Language: en so this is the second part of our cash so this is the second part of our cash so this is the second part of our cash summary quiz in the same cash that we summary quiz in the same cash that we summary quiz in the same cash that we have seen in the previous quiz assuming have seen in the previous quiz assuming have seen in the previous quiz assuming that this is the sequence of accesses that this is the sequence of accesses that this is the sequence of accesses that happens and that the cash is that happens and that the cash is that happens and that the cash is completely empty at start meaning all of completely empty at start meaning all of completely empty at start meaning all of the valid bits are 0 the question is how the valid bits are 0 the question is how the valid bits are 0 the question is how many misses do we have in this sequence many misses do we have in this sequence many misses do we have in this sequence and how many blocks are written there to and how many blocks are written there to and how many blocks are written there to memory during this sequence

## Cache Summary Quiz Solution 2 HPCA Part 3
Kind: captions Language: en let's look at the solution to our cash let's look at the solution to our cash let's look at the solution to our cash summary quiz we already know that the summary quiz we already know that the summary quiz we already know that the least significant 5 bits are the offset least significant 5 bits are the offset least significant 5 bits are the offset bits that there are eight blocks in this bits that there are eight blocks in this bits that there are eight blocks in this cache and because it's 2-way cache and because it's 2-way cache and because it's 2-way set-associative that means that the next set-associative that means that the next set-associative that means that the next two bits are the index bits so all of two bits are the index bits so all of two bits are the index bits so all of these have the same index bits which these have the same index bits which these have the same index bits which means they map to the same cache set so means they map to the same cache set so means they map to the same cache set so we only really need to consider what we only really need to consider what we only really need to consider what happens in the one set where they go happens in the one set where they go happens in the one set where they go this is a load it's gonna be a miss this is a load it's gonna be a miss this is a load it's gonna be a miss because the cache is empty so we have a because the cache is empty so we have a because the cache is empty so we have a Miss now the valid dirty and tag bits in Miss now the valid dirty and tag bits in Miss now the valid dirty and tag bits in the set are gonna be 1 here and 0 here the set are gonna be 1 here and 0 here the set are gonna be 1 here and 0 here dirty is 0 and the tag is b c d e 0 0 dirty is 0 and the tag is b c d e 0 0 dirty is 0 and the tag is b c d e 0 0 it's actually gonna have slightly more it's actually gonna have slightly more it's actually gonna have slightly more bits but this is the only part that will bits but this is the only part that will bits but this is the only part that will really differ between these the next really differ between these the next really differ between these the next access is c d e f it's gonna be a mess access is c d e f it's gonna be a mess access is c d e f it's gonna be a mess and we'll fetch it into the other entry and we'll fetch it into the other entry and we'll fetch it into the other entry here the dirty bit is 0 because this is here the dirty bit is 0 because this is here the dirty bit is 0 because this is a load next we have a store hit because a load next we have a store hit because a load next we have a store hit because we are accessing this it's a hit because we are accessing this it's a hit because we are accessing this it's a hit because it's a store this becomes dirty next we it's a store this becomes dirty next we it's a store this becomes dirty next we have a store to another world in the have a store to another world in the have a store to another world in the same block so this block becomes dirty same block so this block becomes dirty same block so this block becomes dirty and then we have a store again to BCD so and then we have a store again to BCD so and then we have a store again to BCD so it's just a hit that sets this dirty bit it's just a hit that sets this dirty bit it's just a hit that sets this dirty bit again to 1 so overall we had two misses again to 1 so overall we had two misses again to 1 so overall we had two misses and 0 right backs in this sequence



---

# Playlist 2 Module 12 Raw Transcript

## Cache Reivew Outro HPCA Part 3
Kind: captions Language: en in this lesson we have reviewed how in this lesson we have reviewed how in this lesson we have reviewed how caches work and what are the concerns caches work and what are the concerns caches work and what are the concerns and choices when designing caches we and choices when designing caches we and choices when designing caches we will use this knowledge in most of the will use this knowledge in most of the will use this knowledge in most of the subsequent lessons in this course subsequent lessons in this course subsequent lessons in this course starting with lessons on virtual memory starting with lessons on virtual memory starting with lessons on virtual memory and advanced caches and we will and advanced caches and we will and advanced caches and we will definitely need this knowledge for our definitely need this knowledge for our definitely need this knowledge for our projects



---


# 10_Virtual_Memory (Raw Transcripts)

# Playlist 3 Module 1 Raw Transcript

## Virtual Memory Intro： Part 4
Kind: captions Language: en in this lesson we will be discussing the in this lesson we will be discussing the in this lesson we will be discussing the support computer architectures provide support computer architectures provide support computer architectures provide for virtual memory this is an important for virtual memory this is an important for virtual memory this is an important topic because this makes modern topic because this makes modern topic because this makes modern operating systems a lot more efficient

## Why Virtual Memory： Part 4
Kind: captions Language: en before we go into how virtual memory before we go into how virtual memory before we go into how virtual memory Works let's for see why do we even want Works let's for see why do we even want Works let's for see why do we even want to have virtual memory and the short to have virtual memory and the short to have virtual memory and the short answer is that we want to have virtual answer is that we want to have virtual answer is that we want to have virtual memory because the programmer and the memory because the programmer and the memory because the programmer and the hardware have different views of what hardware have different views of what hardware have different views of what memory means in the hardware view the memory means in the hardware view the memory means in the hardware view the machine has some memory modules with an machine has some memory modules with an machine has some memory modules with an actual amount of memory that can be actual amount of memory that can be actual amount of memory that can be accessed by the real processor let's say accessed by the real processor let's say accessed by the real processor let's say that this module is 2 GB and this module that this module is 2 GB and this module that this module is 2 GB and this module is 2 GB is 2 GB is 2 GB so what the hardware really has is 4 GB so what the hardware really has is 4 GB so what the hardware really has is 4 GB of memory and the addresses for this 4 of memory and the addresses for this 4 of memory and the addresses for this 4 GB are going to be 0 through 4 GB in GB are going to be 0 through 4 GB in GB are going to be 0 through 4 GB in contrast the programmer's view of memory contrast the programmer's view of memory contrast the programmer's view of memory is that it's a huge array with addresses is that it's a huge array with addresses is that it's a huge array with addresses that go from zero to a very very large that go from zero to a very very large that go from zero to a very very large number if it's a 64-bit machine this is number if it's a 64-bit machine this is number if it's a 64-bit machine this is many many gigabytes obviously this is many many gigabytes obviously this is many many gigabytes obviously this is much more than what the machine really much more than what the machine really much more than what the machine really has has has some of this memory might be reserved some of this memory might be reserved some of this memory might be reserved for the system some of this memory for the system some of this memory for the system some of this memory contains the actual program instructions contains the actual program instructions contains the actual program instructions some of it is for static data then we some of it is for static data then we some of it is for static data then we have the memory for the Heap which is have the memory for the Heap which is have the memory for the Heap which is where maloc Finds Its memory and this where maloc Finds Its memory and this where maloc Finds Its memory and this Heap can grow and then some of it Heap can grow and then some of it Heap can grow and then some of it usually very high in the memory address usually very high in the memory address usually very high in the memory address space is the stack and that typically space is the stack and that typically space is the stack and that typically grows this way but the programmer grows this way but the programmer grows this way but the programmer doesn't really want to be bothered by doesn't really want to be bothered by doesn't really want to be bothered by how much space is there between the Heap how much space is there between the Heap how much space is there between the Heap and the stack what they want to do is and the stack what they want to do is and the stack what they want to do is simply maloc or push things on the stack simply maloc or push things on the stack simply maloc or push things on the stack and they never run out of memory and in and they never run out of memory and in and they never run out of memory and in a 64-bit address space chances are they a 64-bit address space chances are they a 64-bit address space chances are they won't actually run out of memory but the won't actually run out of memory but the won't actually run out of memory but the problem is the actual amount of memory problem is the actual amount of memory problem is the actual amount of memory that they're using might be less or it that they're using might be less or it that they're using might be less or it might be much more than the actual might be much more than the actual might be much more than the actual memory that the machine has here things memory that the machine has here things memory that the machine has here things are actually much more complicated are actually much more complicated are actually much more complicated because we typically don't run only one because we typically don't run only one because we typically don't run only one program on a machine we actually are program on a machine we actually are program on a machine we actually are simultaneously having multiple programs simultaneously having multiple programs simultaneously having multiple programs run you might be having something that run you might be having something that run you might be having something that browses files here an MP3 player playing browses files here an MP3 player playing browses files here an MP3 player playing in the background and the word processor in the background and the word processor in the background and the word processor here each of these sees the address here each of these sees the address here each of these sees the address space that begins at zero and ends at a space that begins at zero and ends at a space that begins at zero and ends at a very large address we don't want to very large address we don't want to very large address we don't want to write our programs so that this needs to write our programs so that this needs to write our programs so that this needs to run as the first program in the system run as the first program in the system run as the first program in the system this needs to run as the second program this needs to run as the second program this needs to run as the second program in the system you want to write a in the system you want to write a in the system you want to write a program as if it was the only program in program as if it was the only program in program as if it was the only program in the system and then just run multiple the system and then just run multiple the system and then just run multiple programs like that but that means that programs like that but that means that programs like that but that means that this program's idea of memory is it this program's idea of memory is it this program's idea of memory is it thinks it has all of this memory this thinks it has all of this memory this thinks it has all of this memory this program similarly thinks it has all of program similarly thinks it has all of program similarly thinks it has all of this memory so as far as these two this memory so as far as these two this memory so as far as these two programs are concerned for example their programs are concerned for example their programs are concerned for example their code might well be at the same address code might well be at the same address code might well be at the same address in each program although in fact they in each program although in fact they in each program although in fact they have different instructions at those have different instructions at those have different instructions at those addresses so virtual ual memory is a way addresses so virtual ual memory is a way addresses so virtual ual memory is a way of of of reconciling how the programmer's view reconciling how the programmer's view reconciling how the programmer's view Memory and how the hardware actually Memory and how the hardware actually Memory and how the hardware actually needs to view Memory in order to work

## Virtual Memory Quiz： Part 4
Kind: captions Language: en Let's see if we understood what virtual Let's see if we understood what virtual Let's see if we understood what virtual memory is about. Let's say we have a memory is about. Let's say we have a memory is about. Let's say we have a computer with 16 active applications, computer with 16 active applications, computer with 16 active applications, each with a 32-bit address space, each with a 32-bit address space, each with a 32-bit address space, meaning that the application generates meaning that the application generates meaning that the application generates 32-bit addresses. So, it sees 4 GB of 32-bit addresses. So, it sees 4 GB of 32-bit addresses. So, it sees 4 GB of memory potentially. The question for you memory potentially. The question for you memory potentially. The question for you is what does this system actually have is what does this system actually have is what does this system actually have as far as memory is concerned? two 2 GB as far as memory is concerned? two 2 GB as far as memory is concerned? two 2 GB memory modules, four 4 GB memory memory modules, four 4 GB memory memory modules, four 4 GB memory modules, eight 8 GB memory modules, one modules, eight 8 GB memory modules, one modules, eight 8 GB memory modules, one 16 GB memory module. 16 GB memory module. 16 GB memory module. Select all of the answers here that are Select all of the answers here that are Select all of the answers here that are possible. That is, all of the answers possible. That is, all of the answers possible. That is, all of the answers that might work with this should be that might work with this should be that might work with this should be checked.

## Virtual Memory Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our virtual memory quiz we said that the virtual memory quiz we said that the virtual memory quiz we said that the computer has 16 active applications each computer has 16 active applications each computer has 16 active applications each with a 32-bit address space so each with a 32-bit address space so each with a 32-bit address space so each potentially sees a four gigabyte memory potentially sees a four gigabyte memory potentially sees a four gigabyte memory what is the real memory in the system it what is the real memory in the system it what is the real memory in the system it turns out that all of these are turns out that all of these are turns out that all of these are potentially correct we can have a system potentially correct we can have a system potentially correct we can have a system that has only 2 2 gigabyte memory that has only 2 2 gigabyte memory that has only 2 2 gigabyte memory modules so it really has a total of 4 modules so it really has a total of 4 modules so it really has a total of 4 gigabyte physical memory yet it can run gigabyte physical memory yet it can run gigabyte physical memory yet it can run 16 applications each of which thinks 16 applications each of which thinks 16 applications each of which thinks that it has 4 gigabytes of memory we can that it has 4 gigabytes of memory we can that it has 4 gigabytes of memory we can also have more memory even more memory also have more memory even more memory also have more memory even more memory the different combination of memory and the different combination of memory and the different combination of memory and so on so pretty much what the so on so pretty much what the so on so pretty much what the applications think they have in terms of applications think they have in terms of applications think they have in terms of memory and what the system really has memory and what the system really has memory and what the system really has can be completely decoupled by virtual can be completely decoupled by virtual can be completely decoupled by virtual memory

## Processor's View of Memory： Part 4
Kind: captions Language: en so let's look at how the processor sees so let's look at how the processor sees so let's look at how the processor sees the memory the processor sees what we the memory the processor sees what we the memory the processor sees what we call physical memory which is the memory call physical memory which is the memory call physical memory which is the memory contained in the actual memory modules contained in the actual memory modules contained in the actual memory modules that we bought and put in the system the that we bought and put in the system the that we bought and put in the system the amount of that memory sometimes even amount of that memory sometimes even amount of that memory sometimes even lower than four gigabytes it is almost lower than four gigabytes it is almost lower than four gigabytes it is almost never four gigabytes per process that we never four gigabytes per process that we never four gigabytes per process that we have in the system because there can be have in the system because there can be have in the system because there can be tens or even hundreds of processes in a tens or even hundreds of processes in a tens or even hundreds of processes in a modern operating system and it is never modern operating system and it is never modern operating system and it is never 16 exabytes per process this is how much 16 exabytes per process this is how much 16 exabytes per process this is how much memory we need if we have 2 to the 64th memory we need if we have 2 to the 64th memory we need if we have 2 to the 64th memory locations which is what you would memory locations which is what you would memory locations which is what you would have with a 64-bit address so if each have with a 64-bit address so if each have with a 64-bit address so if each process in a 64-bit machine thinks it process in a 64-bit machine thinks it process in a 64-bit machine thinks it has this much memory there is just no has this much memory there is just no has this much memory there is just no way we can have enough actual memory for way we can have enough actual memory for way we can have enough actual memory for that so we conclude that the amount of that so we conclude that the amount of that so we conclude that the amount of physical memory we have is usually less physical memory we have is usually less physical memory we have is usually less than what all the programs can access if than what all the programs can access if than what all the programs can access if all the programs access all the memory all the programs access all the memory all the programs access all the memory they possibly can they possibly can they possibly can they will access much much more than the they will access much much more than the they will access much much more than the memory we actually have the addresses memory we actually have the addresses memory we actually have the addresses that the processor uses for the physical that the processor uses for the physical that the processor uses for the physical memory have a one-to-one mapping to the memory have a one-to-one mapping to the memory have a one-to-one mapping to the bytes or words in the physical memory so bytes or words in the physical memory so bytes or words in the physical memory so a given address always goes to the same a given address always goes to the same a given address always goes to the same physical location and the physical physical location and the physical physical location and the physical location always has exactly one physical location always has exactly one physical location always has exactly one physical address

## Mapping Virtual Physical Memory： Part 4
Kind: captions Language: en so when a program generates a virtual so when a program generates a virtual so when a program generates a virtual address for example does a load or a address for example does a load or a address for example does a load or a store to it the processor needs to store to it the processor needs to store to it the processor needs to access some physical address the access some physical address the access some physical address the question is how does the processor map question is how does the processor map question is how does the processor map what the program is trying to access to what the program is trying to access to what the program is trying to access to what really should be accessed this what really should be accessed this what really should be accessed this mapping would be really hard if every mapping would be really hard if every mapping would be really hard if every bite of virtual memory could map to an bite of virtual memory could map to an bite of virtual memory could map to an arbitrary bite in the physical memory arbitrary bite in the physical memory arbitrary bite in the physical memory then we would basically have to have a then we would basically have to have a then we would basically have to have a huge table of mappings and we don't want huge table of mappings and we don't want huge table of mappings and we don't want to do do that cuz that table would be to do do that cuz that table would be to do do that cuz that table would be very large and consume a lot of our very large and consume a lot of our very large and consume a lot of our memory so instead the program's memory memory so instead the program's memory memory so instead the program's memory is divided into equal size chunks called is divided into equal size chunks called is divided into equal size chunks called Pages a typical page size is 4 kiloby so Pages a typical page size is 4 kiloby so Pages a typical page size is 4 kiloby so this would be page zero in this program this would be page zero in this program this would be page zero in this program page one in this program page two 3 4 page one in this program page two 3 4 page one in this program page two 3 4 Etc so pretty much the first 4 kilobytes Etc so pretty much the first 4 kilobytes Etc so pretty much the first 4 kilobytes will be page zero the next 4 kilobytes will be page zero the next 4 kilobytes will be page zero the next 4 kilobytes will be page one and and so on note that will be page one and and so on note that will be page one and and so on note that each page is aligned to the page size so each page is aligned to the page size so each page is aligned to the page size so the page for example is 4 kiloby and it the page for example is 4 kiloby and it the page for example is 4 kiloby and it begins at a 4 kilobyte boundary so this begins at a 4 kilobyte boundary so this begins at a 4 kilobyte boundary so this is our virtual memory our physical is our virtual memory our physical is our virtual memory our physical memory is divided into slots that can memory is divided into slots that can memory is divided into slots that can hold pages and these slots are called hold pages and these slots are called hold pages and these slots are called frames so if you remember your caches frames so if you remember your caches frames so if you remember your caches pretty much the physical memory behaves pretty much the physical memory behaves pretty much the physical memory behaves like a cache for the virtual memory in like a cache for the virtual memory in like a cache for the virtual memory in that it has a certain number of places that it has a certain number of places that it has a certain number of places where we can put where we can put where we can put pages so a page is kind of like a memory pages so a page is kind of like a memory pages so a page is kind of like a memory block and the frame is kind of like a block and the frame is kind of like a block and the frame is kind of like a cach line except that this is the actual cach line except that this is the actual cach line except that this is the actual memory we have and this is the memory memory we have and this is the memory memory we have and this is the memory that the program just thinks it has but that the program just thinks it has but that the program just thinks it has but we never actually have that memory so we never actually have that memory so we never actually have that memory so now the operating system creates a now the operating system creates a now the operating system creates a mapping where it decides which pages in mapping where it decides which pages in mapping where it decides which pages in the program will map to which frames we the program will map to which frames we the program will map to which frames we can do something like this for example can do something like this for example can do something like this for example so page zero for example the operating so page zero for example the operating so page zero for example the operating system decides to put it in frame zero system decides to put it in frame zero system decides to put it in frame zero page one can be in frame two page two page one can be in frame two page two page one can be in frame two page two can be in frame three if we have another can be in frame three if we have another can be in frame three if we have another process it has its own page zero page process it has its own page zero page process it has its own page zero page one Etc and these Pages might map to one Etc and these Pages might map to one Etc and these Pages might map to different frames if we want this page different frames if we want this page different frames if we want this page one and this page one to be the same one and this page one to be the same one and this page one to be the same because these two processes needs to because these two processes needs to because these two processes needs to share memory then we can map them to the share memory then we can map them to the share memory then we can map them to the same same same frame and now when this processor writes frame and now when this processor writes frame and now when this processor writes to its memory to its memory to its memory and this processor reads to the same and this processor reads to the same and this processor reads to the same address they actually are accessing the address they actually are accessing the address they actually are accessing the same memory location so this process same memory location so this process same memory location so this process reads what this process reads what this process reads what this process wrote but for page zero for example they wrote but for page zero for example they wrote but for page zero for example they have different memory there so this have different memory there so this have different memory there so this process sees its own content for page process sees its own content for page process sees its own content for page zero and this process sees its own zero and this process sees its own zero and this process sees its own content for page zero so who content for page zero so who content for page zero so who decides how to do this mapping here and decides how to do this mapping here and decides how to do this mapping here and here and the answer is the operating here and the answer is the operating here and the answer is the operating system design system design system design and the actual mapping mechanism is and the actual mapping mechanism is and the actual mapping mechanism is called a page table it's a table that called a page table it's a table that called a page table it's a table that says for each page in a process where is says for each page in a process where is says for each page in a process where is that page really mapped in the physical that page really mapped in the physical that page really mapped in the physical memory this is the page table for this memory this is the page table for this memory this is the page table for this process another process with its own process another process with its own process another process with its own idea of virtual memory will have a idea of virtual memory will have a idea of virtual memory will have a different page table that says for each different page table that says for each different page table that says for each page here where does it go here

## Page Table Quiz： Part 4
Kind: captions Language: en so let's see if we understood the so let's see if we understood the so let's see if we understood the relationship between page size and relationship between page size and relationship between page size and frames in memory let's say we have a frames in memory let's say we have a frames in memory let's say we have a system whose physical memory is only two system whose physical memory is only two system whose physical memory is only two gigabytes the virtual memory seen which gigabytes the virtual memory seen which gigabytes the virtual memory seen which processes four gigabytes the page size processes four gigabytes the page size processes four gigabytes the page size in this system is four kilobytes the in this system is four kilobytes the in this system is four kilobytes the question for you is how many page frames question for you is how many page frames question for you is how many page frames do we have in this system and how many do we have in this system and how many do we have in this system and how many entries does each page table have and entries does each page table have and entries does each page table have and entry is what keeps a mapping for a entry is what keeps a mapping for a entry is what keeps a mapping for a single page to where it is in physical single page to where it is in physical single page to where it is in physical memory

## Page Table Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our page let's look at the solution to our page let's look at the solution to our page size quiz our physical memory is 2 GB size quiz our physical memory is 2 GB size quiz our physical memory is 2 GB our page size is 4 kilobytes how many our page size is 4 kilobytes how many our page size is 4 kilobytes how many page frames we have well a page frame is page frames we have well a page frame is page frames we have well a page frame is a place in physical memory where a page a place in physical memory where a page a place in physical memory where a page might fit so the number of frames is might fit so the number of frames is might fit so the number of frames is simply 2 GB the physical memory size simply 2 GB the physical memory size simply 2 GB the physical memory size divided by the page size the frame is divided by the page size the frame is divided by the page size the frame is the same size as a page because it's the same size as a page because it's the same size as a page because it's basically a place for a page to be in basically a place for a page to be in basically a place for a page to be in when we are doing things like this when we are doing things like this when we are doing things like this usually it's a lot easier to think about usually it's a lot easier to think about usually it's a lot easier to think about this as what power of two is it so 2 this as what power of two is it so 2 this as what power of two is it so 2 gabes is 2 to the 31st bytes 4 kilobytes gabes is 2 to the 31st bytes 4 kilobytes gabes is 2 to the 31st bytes 4 kilobytes is 2 to the 12th bytes so we end up with is 2 to the 12th bytes so we end up with is 2 to the 12th bytes so we end up with 2 to the 19th frames so the answer here 2 to the 19th frames so the answer here 2 to the 19th frames so the answer here is 2 to the 19th how many entries do we is 2 to the 19th how many entries do we is 2 to the 19th how many entries do we have in each page table we need one have in each page table we need one have in each page table we need one entry in a page table for each page in entry in a page table for each page in entry in a page table for each page in the ver memory so the answer here is 4 the ver memory so the answer here is 4 the ver memory so the answer here is 4 GB / 4 kiloby and that is 2 to the 20th GB / 4 kiloby and that is 2 to the 20th GB / 4 kiloby and that is 2 to the 20th or one Mega entry as they would say note or one Mega entry as they would say note or one Mega entry as they would say note that we need a separate page table for that we need a separate page table for that we need a separate page table for each process each process is 4 GB of each process each process is 4 GB of each process each process is 4 GB of virtual memory so each process in our virtual memory so each process in our virtual memory so each process in our system will need a page table that is system will need a page table that is system will need a page table that is this large 2 to the 20th or slightly this large 2 to the 20th or slightly this large 2 to the 20th or slightly more than 1 million more than 1 million more than 1 million entries

## Where Is the Missing Memory： Part 4
Kind: captions Language: en so now that we know that each so now that we know that each so now that we know that each application sees a lot of memory and application sees a lot of memory and application sees a lot of memory and there is much less physical memory than there is much less physical memory than there is much less physical memory than all the applications actually can access all the applications actually can access all the applications actually can access the question is where is the missing the question is where is the missing the question is where is the missing memory so let's say we have one memory so let's say we have one memory so let's say we have one application with four pages another application with four pages another application with four pages another application with let's say four pages a application with let's say four pages a application with let's say four pages a physical memory with let's say for physical memory with let's say for physical memory with let's say for physical frames let's say that each of physical frames let's say that each of physical frames let's say that each of these applications is actually using all these applications is actually using all these applications is actually using all of its pages because there are only four of its pages because there are only four of its pages because there are only four in this application there is a four in this application there is a four in this application there is a four entry page table that for every page entry page table that for every page entry page table that for every page tells us which frame contains it and it tells us which frame contains it and it tells us which frame contains it and it could be any frame in memory this could be any frame in memory this could be any frame in memory this application also has some of its pages application also has some of its pages application also has some of its pages in physical memory but obviously there in physical memory but obviously there in physical memory but obviously there is not enough physical memory for all of is not enough physical memory for all of is not enough physical memory for all of these pages so the question is where are these pages so the question is where are these pages so the question is where are the rest of them and the answer is they the rest of them and the answer is they the rest of them and the answer is they are on the hard disk so some of these are on the hard disk so some of these are on the hard disk so some of these pages are not actually in memory they pages are not actually in memory they pages are not actually in memory they are actually stored on the hard drive in are actually stored on the hard drive in are actually stored on the hard drive in the system these pages cannot be the system these pages cannot be the system these pages cannot be directly accessed by the processor directly accessed by the processor directly accessed by the processor because the processor can only really because the processor can only really because the processor can only really access through loads and stores the access through loads and stores the access through loads and stores the physical memory so if the processor ever physical memory so if the processor ever physical memory so if the processor ever tries to access one of these they need tries to access one of these they need tries to access one of these they need to be brought from the disk before they to be brought from the disk before they to be brought from the disk before they can be accessed we will shortly see how can be accessed we will shortly see how can be accessed we will shortly see how that works but for now just remember that works but for now just remember that works but for now just remember that because the virtual memory of all that because the virtual memory of all that because the virtual memory of all the applications can significantly the applications can significantly the applications can significantly exceed the size of the physical memory exceed the size of the physical memory exceed the size of the physical memory some of the memory that the applications some of the memory that the applications some of the memory that the applications think they have is on the disk not think they have is on the disk not think they have is on the disk not really in memory at all

## Virtual To Physical Translation： Part 4
Kind: captions Language: en so now that we know that the program so now that we know that the program so now that we know that the program generates virtual addresses but the generates virtual addresses but the generates virtual addresses but the processor needs physical addresses to processor needs physical addresses to processor needs physical addresses to access the actual memory let's see how access the actual memory let's see how access the actual memory let's see how the processor does the virtual to the processor does the virtual to the processor does the virtual to physical address physical address physical address translation so the program generates a translation so the program generates a translation so the program generates a virtual address this is what the load virtual address this is what the load virtual address this is what the load for example will compute as its address for example will compute as its address for example will compute as its address the processor divides this address into the processor divides this address into the processor divides this address into the page offset part which tells us the page offset part which tells us the page offset part which tells us where in a page we are and the virtual where in a page we are and the virtual where in a page we are and the virtual page number which which tells us which page number which which tells us which page number which which tells us which page in the process are we talking about page in the process are we talking about page in the process are we talking about if we have a 4 kilobyte page 12 bits are if we have a 4 kilobyte page 12 bits are if we have a 4 kilobyte page 12 bits are telling us where in the page we are and telling us where in the page we are and telling us where in the page we are and the remaining bits are telling us what the remaining bits are telling us what the remaining bits are telling us what the page number is so let's consider the the page number is so let's consider the the page number is so let's consider the address FC 519 08b which is a 32-bit address FC 519 08b which is a 32-bit address FC 519 08b which is a 32-bit address the least significant 12 bits address the least significant 12 bits address the least significant 12 bits are telling us where in the 4 kilobyte are telling us where in the 4 kilobyte are telling us where in the 4 kilobyte page we are so that's the page offset page we are so that's the page offset page we are so that's the page offset the more significant the more significant the more significant bits are telling us what the virtual bits are telling us what the virtual bits are telling us what the virtual page number is now what happens is we page number is now what happens is we page number is now what happens is we take the virtual page number and use it take the virtual page number and use it take the virtual page number and use it as an index into the page table in this as an index into the page table in this as an index into the page table in this case the index into the page table will case the index into the page table will case the index into the page table will be FC 519 so we find the entry with that be FC 519 so we find the entry with that be FC 519 so we find the entry with that index what that entry tells us is what index what that entry tells us is what index what that entry tells us is what the frame number is that corresponds to the frame number is that corresponds to the frame number is that corresponds to this virtual page number that is where this virtual page number that is where this virtual page number that is where the did we put that page in the actual the did we put that page in the actual the did we put that page in the actual physical memory let's say that the frame physical memory let's say that the frame physical memory let's say that the frame number in this entry is number in this entry is number in this entry is 0152 we take this physical frame number 0152 we take this physical frame number 0152 we take this physical frame number and put it together with the page offset and put it together with the page offset and put it together with the page offset into a physical address and that is what into a physical address and that is what into a physical address and that is what we use to actually access the physical we use to actually access the physical we use to actually access the physical memory in this example the physical memory in this example the physical memory in this example the physical address is composed of the frame number address is composed of the frame number address is composed of the frame number and the page offset so this is a 32-bit and the page offset so this is a 32-bit and the page offset so this is a 32-bit physical address that we will use to physical address that we will use to physical address that we will use to access the memory note that the page access the memory note that the page access the memory note that the page offset is present in both the virtual offset is present in both the virtual offset is present in both the virtual and the physical address so from the and the physical address so from the and the physical address so from the virtual address the least significant virtual address the least significant virtual address the least significant bits that correspond to the page offset bits that correspond to the page offset bits that correspond to the page offset do not change when we translate into the do not change when we translate into the do not change when we translate into the physical address but the virtual page physical address but the virtual page physical address but the virtual page number does change because it gets number does change because it gets number does change because it gets translated into the frame number

## Address Translation Quiz： Part 4
Kind: captions Language: en now that we have seen add translation now that we have seen add translation now that we have seen add translation let's do an address translation quiz let's do an address translation quiz let's do an address translation quiz suppose that we have a process with a suppose that we have a process with a suppose that we have a process with a page table that only has four entries page table that only has four entries page table that only has four entries that contain these frame numbers suppose that contain these frame numbers suppose that contain these frame numbers suppose that this machine has a 16 bit virtual that this machine has a 16 bit virtual that this machine has a 16 bit virtual address space and a 20 bit physical address space and a 20 bit physical address space and a 20 bit physical address space that is the virtual address space that is the virtual address space that is the virtual address is a 16 bit address but the address is a 16 bit address but the address is a 16 bit address but the physical address is a 20 bit add given physical address is a 20 bit add given physical address is a 20 bit add given bit vir add bit vir add bit vir add isic virical add do virical add do virical add do translate

## Address Translation Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our address translation quiz we have a address translation quiz we have a address translation quiz we have a 16-bit virtual address space and a four 16-bit virtual address space and a four 16-bit virtual address space and a four entry page table that means that the entry page table that means that the entry page table that means that the most significant two bits of the 16-bit most significant two bits of the 16-bit most significant two bits of the 16-bit address are the page number the address are the page number the address are the page number the remaining 14 bits are going to be the remaining 14 bits are going to be the remaining 14 bits are going to be the page offset so this 16-bit address can page offset so this 16-bit address can page offset so this 16-bit address can be written like this in binary F 0 F 0 be written like this in binary F 0 F 0 be written like this in binary F 0 F 0 the most significant two bits are really the most significant two bits are really the most significant two bits are really the page number which corresponds to the page number which corresponds to the page number which corresponds to this entry in the page table because this entry in the page table because this entry in the page table because this is 3 so we need to look at this this is 3 so we need to look at this this is 3 so we need to look at this entry in the page table in this entry in the page table in this entry in the page table in this particular address we have 0 0 1 F these particular address we have 0 0 1 F these particular address we have 0 0 1 F these two bits correspond to this entry in the two bits correspond to this entry in the two bits correspond to this entry in the page table so now we can write out the page table so now we can write out the page table so now we can write out the physical address that corresponds to physical address that corresponds to physical address that corresponds to this virtual address it is going to this virtual address it is going to this virtual address it is going to contain the frame number from entry contain the frame number from entry contain the frame number from entry number 3 in the page table which is number 3 in the page table which is number 3 in the page table which is going to be 1/7 concatenated with the going to be 1/7 concatenated with the going to be 1/7 concatenated with the page offset which is going to be 1 1 etc page offset which is going to be 1 1 etc page offset which is going to be 1 1 etc and we can now write it out it's going and we can now write it out it's going and we can now write it out it's going to be 0 F 0 F 0 and then we have F and to be 0 F 0 F 0 and then we have F and to be 0 F 0 F 0 and then we have F and this is 5 it's a 20-bit address so it this is 5 it's a 20-bit address so it this is 5 it's a 20-bit address so it will have 5 hex digits similarly here we will have 5 hex digits similarly here we will have 5 hex digits similarly here we are looking at entry number 0 here so are looking at entry number 0 here so are looking at entry number 0 here so it's going to be 1 F followed by the it's going to be 1 F followed by the it's going to be 1 F followed by the page offset so it's going to have 0 0 page offset so it's going to have 0 0 page offset so it's going to have 0 0 and then the remaining digits are going and then the remaining digits are going and then the remaining digits are going to be the same so it's going to have F 1 to be the same so it's going to have F 1 to be the same so it's going to have F 1 0 and then C 7 C because it's 1 1 0 0 0 and then C 7 C because it's 1 1 0 0 0 and then C 7 C because it's 1 1 0 0 and 7 because it's 0 1 1 1 so this is and 7 because it's 0 1 1 1 so this is and 7 because it's 0 1 1 1 so this is the physical address for this particular the physical address for this particular the physical address for this particular virtual address

## Size of Flat Page Table： Part 4
Kind: captions Language: en what we have discussed so far are the what we have discussed so far are the what we have discussed so far are the so-called flat page tables where for so-called flat page tables where for so-called flat page tables where for every page number there is an entry in every page number there is an entry in every page number there is an entry in the table let's talk about the size of the table let's talk about the size of the table let's talk about the size of such a flat page table it has one entry such a flat page table it has one entry such a flat page table it has one entry per page in the entire virtual address per page in the entire virtual address per page in the entire virtual address space even four pages in the address space even four pages in the address space even four pages in the address space that the program never actually space that the program never actually space that the program never actually accesses such as those pages that sit accesses such as those pages that sit accesses such as those pages that sit between the heap and the stack the entry between the heap and the stack the entry between the heap and the stack the entry in such a page table contains the frame in such a page table contains the frame in such a page table contains the frame number plus a few bits that tell us if number plus a few bits that tell us if number plus a few bits that tell us if the page is accessible for example the the page is accessible for example the the page is accessible for example the protection bits or a bit that tells us protection bits or a bit that tells us protection bits or a bit that tells us that the page is not really in physical that the page is not really in physical that the page is not really in physical memory at all so the entry in the page memory at all so the entry in the page memory at all so the entry in the page table is typically similar in size to table is typically similar in size to table is typically similar in size to the actual physical address the entry the actual physical address the entry the actual physical address the entry needs to contain the frame number which needs to contain the frame number which needs to contain the frame number which is most of the bits in the physical is most of the bits in the physical is most of the bits in the physical address it doesn't need to contain the address it doesn't need to contain the address it doesn't need to contain the page offset that the physical address page offset that the physical address page offset that the physical address has but it needs to contain some extra has but it needs to contain some extra has but it needs to contain some extra bits so overall if we have a 32 bit bits so overall if we have a 32 bit bits so overall if we have a 32 bit physical address probably the entry size physical address probably the entry size physical address probably the entry size in the page table will be 32 bits if we in the page table will be 32 bits if we in the page table will be 32 bits if we have a 64 bit physical address probably have a 64 bit physical address probably have a 64 bit physical address probably the entry size will be 64 bits so the the entry size will be 64 bits so the the entry size will be 64 bits so the overall size of a page table is the overall size of a page table is the overall size of a page table is the overall size of the virtual memory of a overall size of the virtual memory of a overall size of the virtual memory of a process divided by the page size this is process divided by the page size this is process divided by the page size this is how many entries we have in the page how many entries we have in the page how many entries we have in the page table times the size of a page table table times the size of a page table table times the size of a page table entry let's look at an example where the entry let's look at an example where the entry let's look at an example where the virtual memory size is 4 gigabytes for a virtual memory size is 4 gigabytes for a virtual memory size is 4 gigabytes for a process the page size is 4 kilobytes and process the page size is 4 kilobytes and process the page size is 4 kilobytes and the size of an entry is 4 bytes in that the size of an entry is 4 bytes in that the size of an entry is 4 bytes in that case we get a 4 megabyte page table per case we get a 4 megabyte page table per case we get a 4 megabyte page table per process now note that a process might process now note that a process might process now note that a process might actually be using less than 4 megabytes actually be using less than 4 megabytes actually be using less than 4 megabytes of actual virtual memory because most of of actual virtual memory because most of of actual virtual memory because most of the pages are unused in such a process the pages are unused in such a process the pages are unused in such a process yet the page table still needs to be 4 yet the page table still needs to be 4 yet the page table still needs to be 4 megabytes so one problem with the flat megabytes so one problem with the flat megabytes so one problem with the flat page tables is that the page table is page tables is that the page table is page tables is that the page table is reasonably large even if the process reasonably large even if the process reasonably large even if the process uses very little of its available uses very little of its available uses very little of its available virtual memory the other problem with virtual memory the other problem with virtual memory the other problem with the flat page table is that if the the flat page table is that if the the flat page table is that if the virtual memory is a 64-bit address space virtual memory is a 64-bit address space virtual memory is a 64-bit address space so if the virtual memory is 2 to the so if the virtual memory is 2 to the so if the virtual memory is 2 to the 64th bytes then we are 64th bytes then we are 64th bytes then we are talking about the page table that is talking about the page table that is talking about the page table that is four billion times this which is much four billion times this which is much four billion times this which is much larger than the memory we actually have larger than the memory we actually have larger than the memory we actually have so the page table like this simply so the page table like this simply so the page table like this simply cannot fit in memory if we have a large cannot fit in memory if we have a large cannot fit in memory if we have a large virtual address space and today's virtual address space and today's virtual address space and today's processors have such a large space so we processors have such a large space so we processors have such a large space so we need to reorganize the page table so need to reorganize the page table so need to reorganize the page table so that it doesn't occupy this much space

## Flat Page Table Size Quiz： Part 4
Kind: captions Language: en so let's see if we can compute the size so let's see if we can compute the size so let's see if we can compute the size of the flat page table let's say that of the flat page table let's say that of the flat page table let's say that our page table needs to have eight bytes our page table needs to have eight bytes our page table needs to have eight bytes per entry for example because it has a per entry for example because it has a per entry for example because it has a 64-bit physical address space let's say 64-bit physical address space let's say 64-bit physical address space let's say that the page size is 4 kilobytes let's that the page size is 4 kilobytes let's that the page size is 4 kilobytes let's say that the system only has 2 processes say that the system only has 2 processes say that the system only has 2 processes that are active let's say that the that are active let's say that the that are active let's say that the system only really has 2 gigabytes of system only really has 2 gigabytes of system only really has 2 gigabytes of physical memory let's say that the physical memory let's say that the physical memory let's say that the programs are creating 32-bit virtual programs are creating 32-bit virtual programs are creating 32-bit virtual addresses let's say that the process addresses let's say that the process addresses let's say that the process number 1 is using only 1 megabyte of its number 1 is using only 1 megabyte of its number 1 is using only 1 megabyte of its available 32-bit address space and let's available 32-bit address space and let's available 32-bit address space and let's say that the process number 2 is using a say that the process number 2 is using a say that the process number 2 is using a gigabyte of its virtual memory it could gigabyte of its virtual memory it could gigabyte of its virtual memory it could use up to 4 gigabytes but it's only use up to 4 gigabytes but it's only use up to 4 gigabytes but it's only using 1 gigabyte the question for you is using 1 gigabyte the question for you is using 1 gigabyte the question for you is what is the total size of all the page what is the total size of all the page what is the total size of all the page tables in the system in megabytes

## Flat Page Table Size Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our flat let's look at the solution to our flat let's look at the solution to our flat page table size quiz we are asked what page table size quiz we are asked what page table size quiz we are asked what the total size of the page tables in the the total size of the page tables in the the total size of the page tables in the system is there are two processes in the system is there are two processes in the system is there are two processes in the system so there will be two page tables system so there will be two page tables system so there will be two page tables we need to add up their sizes the page we need to add up their sizes the page we need to add up their sizes the page size is 4 kiloby the applications have a size is 4 kiloby the applications have a size is 4 kiloby the applications have a 32-bit virtual address space so we have 32-bit virtual address space so we have 32-bit virtual address space so we have two processes times 2 to the 32nd bytes two processes times 2 to the 32nd bytes two processes times 2 to the 32nd bytes of address space divided by 2 to the 12 of address space divided by 2 to the 12 of address space divided by 2 to the 12 bytes in a page this is how many p Pages bytes in a page this is how many p Pages bytes in a page this is how many p Pages we have times the size of an entry is 8 we have times the size of an entry is 8 we have times the size of an entry is 8 bytes which gives us 2 * 8 is 16 * 2 to bytes which gives us 2 * 8 is 16 * 2 to bytes which gives us 2 * 8 is 16 * 2 to the 20th 2 to the 20th bytes is a the 20th 2 to the 20th bytes is a the 20th 2 to the 20th bytes is a megabyte so we have 16 mbes note that it megabyte so we have 16 mbes note that it megabyte so we have 16 mbes note that it didn't matter how much memory the didn't matter how much memory the didn't matter how much memory the processes are actually using the flat processes are actually using the flat processes are actually using the flat page table needs to have an entry for page table needs to have an entry for page table needs to have an entry for every possible page in the virtual every possible page in the virtual every possible page in the virtual address space not only for the pages we address space not only for the pages we address space not only for the pages we are actually using also it doesn't are actually using also it doesn't are actually using also it doesn't matter how much physical memory we matter how much physical memory we matter how much physical memory we actually have what matters is what the actually have what matters is what the actually have what matters is what the size of the entry is so for example this size of the entry is so for example this size of the entry is so for example this entry was designed to accommodate up to entry was designed to accommodate up to entry was designed to accommodate up to a 64-bit physical address even though a 64-bit physical address even though a 64-bit physical address even though the system actually has much less memory the system actually has much less memory the system actually has much less memory why would you do this well because you why would you do this well because you why would you do this well because you want to build a system such that it can want to build a system such that it can want to build a system such that it can expand its memory if necessary for expand its memory if necessary for expand its memory if necessary for example by adding more memory modules



---

# Playlist 3 Module 2 Raw Transcript

## Multi Level Page Tables： Part 4
Kind: captions Language: en so now that we have seen that flat page so now that we have seen that flat page so now that we have seen that flat page tables can be quite large even for tables can be quite large even for tables can be quite large even for 32-bit address spaces and are too big to 32-bit address spaces and are too big to 32-bit address spaces and are too big to even fit in memory for 64-bit address even fit in memory for 64-bit address even fit in memory for 64-bit address spaces let's talk about multi-level page spaces let's talk about multi-level page spaces let's talk about multi-level page tables which is how we reduce the size tables which is how we reduce the size tables which is how we reduce the size of page tables when we have a large of page tables when we have a large of page tables when we have a large address space so the problem with flat address space so the problem with flat address space so the problem with flat page tables is that their size is page tables is that their size is page tables is that their size is proportional to the address space proportional to the address space proportional to the address space meaning how much memory can the meaning how much memory can the meaning how much memory can the application possibly address not how application possibly address not how application possibly address not how much memory it is actually using for a much memory it is actually using for a much memory it is actually using for a 32-bit address space we have seen that 32-bit address space we have seen that 32-bit address space we have seen that we have a reasonably sized page table so we have a reasonably sized page table so we have a reasonably sized page table so for 32-bit virtual addresses the flat for 32-bit virtual addresses the flat for 32-bit virtual addresses the flat page table size is several megabytes page table size is several megabytes page table size is several megabytes note that we can have a very small note that we can have a very small note that we can have a very small application that only uses a couple of application that only uses a couple of application that only uses a couple of kilobytes of memory yet it still needs a kilobytes of memory yet it still needs a kilobytes of memory yet it still needs a page table of this size because the page page table of this size because the page page table of this size because the page table size has nothing to do with how table size has nothing to do with how table size has nothing to do with how much memory we are using it has to do much memory we are using it has to do much memory we are using it has to do with how much memory can we possibly with how much memory can we possibly with how much memory can we possibly address for 64-bit virtual addresses a address for 64-bit virtual addresses a address for 64-bit virtual addresses a flat page table would simply be too big flat page table would simply be too big flat page table would simply be too big the page table would be many many the page table would be many many the page table would be many many gigabytes in size so which simply cannot gigabytes in size so which simply cannot gigabytes in size so which simply cannot use it because it wouldn't fit in the use it because it wouldn't fit in the use it because it wouldn't fit in the amount of physical memory we actually amount of physical memory we actually amount of physical memory we actually have so multi-level page tables are have so multi-level page tables are have so multi-level page tables are designed to try to overcome both of the designed to try to overcome both of the designed to try to overcome both of the problems we want the overall size of the problems we want the overall size of the problems we want the overall size of the page table to be proportional to how page table to be proportional to how page table to be proportional to how much memory we are using and we want our much memory we are using and we want our much memory we are using and we want our page table to work even for 64-bit page table to work even for 64-bit page table to work even for 64-bit virtual addresses as long as the program virtual addresses as long as the program virtual addresses as long as the program is actually not using the entire 64-bit is actually not using the entire 64-bit is actually not using the entire 64-bit virtual address space which no program virtual address space which no program virtual address space which no program really does the idea of why multi-level really does the idea of why multi-level really does the idea of why multi-level page tables work has to do with how the page tables work has to do with how the page tables work has to do with how the virtual address space is usually used by virtual address space is usually used by virtual address space is usually used by the applications recall that the the applications recall that the the applications recall that the application thinks it can address memory application thinks it can address memory application thinks it can address memory with 64-bit addresses so it has this with 64-bit addresses so it has this with 64-bit addresses so it has this many potential memory locations it can many potential memory locations it can many potential memory locations it can access but in reality the application access but in reality the application access but in reality the application only allocates memory like this at the only allocates memory like this at the only allocates memory like this at the beginning of the virtual memory there is beginning of the virtual memory there is beginning of the virtual memory there is the code the static variables and the the code the static variables and the the code the static variables and the heap and at the top of the virtual heap and at the top of the virtual heap and at the top of the virtual address space is the stack so we're address space is the stack so we're address space is the stack so we're really talking about really talking about really talking about two contiguous regions of virtual memory two contiguous regions of virtual memory two contiguous regions of virtual memory that are actually used by the that are actually used by the that are actually used by the application and even if these two are application and even if these two are application and even if these two are several gigabytes each in a 64-bit several gigabytes each in a 64-bit several gigabytes each in a 64-bit address space there will be many many address space there will be many many address space there will be many many terabytes of unused address space here terabytes of unused address space here terabytes of unused address space here but in a flat page table we had to have but in a flat page table we had to have but in a flat page table we had to have a page table entry for each page in this a page table entry for each page in this a page table entry for each page in this huge space here so a multi-level page huge space here so a multi-level page huge space here so a multi-level page table combines the idea of a flat page table combines the idea of a flat page table combines the idea of a flat page table where we use some bits from the table where we use some bits from the table where we use some bits from the virtual address to index the table this virtual address to index the table this virtual address to index the table this works really well in hardware hardware works really well in hardware hardware works really well in hardware hardware really likes using some bits to index really likes using some bits to index really likes using some bits to index some sort of an array but we want to some sort of an array but we want to some sort of an array but we want to avoid the use of table entries that avoid the use of table entries that avoid the use of table entries that correspond to this meaning we want to correspond to this meaning we want to correspond to this meaning we want to have a pretty normal page table like have a pretty normal page table like have a pretty normal page table like thing here and here but somehow not have thing here and here but somehow not have thing here and here but somehow not have anything for this so that we can simply anything for this so that we can simply anything for this so that we can simply omit those page table entries now that omit those page table entries now that omit those page table entries now that we have the idea for what we want let's we have the idea for what we want let's we have the idea for what we want let's see how the multi-level page table is see how the multi-level page table is see how the multi-level page table is actually organized

## Multi Level Page Table Structure： Part 4
Kind: captions Language: en so the multi-level page table still so the multi-level page table still so the multi-level page table still looks at the virtual address in terms of looks at the virtual address in terms of looks at the virtual address in terms of page offset and page number but instead page offset and page number but instead page offset and page number but instead of using the entire page number to of using the entire page number to of using the entire page number to Access one huge table it partitions the Access one huge table it partitions the Access one huge table it partitions the page number into the so-called inner page number into the so-called inner page number into the so-called inner page number and the outer page number page number and the outer page number page number and the outer page number the outer page number tells us which the outer page number tells us which the outer page number tells us which part of the large pay table we would be part of the large pay table we would be part of the large pay table we would be using the inner part tells us which using the inner part tells us which using the inner part tells us which specific entry in that large page table specific entry in that large page table specific entry in that large page table we would be using instead of having the we would be using instead of having the we would be using instead of having the large page table the outer page number large page table the outer page number large page table the outer page number is now used to index into the outer page is now used to index into the outer page is now used to index into the outer page table and each entry in the outer page table and each entry in the outer page table and each entry in the outer page table tells us where to find the table tells us where to find the table tells us where to find the so-called inner page table once we find so-called inner page table once we find so-called inner page table once we find the inner page table we take the inner the inner page table we take the inner the inner page table we take the inner page number and use it to index into the page number and use it to index into the page number and use it to index into the inner page table and what we find there inner page table and what we find there inner page table and what we find there is the actual frame number to AIS is the actual frame number to AIS is the actual frame number to AIS another entry in the outer page page another entry in the outer page page another entry in the outer page page table will point to a different inner table will point to a different inner table will point to a different inner page table so pretty much what we have page table so pretty much what we have page table so pretty much what we have is the outer page number tells us which is the outer page number tells us which is the outer page number tells us which of the small inner page tables to use of the small inner page tables to use of the small inner page tables to use and the inner page number tells us where and the inner page number tells us where and the inner page number tells us where in each of the page tables we need to in each of the page tables we need to in each of the page tables we need to look at so it seems like this is just a look at so it seems like this is just a look at so it seems like this is just a more complicated way to access the page more complicated way to access the page more complicated way to access the page table but still for every possible page table but still for every possible page table but still for every possible page number we will have a frame number in number we will have a frame number in number we will have a frame number in one of the inner page tables the total one of the inner page tables the total one of the inner page tables the total size of the inner page tables will be size of the inner page tables will be size of the inner page tables will be the size of the original flat page table the size of the original flat page table the size of the original flat page table and we still have added the outer page and we still have added the outer page and we still have added the outer page table which we didn't have before so table which we didn't have before so table which we didn't have before so pretty much we still have the same pretty much we still have the same pretty much we still have the same number of entries total here and we have number of entries total here and we have number of entries total here and we have added something so where are the savings

## Two Level Page Table Example： Part 4
Kind: captions Language: en to understand how the multi-level page to understand how the multi-level page to understand how the multi-level page table save space let's look at an table save space let's look at an table save space let's look at an example of a simple and small twole page example of a simple and small twole page example of a simple and small twole page table in this example we will use a very table in this example we will use a very table in this example we will use a very small virtual address where we have an small virtual address where we have an small virtual address where we have an offset and the page number and each of offset and the page number and each of offset and the page number and each of these is four bits so using a flat page these is four bits so using a flat page these is four bits so using a flat page table we would need 2 to the 4th entries table we would need 2 to the 4th entries table we would need 2 to the 4th entries in it one for each possible page number in it one for each possible page number in it one for each possible page number so the entries would be numbered 0 so the entries would be numbered 0 so the entries would be numbered 0 through 15 and we would index with our through 15 and we would index with our through 15 and we would index with our page number here and get our frame page number here and get our frame page number here and get our frame number with a two level page table let's number with a two level page table let's number with a two level page table let's say we split this 4bit page number into say we split this 4bit page number into say we split this 4bit page number into two 2bit pieces so the same virtual two 2bit pieces so the same virtual two 2bit pieces so the same virtual address still has a 4-bit page number address still has a 4-bit page number address still has a 4-bit page number that we split into two 2bit pieces the that we split into two 2bit pieces the that we split into two 2bit pieces the outer part of the page number is used to outer part of the page number is used to outer part of the page number is used to index into a four entry outer page table index into a four entry outer page table index into a four entry outer page table and each entry in this outer page table and each entry in this outer page table and each entry in this outer page table can point to a four entry inner page can point to a four entry inner page can point to a four entry inner page table so in total we have four inner table so in total we have four inner table so in total we have four inner page tables once we find the pointer to page tables once we find the pointer to page tables once we find the pointer to the correct one we can use the inner the correct one we can use the inner the correct one we can use the inner part of the page number to tell us which part of the page number to tell us which part of the page number to tell us which entry to use and that gives us our frame entry to use and that gives us our frame entry to use and that gives us our frame number so as indicated number so as indicated number so as indicated previously the total size of all the previously the total size of all the previously the total size of all the inner page tables here is the same as inner page tables here is the same as inner page tables here is the same as the original size of the flat page table the original size of the flat page table the original size of the flat page table here we had 16 entries here we have four here we had 16 entries here we have four here we had 16 entries here we have four * 4 entries here Plus we added the space * 4 entries here Plus we added the space * 4 entries here Plus we added the space for the outer page table so where are for the outer page table so where are for the outer page table so where are the savings the savings are in that if the savings the savings are in that if the savings the savings are in that if there is an unused large part of the there is an unused large part of the there is an unused large part of the address space such that all of these address space such that all of these address space such that all of these entries do not correspond to physical entries do not correspond to physical entries do not correspond to physical memory then we don't need this and we memory then we don't need this and we memory then we don't need this and we can Mark here in the outer page table can Mark here in the outer page table can Mark here in the outer page table that the entry is not necessary and then that the entry is not necessary and then that the entry is not necessary and then we can eliminate this inner page table we can eliminate this inner page table we can eliminate this inner page table in a large address space the outer page in a large address space the outer page in a large address space the outer page table table will have many pointers most table table will have many pointers most table table will have many pointers most of which will point to Inner page tables of which will point to Inner page tables of which will point to Inner page tables like this so we eliminate them so we end like this so we eliminate them so we end like this so we eliminate them so we end up having one reasonably large outer up having one reasonably large outer up having one reasonably large outer page table but one that we can fit in page table but one that we can fit in page table but one that we can fit in memory and a small number of inner page memory and a small number of inner page memory and a small number of inner page tables Each of which is reasonably sized tables Each of which is reasonably sized tables Each of which is reasonably sized and fits in memory but we don't have and fits in memory but we don't have and fits in memory but we don't have most of the inner page tables so we save most of the inner page tables so we save most of the inner page tables so we save a lot of space

## Two Level Page Table Size： Part 4
Kind: captions Language: en so now let's talk about how big the so now let's talk about how big the so now let's talk about how big the twole page table gets say we have a twole page table gets say we have a twole page table gets say we have a 32-bit address space and a 4 kiloby Page 32-bit address space and a 4 kiloby Page 32-bit address space and a 4 kiloby Page let's say we're using a 1024 entry outer let's say we're using a 1024 entry outer let's say we're using a 1024 entry outer page table and 1,24 entry inner tables page table and 1,24 entry inner tables page table and 1,24 entry inner tables there can be many of them up to 10,24 of there can be many of them up to 10,24 of there can be many of them up to 10,24 of them and let's say that a page table them and let's say that a page table them and let's say that a page table entry is 8 bytes assuming that our page entry is 8 bytes assuming that our page entry is 8 bytes assuming that our page table entry is 8 bytes and that we have table entry is 8 bytes and that we have table entry is 8 bytes and that we have a program that uses virtual memory at a program that uses virtual memory at a program that uses virtual memory at the beginning of memory from addresses the beginning of memory from addresses the beginning of memory from addresses zero through this and at the top of the zero through this and at the top of the zero through this and at the top of the memory from address this to the end memory from address this to the end memory from address this to the end let's look at what the flat page table let's look at what the flat page table let's look at what the flat page table size will be and what our two level page size will be and what our two level page size will be and what our two level page table size will be with this split table size will be with this split table size will be with this split between the inner and outer page tables between the inner and outer page tables between the inner and outer page tables the flat page table size will have one the flat page table size will have one the flat page table size will have one entry for each possible page so we have entry for each possible page so we have entry for each possible page so we have 2 to the 32nd Pages 2 to the 12 bytes in 2 to the 32nd Pages 2 to the 12 bytes in 2 to the 32nd Pages 2 to the 12 bytes in each page so we have 2 to the 20th each page so we have 2 to the 20th each page so we have 2 to the 20th entries and the total size will be 8 * entries and the total size will be 8 * entries and the total size will be 8 * this which ends up being 8 megabytes for this which ends up being 8 megabytes for this which ends up being 8 megabytes for the two- level page table we need to the two- level page table we need to the two- level page table we need to figure out how many inner page tables do figure out how many inner page tables do figure out how many inner page tables do we have here and here our address is now we have here and here our address is now we have here and here our address is now composed of a 12bit offset to index into composed of a 12bit offset to index into composed of a 12bit offset to index into the 4 kilobyte page a 10 bit outer page the 4 kilobyte page a 10 bit outer page the 4 kilobyte page a 10 bit outer page number and the 10 bit inner page number number and the 10 bit inner page number number and the 10 bit inner page number they need to add up to 3 two which they they need to add up to 3 two which they they need to add up to 3 two which they do so we know that we need to have the do so we know that we need to have the do so we know that we need to have the outer page table and its size will be 2 outer page table and its size will be 2 outer page table and its size will be 2 to the 10th entries times 8 bytes per to the 10th entries times 8 bytes per to the 10th entries times 8 bytes per entry here which ends up being 8 entry here which ends up being 8 entry here which ends up being 8 kilobytes for the outer page table now kilobytes for the outer page table now kilobytes for the outer page table now the question is which of these entries the question is which of these entries the question is which of these entries need to point to actual inner page need to point to actual inner page need to point to actual inner page tables and which ones can simply say tables and which ones can simply say tables and which ones can simply say that there is no inner page table to that there is no inner page table to that there is no inner page table to figure that out we need to see which of figure that out we need to see which of figure that out we need to see which of the inner page table entries do we need the inner page table entries do we need the inner page table entries do we need to actually point to pages of memory and to actually point to pages of memory and to actually point to pages of memory and which ones can simply be empty thus we which ones can simply be empty thus we which ones can simply be empty thus we will decompose these addresses this part will decompose these addresses this part will decompose these addresses this part of the address is the page offset the of the address is the page offset the of the address is the page offset the upper part is the page number and we upper part is the page number and we upper part is the page number and we need to split this 20 bit page number need to split this 20 bit page number need to split this 20 bit page number into 10bit groups so let's just write it into 10bit groups so let's just write it into 10bit groups so let's just write it out we have 0 0 0 0 these are the two out we have 0 0 0 0 these are the two out we have 0 0 0 0 these are the two zeros another hex digit for Z zeros another hex digit for Z zeros another hex digit for Z the one and the zero this is the outer the one and the zero this is the outer the one and the zero this is the outer page number this is the inner page page number this is the inner page page number this is the inner page number as you can see all of the number as you can see all of the number as you can see all of the addresses in this range have the same addresses in this range have the same addresses in this range have the same outer page number we start at zero we outer page number we start at zero we outer page number we start at zero we end at zero so only the entry number end at zero so only the entry number end at zero so only the entry number zero here needs to point to an actual zero here needs to point to an actual zero here needs to point to an actual inner page table for this address range inner page table for this address range inner page table for this address range the page number goes from the page number goes from the page number goes from F0 to fffff F these page number numbers F0 to fffff F these page number numbers F0 to fffff F these page number numbers also have the same outer page number also have the same outer page number also have the same outer page number it's all ones so only the last outer it's all ones so only the last outer it's all ones so only the last outer page table entry needs to point to an page table entry needs to point to an page table entry needs to point to an actual inner page table note that there actual inner page table note that there actual inner page table note that there are 1,24 entries here only two of which are 1,24 entries here only two of which are 1,24 entries here only two of which point to Inner page tables so there is point to Inner page tables so there is point to Inner page tables so there is 1,22 entries that simply say nothing so 1,22 entries that simply say nothing so 1,22 entries that simply say nothing so we have two inner page table times we have two inner page table times we have two inner page table times what's the size of one of them well the what's the size of one of them well the what's the size of one of them well the size is 1,24 entries times 8 bytes per size is 1,24 entries times 8 bytes per size is 1,24 entries times 8 bytes per entry which gives us a total of 16 entry which gives us a total of 16 entry which gives us a total of 16 kilobytes adding the size of the outer kilobytes adding the size of the outer kilobytes adding the size of the outer page table we get only 24 kiloby so the page table we get only 24 kiloby so the page table we get only 24 kiloby so the size of a twole page table for this size of a twole page table for this size of a twole page table for this application is only 24 kiloby compared application is only 24 kiloby compared application is only 24 kiloby compared to 8 megabytes for the flat page table to 8 megabytes for the flat page table to 8 megabytes for the flat page table and this is why multi-level page tables and this is why multi-level page tables and this is why multi-level page tables are almost exclusively used in today's are almost exclusively used in today's are almost exclusively used in today's processors especially when we have a processors especially when we have a processors especially when we have a 64-bit address space because for a 64 64-bit address space because for a 64 64-bit address space because for a 64 address space the flat page table size address space the flat page table size address space the flat page table size would be too large to fit in memory but would be too large to fit in memory but would be too large to fit in memory but a multi-level table will fit mostly a multi-level table will fit mostly a multi-level table will fit mostly because the outermost page table will because the outermost page table will because the outermost page table will have many empty entries so we will need have many empty entries so we will need have many empty entries so we will need very few of these inner page tables

## 4 Level PT Quiz： Part 4
Kind: captions Language: en so now that we have seen a two level so now that we have seen a two level so now that we have seen a two level page table let's see if we can work with page table let's see if we can work with page table let's see if we can work with a four level page table a four level page table a four level page table this time let's assume we have a 64-bit this time let's assume we have a 64-bit this time let's assume we have a 64-bit virtual address space a 64 kilobyte page virtual address space a 64 kilobyte page virtual address space a 64 kilobyte page size size size an 8 byte page table entry and we have a an 8 byte page table entry and we have a an 8 byte page table entry and we have a relatively large program that is only relatively large program that is only relatively large program that is only using addresses zero through four using addresses zero through four using addresses zero through four gigabytes from this huge address space gigabytes from this huge address space gigabytes from this huge address space the flat page table size is very large the flat page table size is very large the flat page table size is very large the question for you is it's a power of the question for you is it's a power of the question for you is it's a power of two what power of two so it's 2 to what two what power of two so it's 2 to what two what power of two so it's 2 to what and if you have a four level page table and if you have a four level page table and if you have a four level page table where the page number is split equally where the page number is split equally where the page number is split equally so the bits of the page number are so the bits of the page number are so the bits of the page number are divided into four equal groups to access divided into four equal groups to access divided into four equal groups to access these page tables this page table uses these page tables this page table uses these page tables this page table uses how many kilobytes of memory

## 4 Level PT Solution Quiz： Part 4
Kind: captions Language: en so let's look at the solution to our so let's look at the solution to our so let's look at the solution to our four- level page table quiz we have 2 to four- level page table quiz we have 2 to four- level page table quiz we have 2 to the 64th bytes of address space we have the 64th bytes of address space we have the 64th bytes of address space we have 2 to the 16th bytes in each page we get 2 to the 16th bytes in each page we get 2 to the 16th bytes in each page we get that there are 2 to the that there are 2 to the that there are 2 to the 48th pages in our address space for a 48th pages in our address space for a 48th pages in our address space for a flat page table the page table has 2 to flat page table the page table has 2 to flat page table the page table has 2 to the 48 entries times the size of each the 48 entries times the size of each the 48 entries times the size of each entry is 2 to the 3r so the overall size entry is 2 to the 3r so the overall size entry is 2 to the 3r so the overall size is two to the 51st bytes so the answer is two to the 51st bytes so the answer is two to the 51st bytes so the answer is here 51 this is an enormous page is here 51 this is an enormous page is here 51 this is an enormous page table many many terabytes of page table table many many terabytes of page table table many many terabytes of page table now let's see what the four level page now let's see what the four level page now let's see what the four level page table looks like the page number in this table looks like the page number in this table looks like the page number in this page table is split across four levels page table is split across four levels page table is split across four levels so the page number is split into four so the page number is split into four so the page number is split into four pieces of 12 bits the easiest way to pieces of 12 bits the easiest way to pieces of 12 bits the easiest way to figure out how large the overall page figure out how large the overall page figure out how large the overall page table is is to figure out how many table is is to figure out how many table is is to figure out how many innermost page tables we have 4 GB worth innermost page tables we have 4 GB worth innermost page tables we have 4 GB worth of memory is 2 to the 32nd bytes which of memory is 2 to the 32nd bytes which of memory is 2 to the 32nd bytes which is 2 to the 16th pages of this size so is 2 to the 16th pages of this size so is 2 to the 16th pages of this size so we have two to the 16th pages one we have two to the 16th pages one we have two to the 16th pages one innermost page table has two to the 12 innermost page table has two to the 12 innermost page table has two to the 12 entries so it covers 2 to the 12th pages entries so it covers 2 to the 12th pages entries so it covers 2 to the 12th pages to cover 2 to the 16th consecutive Pages to cover 2 to the 16th consecutive Pages to cover 2 to the 16th consecutive Pages we need 2 to the 4th of these innermost we need 2 to the 4th of these innermost we need 2 to the 4th of these innermost page tables so now we know that we have page tables so now we know that we have page tables so now we know that we have innermost page tables how many page innermost page tables how many page innermost page tables how many page tables at the next level we need to tables at the next level we need to tables at the next level we need to point to them well each table at that point to them well each table at that point to them well each table at that entry can also have 2 to the 12th entry can also have 2 to the 12th entry can also have 2 to the 12th entries and this is only 2 to the 4th so entries and this is only 2 to the 4th so entries and this is only 2 to the 4th so we need only one so this is the page we need only one so this is the page we need only one so this is the page table situation we have the outermost table situation we have the outermost table situation we have the outermost page table has 2 to the 12th or 496 page table has 2 to the 12th or 496 page table has 2 to the 12th or 496 entries but only one of them the first entries but only one of them the first entries but only one of them the first one actually has a a page table at the one actually has a a page table at the one actually has a a page table at the next level that page table at the next next level that page table at the next next level that page table at the next level also has two to the 12 entries but level also has two to the 12 entries but level also has two to the 12 entries but only one of them the first one points to only one of them the first one points to only one of them the first one points to the next level of page table that next the next level of page table that next the next level of page table that next level page table has 16 entries that level page table has 16 entries that level page table has 16 entries that point to innermost page tables so point to innermost page tables so point to innermost page tables so overall the number of these page tables overall the number of these page tables overall the number of these page tables is one here one here one here and 16 of is one here one here one here and 16 of is one here one here one here and 16 of these for a total of 19 of them the size these for a total of 19 of them the size these for a total of 19 of them the size of each of these small page tables is of each of these small page tables is of each of these small page tables is the number of entries 2 to the 12th the number of entries 2 to the 12th the number of entries 2 to the 12th times the size of an entry 2 to the3 so times the size of an entry 2 to the3 so times the size of an entry 2 to the3 so the size of each one of these is 2 to the size of each one of these is 2 to the size of each one of these is 2 to the 15th bytes which corresponds to 32 the 15th bytes which corresponds to 32 the 15th bytes which corresponds to 32 kiloby so overall we have 19 page tables kiloby so overall we have 19 page tables kiloby so overall we have 19 page tables at various levels Each of which is 32 at various levels Each of which is 32 at various levels Each of which is 32 Koby so the solution here is 19 * 32 so Koby so the solution here is 19 * 32 so Koby so the solution here is 19 * 32 so the final answer is 68 kiloby as you can the final answer is 68 kiloby as you can the final answer is 68 kiloby as you can see this is only 68 kiloby less than a see this is only 68 kiloby less than a see this is only 68 kiloby less than a megabyte compare that to many many megabyte compare that to many many megabyte compare that to many many terabytes of page table for a flat page terabytes of page table for a flat page terabytes of page table for a flat page table organization this is why all 64bit table organization this is why all 64bit table organization this is why all 64bit x86 processors nowadays use multi-level x86 processors nowadays use multi-level x86 processors nowadays use multi-level page tables of at least three page tables of at least three page tables of at least three levels

## Choosing The Page Size： Part 4
Kind: captions Language: en Yesterday morning's soldiers on Examples are Yesterday morning's soldiers on Examples are Yesterday morning's soldiers on Examples are used for boys and demonstrations were used for boys and demonstrations were used for boys and demonstrations were happy with hard 2302 Bichhu subscribe happy with hard 2302 Bichhu subscribe happy with hard 2302 Bichhu subscribe support channel Professor Shonku Rahasya pages support channel Professor Shonku Rahasya pages support channel Professor Shonku Rahasya pages ₹2 lakh pages Big pages subscribe and subscribe subscribe subscribe Seems Like a Path Which Can Give Seems Like a Path Which Can Give Seems Like a Path Which Can Give application Do application Do application Do n't forget to subscribe and subscribe Bluetooth Bluetooth Bluetooth pages Vaidya pages Vaidya pages Vaidya subscribe The Amazing subscribe The Amazing subscribe The Amazing in it's going to do subscribe my channel channel channel subscribe subscribe subscribe The Channel Ko subscribe kare subscribe 12345 Mudda ne Bigg Boss wapsi le mode ko switch 12345 Mudda ne Bigg Boss wapsi le mode ko switch 12345 Mudda ne Bigg Boss wapsi le mode ko switch press the subscribe button on the west side

## Memory Access Time With V-P Translation： Part 4
Kind: captions Language: en So now that we have seen how paging and So now that we have seen how paging and So now that we have seen how paging and virtual to physical translation works, virtual to physical translation works, virtual to physical translation works, let's talk about the memory access time let's talk about the memory access time let's talk about the memory access time when we have this translation. So our when we have this translation. So our when we have this translation. So our program does something like this. It program does something like this. It program does something like this. It says load R1 from the address computed says load R1 from the address computed says load R1 from the address computed by adding four to what is in R2. This by adding four to what is in R2. This by adding four to what is in R2. This when computed will be the virtual when computed will be the virtual when computed will be the virtual address that we want to access in the address that we want to access in the address that we want to access in the program. So to do a load or restore, the program. So to do a load or restore, the program. So to do a load or restore, the processor has to compute the virtual processor has to compute the virtual processor has to compute the virtual address. This is quick. Usually it's address. This is quick. Usually it's address. This is quick. Usually it's just addition of two numbers that are just addition of two numbers that are just addition of two numbers that are already in the processor. One is a already in the processor. One is a already in the processor. One is a constant fetch with the instruction. The constant fetch with the instruction. The constant fetch with the instruction. The other one is the register that is inside other one is the register that is inside other one is the register that is inside the processor. Next, the processor has the processor. Next, the processor has the processor. Next, the processor has to compute the page number which is to compute the page number which is to compute the page number which is extremely quick because it just means extremely quick because it just means extremely quick because it just means that we take some bits from the virtual that we take some bits from the virtual that we take some bits from the virtual address and without any address address and without any address address and without any address translation. The next thing that the translation. The next thing that the translation. The next thing that the processor will do is access the cache processor will do is access the cache processor will do is access the cache and sometimes if there is a cache miss and sometimes if there is a cache miss and sometimes if there is a cache miss the actual memory. So again these two the actual memory. So again these two the actual memory. So again these two are extremely quick. Most of the time are extremely quick. Most of the time are extremely quick. Most of the time will be spent accessing the cache and will be spent accessing the cache and will be spent accessing the cache and occasionally we will spend more cycles occasionally we will spend more cycles occasionally we will spend more cycles because we need to access the memory because we need to access the memory because we need to access the memory with virtual to physical translation. with virtual to physical translation. with virtual to physical translation. After computing the page number the After computing the page number the After computing the page number the processor has to compute the physical processor has to compute the physical processor has to compute the physical address of the page table entry where address of the page table entry where address of the page table entry where the translation for the page number is. the translation for the page number is. the translation for the page number is. This is done by adding the page number This is done by adding the page number This is done by adding the page number that we computed to the beginning that we computed to the beginning that we computed to the beginning address of the page table. Then we read address of the page table. Then we read address of the page table. Then we read the actual page table entry and compute the actual page table entry and compute the actual page table entry and compute the physical address by combining the the physical address by combining the the physical address by combining the physical frame number from the page physical frame number from the page physical frame number from the page table entry with the page offset from table entry with the page offset from table entry with the page offset from the virtual address. So how fast is the virtual address. So how fast is the virtual address. So how fast is this? Well, this is fast because it just this? Well, this is fast because it just this? Well, this is fast because it just means adding the page number to some means adding the page number to some means adding the page number to some sort of address. This is fast because it sort of address. This is fast because it sort of address. This is fast because it only means combining two numbers that we only means combining two numbers that we only means combining two numbers that we already have. But how fast is this? And already have. But how fast is this? And already have. But how fast is this? And for that we need to ask ourselves so for that we need to ask ourselves so for that we need to ask ourselves so where is the page table? Is it inside where is the page table? Is it inside where is the page table? Is it inside the processor so it can be quickly the processor so it can be quickly the processor so it can be quickly accessed or is it in memory? We have accessed or is it in memory? We have accessed or is it in memory? We have seen that the page table can be fairly seen that the page table can be fairly seen that the page table can be fairly large. Yes, for a multi-level page table large. Yes, for a multi-level page table large. Yes, for a multi-level page table we can have it be less than a megabyte we can have it be less than a megabyte we can have it be less than a megabyte for some programs but in theory it can for some programs but in theory it can for some programs but in theory it can be extremely large. So we cannot be extremely large. So we cannot be extremely large. So we cannot guarantee that the page table for even guarantee that the page table for even guarantee that the page table for even one process fits on the processor chip one process fits on the processor chip one process fits on the processor chip which means that the page tables really which means that the page tables really which means that the page tables really need to be in memory. And that means need to be in memory. And that means need to be in memory. And that means that for each load or store, we now need that for each load or store, we now need that for each load or store, we now need to have a memory access, which is just to have a memory access, which is just to have a memory access, which is just as slow as when we had a cache miss that as slow as when we had a cache miss that as slow as when we had a cache miss that we were trying to avoid before we got to we were trying to avoid before we got to we were trying to avoid before we got to page tables. Things are even worse page tables. Things are even worse page tables. Things are even worse because for a multi-level page table, because for a multi-level page table, because for a multi-level page table, for example, a four-level page table, we for example, a four-level page table, we for example, a four-level page table, we would need to do this multiple times. would need to do this multiple times. would need to do this multiple times. For example, four times. Compute the For example, four times. Compute the For example, four times. Compute the physical address of the outermost page physical address of the outermost page physical address of the outermost page table. Read that entry. Compute the table. Read that entry. Compute the table. Read that entry. Compute the physical address of the next page table. physical address of the next page table. physical address of the next page table. Go ahead and do that four times. So we Go ahead and do that four times. So we Go ahead and do that four times. So we need four memory accesses to read the need four memory accesses to read the need four memory accesses to read the four page table entries until we get the four page table entries until we get the four page table entries until we get the actual translation after which we can actual translation after which we can actual translation after which we can quickly access the cache. So somehow we quickly access the cache. So somehow we quickly access the cache. So somehow we have a situation where the virtual to have a situation where the virtual to have a situation where the virtual to physical address translation is costing physical address translation is costing physical address translation is costing us more than the memory accesses that we us more than the memory accesses that we us more than the memory accesses that we try to avoid by having the caches.

## V to P Translation Quiz： Part 4
Kind: captions Language: en So to see the impact of virtual to So to see the impact of virtual to So to see the impact of virtual to physical translation on performance, physical translation on performance, physical translation on performance, let's do a quiz about that. In a let's do a quiz about that. In a let's do a quiz about that. In a processor that takes a single cycle to processor that takes a single cycle to processor that takes a single cycle to compute the virtual address for a load compute the virtual address for a load compute the virtual address for a load or store instruction needs one cycle to or store instruction needs one cycle to or store instruction needs one cycle to access the cache on a hit, takes 10 access the cache on a hit, takes 10 access the cache on a hit, takes 10 cycles to access memory. For example, cycles to access memory. For example, cycles to access memory. For example, when we have a cache miss has a 90% hit when we have a cache miss has a 90% hit when we have a cache miss has a 90% hit rate for data that it is trying to rate for data that it is trying to rate for data that it is trying to access and the page table entries that access and the page table entries that access and the page table entries that we need to access cannot be in the we need to access cannot be in the we need to access cannot be in the cache. So we need to access memory cache. So we need to access memory cache. So we need to access memory directly for them. So how many cycles do directly for them. So how many cycles do directly for them. So how many cycles do we need for a load instruction such as we need for a load instruction such as we need for a load instruction such as this?

## V P Translation Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our let's look at the solution to our let's look at the solution to our virtual to physical translation speed virtual to physical translation speed virtual to physical translation speed quiz we were asked how many cycles do we quiz we were asked how many cycles do we quiz we were asked how many cycles do we need for a load instruction if we use a need for a load instruction if we use a need for a load instruction if we use a three level page table we need one cycle three level page table we need one cycle three level page table we need one cycle to compute the virtual address we need to compute the virtual address we need to compute the virtual address we need ten cycles to access the outermost page ten cycles to access the outermost page ten cycles to access the outermost page table 10 cycles flux is the next level table 10 cycles flux is the next level table 10 cycles flux is the next level of page table 10 cycles talks is the of page table 10 cycles talks is the of page table 10 cycles talks is the innermost page table because neither of innermost page table because neither of innermost page table because neither of those can be cached plus we need to do those can be cached plus we need to do those can be cached plus we need to do the actual cache access and maybe the the actual cache access and maybe the the actual cache access and maybe the memory so overall we need one plus three memory so overall we need one plus three memory so overall we need one plus three levels of page tables times memory levels of page tables times memory levels of page tables times memory latency plus one cycle to have a hit in latency plus one cycle to have a hit in latency plus one cycle to have a hit in our cache plus ten percent of the time our cache plus ten percent of the time our cache plus ten percent of the time we have a miss penalty to access the we have a miss penalty to access the we have a miss penalty to access the memory this ends up being 33 cycles memory this ends up being 33 cycles memory this ends up being 33 cycles thirty of which are to access the page thirty of which are to access the page thirty of which are to access the page tables

## V to P Translation Quiz 2： Part 4
Kind: captions Language: en let us repeat the previous quiz accepted let us repeat the previous quiz accepted let us repeat the previous quiz accepted now the page table entries can be cashed now the page table entries can be cashed now the page table entries can be cashed just like data and have the same hit just like data and have the same hit just like data and have the same hit rate like data

## V P Translation Solution 2 Quiz： Part 4
Kind: captions Language: en let's look at the solution to our latest let's look at the solution to our latest let's look at the solution to our latest virtual to physical translation quiz virtual to physical translation quiz virtual to physical translation quiz where we had the memory access times and where we had the memory access times and where we had the memory access times and the hit rates for the cache and we were the hit rates for the cache and we were the hit rates for the cache and we were assuming that the three level page table assuming that the three level page table assuming that the three level page table now can be cached just like data with now can be cached just like data with now can be cached just like data with the same hit rate the same hit rate the same hit rate now we have one cycle to compute the now we have one cycle to compute the now we have one cycle to compute the virtual address virtual address virtual address followed by for each level of the page followed by for each level of the page followed by for each level of the page table table table three times we need to do three times we need to do three times we need to do one cycle to access the cache one cycle to access the cache one cycle to access the cache plus plus plus 10 of the time for each of those 10 of the time for each of those 10 of the time for each of those we will have a miss and go to memory to we will have a miss and go to memory to we will have a miss and go to memory to fetch the page table entry fetch the page table entry fetch the page table entry and then after we do that and then after we do that and then after we do that we can access the data we can access the data we can access the data which takes one cycle plus which takes one cycle plus which takes one cycle plus ten percent of the time it takes ten ten percent of the time it takes ten ten percent of the time it takes ten more cycles to access memory more cycles to access memory more cycles to access memory so now we have so now we have so now we have nine cycles all together nine cycles all together nine cycles all together six of which six of which six of which are used are used are used by our virtual to physical translation by our virtual to physical translation by our virtual to physical translation and only three and only three and only three for for for the instruction itself and the memory the instruction itself and the memory the instruction itself and the memory access access access including the misses so we can see that including the misses so we can see that including the misses so we can see that the virtual to physical translation is the virtual to physical translation is the virtual to physical translation is expensive even if we allow page table expensive even if we allow page table expensive even if we allow page table entries to be treated as data and cached

## Translation Look Aside Buffer (TLB)： Part 4
Kind: captions Language: en so to speed up virtual to physical so to speed up virtual to physical so to speed up virtual to physical translation the processor includes a translation the processor includes a translation the processor includes a structure called the translation structure called the translation structure called the translation lookaside buffer or TLB the TLB is a lookaside buffer or TLB the TLB is a lookaside buffer or TLB the TLB is a cache for translations so now you would cache for translations so now you would cache for translations so now you would ask yourself well how is the TLB better ask yourself well how is the TLB better ask yourself well how is the TLB better than just using the cache itself well than just using the cache itself well than just using the cache itself well first the cache is relatively big it first the cache is relatively big it first the cache is relatively big it could store a lot of translations if we could store a lot of translations if we could store a lot of translations if we only held translations there but we only held translations there but we only held translations there but we don't most of the stuff in the cache is don't most of the stuff in the cache is don't most of the stuff in the cache is data one translation in the cache covers data one translation in the cache covers data one translation in the cache covers a whole page worth of data so the TLB a whole page worth of data so the TLB a whole page worth of data so the TLB that cache is only translations can be that cache is only translations can be that cache is only translations can be extremely small which means extremely extremely small which means extremely extremely small which means extremely fast let's look for example at a program fast let's look for example at a program fast let's look for example at a program that accesses 16 kilobytes of data for that accesses 16 kilobytes of data for that accesses 16 kilobytes of data for that to work well we need about 16 that to work well we need about 16 that to work well we need about 16 kilobytes of cache but this is only 4 kilobytes of cache but this is only 4 kilobytes of cache but this is only 4 pages if we have a 4 kilobyte page so pages if we have a 4 kilobyte page so pages if we have a 4 kilobyte page so the TLB that corresponds to that only the TLB that corresponds to that only the TLB that corresponds to that only needs four entries to cover the same needs four entries to cover the same needs four entries to cover the same amount of memory and with only 4 entries amount of memory and with only 4 entries amount of memory and with only 4 entries we can make the deal be extremely we can make the deal be extremely we can make the deal be extremely extremely fast much less than a cycle extremely fast much less than a cycle extremely fast much less than a cycle for each translation next we have seen for each translation next we have seen for each translation next we have seen that in a multi-level page table we that in a multi-level page table we that in a multi-level page table we access our cache for each level of the access our cache for each level of the access our cache for each level of the page table because we need to look up page table because we need to look up page table because we need to look up all those entries in order to find the all those entries in order to find the all those entries in order to find the final translation in contrast till the final translation in contrast till the final translation in contrast till the only stores the final translation even only stores the final translation even only stores the final translation even if we have a 4 level page table if we have a 4 level page table if we have a 4 level page table eventually we find what a given page eventually we find what a given page eventually we find what a given page number Maps do and we just store the number Maps do and we just store the number Maps do and we just store the frame number in the TLB not the frame number in the TLB not the frame number in the TLB not the intermediate page table entries that we intermediate page table entries that we intermediate page table entries that we have been using to finally come up with have been using to finally come up with have been using to finally come up with the translation so in a cache we'll have the translation so in a cache we'll have the translation so in a cache we'll have for a 4 level page table for accesses to for a 4 level page table for accesses to for a 4 level page table for accesses to do the translation in a TLB we do this do the translation in a TLB we do this do the translation in a TLB we do this in one axis so now a load or a store in one axis so now a load or a store in one axis so now a load or a store just needs to form the address access just needs to form the address access just needs to form the address access the TLB to find the translation and then the TLB to find the translation and then the TLB to find the translation and then access the cache to get the data if we access the cache to get the data if we access the cache to get the data if we have both the TLB hit and the cache hit have both the TLB hit and the cache hit have both the TLB hit and the cache hit this can be done in one or two cycles this can be done in one or two cycles this can be done in one or two cycles but what if we have a TLB miss what if but what if we have a TLB miss what if but what if we have a TLB miss what if we do not find a translation in the TLB we do not find a translation in the TLB we do not find a translation in the TLB in that case we need to perform the in that case we need to perform the in that case we need to perform the translation using the page table or page translation using the page table or page translation using the page table or page tables if we have a multiple level page tables if we have a multiple level page tables if we have a multiple level page table and then put that translation in table and then put that translation in table and then put that translation in our TLB so that it can be used later our TLB so that it can be used later our TLB so that it can be used later reiax is the same page again

## What If We Have A TLB Miss Quiz： Part 4
Kind: captions Language: en but who's gonna do these two things but who's gonna do these two things but who's gonna do these two things should it be the operating system so should it be the operating system so should it be the operating system so when we have a TLB miss we go back into when we have a TLB miss we go back into when we have a TLB miss we go back into the operating system and let it figure the operating system and let it figure the operating system and let it figure out what the page tables have using some out what the page tables have using some out what the page tables have using some sort of software or should the processor sort of software or should the processor sort of software or should the processor automatically read the page table or automatically read the page table or automatically read the page table or page tables and then update the TLB page tables and then update the TLB page tables and then update the TLB without any operating system without any operating system without any operating system intervention which one of these two do intervention which one of these two do intervention which one of these two do you think is correct

## What If We Have A TLB Miss Quiz Solution： Part 4
Kind: captions Language: en to answer a question both are okay to answer a question both are okay to answer a question both are okay options this option is called software options this option is called software options this option is called software TLB miss handling and it has the TLB miss handling and it has the TLB miss handling and it has the advantage of letting the operating advantage of letting the operating advantage of letting the operating system use any sort of page table that system use any sort of page table that system use any sort of page table that it wants because the hardware doesn't it wants because the hardware doesn't it wants because the hardware doesn't really need to access the page table the really need to access the page table the really need to access the page table the hardware simply has the TLB and the job hardware simply has the TLB and the job hardware simply has the TLB and the job of the operating system is to put the of the operating system is to put the of the operating system is to put the correct translations into the TLB but if correct translations into the TLB but if correct translations into the TLB but if you use any organization for the page you use any organization for the page you use any organization for the page table it wants so the operating system table it wants so the operating system table it wants so the operating system for example may not even have the page for example may not even have the page for example may not even have the page table in form of a table it can use a table in form of a table it can use a table in form of a table it can use a binary tree or a hash table or anything binary tree or a hash table or anything binary tree or a hash table or anything else it wants the second option where else it wants the second option where else it wants the second option where the processor automatically reads page the processor automatically reads page the processor automatically reads page tables and updates it will be is called tables and updates it will be is called tables and updates it will be is called hardware TLB miss handling in this case hardware TLB miss handling in this case hardware TLB miss handling in this case the page tables need to be in a form the page tables need to be in a form the page tables need to be in a form that is easily accessible by the that is easily accessible by the that is easily accessible by the hardware so we will have a flat or a hardware so we will have a flat or a hardware so we will have a flat or a multi-level page table like we have seen multi-level page table like we have seen multi-level page table like we have seen already in our previous videos this already in our previous videos this already in our previous videos this approach does require more Hardware approach does require more Hardware approach does require more Hardware because the hardware now needs to because the hardware now needs to because the hardware now needs to actually be able to access the page actually be able to access the page actually be able to access the page tables and sequence the accesses to the tables and sequence the accesses to the tables and sequence the accesses to the multiple levels and so on but it is multiple levels and so on but it is multiple levels and so on but it is faster than software handling this is faster than software handling this is faster than software handling this is treated kind of like a cache miss almost treated kind of like a cache miss almost treated kind of like a cache miss almost whereas here we need to execute a little whereas here we need to execute a little whereas here we need to execute a little program to fill the TLB so because this program to fill the TLB so because this program to fill the TLB so because this is faster and because hardware is cheap is faster and because hardware is cheap is faster and because hardware is cheap these days most high performance these days most high performance these days most high performance processors like x86 will use this processors like x86 will use this processors like x86 will use this approach of hardware TLB miss handling approach of hardware TLB miss handling approach of hardware TLB miss handling but some embedded processors will use but some embedded processors will use but some embedded processors will use the software TLB miss handling because the software TLB miss handling because the software TLB miss handling because they are concerned about the hardware they are concerned about the hardware they are concerned about the hardware cost so they want to be as simple as cost so they want to be as simple as cost so they want to be as simple as possible and also because in these possible and also because in these possible and also because in these embedded processors TLB misses occur embedded processors TLB misses occur embedded processors TLB misses occur less frequently because they are running less frequently because they are running less frequently because they are running more regular applications



---

# Playlist 3 Module 3 Raw Transcript

## TLB Size Quiz： Part 4
Kind: captions Language: en so now that we have seen what the tlb so now that we have seen what the tlb so now that we have seen what the tlb size should be size should be size should be let's see if we understood it well let's see if we understood it well let's see if we understood it well suppose we have a processor with a 32 suppose we have a processor with a 32 suppose we have a processor with a 32 kilobyte cache and a 64 byte block size kilobyte cache and a 64 byte block size kilobyte cache and a 64 byte block size suppose we also have virtual to physical suppose we also have virtual to physical suppose we also have virtual to physical address translation with a 4 kilobyte address translation with a 4 kilobyte address translation with a 4 kilobyte page size the question for you is how page size the question for you is how page size the question for you is how many tlb entries do we need if we want many tlb entries do we need if we want many tlb entries do we need if we want to have similar miss rates in this cache to have similar miss rates in this cache to have similar miss rates in this cache and in our tlb the choices you have are and in our tlb the choices you have are and in our tlb the choices you have are 8 to 512 entries 8 to 512 entries 8 to 512 entries 64 to 32 000 64 to 32 000 64 to 32 000 fewer than 64 entries or more than 256 fewer than 64 entries or more than 256 fewer than 64 entries or more than 256 entries choose the best answer here

## TLB Size Solution Quiz： Part 4
Kind: captions Language: en let's look at the solution to our till let's look at the solution to our till let's look at the solution to our till besides quiz we have a cache that is 32 besides quiz we have a cache that is 32 besides quiz we have a cache that is 32 kilobytes in size and has a 64 byte kilobytes in size and has a 64 byte kilobytes in size and has a 64 byte block size we have a 4 kilobyte page block size we have a 4 kilobyte page block size we have a 4 kilobyte page size so the question is how many TLB size so the question is how many TLB size so the question is how many TLB entries do we need to have a similar hit entries do we need to have a similar hit entries do we need to have a similar hit rate and miss rate to our cache well if rate and miss rate to our cache well if rate and miss rate to our cache well if the processor is just accessing up to 32 the processor is just accessing up to 32 the processor is just accessing up to 32 kilobytes of memory then our TLB needs kilobytes of memory then our TLB needs kilobytes of memory then our TLB needs to cover the same amount of memory that to cover the same amount of memory that to cover the same amount of memory that the cache does and that means we need 8 the cache does and that means we need 8 the cache does and that means we need 8 pages now it looks like this would be pages now it looks like this would be pages now it looks like this would be the best answer however if the processor the best answer however if the processor the best answer however if the processor is not accessing all of the data in each is not accessing all of the data in each is not accessing all of the data in each of the pages then we would need more of the pages then we would need more of the pages then we would need more pages to cover the same amount of memory pages to cover the same amount of memory pages to cover the same amount of memory because in the cache with a 64 byte because in the cache with a 64 byte because in the cache with a 64 byte block size we can fit 512 different block size we can fit 512 different block size we can fit 512 different blocks and they can be spread around the blocks and they can be spread around the blocks and they can be spread around the whole memory in contrast with 8 entries whole memory in contrast with 8 entries whole memory in contrast with 8 entries in the TLB we can only be accessing 8 in the TLB we can only be accessing 8 in the TLB we can only be accessing 8 different pages while these 500 blocks different pages while these 500 blocks different pages while these 500 blocks theoretically could be in 512 different theoretically could be in 512 different theoretically could be in 512 different pages so our TLB in this case needs to pages so our TLB in this case needs to pages so our TLB in this case needs to have up to 512 different pages in order have up to 512 different pages in order have up to 512 different pages in order to have hits for the same data that the to have hits for the same data that the to have hits for the same data that the cache does in reality the number of cache does in reality the number of cache does in reality the number of pages we need for the same hit rate will pages we need for the same hit rate will pages we need for the same hit rate will be somewhere in between these two so be somewhere in between these two so be somewhere in between these two so this is the best answer

## TLB Organization： Part 4
Kind: captions Language: en because tlb is kind of like a cache because tlb is kind of like a cache because tlb is kind of like a cache let's talk about how it is organized in let's talk about how it is organized in let's talk about how it is organized in terms of cache properties terms of cache properties terms of cache properties first first first is it direct map or fully associative or is it direct map or fully associative or is it direct map or fully associative or somewhere in between well it is small so somewhere in between well it is small so somewhere in between well it is small so it is already very fast so it tends to it is already very fast so it tends to it is already very fast so it tends to be fully or highly associative be fully or highly associative be fully or highly associative usually we don't have direct map tlbs usually we don't have direct map tlbs usually we don't have direct map tlbs because that would sacrifice the hit because that would sacrifice the hit because that would sacrifice the hit rate for speed but because it's so small rate for speed but because it's so small rate for speed but because it's so small it's already very fast so we don't it's already very fast so we don't it's already very fast so we don't really need that really need that really need that how about the tlb size how about the tlb size how about the tlb size we want it to hit at least as often as we want it to hit at least as often as we want it to hit at least as often as our cache so we wanted to cover more our cache so we wanted to cover more our cache so we wanted to cover more memory than the cache memory than the cache memory than the cache so we usually want our tlb to be so we usually want our tlb to be so we usually want our tlb to be somewhere about 64 to 512 entries somewhere about 64 to 512 entries somewhere about 64 to 512 entries what if we need more tlb entries in what if we need more tlb entries in what if we need more tlb entries in order to get the hit rate that we want order to get the hit rate that we want order to get the hit rate that we want well you don't want it to be too large well you don't want it to be too large well you don't want it to be too large because then it's gonna become slow because then it's gonna become slow because then it's gonna become slow in that case you want to have a two in that case you want to have a two in that case you want to have a two level tlb the first level or at level level tlb the first level or at level level tlb the first level or at level one tlb will be the small and fast one one tlb will be the small and fast one one tlb will be the small and fast one that hits in a single cycle but if you that hits in a single cycle but if you that hits in a single cycle but if you have a miss there we have a larger level have a miss there we have a larger level have a miss there we have a larger level two tlb two tlb two tlb it has a hit time that is several cycles it has a hit time that is several cycles it has a hit time that is several cycles so it's significantly slower than l1 but so it's significantly slower than l1 but so it's significantly slower than l1 but still way faster than going through still way faster than going through still way faster than going through memory and doing the translation memory and doing the translation memory and doing the translation and it is large it can be several and it is large it can be several and it is large it can be several thousand entries because thousand entries because thousand entries because with several thousand entries we can with several thousand entries we can with several thousand entries we can still support relatively good hit times still support relatively good hit times still support relatively good hit times but not a single cycle time

## TLB Performance Quiz： Part 4
Kind: captions Language: en now that we have seen how tlbs work now that we have seen how tlbs work now that we have seen how tlbs work let's do a quiz on tlb performance let's do a quiz on tlb performance let's do a quiz on tlb performance suppose we have a program with a 1 suppose we have a program with a 1 suppose we have a program with a 1 Megabyte array that it reads one bite at Megabyte array that it reads one bite at Megabyte array that it reads one bite at a time from start to end and this start a time from start to end and this start a time from start to end and this start to end read happens 10 times so 10 times to end read happens 10 times so 10 times to end read happens 10 times so 10 times we sweep through the array reading it we sweep through the array reading it we sweep through the array reading it suppose also that no other memory other suppose also that no other memory other suppose also that no other memory other than this 1 megab array is accessed than this 1 megab array is accessed than this 1 megab array is accessed during this time suppose that the during this time suppose that the during this time suppose that the processor has a 4 kiloby Page size and a processor has a 4 kiloby Page size and a processor has a 4 kiloby Page size and a level one tlb that has 100 28 entries level one tlb that has 100 28 entries level one tlb that has 100 28 entries and the level two tlb with 1024 entries and the level two tlb with 1024 entries and the level two tlb with 1024 entries and the tlbs are initially empty they and the tlbs are initially empty they and the tlbs are initially empty they have no translations cached when we have no translations cached when we have no translations cached when we begin the first read the array here is begin the first read the array here is begin the first read the array here is Page aligned meaning the array begins at Page aligned meaning the array begins at Page aligned meaning the array begins at the beginning of a page and the tlb is a the beginning of a page and the tlb is a the beginning of a page and the tlb is a direct map the question for you is in direct map the question for you is in direct map the question for you is in the level one tlb we have how many hits the level one tlb we have how many hits the level one tlb we have how many hits and how many misses and in the level two and how many misses and in the level two and how many misses and in the level two be how many hits and how many misses

## TLB Performance Solution Quiz： Part 4
Kind: captions Language: en let's discuss the solution to our tlb let's discuss the solution to our tlb let's discuss the solution to our tlb performance quiz we have a one megabyte performance quiz we have a one megabyte performance quiz we have a one megabyte array that we are reading one byte at a array that we are reading one byte at a array that we are reading one byte at a time one megabyte is 2 to the 20th bytes time one megabyte is 2 to the 20th bytes time one megabyte is 2 to the 20th bytes our page is 4 kilobytes our page is 4 kilobytes our page is 4 kilobytes which is 2 to the 12th which is 2 to the 12th which is 2 to the 12th so there are 256 pages in this array now so there are 256 pages in this array now so there are 256 pages in this array now what happens is once you have the first what happens is once you have the first what happens is once you have the first access you will have a level 1 tlb miss access you will have a level 1 tlb miss access you will have a level 1 tlb miss a level 2 tlb miss and the translation a level 2 tlb miss and the translation a level 2 tlb miss and the translation will be generated and placed in both obs will be generated and placed in both obs will be generated and placed in both obs next when we access the second byte we next when we access the second byte we next when we access the second byte we have a tlb hit because that byte is in have a tlb hit because that byte is in have a tlb hit because that byte is in the same page as the previous one so the same page as the previous one so the same page as the previous one so after the first miss after the first miss after the first miss we will have we will have we will have hits for the remainder of the four hits for the remainder of the four hits for the remainder of the four kilobyte page in the first sweep we will kilobyte page in the first sweep we will kilobyte page in the first sweep we will have 256 have 256 have 256 l1 l1 l1 tlb misses because that's how many pages tlb misses because that's how many pages tlb misses because that's how many pages we accessed and we will have 4095 we accessed and we will have 4095 we accessed and we will have 4095 this is one less than four kilobytes this is one less than four kilobytes this is one less than four kilobytes times 256 times 256 times 256 tlb hits at level one after this first tlb hits at level one after this first tlb hits at level one after this first sweep sweep sweep the level one tlb the level one tlb the level one tlb contains the entries for the second half contains the entries for the second half contains the entries for the second half of the array because the array needs 256 of the array because the array needs 256 of the array because the array needs 256 pages we only have 128 entries pages we only have 128 entries pages we only have 128 entries the level 2 tlb however the level 2 tlb however the level 2 tlb however contains the mappings for the entire contains the mappings for the entire contains the mappings for the entire array because it still has enough array because it still has enough array because it still has enough elements to hold them so what's going to elements to hold them so what's going to elements to hold them so what's going to happen after the first sweep happen after the first sweep happen after the first sweep is the level one tlb will continue doing is the level one tlb will continue doing is the level one tlb will continue doing this 256 hits and this many misses each this 256 hits and this many misses each this 256 hits and this many misses each time we sweep but the level 2 tlb will time we sweep but the level 2 tlb will time we sweep but the level 2 tlb will start hitting all the time start hitting all the time start hitting all the time so what we have so what we have so what we have is is is 10 times 256 misses here 10 times 256 misses here 10 times 256 misses here and 10 times and 10 times and 10 times hits here hits here hits here now the question is now the question is now the question is for l2 what do we have we know that we for l2 what do we have we know that we for l2 what do we have we know that we will have 256 misses will have 256 misses will have 256 misses that is because during the first sweep that is because during the first sweep that is because during the first sweep we have no mappings for any of the 256 we have no mappings for any of the 256 we have no mappings for any of the 256 pages pages pages after the first sweep after the first sweep after the first sweep we will have the mapping for all the we will have the mapping for all the we will have the mapping for all the pages so we will only have hits but the pages so we will only have hits but the pages so we will only have hits but the question is how many of them question is how many of them question is how many of them well well well note that we only access the level 2 note that we only access the level 2 note that we only access the level 2 till b if we have a miss in the level 1 till b if we have a miss in the level 1 till b if we have a miss in the level 1 tlb so the number of hits here will be tlb so the number of hits here will be tlb so the number of hits here will be this minus this so for nine sweeps we this minus this so for nine sweeps we this minus this so for nine sweeps we will be having these misses go here in will be having these misses go here in will be having these misses go here in big hits big hits big hits so this is the answer to this quiz

## Virtual Memory Outro： Part 4
Kind: captions Language: en now we know how virtual memory works now we know how virtual memory works now we know how virtual memory works this is important in its own right and this is important in its own right and this is important in its own right and we will also need it for our next lesson we will also need it for our next lesson we will also need it for our next lesson which is about advanced caching which is about advanced caching which is about advanced caching optimizations

## Advanced Caches Intro： Part 4
Kind: captions Language: en in this lesson we will look into more in this lesson we will look into more in this lesson we will look into more advanced topics in caching like advanced topics in caching like advanced topics in caching like multi-level caches and various cache multi-level caches and various cache multi-level caches and various cache optimizations these are really important optimizations these are really important optimizations these are really important to get good overall performance and to get good overall performance and to get good overall performance and energy efficiency

## Improving Cache Performance： Part 4
Kind: captions Language: en so this is a lesson on improving cash so this is a lesson on improving cash so this is a lesson on improving cash performance there are many methods for performance there are many methods for performance there are many methods for improving cash performance but in improving cash performance but in improving cash performance but in general they can be grouped in three general they can be grouped in three general they can be grouped in three categories and these three categories categories and these three categories categories and these three categories have to do with the average memory have to do with the average memory have to do with the average memory access time or a mat which is equal to access time or a mat which is equal to access time or a mat which is equal to hit time plus miss rate times miss hit time plus miss rate times miss hit time plus miss rate times miss penalty of a cache so pretty much if we penalty of a cache so pretty much if we penalty of a cache so pretty much if we always hit we get the hit time if we always hit we get the hit time if we always hit we get the hit time if we miss we get the Miss penalty and how miss we get the Miss penalty and how miss we get the Miss penalty and how often we miss is the Miss rate and the often we miss is the Miss rate and the often we miss is the Miss rate and the average memory access time as seen by average memory access time as seen by average memory access time as seen by the processor is equal to this so the the processor is equal to this so the the processor is equal to this so the methods for improving cache performance methods for improving cache performance methods for improving cache performance can be those that try to reduce the hit can be those that try to reduce the hit can be those that try to reduce the hit time those that try to reduce the Miss time those that try to reduce the Miss time those that try to reduce the Miss rate or trying to reduce the Miss rate or trying to reduce the Miss rate or trying to reduce the Miss penalty all of these would reduce the penalty all of these would reduce the penalty all of these would reduce the average memory access time which average memory access time which average memory access time which improves the overall performance of the improves the overall performance of the improves the overall performance of the processor when it needs to access the processor when it needs to access the processor when it needs to access the cache

## Reduce Hit Time： Part 4
Kind: captions Language: en so let's first look at the methods that so let's first look at the methods that so let's first look at the methods that try to reduce hit time some of the try to reduce hit time some of the try to reduce hit time some of the methods are pretty obvious like reduce methods are pretty obvious like reduce methods are pretty obvious like reduce the cache size that's gonna improve the the cache size that's gonna improve the the cache size that's gonna improve the hit time but this is bad for the miss hit time but this is bad for the miss hit time but this is bad for the miss rate so the overall aim at might not rate so the overall aim at might not rate so the overall aim at might not actually be improved because we are actually be improved because we are actually be improved because we are gonna improve the hit time but we're gonna improve the hit time but we're gonna improve the hit time but we're gonna increase the miss rate and thus gonna increase the miss rate and thus gonna increase the miss rate and thus pay more of those miss penalties that pay more of those miss penalties that pay more of those miss penalties that are quite large so we can do this but we are quite large so we can do this but we are quite large so we can do this but we have to do it carefully to balance the have to do it carefully to balance the have to do it carefully to balance the miss rate and the hit time the next miss rate and the hit time the next miss rate and the hit time the next simple method is reduce the cache simple method is reduce the cache simple method is reduce the cache associativity this is gonna make the associativity this is gonna make the associativity this is gonna make the cache faster because we need to search cache faster because we need to search cache faster because we need to search in only one or few places for a block on in only one or few places for a block on in only one or few places for a block on a hit but this is also bad for miss rate a hit but this is also bad for miss rate a hit but this is also bad for miss rate for similar reasons when we reduce for similar reasons when we reduce for similar reasons when we reduce associativity we are creating more associativity we are creating more associativity we are creating more misses because blocks are gonna start misses because blocks are gonna start misses because blocks are gonna start having conflicts and start kicking each having conflicts and start kicking each having conflicts and start kicking each other out even though they might coexist other out even though they might coexist other out even though they might coexist peacefully in a more associative cache peacefully in a more associative cache peacefully in a more associative cache so the last simple methods for reducing so the last simple methods for reducing so the last simple methods for reducing hit time involve overlapping the cache hit time involve overlapping the cache hit time involve overlapping the cache hit with another hit overlapping the hit with another hit overlapping the hit with another hit overlapping the cache hit with the TLB access optimizing cache hit with the TLB access optimizing cache hit with the TLB access optimizing the lookups so that common cases get the lookups so that common cases get the lookups so that common cases get faster without sacrificing the uncommon faster without sacrificing the uncommon faster without sacrificing the uncommon case too much and maintaining the case too much and maintaining the case too much and maintaining the replacement state in the cache more replacement state in the cache more replacement state in the cache more quickly so on hits we need to update quickly so on hits we need to update quickly so on hits we need to update some state that we will need later for some state that we will need later for some state that we will need later for replacements and we can make this more replacements and we can make this more replacements and we can make this more efficient so we will look at four efficient so we will look at four efficient so we will look at four methods that are trying to achieve these methods that are trying to achieve these methods that are trying to achieve these four things here

## Pipelined Caches： Part 4
Kind: captions Language: en so one way of speeding up hit times is so one way of speeding up hit times is so one way of speeding up hit times is to overlap one hit with another to overlap one hit with another to overlap one hit with another and we can achieve this for example and we can achieve this for example and we can achieve this for example through pipelining the cache if the through pipelining the cache if the through pipelining the cache if the cache takes multiple cycles to access we cache takes multiple cycles to access we cache takes multiple cycles to access we can have a situation where an axis come can have a situation where an axis come can have a situation where an axis come in cycle n and it's a hit and now if in cycle n and it's a hit and now if in cycle n and it's a hit and now if another axis comes in cycle n plus one another axis comes in cycle n plus one another axis comes in cycle n plus one and it would be a hit tool and it would be a hit tool and it would be a hit tool in a non-pipeline cache this second in a non-pipeline cache this second in a non-pipeline cache this second access has to wait until the first access has to wait until the first access has to wait until the first access is done using the cache and it access is done using the cache and it access is done using the cache and it takes multiple cycles to do that so in takes multiple cycles to do that so in takes multiple cycles to do that so in this situation the hit time as seen by this situation the hit time as seen by this situation the hit time as seen by each access is the actual hit time of a each access is the actual hit time of a each access is the actual hit time of a cache cache cache plus the wait time plus the wait time plus the wait time that the access suffers because it that the access suffers because it that the access suffers because it cannot access the cache until the cannot access the cache until the cannot access the cache until the previous one is done and in that previous one is done and in that previous one is done and in that situation pipelining the cache so that situation pipelining the cache so that situation pipelining the cache so that we can send in accesses one after the we can send in accesses one after the we can send in accesses one after the other will improve the overall hit time other will improve the overall hit time other will improve the overall hit time now it may sound straightforward how to now it may sound straightforward how to now it may sound straightforward how to pipeline the cache you just divide it in pipeline the cache you just divide it in pipeline the cache you just divide it in let's say three stages but how do you let's say three stages but how do you let's say three stages but how do you split split split what amounts to basically a read from a what amounts to basically a read from a what amounts to basically a read from a large array because that's really what large array because that's really what large array because that's really what the cache is the cache is the cache is remember that the cache access consists remember that the cache access consists remember that the cache access consists of using the index part of the address of using the index part of the address of using the index part of the address to find the set to find the set to find the set reading out the tags and valid bits that reading out the tags and valid bits that reading out the tags and valid bits that correspond to the blocks in that set correspond to the blocks in that set correspond to the blocks in that set comparing the tags and checking the comparing the tags and checking the comparing the tags and checking the valid bits for each of these to see valid bits for each of these to see valid bits for each of these to see whether it has a hit whether it has a hit whether it has a hit combining these combining these combining these so that we know whether we have an so that we know whether we have an so that we know whether we have an overall hit overall hit overall hit and where in our set once we know where and where in our set once we know where and where in our set once we know where we read out the data and use the offset we read out the data and use the offset we read out the data and use the offset to choose the right part of the large to choose the right part of the large to choose the right part of the large cache block at which point we have the cache block at which point we have the cache block at which point we have the data that the processor wants and our data that the processor wants and our data that the processor wants and our cache access is done so one example of a cache access is done so one example of a cache access is done so one example of a cache pipeline would be to have this cache pipeline would be to have this cache pipeline would be to have this part part part reading out the tags and so on from the reading out the tags and so on from the reading out the tags and so on from the cache array be stage one cache array be stage one cache array be stage one determining the hits determining the hits determining the hits and beginning the data read would be and beginning the data read would be and beginning the data read would be stage two and finishing the actual data stage two and finishing the actual data stage two and finishing the actual data read all the way to getting the data read all the way to getting the data read all the way to getting the data would be stage three so as you can see would be stage three so as you can see would be stage three so as you can see we can pipeline the cache access we can pipeline the cache access we can pipeline the cache access even if we don't know how to actually even if we don't know how to actually even if we don't know how to actually break down break down break down the reading from the cache array the reading from the cache array the reading from the cache array especially if the tags and the valid especially if the tags and the valid especially if the tags and the valid bits are read before we determine the bits are read before we determine the bits are read before we determine the hit and we only read the data part of hit and we only read the data part of hit and we only read the data part of the cache after we determine the hit in the cache after we determine the hit in the cache after we determine the hit in that case just those two can be separate that case just those two can be separate that case just those two can be separate stages usually the actual cache hit time stages usually the actual cache hit time stages usually the actual cache hit time for level one caches will be one two or for level one caches will be one two or for level one caches will be one two or three cycles three cycles three cycles one cycle caches don't need pipelining one cycle caches don't need pipelining one cycle caches don't need pipelining and two and three cycle caches can be and two and three cycle caches can be and two and three cycle caches can be relatively easily pipelined into two or relatively easily pipelined into two or relatively easily pipelined into two or three stages so usually level one caches three stages so usually level one caches three stages so usually level one caches will be pipelined

## TLB And Cache Hit： Part 4
Kind: captions Language: en next hit time is affected by having to next hit time is affected by having to next hit time is affected by having to access the tlb before we access the access the tlb before we access the access the tlb before we access the cache remember that the processor starts cache remember that the processor starts cache remember that the processor starts out with a virtual address we use some out with a virtual address we use some out with a virtual address we use some part of that virtual address to index part of that virtual address to index part of that virtual address to index into the tlb and find the frame number into the tlb and find the frame number into the tlb and find the frame number we then combine that with the page we then combine that with the page we then combine that with the page offset and now that we have the physical offset and now that we have the physical offset and now that we have the physical address we can use it to access our address we can use it to access our address we can use it to access our cache and get the data from it so really cache and get the data from it so really cache and get the data from it so really if the tlb takes one cycle and the cache if the tlb takes one cycle and the cache if the tlb takes one cycle and the cache takes one cycle we need two cycles from takes one cycle we need two cycles from takes one cycle we need two cycles from when the processor gives us the address when the processor gives us the address when the processor gives us the address until we actually get the data because until we actually get the data because until we actually get the data because really the tlb access and the cash really the tlb access and the cash really the tlb access and the cash access have to happen one after the access have to happen one after the access have to happen one after the other a cash that is accessed using a other a cash that is accessed using a other a cash that is accessed using a physical address is called a physically physical address is called a physically physical address is called a physically accessed cache a physical cache and also accessed cache a physical cache and also accessed cache a physical cache and also it is called physically indexed or it is called physically indexed or it is called physically indexed or physically tagged or P PT physically tagged or P PT physically tagged or P PT cash the overall hit latency of that cash the overall hit latency of that cash the overall hit latency of that cash really is the tlb hit latency and cash really is the tlb hit latency and cash really is the tlb hit latency and the cash heat latency

## Virtually Accessed Cache： Part 4
Kind: captions Language: en we can improve the overall hit latency we can improve the overall hit latency we can improve the overall hit latency of the cache using a virtually accessed of the cache using a virtually accessed of the cache using a virtually accessed cache in that case the virtual address cache in that case the virtual address cache in that case the virtual address is what we use to access the cache and is what we use to access the cache and is what we use to access the cache and get the data on a cache miss we would get the data on a cache miss we would get the data on a cache miss we would use the virtual address to tell us what use the virtual address to tell us what use the virtual address to tell us what the physical address is so that we can the physical address is so that we can the physical address is so that we can bring data into our cache but on cache bring data into our cache but on cache bring data into our cache but on cache hits we can get the data without doing hits we can get the data without doing hits we can get the data without doing the TLB access at all so the advantages the TLB access at all so the advantages the TLB access at all so the advantages of this virtually accessed cache over of this virtually accessed cache over of this virtually accessed cache over the physically accessed one are that the the physically accessed one are that the the physically accessed one are that the overall hit time is just the cache hit overall hit time is just the cache hit overall hit time is just the cache hit time with no TLB latency added to it time with no TLB latency added to it time with no TLB latency added to it that's nice and we don't need the TLB that's nice and we don't need the TLB that's nice and we don't need the TLB access on cache hit so we can save access on cache hit so we can save access on cache hit so we can save energy energy energy we like that tool so we have seen that a we like that tool so we have seen that a we like that tool so we have seen that a virtually accessed cache has the hit virtually accessed cache has the hit virtually accessed cache has the hit time that is just the cache hit time time that is just the cache hit time time that is just the cache hit time because we use the virtual address to because we use the virtual address to because we use the virtual address to access the cache and because of that we access the cache and because of that we access the cache and because of that we get the data get the data get the data even without address translation on a even without address translation on a even without address translation on a cache hit so we can only access the TLB cache hit so we can only access the TLB cache hit so we can only access the TLB on a Miss which saves us TLB misses and on a Miss which saves us TLB misses and on a Miss which saves us TLB misses and saves energy which is also nice so it saves energy which is also nice so it saves energy which is also nice so it looks like the virtual access cache looks like the virtual access cache looks like the virtual access cache would win every time so why are we even would win every time so why are we even would win every time so why are we even considering physically access caches considering physically access caches considering physically access caches well first of all the TLB in addition to well first of all the TLB in addition to well first of all the TLB in addition to containing the translation for the containing the translation for the containing the translation for the physical address also contains physical address also contains physical address also contains permissions that we need to tell us permissions that we need to tell us permissions that we need to tell us whether we are allowed to read or write whether we are allowed to read or write whether we are allowed to read or write or execute certain pages so even though or execute certain pages so even though or execute certain pages so even though we don't need a physical address from we don't need a physical address from we don't need a physical address from the TLB in order to get our data we the TLB in order to get our data we the TLB in order to get our data we still need to access that he will be still need to access that he will be still need to access that he will be even on cache hits just to get the even on cache hits just to get the even on cache hits just to get the permissions that tell us whether we are permissions that tell us whether we are permissions that tell us whether we are allowed to read write or execute that allowed to read write or execute that allowed to read write or execute that location so this advantage really location so this advantage really location so this advantage really doesn't exist in real processors a doesn't exist in real processors a doesn't exist in real processors a bigger problem is that the virtual bigger problem is that the virtual bigger problem is that the virtual address is specific to a particular address is specific to a particular address is specific to a particular process so if we were running one process so if we were running one process so if we were running one process and filled the cache with its process and filled the cache with its process and filled the cache with its data once we begin running another data once we begin running another data once we begin running another process that other process will have process that other process will have process that other process will have virtual addresses that might overlap virtual addresses that might overlap virtual addresses that might overlap with the addresses from the previous with the addresses from the previous with the addresses from the previous process but they should be going to process but they should be going to process but they should be going to different data pretty much in the TLB different data pretty much in the TLB different data pretty much in the TLB there would be different translation there would be different translation there would be different translation for that other process and we should be for that other process and we should be for that other process and we should be accessing different actual memory accessing different actual memory accessing different actual memory locations because our cache only knows locations because our cache only knows locations because our cache only knows about virtual addresses in this case we about virtual addresses in this case we about virtual addresses in this case we now need to flush our cache meaning now need to flush our cache meaning now need to flush our cache meaning remove all the data from it every time remove all the data from it every time remove all the data from it every time we do a context switch once we are we do a context switch once we are we do a context switch once we are switching to another process we know switching to another process we know switching to another process we know that virtual addresses are going to map that virtual addresses are going to map that virtual addresses are going to map to different locations so what we have to different locations so what we have to different locations so what we have to do is get rid of everything we have to do is get rid of everything we have to do is get rid of everything we have in the cache this means that we have a in the cache this means that we have a in the cache this means that we have a burst of cache misses every time we burst of cache misses every time we burst of cache misses every time we switch processes processes are switched switch processes processes are switched switch processes processes are switched once every millisecond or so so it once every millisecond or so so it once every millisecond or so so it doesn't sound like a big deal but keep doesn't sound like a big deal but keep doesn't sound like a big deal but keep in mind that the cache can be large so in mind that the cache can be large so in mind that the cache can be large so it takes a lot of misses to bring in the it takes a lot of misses to bring in the it takes a lot of misses to bring in the data into it so this is a disadvantage

## Virtually Indexed Physically Tagged： Part 4
Kind: captions Language: en so we invented a third type of cache so we invented a third type of cache so we invented a third type of cache which is called the virtually indexed which is called the virtually indexed which is called the virtually indexed and physically tagged cache which is and physically tagged cache which is and physically tagged cache which is trying to combine the advantages of the trying to combine the advantages of the trying to combine the advantages of the two types of cache it works like this we two types of cache it works like this we two types of cache it works like this we start with the virtual address we break start with the virtual address we break start with the virtual address we break it down into the cache offset index and it down into the cache offset index and it down into the cache offset index and tag but we don't use the tag we do use tag but we don't use the tag we do use tag but we don't use the tag we do use the index bits from the virtual address the index bits from the virtual address the index bits from the virtual address to find the set that we want in the to find the set that we want in the to find the set that we want in the cache we read the valid bits and the cache we read the valid bits and the cache we read the valid bits and the tags tags tags here but meanwhile we use the page here but meanwhile we use the page here but meanwhile we use the page number from the virtual address to number from the virtual address to number from the virtual address to access the tlb and get the frame number access the tlb and get the frame number access the tlb and get the frame number so now that we got the physical address so now that we got the physical address so now that we got the physical address we perform the tag check using the we perform the tag check using the we perform the tag check using the physical address not the virtual one physical address not the virtual one physical address not the virtual one this is why this is called a virtually this is why this is called a virtually this is why this is called a virtually indexed the index bits come from the indexed the index bits come from the indexed the index bits come from the virtual address the tag bits come from virtual address the tag bits come from virtual address the tag bits come from the physical address cache and from here the physical address cache and from here the physical address cache and from here it proceeds normally we know whether we it proceeds normally we know whether we it proceeds normally we know whether we have a hit and so on so how does this have a hit and so on so how does this have a hit and so on so how does this help note that the cache array and the help note that the cache array and the help note that the cache array and the tlb axis are proceeding in parallel so tlb axis are proceeding in parallel so tlb axis are proceeding in parallel so if the tlb is fast enough and usually it if the tlb is fast enough and usually it if the tlb is fast enough and usually it is because it's small the hit time will is because it's small the hit time will is because it's small the hit time will be equal to the cach hit time it's be equal to the cach hit time it's be equal to the cach hit time it's actually the maximum of the two which is actually the maximum of the two which is actually the maximum of the two which is usually the cach hit time so this is usually the cach hit time so this is usually the cach hit time so this is just like the virtual or virtual index just like the virtual or virtual index just like the virtual or virtual index virtually tag cache and we like that we virtually tag cache and we like that we virtually tag cache and we like that we get the speed advantage of the virtual get the speed advantage of the virtual get the speed advantage of the virtual caches with this type of cache to do we caches with this type of cache to do we caches with this type of cache to do we need to flush on a context switch well need to flush on a context switch well need to flush on a context switch well it turns out no because if we switch the it turns out no because if we switch the it turns out no because if we switch the process so that virtual addresses process so that virtual addresses process so that virtual addresses correspond to different physical correspond to different physical correspond to different physical addresses what we find in the cache will addresses what we find in the cache will addresses what we find in the cache will be checked against the actual physical be checked against the actual physical be checked against the actual physical address so the virtual address in address so the virtual address in address so the virtual address in another process will map maybe to the another process will map maybe to the another process will map maybe to the same set but once it Maps there the same set but once it Maps there the same set but once it Maps there the actual tag is not going to match because actual tag is not going to match because actual tag is not going to match because these two have different tags these two have different tags these two have different tags so this is like the physical cash and is so this is like the physical cash and is so this is like the physical cash and is nice but what about aliasing recall that nice but what about aliasing recall that nice but what about aliasing recall that aliasing happens because multiple aliasing happens because multiple aliasing happens because multiple virtual addresses in the same address virtual addresses in the same address virtual addresses in the same address space might map to the same physical space might map to the same physical space might map to the same physical address but because we do virtual access address but because we do virtual access address but because we do virtual access to the cache these addresses might end to the cache these addresses might end to the cache these addresses might end up in different places in the cache thus up in different places in the cache thus up in different places in the cache thus right to one will not be seen by the right to one will not be seen by the right to one will not be seen by the other it turns out that there are no other it turns out that there are no other it turns out that there are no aliasing problems in the virtually index aliasing problems in the virtually index aliasing problems in the virtually index physically t cach as long as the cach is physically t cach as long as the cach is physically t cach as long as the cach is small enough and it has to do with where small enough and it has to do with where small enough and it has to do with where are the index bits coming from so this are the index bits coming from so this are the index bits coming from so this is very very nice because it basically is very very nice because it basically is very very nice because it basically gives us the correctness the no flush on gives us the correctness the no flush on gives us the correctness the no flush on Contex switch of the physical cashes Contex switch of the physical cashes Contex switch of the physical cashes with the heat time of the virtual caches with the heat time of the virtual caches with the heat time of the virtual caches but let's figure out what do we need to but let's figure out what do we need to but let's figure out what do we need to do in order to not have aliasing

## Aliasing in Virtually Accessed Caches： Part 4
Kind: captions Language: en we have seen that virtually accessed we have seen that virtually accessed we have seen that virtually accessed caches can overlap the latency of the caches can overlap the latency of the caches can overlap the latency of the TLB lookup and the cache look up however TLB lookup and the cache look up however TLB lookup and the cache look up however virtually accessed caches have a big virtually accessed caches have a big virtually accessed caches have a big problem called aliasing the problem of problem called aliasing the problem of problem called aliasing the problem of aliasing occurs because in the virtual aliasing occurs because in the virtual aliasing occurs because in the virtual address space of the application we can address space of the application we can address space of the application we can have one page map to some part of the have one page map to some part of the have one page map to some part of the physical address space for example we physical address space for example we physical address space for example we can use the N map function in Linux and can use the N map function in Linux and can use the N map function in Linux and other operating systems to map part of a other operating systems to map part of a other operating systems to map part of a file so that it appears in a range of file so that it appears in a range of file so that it appears in a range of memory that we can address the problem memory that we can address the problem memory that we can address the problem occurs when another page in our virtual occurs when another page in our virtual occurs when another page in our virtual address space is also mapped to the same address space is also mapped to the same address space is also mapped to the same physical addresses for example when physical addresses for example when physical addresses for example when doing an a map of the same part of the doing an a map of the same part of the doing an a map of the same part of the same file to another address so now what same file to another address so now what same file to another address so now what we have is two virtual addresses will we have is two virtual addresses will we have is two virtual addresses will actually refer to the same physical actually refer to the same physical actually refer to the same physical location to see why this is a problem in location to see why this is a problem in location to see why this is a problem in a virtual access cache let's look at two a virtual access cache let's look at two a virtual access cache let's look at two specific addresses so let's say that a specific addresses so let's say that a specific addresses so let's say that a and B are 1 2 3 4 5 0 0 0 and a b c d e and B are 1 2 3 4 5 0 0 0 and a b c d e and B are 1 2 3 4 5 0 0 0 and a b c d e 0 0 0 and let's say that we have a 64 0 0 0 and let's say that we have a 64 0 0 0 and let's say that we have a 64 kilobyte direct mapped cache with a 16 kilobyte direct mapped cache with a 16 kilobyte direct mapped cache with a 16 byte block size and that is virtually byte block size and that is virtually byte block size and that is virtually accessed so let's say that this is our accessed so let's say that this is our accessed so let's say that this is our 64 kilobyte cache for this cache we have 64 kilobyte cache for this cache we have 64 kilobyte cache for this cache we have a 4 bit offset part of the address a 4 bit offset part of the address a 4 bit offset part of the address because of the 16 byte block size it's a because of the 16 byte block size it's a because of the 16 byte block size it's a 64 kilobyte cache so the next 12 bits 64 kilobyte cache so the next 12 bits 64 kilobyte cache so the next 12 bits are going to be the index into this are going to be the index into this are going to be the index into this cache 64 kilobytes divided by 16 bytes cache 64 kilobytes divided by 16 bytes cache 64 kilobytes divided by 16 bytes is 4 kilo entries so we need 2 to the 12 is 4 kilo entries so we need 2 to the 12 is 4 kilo entries so we need 2 to the 12 index bits to tell us which index do we index bits to tell us which index do we index bits to tell us which index do we have and the rest of the bits in the have and the rest of the bits in the have and the rest of the bits in the addresses attacks so this is how the addresses attacks so this is how the addresses attacks so this is how the aliasing problem occurs let's say that aliasing problem occurs let's say that aliasing problem occurs let's say that the processor writes a value of 16 to a the processor writes a value of 16 to a the processor writes a value of 16 to a it breaks down the address indexes into it breaks down the address indexes into it breaks down the address indexes into the cache using the index of hex 500 the cache using the index of hex 500 the cache using the index of hex 500 let's say that this is a cache miss so let's say that this is a cache miss so let's say that this is a cache miss so we go to memory and from the physical we go to memory and from the physical we go to memory and from the physical location location location where this data is mapping to we fetch a where this data is mapping to we fetch a where this data is mapping to we fetch a value and let's say that this value is 4 value and let's say that this value is 4 value and let's say that this value is 4 once we fetch the cache block we now put once we fetch the cache block we now put once we fetch the cache block we now put 16 there and the new content of the 16 there and the new content of the 16 there and the new content of the cache block is 16 let's say this is a cache block is 16 let's say this is a cache block is 16 let's say this is a write-back cache so this 16 just stays write-back cache so this 16 just stays write-back cache so this 16 just stays there now let's see what happens when we there now let's see what happens when we there now let's see what happens when we try to read B we index into the cache try to read B we index into the cache try to read B we index into the cache using the index for B which is e 0 0 hex using the index for B which is e 0 0 hex using the index for B which is e 0 0 hex and because what we are looking for is and because what we are looking for is and because what we are looking for is not here we go to memory and fetch the not here we go to memory and fetch the not here we go to memory and fetch the value and this maps to the same physical value and this maps to the same physical value and this maps to the same physical memory location so we are going to get 4 memory location so we are going to get 4 memory location so we are going to get 4 again and now we are going to read that again and now we are going to read that again and now we are going to read that note that this problem is not just a note that this problem is not just a note that this problem is not just a problem on the cache misses from now on problem on the cache misses from now on problem on the cache misses from now on whenever we write 2a we will be changing whenever we write 2a we will be changing whenever we write 2a we will be changing this cache block and whenever we read this cache block and whenever we read this cache block and whenever we read from B we will be reading this cache from B we will be reading this cache from B we will be reading this cache block so they never really end up block so they never really end up block so they never really end up sharing the data as they should what sharing the data as they should what sharing the data as they should what should have happened is because a and B should have happened is because a and B should have happened is because a and B are really the same data when we write are really the same data when we write are really the same data when we write to a and read B we should be getting the to a and read B we should be getting the to a and read B we should be getting the same data and this problem results in same data and this problem results in same data and this problem results in incorrect execution whenever we have incorrect execution whenever we have incorrect execution whenever we have such a mapping unfortunately such such a mapping unfortunately such such a mapping unfortunately such mappings are perfectly legal to do in mappings are perfectly legal to do in mappings are perfectly legal to do in most operating systems so virtually most operating systems so virtually most operating systems so virtually accessed caches need additional support accessed caches need additional support accessed caches need additional support for this every time we write to any for this every time we write to any for this every time we write to any virtual location we would have to do a virtual location we would have to do a virtual location we would have to do a check for aliases or different versions check for aliases or different versions check for aliases or different versions of the same physical data in the cache of the same physical data in the cache of the same physical data in the cache and either invalidate them remove them and either invalidate them remove them and either invalidate them remove them from the cache or update them so that from the cache or update them so that from the cache or update them so that they would see the new value and this is they would see the new value and this is they would see the new value and this is really expensive to do any kind of really expensive to do any kind of really expensive to do any kind of defeats the purpose of virtually defeats the purpose of virtually defeats the purpose of virtually accessed caches which are all about the accessed caches which are all about the accessed caches which are all about the latency and not needing to do latency and not needing to do latency and not needing to do translation now when we write here we translation now when we write here we translation now when we write here we would need to do translation in order to would need to do translation in order to would need to do translation in order to check where else do we have the same check where else do we have the same check where else do we have the same happen to summarize this will like happen to summarize this will like happen to summarize this will like virtually accessed caches because they virtually accessed caches because they virtually accessed caches because they allow us to overlap TLB latency with allow us to overlap TLB latency with allow us to overlap TLB latency with cache latency but we cannot use them cache latency but we cannot use them cache latency but we cannot use them because of the aliasing problem that because of the aliasing problem that because of the aliasing problem that seriously complicates their seriously complicates their seriously complicates their implementation

## VIPT Cache Aliasing： Part 4
Kind: captions Language: en so let's look at the problem of alas so let's look at the problem of alas so let's look at the problem of alas sing in the virtually index physically sing in the virtually index physically sing in the virtually index physically tagged caches this is our virtual tagged caches this is our virtual tagged caches this is our virtual address and the way we access the cach address and the way we access the cach address and the way we access the cach is these are the offset bits this would is these are the offset bits this would is these are the offset bits this would be our index bits and this would be the be our index bits and this would be the be our index bits and this would be the tag if we use this to access the cache tag if we use this to access the cache tag if we use this to access the cache in a virtually indexed cache we use the in a virtually indexed cache we use the in a virtually indexed cache we use the index part of the address from the index part of the address from the index part of the address from the virtual address the rest comes from the virtual address the rest comes from the virtual address the rest comes from the physical address but how do we form the physical address but how do we form the physical address but how do we form the physical address well some lower bits of physical address well some lower bits of physical address well some lower bits of the virtual address will be the page the virtual address will be the page the virtual address will be the page offset the rest will be the page number offset the rest will be the page number offset the rest will be the page number and now remember that the page number and now remember that the page number and now remember that the page number gets translated to become the frame gets translated to become the frame gets translated to become the frame number in the physical address while the number in the physical address while the number in the physical address while the page offset just goes and becomes the page offset just goes and becomes the page offset just goes and becomes the least significant bits of the physical least significant bits of the physical least significant bits of the physical address and in a virtually indexed address and in a virtually indexed address and in a virtually indexed physically tagged cache our tag comes physically tagged cache our tag comes physically tagged cache our tag comes from here but our index came from here from here but our index came from here from here but our index came from here so that we could begin the aess quickly so that we could begin the aess quickly so that we could begin the aess quickly the thing to note is that the index bits the thing to note is that the index bits the thing to note is that the index bits are relatively close to the list are relatively close to the list are relatively close to the list significant bit of the virtual address significant bit of the virtual address significant bit of the virtual address and that the page offset has a number of and that the page offset has a number of and that the page offset has a number of bits so for a small cache it can happen bits so for a small cache it can happen bits so for a small cache it can happen that the index bits that we used all that the index bits that we used all that the index bits that we used all come from the page offset which means come from the page offset which means come from the page offset which means they're the same as if we took them from they're the same as if we took them from they're the same as if we took them from the physical address so although we are the physical address so although we are the physical address so although we are indexing using the virtual address in indexing using the virtual address in indexing using the virtual address in reality we are actually using the same reality we are actually using the same reality we are actually using the same index that we would be using if we were index that we would be using if we were index that we would be using if we were using the physical address which means using the physical address which means using the physical address which means there would be no aliasing how is there there would be no aliasing how is there there would be no aliasing how is there no alas syn no alas syn no alas syn well because the virtual pages that have well because the virtual pages that have well because the virtual pages that have different page numbers that map to the different page numbers that map to the different page numbers that map to the same frame number can only differ in the same frame number can only differ in the same frame number can only differ in the page number but they have to have the page number but they have to have the page number but they have to have the same offset for the same data and same offset for the same data and same offset for the same data and because only the offset matters for the because only the offset matters for the because only the offset matters for the index if the index is low enough here index if the index is low enough here index if the index is low enough here what happens is pretty much all of the what happens is pretty much all of the what happens is pretty much all of the data that can possibly be in the same data that can possibly be in the same data that can possibly be in the same place in the physical memory has to be place in the physical memory has to be place in the physical memory has to be in the same set because the index is in the same set because the index is in the same set because the index is determined only from the part that has determined only from the part that has determined only from the part that has to be the same so we have that there is to be the same so we have that there is to be the same so we have that there is no alas sync if all of the index bits no alas sync if all of the index bits no alas sync if all of the index bits that we use came from the page offset that we use came from the page offset that we use came from the page offset because really those are the same index because really those are the same index because really those are the same index bits that would come from the physical bits that would come from the physical bits that would come from the physical address if we had the physical index address if we had the physical index address if we had the physical index cache we like this very much but the cache we like this very much but the cache we like this very much but the cash has to be small enough to do this cash has to be small enough to do this cash has to be small enough to do this so for example if we have a 4 kilobyte so for example if we have a 4 kilobyte so for example if we have a 4 kilobyte page we would have a 12bit page offset page we would have a 12bit page offset page we would have a 12bit page offset if our cache has a 32 byte block it has if our cache has a 32 byte block it has if our cache has a 32 byte block it has a 5bit block offset here that means that a 5bit block offset here that means that a 5bit block offset here that means that the index which still needs to fit in the index which still needs to fit in the index which still needs to fit in the 12 bits of the page offset can only the 12 bits of the page offset can only the 12 bits of the page offset can only have seven bits so we can only have 128 have seven bits so we can only have 128 have seven bits so we can only have 128 sets in this cache which limits its size sets in this cache which limits its size sets in this cache which limits its size if we have more sets than this then some if we have more sets than this then some if we have more sets than this then some of the index bits here come from the of the index bits here come from the of the index bits here come from the page number and that changes when we page number and that changes when we page number and that changes when we have the frame number so it can easily have the frame number so it can easily have the frame number so it can easily be that the differ in these bits here be that the differ in these bits here be that the differ in these bits here end up mapping to the same frame number end up mapping to the same frame number end up mapping to the same frame number which we don't want because that means which we don't want because that means which we don't want because that means that multiple sets in the cache could that multiple sets in the cache could that multiple sets in the cache could have the same actual data which is bad have the same actual data which is bad have the same actual data which is bad because we cannot find it then



---

